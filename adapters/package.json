{
  "name": "@ucil/adapters",
  "version": "0.1.0",
  "private": true,
  "description": "UCIL host adapters — Claude Code, Codex, Aider, Cline, Cursor, Ollama",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "check": "biome check .",
    "format": "biome format --write .",
    "test": "vitest run",
    "vitest": "vitest"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
