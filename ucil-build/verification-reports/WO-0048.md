# Verification Report: WO-0048 — g1-result-fusion

**Verifier session**: vrf-128141fb-5f0a-4ced-a582-e07e00846159
**Branch**: feat/WO-0048-g1-result-fusion
**HEAD**: d450ea540254a2e37dfa9c8188d1e879fc669c4f
**Verified at**: 2026-05-04T23:35:00Z
**Verdict**: **PASS**
**Feature**: P2-W7-F02

## Acceptance criteria

| #  | Criterion                                                                                                    | Result | Notes |
|----|--------------------------------------------------------------------------------------------------------------|--------|-------|
| 01 | `cargo build -p ucil-daemon`                                                                                 | PASS   | finished `dev` profile in 3.04s |
| 02 | `cargo clippy -p ucil-daemon --all-targets -- -D warnings`                                                   | PASS   | finished in 3.95s; doc-markdown delta accounted-for via clippy gate |
| 03 | `cargo test -p ucil-daemon executor::test_g1_result_fusion -- --nocapture`                                   | PASS   | 1 passed; 0 failed; 124 filtered out |
| 04 | `grep -nE '^pub async fn test_g1_result_fusion' crates/ucil-daemon/src/executor.rs` not nested in `mod tests {}` | PASS | line 2391 at column 0; only nested mod (`fake_serena_hover_client` 1895-1957) closes well before line 2391 |
| 05 | Sub-assertion 1 — location merge                                                                             | PASS   | 2 fused entries (3-source merge at (util.rs, 10, 20); 1-source at (util.rs, 30, 35)) |
| 06 | Sub-assertion 2 — field union + `contributing_sources` authority order                                       | PASS   | 4 keys at the merged location; `[Serena, TreeSitter, AstGrep]` |
| 07 | Sub-assertion 3 — authority resolution + `G1Conflict` row                                                    | PASS   | `signature` resolves to Serena's value; one `G1Conflict` row with `winner=Serena, losers=[(AstGrep, "fn foo()")]`; `ast_kind` (single contributor) records no conflict |
| 08 | Sub-assertion 4 — disposition pass-through                                                                   | PASS   | 4 entries, all `Available`, input order, `master_timed_out=false` |
| 09 | `cargo test -p ucil-daemon executor::test_g1_parallel_execution -- --nocapture` regression                   | PASS   | 1 passed; 4.75s |
| 10 | `cargo test -p ucil-daemon plugin_manager::{test_hot_cold_lifecycle, test_manifest_parser, test_lifecycle_state_machine, test_hot_reload, test_circuit_breaker}` (one selector per cargo invocation) | PASS | each invocation: 1 passed |
| 11 | `cargo test -p ucil-daemon --test plugin_manager`                                                            | PASS   | 3 passed |
| 11 | `cargo test -p ucil-daemon --test plugin_manifests` (verifier removes `UCIL_SKIP_EXTERNAL_PLUGIN_TESTS=1`)   | PASS   | 2 passed (`ast_grep_manifest_health_check`, `probe_manifest_health_check`) |
| 12 | `cargo test -p ucil-daemon --test e2e_mcp_stdio --test e2e_mcp_with_kg`                                      | PASS   | 1 + 1 passed |
| 13 | `cargo test --test test_plugin_lifecycle`                                                                    | PASS   | 3 passed |
| 14 | `cargo test --test test_lsp_bridge`                                                                          | PASS   | 5 passed |
| 15 | `cargo test --workspace --no-fail-fast`                                                                      | PASS   | 0 failures across the workspace; the lone `1 ignored` is the pre-existing `crates/ucil-core/tests/fixture::rust_project_loads` slow test on the DEC-0003 ignore-allowlist (NOT introduced by this WO — `git diff main...HEAD` does not touch `crates/ucil-core/**`) |
| 16 | `bash scripts/verify/coverage-gate.sh ucil-daemon 85 75` (manual workaround per WO-0046 lessons)             | PASS   | crate aggregate line=89.76% (floor 85%); `executor.rs` 93.68% (vs. 92.82% baseline cited by WO-0047 verification report — +0.86 pp). Branch unavailable on stable. See `coverage-ucil-daemon.md`. |
| 17 | Stub-scan delta zero                                                                                         | PASS   | HEAD: 0; main: 0; delta: 0 |
| 18 | Allow-list 3-dot diff                                                                                        | PASS   | exactly 4 paths: `crates/ucil-daemon/src/executor.rs`, `crates/ucil-daemon/src/lib.rs`, `scripts/verify/P2-W7-F02.sh`, `ucil-build/work-orders/0048-ready-for-review.md` |
| 19 | No `*.toml` change                                                                                           | PASS   | `git diff --name-only main...HEAD -- '*.toml'` empty |
| 20 | All 6 new symbols re-exported in `lib.rs`                                                                    | PASS   | `fuse_g1`, `G1Conflict`, `G1FusedEntry`, `G1FusedLocation`, `G1FusedOutcome`, `G1FusionEntry` — all on the single `pub use executor::{...}` line at `lib.rs:121`; alphabetical-within-block ordering preserved |
| 21 | Mutation A — fuse_g1 body neutered (runtime-only variant per WO ready-for-review marker, with `#[allow(unreachable_code, clippy::needless_return, clippy::let_unit_value)]` to suppress the `#![deny(warnings)]` cascade) | PASS | stashed → FAIL at `executor.rs:2496` (panic: "(1) location merge: expected 2 fused entries (3 contributors at (util.rs, 10, 20) + 1 at (util.rs, 30, 35)); got 0: entries=[]"); restored → PASS |
| 22 | Mutation B — `G1ToolKind::Serena => 0,` flipped to `=> 3,` via literal sed                                   | PASS   | stashed → FAIL at `executor.rs:2523` (panic: "(2) contributing_sources must be authority-ordered [Serena, TreeSitter, AstGrep]; got [TreeSitter, AstGrep, Serena]"); restored → PASS. Fail-mode is sub-assertion 2, NOT 3 — matches the executor's ready-for-review observation cited in critic report line 416. EITHER panic catches the mutation; both prove `authority_rank` is load-bearing. |
| 23 | `tests/fixtures/**` not modified                                                                             | PASS   | empty diff |
| 24 | `feature-list*.json` not modified by executor                                                                | PASS   | empty diff |
| 25 | Master-plan not modified                                                                                     | PASS   | empty diff |
| 26 | Commit cadence ≥ 3                                                                                           | PASS   | 5 commits on feature branch |
| 27 | Branch up-to-date with `origin`                                                                              | PASS   | `git rev-parse HEAD` = `git rev-parse @{u}` = d450ea5 (verified pre-mutation; coverage report rewrite happens after the flip in this verification commit) |

Verify driver `bash scripts/verify/P2-W7-F02.sh` → `[OK] P2-W7-F02`.

## Mutation checks (reality-check + AC21/AC22)

| Feature   | Mutation | Stashed → fail? | Restored → pass? | Verdict |
|-----------|----------|-----------------|------------------|---------|
| P2-W7-F02 | A: `fuse_g1` body replaced with `let _ = outcome; return G1FusedOutcome::default();` (with `#[allow(unreachable_code, clippy::needless_return, clippy::let_unit_value)]` to suppress the `#![deny(warnings)]` cascade per WO-0046 lessons line 245 / WO-0047 verification report) | yes (panic at `executor.rs:2496` — sub-assertion 1: `expected 2 fused entries; got 0: entries=[]`) | yes (1 passed; 0 failed; 0.00 s) | OK |
| P2-W7-F02 | B: `G1ToolKind::Serena => 0,` flipped to `=> 3,` in `authority_rank` (literal sed) | yes (panic at `executor.rs:2523` — sub-assertion 2: `contributing_sources must be authority-ordered [Serena, TreeSitter, AstGrep]; got [TreeSitter, AstGrep, Serena]`) | yes (1 passed; 0 failed; 0.00 s) | OK |

Note on Mutation B fail-mode: the WO `acceptance` AC22 predicted the
panic would fire at sub-assertion 3 (`fields["signature"] !=
"fn foo() -> i32"`), but the executor's ready-for-review marker (cited
in critic report line 416) observed the panic actually fires earlier at
sub-assertion 2 (`contributing_sources` order — Serena now last instead
of first once its rank is `3 ≥ AstGrep's 2`). EITHER panic catches the
mutation; both prove `authority_rank` is load-bearing. The current
verifier session reproduced the executor's empirical fail-mode. Documented
for the post-mortem.

The repo's auto-runner `scripts/reality-check.sh P2-W7-F02` exits 1
("ZERO tests run with code stashed — module was removed") because the
test selector `executor::test_g1_result_fusion` lives in the same file
as the implementation, so stashing the file removes both the
implementation AND the test. This is the same shape as WO-0047 verifier
encountered for `executor::test_g1_parallel_execution`. Per DEC-0007
(per-WO `cargo-mutants` removed) the pre-baked AC21/AC22 mutations are
the authoritative anti-laziness layer, and both demonstrate the
mutation property cleanly: the test fails at runtime when the
implementation is altered, and passes when restored.

## Stub scan

`grep -nE 'todo!\(\)|unimplemented!\(\)|panic!\(".*not yet|TODO|FIXME|NotImplementedError'`
on every changed Rust file (`crates/ucil-daemon/src/executor.rs`,
`crates/ucil-daemon/src/lib.rs`) returns ZERO matches.

`ast-grep --pattern 'panic!("$_")' --lang rust crates/ucil-daemon/src/executor.rs`
returns 2 matches:

  - `executor.rs:1713` — `other => panic!("expected UnsupportedExtension, got {other:?}")` — pre-existing in WO-0042 baseline; inside `#[cfg(test)]` test code; operator-actionable assertion for an unexpected enum variant in a test fixture; NOT a stub.
  - `executor.rs:2424` — `other => panic!("make_entry: fields argument must be a JSON object, got {other:?}")` — new in this WO; inside `#[cfg(test)]` `make_entry` helper; operator-actionable assertion for a test-helper input bug; NOT a stub.

Neither matches the AC17 `panic!\(".*not yet` pattern. Both are inside
test code (per root CLAUDE.md, `panic!` and `.unwrap()`/`.expect()` are
permitted only inside `#[cfg(test)]`).

`.unwrap()` / `.expect()` outside `#[cfg(test)]`: NONE in the diff.
The `fuse_g1` body uses `?` only on `serde_json::from_value` decode
inside a `match` that logs via `tracing::warn!` and continues, per the
partial-results semantic from master-plan §5.1 line 445.

## Quality gates

| Crate       | Mutation score | Line cov | Branch cov | Verdict |
|-------------|----------------|----------|------------|---------|
| ucil-daemon | n/a (per-WO `cargo-mutants` gate removed by DEC-0007; reality-check + pre-baked AC21/AC22 substitute) | 89.76% (floor 85%) | unavailable on stable | PASS |

`executor.rs` (the file touched by WO-0048) sits at **93.68%** line
coverage in isolation. Coverage gate continues to require the manual
`env -u RUSTC_WRAPPER` workaround (now 11th consecutive WO using this
protocol; standing escalation `20260419-0152-monitor-phase1-gate-red-integration-gaps.md`
remains archived). The `coverage-ucil-daemon.md` file was overwritten
with PASS-via-workaround content (the auto-generated FAIL was due to
the long-standing profraw merge issue, not real coverage shortfall).

## Critic alignment

The critic report (`ucil-build/critic-reports/WO-0048.md`, verdict CLEAN)
flagged ONE soft warning:

  1. Commit `23ad2b0` size — 297 insertions, ~95 LOC above the 200-LOC soft target. Verifier reviewed: bundles the frozen `test_g1_result_fusion` selector (with five sub-assertions + local `TestG1Source` impl + `make_entry` helper) plus the lib.rs re-export line update + WO-0048 preamble paragraph. DEC-0005 module-coherence permits this carve-out (precedent at WO-0046 252 LOC, WO-0047 multi-source 605 LOC). NOT a verifier blocker; informational only.

No blockers. All 8 critic gates (stub detection, mocked-critical-deps,
skipped/ignored tests, weak assertions, hallucinated imports, feature
coverage, commit hygiene, doc + public API) reported CLEAN.

## Features flipped

- P2-W7-F02 → passes=true (commit d450ea5) — flipped via `scripts/flip-feature.sh`

## Notes for the next reader

- `tests/integration/fixture::rust_project_loads` continues to be `#[ignore]`'d per DEC-0003 slow-test allow-list — NOT introduced by this WO.
- Coverage gate manual workaround is contractual until the standing escalation is resolved.
- `fuse_g1`'s `# Examples` rustdoc block is `no_run`-gated, which avoids the need for a real `Vec<Box<dyn G1Source>>` fixture in doctests; the workspace-wide doctest run (AC15) compiled it cleanly.
- 6 new public symbols cleanly re-exported: zero deferred re-export debt — now 7 consecutive cleared (WO-0042 → 0048).
