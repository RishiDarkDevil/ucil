# Verification Report: WO-0029 — LSP fallback server spawner (P1-W5-F07)

**Verifier session**: vrf-83fc31b5-1acd-4b2b-8e96-2f936c68c8d2
**Branch**: feat/WO-0029-lsp-fallback-server-spawner
**Final commit**: 9601547fd1a4d7192e04b374a212e2a08d2bec8e
**Verified at**: 2026-04-18T12:38:18Z
**Verdict**: PASS

## Workflow

Fresh verifier session. `cd`'d into worktree
`/home/rishidarkdevil/Desktop/ucil-wt/WO-0029`, confirmed branch
matches WO's `worktree_branch`, ran `cargo clean` (removed 2961 files,
1.5 GiB), confirmed `git status -s` clean. Rust toolchain matches
`rust-toolchain.toml` (`stable-x86_64-unknown-linux-gnu`).

## Criteria

| # | Criterion | Result | Duration | Notes |
|---|-----------|--------|----------|-------|
| 1 | `cargo nextest run -p ucil-lsp-diagnostics server_sharing::test_fallback_spawn` | PASS | 5.026s | 1/1 passed (27 skipped via selector filter) |
| 2 | `cargo nextest run -p ucil-lsp-diagnostics` | PASS | 5.046s | 28/28 passed (5 new server_sharing tests + 23 pre-existing) |
| 3 | `cargo clippy -p ucil-lsp-diagnostics --all-targets -- -D warnings` | PASS | 2.35s | clippy pedantic + nursery + deny(warnings), no findings |
| 4 | `cargo doc -p ucil-lsp-diagnostics --no-deps` | PASS | 0.75s | rustdoc clean — no broken intra-doc links |
| 5 | `cargo build -p ucil-lsp-diagnostics` | PASS | 2.87s | clean compile from cargo-clean state |
| 6 | `test -f crates/ucil-lsp-diagnostics/src/server_sharing.rs` | PASS | <1s | file present (919 LOC) |
| 7 | `grep -q 'pub mod server_sharing' crates/ucil-lsp-diagnostics/src/lib.rs` | PASS | <1s | wired through `lib.rs:23` + re-exports `:40` |
| 8 | `grep -q 'fn test_fallback_spawn' crates/ucil-lsp-diagnostics/src/server_sharing.rs` | PASS | <1s | resolves at module root (`:689`), not under `mod tests {…}` — frozen-pattern selector discipline upheld per WO-0006/0007/0011/0013 lesson |
| 9 | `! grep -Rn 'std::process::Command' crates/ucil-lsp-diagnostics/src/server_sharing.rs` | PASS | <1s | every spawn uses `tokio::process::Command` |
| 10 | `! grep -RnE 'todo!\\(\|unimplemented!\\(\|#\\[ignore\\]' crates/ucil-lsp-diagnostics/src/server_sharing.rs` | PASS | <1s | no stubs, no skipped tests |

## Workspace sanity

`cargo build --workspace` → clean (no other crate broken by the new
`server_sharing` module / `with_fallback_spawner` constructor).

## Mutation checks

| Feature | Stashed → fail? | Popped → pass? | Verdict |
|---------|-----------------|----------------|---------|
| P1-W5-F07 | yes (0 tests via selector) | yes (28/28) | OK |

`scripts/reality-check.sh P1-W5-F07` identified the 3-file changeset
(`bridge.rs`, `lib.rs`, `server_sharing.rs`), per-file rolled each back
to its introducing commit's parent (`server_sharing.rs` deleted as it
did not exist before `1e5c7d1`; `bridge.rs` and `lib.rs` reverted to
their pre-WO state).

The script's automated verdict was the documented brand-new-module
false-positive (escalation `20260415-1630-reality-check-per-file-rollback.md`;
precedents WO-0014 / WO-0015 / WO-0016 / WO-0023 / WO-0026): with the
new module removed, the nextest selector
`server_sharing::test_fallback_spawn` matches **0 tests** (since the
test itself lives inside `server_sharing.rs` at module root), and the
script's "0 tests = fake-green" heuristic does not distinguish
"selector empty because module is gone" from "weak assertion". The
stashed-state log (`/tmp/reality-stashed.log`) shows:

```
running 0 tests
test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 21 filtered out; finished in 0.00s
```

The 21 filtered-out tests confirm the crate still compiles after
rollback (lib.rs no longer references `server_sharing`; bridge.rs
loses `with_fallback_spawner`); only the selector match is empty —
i.e. the test depends entirely on the new code. After restoration,
the same selector matches and **all 28 tests pass** (verified via the
isolated criterion-2 nextest run above).

This is the correct mutation oracle for a module-introduction WO:
"selector matches 0 tests when module removed → tests depend on real
code." Working tree and upstream remain clean post-restoration
(`git status -s` empty, HEAD = `9601547`).

## Stub scan

No `todo!()`, `unimplemented!()`, `NotImplementedError`, single-`pass`
bodies, `Default::default()` shortcut returns, empty `fn` bodies, or
single-line `None`-returning shortcuts in the three changed source
files. The only `panic!()` in the diff lives at
`server_sharing.rs:731` inside the `#[tokio::test] test_fallback_spawn`
function on an unreachable `LspTransport::DelegatedToSerena` match arm
— a legitimate exhaustiveness guard inside `#[cfg(test)]` code, not a
production stub. No `#[ignore]`, `.skip()`, `xfail`, or
`#[allow(dead_code)]` anywhere in the diff.

## Quality gates

| Crate | Mutation score | Line cov | Branch cov | Verdict |
|-------|----------------|----------|------------|---------|
| ucil-lsp-diagnostics | n/a (DEC-0007 — cargo-mutants skipped per-WO; reality-check + critic stub-scan + coverage gate are authoritative) | 94.06% (floor 85%) | n/a (toolchain limitation — no branch coverage emitted by `cargo llvm-cov` on this stable rustc) | PASS |

Coverage report at
`ucil-build/verification-reports/coverage-ucil-lsp-diagnostics.md`:
1472/1565 lines covered (94.06%); 158/175 functions (90.29%);
2214/2360 regions (93.81%). All comfortably above the 85% line floor;
branch percentage is `n/a` because this rustc version's `llvm-cov`
backend does not emit branch counts (consistent with prior WO reports
on the same crate, e.g. WO-0023, WO-0016).

Per `DEC-0007`, `scripts/verify/mutation-gate.sh` (cargo-mutants) is
not run per-WO; the reality-check (above) + critic stub-scan + coverage
gate are the authoritative anti-laziness layers.

## Forbidden-path audit

`git diff --name-only origin/main..HEAD` returns:

```
crates/ucil-lsp-diagnostics/src/bridge.rs
crates/ucil-lsp-diagnostics/src/lib.rs
crates/ucil-lsp-diagnostics/src/server_sharing.rs
ucil-build/critic-reports/WO-0029.md
ucil-build/work-orders/0029-ready-for-review.md
```

None of the WO's `forbidden_paths` (`feature-list.json`,
`feature-list.schema.json`, master plan, `tests/fixtures/**`,
`scripts/gate/**`, `scripts/flip-feature.sh`, `.githooks/**`, sibling
crate trees `ucil-{daemon,core,treesitter,cli,agents,embeddings}`) are
touched. Scope stays inside `ucil-lsp-diagnostics`.

## Rust-style invariants

- `#![deny(warnings)]` and `#![warn(clippy::all, clippy::pedantic, clippy::nursery)]`
  set in `lib.rs:15-16` — clippy passes with the WO's `-D warnings`.
- No `.unwrap()` / `.expect()` outside `#[cfg(test)]` in the new
  module — `lock_handles` uses `unwrap_or_else(PoisonError::into_inner)`
  to recover from poisoning per the rust-style.md error rule.
- Every IO `.await` wrapped in `tokio::time::timeout`:
  `shutdown_handle` wraps `child.wait()` (`server_sharing.rs:604`,
  `:610`); `reap_idle` uses `tokio::time::sleep` (timer, not IO) and
  `Notify::notified()` (sync primitive, not IO) inside a
  `tokio::select!` (`:634-637`).
- `thiserror::Error` enum `ServerSharingError` with three concrete
  variants; `#[non_exhaustive]` so future variants do not constitute
  a SemVer break.
- All spawns through `tokio::process::Command`; `kill_on_drop(true)`
  set on every `Command` so partial spawn failures and `Drop` paths
  do not leak processes.

## DEC-0008 compliance

`bridge.rs:172` still reads
`pub fn new(serena_managed: bool) -> Self` byte-for-byte unchanged —
`with_fallback_spawner` is the additive constructor mandated by
DEC-0008 §Consequences. No `serena_managed` field semantics were
altered.

## Frozen-pattern test selector discipline (DEC-0005 / WO-0006-0013 lesson)

`server_sharing::test_fallback_spawn` resolves at module root (line
689, preceded by `#[cfg(test)] #[cfg(unix)] #[tokio::test]`), NOT
inside `mod tests { … }`. The supporting tests
(`test_binary_not_found`, `test_partial_spawn_failure_does_not_panic`,
`test_touch_unknown_language_is_noop`,
`test_bridge_with_fallback_spawner_installs_endpoints`,
`test_error_display_renders_context`, `test_shutdown_timeout_is_reasonable`)
live inside `mod tests { … }` per the existing convention for
non-frozen tests — see `bridge.rs` for the same split.

## Features flipped

- P1-W5-F07 → passes=true (commit pending — flip + report commit
  performed below)
