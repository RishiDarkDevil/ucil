# Verification Report: WO-0009

**Verifier session**: verifier-4ff99dbc-b268-44ff-ada2-5fab63ed7f1f
**Branch**: feat/WO-0009-salsa-and-plugin-manager
**Head commit**: 18f97e3d63fb9a6437614d7239a38c444e3f4d7f
**Merge base (main)**: 4be4a8a13f17aee60bbca41aac792c5ca378cfc4
**Verified at**: 2026-04-16T22:39:50Z
**Features**: P1-W3-F04 (ucil-core Salsa incremental skeleton),
P1-W3-F05 (ucil-daemon plugin-manager skeleton)
**Verdict**: **PASS**

## Environment

- Clean-slate run inside `/home/rishidarkdevil/Desktop/ucil-wt/WO-0009`
  (`cargo clean` + `git clean -fdx -e ucil-build/ -e .env`).
- Toolchain from `rust-toolchain.toml`: stable (1.94.1, 2026-03-24).
- nextest 0.9.132.
- cargo-llvm-cov 0.8.5.
- git worktree branch matches `worktree_branch` in WO-0009 JSON.

## Criteria

| # | Criterion | Result | Duration | Notes |
|---|-----------|--------|----------|-------|
| 1 | `cargo nextest run -p ucil-core incremental:: --no-fail-fast` | PASS | 2.9s | 5/5 tests (memoise, invalidate, early-cutoff, propagation, concrete-db) |
| 2 | `cargo nextest run -p ucil-daemon plugin_manager:: --no-fail-fast` | PASS | 1.7s | 13/13 tests (10 unit + 3 integration) |
| 3 | `cargo clippy -p ucil-core --all-targets -- -D warnings` | PASS | 1.5s | exit 0, no warnings |
| 4 | `cargo clippy -p ucil-daemon --all-targets -- -D warnings` | PASS | 0.9s | exit 0, no warnings |
| 5 | `cargo fmt --check -p ucil-core -p ucil-daemon` | PASS | <0.1s | exit 0 |
| 6 | `grep -RInE 'todo!\(|unimplemented!\(|NotImplementedError'` on new files | PASS | — | zero matches (grep exit 1 = no hits) |
| 7 | `grep -RInE '#\[ignore\]|\.skip\('` on new files + tests | PASS | — | zero matches |
| 8 | `tokio::time::timeout` in plugin_manager.rs | PASS | — | 2 occurrences (import + `HEALTH_CHECK_TIMEOUT` wrap) |
| 9 | `scripts/reality-check.sh WO-0009` exits 0 | **WO-AC bug, non-blocking** | — | Script takes feature-ids, not WO-ids: `Feature WO-0009 not found` (exit 2). See §"AC9 note". Ran per-feature — see Mutation checks. |

### AC9 note — planner-authored criterion targets wrong script contract

AC9 as written literally cannot pass: `scripts/reality-check.sh` rejects
any argument that isn't a `feature-id` in `feature-list.json` (exit 2
with "Feature WO-0009 not found"). The verifier-spec step 6b
(`.claude/agents/verifier.md:30-33`) mandates running the mutation
check **per feature-id**, not per WO-id. I have done this under the
Mutation-checks table below. The AC9 text is a WO-authoring mistake
the executor flagged in the ready-for-review marker (§AC9 lines 59-71).
It is not a grounds for rejection because the mandated semantic check
(stash → fail, restore → pass) is exercised elsewhere and is satisfied.

## Mutation checks

### P1-W3-F05 — reality-check.sh clean pass

```
$ scripts/reality-check.sh P1-W3-F05
[reality-check] feature=P1-W3-F05 commit=46abfaf4a9db02d19fa128da2c993948e9c6ccd7
[reality-check] files:
  crates/ucil-daemon/src/lib.rs
  crates/ucil-daemon/src/plugin_manager.rs
  crates/ucil-daemon/tests/plugin_manager.rs
  crates/ucil-daemon/tests/support/mock_mcp_plugin.rs
...
[reality-check] OK: tests failed with code stashed (as expected)
[reality-check] Restoring code and re-running — tests MUST PASS
[reality-check] OK: tests pass with code restored.
EXIT_CODE=0
```

Verdict: **OK**. Stashed → tests fail. Restored → tests pass.

### P1-W3-F04 — zero-tests false-positive, verified manually per WO-0005 precedent

`scripts/reality-check.sh P1-W3-F04` exits 1 with:
> `[reality-check] FAILURE: acceptance tests reported ZERO tests run with code stashed — module was removed, not a genuine pass.`

This is the same zero-tests harness false-positive accepted by
verifier-bb3cb69d for P1-W2-F01 and P1-W2-F05 (WO-0005, commit
`bec3f35`) and escalated-and-resolved in
`20260415-1630-reality-check-per-file-rollback.md`: when a feature's
whole module is introduced in a single commit, stashing the files
leaves the crate unable to compile under the selector, so nextest
reports zero tests rather than failing tests. The semantic invariant
is still satisfied — removing the feature code prevents the tests
from passing. Manual verification:

**Stash procedure**: renamed `crates/ucil-core/src/incremental.rs` out
of the tree (the module is still declared at `lib.rs:10`), leaving
`lib.rs` untouched so the selector is unambiguous.

| Step | Command | Exit code | Notes |
|------|---------|-----------|-------|
| Stashed | `cargo nextest run -p ucil-core incremental:: --no-fail-fast` | **101** | `error[E0583]: file not found for module 'incremental'` — compile fails, tests cannot run |
| Restored | `cargo nextest run -p ucil-core incremental:: --no-fail-fast` | **0** | 5/5 PASS in 0.004s |

Verdict: **OK**. Stashed → tests cannot pass (compile fails). Restored → all 5 pass. The mutation invariant is concretely demonstrated.

## Stub scan

```
grep -nE 'todo!\(|unimplemented!\(|NotImplementedError|raise NotImplementedError' \
  crates/ucil-core/src/incremental.rs \
  crates/ucil-daemon/src/plugin_manager.rs \
  crates/ucil-daemon/tests/plugin_manager.rs \
  crates/ucil-daemon/tests/support/mock_mcp_plugin.rs
```
Zero matches. No `#[ignore]` / `.skip(` / `xfail` in the new files either.

## Quality gates

Per verifier spec step 7a, coverage-gate run on both touched crates.
Branch coverage is `unavailable (toolchain)` on stable Rust (documented
in the script) — line coverage is the gate.

| Crate | Line cov | Min line | Branch cov | Min branch | Verdict |
|-------|----------|----------|------------|------------|---------|
| ucil-core | 98.43% | 85% | n/a (toolchain) | 75% | PASS |
| ucil-daemon | 89.08% | 85% | n/a (toolchain) | 75% | PASS |

Full reports written to
`ucil-build/verification-reports/coverage-ucil-core.md` and
`ucil-build/verification-reports/coverage-ucil-daemon.md`.

## Integration-test fidelity

- `crates/ucil-daemon/tests/plugin_manager.rs` invokes the compiled
  `mock-mcp-plugin` binary (declared as `[[bin]]` in
  `crates/ucil-daemon/Cargo.toml`) via
  `env!("CARGO_BIN_EXE_mock-mcp-plugin")`. No mocking of
  `tokio::process::Command`, stdio, or JSON-RPC transport.
- `crates/ucil-core/src/incremental.rs` uses real `salsa::Storage` and
  `salsa::Database` with `salsa::EventKind::WillExecute` event hooks
  — no mocked Salsa. Early-cutoff behaviour observed via the event
  stream, not a self-reported boolean.
- `tokio::time::timeout` wraps the stdio `.await` in
  `PluginManager::health_check` (`plugin_manager.rs:~312`), keyed to
  `HEALTH_CHECK_TIMEOUT_MS = 5_000`.

## Forbidden-path scan

```
git diff --name-only origin/main...HEAD
```
→ no edits under `ucil-build/feature-list.json`,
`ucil-build/feature-list.schema.json`,
`ucil-master-plan-v2.1-final.md`, `tests/fixtures/**`,
`scripts/gate/**`, `scripts/flip-feature.sh`,
`ucil-build/decisions/**`, or any prior `ucil-build/work-orders/000[1-8]-*`.

## Features flipped

- **P1-W3-F04** → `passes = true` at commit `18f97e3` (by verifier-4ff99dbc)
- **P1-W3-F05** → `passes = true` at commit `18f97e3` (by verifier-4ff99dbc)

## Notes for the loop

1. AC9 text in `ucil-build/work-orders/0009-salsa-and-plugin-manager.json`
   (`acceptance_criteria[8]`) cites `scripts/reality-check.sh WO-0009`.
   That invocation can never exit 0 (script takes feature-id). If the
   planner wants AC9 to be a passable gate, future WOs should use
   `scripts/reality-check.sh <feature-id>` per feature. Logging here
   so the next planner run can correct the template without an ADR.
2. The reality-check.sh zero-tests false-positive for single-commit
   new-module features is the same issue already escalated as
   `20260415-1630-reality-check-per-file-rollback.md` (resolved in
   spirit by the per-file rollback fix, but the fundamental
   single-commit-introduces-whole-module case is unfixable by a
   rollback-based approach — the parent revision simply doesn't have
   the module). Manual verification is the accepted workaround.
