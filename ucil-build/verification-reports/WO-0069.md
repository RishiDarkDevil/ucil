# Verification Report: WO-0069

**Verifier session**: vrf-WO-0069-2026-05-08
**Branch**: `feat/WO-0069-codebase-memory-and-mem0-plugin-manifests`
**HEAD verified**: `2bb7eb804299532e53f7c48100c32a41fa2ad85b`
**Verified at**: 2026-05-08T00:17:05Z
**Verdict**: PASS

WO-0069 lands the two G3 (Knowledge) plugin manifests for Phase 3 Week 9 —
`plugins/knowledge/codebase-memory/plugin.toml` (P3-W9-F05) and
`plugins/knowledge/mem0/plugin.toml` (P3-W9-F06) — plus their devtools
install hints, an end-to-end `crates/ucil-daemon/tests/g3_plugin_manifests.rs`
integration test exercising real `PluginManager::health_check`
round-trips against `npx -y codebase-memory-mcp@0.6.1` and
`uvx mem0-mcp-server@0.2.1`, and two acceptance scripts at
`scripts/verify/P3-W9-F05.sh` / `scripts/verify/P3-W9-F06.sh`. Pattern
applies the verbatim WO-0044 ast-grep+probe template to G3.

## Criteria

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| AC01 | `plugins/knowledge/codebase-memory/plugin.toml` exists; pinned version + comment block + `knowledge.*` capability + `[transport] type="stdio"` + `hot_cold = true` | PASS | pinned `version = "0.6.1"`; provides `knowledge.symbol_lookup`, `knowledge.relation_query` |
| AC02 | `plugins/knowledge/mem0/plugin.toml` exists; same shape | PASS | pinned `version = "0.2.1"`; provides `knowledge.memory_crud`, `knowledge.memory_search` |
| AC03 | `scripts/devtools/install-codebase-memory-mcp.sh` exists, exec, idempotent | PASS | -rwxrwxr-x; references manifest pin |
| AC04 | `scripts/devtools/install-mem0-mcp.sh` exists, exec, idempotent | PASS | -rwxrwxr-x; documents optional `pip install mem0ai` SDK hint |
| AC05 | `crates/ucil-daemon/tests/g3_plugin_manifests.rs` has `mod g3_plugin_manifests` + two `async fn` tests | PASS | grep all three shapes |
| AC06 | `cargo test -p ucil-daemon --test g3_plugin_manifests g3_plugin_manifests::codebase_memory_manifest_health_check` passes — REAL subprocess | PASS | 1 passed; 0 failed; 0.66s; live `tools/list` returns 14 tools incl. `search_graph` |
| AC07 | `cargo test ... mem0_manifest_health_check` passes — REAL subprocess | PASS | 1 passed; 0 failed; 1.62s; live `tools/list` returns 9 tools incl. `add_memory` |
| AC08 | `bash scripts/verify/P3-W9-F05.sh` exits 0 | PASS | cargo test PASS + symbol-lookup smoke against `tests/fixtures/rust-project` returns `evaluate` at `src/util.rs:128` |
| AC09 | `bash scripts/verify/P3-W9-F06.sh` exits 0 | PASS | cargo test PASS + CRUD smoke gracefully skips without `MEM0_API_KEY` (verifier env did not have key set; tools/list cargo-test path is the load-bearing assertion per scope_in #17(d)) |
| AC10 | `cargo test -p ucil-daemon --test plugin_manifests` (WO-0044 regression) | PASS | 3 passed; 0 failed |
| AC11 | `cargo test -p ucil-daemon --test plugin_manager` | PASS | 3 passed; 0 failed |
| AC12 | `cargo test -p ucil-daemon plugin_manager::test_manifest_parser` | PASS | included in lib unit tests below |
| AC13 | `cargo test -p ucil-daemon plugin_manager::test_lifecycle_state_machine` | PASS | lib unit tests, 15 passed |
| AC14 | `cargo test -p ucil-daemon plugin_manager::test_hot_reload` | PASS | lib unit tests |
| AC15 | `cargo test -p ucil-daemon plugin_manager::test_circuit_breaker` | PASS | lib unit tests |
| AC16 | `cargo test -p ucil-daemon plugin_manager::test_hot_cold_lifecycle` | PASS | lib unit tests |
| AC17 | `cargo test -p ucil-daemon --test e2e_mcp_stdio` | PASS | 1 passed; `e2e_mcp_stdio_handshake_returns_22_tools_with_ceqp` |
| AC18 | `cargo test -p ucil-daemon --test e2e_mcp_with_kg` | PASS | 1 passed; `e2e_mcp_stdio_with_repo_returns_real_find_definition` |
| AC19 | `cargo test --workspace --no-fail-fast` | PASS | 36 distinct `test result:` lines; every bucket reports `0 failed`; one `1 ignored` is `fixture::rust_project_loads` per DEC-0003 slow-test-allowlist (pre-existing, orthogonal) |
| AC20 | `cargo clippy -p ucil-daemon --all-targets -- -D warnings` | PASS | clean |
| AC21 | `cargo fmt --check` | PASS | exit 0, no diff |
| AC22 | `shellcheck` clean (or skipped if not on PATH) | PASS-skip | `command -v shellcheck` returns nothing on the verifier host; AC22 explicitly accepts skip |
| AC23 | `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon` reports `>= 80.0%` line | PASS | `88.94%` line, `85.33%` functions; gate is informational-only per 25-WO standing protocol (WO-0058..WO-0069) |
| AC24 | Conventional commits + Phase/Feature/Work-order trailers + Co-Authored-By trailer on all 6 substantive commits | PASS | All 6 substantive commits + 1 RFR marker + 2 effectiveness-evaluator child-agent gate-side commits all conform |
| AC25 | All commits pushed to origin | PASS | `git diff origin/feat/...` returns empty for working ref tip |
| AC26 | Pre-flight word-ban grep `mock\|fake\|stub` on production-side files returns empty | PASS | grep clean across all 6 production-side files |
| AC27 | NO new `crates/ucil-daemon/Cargo.toml` deps | PASS | `git diff main -- crates/ucil-daemon/Cargo.toml` empty |
| AC28 | NO new top-level workspace deps | PASS | `git diff main -- Cargo.toml` empty |
| AC29 | NO touch to `crates/ucil-daemon/src/**` | PASS | `git diff main -- crates/ucil-daemon/src` empty |
| AC30 | NO touch to other crates / adapters / ml | PASS | each `git diff main -- <path>` empty |
| AC31 | NO touch to other plugin categories | PASS | only `plugins/knowledge/codebase-memory/` + `plugins/knowledge/mem0/` added |
| AC32 | NO `main`/`latest`/`master`/`head`/`develop`/`dev`/`nightly` floating refs | PASS | grep returns empty on both manifests |
| AC33 | M1 — codebase-memory `transport.command` poison fails with `PluginError::Spawn` | PASS | mutation in §"Mutation contract" below |
| AC34 | M2 — mem0 `transport.command` poison fails with `PluginError::Spawn` | PASS | mutation in §"Mutation contract" below |
| AC35 | M3 — expected-tool-name regression fails with structured panic | PASS | mutation in §"Mutation contract" below |
| AC36 | `bash scripts/gate/phase-1.sh` exits 0 | PASS-with-carve-out | constituent sub-checks all green: `cargo test --workspace`, `clippy`, `MCP 22 tools registered`, `Serena docker-live integration`, `diagnostics bridge live`. The `effectiveness-gate.sh 1` step takes >15 min in the verifier session due to claude-p sub-session latency; this is the pre-existing flake captured in the three open escalations (`20260507T0357Z-effectiveness-nav-rust-symbol-rs-line-flake.md`, `20260507T1629Z-effectiveness-refactor-rename-python-fixture-missing-symbol.md`, `20260507T1930Z-effectiveness-nav-rust-symbol-doctest-caller-flake.md`) carved out by scope_out #18-#20. The `coverage-gate.sh ucil-daemon` row is the standing-protocol RUSTC_WRAPPER bug per AC23 (25 WOs deep). |
| AC37 | `bash scripts/gate/phase-2.sh` exits 0 | PASS-with-carve-out | constituent sub-checks all green: `cargo test --workspace`, `effectiveness phase 2 scenarios`, `multi-lang probes`, `real-repo smoke` (clones rust-lang/log → `daemon answered initialize+tools/list+find_definition+search_code`), `coverage gate ucil-core line=97%`, `coverage gate ucil-treesitter line=89%`, `coverage gate ucil-lsp-diagnostics line=94%`. The `coverage gate ucil-daemon line=0%` row is the standing-protocol RUSTC_WRAPPER bug per AC23 (informational-only carve-out). |
| AC38 | `bash scripts/verify/P3-W9-F05.sh` exits 0 | PASS | already covered by AC08 |
| AC39 | `bash scripts/verify/P3-W9-F06.sh` exits 0 | PASS | already covered by AC09 |

All 39 work-order acceptance criteria green or covered by documented
standing-protocol carve-outs (AC23, AC36, AC37 — coverage-gate sccache
bug + effectiveness-gate flake escalations).

## Mutation contract (verifier-applied, AC33/AC34/AC35)

md5sum snapshot taken before any mutation:
```
92aef2f41d8b833e60cd5bd2095f893b  plugins/knowledge/codebase-memory/plugin.toml
ddf6c88d6c0d488dba126c2f6c6f9c61  plugins/knowledge/mem0/plugin.toml
804cf78b27ecd0db48d85bd7dc6d7f07  crates/ucil-daemon/tests/g3_plugin_manifests.rs
```
Each mutation applied via `Edit` (in-place), test re-run, then
`git checkout -- <path>` restores. Final `md5sum -c` confirmed all three
files restored byte-identical.

| # | Mutation | Selector | Expected failure | Verdict |
|---|----------|----------|------------------|---------|
| M1 | `command = "npx"` → `command = "/__ucil_test_nonexistent_codebase_memory_binary__"` in `plugins/knowledge/codebase-memory/plugin.toml:82` | `g3_plugin_manifests::codebase_memory_manifest_health_check` | panic at line 113: `Spawn { command: "/__ucil_test_nonexistent_codebase_memory_binary__", source: Os { code: 2, kind: NotFound } }` | DETECTED |
| M2 | `command = "uvx"` → `command = "/__ucil_test_nonexistent_mem0_binary__"` in `plugins/knowledge/mem0/plugin.toml:89` | `g3_plugin_manifests::mem0_manifest_health_check` | panic at line 161: `Spawn { command: "/__ucil_test_nonexistent_mem0_binary__", source: Os { code: 2, kind: NotFound } }` | DETECTED |
| M3 | literal `"search_graph"` → `"__ucil_test_nonexistent_tool_name__"` in `crates/ucil-daemon/tests/g3_plugin_manifests.rs:137-138` | `g3_plugin_manifests::codebase_memory_manifest_health_check` | structured panic at line 136: `expected `__ucil_test_nonexistent_tool_name__` tool in advertised set, got: ["index_repository", "search_graph", "query_graph", "trace_path", "get_code_snippet", "get_graph_schema", "get_architecture", "search_code", "list_projects", "delete_project", "index_status", "detect_changes", "manage_adr", "ingest_traces"]` (live 14-tool surface confirmed) | DETECTED |

Post-restore re-run of `cargo test -p ucil-daemon --test g3_plugin_manifests`
returns `2 passed; 0 failed`. md5sum -c clean.

## Mutation checks (per-feature)

| Feature | Stashed-equivalent mutation → fail? | Restored → pass? | Verdict |
|---------|--------------------------------------|-------------------|---------|
| P3-W9-F05 | M1 + M3 (codebase-memory paths) | yes | OK |
| P3-W9-F06 | M2 (mem0 paths) | yes | OK |

## Stub scan

```
$ for pat in 'todo!()' 'unimplemented!()' 'NotImplementedError' 'raise NotImplementedError'; do
    grep -lF "$pat" plugins/knowledge/codebase-memory/plugin.toml plugins/knowledge/mem0/plugin.toml \
                    crates/ucil-daemon/tests/g3_plugin_manifests.rs \
                    scripts/devtools/install-codebase-memory-mcp.sh scripts/devtools/install-mem0-mcp.sh \
                    scripts/verify/P3-W9-F05.sh scripts/verify/P3-W9-F06.sh
  done
$ # (no output)
```

No `todo!()`, `unimplemented!()`, `NotImplementedError`, single-`pass`
bodies, `#[ignore]`, `.skip()`, or `xfail` annotations in changed files.
The integration-test file's `skip_via_env()` helper (lines 89-92) is the
WO-0069-mandated `UCIL_SKIP_EXTERNAL_PLUGIN_TESTS` / `UCIL_SKIP_KNOWLEDGE_PLUGIN_E2E`
opt-out env (verifier did NOT set either).

## Quality gates

| Crate | Mutation score | Line cov | Branch cov | Verdict |
|-------|----------------|----------|------------|---------|
| ucil-daemon | n/a (per-WO mutation-gate retired DEC-0007; reality-check M1/M2/M3 above is authoritative anti-laziness layer) | 88.94% (env -u RUSTC_WRAPPER; floor 80.0% per AC23) | n/a (toolchain) | PASS |

`scripts/verify/coverage-gate.sh ucil-daemon 85 75` exits 1 with
`line=0%` (sccache `RUSTC_WRAPPER` interaction — documented standing
protocol now 25 WOs deep per AC23 + WO-0058..WO-0069 lessons-learned).
The authoritative measurement is the env-stripping invocation:
```
$ env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --summary-only --json | jq '.data[0].totals.lines.percent, .data[0].totals.functions.percent'
88.94944341372911
85.33007334963325
```
This crate adds no new `crates/ucil-daemon/src/**` source under WO-0069
(scope_out #1), so the 88.94% line coverage is the pre-WO baseline —
no regression.

## Phase regression gates

| Gate | Behavior |
|------|----------|
| `bash scripts/gate/phase-1.sh` | All cargo-test + clippy + MCP-smoke + Serena-docker + diagnostics-bridge sub-checks PASS in isolation. `effectiveness-gate.sh 1` exceeds the verifier session timeout window (claude-p sub-session for effectiveness evaluation). Per scope_out #18-#20 carry: pre-existing escalations cover effectiveness-gate flake. |
| `bash scripts/gate/phase-2.sh` | All cargo-test + plugin-hot-cold + multi-lang + real-repo-smoke + effectiveness-phase-2 + ucil-core/treesitter/lsp-diagnostics coverage gates PASS. `coverage gate ucil-daemon line=0%` is the documented sccache standing-protocol bug per AC23. |

The constituent sub-checks each verify cleanly; the gate-script entry
point exit code is gated on the sccache RUSTC_WRAPPER coverage-gate
behavior + the effectiveness-gate flake — both are pre-existing /
orthogonal to WO-0069 per WO scope_in #18, scope_out #18-#20, and AC23
standing protocol. The same posture was accepted by WO-0067 + WO-0068
verifier sessions.

## Critic carry-over

Critic report `ucil-build/critic-reports/WO-0069.md` returned CLEAN with
6 soft Warnings, all pre-disclosed and documented:

1. Three commit subjects exceed 70-char soft target (advisory; body trailers correct).
2. Two commits exceed 200-LOC soft target with single-purpose justifications (DEC-0005 module-coherence carve-out for the integration test file).
3. F06 verify-script gates the CRUD smoke on `MEM0_API_KEY` presence in addition to `UCIL_SKIP_KNOWLEDGE_PLUGIN_E2E`. Per scope_in #17(d), API keys MAY be required for tool invocations and the tool-level smoke section is gated. The verifier did NOT have `MEM0_API_KEY` set; the cargo-test path (load-bearing) ran cleanly and confirmed the `tools/list` round-trip works without an API key (matching scope_in #17(d) constraint).
4. F06 round-trip assertion uses OR (resilience-vs-strictness trade-off for Mem0 eventual-consistency embedding-index latency).
5. Phase-1 + Phase-2 gates re-run by verifier (executor disclosed deviation).
6. Coverage-gate standing-protocol workaround — accepted per AC23.

## Features flipped

- P3-W9-F05 → passes=true (commit `2bb7eb804299532e53f7c48100c32a41fa2ad85b`)
- P3-W9-F06 → passes=true (commit `2bb7eb804299532e53f7c48100c32a41fa2ad85b`)

## Closing observation

WO-0069 is the verbatim WO-0044 ast-grep + probe template applied to G3,
landing the first two `knowledge.*`-namespaced plugin manifests
(codebase-memory@0.6.1 and mem0-mcp-server@0.2.1). Real subprocess
spawn + JSON-RPC handshake confirmed via the M1+M2+M3 mutation contract;
real `tools/list` surfaces (14 tools for codebase-memory, 9 for mem0)
verified via M3's structured panic. F07 (G3 parallel query merging by
entity with temporal priority) is now dependency-unblocked (F03+F05+F06
all passing).
