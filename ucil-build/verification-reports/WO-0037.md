# Verification Report: WO-0037

**Verifier session**: verifier-3692c7e0-2961-4a2c-a3a5-cf5416a729d0
**Branch**: feat/WO-0037-serena-g1-hover-fusion
**Commit**: 4a5d7211a73524d7615fee0c2d042fb5bc31ea37
**Work order**: `ucil-build/work-orders/0037-serena-g1-hover-fusion.json`
**Feature**: P1-W5-F02 (Serena → G1 structural fusion)
**Verified at**: 2026-04-18T17:49:27Z
**Verdict**: PASS

## Environment

- Clean slate: `cargo clean -p ucil-daemon` (121 files removed). Post-coverage `cargo clean -p ucil-core -p ucil-treesitter -p ucil-cli -p ucil-daemon` forced a fresh instrumented build.
- Rust toolchain: stable (`rust-toolchain.toml` channel=stable), pinned via rustup override.
- Git worktree: `/home/rishidarkdevil/Desktop/ucil-wt/WO-0037` on branch `feat/WO-0037-serena-g1-hover-fusion`, up-to-date with origin.

## Acceptance criteria (work-order)

| # | Criterion | Result | Duration | Notes |
|---|-----------|--------|----------|-------|
| 1 | `cargo nextest run -p ucil-daemon 'executor::test_serena_g1_fusion' --no-fail-fast` | PASS | 1.86s | 1 test run: 1 passed, 114 skipped — the frozen F02 selector. |
| 2 | `cargo nextest run -p ucil-daemon --no-fail-fast` | PASS | 1.02s | 115 tests run: 115 passed, 0 skipped — no regressions in daemon. |
| 3 | `cargo clippy -p ucil-daemon --all-targets -- -D warnings` | PASS | 1.44s | No `^error` lines. |
| 4 | `cargo doc -p ucil-daemon --no-deps` | PASS | 0.95s | No `^error` or `^warning: unresolved` lines — rustdoc clean. |
| 5 | `cargo fmt --check` | PASS | <1s | Exit 0, no diff. |
| 6 | `grep -qE 'pub trait SerenaHoverClient' crates/ucil-daemon/src/executor.rs` | PASS | — | Present at executor.rs:640. |
| 7 | `grep -qE 'pub struct HoverDoc' crates/ucil-daemon/src/executor.rs` | PASS | — | Present at executor.rs:571. |
| 8 | `grep -qE 'pub enum HoverSource' crates/ucil-daemon/src/executor.rs` | PASS | — | Present at executor.rs (non_exhaustive). |
| 9 | `grep -qE 'pub (async )?fn enrich_find_definition' crates/ucil-daemon/src/executor.rs` | PASS | — | Present at executor.rs:748. |
| 10 | `grep -qE 'pub use executor::.*(SerenaHoverClient\|enrich_find_definition)' crates/ucil-daemon/src/lib.rs` | PASS | — | Re-export line present. |
| 11 | `grep -qE 'fn test_serena_g1_fusion' crates/ucil-daemon/src/executor.rs` | PASS | — | Test at module root per DEC-0005. |

## Feature acceptance tests (feature-list.json)

| Feature | Selector | Result | Notes |
|---------|----------|--------|-------|
| P1-W5-F02 | `-p ucil-daemon executor::test_serena_g1_fusion` (cargo_test) | PASS | 1 test, 1 passed — rerun from feature-list acceptance, independent of WO criteria. |

## Mutation checks

The automated `scripts/reality-check.sh P1-W5-F02` reported a false positive because the feature's source is split across three `Feature: P1-W5-F02` commits (`0108679` impl, `7d4d358` test, `c065799` re-exports). The script's per-file rollback baseline rolls each file back to the *newest* commit-that-touched-it's parent; for `executor.rs` that parent (`7d4d358`) still contains the full `enrich_find_definition` body plus the test, so the rollback leaves the feature intact and the test passes — not a fake-green, a harness-script scoping limitation.

To satisfy WO acceptance criterion #8 (the authoritative mutation contract — "git stash the body of `enrich_find_definition` … Scenario A fails"), I ran a manual targeted mutation via a Python line-range rewrite:

```
# Replaced executor.rs:748-772 (the fn body) with:
pub async fn enrich_find_definition<C: SerenaHoverClient + ?Sized>(
    resolution: ucil_core::knowledge_graph::SymbolResolution,
    callers: Vec<Caller>,
    client: Option<&C>,
) -> EnrichedFindDefinition {
    let _ = client;
    EnrichedFindDefinition { resolution, callers, hover: None }
}
```

Then: `cargo nextest run -p ucil-daemon 'executor::test_serena_g1_fusion' --no-fail-fast`.

| Mutation | Result | Assertion |
|----------|--------|-----------|
| Stubbed body (ignore client → `hover: None`) | FAIL (as required) | Scenario A at `executor.rs:1297` — `assertion 'left == right' failed … left: None, right: Some(HoverDoc { markdown: "## foo\n\nReturns bar", source: Serena })`. |
| Restored (`git checkout HEAD -- executor.rs`) | PASS | 1 test run: 1 passed. |

The test is authentic: it fails when the implementation is neutered and passes when restored, exactly as the WO's acceptance-criterion-#8 rubric specifies.

| Feature | Stubbed → fail? | Restored → pass? | Verdict |
|---------|-----------------|------------------|---------|
| P1-W5-F02 | yes (Scenario A assertion) | yes | OK |

## Stub scan

`rg 'todo!\\(\\)\|unimplemented!\\(\\)\|NotImplementedError'` over `crates/ucil-daemon/src/{executor,lib}.rs`: zero matches. `rg '#\\[ignore\\]\|\\.skip\\(\|xfail'` over the same: zero matches. No stubs, no skipped tests.

## Quality gates

### Coverage gate

`scripts/verify/coverage-gate.sh ucil-daemon 85 75` exit 0 after a fresh `cargo clean` forced instrumented rebuild (the first attempt hit a cargo-llvm-cov profraw-merge error because the cached non-instrumented binaries survived `cargo clean -p ucil-daemon`; cleaning the ucil-daemon transitive deps as well produced fresh profraw files and the gate then passed).

| Crate | Line cov | Branch cov | Floor | Verdict |
|-------|----------|------------|-------|---------|
| ucil-daemon | 90.39% (4385/4851 lines) | _unavailable (toolchain — stable Rust branches count=0)_ | 85% line / 75% branch | PASS |

Full JSON summary in `ucil-build/verification-reports/coverage-ucil-daemon.md`:
- functions 87.24% (383/439)
- regions 92.04% (6872/7466)
- instantiations 57.14% (404/707)

Mutation-gate (cargo-mutants) omitted per DEC-0007 (removed from per-WO gating; reality-check + manual mutation + stub-scan + coverage are the anti-laziness layers).

## Scope compliance

Diff `origin/main...HEAD`:

```
Cargo.lock                                      |   1 +
crates/ucil-daemon/Cargo.toml                   |   5 +
crates/ucil-daemon/src/executor.rs              | 487 +++++++++
crates/ucil-daemon/src/lib.rs                   |  16 +-
ucil-build/work-orders/0037-ready-for-review.md |  48 +
```

All paths in the WO's `scope_in`; none touch any entry in `forbidden_paths` (`server.rs`, `plugin_manager.rs`, `lifecycle.rs`, `session_manager.rs`, `session_ttl.rs`, `storage.rs`, `startup.rs`, `priority_queue.rs`, `text_search.rs`, `understand_code.rs`, `watcher.rs`, `main.rs`, `crates/ucil-core/**`, `crates/ucil-lsp-diagnostics/**`, etc.).

## Features flipped

- P1-W5-F02 → passes=true, last_verified_commit=4a5d7211a73524d7615fee0c2d042fb5bc31ea37, last_verified_by=verifier-3692c7e0-2961-4a2c-a3a5-cf5416a729d0.
