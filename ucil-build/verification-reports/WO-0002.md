# Verification Report: WO-0002

**Verifier session**: vrf-20260415-wo0002-b3c4d5e6
**Branch**: feat/WO-0002-ucil-core-foundations
**Verified at**: 2026-04-15T12:00:00Z
**Verdict**: REJECT

## Criteria

| # | Criterion | Result | Duration | Notes |
|---|-----------|--------|----------|-------|
| 1 | `cargo test -p ucil-core types::` | PASS | 0.53s | 7 tests green (roundtrip serde, Clone, PartialEq, Debug for all 7 types) |
| 2 | `cargo test -p ucil-core schema_migration::` | PASS | 0.04s | 5 tests green (stamp_creates_table_and_row, check_returns_ok_after_stamp, check_returns_ok_on_empty_db, check_returns_downgrade_error_for_future_version, stamp_is_idempotent_in_rows) |
| 3 | `cargo test -p ucil-core otel::` | PASS | 0.04s | 1 test green (init_span_shutdown_no_panic) |
| 4 | `cargo clippy -p ucil-core -- -D warnings` | PASS | 0.35s | zero warnings or errors |
| 5 | `cargo fmt -p ucil-core --check` | PASS | 0.10s | no formatting violations |
| 6 | `cargo build --workspace` | PASS | 0.43s | all 7 crates compile (ucil-core, ucil-daemon, ucil-cli, ucil-treesitter, ucil-lsp-diagnostics, ucil-embeddings, ucil-agents) |

## Mutation checks

| Feature | Stashed → fail? | Popped → pass? | Verdict |
|---------|-----------------|----------------|---------|
| P0-W1-F02 | **NO** (tests passed with code stashed — EXIT 1 from reality-check.sh) | N/A | **FAIL** |
| P0-W1-F07 | yes | yes | OK |
| P0-W1-F09 | yes | yes | OK |

### P0-W1-F02 mutation check detail

`scripts/reality-check.sh P0-W1-F02` selected commit `e5ecae2` ("feat(core): wire lib.rs — declare modules
and re-export public symbols") as the most-recent source-changing commit tagged `Feature: P0-W1-F02`.
That commit only touched `crates/ucil-core/src/lib.rs`. When the script reverted `lib.rs` to its
pre-wiring state (removing `pub mod types;`), `cargo test -p ucil-core types::` found **0 matching tests**
(the types module is no longer declared) and exited 0. The reality-check script correctly reported
"FAILURE: acceptance tests PASSED with code stashed — this is a fake-green test" and exited 1.

Root issue: `scripts/reality-check.sh` identifies the implementation file to stash by finding the
most-recent commit with source-file changes tagged `Feature: P0-W1-F02`. The actual types
implementation landed in an **earlier** commit `ea983dd` ("feat(core): implement types.rs — 7 serde
domain types") which is also tagged `Feature: P0-W1-F02`. The script walks backwards only to find
the first commit with source changes; it stops at the lib.rs commit and never reaches types.rs.

The code under test is **not** a stub. The types.rs implementation is complete, the tests are real,
and a manual mutation (removing types.rs content and re-running) would fail correctly. The defect is
in the mutation-check script's commit-selection logic, not in the production code.

## Stub scan

No `todo!()`, `unimplemented!()`, `NotImplementedError`, or single-`pass` bodies in
`crates/ucil-core/src/` (types.rs, schema_migration.rs, otel.rs, lib.rs).

## Features flipped

None — rejection issued. No features flipped.
