# Verification Report: WO-0044

**Verifier session**: vrf-9a2a73a0-4e6e-4deb-a033-6e1ba9ae4a42
**Branch**: `feat/WO-0044-ast-grep-probe-plugin-manifests`
**Tip commit (pre-flip)**: `172c9b5470494729db0f96135492771591edbfad`
**Verified at**: 2026-05-05T01:02:00Z
**Verdict**: **PASS**

## Scope

WO-0044 lands the two external-binary plugin manifests for Phase 2
Week 6:

1. **P2-W6-F05 — ast-grep manifest**: `plugins/structural/ast-grep/plugin.toml`
   pinned to `@notprolands/ast-grep-mcp@1.1.1` (community canonical
   stdio MCP wrapper around ast-grep CLI 0.42.1; no first-party `mcp`
   subcommand on upstream at this pin).
2. **P2-W6-F06 — probe manifest**: `plugins/search/probe/plugin.toml`
   pinned to `@probelabs/probe@0.6.0-rc315` invoking the consolidated
   `probe mcp` subcommand (rebrand from `@buger/probe-mcp@1.0.0`).

Plus the integration test surface, devtools install hints, and
acceptance verify scripts. Zero source-code changes under
`crates/ucil-daemon/src/**` — both manifests exercise existing
`PluginManifest::from_path` + `PluginManager::health_check` /
`health_check_with_timeout` surfaces unchanged.

Diff footprint (`git diff --name-only origin/main...HEAD`):

```
crates/ucil-daemon/tests/plugin_manifests.rs   (NEW, 148 LOC)
plugins/search/probe/plugin.toml               (NEW, 62 LOC)
plugins/structural/ast-grep/plugin.toml        (NEW, 62 LOC)
scripts/devtools/install-ast-grep.sh           (NEW, 40 LOC, mode 100755)
scripts/devtools/install-probe.sh              (NEW, 36 LOC, mode 100755)
scripts/verify/P2-W6-F05.sh                    (REWRITE stub)
scripts/verify/P2-W6-F06.sh                    (REWRITE stub)
ucil-build/work-orders/0044-ready-for-review.md (NEW)
```

No `forbidden_paths` entries touched (35-entry list honoured 1:1).

## Clean-slate environment

```
$ cd /home/rishidarkdevil/Desktop/ucil-wt/WO-0044
$ git rev-parse --abbrev-ref HEAD
feat/WO-0044-ast-grep-probe-plugin-manifests
$ cargo clean
     Removed 3756 files, 3.4GiB total
$ git clean -fdx -e ucil-build/ -e .env -e node_modules
(clean)
$ rustup show active-toolchain
stable-x86_64-unknown-linux-gnu (overridden by rust-toolchain.toml)
$ ast-grep --version    → ast-grep 0.42.1
$ probe   --version    → probe-code 0.6.0
$ npx     --version    → 10.9.7
$ env | grep UCIL_SKIP_EXTERNAL_PLUGIN_TESTS
(unset — verifier env does NOT mask either test)
```

## 31 acceptance_criteria (re-verified from clean slate)

The work-order specifies 31 entries: 13 file/grep sentinels + 14
cargo-test/clippy/fmt sub-checks + 2 verify-script entries + 2 mutation
checks (carved out into a separate section below).

| #  | Criterion | Result | Notes |
|----|-----------|--------|-------|
| 01 | `test -f plugins/structural/ast-grep/plugin.toml` | PASS | |
| 02 | `test -f plugins/search/probe/plugin.toml` | PASS | |
| 03 | `test -x scripts/verify/P2-W6-F05.sh` | PASS | |
| 04 | `test -x scripts/verify/P2-W6-F06.sh` | PASS | |
| 05 | `test -f crates/ucil-daemon/tests/plugin_manifests.rs` | PASS | |
| 06 | `test -x scripts/devtools/install-ast-grep.sh` | PASS | |
| 07 | `test -x scripts/devtools/install-probe.sh` | PASS | |
| 08 | `grep -q '^name = "ast-grep"' plugins/structural/ast-grep/plugin.toml` | PASS | |
| 09 | `grep -q '^name = "probe"' plugins/search/probe/plugin.toml` | PASS | |
| 10 | `grep -q '^type = "stdio"' plugins/structural/ast-grep/plugin.toml` | PASS | |
| 11 | `grep -q '^type = "stdio"' plugins/search/probe/plugin.toml` | PASS | |
| 12 | `! grep -q '"main"' plugins/structural/ast-grep/plugin.toml` | PASS | no moving ref |
| 13 | `! grep -q '"main"' plugins/search/probe/plugin.toml` | PASS | no moving ref |
| 14 | `cargo build -p ucil-daemon --tests --quiet` | PASS | exit 0 from clean cargo |
| 15 | `cargo test -p ucil-daemon --test plugin_manifests plugin_manifests::ast_grep_manifest_health_check` + summary alternation grep | PASS | `1 passed; 0 failed; … finished in 1.99s` |
| 16 | `cargo test -p ucil-daemon --test plugin_manifests plugin_manifests::probe_manifest_health_check` + summary alternation grep | PASS | `1 passed; 0 failed; … finished in 2.65s` |
| 17 | `cargo test -p ucil-daemon --test plugin_manager` + summary grep | PASS | 3 passed (P2-W6-F01 / WO-0009) |
| 18 | `cargo test -p ucil-daemon plugin_manager::test_manifest_parser` + summary grep | PASS | 1 passed (P2-W6-F01 / WO-0042) |
| 19 | `cargo test -p ucil-daemon plugin_manager::test_lifecycle_state_machine` + summary grep | PASS | 1 passed (P2-W6-F02 / WO-0042) |
| 20 | `cargo test -p ucil-daemon plugin_manager::test_hot_reload` + summary grep | PASS | 1 passed (P2-W6-F03 / WO-0043) |
| 21 | `cargo test -p ucil-daemon plugin_manager::test_circuit_breaker` + summary grep | PASS | 1 passed (P2-W6-F04 / WO-0043) |
| 22 | `cargo test -p ucil-daemon plugin_manager::test_hot_cold_lifecycle` + summary grep | PASS | 1 passed (P1-W3-F06) |
| 23 | `cargo test -p ucil-daemon --test e2e_mcp_stdio` + summary grep | PASS | 1 passed (Phase-1 stub-path) |
| 24 | `cargo test -p ucil-daemon --test e2e_mcp_with_kg` + summary grep | PASS | 1 passed (Phase-1 KG-bootstrap) |
| 25 | `cargo test --workspace --no-fail-fast` + `! grep FAILED` | PASS | 34 green summary lines, zero `FAILED` lines |
| 26 | `cargo clippy -p ucil-daemon --all-targets -- -D warnings` + `! grep '^error'` | PASS | 0 errors, 0 warnings |
| 27 | `cargo fmt --check` | PASS | exit 0 |
| 28 | shellcheck (4 scripts) | SKIP | shellcheck not on PATH; same skip-with-log handling as the WO authorises (`or absent — if shellcheck is not on PATH, skip with a clear log line`). All four scripts use `set -euo pipefail` and quoted variables. |
| 29 | `bash scripts/verify/P2-W6-F05.sh` | PASS | `[OK] P2-W6-F05` (ast-grep returned 77506 bytes containing `TaskManager`) |
| 30 | `bash scripts/verify/P2-W6-F06.sh` | PASS | `[OK] P2-W6-F06` (probe search 7382 bytes under 16384 cap; extract 370 bytes containing `fn evaluate`) |

All 30 non-mutation criteria green. Mutation criteria (entries 31-32 in
the WO) reported in the dedicated section below.

## Feature acceptance_tests (frozen feature-list selectors)

| Feature | Selector (frozen) | Result |
|---------|-------------------|--------|
| P2-W6-F05 | `bash scripts/verify/P2-W6-F05.sh` | PASS — `[OK] P2-W6-F05`, exit 0 |
| P2-W6-F06 | `bash scripts/verify/P2-W6-F06.sh` | PASS — `[OK] P2-W6-F06`, exit 0 |

Both verify scripts spawn the manifest's `transport.command` as a real
subprocess via the integration tests (real `npx -y …` → real
JSON-RPC `initialize → notifications/initialized → tools/list`), and
then exercise a focused real-binary smoke against the read-only
fixture projects.

## Mutation checks (pre-baked per WO-0044 acceptance entries 19 + 20)

| # | Mutation | Test | Stashed → fail? | Restored → pass? | Verdict |
|---|----------|------|-----------------|------------------|---------|
| 1 | `plugins/structural/ast-grep/plugin.toml`: `command = "npx"` → `"/__ucil_test_nonexistent_astgrep_binary__"` | `plugin_manifests::ast_grep_manifest_health_check` | **FAILED** — `Spawn { command: "/__ucil_test_nonexistent_astgrep_binary__", source: Os { code: 2, kind: NotFound, message: "No such file or directory" } }` (panic at `tests/plugin_manifests.rs:88:14`) | **PASSED** — `1 passed; 0 failed; … finished in 0.81s` after `git checkout -- plugins/structural/ast-grep/plugin.toml` | OK |
| 2 | `plugins/search/probe/plugin.toml`: `command = "npx"` → `"/__ucil_test_nonexistent_probe_binary__"` | `plugin_manifests::probe_manifest_health_check` | **FAILED** — `Spawn { command: "/__ucil_test_nonexistent_probe_binary__", source: Os { code: 2, kind: NotFound, message: "No such file or directory" } }` (panic at `tests/plugin_manifests.rs:128:14`) | **PASSED** — `1 passed; 0 failed; … finished in 1.15s` after `git checkout -- plugins/search/probe/plugin.toml` | OK |

Both mutations confirm the new manifests' `[transport].command` field
is the **sole** reason the tests reach a real MCP server — the
integration tests are not silently green via mocks. After every
mutation step, `git status --short` was clean before proceeding.

## Stub scan

```
$ ast-grep run --pattern 'todo!()'           --lang rust crates/ucil-daemon/tests/plugin_manifests.rs   → 0 hits
$ ast-grep run --pattern 'unimplemented!()'  --lang rust crates/ucil-daemon/tests/plugin_manifests.rs   → 0 hits
$ ast-grep run --pattern 'NotImplementedError' --lang rust crates/ucil-daemon/tests/plugin_manifests.rs → 0 hits
$ Grep '#[ignore]|.skip(|xfail|it.skip'      crates/ucil-daemon/tests/plugin_manifests.rs               → 0 hits
$ Grep 'TODO|FIXME|XXX|stub'                 scripts/verify/P2-W6-F05.sh scripts/verify/P2-W6-F06.sh    → 0 hits
```

The single `mock` token in the diff is a **prohibition** in the
test-file docstring (`tests/plugin_manifests.rs:10` — "Mocking
`tokio::process::Command`, the spawned MCP server, or the JSON-RPC
dialogue is forbidden"), not actual use of mocks. The runtime
`UCIL_SKIP_EXTERNAL_PLUGIN_TESTS` opt-out is explicitly authorised by
the WO `scope_in` clause and is **not set** in the verifier's
environment.

## Quality gates

One Rust crate touched: `ucil-daemon`.

| Crate       | Line cov | Branch cov | Verdict | Method |
|-------------|----------|------------|---------|--------|
| ucil-daemon | **89.51%** (5299 / 5920, floor 85%, +4.51 pp) | n/a (stable toolchain count=0 sentinel) | **PASS** | documented `env -u RUSTC_WRAPPER cargo llvm-cov` workaround (carried through WO-0039 retry-1 / WO-0040 / WO-0041 / WO-0042 / WO-0043). Tracking escalation: `ucil-build/escalations/20260419-0152-monitor-phase1-gate-red-integration-gaps.md`. |

`crates/ucil-daemon/src/plugin_manager.rs` (the file consumed
read-only by WO-0044's new tests via `PluginManifest::from_path` +
`PluginManager::health_check_with_timeout`) sits at **87.18% line
coverage** — the new integration tests at
`crates/ucil-daemon/tests/plugin_manifests.rs` add fresh exercise of
the spawn → JSON-RPC handshake against two new real subprocess
targets, and both mutation checks confirm the spawn path is
genuinely traversed.

Full per-file detail in
`ucil-build/verification-reports/coverage-ucil-daemon.md`.

Per DEC-0007 the cargo-mutants per-WO mutation-gate has been removed.
Reality-check (replaced by the two pre-baked transport.command
mutations above), critic stub-scan, and coverage gate are the
authoritative anti-laziness layers; this WO clears all three.

### Why `scripts/verify/coverage-gate.sh` exits 1 in this environment

In the verifier's shell, `RUSTC_WRAPPER=sccache` plus subprocess-write
races leave corrupt-header `.profraw` files that the script's prune
step does not catch. The documented `env -u RUSTC_WRAPPER` workaround
(used by every WO-0039 retry-1, WO-0040, WO-0041, WO-0042, WO-0043
verifier) restores the correct numbers — see the dedicated section in
`coverage-ucil-daemon.md` for the full workaround command sequence.

## Critic findings — re-checked

Critic verdict was **CLEAN** (`ucil-build/critic-reports/WO-0044.md`).
The three warnings are all reasonable judgment calls or
explicitly-authorised by the WO `scope_in`:

1. **probe pinned to `0.6.0-rc315`** — immutable npm tag, not a moving
   `latest`. Reproducibility invariant in `.claude/rules/commit-style.md`
   preserved. The leading comment block in
   `plugins/search/probe/plugin.toml:1-30` explicitly justifies the
   pin (upstream has not yet cut a stable 0.6.0; `latest` resolves to
   `0.6.0-rc315`). **Verifier acks — acceptable as-is.**
2. **`health_check_with_timeout` rather than bare `health_check`** —
   the function is `pub` (`crates/ucil-daemon/src/plugin_manager.rs:975`)
   and its rustdoc explicitly recommends a ≥30 s budget for first-run
   `npx -y` package fetches. Semantics identical to `health_check`
   (which delegates with `HEALTH_CHECK_TIMEOUT_MS` = 5 s). The WO's
   intent — real subprocess, real JSON-RPC handshake — is fully
   preserved. **Verifier acks — reasonable judgment call.**
3. **Two commits over the 50-LOC soft target** (148 LOC integration
   test, 111 LOC verify script) — explicitly authorised by the WO
   `scope_in` commit ladder (DEC-0005 module-coherence-commits applies
   to single-coherent-test-file / single-script-file commits).
   **Verifier acks — within ADR-sanctioned envelope.**

## Logging discipline

No `println!` / `eprintln!` introduced in the new test file (`grep -E
'^\+.*(println!|eprintln!)' the diff` → 0 hits). The verify scripts
emit operator-actionable `[INFO]`, `[OK]`, and `[FAIL]` lines per the
WO-0044 lessons-applied convention.

## Protocol summary

| Step | verifier.md spec | Result |
|------|------------------|--------|
| cd into worktree | OK | `/home/rishidarkdevil/Desktop/ucil-wt/WO-0044` |
| confirm branch matches `worktree_branch` | OK | `feat/WO-0044-ast-grep-probe-plugin-manifests` |
| clean slate | OK | `cargo clean` removed 3.4 GiB; `git clean -fdx -e ucil-build/ -e .env -e node_modules` (clean) |
| reinstall toolchain | OK | stable (via `rust-toolchain.toml`) |
| re-run 31 acceptance_criteria | OK | 30 PASS, 1 SKIP (shellcheck absent — authorised), all 31 effective gates green |
| feature acceptance_tests | OK | P2-W6-F05 + P2-W6-F06 PASS |
| reality-check / mutation | OK | both pre-baked transport.command mutations stash → fail with `PluginError::Spawn`; restore → pass |
| stub scan | OK | clean |
| coverage gate | OK via documented workaround | 89.51% line (+4.51 pp above floor) |
| feature flip | this commit | both features flipped to `passes=true` via `scripts/flip-feature.sh` |
| write + commit verification report | this commit | — |

## Features flipped

- P2-W6-F05 → `passes=true`
- P2-W6-F06 → `passes=true`

## Verdict

**PASS**. All 30 non-mutation acceptance_criteria green from a clean
slate (one shellcheck SKIP authorised by the WO). Both pre-baked
`[transport].command` mutation checks confirm the new manifests are
the **sole** reason the integration tests reach a real MCP server.
Stub scan clean. Coverage 89.51% line (+4.51 pp above 85% floor) via
the documented one-shot `env -u RUSTC_WRAPPER` workaround.

Phase-1 + WO-0042 + WO-0043 regression guards
(`test_hot_cold_lifecycle`, `test_manifest_parser`,
`test_lifecycle_state_machine`, `test_hot_reload`,
`test_circuit_breaker`, `--test plugin_manager`, `--test e2e_mcp_stdio`,
`--test e2e_mcp_with_kg`) all remain green.
