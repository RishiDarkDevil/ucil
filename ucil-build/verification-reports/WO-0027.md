# Verification Report: WO-0027 (retry 3)

**Verifier session**: `vrf-52fc1d30-f2f7-4bb5-8fb4-70b186f8eac2`
**Branch**: `feat/WO-0027-watchman-detection-and-backend-selection`
**HEAD verified**: `de5039d3c9409ec9b203e21e540b504e36a048aa`
**Verified at**: 2026-04-18T11:06:50Z
**Verdict**: **REJECT** (escalation trigger #1 — 3rd consecutive rejection)

## Context

The working tree (`crates/ucil-daemon/src/watcher.rs`,
`crates/ucil-daemon/src/lib.rs`, `Cargo.toml`,
`crates/ucil-daemon/Cargo.toml`, `Cargo.lock`) is byte-identical to
retry-1 (`650c854`) and retry-2 (`592c908`). The executor has pushed no
fix between retry 2 and this spin-up. `ucil-build/` paths are the only
diff, carrying prior verifier artifacts.

This is a clean-slate re-verification executed exactly per
`.claude/agents/verifier.md`:

1. `cd` to `/home/rishidarkdevil/Desktop/ucil-wt/WO-0027`
2. Confirmed branch `feat/WO-0027-watchman-detection-and-backend-selection`
3. `cargo clean` + `rm -rf target/` + `git clean -fdx -e ucil-build/ -e .env`
4. Toolchain: `stable-x86_64-unknown-linux-gnu` (matches
   `rust-toolchain.toml`), `cargo 1.94.1`, `rustfmt 1.8.0`
5. Ran each acceptance criterion in isolation from the clean slate
6. Ran the F03 feature's `acceptance_tests` selector and the manual
   mutation check (step 6b)
7. Ran `scripts/verify/coverage-gate.sh ucil-daemon 85 75` — gate exited 1

## Criteria

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| AC1 | `cargo nextest run -p ucil-daemon 'watcher::test_watchman_detection' --no-fail-fast` | PASS | nextest: `1 test run: 1 passed, 61 skipped` in 0.003s |
| AC2 | `cargo nextest run -p ucil-daemon 'watcher::' --no-fail-fast` | PASS | nextest: `13 tests run: 13 passed, 49 skipped` in 2.135s (>= 13 floor) |
| AC3 | `cargo clippy -p ucil-daemon --all-targets -- -D warnings` | PASS | no `^error`, no `^warning:` lines |
| AC4 | `cargo doc -p ucil-daemon --no-deps` | PASS | no `^error`, no `^warning: unresolved` |
| AC5 | `cargo fmt --check` | PASS | exit 0 |
| AC6 | `grep 'pub enum WatcherBackend' watcher.rs` | PASS | present |
| AC7 | `grep 'pub fn detect_watchman' watcher.rs` | PASS | present |
| AC8 | `grep 'pub fn auto_select_backend' watcher.rs` | PASS | present |
| AC9 | `grep 'pub fn new_with_backend' watcher.rs` | PASS | present |
| AC10 | `grep 'pub use watcher::' lib.rs` | PASS | present |

## Feature-list acceptance_tests (P1-W3-F03)

| Selector | Result |
|----------|--------|
| `cargo nextest run -p ucil-daemon watcher::test_watchman_detection` | PASS |

## Manual mutation check (reality-check step 6b)

Per `.claude/agents/verifier.md` step 6b. `scripts/reality-check.sh`
still has the per-file-rollback false-positive documented in
`ucil-build/escalations/20260415-1630-reality-check-per-file-rollback.md`,
so the authoritative signal is the manual two-step.

| Step | Action | Test result |
|------|--------|-------------|
| 1 | Replace `detect_watchman()` body with `None` | `test_watchman_detection` FAILS (`watchman should be detected via fake shim`) |
| 2 | Restore original body | `test_watchman_detection` PASSES |

**Verdict**: mutation guarantee intact.

## Stub scan

No `todo!()`, `unimplemented!()`, `#[ignore]`, `NotImplementedError`,
or `pass`-only bodies in the changed files:

```
$ git diff main..HEAD -- crates/ucil-daemon/src/*.rs | grep -E '^\+' | grep -E 'todo!\(\)|unimplemented!\(\)|#\[ignore\]|NotImplementedError|raise NotImplementedError'
(no matches)
```

## Quality gates

| Crate | Line cov | Branch cov | Verdict |
|-------|----------|------------|---------|
| ucil-daemon | n/a (`cargo test` aborted under coverage instrumentation — 4/59 session_manager panics) | n/a | **FAIL** |

Full per-crate coverage report:
`ucil-build/verification-reports/coverage-ucil-daemon.md`.

### Coverage gate failure (authoritative — same as retry 1 / retry 2)

```
[coverage-gate] Running cargo llvm-cov on 'ucil-daemon' (min_line=85%, min_branch=75%)...
[coverage-gate] FAIL — cargo test under coverage errored for ucil-daemon
```

4 session_manager panics with `Io(NotFound)` when spawning `git` —
the WO-0027 watcher tests' `std::env::set_var("PATH", <empty>)`
leaks into sibling libtest threads. `cargo nextest` (per-test process
isolation) masks this; `cargo test -p ucil-daemon` (which
`cargo llvm-cov` drives) doesn't. See
`ucil-build/rejections/WO-0027.md` for full repro and suggested fix.

## Features flipped

None. `P1-W3-F03` remains `passes=false`; `attempts` incremented
`2 -> 3` via `scripts/flip-feature.sh P1-W3-F03 fail`.

## Escalation

This is the 3rd consecutive verifier rejection on feature P1-W3-F03.
Per root `CLAUDE.md` escalation trigger #1, the outer loop must halt.
Companion escalation filed at
`ucil-build/escalations/20260418-1106-wo-WO-0027-verifier-fail-3x.md`
with the recommended Option 2 fix (additive `detect_watchman_in`
helper via `which::which_in` — no public API break, keeps frozen F03
selector name).
