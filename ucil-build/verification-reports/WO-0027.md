# Verification Report: WO-0027

**Verifier session**: `vrf-3b71d8ae-adc8-4846-a412-c61263b858ef`
**Branch**: `feat/WO-0027-watchman-detection-and-backend-selection`
**HEAD**: `650c854e8534fcd68941498f0b5996a1b3f5710c`
**Verified at**: 2026-04-18T10:47:01Z
**Verdict**: **REJECT**
**Feature**: P1-W3-F03 (`ucil-daemon` Watchman detection + backend selection)

See `ucil-build/rejections/WO-0027.md` for the authoritative failure summary and suggested fix. This report captures the full evidence trail.

## Criteria (WO-0027 acceptance_criteria[])

The WO acceptance regexes at indices 0 and 1 were written for `cargo test`'s summary line (`test result: ok. N passed`) but the run happens through `cargo nextest` (summary shape `N tests run: M passed`). The literal regex fails both tools; recorded "Intent" column to show the behaviour the AC was designed to verify (which passes), alongside the literal regex exit code.

| # | Criterion                                                                              | Literal exit | Intent | Duration | Notes |
|---|----------------------------------------------------------------------------------------|--------------|--------|----------|-------|
| 1 | `cargo nextest run -p ucil-daemon 'watcher::test_watchman_detection' \| grep 'tests run: 1, 1 passed'` | 1 (regex mismatch)  | PASS (1 test run, 1 passed) | 4.1s | planner-AC regex written for wrong runner format |
| 2 | `cargo nextest run -p ucil-daemon 'watcher::' \| grep 'tests run: 13+'` | 1 (regex mismatch) | PASS (13 tests run, 13 passed) | 2.2s | same planner-AC regex defect |
| 3 | `cargo clippy -p ucil-daemon --all-targets -- -D warnings` (no `^error`)               | 0 | PASS | 2.7s | pedantic + nursery clean |
| 4 | `cargo doc -p ucil-daemon --no-deps` (no `^error` / `^warning: unresolved`)            | 0 | PASS | 0.95s | rustdoc landmine avoided |
| 5 | `cargo fmt --check`                                                                    | 0 | PASS | 0.1s | - |
| 6 | `grep -q 'pub enum WatcherBackend' crates/ucil-daemon/src/watcher.rs`                  | 0 | PASS | - | - |
| 7 | `grep -q 'pub fn detect_watchman' crates/ucil-daemon/src/watcher.rs`                   | 0 | PASS | - | - |
| 8 | `grep -q 'pub fn auto_select_backend' crates/ucil-daemon/src/watcher.rs`               | 0 | PASS | - | - |
| 9 | `grep -q 'pub fn new_with_backend' crates/ucil-daemon/src/watcher.rs`                  | 0 | PASS | - | - |
| 10 | `grep -q 'pub use watcher::' crates/ucil-daemon/src/lib.rs`                           | 0 | PASS | - | - |

## Feature acceptance_tests (authoritative spec from feature-list.json)

`P1-W3-F03.acceptance_tests = [{kind: "cargo_test", selector: "-p ucil-daemon watcher::test_watchman_detection"}]`

- `cargo test -p ucil-daemon watcher::test_watchman_detection` → `test result: ok. 1 passed; 0 failed`.
- `cargo nextest run -p ucil-daemon watcher::test_watchman_detection --no-fail-fast` → `1 test run: 1 passed, 61 skipped`.

## Mutation checks

`scripts/reality-check.sh P1-W3-F03` tripped the documented per-file-rollback false positive (same escalation `20260415-1630-reality-check-per-file-rollback.md`, same precedent as WO-0026): the script picks the newest `Feature: P1-W3-F03`-trailed commit per file, which for `watcher.rs` is `b0f7a33e` (the drain-until-match test tweak). The resulting rollback state still contains the new `detect_watchman` implementation and the new test, so the stashed run passes and the script flags it as "fake-green".

Manual two-step performed (precedent: WO-0026 retry 3):

| Step | Action                                                                                                                   | Result |
|------|--------------------------------------------------------------------------------------------------------------------------|--------|
| a    | Back up `watcher.rs`/`lib.rs`; `git show main:…` → overwrite both files with their main-tree content; re-run `cargo nextest run -p ucil-daemon watcher::test_watchman_detection` | **0 tests matched** (`error: no tests to run`). Oracle depends on the new code. ✓ |
| b    | Restore `watcher.rs`/`lib.rs` from backup; apply an in-place body mutation to `detect_watchman()` → `None`; re-run the same selector | **FAIL**: `thread panicked at watcher.rs:1159:40: 'watchman should be detected via fake shim'` ✓ |
| c    | Restore `watcher.rs` from backup; re-run; `git status --porcelain` clean                                                  | **PASS** (1 test run, 1 passed), tree clean ✓ |

| Feature   | Stashed → fail? | Popped → pass? | Verdict |
|-----------|-----------------|----------------|---------|
| P1-W3-F03 | yes (0 tests on main-tree rollback; 1 failed on body mutation) | yes (1 passed on restore) | OK |

## Stub scan

```
$ git diff --name-only main..HEAD
Cargo.lock
Cargo.toml
crates/ucil-daemon/Cargo.toml
crates/ucil-daemon/src/lib.rs
crates/ucil-daemon/src/watcher.rs
ucil-build/critic-reports/WO-0027.md

$ grep -nE 'todo!\(\)|unimplemented!\(\)|NotImplementedError|raise NotImplementedError' crates/ucil-daemon/src/watcher.rs crates/ucil-daemon/src/lib.rs
(no matches)

$ grep -nE '#\[ignore\]|\.skip\(\)|xfail' crates/ucil-daemon/src/watcher.rs crates/ucil-daemon/src/lib.rs
(no matches)
```

No stubs, no ignored/skipped tests, no weak assertions.

## Quality gates

Touched crate (via `git diff --name-only origin/main...HEAD | grep -oE 'crates/[^/]+' | sort -u`):

```
crates/ucil-daemon
```

Per DEC-0007, only the coverage gate runs per WO.

| Crate       | Line cov | Branch cov | Verdict |
|-------------|----------|------------|---------|
| ucil-daemon | **unmeasurable** (`cargo test -p ucil-daemon` fails under llvm-cov — see rejection §Root cause) | n/a | **FAIL** |

Report at `ucil-build/verification-reports/coverage-ucil-daemon.md`.

Repro summary (full detail in rejection): on a fully clean worktree (`cargo clean && rm -rf target/`) the gate exits 1. The underlying `cargo test -p ucil-daemon --lib` fails 5/5 times, each time because `test_watchman_detection` et al. mutate process-wide `PATH` while concurrent `session_manager::tests::*` invoke `git` via `tokio::process::Command`. On the `main` branch (source-identical to pre-WO-0027), the same sequence passes 5/5 and the gate reports `line=89.04%`.

## Features flipped

- **P1-W3-F03** → `passes=false` (no change from pre-verify state); `attempts` incremented `0 → 1`.
- No features flipped to `true` this session.

## Retry pointer

- This is retry 1. The executor's locally-green claim is substantively correct for the nextest path but does not hold under `cargo test` concurrency, which is what the coverage gate drives. Rejection details in `ucil-build/rejections/WO-0027.md`; suggested fix is option 2 there (`which::which_in` + a test-visible PATH override hook) which is the smallest change that keeps the frozen selector and does not touch files in `forbidden_paths`.
