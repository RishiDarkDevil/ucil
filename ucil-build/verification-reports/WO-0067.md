# Verification Report: WO-0067

**Verifier session**: vrf-89f8df59-8a1d-4774-b4a2-c3ecdaba86e5
**Branch**: feat/WO-0067-classifier-and-reason-parser
**HEAD commit verified**: 56f4b2531fe46aecf3765c702b0ac69bc203b782
**Verified at**: 2026-05-07T20:33:00Z
**Verdict**: PASS

WO-0067 lands the deterministic-fallback orchestration brain for Phase 3
Week 9 — two coordinated additions in `ucil-core`:
1. `pub enum QueryType` (10 §6.2-frozen variants), `pub struct
   ClassifierOutput`, `pub fn classify_query(&str, &[&str]) ->
   ClassifierOutput` (3-step precedence ladder: tool_name → keyword →
   default `UnderstandCode`), `pub const fn group_weights_for(QueryType)
   -> [f32; 8]` over `const QUERY_WEIGHT_MATRIX: [[f32; 8]; 10]` (§6.2
   lines 649–658 verbatim), `const TOOL_NAME_MAP` (12 entries), `const
   KEYWORD_RULES` (28 ordered patterns), and `fn
   test_deterministic_classifier` at module root with SA1..SA6.
2. NEW `crates/ucil-core/src/ceqp.rs` — `pub enum Intent` (5 §8.3
   variants), `pub enum PlannedAction` (4 variants), `pub struct
   ParsedReason`, `pub fn parse_reason(&str) -> ParsedReason` (5-step
   pipeline with intent + domains + planned_action + knowledge_gaps
   extraction), and `fn test_reason_parser` at module root with
   SA1..SA8.

Both modules are pure-deterministic — no IO, no async, no logging, no
plugins, no `regex` crate, no new dependencies. Wired through
`crates/ucil-core/src/lib.rs` via `pub mod ceqp;`.

## Acceptance criteria

| #  | Criterion | Result | Notes |
|----|-----------|--------|-------|
| AC01 | `pub enum QueryType` with 10 variants in §6.2 declaration order + frozen derives | PASS | grep + manual review of `crates/ucil-core/src/fusion.rs:374`. |
| AC02 | `pub struct ClassifierOutput` with 4 fields (`query_type`, `intent_hint`, `domain_tags`, `group_weight_overrides`) | PASS | `fusion.rs:426`. |
| AC03 | `pub fn classify_query(&str, &[&str]) -> ClassifierOutput` with the 3-step precedence ladder | PASS | `fusion.rs:606`. |
| AC04 | `pub const fn group_weights_for(QueryType) -> [f32; 8]` | PASS | `fusion.rs:555`. |
| AC05 | `const QUERY_WEIGHT_MATRIX: [[f32; 8]; 10]` rows §6.2 lines 649-658 verbatim | PASS | SA4 + SA5 confirm; M2 mutation regression-tests row alignment. |
| AC06 | `tool_name → QueryType` map covers ≥12 §3.2 tools | PASS | SA1 covers all 12; mapping verified verbatim. |
| AC07 | `pub fn test_deterministic_classifier()` exists, `#[test]`, frozen selector resolves | PASS | `cargo test -p ucil-core fusion::test_deterministic_classifier` runs 1 test. |
| AC08 | Test exercises SA1..SA6 inclusive | PASS | sub-assertions inline-rustdoc-numbered. |
| AC09 | NEW module `crates/ucil-core/src/ceqp.rs` declared via `pub mod ceqp;` | PASS | `lib.rs` line confirmed. |
| AC10 | `pub enum Intent` 5 variants, default `Understand`, snake_case serde | PASS | `ceqp.rs:36`. |
| AC11 | `pub enum PlannedAction` 4 variants, default `Read`, snake_case serde | PASS | `ceqp.rs:65`. |
| AC12 | `pub struct ParsedReason` with 4 fields + frozen derives | PASS | `ceqp.rs:89`. |
| AC13 | `pub fn parse_reason(&str) -> ParsedReason` implements 5-step pipeline | PASS | `ceqp.rs:360`. |
| AC14 | `pub fn test_reason_parser()` exists, `#[test]`, frozen selector resolves | PASS | `cargo test -p ucil-core ceqp::test_reason_parser` runs 1 test. |
| AC15 | Test exercises SA1..SA8 inclusive | PASS | sub-assertions inline-rustdoc-numbered. |
| AC16 | `cargo test -p ucil-core fusion::test_deterministic_classifier` passes from clean | PASS | 1 passed, 0 failed. |
| AC17 | `cargo test -p ucil-core ceqp::test_reason_parser` passes from clean | PASS | 1 passed, 0 failed. |
| AC18 | `cargo clippy --all-targets -p ucil-core -- -D warnings` clean | PASS | Finished `dev` profile with 0 warnings. |
| AC19 | `cargo fmt --all -- --check` clean | PASS | No diff. |
| AC20 | `cargo test --workspace --no-fail-fast` green | PASS | All 30+ test-result lines report `ok`; one pre-existing ignored test (`fixture::rust_project_loads`). |
| AC21 | `bash scripts/gate/phase-1.sh` exits 0 | KNOWN-CARRY (accepted) | sccache/RUSTC_WRAPPER coverage-gate carry-over per scope_out #14 (22nd consecutive WO). Real coverage measured via standing-protocol AC23 reports 97.21% line — well above the 85% floor. |
| AC22 | `bash scripts/gate/phase-2.sh` exits 0 | KNOWN-CARRY (accepted) | Same carry; per-crate coverage real values: ucil-core 97.21%, ucil-embeddings 89.46%. |
| AC23 | Coverage informational: `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-core --summary-only --json | jq '.data[0].totals.lines.percent'` >= 85.0 | PASS | **97.21%** (line); functions 95.60%; regions 95.00%. Branch coverage n/a (Rust LLVM toolchain). |
| AC24 | NEW `scripts/verify/P3-W9-F01.sh` exists, executable, frozen shebang/IFS | PASS | `100755`, `#!/usr/bin/env bash`, `set -euo pipefail`, `IFS=$'\n\t'`. |
| AC25 | Verify script runs frozen-selector + 4 rename-drift greps | PASS | `pub fn classify_query`, `pub fn group_weights_for`, `pub enum QueryType`, `fn test_deterministic_classifier`. |
| AC26 | `bash scripts/verify/P3-W9-F01.sh` exits 0 | PASS | `[OK] P3-W9-F01 deterministic classifier wired and verified`. |
| AC27 | NEW `scripts/verify/P3-W9-F02.sh` with same shebang/IFS | PASS | `100755`, frozen shebang/IFS confirmed. |
| AC28 | Verify script runs frozen-selector + 5 rename-drift greps | PASS | `pub fn parse_reason`, `pub enum Intent`, `pub struct ParsedReason`, `pub mod ceqp` in lib.rs, `fn test_reason_parser`. |
| AC29 | `bash scripts/verify/P3-W9-F02.sh` exits 0 | PASS | `[OK] P3-W9-F02 CEQP reason parser wired and verified`. |
| AC30 | Word-ban: NO LITERAL `mock|fake|stub` in NEW non-`#[cfg(test)]` code | PASS | `grep -inE 'mock|fake|stub' crates/ucil-core/src/{fusion.rs,ceqp.rs}` returns zero matches. |
| AC31 | NO new deps in `crates/ucil-core/Cargo.toml` | PASS | `git diff main -- crates/ucil-core/Cargo.toml` is empty. |
| AC32 | NO `regex` import in `crates/ucil-core/src/` | PASS | `grep -rE '^use regex|extern crate regex' crates/ucil-core/src/` returns zero matches. |
| AC33 | Conventional Commits with required trailers | PASS | All 4 implementation commits + RFR commit carry `Phase: 3`, `Feature: P3-W9-F01`/`F02`, `Work-order: WO-0067`, `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>`. |
| AC34 | All commits pushed to origin | PASS | Worktree status `up to date with origin/feat/WO-0067-classifier-and-reason-parser`. |
| AC35 | Mutation M1 (verifier-applied: bypass tool_name match) → SA1 fails | PASS | See Mutation Checks below. |
| AC36 | Mutation M2 (verifier-applied: swap rows 0/1 in QUERY_WEIGHT_MATRIX) → SA4 fails | PASS | See Mutation Checks below. |
| AC37 | Mutation M3 (verifier-applied: force Understand-first / early-return) → SA1+SA2 fail | PASS | See Mutation Checks below. |

## Mutation checks

Mutations applied via in-place file edit, run, then restored via
`git checkout -- <file>`. After each mutation cycle, the source file
md5sum was re-verified against a `/tmp/<file>-orig.rs` snapshot taken
prior to any mutation.

| Mutation | File | Action | Test result | Expected | Verdict |
|----------|------|--------|-------------|----------|---------|
| M1 | `crates/ucil-core/src/fusion.rs` | Bypass tool_name match — replace the `for &(name, qt) in TOOL_NAME_MAP { if name == tool_name { return ClassifierOutput { query_type: qt, … } } }` body with a no-op iteration that still references TOOL_NAME_MAP + tool_name (to keep `#![deny(warnings)]` happy) | FAIL on SA1: `assertion left == right failed: (SA1) tool_name primary signal: classify_query("find_definition", &[]) must yield FindDefinition; got UnderstandCode` | SA1 fails | OK |
| M2 | `crates/ucil-core/src/fusion.rs` | Swap rows 0 (`understand_code`) and 1 (`find_definition`) in `QUERY_WEIGHT_MATRIX` | FAIL on SA4: `assertion left == right failed: (SA4) UnderstandCode row mismatch — §6.2 line 649; left: [3.0, 1.5, 1.0, 0.5, 0.5, 0.0, 0.5, 0.0]; right: [2.0, 1.0, 2.5, 1.5, 2.0, 0.5, 1.0, 0.5]` | SA4 fails | OK |
| M3 | `crates/ucil-core/src/ceqp.rs` | Prepend `let _ = padded; return Intent::Understand;` to `classify_intent` (forces Understand for all inputs — equivalent to RFR's documented patch) | FAIL on SA1: `assertion left == right failed: (SA1) AddFeature: 'add a feature' phrase must yield AddFeature; left: Understand; right: AddFeature`. (SA2 would also have failed; SA1 fires first via short-circuit panic.) | Test fails (mutation detected) | OK |

After M1/M2/M3, both `crates/ucil-core/src/fusion.rs` and
`crates/ucil-core/src/ceqp.rs` were restored to the pre-mutation
md5sum (d14cf39e05bb4a88c8f0fbe1c9b152f8 / 78318b0a25060f72e5e53473b6af0b8c)
and both frozen-selector tests re-run green:

```
test fusion::test_deterministic_classifier ... ok
test ceqp::test_reason_parser ... ok
```

### Note on `scripts/reality-check.sh`

Independent invocation of `bash scripts/reality-check.sh P3-W9-F01`
produced false-positive failures due to a known harness bug — the
script's unconditional `git stash pop` interacts badly with
pre-existing unrelated stashes (the worktree had two unrelated
auto-stashes from earlier Phase 2 sessions). The popped stash
introduced merge conflicts in `ucil-build/verification-reports/coverage-*.md`
that have nothing to do with WO-0067's source changes. After cleaning
the conflicts via `git checkout HEAD -- …`, both source files were
verified intact (md5sum matched pre-mutation snapshots) and both
frozen tests re-run green.

The authoritative mutation evidence for this WO is the M1/M2/M3
verifier-applied contract above, which directly demonstrates that the
test code is mutation-aware. The reality-check.sh harness bug is
orthogonal to WO-0067 — it pre-dates this WO and affects every
verifier run on this worktree until the unrelated stashes are dropped.

## Stub scan

No `todo!()`, `unimplemented!()`, `panic!()` (outside test panic
strings), or `raise NotImplementedError` substrings in the WO-0067
diff. Verified via `grep -nE 'todo!\(\)|unimplemented!\(\)|panic!\(|raise NotImplementedError'`
on `crates/ucil-core/src/{fusion.rs,ceqp.rs}` → zero matches.

## Word-ban grep (AC30)

`grep -inE 'mock|fake|stub' crates/ucil-core/src/{fusion.rs,ceqp.rs}`
returns ZERO matches in either file (case-insensitive). Both production
and test code are clean of the banned substrings.

## Quality gates

| Crate | Line cov | Branch cov | Functions cov | Verdict |
|-------|----------|------------|---------------|---------|
| ucil-core | 97.21% (floor 85%) | n/a (Rust LLVM toolchain) | 95.60% | PASS |

Coverage gate per `scripts/verify/coverage-gate.sh ucil-core 85 75`
exits 0 with "[coverage-gate] PASS — ucil-core line=97% branch=n/a"
once profile data is regenerated via the standing protocol
`env -u RUSTC_WRAPPER cargo llvm-cov`. The fresh report is committed
to `ucil-build/verification-reports/coverage-ucil-core.md` (PASS,
generated 2026-05-07T20:32:22Z).

Per DEC-0007, `mutation-gate.sh` (cargo-mutants) is no longer part of
per-WO gating; reality-check + critic stub-scan + coverage gate are
the authoritative anti-laziness layers. Coverage gate green; M1/M2/M3
verifier mutations all detected; stub scan + word ban clean.

## Workspace test summary

`cargo test --workspace --no-fail-fast` green:
- ucil-core: 42 unit tests + 7 doctests + 0 lib tests + 7 proptest types + 2 smoke = all green.
- ucil-daemon, ucil-treesitter, ucil-embeddings, ucil-lsp-diagnostics: all green; one pre-existing
  `#[ignore]`d Docker integration test (unrelated to WO-0067).

## Verify scripts

```
$ bash scripts/verify/P3-W9-F01.sh
[INFO] P3-W9-F01: running cargo test fusion::test_deterministic_classifier...
[INFO] P3-W9-F01: shellcheck not on PATH; skipping lint.
[OK] P3-W9-F01 deterministic classifier wired and verified

$ bash scripts/verify/P3-W9-F02.sh
[INFO] P3-W9-F02: running cargo test ceqp::test_reason_parser...
[INFO] P3-W9-F02: shellcheck not on PATH; skipping lint.
[OK] P3-W9-F02 CEQP reason parser wired and verified
```

## Features flipped

- P3-W9-F01 → passes=true (commit 56f4b2531fe46aecf3765c702b0ac69bc203b782)
- P3-W9-F02 → passes=true (commit 56f4b2531fe46aecf3765c702b0ac69bc203b782)
