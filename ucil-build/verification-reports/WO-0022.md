---
work_order: WO-0022
slug: storage-layout-and-crash-recovery
branch: feat/WO-0022-storage-layout-and-crash-recovery
ref: 636a8c95ce497a813e2100b3fbd948b6e359a15c
verifier_session: vrf-WO-0022-20260418
features: [P1-W2-F06, P1-W3-F09]
verdict: PASS
verified_at: 2026-04-18T06:50:00Z
---

# Verification Report: WO-0022 — storage layout + crash-recovery checkpoint

**Verifier session**: vrf-WO-0022-20260418
**Branch**: feat/WO-0022-storage-layout-and-crash-recovery
**Ref**: `636a8c9` (`636a8c95ce497a813e2100b3fbd948b6e359a15c`)
**Verified at**: 2026-04-18T06:50:00Z
**Verdict**: **PASS**

## Scope

Two features:

- **P1-W2-F06** — Two-tier `.ucil/` layout (`shared/` + `branches/<branch>/`).
  Frozen selector: `storage::test_two_tier_layout`.
- **P1-W3-F09** — Crash-recovery via `.ucil/checkpoint.json`.
  Frozen selector: `lifecycle::test_crash_recovery`.

Touched files (git diff --name-only origin/main...HEAD):

- `crates/ucil-daemon/src/lib.rs`
- `crates/ucil-daemon/src/lifecycle.rs`
- `crates/ucil-daemon/src/storage.rs`
- `ucil-build/work-orders/0022-ready-for-review.md`

## Clean-slate rebuild

- `cargo clean -p ucil-daemon` → removed 140 files (301.8 MiB).
- `cargo build -p ucil-daemon` from scratch → compiled clean.
- `cargo build --workspace` → compiled clean (all downstream crates
  retargeted after the daemon rebuild).
- `rustup show active-toolchain` → `stable-x86_64-unknown-linux-gnu`
  (overridden by `rust-toolchain.toml`).

## Criteria

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| 1 | `test -f crates/ucil-daemon/src/storage.rs` | PASS | new module in place |
| 2 | `cargo build --workspace` | PASS | clean workspace build |
| 3 | `cargo nextest run -p ucil-daemon storage::test_two_tier_layout` matches exactly 1 test, 1 passed | PASS | grep-count=1, awk exits 0 |
| 4 | `cargo nextest run -p ucil-daemon lifecycle::test_crash_recovery` matches exactly 1 test, 1 passed | PASS | grep-count=1, awk exits 0 |
| 5 | `cargo nextest run -p ucil-daemon storage::` ≥ 5 tests all pass | PASS | 6 tests ran, 6 passed |
| 6 | `cargo nextest run -p ucil-daemon lifecycle::` ≥ 11 tests all pass | PASS | 14 tests ran (10 existing PidFile/Shutdown/Lifecycle + 4 new Checkpoint), 14 passed |
| 7 | `cargo clippy -p ucil-daemon --all-targets -- -D warnings` | PASS | zero warnings (pedantic + nursery regime still clean) |
| 8 | `cargo doc -p ucil-daemon --no-deps` | PASS | no `warning`/`error` lines emitted |
| 9 | `! grep -rnE 'todo!\|unimplemented!\|#\[ignore\]' storage.rs lifecycle.rs` | PASS | zero matches |
| 10 | `! grep -qE '^\s*mod\s+tests\s*\{' storage.rs` | PASS | tests are flat module-root, not wrapped |
| 11 | `! grep -qE '^\s*mod\s+tests\s*\{' lifecycle.rs` | PASS | tests are flat module-root, not wrapped |
| 12 | `grep -q 'pub struct StorageLayout' storage.rs` | PASS | declared |
| 13 | `grep -q 'pub enum StorageError' storage.rs` | PASS | declared |
| 14 | `grep -q 'pub struct Checkpoint' lifecycle.rs` | PASS | declared |
| 15 | `grep -q 'pub enum CheckpointError' lifecycle.rs` | PASS | declared |
| 16 | required test functions (storage) | PASS | `test_two_tier_layout`, `test_branch_name_with_slashes_is_sanitised`, `test_empty_branch_rejected`, `test_init_is_idempotent` all present |
| 17 | required test functions (lifecycle) | PASS | `test_crash_recovery`, `test_checkpoint_write_then_read_roundtrip`, `test_checkpoint_read_missing_returns_none`, `test_checkpoint_read_malformed_returns_parse_error` all present |
| 18 | `pub mod storage` in lib.rs | PASS | declared |
| 19 | `pub use storage::...StorageLayout` in lib.rs | PASS | single-line re-export |
| 20 | `pub use lifecycle::...Checkpoint` in lib.rs | PASS | extended re-export |
| 21 | forbidden-path diffs (8 paths) | PASS | all 0 lines |
| 22 | `bash scripts/reality-check.sh P1-W2-F06` | PASS | stashed → tests fail; popped → tests pass |
| 23 | `bash scripts/reality-check.sh P1-W3-F09` | PASS | stashed → tests fail; popped → tests pass |

## Frozen-selector test runs

```
$ cargo nextest run -p ucil-daemon storage::test_two_tier_layout --status-level=pass --hide-progress-bar
Starting 1 test across 4 binaries (48 tests skipped)
    PASS [   0.002s] ucil-daemon storage::test_two_tier_layout
 Summary [   0.003s] 1 test run: 1 passed, 48 skipped

$ cargo nextest run -p ucil-daemon lifecycle::test_crash_recovery --status-level=pass --hide-progress-bar
Starting 1 test across 4 binaries (48 tests skipped)
    PASS [   0.003s] ucil-daemon lifecycle::test_crash_recovery
 Summary [   0.003s] 1 test run: 1 passed, 48 skipped
```

## storage:: suite (6 passed, 0 failed)

```
PASS  storage::test_storage_error_io_display_mentions_path
PASS  storage::test_two_tier_layout
PASS  storage::test_empty_branch_rejected
PASS  storage::test_init_is_idempotent
PASS  storage::test_branch_name_with_slashes_is_sanitised
PASS  storage::test_shared_paths_are_branch_independent
```

## lifecycle:: suite (14 passed, 0 failed)

```
PASS  lifecycle::test_pid_file_drop_removes_file
PASS  lifecycle::test_shutdown_reason_debug_is_readable
PASS  lifecycle::test_checkpoint_read_missing_returns_none
PASS  lifecycle::test_pid_file_write_creates_file_with_current_pid
PASS  lifecycle::test_checkpoint_read_malformed_returns_parse_error
PASS  lifecycle::test_crash_recovery
PASS  lifecycle::test_pid_file_error_io_display
PASS  lifecycle::test_pid_file_read_returns_written_pid
PASS  lifecycle::test_pid_file_error_stale_display
PASS  lifecycle::test_pid_file_double_write_is_idempotent_or_errors_cleanly
PASS  lifecycle::test_checkpoint_write_then_read_roundtrip
PASS  lifecycle::test_lifecycle_pid_file_path_exposes_owned_path
PASS  lifecycle::test_pid_file_read_rejects_non_numeric_contents
PASS  lifecycle::test_pid_file_path_accessor
```

## Mutation checks (reality-check oracle)

| Feature | Stashed → failed? | Popped → passed? | Verdict |
|---------|-------------------|------------------|---------|
| P1-W2-F06 | yes | yes | **OK** |
| P1-W3-F09 | yes | yes | **OK** |

`scripts/reality-check.sh P1-W2-F06` log:

```
[reality-check] feature=P1-W2-F06 commit=861dbcdd609d4bcc6292c3d5ac95a5674d1812de
[reality-check] files:
  crates/ucil-daemon/src/lib.rs
  crates/ucil-daemon/src/storage.rs
[reality-check] Running acceptance tests with code stashed — they MUST FAIL
[reality-check] OK: tests failed with code stashed (as expected)
[reality-check] Restoring code and re-running — tests MUST PASS
[reality-check] OK: tests pass with code restored.
```

`scripts/reality-check.sh P1-W3-F09` log:

```
[reality-check] feature=P1-W3-F09 commit=861dbcdd609d4bcc6292c3d5ac95a5674d1812de
[reality-check] files:
  crates/ucil-daemon/src/lib.rs
  crates/ucil-daemon/src/lifecycle.rs
[reality-check] Running acceptance tests with code stashed — they MUST FAIL
[reality-check] OK: tests failed with code stashed (as expected)
[reality-check] Restoring code and re-running — tests MUST PASS
[reality-check] OK: tests pass with code restored.
```

## Stub scan

No `todo!()`, `unimplemented!()`, `#[ignore]`, `NotImplementedError`,
single-`pass` bodies, `.skip()`, or `xfail` markers anywhere in
`crates/ucil-daemon/src/{storage.rs,lifecycle.rs,lib.rs}`.

`now_unix_secs()` in `lifecycle.rs` returns `0` on the
clock-before-`UNIX_EPOCH` pathological path via
`.map_or(0, |d| d.as_secs())`; this mirrors the existing
`session_manager::now_unix_secs` helper explicitly cited by the
work-order `scope_in` line 30 as the intentional duplication target
and is not a stub. The critic report (§1 Stub detection) concurs.

No mocks of `std::fs`, `serde_json`, Serena, LSP, SQLite, LanceDB, or
Docker — tests use real `tempfile::TempDir` + real `std::fs` +
`serde_json` round-trips (grep for `mock|fake|stub|MagicMock` on the
new surface returns 0 matches).

## Quality gates

| Crate | Line cov | Branch cov | Line floor | Verdict |
|-------|----------|------------|------------|---------|
| ucil-daemon | 87.99% | n/a (toolchain) | 85% | **PASS** |

Full report at `ucil-build/verification-reports/coverage-ucil-daemon.md`:
lines 1355/1540, regions 2149/2431 (88.40%), functions 170/205
(82.93%). Per DEC-0007 the cargo-mutants gate is removed from
per-WO gating; the reality-check oracle (above) is the authoritative
mutation check.

## Scope discipline

`git diff --name-only origin/main...HEAD`:

```
crates/ucil-daemon/src/lib.rs
crates/ucil-daemon/src/lifecycle.rs
crates/ucil-daemon/src/storage.rs
ucil-build/work-orders/0022-ready-for-review.md
```

Forbidden-path diff counts (all expected 0):

| Path | `git diff origin/main..HEAD | wc -l` |
|------|-------------------------------------|
| `crates/ucil-treesitter/` | 0 |
| `crates/ucil-core/` | 0 |
| `crates/ucil-lsp-diagnostics/` | 0 |
| `crates/ucil-cli/` | 0 |
| `crates/ucil-embeddings/` | 0 |
| `crates/ucil-agents/` | 0 |
| `adapters/` | 0 |
| `ucil-build/feature-list.json` | 0 |

`crates/ucil-daemon/Cargo.toml` unchanged (no new deps added).

## Commit hygiene

5 commits on the branch (`bca37f0` → `636a8c9`):

- `bca37f0` — feat(daemon): add storage module with two-tier .ucil/
  layout (Feature: P1-W2-F06).
- `4c555c9` — feat(daemon): lifecycle Checkpoint persists indexing
  progress for crash recovery (Feature: P1-W3-F09).
- `a411d7c` — chore(daemon): re-export StorageLayout + cross-reference
  Checkpoint in lib rustdoc (Features: P1-W2-F06 + P1-W3-F09).
- `861dbcd` — chore(daemon): preserve single-line lifecycle re-export
  for acceptance grep (Features: P1-W2-F06 + P1-W3-F09).
- `636a8c9` — chore(wo): mark WO-0022 ready for review (Features:
  P1-W2-F06 + P1-W3-F09).

All commits carry `Phase: 1`, `Feature: <id>`, and `Work-order: WO-0022`
trailers. No `--amend`, no force-push, no `--no-verify` hints in the
commit objects. Two commit-size warnings on the module-introduction
commits are covered by DEC-0005/DEC-0001 (module + impl + tests bound
together) and the reality-check atomicity requirement — both
documented by the critic report (§7) and the ready-for-review marker.

## WO-0006 rejection-mode check

The WO-0006 rejection mode (`storage::test_two_tier_layout` selector
not resolving because tests were wrapped in `mod tests { }`) is
explicitly not present here:

- `grep -qE '^\s*mod\s+tests\s*\{' crates/ucil-daemon/src/storage.rs`
  → no match (exit 1).
- `grep -qE '^\s*mod\s+tests\s*\{' crates/ucil-daemon/src/lifecycle.rs`
  → no match (exit 1).
- Both frozen selectors resolve to exactly 1 test each under the
  flat module-root `#[cfg(test)] #[test] fn ...` pattern.

## Features flipped

- **P1-W2-F06** → `passes=true` at commit `636a8c9`
- **P1-W3-F09** → `passes=true` at commit `636a8c9`
