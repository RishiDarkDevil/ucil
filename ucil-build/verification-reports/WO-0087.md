# Verification Report: WO-0087

**Verifier session**: vrf-72d554ce-e23f-43da-850b-c20f7d0b3c0a
**Branch**: feat/WO-0087-aider-repo-map-pagerank
**HEAD verified**: 4b9ba268910d5d2dc52d82b8e90cb45d82cd1683
**Verified at**: 2026-05-09T03:30:00Z
**Verdict**: PASS
**Retry**: 2 (incremented from 1 → 2)

## Summary

P3-W10-F01 (Aider repo-map reimplemented in Rust) lands on a clean
worktree under attempt 2 after a successful RCA-driven retry. The
critic reported CLEAN at `ucil-build/critic-reports/WO-0087.md` and
the verifier independently re-validates every load-bearing assertion
from a `cargo clean` baseline:

* Frozen test `context_compiler::test_repo_map_pagerank` exists at
  module root of `crates/ucil-core/src/context_compiler.rs:759` per
  DEC-0007 and resolves uniquely under the canonical selector.
* The test passes in 0.00s on a 6-entity / 6-relation / 3-file fixture.
* Workspace clippy clean at `-D warnings`; rustfmt check clean.
* Workspace-wide regression: `cargo test --workspace --no-fail-fast`
  green after symlinking the gitignored ONNX artefacts (the WO-0059
  standing environmental dependency unrelated to WO-0087's repo-map
  surface).
* Mutation contract M1 / M2 / M3 each surface their named SA1 / SA2 /
  SA3a panic body verbatim; md5 round-trip after each `git checkout --`
  returns OK.
* Coverage gate `coverage-gate.sh ucil-core 85 75` reports
  **PASS — line=97%** (well above the 85% floor; branch n/a per
  toolchain).

## Criteria

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| AC01 | `test -f crates/ucil-core/src/context_compiler.rs` | PASS | 862 LOC |
| AC02 | `^pub mod context_compiler;$` in lib.rs | PASS | line 11 |
| AC03 | `^pub use context_compiler::\{` in lib.rs | PASS | line 36 |
| AC04 | `^pub struct RepoMap\b` | PASS | declared |
| AC05 | `^pub struct RepoMapOptions\b` | PASS | declared with `Default` impl |
| AC06 | `^pub struct RankedSymbol\b` | PASS | declared |
| AC07 | `pub enum RepoMapError\b` | PASS | thiserror::Error + #[non_exhaustive] |
| AC08 | `pub fn build_repo_map` | PASS | entry point |
| AC09 | `pub fn personalized_page_rank` | PASS | pure kernel |
| AC10 | `recency_bias_multiplier` 50.0 | PASS | Default impl line 188 |
| AC11 | `damping` 0.85 | PASS | Default impl line 185 |
| AC12 | tracing span `name = "ucil.core.context_compiler.page_rank"` | PASS | line 326 area |
| AC13 | `#[cfg(test)]` ≥ 1 | PASS | 4 occurrences (618/622/645/665/757) |
| AC14 | `fn test_repo_map_pagerank` at module root | PASS | line 759 |
| AC15 | selector resolves uniquely under `-- --list` | PASS | exactly 1 match: `context_compiler::test_repo_map_pagerank: test` |
| AC16 | `cargo test -p ucil-core context_compiler::test_repo_map_pagerank` | PASS | `test result: ok. 1 passed` |
| AC17 | clippy + fmt | PASS | `cargo clippy -p ucil-core --all-targets -- -D warnings` exit 0; `cargo fmt --check -p ucil-core` exit 0 |
| AC18 | workspace-wide tests | PASS* | green after ONNX artefact symlink (WO-0059 standing env-dep — see §Disclosed deviations §A) |
| AC19 | word-ban `mock\|fake\|stub` in production-side files | PASS | empty grep |
| AC20 | stub-scan | PASS | no `todo!()`/`unimplemented!()`/`NotImplementedError` in new files |
| AC21 | anti-laziness `#[ignore]` | PASS | empty grep |
| AC22 | Cargo.toml unchanged | PASS | `git diff main -- Cargo.toml crates/ucil-core/Cargo.toml = 0` |
| AC23 | M1 mutation → SA2 panic | PASS | observed verbatim — see Mutation table |
| AC24 | M2 mutation → SA3a panic | PASS | observed verbatim with `total_tokens=71 > 30` — see Mutation table |
| AC25 | M3 mutation → SA1 panic | PASS | observed verbatim with `observed "a::root"` — see Mutation table |
| AC26 | mutations reversible via `git checkout --` | PASS | md5 `a13e2b0b4aec53ba1863ec50b9817785` preserved after each restore |
| AC27 | RFR `ucil-build/work-orders/0087-ready-for-review.md` exists with required sections | PASS | summary + M1/M2/M3 mutation contract + convergence diagnostics + path #4(a)/#4(b) decision log + 3 disclosed deviations |
| AC28 | zero merge commits | PASS | `git log feat/WO-0087-aider-repo-map-pagerank ^main --merges` empty |
| AC29 | tests/fixtures untouched | PASS | empty diff |
| AC30 | feature-list.json + schema untouched (pre-flip) | PASS | empty diff |
| AC31 | master-plan untouched | PASS | empty diff |
| AC32 | no `std::process::Command` in new module | PASS | empty grep |
| AC33 | no `unsafe` blocks | PASS | empty grep |
| AC34 | no `.unwrap()`/`.expect()` outside `#[cfg(test)]` | PASS | all 11 hits at lines 667-833 are inside the test (gated by `#[cfg(test)]` at line 757) |
| AC35 | commit cadence (4-6 commits per DEC-0005) | PASS | 6 commits matching skeleton + kernel + KG-wiring/budget + test + canonical RFR + slug-named pointer |

## Mutation contracts (M1/M2/M3)

Pre-mutation md5 baseline (verifier fresh capture):

```
a13e2b0b4aec53ba1863ec50b9817785  crates/ucil-core/src/context_compiler.rs
```

(matches the executor's RFR-stated value verbatim.)

| Mutation | Patch site | Selector | Observed panic body | md5 restore |
|----------|------------|----------|---------------------|-------------|
| **M1** (recency-bias multiplier removal) | `context_compiler.rs:561` — `options.recency_bias_multiplier * uniform_mass` → `uniform_mass` | `cargo test -p ucil-core context_compiler::test_repo_map_pagerank` | `(SA2) recency-bias top symbol expected file_a.rs; observed file_path="src/file_b.rs", qualified_name="b::handler"` (left=`"src/file_b.rs"`, right=`"src/file_a.rs"`) at line 814 | OK |
| **M2** (token-budget fit removal) | `context_compiler.rs:453-465` — replace prefix-greedy `fit_to_budget` with sum-only no-op | `cargo test -p ucil-core context_compiler::test_repo_map_pagerank` | `(SA3a) token budget exceeded: total_tokens=71 > 30` at line 826 | OK |
| **M3** (PageRank update equation negation) | `context_compiler.rs:420` — `(1.0 - options.damping).mul_add(pers_v, options.damping * incoming_sum)` → `(1.0 - options.damping).mul_add(pers_v, -options.damping * incoming_sum)` | `cargo test -p ucil-core context_compiler::test_repo_map_pagerank` | `(SA1) structural pagerank winner expected b::handler; observed "a::root"` (left=`"a::root"`, right=`"b::handler"`) at line 791 | OK |

All three mutations exited the test with the SA-tagged panic body
named in the RFR (and mirrored in scope_in #8 of the work-order).
After each `git checkout -- crates/ucil-core/src/context_compiler.rs`,
`md5sum -c /tmp/wo-0087-vrf-orig.md5` returned `OK` — zero accidental
drift from the baseline.

## Reality-check (`scripts/reality-check.sh`) — informational

`scripts/reality-check.sh P3-W10-F01` exits 1 with the documented
standing carry-forward bug:

> `[reality-check] FAILURE: acceptance tests reported ZERO tests run
> with code stashed — module was removed, not a genuine pass.`

This is the well-known `reality-check.sh` false-positive when the
frozen acceptance test lives at MODULE ROOT in the same file as the
production code (per DEC-0007 frozen-selector module-root placement,
mandated by scope_in #7). When `reality-check.sh` stashes
`crates/ucil-core/src/context_compiler.rs` it removes BOTH the
production code AND the test, so the cargo selector resolves to 0
tests — which the script (correctly) treats as fake-green-by-removal.
For DEC-0007 features the **authoritative anti-laziness layer** is
the in-place M1/M2/M3 sed-style mutation contract documented above
(per WO-0084 §`Reality-check` header + WO-0067/0068/0069/0070/0072/
0073/0074/0075/0076/0077/0079/0080/0082/0083/0084/0085/0086 verifier
precedent — now 16+ WOs deep). Both the M1/M2/M3 verbatim panic
re-validation above AND the critic's stub-scan + coverage-gate at 97%
satisfy the three anti-laziness layers per DEC-0007.

## Stub scan

```
$ rg -nE 'todo!\(\)|unimplemented!\(\)|NotImplementedError|raise NotImplementedError|TODO: implement' \
       crates/ucil-core/src/context_compiler.rs crates/ucil-core/src/lib.rs
(no matches)
```

No `pass`-only bodies; no bare-literal stubs; no `Default::default()`
returned as a real implementation. Word-ban grep
`mock|fake|stub` over both files returns empty.

## Quality gates

| Crate | Line cov | Branch cov | gate result | Verdict |
|-------|----------|------------|-------------|---------|
| ucil-core | **97%** (97.0% reported by `coverage-gate.sh ucil-core 85 75`) | n/a (toolchain — `cargo-llvm-cov` branches.count == 0 on stable) | `coverage-gate.sh ucil-core 85 75` exit 0 | **PASS** |

Coverage gate ran cleanly without the historical sccache
RUSTC_WRAPPER workaround — the `coverage-gate.sh` script's prune-step
+ `cargo llvm-cov clean --workspace` invocation produced a clean
profraw set on this run. (Standing protocol carry-forward AC23 from
38+ prior WOs is acknowledged but did not surface here.)

The full coverage report is at
`ucil-build/verification-reports/coverage-ucil-core.md`.

## Disclosed deviations

### §A — Pre-existing `ucil-embeddings::models::test_coderankembed_inference` failure (AC18 environmental dep)

`cargo test --workspace --no-fail-fast` reports one failing test on
the WO-0087 worktree before symlink:

```
test models::test_coderankembed_inference ... FAILED
thread 'models::test_coderankembed_inference' panicked at crates/ucil-embeddings/src/models.rs:920:5:
CodeRankEmbed model artefacts not present at "/home/rishidarkdevil/Desktop/ucil-wt/WO-0087/ml/models/coderankembed";
run `bash scripts/devtools/install-coderankembed.sh` first (P2-W8-F02 / WO-0059);
got model.onnx exists=false, tokenizer.json exists=false
```

Verifier confirmed this is an **environmental** dependency — the
ONNX model artefacts at `ml/models/coderankembed/{model.onnx,tokenizer.json}`
are gitignored (per the directory's `.gitignore`) and exist in the
main worktree (`/home/rishidarkdevil/Desktop/ucil/ml/models/coderankembed/`)
but were never copied into this worktree. After symlinking both files
from the main worktree (`ln -sf`), the workspace test suite passed
cleanly with zero failures.

This is the WO-0059 standing environmental requirement, NOT a
regression introduced by WO-0087. The repo-map module under WO-0087
does not touch the ONNX inference path. Documented in the executor's
RFR §A; verifier confirms.

### §B — RCA-driven topology change (RFR §B carry-forward)

The executor disclosed a topology deviation from `scope_in #8`'s
example DAG (`root → child{1,2} → handler → helper → leaf`) per the
RCA at `verification-reports/root-cause-WO-0087.md` (95% confidence).
The replacement edge set still satisfies the spec-frozen invariants
(6 entities + 6 `calls` relations + 3 files; SA1/SA2/SA3 panic bodies
unchanged; M1/M2/M3 still surface SA2/SA3a/SA1 verbatim). Verifier
re-validated all three mutation outcomes against the new topology
above. Spirit-over-literal precedent from WO-0070 / WO-0083 / WO-0084.

### §C — `cargo test ... -- --list` separator

The work-order's `acceptance_criteria` AC15 string uses the form
`cargo test -p ucil-core context_compiler::test_repo_map_pagerank --list`,
but recent Cargo (1.94+) requires the `-- --list` separator. Verifier
used the corrected form (`cargo test -p ucil-core context_compiler::test_repo_map_pagerank -- --list`)
and confirmed the count `1` is identical between forms.

## Carry-forward standing protocols (acknowledged, not blockers)

* **`scripts/reality-check.sh` standing carry-forward** —
  documented in §Reality-check above. The M1/M2/M3 mutation contract
  IS the authoritative anti-laziness layer per the 16+ WO precedent.
* **AC30/AC31 effectiveness-gate flake** — three open escalations
  predate WO-0087; not a blocker for this WO.
* **AC18 ONNX environmental dependency** — disclosed §A above; not a
  WO-0087 regression.

## Features flipped

- **P3-W10-F01** → `passes=true` (verified at commit `4b9ba268910d5d2dc52d82b8e90cb45d82cd1683`).

## Reproduction

```bash
cd /home/rishidarkdevil/Desktop/ucil-wt/WO-0087
git checkout feat/WO-0087-aider-repo-map-pagerank
git pull --ff-only
cargo clean
cargo test -p ucil-core context_compiler::test_repo_map_pagerank
# expect: test result: ok. 1 passed
cargo clippy -p ucil-core --all-targets -- -D warnings  # exit 0
cargo fmt --check -p ucil-core                          # exit 0
md5sum crates/ucil-core/src/context_compiler.rs > /tmp/wo-0087-vrf-orig.md5
# Apply M1: sed -i 's|options.recency_bias_multiplier \* uniform_mass|uniform_mass|' \
#            crates/ucil-core/src/context_compiler.rs
# Re-run cargo test → SA2 panic verbatim
git checkout -- crates/ucil-core/src/context_compiler.rs && md5sum -c /tmp/wo-0087-vrf-orig.md5
# Apply M2 (multi-line python edit replacing fit_to_budget body)
# Re-run cargo test → SA3a panic verbatim (total_tokens=71 > 30)
git checkout -- crates/ucil-core/src/context_compiler.rs && md5sum -c /tmp/wo-0087-vrf-orig.md5
# Apply M3: sed -i 's|options.damping \* incoming_sum)|-options.damping \* incoming_sum)|' \
#            crates/ucil-core/src/context_compiler.rs
# Re-run cargo test → SA1 panic verbatim (observed "a::root")
git checkout -- crates/ucil-core/src/context_compiler.rs && md5sum -c /tmp/wo-0087-vrf-orig.md5
ln -sf /home/rishidarkdevil/Desktop/ucil/ml/models/coderankembed/model.onnx ml/models/coderankembed/model.onnx
ln -sf /home/rishidarkdevil/Desktop/ucil/ml/models/coderankembed/tokenizer.json ml/models/coderankembed/tokenizer.json
cargo test --workspace --no-fail-fast  # exit 0
bash scripts/verify/coverage-gate.sh ucil-core 85 75  # exit 0, line=97%
```

## Citations

* Work-order: `ucil-build/work-orders/0087-aider-repo-map-pagerank.json`
* RFR: `ucil-build/work-orders/0087-ready-for-review.md`
* Critic report (CLEAN): `ucil-build/critic-reports/WO-0087.md`
* First-attempt rejection: `ucil-build/rejections/WO-0087.md`
* Root-cause analysis: `ucil-build/verification-reports/root-cause-WO-0087.md`
* DEC-0007 (frozen-selector module-root placement)
* DEC-0005 (module-coherence commits)
* Master plan: §1.1 line 44, §3.5, §4.5 line 345, §6.1 line 506,
  §6.3 line 660, §17.2 line 1634, §18 Phase 3 Week 10 line 1808
