# Verification Report: WO-0081 — test-runner-mcp G8 plugin manifest (DEC-0024 revival)

**Verifier session**: vrf-wo-0081-archived-supersede-rejection
**Branch**: `feat/WO-0081-test-runner-mcp-plugin-manifest` (HEAD `7e7ba2c`)
**Verified at**: 2026-05-08T14:45:12Z
**Verdict**: **REJECT** — WO-0081 archived/superseded by DEC-0025; no implementation present on the branch to verify.
**Feature**: P3-W11-F07 — `passes` remains `false`; `attempts` remains 0 (executor halted at pre-flight per Oracle hierarchy escape hatch).

## Pre-verification state survey

| Artifact | Expected | Actual |
|---|---|---|
| `ucil-build/work-orders/0081-test-runner-mcp-plugin-manifest.json` | present (active WO) | **archived** to `ucil-build/work-orders/archive/0081-...json` (commit `70a2670`) |
| `ucil-build/work-orders/0081-ready-for-review.md` | present (executor's RFR) | **missing** — not authored (halt-and-escalate path taken) |
| `ucil-build/decisions/DEC-0025-correct-test-runner-mcp-tool-surface.md` | n/a | **present on main** (commit `4d9d033`) — supersedes DEC-0024 |
| `ucil-build/escalations/20260508T1435Z-wo-0081-dec-0024-tool-surface-mismatch.md` | n/a | **resolved: true** (Option A accepted) |
| `ucil-build/critic-reports/WO-0081.md` | present | **present** (commit `afd1f22`) — verdict "BLOCKED — no implementation" |
| `feat/WO-0081-test-runner-mcp-plugin-manifest` worktree | implementation pushed | only `7e7ba2c` (escalation commit); no source/test/plugin/script diff vs `main` |

## Acceptance criteria (per archived WO-0081 JSON, scope_in items 1–36 / acceptance_criteria 1–18)

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| 1 | `test -f plugins/testing/test-runner/plugin.toml` | **FAIL** | path missing on `main` and on the branch |
| 2 | `grep -q '^name = "test-runner"$'` in manifest | FAIL | manifest absent |
| 3 | `grep -qE '^command = "(npx\|node)"$'` in manifest | FAIL | manifest absent |
| 4 | manifest carries pinned-ref test-runner-mcp@<semver-or-sha> OR vendored Tier-2 dir | FAIL | manifest absent |
| 5 | `test -x scripts/devtools/install-test-runner-mcp.sh` | FAIL | script absent |
| 6 | `test -x scripts/verify/P3-W11-F07.sh` | PASS (mechanically) | the existing TODO-stub on main IS executable, but the AC's substantive intent (replaced) is unmet |
| 7 | `! grep -qE '^echo "TODO: implement acceptance test for P3-W11-F07"$'` in verify script | **FAIL** | the TODO-stub line is still present verbatim |
| 8 | `! grep -qE 'std::process::Command'` in `g8_plugin_manifests.rs` | PASS (vacuously) | no diff against the file |
| 9 | `! grep -qiE 'mock\|fake\|stub'` in production-side files | PASS (vacuously) | files don't exist |
| 10 | `! grep -qE '"(main\|latest\|...)"'` in manifest | PASS (vacuously) | manifest absent |
| 11 | `! grep -qE 'unsafe[[:space:]]*\{[^}]*set_var'` in test file | PASS (vacuously) | no diff |
| 12 | `#[tokio::test] async fn test_runner_manifest_health_check` present | **FAIL** | test never written |
| 13 | `cargo test -p ucil-daemon --test g8_plugin_manifests g8_plugin_manifests::test_runner_manifest_health_check ... 'test result: ok. 1 passed'` | **FAIL** | selector matches zero tests; nothing to run |
| 14 | `cargo clippy -p ucil-daemon -- -D warnings` | NOT-RUN | no production diff in scope |
| 15 | `cargo fmt --check -p ucil-daemon` | NOT-RUN | no production diff in scope |
| 16 | `scripts/verify/P3-W11-F07.sh` exit 0 | **FAIL** | script is still the TODO-stub returning 1 |
| 17 | `test -f ucil-build/work-orders/0081-ready-for-review.md` | **FAIL** | RFR not authored |
| 18 | `git log feat/...^main --merges` empty | PASS (trivially) | one non-merge commit |

**Verdict-determining failures**: ACs #1, #2, #3, #4, #5, #7, #12, #13, #16, #17. 10 hard fails; the 6 vacuous "passes" are not load-bearing.

## Reality-check mutation (step 6b)

**Skipped — N/A.** `scripts/reality-check.sh P3-W11-F07` requires the feature's source to exist before stashing. Diff against `main` for `crates/**`, `plugins/**`, `scripts/**`, `adapters/**`, `ml/**`, `plugin/**` is empty:

```
$ git diff main..feat/WO-0081-test-runner-mcp-plugin-manifest -- 'crates/**' 'plugins/**' 'scripts/**' 'adapters/**' 'ml/**' 'plugin/**'
(no output)
```

## Stub scan (step 6c)

**Clean.** `ast-grep` for `todo!()` / `unimplemented!()` / `NotImplementedError` / single-`pass` bodies over the diff returns zero matches. Diff is `ucil-build/`-only documentation.

## Quality gates (step 7)

| Gate | Crate | Result | Notes |
|---|---|---|---|
| coverage-gate.sh | (none) | **AUTO-SKIP** | `git diff --name-only origin/main...HEAD \| grep -oE 'crates/[^/]+'` returns empty; no crate to gate |

Per the verifier instructions step 7: "Auto-skip (exit 0) for crates that don't yet exist; that's fine." The same exit-0 logic applies when zero crates are touched.

## Why nothing was implemented

The WO-0081 executor performed the planner-prescribed Tier-1 live capture (scope_in #5) and discovered DEC-0024's source data is wrong on two material points:

1. `test-runner-mcp` is **NOT published to npm** (404 on the bare name); only the third-party scoped mirror `@iflow-mcp/mcp-test-runner@0.2.1` exists.
2. Upstream advertises **ONE tool** (`run_tests`) whose `framework` enum has 7 values (bats/pytest/flutter/jest/go/rust/generic), NOT six distinct tools as DEC-0024 §Decision point 3 prescribed.

Hard AC #4 (`health.tools.len() >= 6`) is therefore unsatisfiable by Tier-1 at every commit in upstream history (back to `463bd5d6`, 2025-01-18). The executor halted per the root `CLAUDE.md` Oracle hierarchy escape hatch ("If you encounter a conflict, STOP and write `ucil-build/decisions/proposed-*.md` — never silently deviate"), authored `proposed-DEC-0025`, and emitted the escalation. The user accepted Option A; DEC-0025 was promoted on `main`; WO-0081 was archived.

## Forbidden-path discipline

Verified by inspecting the branch diff:

- `ucil-build/feature-list.json` — **NOT touched** ✓
- `ucil-build/feature-list.schema.json` — **NOT touched** ✓
- `ucil-master-plan-v2.1-final.md` — **NOT touched** ✓
- `tests/fixtures/**` — **NOT touched** ✓
- `scripts/gate/**` — **NOT touched** ✓
- `scripts/flip-feature.sh` — **NOT touched** ✓
- `crates/ucil-daemon/tests/plugin_manifests.rs` — **NOT touched** ✓
- `crates/ucil-daemon/tests/g{3,4,5,6,7,8}_plugin_manifests.rs` — **NOT touched** ✓
- `plugins/{quality,knowledge,architecture,context,platform,testing/mcp-pytest-runner}/**` — **NOT touched** ✓

## Anti-laziness contract (sanity)

Verified clean — verifier did not flip `passes`; executor did not flip `passes`. No `#[ignore]` / `.skip()` / `xfail` introduced. No `todo!()` / `unimplemented!()` / `NotImplementedError`. No `git revert` / `git reset` of verifier-rejected code. No `--no-verify`, no `--amend` after push. No `feature-list.json` mutation outside whitelist. No fixture mutations.

## Features flipped

**NONE.** P3-W11-F07 remains `passes: false`, `attempts: 0`.

## Outcome

`ucil-build/rejections/WO-0081.md` overwritten with retry=0 rejection citing the archived/superseded status. The next harness iteration will rely on the planner emitting a fresh successor WO with DEC-0025-corrected acceptance criteria; that successor WO will go through its own executor → critic → verifier cycle.
