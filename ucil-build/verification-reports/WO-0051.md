# Verification Report: WO-0051

**Verifier session**: verifier-4e4a413d-a19b-4c26-bc29-27d60c9965cc
**Branch**: feat/WO-0051-ripgrep-plugin-manifest-and-smoke
**HEAD at verification**: e9ed6ae83afe7d8b5edb36d79579925412342421
**Verified at**: 2026-05-05T16:36:26Z
**Verdict**: PASS

WO-0051 lands the **ripgrep** plugin manifest + dev-tools install hint
+ a parse-only `plugin_manifests::ripgrep_manifest_parses` test (inside
the existing WO-0044 wrapper module) + a rewritten end-to-end smoke
script `scripts/verify/P2-W7-F07.sh`. Per **DEC-0009**, ripgrep runs
in-process via `crates/ucil-daemon/src/text_search.rs` (WO-0035) — the
manifest's `[transport]` table is declarative metadata, NOT a launch
target — so the test is **parse-only** and the smoke script does NOT
exercise an MCP `health_check` round-trip (in contrast to ast-grep /
probe in WO-0044).

All 29 acceptance criteria green. Pre-baked AC19/AC20/AC21 mutations
each force the expected runtime panic at the cited assertion site;
restoring greens the failure. The repo's auto-runner
`scripts/reality-check.sh P2-W7-F07` exits 1 ("ZERO tests run with
code stashed — module was removed") because the only `.rs` source
artifact this WO touches is the test file itself
(`crates/ucil-daemon/tests/plugin_manifests.rs`); stashing it removes
the test that selects on `plugin_manifests::ripgrep_manifest_parses`,
which is the same structural shape the WO-0047 / WO-0048 / WO-0050
verifiers documented and the same posture per **DEC-0007**: per-WO
`cargo-mutants` is removed; the pre-baked AC19/AC20/AC21 mutations
(provides emptied, name corrupted, `--json` flag dropped) are the
authoritative anti-laziness layer.

## Acceptance criteria

| #  | Criterion                                                                                          | Result | Notes |
|----|----------------------------------------------------------------------------------------------------|--------|-------|
| 01 | `test -f plugins/search/ripgrep/plugin.toml`                                                       | PASS   | Manifest present at canonical path |
| 02 | `test -x scripts/verify/P2-W7-F07.sh`                                                              | PASS   | Executable bit set |
| 03 | `test -x scripts/devtools/install-ripgrep.sh`                                                      | PASS   | Executable bit set |
| 04 | `grep -q '^name = "ripgrep"' plugins/search/ripgrep/plugin.toml`                                   | PASS   | `[plugin].name = "ripgrep"` |
| 05 | `grep -q 'provides = \["search.text"\]' plugins/search/ripgrep/plugin.toml`                        | PASS   | Single capability, exact form |
| 06 | `! grep -q '"main"' plugins/search/ripgrep/plugin.toml`                                            | PASS   | No moving `main` ref; pinned 14.1.1 |
| 07 | `cargo test -p ucil-daemon --test plugin_manifests plugin_manifests::ripgrep_manifest_parses` 0    | PASS   | `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 2 filtered out; finished in 0.00s` |
| 08 | Selector `fn ripgrep_manifest_parses` lives inside `mod plugin_manifests {}`                       | PASS   | line 157 inside wrapper opened at line 36; DEC-0007 module-root placement WITHIN wrapper |
| 09 | `bash scripts/verify/P2-W7-F07.sh` exits 0 → `[OK] P2-W7-F07`                                      | PASS   | Final line printed; full log captured |
| 10 | `rg --json` emits `"type":"match"`, `"path"`, `fn evaluate` markers                                | PASS   | All three structural greps matched |
| 11 | `.gitignore` honoured: zero `target/` paths in `rg --files-with-matches` output                    | PASS   | 0 matches against `target/`; fixture `tests/fixtures/rust-project/src/util.rs` present (proves search ran) |
| 12 | WO-0044 sentinels: `ast_grep_manifest_health_check`, `probe_manifest_health_check` 0               | PASS   | 1 passed each (npx cache warm; real MCP servers spoke JSON-RPC) |
| 13 | WO-0042/0043 sentinels: `plugin_manager::*`                                                        | PASS   | 15 passed across `plugin_manager::tests::*`, `test_lifecycle_state_machine`, `test_manifest_parser`, `test_hot_cold_lifecycle`, `test_circuit_breaker`, `test_hot_reload`, `test_health_check_timeout` |
| 14 | Phase-1 sentinels: `e2e_mcp_stdio`, `e2e_mcp_with_kg`                                              | PASS   | `e2e_mcp_stdio_handshake_returns_22_tools_with_ceqp` 1 passed; `e2e_mcp_stdio_with_repo_returns_real_find_definition` 1 passed |
| 15 | `cargo test --workspace --no-fail-fast`                                                            | PASS   | 0 failed across all binaries; 1 pre-existing `#[ignore]` (DEC-0003 SLOW-TEST `fixture::rust_project_loads`) |
| 16 | `cargo clippy -p ucil-daemon --all-targets -- -D warnings`                                         | PASS   | 0 warnings |
| 17 | `cargo fmt --check`                                                                                | PASS   | exit 0 |
| 18 | `shellcheck` fallback                                                                              | PASS   | shellcheck not on PATH; verify script logged `[INFO] shellcheck not on PATH; skipping shellcheck step.` per AC18 fallback |
| 19 | Pre-baked mutation #1 (provides emptied)                                                           | PASS   | See dedicated Mutation section below |
| 20 | Pre-baked mutation #2 (name corrupted)                                                             | PASS   | See dedicated Mutation section below |
| 21 | Pre-baked mutation #3 (verify script `--json` dropped)                                             | PASS   | See dedicated Mutation section below |
| 22 | Allow-list (`git diff --name-only main...HEAD`) lists only the 5 expected paths                    | PASS   | matches WO scope_in[9]: `crates/ucil-daemon/tests/plugin_manifests.rs`, `plugins/search/ripgrep/plugin.toml`, `scripts/devtools/install-ripgrep.sh`, `scripts/verify/P2-W7-F07.sh`, `ucil-build/work-orders/0051-ready-for-review.md` |
| 23 | `Cargo.toml`, `rust-toolchain.toml` unchanged                                                      | PASS   | empty diff |
| 24 | `tests/fixtures/**` unchanged                                                                      | PASS   | empty diff |
| 25 | `ucil-build/feature-list.json` and `feature-list.schema.json` unchanged by executor                | PASS   | empty diff in main...HEAD; this verifier flips passes per the harness contract |
| 26 | `ucil-master-plan-v2.1-final.md` unchanged                                                         | PASS   | empty diff |
| 27 | Stub-scan in new/edited regions of all 4 artifacts                                                 | PASS   | 0 hits for `todo!()`, `unimplemented!()`, `panic!(".*not yet`, `TODO`, `FIXME` |
| 28 | Commit cadence: 4 commits matching the ladder in scope_in[10] (+1 ready-for-review marker)         | PASS   | `26fbedb` plugins.toml, `4f43ffd` install-ripgrep.sh, `5c6bce2` test extension, `82c5c71` verify script, `e9ed6ae` ready-for-review marker |
| 29 | Branch up-to-date with origin                                                                      | PASS   | `git rev-parse HEAD` == `git rev-parse @{u}` == `e9ed6ae`; `git status --porcelain` empty before flip |

`scripts/verify/P2-W7-F07.sh` end-to-end log →
```
[INFO] shellcheck not on PATH; skipping shellcheck step.
[INFO] P2-W7-F07: ripgrep 14.1.1
[INFO] P2-W7-F07: manifest plugins/search/ripgrep/plugin.toml present.
[INFO] P2-W7-F07: running cargo test plugin_manifests::ripgrep_manifest_parses...
[INFO] P2-W7-F07: cargo test PASS.
[INFO] P2-W7-F07: exercising rg --json against tests/fixtures/rust-project...
[INFO] P2-W7-F07: rg --json structural markers PASS.
[INFO] P2-W7-F07: warming workspace build (target/ must exist for the gitignore-respect check)...
[INFO] P2-W7-F07: rg --files-with-matches 'fn evaluate' . — must NOT descend into target/...
[INFO] P2-W7-F07: .gitignore-respect sub-check PASS.
[OK] P2-W7-F07
```

## Feature acceptance_tests (frozen feature-list selectors)

| Feature   | Acceptance test                                               | Result |
|-----------|---------------------------------------------------------------|--------|
| P2-W7-F07 | `script: scripts/verify/P2-W7-F07.sh` (exit 0)                | PASS   |

## Mutation checks (pre-baked AC19 / AC20 / AC21 — authoritative per DEC-0007)

| # | Mutation                                                                                            | Stashed → fail?                                                                                                                                                  | Restored → pass?                          | Verdict |
|---|-----------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------|---------|
| 1 | `plugins/search/ripgrep/plugin.toml`: `provides = ["search.text"]` → `provides = []`                | **FAILED** — panic at `crates/ucil-daemon/tests/plugin_manifests.rs:167:9` `capabilities.provides must include search.text; observed []` (expected sub-assertion 3) | `1 passed; 0 failed; finished in 0.00s` after `git checkout --` | OK |
| 2 | `plugins/search/ripgrep/plugin.toml`: `name = "ripgrep"` → `name = "x"`                              | **FAILED** — panic at `crates/ucil-daemon/tests/plugin_manifests.rs:162:9` `assertion left == right failed: plugin.name must be exactly ripgrep; observed x` (expected sub-assertion 2) | `1 passed; 0 failed; finished in 0.00s` after `git checkout --` | OK |
| 3 | `scripts/verify/P2-W7-F07.sh`: drop `--json` flag (`rg --json 'fn evaluate'` → `rg 'fn evaluate'`)   | **FAILED** — script printed `[FAIL] P2-W7-F07: ripgrep --json output missing structural marker '"type":"match"'` and exited 1                                  | `[OK] P2-W7-F07` after `git checkout --` | OK |

After every mutation, `git status --porcelain` was empty before the
next step. Each mutation pin-points the test/script's assertion at the
exact line / message documented in the WO acceptance entries.

### Reality-check disposition (for the record)

`scripts/reality-check.sh P2-W7-F07` exits **1** with
`[reality-check] FAILURE: acceptance tests reported ZERO tests run
with code stashed — module was removed, not a genuine pass.`

The auto-runner extracts changed source files via the regex
`\.(rs|ts|tsx|py)$`, which excludes the manifest TOML, the
install-hint shell, and the verify-script shell. The only matching
artifact is the test file itself. When the test file is rolled back
to its parent commit, the test selector
`plugin_manifests::ripgrep_manifest_parses` matches zero tests, which
the script's zero-tests-run safeguard treats as fake-green.

This is the same structural posture the WO-0047 / WO-0048 / WO-0050
verifiers documented (`executor::test_g1_parallel_execution`,
`executor::test_g1_result_fusion`, `executor::test_find_references_tool`
co-located with their impls). Per **DEC-0007** (per-WO `cargo-mutants`
removed), the **pre-baked AC19 / AC20 / AC21 mutations are the
authoritative anti-laziness layer**, and the manual stash / restore
above demonstrates each fires its expected assertion at the cited
location.

A side observation: when this verifier ran `scripts/reality-check.sh`,
its defensive `git stash pop 2>/dev/null || true` popped a leftover
WO-0050-shaped stash (sourced from the shared `.git/refs/stash` across
worktrees). Those WO-0050 leftovers (`crates/ucil-core/src/lib.rs`
modified + `crates/ucil-core/src/fusion.rs` untracked) were
re-stashed under the message
`wo-0051-vrf-restash-WO0050-leftovers` so WO-0050's verifier (or
triage) can recover them; they are NOT in the WO-0051 commit history
and are NOT reflected in this report's verdict.

## Stub scan

```
$ for f in plugins/search/ripgrep/plugin.toml \
           scripts/devtools/install-ripgrep.sh \
           scripts/verify/P2-W7-F07.sh \
           crates/ucil-daemon/tests/plugin_manifests.rs; do
    echo "--- $f ---"
    grep -nE 'todo!\(\)|unimplemented!\(\)|panic!\(".*not yet|TODO|FIXME' "$f" || echo '  0 hits'
  done
```
returns `0 hits` for every file (the existing untouched ast-grep /
probe blocks of `plugin_manifests.rs` are out of scope per AC27).

## Quality gates

| Crate       | Mutation score                                              | Line cov            | Branch cov           | Verdict |
|-------------|-------------------------------------------------------------|---------------------|----------------------|---------|
| ucil-daemon | per-WO `cargo-mutants` removed by DEC-0007; AC19/20/21 are authoritative | 89.76% (floor 85%) | n/a (toolchain) | PASS |

Coverage gate (`scripts/verify/coverage-gate.sh ucil-daemon 85 75`) →
`[coverage-gate] PASS — ucil-daemon line=90% branch=n/a` (full report
in `ucil-build/verification-reports/coverage-ucil-daemon.md`).

## Features flipped

- **P2-W7-F07** → `passes=true`, `last_verified_by =
  verifier-4e4a413d-a19b-4c26-bc29-27d60c9965cc`,
  `last_verified_commit = e9ed6ae`, `attempts = 0` (first attempt,
  no retries needed).
