# Verification Report: WO-0004 (retry 2)

**Verifier session**: vrf-8b2d3f91-c5e7-4a12-b847-0f6e29d15c84
**Branch**: feat/WO-0004-init-pipeline-and-ci
**HEAD commit**: cd1b423473c19ff2015ef1f2700df7926e21074c
**Verified at**: 2026-04-15T20:30:00Z
**Verdict**: REJECT

---

## Criteria

All 6 acceptance criteria run from clean build in worktree
`/home/rishidarkdevil/Desktop/ucil-wt/WO-0004` after `cargo clean -p ucil-cli`.

| # | Criterion | Result | Duration | Notes |
|---|-----------|--------|----------|-------|
| 1 | `cargo nextest run -p ucil-cli --test-threads 1` — `test_llm_provider_selection` | PASS | 0.007s | 8/8 tests green |
| 2 | `cargo nextest run -p ucil-cli --test-threads 1` — `test_plugin_health_verification` | PASS | 0.026s | 8/8 tests green |
| 3 | `cargo nextest run -p ucil-cli --test-threads 1` — `test_init_report_json` | PASS | 0.026s | 8/8 tests green |
| 4 | `bash scripts/verify/P0-W1-F08.sh` | PASS | ~6s | YAML valid; `ucil init --no-install-plugins` produced `init_report.json` |
| 5 | `cargo clippy -p ucil-cli -- -D warnings` | PASS | 0.23s | No warnings |
| 6 | `cargo build -p ucil-cli` | PASS | 0.06s | Clean build |

---

## Mutation checks

| Feature | Script result | Stashed → fail? | Popped → pass? | Verdict |
|---------|---------------|-----------------|----------------|---------|
| P0-W1-F04 | exit 1 | FAIL — 0 tests ran | N/A | REJECT |
| P0-W1-F05 | exit 1 | FAIL — 0 tests ran | N/A | REJECT |
| P0-W1-F06 | exit 3 | N/A — no tagged commit | N/A | REJECT |
| P0-W1-F08 | exit 0 | N/A — no .rs/.ts/.py files | N/A | TRIVIAL PASS |

### Root cause — F04 and F05

`reality-check.sh` uses the feature-list `acceptance_tests[*].selector` as a
nextest/cargo-test filter. The selectors are:

| Feature | Frozen selector | Actual nextest test path |
|---------|----------------|--------------------------|
| P0-W1-F04 | `commands::init::test_llm_provider_selection` | `commands::init::tests::test_llm_provider_selection` |
| P0-W1-F05 | `commands::init::test_plugin_health_verification` | `commands::init::tests::test_plugin_health_verification` |

`commands::init::test_llm_provider_selection` is NOT a substring of
`commands::init::tests::test_llm_provider_selection` (the `::tests::` component breaks
the match). Both nextest and cargo test find 0 matching tests → `zero_tests=1` → exit 1.

This is independent of the co-located tests problem. Even when `d2af2f9^:init.rs` is
restored (which DOES contain `test_plugin_health_verification`), the broken selector
causes 0 tests to run for F05.

### Root cause — F06

No commit in the branch carries `Feature: P0-W1-F06` trailer. The feature's implementation
was included in `a5a5470` (tagged only `Feature: P0-W1-F04`). `reality-check.sh` cannot
locate a source commit for F06 and exits 3.

---

## Stub scan

No `todo!()`, `unimplemented!()`, bare `pass` bodies, or trivial literal returns found in
changed files:
- `crates/ucil-cli/src/commands/init.rs`
- `.github/workflows/ci.yml`
- `scripts/verify/P0-W1-F08.sh`

Prior B1 fix (`tokio::time::timeout` on `verify_plugin_health()`) confirmed correct
in commit `d2af2f9`.

---

## Features NOT flipped

All four features remain `passes=false`.

- P0-W1-F04: not flipped (mutation check exit 1)
- P0-W1-F05: not flipped (mutation check exit 1)
- P0-W1-F06: not flipped (mutation check exit 3 — no tagged commit)
- P0-W1-F08: not flipped (dependency features not passing)

---

## Required fixes (see `rejections/WO-0004.md` for full detail)

1. **Create `crates/ucil-cli/tests/init.rs`** with tests structured under
   `mod commands { mod init { async fn test_llm_provider_selection() ... } }` so
   nextest paths match the frozen selectors AND the tests survive rollback of `init.rs`.
2. **Remove F04/F05/F06 test bodies from `mod tests { }` in `init.rs`** (keep
   pre-existing language-detection tests that were there before WO-0004).
3. **Add chore commit tagged `Feature: P0-W1-F06`** so reality-check can locate the
   source commit.
