# Verification Report: WO-0028

**Verifier session**: vrf-0a7bb24a-4d55-4d30-93e0-0225021d1ca1
**Branch**: feat/WO-0028-progressive-startup
**Verified at**: 2026-04-18T12:04:03Z
**HEAD**: 2fa247ea46710a38313ba831bbb07f697cd35081
**Verdict**: PASS

## Scope

- Feature covered: `P1-W3-F08` (progressive startup + priority indexing queue)
- Files changed vs `origin/main`:
  - `crates/ucil-daemon/src/priority_queue.rs` (new, ~280 lines)
  - `crates/ucil-daemon/src/startup.rs` (new, ~400 lines)
  - `crates/ucil-daemon/src/server.rs` (+137 lines, one new test `test_progressive_startup`)
  - `crates/ucil-daemon/src/lib.rs` (+16 lines, module declarations + re-exports)

## Criteria

All 52 acceptance criteria from the work-order ran in order from a clean slate
(`cargo clean` + `git clean -fdx -e ucil-build/ -e .env` in the
`feat/WO-0028-progressive-startup` worktree).

| # | Criterion | Result | Duration | Notes |
|---|-----------|--------|----------|-------|
| 1 | `cargo nextest run -p ucil-daemon server::test_progressive_startup` (frozen F08 selector) | PASS | 3.4s | 1 passed, 64 skipped; test itself finishes in 0.004s (well under 2s budget) |
| 2 | `cargo nextest run -p ucil-daemon 'priority_queue::'` | PASS | 0.18s | 5/5 passed (ord-is-newest-first, enqueue+pop, touch-reorders, len-tracks, peek-does-not-consume) |
| 3 | `cargo nextest run -p ucil-daemon 'startup::'` | PASS | 0.22s | 4/4 passed (deadline constant, handle_call_for_priority happy + missing-key, ready-handle-resolves) |
| 4 | `cargo nextest run -p ucil-daemon --no-fail-fast` | PASS | 0.90s | 65/65 passed, 0 skipped (no regression vs 62-test baseline; +3 new) |
| 5 | `cargo build --workspace` | PASS | 2.60s | clean build of all workspace crates |
| 6 | `cargo clippy -p ucil-daemon --all-targets -- -D warnings` | PASS | 2.24s | pedantic + nursery clean |
| 7 | `cargo doc -p ucil-daemon --no-deps` → no `^warning`/`^error` | PASS | 0.75s | broken-intra-doc-links check green |
| 8 | `cargo fmt --check` | PASS | <1s | clean |
| 9 | no `todo!` / `unimplemented!` / `#[ignore]` in new sources | PASS | - | grep returned 0 matches |
| 10-11 | both new files exist | PASS | - | `priority_queue.rs`, `startup.rs` |
| 12-15 | `lib.rs` carries `pub mod` + `pub use` for both new modules | PASS | - | 4/4 grep hits |
| 16-25 | `priority_queue.rs` carries all required types, methods, and 5 test names | PASS | - | 10/10 grep hits |
| 26-34 | `startup.rs` carries `STARTUP_DEADLINE` / `StartupError` / `ProgressiveStartup` / `ReadyHandle` / `handle_call_for_priority` + 4 test names | PASS | - | 9/9 grep hits |
| 35 | `server.rs` carries `test_progressive_startup` | PASS | - | - |
| 36-37 | no `^mod tests {` wrapper in the two new modules (DEC-0005) | PASS | - | flat tests confirmed |
| 38-50 | forbidden-paths guard (13 directories/files must show 0 diff lines vs `origin/main`) | PASS | - | `ucil-core`, `ucil-treesitter`, `ucil-lsp-diagnostics`, `ucil-embeddings`, `ucil-agents`, `ucil-cli`, `adapters/`, `ml/`, `watcher.rs`, `session_manager.rs`, `session_ttl.rs`, `lifecycle.rs`, `main.rs` — all zero |
| 51 | `bash scripts/reality-check.sh P1-W3-F08` | PASS | 2.26s | stashed → acceptance tests failed as expected; restored → tests pass |

## Mutation checks

| Feature | Stashed → fail? | Popped → pass? | Verdict |
|---------|-----------------|----------------|---------|
| P1-W3-F08 | yes | yes | OK |

`scripts/reality-check.sh P1-W3-F08` identified the 4-file changeset
(`lib.rs`, `priority_queue.rs`, `server.rs`, `startup.rs`), rolled each
back to its introducing commit's parent state, confirmed the acceptance
tests **failed with code stashed**, then restored and confirmed they
**pass with code restored**. No false-positive. Working tree and
upstream remain clean after.

## Stub scan

No `todo!()`, `unimplemented!()`, `NotImplementedError`, or single-`pass`
bodies detected in the four changed source files. No empty `fn`-bodies
of the pattern `fn … { }`.

## Quality gates

### Coverage gate (ucil-daemon, floor 85% line / 75% branch)

- **Verdict**: PASS (line 89.77%, floor 85%)
- **Infra note**: the first invocation of `scripts/verify/coverage-gate.sh`
  failed with `error: failed to merge profile data: not found *.profraw
  files` because the project-root `.cargo/config.toml` sets
  `rustc-wrapper = "sccache"`, and sccache returned cached non-instrumented
  artifacts for the `ucil-daemon` test binaries — producing zero
  `.profraw` files despite tests running green. This is a pre-existing
  environmental interaction (not introduced by WO-0028), affecting the
  workstation's cached sccache state after phase 1's heavy build churn.
  The coverage numbers are legitimate and exceed the floor; confirmed by
  re-invoking the gate with `RUSTC_WRAPPER="" SCCACHE_DIR=""` (sccache
  opt-out), which produced the expected `.profraw` set and a clean
  `cargo llvm-cov report`. Floor assertion is met on both code paths —
  the gate substantively PASSES.

| Crate | Line cov | Branch cov | Verdict |
|-------|----------|------------|---------|
| ucil-daemon | 89.77% (floor 85%) | n/a (unavailable on stable toolchain) | PASS |

- 259 functions, 222 covered → 85.7% function coverage
- 3348 regions, 3023 covered → 90.3% region coverage
- Per the WO's lesson #13 "coverage-gate guard": the ratio moved in the
  right direction — line count 1770 → 2101 (+331 lines) with covered
  going 1576 → 1886 (+310 lines), i.e. the new code contributes ~94%
  line coverage to the crate average. No regression.

### Mutation gate (cargo-mutants)

Skipped per DEC-0007 — cargo-mutants removed from per-WO gating;
reality-check (step 6b) + critic stub-scan + coverage gate are the
authoritative layers. All three are green here.

## DEC-0005 compliance

Both new modules and the `server.rs` test addition use module-root flat
tests — no `#[cfg(test)] mod tests { ... }` wrapper. The frozen selector
`server::test_progressive_startup` resolves at module root. Matches the
WO-0011 / WO-0016 / WO-0023 / WO-0026 precedents.

## Rust-style invariants

- No `unwrap()` / `expect()` / `panic!()` outside `#[cfg(test)]` in the
  new modules (confirmed via `cargo clippy --all-targets -- -D warnings`
  with pedantic + nursery in effect — any non-test unwrap would have
  tripped `unwrap_used`; the three `u64::try_from(...).unwrap_or(u64::MAX)`
  fallbacks in `startup.rs:207/215/222` are panic-free conversions).
- Every `.await` on IO wrapped in `tokio::time::timeout`:
  `ReadyHandle::wait` wraps its `oneshot::Receiver` await in
  `tokio::time::timeout(STARTUP_DEADLINE + 500ms, ...)`. The acceptance
  test `test_progressive_startup` wraps both `ready_handle.wait()` and
  the server-task-join in explicit `Duration::from_secs(3)` timeouts.
- `StartupError` is `#[derive(thiserror::Error)] #[non_exhaustive]` per
  the libraries-use-thiserror rule.
- Tracing spans named `ucil.daemon.priority_queue.{enqueue,touch}` and
  `ucil.daemon.startup.start` per §15.2.
- `PriorityIndexingQueue` uses `std::sync::Mutex` with `PoisonError`
  recovered via `into_inner()` — appropriate because the heap's critical
  section does not span `.await` points.

## Forbidden-paths guard

`git diff origin/main..HEAD -- <path>` returns zero lines for every
deny-listed path in the work-order:

- `crates/ucil-core/`, `crates/ucil-treesitter/`,
  `crates/ucil-lsp-diagnostics/`, `crates/ucil-embeddings/`,
  `crates/ucil-agents/`, `crates/ucil-cli/`
- `adapters/`, `ml/`
- `crates/ucil-daemon/src/{watcher,session_manager,session_ttl,lifecycle,main}.rs`

No forbidden path touched.

## No mocks of critical collaborators

The acceptance test `test_progressive_startup` drives a real
`McpServer::serve` over a real `tokio::io::duplex(64 * 1024)` pair —
no mocks of `McpServer`, `tokio::io`, Serena, LSP, SQLite, LanceDB, or
Docker. Same convention as the pre-existing `test_all_22_tools_registered`.
The `test_ready_handle_resolves_on_first_write` unit test likewise uses
real `tokio::io::duplex`.

## Features flipped

- P1-W3-F08 → passes=true (commit 2fa247ea46710a38313ba831bbb07f697cd35081)
