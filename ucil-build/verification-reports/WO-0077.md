# Verification Report: WO-0077

**Verifier session**: vrf-WO-0077-19139f01-bc03-42c5-850f-c2c7a61adf3f
**Branch**: feat/WO-0077-mcp-pytest-runner-plugin-manifest
**HEAD**: dbe4558bd88eb2ebf7084b3ed4abd30053ede690
**Verified at**: 2026-05-08T10:47:22Z
**Verdict**: PASS

## Scope

WO-0077 is the G8 (Testing+CI) external-binary plugin manifest pass for Phase 3
Week 11 — `plugins/testing/mcp-pytest-runner/plugin.toml` (P3-W11-F08), its
devtools install hint, an end-to-end `crates/ucil-daemon/tests/g8_plugin_manifests.rs`
integration test exercising a real `PluginManager::health_check_with_timeout`
round-trip via `uvx mcp-pytest-runner@0.2.1`, and a replacement for the
TODO-stub acceptance script at `scripts/verify/P3-W11-F08.sh` that pairs an MCP
`tools/list` smoke with a tool-level pytest-discover + selective-run smoke
against `tests/fixtures/python-project`. P3-W11-F07 (test-runner-mcp) is
explicitly EXCLUDED per DEC-0021 — no canonical upstream is published.

5 files changed, 1151 insertions(+), 4 deletions(-). 5 atomic commits, 0 merges.

## Diff

| File | Status | LOC |
|------|--------|-----|
| `plugins/testing/mcp-pytest-runner/plugin.toml` | new | 186 |
| `scripts/devtools/install-mcp-pytest-runner.sh` | new | 104 |
| `crates/ucil-daemon/tests/g8_plugin_manifests.rs` | new | 200 |
| `scripts/verify/P3-W11-F08.sh` | TODO stub → real | +468 |
| `ucil-build/work-orders/0077-ready-for-review.md` | new | 189 |

## Criteria

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
|  1 | `test -f plugins/testing/mcp-pytest-runner/plugin.toml` | PASS | manifest present |
|  2 | TOML semantic checks (name, category, transport.type, testing.* prefix) | PASS | python tomllib parse OK |
|  3 | no moving tags (`main\|latest\|master\|head\|develop\|dev\|nightly`) | PASS | pinned `0.2.1` |
|  4 | `test -x scripts/devtools/install-mcp-pytest-runner.sh` | PASS | executable bit set |
|  5 | install script runs cleanly, exits 0 | PASS | warmed uvx cache |
|  6 | `test -f crates/ucil-daemon/tests/g8_plugin_manifests.rs` | PASS | file present |
|  7 | `mod g8_plugin_manifests { ... }` at file root | PASS | line 86, no nested `mod tests` |
|  8 | `#[tokio::test]` count >= 1 | PASS | 1 hit |
|  9 | `! grep -qE 'std::process::Command' …g8_plugin_manifests.rs` | PASS | W1 lesson applied |
| 10 | `cargo test -p ucil-daemon --test g8_plugin_manifests g8_plugin_manifests::mcp_pytest_runner_manifest_health_check` matches `test result: ok\. 1 passed; 0 failed` | PASS | 0.28s on warm uv cache |
| 11 | `bash scripts/verify/P3-W11-F08.sh` exits 0 | PASS | tools/list 2 advertised; discover 159 nodes; execute 3/3 passed |
| 12 | no `mock\|fake\|stub` words in production-side files | PASS | only `Mocking … is forbidden` rustdoc negative-assertion in test file (exempt per WO-0048/0072/0075/0076 §executor) |
| 13 | TODO-stub literal removed from `scripts/verify/P3-W11-F08.sh` | PASS | replaced by real implementation |
| 14 | `cargo clippy -p ucil-daemon --tests --all-targets -- -D warnings` | PASS | 0 warnings |
| 15 | `cargo fmt --all -- --check` | PASS | clean |
| 16 | `cargo test -p ucil-daemon --no-fail-fast` | PASS | full suite green (g8 + plugin_manifests + 27 doctests + unit) |
| 17 | `git diff main..feat -- crates/ucil-daemon/src/` empty | PASS | 0 lines |
| 18 | `git diff main..feat -- crates/ucil-core/src/` empty | PASS | 0 lines |
| 19 | `git diff main..feat -- tests/fixtures/` empty | PASS | 0 lines |
| 20 | `git diff main..feat -- ucil-build/feature-list.json …schema.json …master-plan.md` empty | PASS | 0 lines |
| 21 | `git diff main..feat -- crates/ucil-daemon/tests/g{3,4,5,6,7}_plugin_manifests.rs plugin_manifests.rs` empty | PASS | 0 lines (peer suite isolation) |
| 22 | `git diff main..feat -- scripts/verify/P3-W11-F07.sh` empty | PASS | 0 lines (F07 deferred per DEC-0021) |
| 23 | substantive coverage gate (`env -u RUSTC_WRAPPER cargo llvm-cov`) | PASS | **89.663%** line coverage (≥ 85% floor) |
| 24 | `git log feat ^main --merges \| wc -l == 0` | PASS | 0 merges, linear history |
| 25 | trailer audit `Work-order: WO-0077` | PASS | 5 commits |
| 26 | trailer audit `Feature: P3-W11-F08` | PASS | 5 commits |

## Mutation checks (M1 + M2)

Both pre-baked in WO scope_in #5/#6 with file/line/restore/md5-confirm protocol.

### M1 — `transport.command` poison (uvx → ENOENT)

```
md5 pre:    9d23c419983f5f675aeda44e5e9a87ca  plugins/testing/mcp-pytest-runner/plugin.toml
mutation:   command = "uvx"  →  command = "/__ucil_test_nonexistent_M1__"
selector:   cargo test -p ucil-daemon --test g8_plugin_manifests g8_plugin_manifests::mcp_pytest_runner_manifest_health_check
result:     FAILED — panicked at crates/ucil-daemon/tests/g8_plugin_manifests.rs:162:14
            "health-check mcp-pytest-runner MCP server: Spawn { command: \"/__ucil_test_nonexistent_M1__\",
             source: Os { code: 2, kind: NotFound, message: \"No such file or directory\" } }"
restore:    git checkout -- plugins/testing/mcp-pytest-runner/plugin.toml
md5 post:   plugins/testing/mcp-pytest-runner/plugin.toml: OK
```

`PluginError::Spawn { source: Os { code: 2, kind: NotFound } }` chain is the
exact shape prescribed by WO-0072/0076 §10. This proves the integration test
genuinely launches the upstream binary as a subprocess (the warm uv cache makes
the green path fast — ~0.28s — but the subprocess is real, as M1 confirms).

### M2 — expected-tool-name regression (`discover_tests` → `NON_EXISTENT_TOOL_M2`)

```
md5 pre:    001e36487b35bd72c486ea624c559ee4  crates/ucil-daemon/tests/g8_plugin_manifests.rs
mutation:   "discover_tests"  →  "NON_EXISTENT_TOOL_M2"  (1 occurrence — assertion literal only)
selector:   cargo test -p ucil-daemon --test g8_plugin_manifests g8_plugin_manifests::mcp_pytest_runner_manifest_health_check
result:     FAILED — panicked at crates/ucil-daemon/tests/g8_plugin_manifests.rs:193:9
            (SA1) expected `discover_tests` tool in advertised set;
                  got: ["execute_tests", "discover_tests"];
                  want: "discover_tests"
restore:    git checkout -- crates/ucil-daemon/tests/g8_plugin_manifests.rs
md5 post:   crates/ucil-daemon/tests/g8_plugin_manifests.rs: OK
```

SA1-tagged panic body emitted exactly as prescribed (DEC-0007 SA-numbered format
+ WO-0072/0076 §11 verbatim shape). The `got:` field captures the live
`tools/list` reply verbatim — `["execute_tests", "discover_tests"]` — confirming
the executor's live-capture protocol per WO-0074/0075/0076 §executor #1 was
honoured (snake_case-as-emitted-by-upstream, no kebab-case translation).

| Mutation | Pre-md5 | Post-md5 OK | Stashed → fail | Restored → pass |
|----------|---------|-------------|----------------|-----------------|
| M1 (transport.command poison) | yes | yes | yes | yes |
| M2 (expected-tool-name regression) | yes | yes | yes | yes |

After both restorations the integration test was re-run and returned `1 passed;
0 failed; 0 ignored; 0 measured` — clean baseline.

## Reality-check (informational)

`scripts/reality-check.sh P3-W11-F08` exited 0 with both phases reporting OK
(stash → fail; restore → pass). The script also encountered the documented
pre-existing-stash bug per WO-0077 scope_out #15 (auto-popped a stale stash from
a prior session, leaving `ucil-build/verification-reports/coverage-ucil-daemon.md`
in a `UU` state). Resolved by `git checkout HEAD --`; the conflict file is a
verification-reports artifact and unrelated to WO-0077 source. M1/M2 in-place
mutation contract is the authoritative anti-laziness layer per scope_out #15.

## Stub scan

Production-side files (`plugins/testing/mcp-pytest-runner/plugin.toml`,
`scripts/devtools/install-mcp-pytest-runner.sh`, `scripts/verify/P3-W11-F08.sh`)
and the new test file `crates/ucil-daemon/tests/g8_plugin_manifests.rs` are
clear of `todo!()`, `unimplemented!()`, `raise NotImplementedError`, single-line
`pass` function bodies, and bare-literal default returns.

The only `pass` literals in changed files appear at
`scripts/verify/P3-W11-F08.sh:448, 456, 460` — each inside a Python
`except Exception:` cleanup block within the embedded tool-level smoke (close
stdin / terminate / kill / close logfile, swallow exceptions). These are
legitimate exception-swallow pass statements in finally blocks, not stub
function bodies; the verifier rule targets stubbed implementations.

The only `Mocking` literal in the test file is the rustdoc negative-assertion
"Mocking … is forbidden" (lines 11-15) — exempt per WO-0048 line 363 +
WO-0072/0075/0076 §executor "Negative assertion forbidding mocking, NOT a
mock implementation".

## Quality gates

| Crate | `coverage-gate.sh ucil-daemon 85 75` | Substantive line cov (`env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --tests --summary-only --json`) | Verdict |
|-------|--------------------------------------|-----------------------------------------------------------------------------------------------------------------|---------|
| `ucil-daemon` | exit 1 (`failed to merge profile data: not found *.profraw files in target` — known harness bug per WO-0077 scope_out #16, sccache RUSTC_WRAPPER interaction, now 33 WOs deep, surfaces intermittently per WO-0067..WO-0076 standing protocol) | **89.663%** line coverage; floor 85% | PASS via substantive measurement |

`scripts/verify/coverage-gate.sh ucil-daemon 85 75` exited 1 with the same
profraw-merge failure pattern surfaced in WO-0070..WO-0076. The gate's two-phase
invocation (`cargo llvm-cov show-env --export-prefix` + `cargo test -p <crate>`
+ `cargo llvm-cov report`) interacts with the workspace `.cargo/config.toml`'s
`rustc-wrapper = "sccache"` in a way that produces no profraw data after the
profraw-prune step. Per the WO-0077 scope_out #16 standing protocol (carried
verbatim from WO-0067..WO-0076), the substantive AC23 line-coverage check via
the `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --tests
--summary-only --json | jq '.data[0].totals.lines.percent'` workaround is the
authoritative measurement. Result: 89.663% (≥ 85% floor per master-plan §15.4).
This is the same workaround applied by the WO-0070/0072/0073/0074/0075/0076
verifier reports.

## Live tool name capture

The integration test asserts the canonical tool name `discover_tests` is in the
live `tools/list` reply. The M2 mutation's `got:` field captured the verbatim
reply: `["execute_tests", "discover_tests"]` — matching the executor's RFR
`§Live tool name capture` documentation (`mcp-pytest-runner` v0.2.1 advertises
two tools: `execute_tests` and `discover_tests`). Snake_case-as-emitted-by-
upstream was correctly preserved (no kebab-case translation) per WO-0074/0075/
0076 §executor #1 lesson.

The verify script's tool-level smoke additionally exercised both tools end-to-
end against a tmpdir copy of `tests/fixtures/python-project`:

- `discover_tests` → 159 canonical pytest node IDs (e.g.
  `tests/test_evaluator.py::test_evaluate_arithmetic`)
- `execute_tests` → 3-node subset run; total=3 passed=3 failed=0 skipped=0

These satisfy the F08 spec line "pytest hierarchical test discovery and
selective re-run by node ID verified on the fixture python-project" verbatim.

## Forbidden-paths discipline

All 13 forbidden-path globs return 0 lines from `git diff main..HEAD --`:

- `crates/ucil-daemon/src/` — empty
- `crates/ucil-core/src/` — empty
- `tests/fixtures/` — empty (read-only fixture; tmpdir copies live in tests/scripts)
- `ucil-build/feature-list.json`, `feature-list.schema.json`,
  `ucil-master-plan-v2.1-final.md` — all empty (oracle docs immutable)
- `crates/ucil-daemon/tests/g3,g4,g5,g6,g7_plugin_manifests.rs`,
  `crates/ucil-daemon/tests/plugin_manifests.rs` — all empty (peer suite isolation)
- `scripts/verify/P3-W11-F07.sh` — empty (F07 deferred per DEC-0021; TODO stub
  preserved as marker of deferral state)

DEC-0021 (test-runner-mcp deferral) is present at
`ucil-build/decisions/DEC-0021-defer-test-runner-mcp-plugin-no-canonical-upstream.md`
on `main` (planner-authored alongside this WO).

## Commit hygiene

5 atomic feature commits, all Conventional-Commit-formatted with
`Phase: 3` + `Feature: P3-W11-F08` + `Work-order: WO-0077` trailers:

```
dbe4558 chore(rfr):    mark WO-0077 ready-for-review
8342633 test(verify):  replace P3-W11-F08 TODO stub with real smoke
71b8158 test(daemon):  add g8_plugin_manifests health-check for mcp-pytest-runner
b4682f8 feat(devtools): add install-mcp-pytest-runner.sh warm-up script
ba2f5d2 feat(plugins-testing): add mcp-pytest-runner@0.2.1 plugin manifest
```

`git log feat ^main --merges | wc -l == 0`. No `--amend` / `--no-verify` /
`--force` traces in the branch history.

## Lessons confirmed applied (RFR audit)

- WO-0076 §planner: G7 → G8 paired-manifest template port; SOLO-FEATURE shape
  (test-runner-mcp peer deferred per DEC-0021, mirroring WO-0072 codegraph-
  context's F10-via-DEC-0019 deferral).
- WO-0076 §planner: DEC-0020 preemptive-deferral pattern → DEC-0021 lineage
  chain DEC-0019 → DEC-0020 → DEC-0021 cited in RFR §"Lessons applied".
- WO-0074/0075/0076 §executor #1: live `tools/list` capture before sealing
  assertion literal — confirmed via M2's `got:` field (`["execute_tests",
  "discover_tests"]`).
- WO-0075/0076 §executor W1: `tokio::process::Command` discipline — pre-emptive
  AC enforced; rustdoc rephrased to avoid the literal string per the
  pre-emptive carve-out.
- WO-0074/0075/0076 §executor #5: tmpdir-copy of `tests/fixtures/python-project`
  before invoking upstream — both verify script and (per documentation)
  test file follow the discipline.
- WO-0067..WO-0076 §verifier: standing AC23 substantive coverage measurement
  via `env -u RUSTC_WRAPPER cargo llvm-cov` — applied here, 89.663%.

## Features flipped

- P3-W11-F08 → `passes=true` (last_verified_commit=dbe4558bd88eb2ebf7084b3ed4abd30053ede690)
