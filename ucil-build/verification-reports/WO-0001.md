# Verification Report: WO-0001

**Verifier session**: 6c4f652d-b7fa-40fd-a006-15511bf9129c
**Branch**: feat/WO-0001-workspace-skeleton
**Verified at**: 2026-04-15T06:00:00Z
**Verdict**: PASS

---

## Environment

- Branch confirmed: `feat/WO-0001-workspace-skeleton` at HEAD `7f5650c7e96631c425199a82fe2f5c38bd312f34`
- Clean slate: `cargo clean` removed 887 files (355.6 MiB); `git clean -fdx -e ucil-build/ -e .env` removed adapters/dist/, ml/.ruff_cache/, ml/dist/
- Toolchain: `stable-x86_64-unknown-linux-gnu` (active, overridden by rust-toolchain.toml, components: rustfmt + clippy) ✓

---

## Criteria

| # | Criterion | Result | Duration | Notes |
|---|-----------|--------|----------|-------|
| 1 | `cargo build --workspace` exits 0 | PASS | 4.3s | All 7 crates compiled clean from scratch |
| 2 | `cargo clippy --workspace -- -D warnings` exits 0 | PASS | 2.1s | No warnings in any crate |
| 3 | `cargo fmt --all --check` exits 0 | PASS | 0.05s | All files already formatted |
| 4 | `cargo test -p ucil-core` exits 0 | PASS | 0.38s | 2 tests: version_is_semver, version_has_two_dots |
| 5 | `pnpm -C adapters install && pnpm -C adapters build` exits 0 | PASS | 0.58s | lockfile frozen; tsc compiled index.ts cleanly |
| 6 | `cd ml && uv build` exits 0 | PASS | 0.57s | Built ucil_ml-0.1.0.tar.gz and .whl |
| 7 | `bash scripts/verify/P0-W1-F10.sh` exits 0 | PASS | 0.003s | "OK: all §17 required directories are present" |

---

## Mutation checks

> **Note**: `scripts/reality-check.sh` has a bug — with `set -euo pipefail`, the
> `CHANGED_FILES=$(... | grep ...)` pipeline exits non-zero (grep returns 1 on no
> match), which causes an early silent exit. The script also picks the most recent
> commit with a matching `Feature:` trailer, which for both features is an
> administrative commit (ready-for-review marker) that contains no source files.
> Manual mutation checks were performed in lieu of the automated script.

| Feature | Stashed → fail? | Restored → pass? | Method | Verdict |
|---------|-----------------|-----------------|--------|---------|
| P0-W1-F01 | yes (removed `crates/ucil-core/src/lib.rs` → compile error E0433) | yes (2/2 tests pass) | manual mv/restore | OK |
| P0-W1-F10 | yes (removed `crates/` dir → script exits 1 "required directory missing: crates") | yes (script exits 0) | manual mv/restore | OK |

---

## Stub scan

Scanned: adapters/src/index.ts, crates/{ucil-agents,ucil-cli,ucil-core,ucil-daemon,ucil-embeddings,ucil-lsp-diagnostics,ucil-treesitter}/src/*.rs, ml/src/ucil_ml/__init__.py

- No `todo!()` found (ast-grep + grep)
- No `unimplemented!()` found
- No `NotImplementedError` or bare `pass` in Python files
- No weak assertions in tests (`version_is_semver` and `version_has_two_dots` make real semantic checks)

---

## Features flipped

- P0-W1-F01 → passes=true (commit 7f5650c7e96631c425199a82fe2f5c38bd312f34)
- P0-W1-F10 → passes=true (commit 7f5650c7e96631c425199a82fe2f5c38bd312f34)

---

## Known issues (non-blocking)

1. **reality-check.sh pipefail bug**: The script exits 1 silently when the grep
   pipeline finds no source files (no-match returns exit 1, which with `pipefail`
   propagates through the command substitution assignment). Recommend adding `|| true`
   or `|| echo ""` at end of the grep pipeline. Not a blocker for this WO.

2. **reality-check.sh wrong commit selection**: The script picks the most recent
   commit matching `Feature: <ID>`, but administrative commits (ready-for-review
   markers, critic reports) use multi-feature trailers and land last. The script
   should scan back for the most recent commit that actually changed source files.
   Not a blocker for this WO.

3. **Critic W-2**: commit 40c788a bundles three logical changes. Noted for future WOs.
