# Verification Report: WO-0015

**Verifier session**: vrf-ad53cb2d-0e1f-42f8-8f27-c9df0a6aea04
**Branch**: feat/WO-0015-lsp-diagnostics-client
**Verified at**: 2026-04-18T02:00:00Z
**HEAD**: b1129d8ff10918299de3f5d72cf1437304846e06
**Verdict**: PASS

## Scope

- Work-order: `ucil-build/work-orders/0015-lsp-diagnostics-client.json`
- Feature: P1-W5-F04 (`ucil-lsp-diagnostics::diagnostics` — LSP JSON-RPC client via `SerenaClient` trait)
- Files changed vs `origin/main`:
  - `Cargo.lock`
  - `Cargo.toml` (workspace — add `lsp-types`, `async-trait`)
  - `crates/ucil-lsp-diagnostics/Cargo.toml`
  - `crates/ucil-lsp-diagnostics/src/bridge.rs` (extended with `with_serena_client` / `diagnostics_client` / `NoLspServerConfigured`)
  - `crates/ucil-lsp-diagnostics/src/diagnostics.rs` (NEW — `SerenaClient` trait + `DiagnosticsClient`)
  - `crates/ucil-lsp-diagnostics/src/lib.rs` (re-exports extended)
  - `ucil-build/work-orders/0015-ready-for-review.md`

## Clean-slate setup

- `cargo clean` → 2605 files removed (1.4 GiB).
- `git clean -fdx -e ucil-build/ -e .env` → tree clean, no residue.
- Toolchain: `stable-x86_64-unknown-linux-gnu` (matches `rust-toolchain.toml` channel = "stable").

## Acceptance criteria (work-order)

| # | Criterion | Result | Duration | Notes |
|---|-----------|--------|----------|-------|
| 1 | `cargo nextest run -p ucil-lsp-diagnostics 'diagnostics::' --status-level=pass --hide-progress-bar` ≥ 5 PASS | PASS | 2.95s compile + 0.003s tests | 5 PASS lines (`test_no_lsp_configured_returns_error`, `test_call_hierarchy_outgoing_via_serena`, `test_diagnostics_via_serena`, `test_call_hierarchy_incoming_via_serena`, `test_type_hierarchy_supertypes_via_serena`), 4 skipped (bridge tests correctly filtered by prefix) |
| 2 | `cargo build -p ucil-lsp-diagnostics` no warnings | PASS | — | grep of `warning\|error` against build output: 0 matches |
| 3 | `cargo clippy -p ucil-lsp-diagnostics --all-targets -- -D warnings` | PASS | 1.90s | pedantic + nursery clean; 0 warnings |
| 4 | `cargo doc -p ucil-lsp-diagnostics --no-deps` no warnings | PASS | 0.44s | grep of `warning\|error`: 0 matches |
| 5 | `! grep -rn 'todo!\|unimplemented!\|#\[ignore\]' crates/ucil-lsp-diagnostics/src/` | PASS | — | 0 matches |
| 6 | `! grep -q 'ucil-daemon' crates/ucil-lsp-diagnostics/Cargo.toml` | PASS | — | 0 matches — DEC-0008 cycle-free dep respected |
| 7 | `grep -q 'pub fn new(serena_managed: bool) -> Self' crates/ucil-lsp-diagnostics/src/bridge.rs` | PASS | — | `bridge.rs:161: pub fn new(serena_managed: bool) -> Self` — DEC-0008 §Consequences freeze honoured |
| 8 | `grep -q 'with_serena_client' crates/ucil-lsp-diagnostics/src/bridge.rs` | PASS | — | 5 matches (constructor at line 189 + 4 doc cross-refs) |
| 9 | `grep -c 'tokio::time::timeout' crates/ucil-lsp-diagnostics/src/diagnostics.rs` ≥ 4 | PASS | — | 7 matches (4 dispatch methods + 3 test/doc refs) |
| 10 | `bash scripts/reality-check.sh P1-W5-F04` | PASS (manual) | — | Automated script tripped on the documented new-module zero-tests edge case (escalation `20260415-1630-reality-check-per-file-rollback.md`); verifier performed the manual two-state check per WO-0014 precedent. Results identical to what a clean reality-check would produce — see "Mutation checks" below. |

## Feature acceptance_tests (feature-list.json oracle)

| Feature | Selector | Result | Notes |
|---------|----------|--------|-------|
| P1-W5-F04 | `-p ucil-lsp-diagnostics diagnostics::` | PASS | 5/5 module-root tests pass (one per WO acceptance goal: four `test_*_via_serena` + `test_no_lsp_configured_returns_error`); 4 bridge tests correctly skipped by the prefix selector. |

## Mutation checks

Automated `scripts/reality-check.sh P1-W5-F04` aborted at the "zero tests with
code stashed" guard — a known false-positive for features whose tests live
inside the stashed files (module-level tests vanish with the module). This
matches the WO-0014 / P1-W5-F03 precedent documented at
`ucil-build/verification-reports/WO-0014.md:52-62` and the escalation
`20260415-1630-reality-check-per-file-rollback.md`. Verifier then
performed the manual mutation check: copy-aside → roll back to `origin/main`
→ re-run → restore → re-run.

| Feature | Stashed → fail? | Popped → pass? | Verdict |
|---------|-----------------|----------------|---------|
| P1-W5-F04 | YES — with `bridge.rs` + `lib.rs` rolled to `origin/main` and `diagnostics.rs` removed, `cargo nextest run -p ucil-lsp-diagnostics 'diagnostics::'` returned **0 tests to run** (the `diagnostics` module no longer exists; `error: no tests to run` from the harness is the new-module failure signal per reality-check.sh `grep -qE 'Running 0 tests\|...'`) | YES — after restoring the three files from `/tmp/vrf-wo0015-*.bak`, `cargo nextest run -p ucil-lsp-diagnostics 'diagnostics::'` returned 5/5 PASS + 4 skipped | OK |

Repro (manual):
```
# backup
cp crates/ucil-lsp-diagnostics/src/{bridge,diagnostics,lib}.rs /tmp/vrf-wo0015-*.bak
# roll back to origin/main pre-WO-0015
git show origin/main:crates/ucil-lsp-diagnostics/src/bridge.rs > crates/ucil-lsp-diagnostics/src/bridge.rs
git show origin/main:crates/ucil-lsp-diagnostics/src/lib.rs > crates/ucil-lsp-diagnostics/src/lib.rs
rm crates/ucil-lsp-diagnostics/src/diagnostics.rs
cargo nextest run -p ucil-lsp-diagnostics 'diagnostics::'    # 0 tests to run
# restore
cp /tmp/vrf-wo0015-{bridge,diagnostics,lib}.rs.bak crates/ucil-lsp-diagnostics/src/{bridge,diagnostics,lib}.rs
cargo nextest run -p ucil-lsp-diagnostics 'diagnostics::'    # 5/5 PASS
```

Working tree returned to `clean vs. HEAD` after restoration (`git status: nothing to commit`).

## Stub scan

No `todo!()`, `unimplemented!()`, `NotImplementedError`, `raise NotImplementedError`,
`.unwrap()`, `.expect()`, or single-`pass` bodies in changed files. Dispatch
methods each call `self.serena.<method>(…).await` inside
`tokio::time::timeout(Duration::from_millis(LSP_REQUEST_TIMEOUT_MS), ...)`
and lift `Elapsed` into `DiagnosticsClientError::Timeout` — real logic, not
stubs. `FakeSerenaClient` is a real `impl SerenaClient for …` inside
`#[cfg(test)]`, not a mock of Serena's MCP wire format (DEC-0008 §4).

## Quality gates

| Crate | Mutation score | Line cov | Branch cov | Verdict |
|-------|----------------|----------|------------|---------|
| ucil-lsp-diagnostics | n/a (DEC-0007: cargo-mutants removed from per-WO gate; reality-check.sh is the oracle — OK above) | 99.375% (floor 85%) | n/a (toolchain does not emit branch data) | PASS |

Coverage gate: `scripts/verify/coverage-gate.sh ucil-lsp-diagnostics 85 75`
→ exit 0, line=99.375%, 318/320 lines, 40/40 functions, 40/40 instantiations.
Report: `ucil-build/verification-reports/coverage-ucil-lsp-diagnostics.md`.

## Features flipped

- P1-W5-F04 → passes=true (commit b1129d8ff10918299de3f5d72cf1437304846e06)
