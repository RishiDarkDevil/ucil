# Verification Report: WO-0059

**Verifier session**: vrf-0f42e2ee-649b-41e0-b983-5a6b98344270
**Branch**: `feat/WO-0059-ucil-embeddings-coderankembed`
**HEAD**: `6cd94abde6e74a55d153c56bbdcb71f3dd9d10df` (matches origin)
**Verified at**: 2026-05-07T00:00:00Z
**Verdict**: **REJECT**

## TL;DR

All 30 work-order acceptance criteria pass (with three soft-fails accepted-
as-documented per WO-0058 line 568 precedent). All three pre-baked
mutations M1/M2/M3 panic correctly. **The verifier-universal coverage
gate fails: line 80% < floor 85%.** Per `.claude/agents/verifier.md` step 7
("If the gate exits non-zero → reject … Do not flip the feature even if
step 6 passed"), this is a hard reject.

## Setup

- `git worktree list` shows `/home/rishidarkdevil/Desktop/ucil-wt/WO-0059`
  on branch `feat/WO-0059-ucil-embeddings-coderankembed` matching the
  WO's `worktree_branch`. Working tree clean before verification, branch
  up-to-date with origin.
- `rust-toolchain.toml` channel = `stable`, components `rustfmt + clippy`.
  `rustup show` confirms `stable-x86_64-unknown-linux-gnu` overrides
  active.
- `cargo clean` removed 4.1 GiB before verification began (the AC11/30
  workspace test pulled the cache up significantly first time; clean
  re-runs are stable).

## Acceptance Criteria

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| AC01 | `grep -cE '^tokenizers = ' Cargo.toml` = 1 | PASS | line 205 |
| AC02 | `grep -cE '^tokenizers\.workspace = true' crates/ucil-embeddings/Cargo.toml` = 1 | PASS | line 14 |
| AC03 | `models.rs` declares `pub struct CodeRankEmbed` | PASS | line 214 |
| AC04 | `pub const EMBEDDING_DIM: usize = 768;` | PASS | line 88 |
| AC05 | `#[non_exhaustive]` + `pub enum CodeRankEmbedError` | PASS | 3 grep hits (≥2) |
| AC06 | `pub fn (load|embed)(` count = 2 | PASS | both at column 4 |
| AC07 | `pub mod models;|pub use models::` count ≥2 | PASS | 2 hits |
| AC08 | `^fn test_coderankembed_inference` at module root | PASS | line 436, column 0 |
| AC09 | `assert_eq!(.*\.len(),\s*768` | PASS | line 483 (test) + line 341 (rustdoc) |
| AC10 | `test -x scripts/devtools/install-coderankembed.sh` | PASS | mode 100755 |
| AC11 | `test -x scripts/verify/P2-W8-F02.sh` | PASS | mode 100755 |
| AC12 | `cargo build --workspace --tests` | PASS | exit 0 |
| AC13 | `cargo clippy -p ucil-embeddings -- -D warnings` | PASS | exit 0 |
| AC14 | `cargo fmt -- --check` | PASS | exit 0 |
| AC15 | `bash scripts/devtools/install-coderankembed.sh` | PASS | idempotent (sha256 match short-circuit) |
| AC16 | `bash scripts/verify/P2-W8-F02.sh` | PASS | `[OK] P2-W8-F02` |
| AC17 | `cargo test -p ucil-embeddings models::test_coderankembed_inference -- --nocapture` | PASS | `1 passed; 0 failed` in 0.43s |
| AC18 (M1) | `load` body neutered → test fails | PASS | panics at `models.rs:461:10` (SA1 `expect("CodeRankEmbed::load on real ml/models/coderankembed: MissingModelFile { path: \"\" }")`) |
| AC19 (M2) | `embed` body neutered → test fails | PASS | panics at `models.rs:486:5` (SA4 `assertion left == right failed; left: 0; right: 768`) |
| AC20 (M3) | `EMBEDDING_DIM = 768 → 100` → test fails | PASS | panics at `models.rs:475:10` (SA3 `expect("CodeRankEmbed::embed on real Rust snippet: DimensionMismatch { expected: 100, got: 768 }")`) |
| AC21 | `git diff --name-only main...HEAD` allow-list | PASS | 9 paths, all in-allow-list |
| AC22 | commit subject ≤70 chars | SOFT-FAIL → ACCEPTED | `c4d375a` 73 chars; constraint chain documented per WO-0058 line 568 precedent |
| AC23 | Phase / Feature / Work-order trailer | PASS | 10/10/10 |
| AC24 | commit count ∈ [3, 8] | SOFT-FAIL → ACCEPTED | 10 commits (2 over soft ceiling); two extra commits explained in RFR (refactor for upstream dual-input model + RFR-amendment for AC24 deviation, both forced by `--amend after push` / `--force` forbids) |
| AC25 / AC28 | rustdoc unbacked-uppercase grep = 0 | SOFT-FAIL → ACCEPTED | 10 hits (filter false-positives on hyphenated backticked refs like `` `P2-W8-F02` ``); `cargo clippy` (`clippy::doc_markdown` lint) is the authoritative gate and passes clean. WO-0058 onnx_inference.rs returns 11 with the same grep — strictly worse — and was accepted. |
| AC26 | `git ls-files ml/models/coderankembed/` returns only `.gitignore` | PASS | the 138MB binary stays uncommitted |
| AC27 | RFR marker exists | PASS | `ucil-build/work-orders/0059-ready-for-review.md` |
| AC29 | F01 frozen test still passes | PASS | `1 passed; 0 failed` |
| AC30 | workspace tests still pass | PASS | every `test result:` line `0 failed`; one pre-existing `1 ignored` in fixture::rust_project_loads (unrelated) |

## Mutation checks

| Layer | Result | Notes |
|-------|--------|-------|
| `scripts/reality-check.sh P2-W8-F02` (verifier step 6b) | STRUCTURAL FAIL — accepted | Stashing `models.rs` removes both impl AND test (DEC-0007 frozen-selector module-root placement). Per WO-0058 verification-report (lines 73-79) precedent, the per-WO M1/M2/M3 mutations cover the anti-laziness signal at function-body granularity that the reality-check script cannot when test + impl share a file. |
| Per-WO M1 (`load` early-Err) | PASS | SA1 panic at `models.rs:461:10` — `MissingModelFile { path: "" }` |
| Per-WO M2 (`embed` early-empty-Vec) | PASS | SA4 panic at `models.rs:486:5` — `assertion left == right failed; left: 0; right: 768` |
| Per-WO M3 (`EMBEDDING_DIM = 768 → 100`) | PASS | SA3 panic at `models.rs:475:10` — `DimensionMismatch { expected: 100, got: 768 }` |
| Restore via `cp /tmp/wo-0059-models-original.rs crates/ucil-embeddings/src/models.rs` after each | PASS | `diff -q` against pre-verification snapshot is empty; AC17 reruns green |

## Stub scan

`rg -nE 'todo!\(\)|unimplemented!\(\)|NotImplementedError|^\s*pass$|raise NotImplementedError'` against `crates/ucil-embeddings/src/models.rs` + the two new shell scripts returns **zero hits**. No `unwrap()` outside `#[cfg(test)]`. No `unsafe` blocks. Critic report (`ucil-build/critic-reports/WO-0059.md`) corroborates.

## Quality gates

| Crate | Mutation score | Line cov | Branch cov | Verdict |
|-------|----------------|----------|------------|---------|
| ucil-embeddings | n/a (DEC-0007 removed cargo-mutants per-WO; M1/M2/M3 cover anti-laziness) | **80%** (156/195 lines, **floor 85%**) | unavailable (toolchain) | **FAIL** |

Per-file breakdown:

| File | Lines | Coverage |
|------|-------|----------|
| `crates/ucil-embeddings/src/onnx_inference.rs` (pre-existing, WO-0058) | 82 | 87.80% (unchanged) |
| `crates/ucil-embeddings/src/models.rs` (NEW, WO-0059) | 113 | 74.34% |

The new `models.rs` ships only one happy-path test, leaving 23 reachable
error-return branches across 7 `CodeRankEmbedError` variants uncovered.
Detailed line-by-line breakdown lives in `ucil-build/rejections/WO-0059.md`.

## Verdict

**REJECT** — coverage gate exits 1.

Per `.claude/agents/verifier.md` step 7: "If the gate exits non-zero →
reject (same path as step 8 below). Do not flip the feature even if step
6 passed."

Feature `P2-W8-F02` `attempts` increments to 1 via `scripts/flip-feature.sh
P2-W8-F02 fail`. Rejection report at `ucil-build/rejections/WO-0059.md`
lists three illustrative additional `#[test]` functions (negative-path
tests for `load`'s missing-file branches and the `Tokenizer`-error
branch) that should lift coverage above 85% in a single follow-up
commit.

## Files written by this verifier session

- `ucil-build/verification-reports/WO-0059.md` (this file)
- `ucil-build/rejections/WO-0059.md` (rejection narrative)
- `ucil-build/verification-reports/coverage-ucil-embeddings.md` (regenerated by `coverage-gate.sh`)
- `ucil-build/feature-list.json` (mutated by `scripts/flip-feature.sh P2-W8-F02 fail`: `passes` stays false, `attempts` 0 → 1, `last_verified_*` updated)

## Notes for the next attempt

- The reality-check.sh structural-fail is permanent for this WO's design (DEC-0007 keeps the test colocated with the impl). The M1/M2/M3 per-WO mutations remain the authoritative anti-laziness layer; they passed cleanly this attempt.
- The coverage shortfall is a test-density issue, not a design flaw. 2-3 negative-path `#[test]` functions in `models.rs` should fix it — see the "Next step" section of `ucil-build/rejections/WO-0059.md` for concrete proposals.
- `tempfile = "3"` is already a workspace dep (used by other crates); adding `tempfile.workspace = true` to `crates/ucil-embeddings/Cargo.toml [dev-dependencies]` is the only cargo wiring needed. That single line is in-allow-list.
- The 8 (now 11 with this verifier's rejection + verification report + flip-feature commits) committed paths remain disjoint from forbidden_paths and from in-flight WO-0057's allow-list.
