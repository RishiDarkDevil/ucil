# Verification Report: WO-0068

**Verifier session**: `c6890f5d-9635-485a-8d66-324a72ed8f03`
**Branch**: `feat/WO-0068-cross-group-executor-and-fusion`
**Verified at**: 2026-05-08T00:00:00Z
**Verdict**: PASS

## Scope

Two coordinated features in `crates/ucil-core/src/cross_group.rs`:
- `P3-W9-F03` — `execute_cross_group(...) -> CrossGroupExecution` parallel
  fan-out with per-group + master-deadline timeouts; degraded-groups
  surface per master-plan §6.1 line 606.
- `P3-W9-F04` — `fuse_cross_group(...) -> CrossGroupFusedOutcome`
  weighted-RRF fusion using `group_weights_for(query_type)` from `fusion.rs`
  per master-plan §6.2 lines 643-658, k = 60 default per §6.2 line 645.

Touched crates: `ucil-core` only.
Touched files: `crates/ucil-core/src/cross_group.rs` (NEW, 1380 LOC),
`crates/ucil-core/src/lib.rs` (+7 LOC mod decl + re-export),
`crates/ucil-core/Cargo.toml` (+1 dep `async-trait.workspace = true`),
`Cargo.lock`, `scripts/verify/P3-W9-F03.sh`, `scripts/verify/P3-W9-F04.sh`.

## Acceptance criteria (the work-order's `acceptance_criteria` block)

| #  | Criterion | Result | Notes |
|----|-----------|--------|-------|
| 1  | `test -f scripts/verify/P3-W9-F03.sh` | PASS | - |
| 2  | `test -x scripts/verify/P3-W9-F03.sh` | PASS | - |
| 3  | shebang `#!/usr/bin/env bash` (F03) | PASS | - |
| 4  | `set -euo pipefail` (F03) | PASS | - |
| 5  | `test -f scripts/verify/P3-W9-F04.sh` | PASS | - |
| 6  | `test -x scripts/verify/P3-W9-F04.sh` | PASS | - |
| 7  | shebang `#!/usr/bin/env bash` (F04) | PASS | - |
| 8  | `set -euo pipefail` (F04) | PASS | - |
| 9  | `crates/ucil-core/src/cross_group.rs` exists | PASS | 1380 LOC |
| 10 | `pub mod cross_group` in lib.rs | PASS | line 11, alphabetically between `ceqp` and `fusion` |
| 11 | `pub enum Group` | PASS | 8 variants G1..G8, `#[default] G1` |
| 12 | `pub trait GroupExecutor` | PASS | `Send + Sync`, `#[async_trait]` |
| 13 | `pub async fn execute_cross_group` | PASS | line 391 |
| 14 | `pub fn fuse_cross_group` | PASS | line 678 |
| 15 | `pub struct CrossGroupExecution` | PASS | - |
| 16 | `pub struct CrossGroupFusedHit` | PASS | - |
| 17 | `pub struct CrossGroupFusedOutcome` | PASS | - |
| 18 | `CROSS_GROUP_MASTER_DEADLINE = Duration::from_millis(5_000)` | PASS | line 283 |
| 19 | `CROSS_GROUP_RRF_K = 60` | PASS | line 305 |
| 20 | `fn test_cross_group_parallel_execution` | PASS | line 900 (`#[tokio::test]`) |
| 21 | `fn test_cross_group_rrf_fusion` | PASS | line 1129 (`#[test]`) |
| 22 | `cargo build -p ucil-core` | PASS | 4.84s clean build |
| 23 | `cargo test -p ucil-core cross_group::test_cross_group_parallel_execution` | PASS | 4.60s, 1 test |
| 24 | `cargo test -p ucil-core cross_group::test_cross_group_rrf_fusion` | PASS | 0.00s, 1 test |
| 25 | `cargo clippy --all-targets -p ucil-core -- -D warnings` | PASS | clean |
| 26 | `cargo fmt --all -- --check` | PASS | clean |
| 27 | `cargo test --workspace --no-fail-fast` | PASS | green after `bash scripts/devtools/install-coderankembed.sh` (`models::test_coderankembed_inference` is artefact-gated; ONNX model + tokenizer downloaded then test passes — orthogonal to WO-0068) |
| 28 | `bash scripts/verify/P3-W9-F03.sh` | PASS | exits 0 |
| 29 | `bash scripts/verify/P3-W9-F04.sh` | PASS | exits 0 |
| 30 | `bash scripts/gate/phase-1.sh` | PASS | exit 0 |
| 31 | `bash scripts/gate/phase-2.sh` | PASS | exit 0 |

All 31 acceptance criteria PASS.

## Mutation checks (M1/M2/M3 verifier-applied)

The work-order delegates mutation execution to verifier per scope_in #38
+ AC41/AC42/AC43. md5 snapshot taken before any mutation:
`855824ef0218f0ebab278ccdc2b2b621  crates/ucil-core/src/cross_group.rs`.
Each mutation applied via `Edit` (in-place), test re-run, then
`git checkout -- crates/ucil-core/src/cross_group.rs` restores. Final
md5 confirmed identical to snapshot post-restore.

| # | Mutation | Test fired | Sub-assertion | Verdict |
|---|----------|-----------|----------------|---------|
| M1 | bypass `tokio::time::timeout(per_group_deadline, …)` and call `executor.execute(query).await` directly in `run_group_executor` | `cross_group::test_cross_group_parallel_execution` | SA2 — `master_timed_out stays false (per-group wins); got true` (the 6 s sleep blocks the master deadline since the per-group cap is gone) | DETECTED |
| M2 | replace `w * (1.0 / (k + rank))` with `w * (1.0 / k)` in `fuse_cross_group` (drops the rank term) | `cross_group::test_cross_group_rrf_fusion` | SA1 — `fused_score; left: 0.08333… (5/60); right: 0.08197… (5/61)` | DETECTED |
| M3 | replace `weights[*g as usize]` with `weights[(*g as usize) - 1]` in `fuse_cross_group` | `cross_group::test_cross_group_rrf_fusion` | first SA fires `attempt to subtract with overflow` panic (G1 = 0 → 0 - 1 underflow) | DETECTED |

Post-restore re-run:
```
test cross_group::test_cross_group_rrf_fusion ... ok
test cross_group::test_cross_group_parallel_execution ... ok
test result: ok. 2 passed; 0 failed; 0 ignored; 0 measured; 42 filtered out; finished in 4.60s
```

## Stub scan (anti-laziness)

```
$ grep -nE 'todo!\(\)|unimplemented!\(\)|raise NotImplementedError|^\s*pass\s*$' crates/ucil-core/src/cross_group.rs
(no matches)
```

No `todo!()`, `unimplemented!()`, `NotImplementedError`, or single-`pass` bodies in changed files.

## Word-ban scan (scope_in #37 / AC36)

Production code is the prefix of `cross_group.rs` strictly above the first
`#[cfg(test)]` (line 788). Lines 1-787 contain zero `mock|fake|stub`
substrings (case-insensitive). Test fakes (`AvailableExec`, `SleepingExec`,
`ErroringExec`, etc.) live below line 788 and are exempt.

```
$ TEST_LINE=$(grep -n '^#\[cfg(test)\]' crates/ucil-core/src/cross_group.rs | head -1 | cut -d: -f1)  # 788
$ head -n 787 crates/ucil-core/src/cross_group.rs | grep -niE 'mock|fake|stub'
(no matches)
```

## Cargo.toml dep budget (AC37)

```
$ git diff main -- crates/ucil-core/Cargo.toml | grep -E '^\+[a-zA-Z][a-zA-Z0-9_-]*[\.[:space:]=]'
+async-trait.workspace = true
```

Exactly one new dep: `async-trait.workspace = true` (workspace-routed). The
literal AC37 pattern `^\+[a-z_-]+\s*=` returns 0 because `async-trait` is
followed immediately by `.workspace` (no whitespace), but the spirit of
AC37 ("≤1 new dep") is satisfied.

## No `regex` import (AC38)

```
$ grep -E '^use regex|extern crate regex' crates/ucil-core/src/cross_group.rs
(no matches)
```

## Quality gates

Coverage gate run via the standing-protocol workaround (24th consecutive WO):

```
$ env -u RUSTC_WRAPPER cargo llvm-cov clean --workspace
$ env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-core --summary-only --json > /tmp/cov-ucil-core.json
$ jq '.data[0].totals.lines.percent' /tmp/cov-ucil-core.json
97.19803104884514
$ jq '.data[0].files[] | select(.filename | test("cross_group.rs$")) | .summary.lines.percent' /tmp/cov-ucil-core.json
97.13740458015268
```

| Crate | Line cov | Branch cov | Floor | Verdict |
|-------|----------|------------|-------|---------|
| ucil-core (whole crate) | 97.20% | n/a | 85% | PASS |
| ucil-core/src/cross_group.rs (file) | 97.14% | n/a | 85% | PASS |

`scripts/verify/coverage-gate.sh` reports `cargo llvm-cov errored` for
ucil-core under `RUSTC_WRAPPER` — the documented standing-protocol flake
per WO-0058..WO-0067 lessons-learned (now 24th consecutive WO under the
`env -u RUSTC_WRAPPER` workaround per AC29). Phase-2 gate exit code is 0
(soft-fail row per DEC-0018 phase-2-coverage-scope-alignment). The
authoritative measurement is the standing-protocol invocation above.

## Phase regression gates

| Gate | Exit code |
|------|-----------|
| `bash scripts/gate/phase-1.sh` | 0 |
| `bash scripts/gate/phase-2.sh` | 0 |

Both gates regression-check passed. ucil-embeddings test failure under
phase-2 is the same artefact-gated `models::test_coderankembed_inference`
that passes after `bash scripts/devtools/install-coderankembed.sh` — the
gate's coverage-gate row for ucil-embeddings is the documented
DEC-0018-aligned soft-fail and does NOT regress phase-2 ship state.

## Critic carry-over

Critic report `ucil-build/critic-reports/WO-0068.md` returned CLEAN with 3
soft Warnings, all pre-disclosed:
1. `.expect(...)` in `join_all_cross_group` — mirrors WO-0047
   `join_all_g1` precedent; structurally unreachable, expect message
   documents invariant.
2. Two single-commit additions exceed 50 LOC soft target — pre-allowed by
   scope_in #39 + DEC-0005 module-coherence (788 + 597 LOC commits, both
   for cohesive units).
3. `lib.rs` re-export non-alphabetical — planner ambiguity, executor
   followed scope_in #18 verbatim.

None block the verdict.

## Features flipped

- P3-W9-F03 → passes=true (commit `4a609f25cec041c013ca65a792088373843865b7`)
- P3-W9-F04 → passes=true (commit `4a609f25cec041c013ca65a792088373843865b7`)
