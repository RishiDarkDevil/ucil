# Verification Report: WO-0080

**Verifier session**: vrf-90cea9cb-9e1e-4bdc-a805-aec80e6624be
**Branch**: `feat/WO-0080-ruff-quality-plugin-manifest`
**HEAD verified**: `66e1780ddb9ba9f9f397a410375552b419db00c2`
**Verified at**: 2026-05-08T15:00:00Z
**Verdict**: PASS

WO-0080 lands the Ruff (G7 Quality, P0 priority) external MCP plugin manifest
at `plugins/quality/ruff/plugin.toml` (P3-W11-F03), pinned to the immutable
`mcp-server-analyzer@0.1.2` PyPI release published by Anselmoo. Tier-1
install path per DEC-0023 §Decision point 2(Tier 1) — the Anselmoo wrapper
combining Ruff + Vulture into a single MCP server. Five linear commits on
the feat branch; critic CLEAN; reality-check tripped the documented
pre-existing-stash bug per WO-0079 scope_out #18 (informational; M1/M2
mutation contract is the load-bearing anti-laziness layer).

## Criteria

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| AC1 | `plugins/quality/ruff/plugin.toml` exists with `name="ruff"`, `transport.command` ∈ {`uvx`,`python`}, pinned `mcp-server-analyzer@<ver>`, `[capabilities].provides` ⊇ `quality.ruff.*`, `hot_cold=true`, `idle_timeout_minutes=10` | PASS | parses cleanly via `tomllib`; `name=ruff`, `version=0.1.2`, `transport.kind=stdio`, `provides=[quality.ruff.lint, quality.ruff.format]`, `hot_cold=true`, `idle_timeout_minutes=10` |
| AC2 | `scripts/devtools/install-ruff-mcp.sh` exists, executable (`chmod +x`), exits 0 unconditionally | PASS | `-rwxrwxr-x`; runs `uvx mcp-server-analyzer@0.1.2 --help` for warm-up; `exit 0` |
| AC3 | `scripts/verify/P3-W11-F03.sh` replaces TODO-stub, exercises cargo-test selector + tools/call ruff-check smoke; exits 0 on success | PASS | `[OK] P3-W11-F03`; tools/list returned 5 advertised tools; tools/call ruff-check returned 2 findings (first: rule=E401, line=8, file=`<stdin>`, message=`Multiple imports on one line`) |
| AC4 | `crates/ucil-daemon/tests/g7_plugin_manifests.rs` gains ONE new test `ruff_manifest_health_check` inside the existing `mod g7_plugin_manifests { ... }` block | PASS | `grep -c '^[[:space:]]*async fn '` returns 3 (eslint, semgrep, ruff); ruff test at line 452-530 |
| AC5 | `cargo test -p ucil-daemon --test g7_plugin_manifests g7_plugin_manifests::ruff_manifest_health_check` resolves uniquely + green | PASS | `1 passed; 0 failed; 0 ignored` in 0.60s after `cargo clean` baseline |
| AC6 | `cargo clippy -p ucil-daemon -- -D warnings` green; `cargo fmt --check -p ucil-daemon` green | PASS | clippy clean; fmt no-diff |
| AC7 | M1 (transport.command poison) FAILS the cargo-test selector with `PluginError::Spawn { Os { kind: NotFound } }` | PASS | verifier-side reproduced; see §"Mutation contract" below |
| AC8 | M2 (expected-tool-name regression) FAILS the cargo-test selector with structured panic carrying full live tools/list array | PASS | verifier-side reproduced; panic body emitted full tool list `["ruff-check", "ruff-format", "ruff-check-ci", "vulture-scan", "analyze-code"]` |
| AC9 | M1/M2 reversible via `git checkout --`; md5 round-trip OK | PASS | both files md5-identical to pre-mutation snapshots after restore |
| AC10 | RFR `ucil-build/work-orders/0080-ready-for-review.md` exists | PASS | -rw-rw-r--; carries summary + M1/M2 contracts + live tools/list capture + Tier decision + PyPI vs serverInfo.name doc |
| AC11 | `git log feat/WO-0080-... ^main --merges` empty | PASS | empty output; zero merge commits on feat |

All 11 work-order acceptance criteria green. 18 mechanical
`acceptance_criteria` (the WO's shell-executable list) all green:

| acceptance_criteria # | Result | Notes |
|------|--------|-------|
| 1 | `test -f plugins/quality/ruff/plugin.toml` | PASS |
| 2 | `grep -q '^name = "ruff"$' plugin.toml` | PASS |
| 3 | `grep -qE '^command = "(uvx\|python)"$'` plugin.toml | PASS |
| 4 | `grep -qE 'mcp-server-analyzer@[0-9]+\.[0-9]+\.[0-9]+'` (or Tier-2 vendor) | PASS — Tier-1 pin `mcp-server-analyzer@0.1.2` |
| 5 | `test -x scripts/devtools/install-ruff-mcp.sh` | PASS |
| 6 | `test -x scripts/verify/P3-W11-F03.sh` | PASS |
| 7 | `! grep -qE '^echo "TODO: implement acceptance test for P3-W11-F03"$'` | PASS — TODO-stub fully replaced |
| 8 | `! grep -qE 'std::process::Command'` on tests/g7_plugin_manifests.rs | PASS — tokio::process only |
| 9 | `! grep -qiE 'mock\|fake\|stub'` on production-side files | PASS |
| 10 | `! grep -qE '"(main\|latest\|master\|head\|develop\|dev\|nightly)"'` on plugin.toml | PASS — only frozen `0.1.2` tag |
| 11 | `! grep -qE 'unsafe[[:space:]]*\{[^}]*set_var'` on tests/g7_plugin_manifests.rs | PASS — line-based regex doesn't match the pre-existing semgrep `unsafe { set_var(...) }` block (split across lines); no NEW unsafe blocks introduced by this WO per scope_in #17 + #34 |
| 12 | `grep -qE '#\[tokio::test\][[:space:]]*async fn ruff_manifest_health_check'` | PASS |
| 13 | `cargo test ... ruff_manifest_health_check` summary line matches `1 passed` | PASS |
| 14 | `cargo clippy -p ucil-daemon -- -D warnings` | PASS |
| 15 | `cargo fmt --check -p ucil-daemon` | PASS |
| 16 | `scripts/verify/P3-W11-F03.sh` exits 0 | PASS — `[OK] P3-W11-F03` after end-to-end smoke |
| 17 | `test -f ucil-build/work-orders/0080-ready-for-review.md` | PASS |
| 18 | `git log feat ^main --merges` empty | PASS |

## Mutation contract (verifier-applied; both verified in-place)

md5 snapshots taken pre-mutation:
```
950d19de64c7b96641744807a7fbc067  plugins/quality/ruff/plugin.toml
edcd170540d683842f0bb756bf8acb4e  crates/ucil-daemon/tests/g7_plugin_manifests.rs
```

### M1 — `transport.command` poison (uvx → ENOENT)

Verifier-side reproduction:

```
md5 pre:    950d19de64c7b96641744807a7fbc067  plugins/quality/ruff/plugin.toml
mutation:   command = "uvx"  →  command = "/__ucil_test_nonexistent_M1__"
selector:   cargo test -p ucil-daemon --test g7_plugin_manifests g7_plugin_manifests::ruff_manifest_health_check
result:     FAILED — panicked at crates/ucil-daemon/tests/g7_plugin_manifests.rs:491:14
            "health-check ruff MCP server: Spawn { command: \"/__ucil_test_nonexistent_M1__\",
             source: Os { code: 2, kind: NotFound, message: \"No such file or directory\" } }"
restore:    git checkout -- plugins/quality/ruff/plugin.toml
md5 post:   plugins/quality/ruff/plugin.toml: OK (matches pre)
```

The `PluginError::Spawn { source: Os { code: 2, kind: NotFound } }` chain
matches the canonical shape prescribed by WO-0072/0076/0077/0079 §10. This
proves the integration test genuinely launches the upstream binary as a
subprocess.

### M2 — expected-tool-name regression (`ruff-check` → `NON_EXISTENT_TOOL_M2`)

Verifier-side reproduction (no operator-state gate — Ruff is self-resolving;
M2 is fully exercisable in-place, contrast with WO-0079 graphiti's M2 that
required FalkorDB):

```
md5 pre:    edcd170540d683842f0bb756bf8acb4e  crates/ucil-daemon/tests/g7_plugin_manifests.rs
mutation:   t == "ruff-check"  →  t == "NON_EXISTENT_TOOL_M2"
selector:   cargo test -p ucil-daemon --test g7_plugin_manifests g7_plugin_manifests::ruff_manifest_health_check
result:     FAILED — panicked at crates/ucil-daemon/tests/g7_plugin_manifests.rs:525:9
            "(SA3) expected `NON_EXISTENT_TOOL_M2` tool in advertised set;
             got: [\"ruff-check\", \"ruff-format\", \"ruff-check-ci\", \"vulture-scan\", \"analyze-code\"]"
restore:    git checkout -- crates/ucil-daemon/tests/g7_plugin_manifests.rs
md5 post:   crates/ucil-daemon/tests/g7_plugin_manifests.rs: OK (matches pre)
```

The structured panic carries the full live `tools/list` array — the SA3
panic body itself doubles as a diagnostic dump of the upstream tool surface
at the pinned version. This proves the test's tools/list assertion is
genuinely load-bearing on the live MCP handshake.

| Mutation | Pre-md5 | Post-md5 OK | Verifier-side reproduced? | Verdict |
|----------|---------|-------------|----------------------------|---------|
| M1 (transport.command poison) | yes | yes | yes (`NotFound` panic shape verified) | OK |
| M2 (expected-tool-name regression) | yes | yes | yes (5-tool live capture in panic body) | OK |

After M1 + M2 restoration, the integration test was re-run from a clean
state and returned `1 passed; 0 failed` — clean baseline preserved.

## Reality-check (informational)

`scripts/reality-check.sh P3-W11-F03` exited 1 with the documented
pre-existing-stash bug per WO-0079 scope_out #18 (carried 36 WOs deep from
WO-0067...WO-0079). The script identified
`crates/ucil-daemon/tests/g7_plugin_manifests.rs` as the only source file
matching its `\.(rs|ts|tsx|py)$` filter (the manifest TOML, install script,
and verify script are all excluded by extension), then reset that file to
its pre-WO state and re-ran the test selector. Without the new
`ruff_manifest_health_check` function, the test selector matched zero
tests, and the script flagged this as a "ZERO tests run with code stashed
— module was removed, not a genuine pass." For a feature that introduces
an entirely new test (rather than mutating production code), the
reality-check's intuition does not apply — the test simply does not exist
in the parent state, which is the correct semantics, not a fake-green.

The M1/M2 in-place mutation contract verified above is the load-bearing
anti-laziness layer per WO-0079 verifier precedent + scope_out #18.

## Stub scan

Production-side files (`plugins/quality/ruff/plugin.toml`,
`scripts/devtools/install-ruff-mcp.sh`, `scripts/verify/P3-W11-F03.sh`,
`crates/ucil-daemon/tests/g7_plugin_manifests.rs`) are clear of
`todo!()`, `unimplemented!()`, `raise NotImplementedError`,
single-line `pass` function bodies, and bare-literal default returns:

```
$ grep -nE 'todo!\(\)|unimplemented!\(\)|NotImplementedError' \
    plugins/quality/ruff/plugin.toml \
    crates/ucil-daemon/tests/g7_plugin_manifests.rs \
    scripts/devtools/install-ruff-mcp.sh \
    scripts/verify/P3-W11-F03.sh
(empty — exit 1, no matches)
```

The three `pass` matches in the diff (visible via `git diff main -- scripts/verify/P3-W11-F03.sh | grep '^+.*pass'`) are inside `try/except Exception: pass` cleanup blocks for `proc.stdin.close()` / `proc.terminate()` / `proc.kill()` / `smoke_log.close()` in the python smoke driver's `finally:` block — acceptable Python idiom for best-effort resource cleanup, not implementation stubs (per critic report §OK).

The only `Mocking` literal in the test file is the rustdoc negative-
assertion "Mocking … is forbidden" (lines 11-15) — exempt per WO-0048
line 363 + WO-0072/0075/0076/0079 §executor "Negative assertion forbidding
mocking, NOT a mock implementation".

## Quality gates

| Crate | `coverage-gate.sh ucil-daemon 85 75` | Verdict |
|-------|--------------------------------------|---------|
| `ucil-daemon` | exit 0 (`PASS — line=88% branch=n/a`) — RUSTC_WRAPPER bug did not surface this run | PASS |

The 35-WO standing-protocol carry-forward (RUSTC_WRAPPER profraw-merge
harness bug per WO-0058...WO-0079 scope_in #23) did NOT surface in this
run; the gate exited 0 cleanly with line=88% (>= 85% floor). No mutation
gate per DEC-0007 (cargo-mutants removed from per-WO gating; reality-
check + critic stub-scan + coverage gate are the authoritative anti-
laziness layers).

## Live tool name capture

The integration test asserts the canonical tool name `ruff-check` is in
the live `tools/list` reply at PyPI tag `mcp-server-analyzer@0.1.2`. The
M2 verifier-side mutation panic body recorded the full advertised set:

```
ruff-check, ruff-format, ruff-check-ci, vulture-scan, analyze-code
```

Manifest lines 62-76 carry this list verbatim in the leading rustdoc;
the integration test's SA3 assertion + the verify script's tools/list
assertion both pin on `ruff-check` (the canonical Ruff lint tool that
maps verbatim to the declared `quality.ruff.lint` capability).

PyPI-name vs serverInfo.name asymmetry per WO-0077 §planner lesson:

| Layer | Value |
|-------|-------|
| PyPI package | `mcp-server-analyzer` |
| Runtime serverInfo.name | `Python Analyzer` (version `3.2.4`) |
| UCIL plugin name (manifest-level) | `ruff` |

The test asserts on `health.name == "ruff"` (sourced from
`manifest.plugin.name` — UCIL's local plugin name), independent of the
runtime serverInfo name asymmetry.

## Forbidden-paths discipline

All forbidden-path globs return 0 lines from `git diff origin/main...HEAD`:

```
ucil-build/feature-list.json                      empty
ucil-build/feature-list.schema.json               empty
ucil-master-plan-v2.1-final.md                    empty
tests/fixtures/**                                  empty
scripts/gate/**                                    empty
scripts/flip-feature.sh                            empty
crates/ucil-daemon/tests/plugin_manifests.rs       empty (WO-0044 G2 regression guard intact)
crates/ucil-daemon/tests/g3_plugin_manifests.rs    empty
crates/ucil-daemon/tests/g4_plugin_manifests.rs    empty
crates/ucil-daemon/tests/g5_plugin_manifests.rs    empty
crates/ucil-daemon/tests/g6_plugin_manifests.rs    empty
crates/ucil-daemon/tests/g8_plugin_manifests.rs    empty
scripts/verify/P3-W11-F02.sh                       empty
scripts/verify/P3-W11-F04.sh                       empty
scripts/verify/P3-W11-F08.sh                       empty
plugins/quality/eslint/**                          empty
plugins/quality/semgrep/**                         empty
plugins/{knowledge,architecture,context,platform,testing}/**    empty
```

## Commit hygiene

5 substantive commits unique to feat (`git log feat ^main`):

```
66e1780 chore(rfr): WO-0080 ready-for-review marker
599fbd9 feat(scripts-verify): replace P3-W11-F03 TODO-stub with end-to-end ruff smoke
54a6b29 test(daemon): add ruff_manifest_health_check inside mod g7_plugin_manifests
09c0e12 feat(devtools): add install-ruff-mcp.sh idempotent helper
641e33c feat(plugins-quality): land plugins/quality/ruff/plugin.toml pinned to mcp-server-analyzer@0.1.2
```

All carry Conventional Commits format with `Phase: 3 / Feature: P3-W11-F03
/ Work-order: WO-0080` trailers + `Co-Authored-By` line. No merge commits
(`git log feat ^main --merges` empty per AC11). Three commits exceed the
~50-line soft target (DEC-0005 module-coherence carve-out applies per
critic Warnings 1-3, non-blocking). `git diff main -- crates/ucil-daemon/tests/g7_plugin_manifests.rs | grep '^-' | grep -v '^---' | wc -l` returns 0 — additive only.

## Features flipped

- P3-W11-F03 → `passes=true` (commit `66e1780ddb9ba9f9f397a410375552b419db00c2`)
- `last_verified_by`: `verifier-90cea9cb-9e1e-4bdc-a805-aec80e6624be`
- `last_verified_ts`: 2026-05-08T15:00:00Z
- `blocked_reason`: `null`
- `attempts`: 0 → 0 (PASS preserves attempt count; first WO attempt per WO scope_in #30 — DEC-0020 deferral consumed no executor cycles, DEC-0023 revival)
