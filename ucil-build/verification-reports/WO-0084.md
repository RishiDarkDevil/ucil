---
work_order: WO-0084
slug: tier-merger-and-conflict-resolution
phase: 3
week: 10
features: [P3-W10-F10, P3-W10-F12]
verifier_session: fc08828a-6f79-4d1f-a417-03c831fade0a
branch: feat/WO-0084-tier-merger-and-conflict-resolution
verified_commit: 9b2fdcb60716b4e5e9779c8295fceca4da484623
verified_at: 2026-05-08T23:30:00Z
verdict: PASS
---

# Verification Report: WO-0084 — tier-merger + conflict-resolution

**Verifier session**: vrf-fc08828a-6f79-4d1f-a417-03c831fade0a
**Branch**: `feat/WO-0084-tier-merger-and-conflict-resolution`
**Head sha**: `9b2fdcb60716b4e5e9779c8295fceca4da484623`
**Verified at**: 2026-05-08T23:30:00Z
**Verdict**: **PASS**

Two coordinated pure-deterministic merge/provenance features in
`crates/ucil-core/` per master-plan §18 Phase 3 Week 10 deliverables #4
+ #6 — F10 conflict-resolution `pub fn resolve_conflict()` extending
`fusion.rs` with the source-authority hierarchy `LSP/AST > SCIP > KG >
text`, AND F12 multi-tier query merging `pub fn merge_across_tiers()`
in net-new `crates/ucil-core/src/tier_merger.rs` with hot/warm/cold
tier-provenance metadata + confidence-band tag.

## Acceptance criteria

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| 1 | `cargo test -p ucil-core fusion::test_conflict_resolution` | PASS | `1 passed; 0 failed`; substring-match unique |
| 2 | `cargo test -p ucil-core tier_merger::test_multi_tier_query_merge` | PASS | `1 passed; 0 failed`; substring-match unique |
| 3 | `cargo clippy -p ucil-core --all-targets -- -D warnings` | PASS | exit 0 |
| 4 | `cargo fmt --check -p ucil-core` | PASS | exit 0 |
| 5 | grep `^pub mod tier_merger` `crates/ucil-core/src/lib.rs` | PASS | line 17 (alphabetical: schema_migration → tier_merger → types) |
| 6 | grep `pub fn resolve_conflict` `crates/ucil-core/src/fusion.rs` | PASS | line 832 |
| 7 | grep `pub fn merge_across_tiers` `crates/ucil-core/src/tier_merger.rs` | PASS | line 229 |
| 8 | grep `pub enum SourceAuthority` `crates/ucil-core/src/fusion.rs` | PASS | line 696 |
| 9 | grep `pub struct ConflictCandidate` `crates/ucil-core/src/fusion.rs` | PASS | line 732 |
| 10 | grep `pub enum ConflictResolution` `crates/ucil-core/src/fusion.rs` | PASS | line 756 |
| 11 | grep `pub enum Tier` `crates/ucil-core/src/tier_merger.rs` | PASS | line 70 |
| 12 | grep `pub struct TieredResult` `crates/ucil-core/src/tier_merger.rs` | PASS | line 101 |
| 13 | grep `pub struct MergedResult` `crates/ucil-core/src/tier_merger.rs` | PASS | line 136 |
| 14 | production-side word-ban `mock|fake|stub` | PASS | head-of-file slice (above first `#[cfg(test)]` at lines 904 / 353) returns zero matches in BOTH files |
| 15 | `! grep -qE 'unsafe' crates/ucil-core/src/tier_merger.rs` | PASS | no `unsafe` keyword |
| 16 | `git log feat/WO-0084-… ^main --merges \| wc -l` | PASS | 0 merge commits |
| 17 | effectiveness-gate.sh phase 3 | CARVE-OUT | AC30/AC31 standing carry-forward (3 open escalations); not a blocker per scope_in #24 / scope_out #12 |
| 18 | M1 mutation flips `fusion::test_conflict_resolution` PASS→FAIL | PASS | SA3 panic; restored OK (md5 match) |
| 19 | M2 mutation flips `tier_merger::test_multi_tier_query_merge` PASS→FAIL | PASS | SA2 panic; restored OK (md5 match) |
| 20 | `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-core --tests --summary-only --json \| jq '.data[0].totals.lines.percent' >= 85` | PASS | **96.58%** ucil-core overall; per-file fusion.rs **95.01%** + tier_merger.rs **87.30%** |
| 21 | `scripts/verify/P3-W10-F10.sh` exit 0 + `scripts/verify/P3-W10-F12.sh` exit 0 | PASS | both green |

All 21 acceptance criteria green from a `cargo clean` baseline.

Additional checks beyond the WO's enumerated acceptance:

- `cargo test -p ucil-daemon server::test_all_22_tools_registered` → `1 passed; 0 failed` (22-tool catalog count regression test still green per scope_in #22).
- `cargo test -p ucil-core --lib` → `46 passed; 0 failed` (no regression in any sibling test in the crate).
- `git diff main -- crates/ucil-core/src/lib.rs | grep -E '^\+pub use' | wc -l` → 0 (per AC #28; no re-export changes in lib.rs).
- `git diff main -- crates/ucil-core/src/lib.rs` shows exactly ONE inserted line: `+pub mod tier_merger;` at the schema_migration → types alphabetical position.

## Mutation contract M1/M2

Pre-mutation md5 snapshot at `/tmp/wo-0084-orig.md5`:

```
c535481cfc0ce3b5b3389fe340a20d8e  crates/ucil-core/src/fusion.rs
2df2d48f6718403622b5b7d4690fde41  crates/ucil-core/src/tier_merger.rs
```

These match the executor's RFR-reported pre-mutation md5sums verbatim — baseline confirmed.

| # | Feature | Mutation site | Patch (in-place via sed) | Targeted SA | Observed panic | Restore md5 |
|---|---------|---------------|---------------------------|-------------|------------------|-------------|
| M1 | P3-W10-F10 | `crates/ucil-core/src/fusion.rs:860` | `if c.source < top_source` → `if c.source > top_source` (invert authority comparison; equivalent to `.iter().map(\|c\| c.source).min()` → `.max()`) | SA3 cross-tier authority precedence | `(SA3) cross-tier authority precedence — LSP/AST beats KG by §18 line 1811 hierarchy; left: Resolved { value: "b", source: Kg, confidence: 0.6 }, right: Resolved { value: "a", source: LspAst, confidence: 0.9 }` (fusion.rs:1333) | OK |
| M2 | P3-W10-F12 | `crates/ucil-core/src/tier_merger.rs:252,282` | drop the `m.contributing_tiers.push(entry.tier); ... .sort();` block AND replace `vec![entry.tier]` with `vec![]` on insertion (tier-provenance erasure) | SA2 single-tier `contributing_tiers=[Hot]` (M2 also flips SA3 + SA5 — Rust halts on first failure at SA2) | `(SA2) single-tier source_tier + contributing_tiers; left: MergedResult { value: "x", source_tier: Hot, confidence: 0.3, contributing_tiers: [] }, right: MergedResult { value: "x", source_tier: Hot, confidence: 0.3, contributing_tiers: [Hot] }` (tier_merger.rs:387) | OK |

Each mutation flipped its targeted test from PASS to FAIL with a substantively distinct failure mode:

- **M1** = boolean-precedence inversion (`<` → `>` on `SourceAuthority` discriminant comparison; the `.min()`-equivalent reduction becomes a `.max()`-equivalent reduction). Catches the authority-precedence regression (Text/KG winning over LspAst).
- **M2** = data-projection erasure (`contributing_tiers` set to `vec![]` and the tier-merge push-and-sort loop deleted). Catches the tier-provenance metadata loss across all three SA bands (SA2/SA3/SA5).

Two substantively distinct surfaces — verifier accepts per WO-0083 §M1+M2+M3 substantively-distinct-failure-modes precedent.

After each `git checkout -- <file>`, `md5sum -c /tmp/wo-0084-orig.md5` returned `OK` for both files. Post-restoration both tests run clean (`1 passed; 0 failed` for each).

### Disclosed deviations (verifier-acceptable)

The executor's RFR transparently disclosed two spirit-over-literal deviations vs `scope_in #15-#16`:

1. **M1 panic surfaces on SA3** (per assertion order in the test) rather than the prescribed SA6 — both SAs are authority-precedence inversions, the mutation contract is substantively satisfied. Per WO-0070/0083 spirit-over-literal precedent. ✓
2. **M2 panic surfaces on SA2** first rather than the prescribed SA3 or SA5 — the mutation also flips SA3 + SA5; the test runner halts on the first failing assertion (SA2) by Rust convention. Substantively satisfied. ✓

Verifier accepts both deviations per the established 14-WO-deep precedent (WO-0067/0068/0069/0070/0072/0073/0074/0075/0076/0077/0079/0080/0082/0083).

## Mutation checks (per-feature reality check)

The M1/M2 in-place mutation contract IS the reality check for this WO per scope_out #13. The standalone `scripts/reality-check.sh` (which uses `git stash`) was NOT used per the WO-0072/0073/0083 lessons §verifier protocol explicitly cited in scope_in #17 — `git stash` is replaced by `sed -i` for in-place mutation + `git checkout --` for restore. Both features are covered by their feature-specific mutation:

| Feature | Stash-equivalent failed? | Restore confirmed? | Verdict |
|---------|--------------------------|---------------------|---------|
| P3-W10-F10 | yes (M1 → SA3 panic at fusion.rs:1333) | yes (md5 OK) | OK |
| P3-W10-F12 | yes (M2 → SA2 panic at tier_merger.rs:387) | yes (md5 OK) | OK |

## Stub scan

No `todo!()` / `unimplemented!()` / `NotImplementedError` / `pass`-only bodies in the new production code (`crates/ucil-core/src/fusion.rs` delta + `crates/ucil-core/src/tier_merger.rs`).

Production-side word-ban grep `mock|fake|stub` over the head-of-file prefix (above first `#[cfg(test)]` at lines 904 + 353) returns zero matches in BOTH files. The frozen tests at module root (`test_conflict_resolution`, `test_multi_tier_query_merge`) live under `#[cfg(test)]` and are exempt per scope_in #13.

## Quality gates

| Crate | Line cov (auth.) | Branch cov | gate-script result | Verdict |
|-------|-------------------|------------|----------------------|---------|
| ucil-core | **96.58%** (`env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-core --tests --summary-only --json | jq '.data[0].totals.lines.percent'` = `96.58206429780033`) — well above 85% floor | n/a (toolchain) | `coverage-gate.sh ucil-core 85 75` reports `cargo llvm-cov report errored` — disclosed sccache RUSTC_WRAPPER + profraw-prune interaction per scope_in #23 (38-WO standing carry-forward, WO-0058..WO-0083) | **PASS** (authoritative measurement) |

Per-file coverage on the new/extended modules:

- `crates/ucil-core/src/fusion.rs`: **95.01%** line coverage
- `crates/ucil-core/src/tier_merger.rs`: **87.30%** line coverage

Both files individually clear the 85% line-coverage floor.

The `[FAIL] cargo llvm-cov report errored for ucil-core` line in `coverage-gate.sh` output is the known sccache RUSTC_WRAPPER + post-prune profraw interaction (the gate-script does not unset `RUSTC_WRAPPER` when sccache is configured globally, and prunes profraws between the test run and the report subcommand). The authoritative measurement (`env -u RUSTC_WRAPPER cargo llvm-cov ... --tests --summary-only --json`) at 96.58% is the official coverage figure for this WO and exceeds the 85% line-coverage floor by a wide margin.

Reality-check (M1+M2 in-place mutation contract) + critic stub-scan (CLEAN per `ucil-build/critic-reports/WO-0084.md`) + this coverage-gate authoritative measurement form the three anti-laziness layers per DEC-0007. All three are green.

## Trace-span coverage

Master-plan §15.2 tracing does NOT apply to this WO. Both `resolve_conflict` and `merge_across_tiers` are pure-deterministic CPU-bound functions — no async, no IO, no `tokio::spawn`, no subprocess calls, no logging. The §15.2 carve-out applies (pure-deterministic-fallback module per WO-0067 §`lessons_applied` #5 + WO-0070 G3 parallel-merge precedent + WO-0083). Critic check 8 (Tracing) accepts the carve-out when explicitly disclosed (per `ucil-build/critic-reports/WO-0084.md`).

## DEC reference

- **DEC-0005** (module-coherence): One feat commit `543e823` lands F10 fusion.rs delta + F12 tier_merger.rs + lib.rs `pub mod` declaration as ONE cohesive unit (995 LOC). Splitting per-feature would produce stub-shaped intermediate states. Cited precedent: WO-0067 ceqp.rs (548 LOC) + WO-0068 cross_group.rs (788 LOC) + WO-0083 server.rs (982 LOC). Verifier accepts the carve-out per scope_in #19.
- **DEC-0007** (frozen-test-at-module-root selector substring-match): Both `test_conflict_resolution` (fusion.rs:1278 area) and `test_multi_tier_query_merge` (tier_merger.rs:356 area) are at MODULE ROOT — NOT inside `mod tests {}` — so the `cargo test -p ucil-core fusion::test_conflict_resolution` / `cargo test -p ucil-core tier_merger::test_multi_tier_query_merge` selectors substring-match resolve uniquely (verified above).
- **DEC-0008 §4** (dependency-inversion seam for production-wiring deferral): No `TierSource` trait introduced (the merger takes pre-loaded `&[TieredResult<T>]` slices, not a trait — the dependency-inversion seam moves to the consumer WO that wires real tier readers, analogous to G1/G2/G3/G4 source deferrals).
- **Master-plan §17 line 1636**: `tier_merger.rs` is now seated in `crates/ucil-core/src/` per the directory-structure entry.
- **Master-plan §18 line 1811** (Phase 3 Week 10 deliverable #4): Conflict resolution: source authority hierarchy LSP/AST > SCIP > KG > text.
- **Master-plan §18 line 1813** (Phase 3 Week 10 deliverable #6): Multi-tier query merging (hot + warm + cold).
- **Master-plan §12.3** (lines 1348-1356): hot/warm/cold tier semantics + confidence bands HOT 0.2-0.4 / WARM 0.5-0.7 / COLD 0.8-1.0 cited verbatim in the `Tier` enum doc comment.
- **Master-plan §11.3** (pull-based-relevance + recency boost): drives the merge-time recency bias (HOT can override COLD by `observed_at` despite COLD's higher static confidence band).

## Carry-forward standing protocols (acknowledged, not blockers)

- **AC23 sccache RUSTC_WRAPPER coverage workaround** — `coverage-gate.sh` reports cargo-llvm-cov error due to the sccache + profraw-prune interaction; the authoritative measurement is `env -u RUSTC_WRAPPER cargo llvm-cov`, which reports 96.58% for ucil-core (above the 85% floor). 38+ WOs deep.
- **AC30/AC31 effectiveness-gate flake** — three open escalations (`20260507T0357Z-effectiveness-nav-rust-symbol-rs-line-flake.md`, `20260507T1629Z-effectiveness-refactor-rename-python-fixture-missing-symbol.md`, `20260507T1930Z-effectiveness-nav-rust-symbol-doctest-caller-flake.md`). Pre-existing standing scope_out per scope_in #24 + scope_out #12. NOT a blocker for this WO.
- **`scripts/reality-check.sh` pre-existing-stash bug** — workaround: M1/M2 in-place mutation contract (sed + git checkout --) is the authoritative anti-laziness layer per WO-0067/0068/0069/0070/0072/0073/0074/0075/0076/0077/0079/0080/0082/0083 precedent (now 14 WOs deep).
- **Critic warning re `find_observed_at` helper subtle correctness fragility** (per `ucil-build/critic-reports/WO-0084.md` Warning #1) — the private helper has a documented edge case under duplicate-`(value, tier, confidence)`-triple inputs that the frozen tests do not exercise. Documented as a future-correctness landmine for the consumer WO that wires real tier sources, **not** a blocker for this WO's acceptance per critic verdict CLEAN.

## Features flipped

- **P3-W10-F10** → `passes=true` (verified at commit 9b2fdcb60716b4e5e9779c8295fceca4da484623)
- **P3-W10-F12** → `passes=true` (verified at commit 9b2fdcb60716b4e5e9779c8295fceca4da484623)

## Notes

- Verifier `cd`'d into `/home/rishidarkdevil/Desktop/ucil-wt/WO-0084` (the executor's worktree) before running `flip-feature.sh`, per the `20260508T1635Z-merge-wo-skip-verifier-on-main-branch.md` escalation lesson — flip lands on `feat/WO-0084-tier-merger-and-conflict-resolution`, NOT main.
- Verifier session id `fc08828a-6f79-4d1f-a417-03c831fade0a` differs from the executor session(s) that wrote commits `543e823` / `3108db7` / `9b2fdcb` — no self-verification.
- `cargo clean` baseline used; full ucil-core rebuild from scratch before any AC was run.
