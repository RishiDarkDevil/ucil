---
retry: 2
rejected_at: 2026-04-16T06:00:00Z
verifier_session: vrf-8c042043-8b28-460a-a587-64ddedcee412
branch: feat/WO-0006-symbol-extraction-chunker-storage
---
# Rejection: WO-0006 (retry 2)

**Verifier session**: vrf-8c042043-8b28-460a-a587-64ddedcee412
**Branch**: feat/WO-0006-symbol-extraction-chunker-storage
**HEAD commit**: 0c604df0170cae8ff67f523fc942ee6e9216fe51
**Rejected at**: 2026-04-16T06:00:00Z

---

## Executive Summary

P1-W2-F02 (symbol extraction) and P1-W2-F03 (chunker) are fully green.
P1-W2-F06 (storage layout) fails for the **same reason as retry 1**: the test
function `test_two_tier_layout` is nested inside `#[cfg(test)] mod tests { ... }`,
giving it the nextest path `storage::tests::test_two_tier_layout`.
The frozen acceptance test selector `storage::test_two_tier_layout` (no `tests::`
component) matches **zero tests** → nextest exits 4.

The executor made **no new commits** between retry 1 and this session. The fix
described in the retry-1 rejection has not been applied.

---

## Failed criterion

**Criterion 3** (feature-list.json `acceptance_tests` for P1-W2-F06):

```
cargo nextest run -p ucil-daemon storage::test_two_tier_layout
```

**Expected**: 1 test runs and passes (exit 0)
**Actual**: exit 4 — `error: no tests to run` (0 tests selected, 8 skipped)

---

## Repro

```
$ cd /home/rishidarkdevil/Desktop/ucil-wt/WO-0006
$ cargo clean                              # 1.4 GiB removed
$ cargo build --workspace                  # exits 0
$ cargo nextest run -p ucil-daemon storage::test_two_tier_layout 2>&1
    Finished `test` profile [unoptimized + debuginfo] target(s) in 2.30s
────────────
 Nextest run ID 73417364-3bdc-4b12-a4a1-3857ba79fc8e with nextest profile: default
    Starting 0 tests across 2 binaries (8 tests skipped)
────────────
     Summary [   0.000s] 0 tests run: 0 passed, 8 skipped
error: no tests to run
(hint: use `--no-tests` to customize)

$ cargo nextest list -p ucil-daemon 2>&1 | grep storage
ucil-daemon storage::tests::test_two_tier_layout
```

The actual registered nextest path is `storage::tests::test_two_tier_layout`
(note the extra `tests::` module component).

---

## Root cause (unchanged from retry 1)

`crates/ucil-daemon/src/storage.rs` lines 174–181:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_two_tier_layout() {
```

The test is inside `mod tests`, so its nextest path is
`storage::tests::test_two_tier_layout`. The frozen selector in
`feature-list.json` is `storage::test_two_tier_layout` (no `tests::`), which
matches nothing.

---

## Required fix

Remove the `mod tests { ... }` wrapper. Place `use super::*;` and the test
function directly at module scope under `#[cfg(test)]`:

```rust
// ── Unit tests ────────────────────────────────────────────────────────────

#[cfg(test)]
use std::path::Path;   // may already be in scope; adjust as needed

#[test]
fn test_two_tier_layout() {
    // body unchanged from current storage::tests::test_two_tier_layout
}
```

This registers the test at nextest path `storage::test_two_tier_layout`,
exactly matching the frozen selector.

---

## Passing criteria (for context)

All other criteria were green in this attempt:

| Criterion | Result |
|-----------|--------|
| P1-W2-F02: `symbols::` (6 tests) | PASS |
| P1-W2-F03: `chunker::` (4 tests) | PASS |
| `cargo clippy -p ucil-treesitter -- -D warnings` | PASS |
| `cargo clippy -p ucil-daemon -- -D warnings` | PASS |
| `cargo build --workspace` | PASS |
| `SymbolKind::` count ≥ 6 (actual: 23) | PASS |
| Stub scan (symbols.rs, chunker.rs, storage.rs) | CLEAN |

---

## Suspected cause of non-fix

The previous rejection commit `0c604df` is on the feature branch (HEAD).
The feature branch has no new executor commits after the retry-1 rejection.
The executor may not have been spawned to apply the fix, or the escalation
blocked re-spawning.

---

## Next step

1. Spawn executor to fix `crates/ucil-daemon/src/storage.rs`:
   remove `mod tests { ... }` wrapper; place `#[test] fn test_two_tier_layout()`
   directly at module scope.
2. Executor must run `cargo nextest run -p ucil-daemon storage::test_two_tier_layout`
   locally to confirm exit 0 before marking ready-for-review.
3. Commit the fix and push to `feat/WO-0006-symbol-extraction-chunker-storage`.
4. Spawn a fresh verifier session (retry 3).

Feature P1-W2-F06 `attempts` incremented to 2 in this rejection (attempts=2,
escalation trigger at attempts=3).
