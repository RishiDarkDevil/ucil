---
retry: 0
rejected_at: 2026-04-15T18:40:46Z
verifier_session: vrf-f1fe06ff-f60e-4432-8bc7-6b6b71c45297
branch: feat/WO-0006-symbol-extraction-chunker-storage
---
# Rejection: WO-0006 (retry 0)

**Verifier session**: vrf-f1fe06ff-f60e-4432-8bc7-6b6b71c45297
**Branch**: feat/WO-0006-symbol-extraction-chunker-storage
**Rejected at**: 2026-04-15T18:40:46Z

---

## Failed criterion

**Criterion 3**: `cargo nextest run -p ucil-daemon storage::test_two_tier_layout --test-threads 4`

**Feature**: P1-W2-F06  
**Expected**: 1 test runs and passes (exit 0)  
**Actual**: exit 4 — "error: no tests to run" (0 tests run, 8 skipped)

---

## Repro

```
$ cd /path/to/ucil-wt/WO-0006
$ cargo clean
$ cargo nextest run -p ucil-daemon storage::test_two_tier_layout --test-threads 4
    Finished `test` profile [unoptimized + debuginfo] target(s) in 0.04s
────────────
 Nextest run ID 3ab7fcd6-efca-4cc6-b66b-e92983592c2e with nextest profile: default
    Starting 0 tests across 2 binaries (8 tests skipped)
────────────
     Summary [   0.000s] 0 tests run: 0 passed, 8 skipped
error: no tests to run
(hint: use `--no-tests` to customize)
```

---

## Root cause

The test in `crates/ucil-daemon/src/storage.rs` is declared inside a
`#[cfg(test)] mod tests { ... }` block (lines 174–231). This gives it the
nextest path `storage::tests::test_two_tier_layout`.

The acceptance test selector frozen in `feature-list.json` is:
```json
{ "kind": "cargo_test", "selector": "-p ucil-daemon storage::test_two_tier_layout" }
```

The selector `storage::test_two_tier_layout` does NOT match
`storage::tests::test_two_tier_layout` — nextest filter matching is exact on
the full module path, not a suffix match. The extra `tests::` component prevents
any match, so nextest finds 0 tests and exits 4.

---

## Required fix

In `crates/ucil-daemon/src/storage.rs`, remove the `mod tests { ... }` wrapper
and place the test function directly under the `#[cfg(test)]` attribute at module
scope:

```rust
// Before (test path: storage::tests::test_two_tier_layout — does NOT match)
#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn test_two_tier_layout() { ... }
}

// After (test path: storage::test_two_tier_layout — matches frozen selector)
#[cfg(test)]
mod test_two_tier {
    use super::*;
    #[test]
    fn test_two_tier_layout() { ... }
}
```

Wait — `mod test_two_tier` would produce `storage::test_two_tier::test_two_tier_layout`
which also doesn't match. The correct fix is to place the test function directly
at module scope (no intermediate module):

```rust
#[cfg(test)]
#[allow(clippy::module_name_repetitions)]
fn setup() { }   // helpers can go here if any

#[test]
fn test_two_tier_layout() {
    // body unchanged
}
```

Actually the correct structure is: NO wrapping module, with `#[test]` directly
in the file scope under `#[cfg(test)]`:

```rust
#[cfg(test)]
#[test]
fn test_two_tier_layout() {
    // same body as current storage::tests::test_two_tier_layout
}
```

This registers the test at path `storage::test_two_tier_layout` which is exactly
what the frozen selector expects.

---

## Passing criteria (for context)

All other criteria passed in this attempt:
- P1-W2-F02 symbol tests: 6/6 PASS
- P1-W2-F03 chunker tests: 4/4 PASS
- `cargo clippy -p ucil-treesitter -- -D warnings`: PASS
- `cargo clippy -p ucil-daemon -- -D warnings`: PASS
- `cargo build --workspace`: PASS
- `SymbolKind::` count ≥ 6 (actual: 23): PASS
- Stub scan (symbols.rs, chunker.rs, storage.rs): CLEAN

---

## Next step

1. Executor fixes `crates/ucil-daemon/src/storage.rs`: remove `mod tests { ... }`
   wrapper, place `#[test] fn test_two_tier_layout()` directly at module scope.
2. Re-run `cargo nextest run -p ucil-daemon storage::test_two_tier_layout` locally
   to confirm the selector now matches.
3. Commit and push the fix.
4. Spawn verifier retry.

Feature P1-W2-F06 `attempts` incremented to 1 in this rejection.
