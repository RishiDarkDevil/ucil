---
retry: 3
rejected_at: 2026-04-16T00:30:00Z
verifier_session: vrf-ad6e4e9e-ceb8-4ae9-abc3-b1c7ad4c27bd
branch: feat/WO-0006-symbol-extraction-chunker-storage
---
# Rejection: WO-0006 (retry 3)

**Verifier session**: vrf-ad6e4e9e-ceb8-4ae9-abc3-b1c7ad4c27bd
**Branch**: feat/WO-0006-symbol-extraction-chunker-storage
**HEAD commit**: ab943e5c425d9a3e48c7b330ee4b34a3a0d45264
**Rejected at**: 2026-04-16T00:30:00Z

---

## Executive Summary

This is the **third consecutive rejection** of P1-W2-F06 (storage layout) for
the identical root cause: the test `test_two_tier_layout` is placed inside
`#[cfg(test)] mod tests { ... }` in `storage.rs`, giving it the nextest path
`storage::tests::test_two_tier_layout`. The frozen acceptance test selector
`storage::test_two_tier_layout` (no `tests::` component) matches **zero tests**
→ nextest exits 4.

The executor made **no new commits** between retry 2 and this session.
The HEAD commit is still the retry-2 rejection commit `ab943e5`.

⚠️ **ESCALATION TRIGGER MET**: P1-W2-F06 has now failed the verifier
3 consecutive times (`attempts` incremented from 2 → 3). Per CLAUDE.md
escalation rule §1 ("Same feature fails verifier 3 times"), the outer loop
must halt and the user must be paged.

P1-W2-F02 and P1-W2-F03 are functionally complete and all their tests pass;
however they cannot be flipped because the WO is blocked as a unit and the
escalation threshold has been reached.

---

## Criteria results

| # | Criterion | Result | Exit | Notes |
|---|-----------|--------|------|-------|
| 1 | `cargo nextest run -p ucil-treesitter symbols::` | **PASS** | 0 | 6/6 tests green |
| 2 | `cargo nextest run -p ucil-treesitter chunker::` | **PASS** | 0 | 4/4 tests green |
| 3 | `cargo nextest run -p ucil-daemon storage::test_two_tier_layout` | **FAIL** | 4 | "0 tests run, 8 skipped" — selector mismatch (3rd occurrence) |
| 4 | `cargo clippy -p ucil-treesitter -- -D warnings` | **PASS** | 0 | clean |
| 5 | `cargo clippy -p ucil-daemon -- -D warnings` | **PASS** | 0 | clean |
| 6 | `cargo build --workspace` | **PASS** | 0 | clean |
| 7 | `grep -c 'SymbolKind::' symbols.rs ≥6` | **PASS** | 0 | count = 23 |

---

## Failed criterion — P1-W2-F06 acceptance test

**Criterion**: `cargo nextest run -p ucil-daemon storage::test_two_tier_layout`

**Expected**: 1 test runs and passes (exit 0)
**Actual**: exit 4 — "0 tests run: 0 passed, 8 skipped", "error: no tests to run"

---

## Repro

```
$ cd /home/rishidarkdevil/Desktop/ucil-wt/WO-0006
$ cargo clean          # 1.4 GiB removed
$ cargo nextest run -p ucil-daemon storage::test_two_tier_layout 2>&1
    Finished `test` profile [unoptimized + debuginfo] target(s) in 2.35s
────────────
 Nextest run ID e68f832d-d4fb-4518-8d5a-3dacd197665f with nextest profile: default
    Starting 0 tests across 2 binaries (8 tests skipped)
────────────
     Summary [   0.000s] 0 tests run: 0 passed, 8 skipped
error: no tests to run
(hint: use `--no-tests` to customize)

$ cargo nextest list -p ucil-daemon 2>&1 | grep storage
ucil-daemon storage::tests::test_two_tier_layout
```

The actual registered nextest path is `storage::tests::test_two_tier_layout`
(note the extra `tests::` module component).

---

## Root cause (unchanged across all 3 retries)

`crates/ucil-daemon/src/storage.rs` lines 174–181:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_two_tier_layout() {
```

The test is inside `mod tests`, so its nextest path is
`storage::tests::test_two_tier_layout`. The frozen selector in
`feature-list.json` is `storage::test_two_tier_layout` (no `tests::`), which
matches nothing.

---

## Stub scan

| File | `todo!()` | `unimplemented!()` | single-`pass` | Result |
|------|-----------|-------------------|---------------|--------|
| `crates/ucil-treesitter/src/symbols.rs` | 0 | 0 | 0 | CLEAN |
| `crates/ucil-treesitter/src/chunker.rs` | 0 | 0 | 0 | CLEAN |
| `crates/ucil-daemon/src/storage.rs` | 0 | 0 | 0 | CLEAN |

---

## Mutation checks

Not run — blocking failure on P1-W2-F06 acceptance test prevents mutation check.
P1-W2-F02 and P1-W2-F03 are green but cannot be flipped individually because
the WO is blocked as a unit and the escalation threshold has been reached.

---

## Features flipped

None. P1-W2-F06 `attempts` incremented from 2 → 3 (escalation threshold met).

---

## Required fix

Remove the `mod tests { ... }` wrapper. Place `use super::*;` and the test
function directly at module scope under `#[cfg(test)]`:

```rust
#[cfg(test)]
use tempfile::TempDir;

#[test]
fn test_two_tier_layout() {
    // body unchanged from current storage::tests::test_two_tier_layout
}
```

This registers the test at nextest path `storage::test_two_tier_layout`,
exactly matching the frozen selector.

The executor must:
1. Apply the fix to `crates/ucil-daemon/src/storage.rs`
2. Run `cargo nextest run -p ucil-daemon storage::test_two_tier_layout` locally — confirm exit 0
3. Commit and push to `feat/WO-0006-symbol-extraction-chunker-storage`
4. Request a fresh verifier session (retry 4)

---

## Escalation

Per CLAUDE.md §Escalation triggers rule 1 ("Same feature fails verifier 3 times"),
an escalation file must be written and the outer loop must halt.
A root-cause-finder review is recommended to confirm the fix path before
the executor attempts retry 4.
