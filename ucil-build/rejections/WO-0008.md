---
retry: 3
rejected_at: 2026-04-16T21:42:53Z
verifier_session: vrf-93914c26-03a1-454e-8b3b-e0ffbd650872
branch: feat/WO-0008-daemon-lifecycle-session-state
---
# Rejection: WO-0008 (retry 3)

**Verifier session**: vrf-93914c26-03a1-454e-8b3b-e0ffbd650872
**Branch**: feat/WO-0008-daemon-lifecycle-session-state
**Rejected at**: 2026-04-16T21:42:53Z
**Verdict**: REJECT

## Summary

Retry-3 re-verification. **No new executor commits have landed on the
branch since retry 2.** The last source commit remains `50f053d
chore(work-order): WO-0008 ready-for-review marker`. The tip
(`17a1d39`) is the retry-2 verifier REJECT commit itself. Running the
full protocol from a clean `cargo clean` slate reproduces the prior two
rejections exactly: all six functional acceptance criteria pass, and
the crate-level mutation-gate still scores 41% (floor 70%).

## Acceptance criteria (all PASS)

| # | Criterion | Result | Duration |
|---|-----------|--------|----------|
| 1 | `cargo nextest run -p ucil-daemon -E 'test(lifecycle::)'` — ≥5 passing | PASS — 7/7 | 0.004s exec |
| 2 | `cargo nextest run -p ucil-daemon -E 'test(=session_manager::test_session_state_tracking)'` — exactly 1 matching | PASS — 1/1 | 0.005s |
| 3 | `cargo nextest run -p ucil-daemon -E 'test(session_manager::)'` — no regression | PASS — 8/8 | 0.004s |
| 4 | `cargo clippy -p ucil-daemon -- -D warnings` | PASS — clean | 1.55s |
| 5 | `cargo build --workspace` | PASS | 3.27s |
| 6 | `fn test_session_state_tracking` line 422 < `mod tests {` line 491 | PASS — module-level placement correct | n/a |

## Stub scan

No `todo!()`, `unimplemented!()`, `NotImplementedError`, or single-`pass`
bodies in the changed files
(`crates/ucil-daemon/src/{lib.rs,lifecycle.rs,session_manager.rs}`).
grep returned zero hits.

## Quality gates

| Crate | Mutation score | Line cov | Branch cov | Verdict |
|-------|----------------|----------|------------|---------|
| ucil-daemon | **41%** (floor 70%) | 84.98% rounded → 85% (floor 85%) | n/a (toolchain) | **FAIL (mutation)** |

### Coverage gate

`scripts/verify/coverage-gate.sh ucil-daemon 85 75` exits **0**. Line
coverage 84.98% passes the integer-rounded floor; branch coverage is
unavailable on the stable toolchain and is treated as a non-failing
skip.

### Mutation gate (HARD BLOCKER — unchanged from retry 2)

`scripts/verify/mutation-gate.sh ucil-daemon 70` exits **1**.

- **Score**: 41% (caught=5, missed=7, unviable=26, timeout=0, failure=0)
- **Floor**: 70%
- **Gap**: −29 points

#### Missed mutants (7 — identical to retries 1 and 2)

Five live on WO-0008's new code; two pre-date it but share the crate gate.

| # | Site | Mutation | WO-0008? | Why it survives |
|---|------|----------|----------|-----------------|
| 1 | `main.rs:10:5` | `replace main -> Result<()> with Ok(())` | no (pre-existing) | no unit test exercises `main()` |
| 2 | `session_manager.rs:189:36` | `replace + with -` in `create_session` (`created_at + DEFAULT_TTL_SECS`) | **yes (P1-W4-F07)** | test overwrites `expires_at` via `set_ttl` before reading it |
| 3 | `session_manager.rs:189:36` | `replace + with *` in `create_session` | **yes (P1-W4-F07)** | same masking by `set_ttl` |
| 4 | `session_manager.rs:225:19` | `replace == with !=` in `detect_branch` | no (pre-existing; P1-W2-F05 surface) | - |
| 5 | `session_manager.rs:346:48` | `replace > with ==` in `purge_expired` | **yes (P1-W4-F07)** | single boundary value cannot distinguish the three operators |
| 6 | `session_manager.rs:346:48` | `replace > with >=` in `purge_expired` | **yes (P1-W4-F07)** | at ex=c+1, nw=c+2 both are false |
| 7 | `session_manager.rs:347:30` | `replace - with +` in `purge_expired` (`before - guard.len()`) | **yes (P1-W4-F07)** | 1−0==1+0==1, both yield the asserted count |

#### Minimum-viable fix (no source change needed — test additions only)

The retry-1 and retry-2 rejections already prescribed this plan. It is
carried forward verbatim:

1. **Default-TTL branch** — create a session without calling `set_ttl`,
   call `purge_expired(created_at + DEFAULT_TTL_SECS + 1)`, assert the
   session was removed. Kills mutants #2 and #3.
2. **Boundary of `>`** — `set_ttl(10)`, call `purge_expired(created_at +
   10)` (== `expires_at`) and confirm it does NOT purge; then
   `purge_expired(created_at + 11)` and confirm it DOES purge. Kills
   mutants #5 and #6.
3. **Multi-session count** — create three sessions, set TTLs so two
   expire and one does not; assert `purge_expired` returns 2 and the
   survivor remains. Kills mutant #7.

Mutants #1 and #4 pre-date this WO; they are best addressed in a
follow-up micro-WO and are not the primary blocker.

## Reality check (informational)

Not re-run in this session. The multi-commit per-file rollback
limitation (escalation `20260415-1630-reality-check-per-file-rollback.md`,
resolved) is known to produce fake-green results for this branch. The
authoritative signal is the mutation-gate score above.

## Repro

```
$ cd /home/rishidarkdevil/Desktop/ucil-wt/WO-0008
$ git log --oneline -1
17a1d39 chore(verifier): WO-0008 retry-2 REJECT — mutation-gate 41% unchanged

$ cargo clean
# Removed 2813 files, 1.6GiB total

$ cargo build --workspace
# Finished `dev` profile in 3.27s

$ cargo nextest run -p ucil-daemon -E 'test(lifecycle::)'
# 7 tests run: 7 passed, 8 skipped

$ cargo nextest run -p ucil-daemon -E 'test(=session_manager::test_session_state_tracking)'
# 1 test run: 1 passed, 14 skipped

$ cargo nextest run -p ucil-daemon -E 'test(session_manager::)'
# 8 tests run: 8 passed, 7 skipped

$ cargo clippy -p ucil-daemon -- -D warnings
# Finished `dev` profile … clean

$ scripts/verify/coverage-gate.sh ucil-daemon 85 75
[coverage-gate] PASS — ucil-daemon line=85% branch=n/a

$ scripts/verify/mutation-gate.sh ucil-daemon 70
[mutation-gate] FAIL — ucil-daemon score=41% < min=70%
```

## Suspected cause

No executor activity since retry-1 or retry-2. Three verifier runs have
now diagnosed the identical shortfall; the executor has not added the
three boundary-style assertions the prior rejections specified.

## Next step

**Escalation trigger #1 fires on this retry.** Both `P1-W3-F01` and
`P1-W4-F07` now have `attempts == 3`, which matches the harness
contract's unconditional halt condition ("Same feature fails verifier
3 times", root `CLAUDE.md` → Escalation triggers #1). The outer loop
should halt; a root-cause-finder or the user should take over.

The prescribed fix is still local to tests only — no production code
change required:

- P1-W3-F01 `attempts` now **2 → 3**
- P1-W4-F07 `attempts` now **2 → 3**

Executor must not mark WO-0008 ready-for-review again until a local
run of `scripts/verify/mutation-gate.sh ucil-daemon 70` exits 0.
