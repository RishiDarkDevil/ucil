---
retry: 2
rejected_at: 2026-04-18T10:56:16Z
verifier_session: vrf-8ca4ed53-dc2a-457f-985a-6a9f61f83769
branch: feat/WO-0027-watchman-detection-and-backend-selection
head_commit: 592c9081423551b994aea976d7a56ee34ca49376
feature: P1-W3-F03
---
# Rejection: WO-0027 (retry 2)

**Verifier session**: `vrf-8ca4ed53-dc2a-457f-985a-6a9f61f83769`
**Branch**: `feat/WO-0027-watchman-detection-and-backend-selection`
**HEAD**: `592c9081423551b994aea976d7a56ee34ca49376` (the retry-1 REJECT commit;
source tree is byte-identical to retry-1's `650c854` — no executor fix was
pushed between retry 1 and this verifier spin-up).
**Rejected at**: 2026-04-18T10:56:16Z
**Verdict**: **REJECT**

## Summary — same bug, same failure, no fix applied

No executor commit has been pushed to the WO-0027 branch since the
retry-1 REJECT commit (`592c908`). The source tree this verifier
evaluated is unchanged from retry 1:

```
$ git log --grep='Feature: P1-W3-F03' --format='%h %s' | head
592c908 chore(verifier): WO-0027 REJECT — coverage gate fails (PATH-mutation race)
650c854 build(lockfile): regenerate Cargo.lock after which + walkdir promotion
8fc6335 chore(wo): ready-for-review marker for WO-0027
b0f7a33 test(daemon): make test_poll_backend_delivers_events drain until match
...
```

The blocking coverage-gate failure from retry 1 therefore reproduces
deterministically on this clean-slate re-verification.

## What passed (intent checks, same as retry 1)

| Check                                                           | Result |
|-----------------------------------------------------------------|--------|
| WO AC1 intent — `watcher::test_watchman_detection` (F03 oracle) | PASS (nextest: 1 run, 1 passed, 61 skipped — 0.002s) |
| WO AC2 intent — `watcher::` ≥13 tests                           | PASS (nextest: 13 run, 13 passed, 49 skipped — 2.149s) |
| WO AC3 — `cargo clippy -p ucil-daemon --all-targets -- -D warnings` | PASS (no `^error`) |
| WO AC4 — `cargo doc -p ucil-daemon --no-deps` (no unresolved)   | PASS |
| WO AC5 — `cargo fmt --check`                                    | PASS |
| WO AC6–AC10 — grep presence checks                              | PASS (`pub enum WatcherBackend`, `pub fn detect_watchman`, `pub fn auto_select_backend`, `pub fn new_with_backend`, `pub use watcher::` all present) |
| Feature `acceptance_tests` selector (cargo_test `-p ucil-daemon watcher::test_watchman_detection`) | PASS |
| Stub scan (changed files)                                       | No `todo!()` / `unimplemented!()` / `#[ignore]` / `.skip()` / `pass`-only bodies in `crates/ucil-daemon/src/{watcher.rs,lib.rs}` |

## Blocking failure — Coverage gate (same PATH-mutation race as retry 1)

`scripts/verify/coverage-gate.sh ucil-daemon 85 75` **exits 1** on this
branch, at the `cargo llvm-cov report` merge step, because the
underlying `cargo test -p ucil-daemon --lib` run aborts when
`session_manager::tests::*` tests panic inside coverage instrumentation.

### Reproduction this session (HEAD = 592c908, 3 consecutive runs after `cargo clean && rm -rf target`)

```
$ cd /home/rishidarkdevil/Desktop/ucil-wt/WO-0027
$ cargo clean && rm -rf target/
$ source <(cargo llvm-cov show-env --export-prefix)
$ for i in 1 2 3; do cargo test -p ucil-daemon --lib 2>&1 | grep '^test result'; done
test result: FAILED. 57 passed; 2 failed; 0 ignored; 0 measured; 0 filtered out; finished in 2.16s
test result: FAILED. 55 passed; 4 failed; 0 ignored; 0 measured; 0 filtered out; finished in 2.15s
test result: FAILED. 55 passed; 4 failed; 0 ignored; 0 measured; 0 filtered out; finished in 2.15s
```

Sample failure panic bodies (run 1):

```
---- session_manager::tests::create_session_returns_fresh_uuid_each_call stdout ----
thread 'session_manager::tests::create_session_returns_fresh_uuid_each_call' (1058745) panicked at
  crates/ucil-daemon/src/session_manager.rs:508:50:
second session: Io(Os { code: 2, kind: NotFound, message: "No such file or directory" })

---- session_manager::tests::discover_worktrees_returns_at_least_one stdout ----
thread 'session_manager::tests::discover_worktrees_returns_at_least_one' (1058748) panicked at
  crates/ucil-daemon/src/session_manager.rs:541:14:
discover_worktrees should succeed inside a git repo: Io(Os { code: 2, kind: NotFound, message: "No such file or directory" })
```

The `Io(NotFound)` is `git` not being resolvable — the session_manager
tests spawn `tokio::process::Command::new("git")` while a watcher test
has cleared `PATH` to an empty tempdir.

### Coverage gate's observed failure mode

After the test run aborts, `cargo-llvm-cov` writes truncated /
incomplete `.profraw` files for the panicking threads; the zero-byte
prune (harness-fix commit `2b6c066`) removes the ones that actually
zeroed out, and the subsequent `cargo llvm-cov report --package
ucil-daemon --summary-only --json` step reports:

```
error: failed to merge profile data: not found *.profraw files in
  /home/rishidarkdevil/Desktop/ucil-wt/WO-0027/target;
  this may occur if target directory is accidentally cleared, or
  running report subcommand without running any tests or binaries
```

This exact symptom is captured in this session's coverage gate report
at `ucil-build/verification-reports/coverage-ucil-daemon.md`.

Per `.claude/agents/verifier.md` step 7: *"If the gate exits non-zero →
reject. Do not flip the feature even if step 6 passed."*

## Root cause — cross-module `PATH`-mutation race (unchanged from retry 1)

The four WO-0027 watcher tests that mutate the process-wide `PATH`
environment variable —

- `watcher::test_watchman_detection`
- `watcher::test_auto_select_backend_returns_notify_when_watchman_absent`
- `watcher::test_auto_select_backend_returns_watchman_when_available_and_above_threshold`
- `watcher::test_auto_select_backend_returns_notify_when_below_threshold`

— do so via `std::env::set_var("PATH", <empty-tempdir>)` behind a
module-level `static ENV_GUARD: std::sync::Mutex<()>` + `RestorePath`
RAII guard (`crates/ucil-daemon/src/watcher.rs:1076-1108`). The guard
serialises *watcher-to-watcher* PATH mutations correctly, **but**
`std::env::set_var` is process-global, not thread-local, and the
guard does not cover concurrent tests in other modules of the same
test binary.

When `cargo test -p ucil-daemon --lib` (the harness path `cargo
llvm-cov` drives) runs, every test executes as a thread inside one
libtest process. While a watcher test holds `ENV_GUARD` and
`PATH=<empty tempdir>`, concurrent `session_manager::tests::*` tests
invoke `tokio::process::Command::new("git")` from
`session_manager.rs:291` / `:311` / `:339`. `git` is unresolvable in
the empty PATH and the spawn fails with `Io(NotFound)`, panicking the
test.

`cargo nextest` (the executor's local check path) runs each test in a
separate child process via `fork+exec`, so the PATH mutation cannot
leak between tests — that is why the executor's 62/62 nextest green
signal missed this. The coverage gate has been the canonical catch
for this failure mode since Phase 0.

## Control (main at `48c5d57`) — same result as retry 1

`main` pre-WO-0027 is still clean under the same commands (per retry 1
already-documented control run):

```
$ cd /tmp/ucil-main-check   # main at 48c5d57
$ cargo clean && rm -rf target
$ source <(cargo llvm-cov show-env --export-prefix)
$ for i in 1 2 3 4 5; do cargo test -p ucil-daemon --lib 2>&1 | grep '^test result'; done
test result: ok. 52 passed; 0 failed; ...
test result: ok. 52 passed; 0 failed; ...
test result: ok. 52 passed; 0 failed; ...
test result: ok. 52 passed; 0 failed; ...
test result: ok. 52 passed; 0 failed; ...
```

So the flakiness is a hard regression introduced by WO-0027 and still
present on this branch — not an environmental or harness issue.

## Mutation check (reality-check.sh hits the documented per-file-rollback false-positive)

Running `scripts/reality-check.sh P1-W3-F03` on this branch emits
`FAILURE: acceptance tests PASSED with code stashed — this is a
fake-green test.` — this is **not** a real mutation-check failure;
it's the same per-file-rollback false-positive the retry-1 verifier
documented. The script's rollback walks `CANDIDATES` newest→oldest
and picks the first commit touching each file, resetting that file
to the introducing commit's **parent**. For `watcher.rs`, the newest
candidate is `b0f7a33` (a test-only commit); `b0f7a33^` = `7c5d903`
still contains all the core WO-0027 production code from `37939f0`
(`detect_watchman` + `count_files_capped` + `auto_select_backend`)
and `b81a723` (`WatcherBackend` enum), so `test_watchman_detection`
continues to resolve against the intact code and passes, giving the
script a fake-green stashed result.

The retry-1 verifier's manual two-step mutation check (stash the
body of `detect_watchman()` → replace with `None` → `test_watchman_detection`
fails; restore → passes) remains the authoritative signal; it passed
for this WO at retry 1 and the source is byte-identical now, so the
anti-laziness guarantee for this feature is intact. The reality-check
false-positive is an orthogonal harness-tooling concern and does NOT
itself constitute a WO-0027 rejection reason. Rejection is solely
driven by the coverage gate failure above.

## Quality gates

| Crate | Line cov | Branch cov | Verdict |
|-------|----------|------------|---------|
| ucil-daemon | n/a (`cargo test` aborted; no complete profraws) | n/a | **FAIL** |

Full per-crate report: `ucil-build/verification-reports/coverage-ucil-daemon.md`.

## Suggested fix (not applied by verifier — for executor or root-cause-finder)

Same recommendations as retry 1. The smallest-blast-radius option remains:

**Option 2 — side-step `PATH` entirely** — `which::which_in` (already
available in the workspace's `which` 7.x series) lets the caller pass an
explicit search path. Rewrite `detect_watchman()` to take an optional
`&OsStr` PATH override (additive, backward-compatible), and have
`test_watchman_detection` / the three `auto_select_backend_*` tests
exercise that override instead of mutating the process env. Public
API stays backward-compatible; frozen F03 selector name stays.

Alternative options (1 and 3) are called out in the retry-1 rejection
report history and remain available. See also DEC-0005 for module-root
test placement constraints when considering option 1 (isolate tests
into a separate test binary under `tests/`).

## Feature state

- `P1-W3-F03` → `passes=false`, `attempts` incremented `1 → 2` (by
  `scripts/flip-feature.sh P1-W3-F03 fail`).
- No features flipped to `true` this session.
- Source working tree was clean before verification and remains clean
  post-verification — only `ucil-build/` paths were written (the
  verifier lock, this rejection overwrite, the verification report
  for this retry, and the coverage gate report).

## Next step — escalation-threshold signal

`attempts = 2` — the planner should consider whether a
root-cause-finder dive is warranted before the 3rd attempt (per root
CLAUDE.md escalation trigger #1, a 3rd consecutive verifier rejection
halts the loop). The fix is narrow (swap to `which::which_in` +
additive optional-PATH-override on `detect_watchman` + update the
four PATH-mutating tests) and does not need an ADR — master-plan §2
line 138 and DEC-0005 are unchanged by this path.
