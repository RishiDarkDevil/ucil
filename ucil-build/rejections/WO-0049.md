---
retry: 1
rejected_at: 2026-05-05T01:03:38Z
verifier_session: vrf-162f0721-7655-4ee7-a843-23bbc200ff78
branch: feat/WO-0049-find-references-and-g1-source-production-wiring
ref: f197ad1389c0e2ef67b67e5364b45fc25970bcfd
resolved: true
resolved_at: 2026-05-05T02:04:10Z
resolved_by: verifier-d249db74-379b-468b-b88d-5ac3141992df
resolution_commit: 9b596ed
re_verified_at: 2026-05-05T13:00:00Z
re_verified_by: verifier-957a26ee-e961-4b82-87b9-9d351621b829
re_verified_branch_head: e3c3f410b0e3cce6a55c4a8d5ec2fd22df4ee969
---
# Rejection: WO-0049 (retry 1)

**Verifier session**: `vrf-162f0721-7655-4ee7-a843-23bbc200ff78`
**Branch**: `feat/WO-0049-find-references-and-g1-source-production-wiring`
**Ref**: `f197ad1389c0e2ef67b67e5364b45fc25970bcfd`
**Rejected at**: 2026-05-05T01:03:38Z
**Verdict**: REJECT — feature P2-W7-F05 implementation absent; the headline acceptance test does not exist on the branch (selector matches 0 tests).

## Summary

The branch contains a **single refactor commit** (`f197ad1`) closing only `scope_in` steps 7 (per-source `tracing::instrument` on `run_g1_source`) and 8 (`.expect(...)` cleanup at `executor.rs:1057`). The headline deliverable for `P2-W7-F05` — the `find_references` MCP tool plus 4 production-shape `G1Source` impls — is **entirely missing** from the diff. The branch is byte-identical to `main` for every file except `crates/ucil-daemon/src/executor.rs`.

The critic already issued a `BLOCKED` verdict in `ucil-build/critic-reports/WO-0049.md` documenting the same gap and expressly told the verifier "do NOT run." I ran the headline test from a clean slate anyway to capture the precise failure shape — it is the canonical "selector matches nothing → masked failure" pattern.

## Failed criterion (frozen feature acceptance test from `feature-list.json`)

**Criterion**: `cargo test -p ucil-daemon server::test_find_references_tool` (the `acceptance_tests[0].selector` for `P2-W7-F05`, frozen per DEC-0007).

**Expected**: PASS, executing the frozen module-root test `test_find_references_tool` that asserts the `find_references` handler routes through `execute_g1` + `fuse_g1`, groups fused entries by `usage_type` (`call` / `import` / `type-annotation`), and emits the `_meta.references_by_usage` BTreeMap-sorted JSON object (per WO-0049 AC03–AC09).

**Actual**: cargo reports `0 passed; 0 failed; 1 filtered out` across every binary in the `ucil-daemon` crate. The test does NOT exist on the branch — `grep -nE 'pub async fn test_find_references_tool|fn test_find_references_tool' crates/ucil-daemon/src/server.rs` returns empty. Cargo's exit code is 0 but the result is a silent no-op, not a real pass — failure mode #6 from the critic checklist ("no test for feature X").

## Repro (verbatim, from a fresh shell)

```
$ cd /home/rishidarkdevil/Desktop/ucil-wt/WO-0049
$ git rev-parse HEAD
f197ad1389c0e2ef67b67e5364b45fc25970bcfd
$ git diff --name-only main...HEAD
crates/ucil-daemon/src/executor.rs
$ git diff --stat main...HEAD
 crates/ucil-daemon/src/executor.rs | 29 +++++++++++++++++++++++++----
 1 file changed, 25 insertions(+), 4 deletions(-)
$ env -u RUSTC_WRAPPER cargo test -p ucil-daemon server::test_find_references_tool -- --nocapture
   ...
test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 1 filtered out; finished in 0.00s
```

The "1 filtered out" line proves the binary CONTAINS tests under `server::*` but NONE matches the selector `server::test_find_references_tool` — the test is genuinely absent (not skipped, not ignored, not failing — never written).

```
$ grep -nE 'pub async fn test_find_references_tool|fn test_find_references_tool' \
    crates/ucil-daemon/src/server.rs
$ # (no output — selector returns zero hits)
$ grep -n 'handle_find_references\|G1SourceFactory\|with_g1_sources\|g1_sources' \
    crates/ucil-daemon/src/server.rs crates/ucil-daemon/src/lib.rs
$ # (no output — none of the required public symbols exist)
$ ls crates/ucil-daemon/src/g1_sources.rs
ls: cannot access 'crates/ucil-daemon/src/g1_sources.rs': No such file or directory
$ ls scripts/verify/P2-W7-F05.sh
ls: cannot access 'scripts/verify/P2-W7-F05.sh': No such file or directory
$ ls ucil-build/work-orders/0049-ready-for-review.md
ls: cannot access 'ucil-build/work-orders/0049-ready-for-review.md': No such file or directory
```

## Acceptance-criteria roll-up (work-order `acceptance` block)

| AC | Status | Evidence |
|----|--------|----------|
| AC01 — `cargo build -p ucil-daemon` exits 0 | PASS | branch builds (only the executor refactor landed; nothing new to break the build) |
| AC02 — `cargo clippy -p ucil-daemon --all-targets -- -D warnings` exits 0 | UNTESTED | irrelevant — feature absent makes the WO fail before clippy matters |
| AC03 — `cargo test -p ucil-daemon server::test_find_references_tool` exits 0 | **FAIL (vacuous)** | selector matches 0 tests; test never written |
| AC04 — `test_find_references_tool` lives at module root of `server.rs` | **FAIL** | function does not exist anywhere in `server.rs` |
| AC05–AC09 — sub-assertions on envelope, meta, bucketing, fused signature, total_references | **FAIL** | sub-assertions cannot run; their parent test does not exist |
| AC10 — `test_find_references_tool_unknown_symbol` exits 0 | **FAIL** | function does not exist |
| AC11 — `test_find_references_tool_missing_name_param` exits 0 | **FAIL** | function does not exist |
| AC12 — `executor::test_g1_result_fusion` regression | NOT-RUN | irrelevant in face of headline-test absence; no need to verify regression on a feature that was never shipped |
| AC13 — `executor::test_g1_parallel_execution` regression | NOT-RUN | same as AC12 |
| AC14 — `server::test_all_22_tools_registered` regression | NOT-RUN | same |
| AC15–AC22 — broader regression suites | NOT-RUN | same |
| AC23 — coverage gate `ucil-daemon 85 75` | NOT-RUN | a coverage gate over a missing implementation is meaningless; would either pass vacuously (no new code to cover) or fail because the absent handler has no coverage |
| AC24 — stub scan for `todo!()`/`unimplemented!()`/`FIXME` and `.expect()` outside `#[cfg(test)]` | PASS (vacuous-positive) | the 25 added lines on `executor.rs` are clean; the `executor.rs:1057` `.expect(...)` was correctly replaced with `flatten() + debug_assert_eq!`; the remaining `.expect(...)` calls in `executor.rs` (lines 1564–onward) are all inside the `#[cfg(test)]` block that begins at line 1559 |
| AC25 — three-dot diff allow-list match | **FAIL** | `git diff --name-only main...HEAD` returns ONLY `crates/ucil-daemon/src/executor.rs`; missing the required additions of `crates/ucil-daemon/src/g1_sources.rs`, `crates/ucil-daemon/src/server.rs`, `crates/ucil-daemon/src/lib.rs`, `scripts/verify/P2-W7-F05.sh`, `ucil-build/work-orders/0049-ready-for-review.md` |
| AC26 — no `Cargo.toml` modifications | PASS | diff touches only `executor.rs`; no manifest changes |
| AC27 — `lib.rs` re-exports for 5 new public symbols | **FAIL** | none of `G1SourceFactory`, `KgG1Source`, `SerenaG1Source`, `AstGrepG1Source`, `DiagnosticsG1Source` exist or are re-exported |
| AC28 — pre-baked mutation #1 (`find_references` route → `false`) | **UNSATISFIABLE** | the routing condition `name == "find_references" && self.g1_sources.is_some()` does not exist on the branch — there is nothing to mutate |
| AC29 — pre-baked mutation #2 (`KgG1Source::execute` body neutered) | **UNSATISFIABLE** | `KgG1Source` does not exist on the branch |
| AC30 — pre-baked mutation #3 (`BTreeMap` group-by loop neutered) | **UNSATISFIABLE** | `handle_find_references` does not exist on the branch |
| AC31 — `tests/fixtures/**` not modified | PASS | empty diff |
| AC32 — `feature-list.json` / schema not modified | PASS | empty diff |
| AC33 — master plan not modified | PASS | empty diff |
| AC34 — at least 4 commits on the branch | **FAIL** | branch has exactly 1 commit (`f197ad1`); cadence floor missed by 3 |
| AC35 — branch up-to-date with origin, working tree clean | PASS | `git rev-parse HEAD` (`f197ad1389c0e2ef67b67e5364b45fc25970bcfd`) matches `git rev-parse @{u}`; `git status --porcelain` empty |

**Summary**: 9 ACs FAIL outright (AC03, AC04, AC05–AC09 collapsed, AC10, AC11, AC25, AC27, AC34) and 3 ACs are UNSATISFIABLE because the code they target was never written (AC28, AC29, AC30). The pre-baked mutations are the authoritative anti-laziness layer per DEC-0007 and **cannot be applied** — that alone is sufficient to reject.

## Reality-check (mutation step 6b)

```
$ bash scripts/reality-check.sh P2-W7-F05
[reality-check] feature=P2-W7-F05 commit=f197ad1389c0e2ef67b67e5364b45fc25970bcfd
[reality-check] files:
  crates/ucil-daemon/src/executor.rs
[reality-check] Stashing:
  crates/ucil-daemon/src/executor.rs
No local changes to save
[reality-check] Running acceptance tests with code stashed — they MUST FAIL
[reality-check] FAILURE: acceptance tests reported ZERO tests run with code stashed — module was removed, not a genuine pass.
```

Reality-check correctly flagged the same "selector matches zero tests" failure mode, confirming the headline test is genuinely absent.

## Stub scan

The 25 lines added to `executor.rs` are clean — no new `todo!()`, `unimplemented!()`, `panic!("...not yet")`, `TODO`, or `FIXME` strings. The pre-existing `.expect(...)` at the old `executor.rs:1057` has been correctly removed and replaced with `slots.into_iter().flatten().collect::<Vec<_>>()` plus a `debug_assert_eq!` invariant — `scope_in` step 8 is genuinely closed. Per-source tracing instrumentation (`scope_in` step 7) is also genuinely landed via `#[tracing::instrument(name = "ucil.group.structural.source", ...)]` on `run_g1_source`. These two refactors are correct and should remain — they are the foundation the missing 14 scope_in items will build on.

## Quality gates

Coverage gate not run. With zero new implementation code on the branch (only a private-helper refactor in `executor.rs`), running coverage would either (a) pass vacuously because there is nothing new to cover, or (b) fail because the absent `handle_find_references` / `g1_sources.rs` impls have no coverage. Neither outcome is meaningful while the headline feature is missing.

## Suspected cause

The executor session ended after landing the two foundational refactors and **never wrote the body of work** — the `g1_sources.rs` module with the 4 `G1Source` impls, the `McpServer.with_g1_sources` builder + `find_references` route + `handle_find_references` method on `server.rs`, the 3 frozen module-root acceptance tests, the `lib.rs` re-exports + preamble paragraph, and `scripts/verify/P2-W7-F05.sh`. The absence of `ucil-build/work-orders/0049-ready-for-review.md` corroborates that the executor never declared the work-order complete; the branch is in an executor-incomplete state, not a feature-failure state.

The critic's BLOCKED verdict at `ucil-build/critic-reports/WO-0049.md` already named all 13 missing items with concrete `git ls-tree` / `git grep` evidence and explicitly recommended "Verifier: do NOT run." I ran the headline acceptance test anyway to capture the precise failure shape for this rejection record, but the conclusion is identical to the critic's.

## Next step

**Executor**: resume on `feat/WO-0049-find-references-and-g1-source-production-wiring` and land the missing 14 of 16 `scope_in` items in fresh commits per the critic's recommendation block (steps 1–6 of "Recommendation to executor" in `ucil-build/critic-reports/WO-0049.md`):

1. Create `crates/ucil-daemon/src/g1_sources.rs` with the 4 `G1Source` impls + `G1SourceFactory` (~250 LOC, split per impl as 4–5 commits).
2. Extend `crates/ucil-daemon/src/server.rs`: `McpServer.g1_sources` field, `with_g1_sources` builder, `find_references` route, `handle_find_references` method (~150 LOC, 1–2 commits).
3. Add the 3 module-root acceptance tests on `server.rs` per DEC-0007 (~300 LOC, 1 commit per DEC-0005 module-coherence allowance).
4. Update `crates/ucil-daemon/src/lib.rs`: `mod g1_sources;`, the 5 re-exports on a single line, and the WO-0049 preamble sentence.
5. Add `scripts/verify/P2-W7-F05.sh` per the standing convention.
6. Write `ucil-build/work-orders/0049-ready-for-review.md` once `cargo test -p ucil-daemon server::test_find_references_tool` is green locally.

Do NOT revert `f197ad1` — both refactors it landed are correct foundations for the remaining work.

Feature `P2-W7-F05` `attempts` increments to **1** with this rejection.

---

## Resolution — RESOLVED at retry-2 (2026-05-05T02:04:10Z)

**Resolved by**: `verifier-d249db74-379b-468b-b88d-5ac3141992df` (retry-2 verifier session)
**Flip commit**: `9b596ed` (`verify(daemon): WO-0049 PASS — flip P2-W7-F05 …`)
**Substantive code commit**: `54fccce0f80b303448d1eb5c229b94c5385a8424`
**Re-verification at retry-3**: `vrf-957a26ee-e961-4b82-87b9-9d351621b829` (this session) at branch HEAD `e3c3f41` — PASS-CONFIRMS

This rejection record is the **historic retry-1 artifact** that captured the executor's under-delivery (only `scope_in` steps 7+8 landed, 14 of 16 missing). The full remediation cycle subsequently executed:

| Time (IST) | Commit    | Event |
|------------|-----------|-------|
| 06:48      | `ddb1e3d` | Executor lands `g1_sources.rs` + 4 G1Source impls (+569 LOC) |
| 06:49      | `e382e57` | `lib.rs` preamble + 5 re-exports |
| 06:51      | `0f04589` | `find_references` route + `handle_find_references` handler |
| 06:53      | `342bc16` | `G1SourceFactory` closure-based interior |
| 06:56      | `caa12a6` | 3 module-root acceptance tests |
| 06:57      | `70b82bc` | `scripts/verify/P2-W7-F05.sh` |
| 07:00      | `5a59c3a` | `.expect` literal cleanup in invariant comment |
| 07:02      | `54fccce` | Ready-for-review marker |
| 07:14      | `063d2e6` | Critic verdict CLEAN (retry 2) |
| 07:36      | `9b596ed` | **Verifier flip — `P2-W7-F05.passes = true`** |

**Current state**: `P2-W7-F05.passes == true` on main (per `ucil-build/feature-list.json`), `last_verified_by = verifier-d249db74-…`, `last_verified_commit = 54fccce0…`, `attempts = 1`.

**Re-verification confirmation (2026-05-05T13:00:00Z by `vrf-957a26ee-…`)**: from a per-crate clean slate on the worktree, the headline `server::test_find_references_tool` (3 tests) + WO-0047 `executor::test_g1_parallel_execution` + WO-0048 `executor::test_g1_result_fusion` + WO-0010 `server::test_all_22_tools_registered` + full `server::` module suite (32 tests) + `scripts/verify/P2-W7-F05.sh` all green. `cargo build` + `cargo clippy --all-targets -- -D warnings` clean. Branch HEAD `e3c3f41` adds zero source code vs the prior verification target `54fccce` — only a re-affirmation note appended to the ready-for-review marker. See `ucil-build/verification-reports/WO-0049.md` "Re-verification at branch HEAD `e3c3f41`" section for the full re-verification log.

**Cross-references for the orchestrator**:
- Verification report: `ucil-build/verification-reports/WO-0049.md`
- Retry-2 critic CLEAN verdict: `ucil-build/critic-reports/WO-0049.md`
- Root-cause-finder note (already-shipped status): `ucil-build/verification-reports/root-cause-WO-0049.md`
- Prior RCA (executor under-delivery diagnosis): commit `3d20569`

**Action items for future verifier re-spawns**: this rejection record is closed. If the harness's stale-rejection detection re-fires on this file, the resolution marker above and the verification report are sufficient evidence that no remediation is required.

resolved: true
