---
retry: 3
rejected_at: 2026-04-18T08:43:56Z
verifier_session: vrf-d5e8f216-d3ae-4317-8909-ae39ee76aa99
branch: feat/WO-0024-kg-crud-and-hot-staging
head_commit: 7aacaa4da7707e09919130c66a946cd7e34ee9bf
---
# Rejection: WO-0024 (retry 3)

**Verifier session**: vrf-d5e8f216-d3ae-4317-8909-ae39ee76aa99
**Branch**: feat/WO-0024-kg-crud-and-hot-staging
**HEAD**: 7aacaa4da7707e09919130c66a946cd7e34ee9bf
**Rejected at**: 2026-04-18T08:43:56Z

## Scope of reject — state identical to retry 2

The same one criterion (`cargo doc -p ucil-core --no-deps`) that
rejected retry 1 and retry 2 still fails. The three `^error:` lines are
produced by the pre-existing ambiguous intra-doc links in
`crates/ucil-core/src/incremental.rs` (introduced silently by WO-0009,
commit `5c2739a`, first caught as a gate by WO-0024 because WO-0024 is
the first work-order whose `acceptance_criteria` includes `cargo doc`).

Between retry 2 (2026-04-18T08:31:58Z) and this retry 3
(2026-04-18T08:43:56Z) **zero work-order source changed**:

- Branch HEAD is still `7aacaa4` (identical to retry 1 & retry 2).
- `git diff origin/main..HEAD -- crates/ucil-core/src/incremental.rs` = **0 lines**.
- `origin/main` HEAD is now `8df64c3` (retry-2 RCA + verifier-artifact
  chore commits only since retry 2). `incremental.rs` on `origin/main`
  is byte-identical to the content that produced the retry-1 / retry-2
  failure.
- The Bucket-D micro-WO recommended by both the retry-1 RCA
  (`ucil-build/verification-reports/root-cause-WO-0024.md`) and the
  retry-2 RCA (same file, superseded content;
  `chore(rca): WO-0024 retry-2 root-cause — loop non-convergence`
  on `main` at commit `8df64c3`) has **not yet been emitted** by the
  planner.

The verifier has no authority to waive the failing criterion
(`.claude/agents/verifier.md:43-45` — "Any failure → OVERWRITE
rejection") and no authority to edit source
(`.claude/agents/verifier.md:51` — "Never edit source code").
Rejecting a third time is the protocol-correct move.

## Failed criterion

**Criterion (WO-0024 `acceptance_criteria[4]`)**:
```
cargo doc -p ucil-core --no-deps 2>&1 | { ! grep -qE '^(warning|error)'; }
```

**Expected**: exit 0 (no `^warning`/`^error` lines).
**Actual**: exit 1 — three `^error:` lines emitted by `rustdoc`,
identical to retry 1 and retry 2.

## Repro (this retry-3 session)

```
$ cd /home/rishidarkdevil/Desktop/ucil-wt/WO-0024
$ cargo clean              # removed 4063 files, 2.4 GiB
$ git clean -fdx -e ucil-build/ -e .env
$ cargo build --workspace  # finished dev profile in 3.26s
$ cargo doc -p ucil-core --no-deps 2>&1 | grep -E '^(warning|error)'
error: `symbol_count` is both a function and a struct
error: `dependent_metric` is both a function and a struct
error: could not document `ucil-core`

$ git diff origin/main..HEAD -- crates/ucil-core/src/incremental.rs | wc -l
0
```

Offending source (`crates/ucil-core/src/incremental.rs:5-6`, unchanged
from retry 1 and retry 2):

```rust
//! ([`FileRevision`]) to two tracked query functions ([`symbol_count`] and
//! [`dependent_metric`]) so the compiler, rustdoc, and the unit-test suite
```

`salsa::tracked fn` creates both a function AND a struct with each
name (`symbol_count` and `dependent_metric`), so `rustdoc` cannot
disambiguate the unadorned ``[`symbol_count`]`` and
``[`dependent_metric`]`` references. `rustdoc`'s own suggested fix is
a 4-character edit per link (append `()` for the function form or
prefix `struct@` for the struct form).

## Escalation trigger #1 — same feature rejected verifier ≥ 3 times

Per root `CLAUDE.md` "Escalation triggers":
> 1. Same feature fails verifier 3 times.

Both `P1-W4-F02` and `P1-W4-F08` have now been rejected three
consecutive times by the verifier (retries 1, 2, 3) for the identical
pre-existing-third-party-file reason. Per the escalation protocol this
warrants a user page (Bucket E in triage vocabulary) — the harness
needs either:

- **(A) the planner to emit the Bucket-D micro-WO** that the existing
  RCA recipe already specifies (2 files, +4 / -2 lines in
  `crates/ucil-core/src/incremental.rs`), let that land on `main`, and
  then re-verify WO-0024 (the branch at `7aacaa4` remains
  flip-ready); **OR**
- **(B) user intervention** — the loop cannot self-resolve this while
  `main` is missing the fix and the verifier has no authority to edit
  source.

An accompanying monitor escalation will be written alongside this
rejection at
`ucil-build/escalations/20260418-0844-WO-0024-retry3-same-failure.md`.

## All 17 other verifier checks PASSED (unchanged from retry 2)

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| 1 | `cargo nextest run -p ucil-core 'knowledge_graph::'` matches ≥ 7 PASS | **PASS** | 8 tests run, 8 passed (F02 frozen selector, `awk '{exit ($1 < 7)}'` is 0) |
| 2 | `cargo nextest run -p ucil-core knowledge_graph::test_hot_staging_writes` matches exactly 1 PASS | **PASS** | 1 test, 1 passed (F08 frozen selector) |
| 3 | `cargo build --workspace` | **PASS** | finished dev profile in 3.26s |
| 4 | `cargo clippy -p ucil-core --all-targets -- -D warnings` (pedantic + nursery) | **PASS** | 0 warnings/errors |
| **5** | **`cargo doc -p ucil-core --no-deps` no `^(warning\|error)`** | **FAIL** | **pre-existing regression from WO-0009, documented above and in retries 1 & 2** |
| 6 | `! grep -rn 'todo!\|unimplemented!\|#\[ignore\]'` in `knowledge_graph.rs` | PASS | 0 matches |
| 7 | Struct anchors — `pub struct Entity`, `Relation`, `HotObservation`, `pub enum WalCheckpointMode` | PASS | all 4 present |
| 8 | `fn upsert_entity` / `get_entity_by_qualified_name` / `get_entity_as_of` / `upsert_relation` / `stage_hot_observation` / `stage_hot_convention_signal` / `stage_hot_architecture_delta` / `checkpoint_wal` | PASS | all 8 present |
| 9 | Test-fn anchors — 7 `fn test_*` entries | PASS | all 7 present |
| 10 | `! grep -qE '^mod\s+tests\s*\{'` (DEC-0005 — no wrapper) | PASS | 0 matches |
| 11 | `chrono` in `crates/ucil-core/Cargo.toml` | PASS | line 24 |
| 12 | `chrono` in workspace `Cargo.toml` | PASS | line 78 |
| 13 | `default-features = false` in workspace `Cargo.toml` | PASS | line 78 |
| 14 | `pub use knowledge_graph::.*{Entity\|HotObservation\|Relation\|WalCheckpointMode}` in `lib.rs` | PASS | lines 27-29 (grouped single-line re-exports) |
| 15 | `! grep -qE 'self\.conn\.execute\('` in `knowledge_graph.rs` | PASS | 0 matches — every writer goes through `execute_in_transaction` |
| 16-18 | Forbidden crate diffs: treesitter / daemon / lsp-diagnostics | PASS | 0 lines each |
| r1 | `bash scripts/reality-check.sh P1-W4-F02` (automated) | FAIL-DOC. | Tripped the documented multi-commit false-positive same as WO-0014 / WO-0015 / WO-0016 / WO-0023 (escalation `20260415-1630-reality-check-per-file-rollback.md`). Rolls back only to the newest-touching-commit's parent; earlier WO-0024 commits leave the module intact. |
| r1 | Manual two-step mutation for P1-W4-F02 | **PASS** | With both `knowledge_graph.rs` and `lib.rs` rolled back to `origin/main` content: F02 selector matches 1 test (pre-existing `test_schema_creation`), fails F02's `≥ 7` floor. Restored: 8/8 PASS. Tests genuinely depend on the WO-0024 code. |
| r2 | `bash scripts/reality-check.sh P1-W4-F08` (automated) | FAIL-DOC. | Same false-positive. |
| r2 | Manual two-step mutation for P1-W4-F08 | **PASS** | Rolled back: F08 selector matches 0 tests, `error: no tests to run`. Restored: 1/1 PASS. |
| cov | `scripts/verify/coverage-gate.sh ucil-core 85 75` | **PASS** | 97% line coverage (floor 85%); branch coverage n/a on stable (nightly-gated); report at `ucil-build/verification-reports/coverage-ucil-core.md` |
| stubs | `grep` for `todo!()`, `unimplemented!()`, `NotImplementedError` over changed files (`knowledge_graph.rs`, `lib.rs`) | PASS | 0 occurrences |

## Suspected cause — unchanged from retries 1 & 2

Pre-existing rustdoc regression in `crates/ucil-core/src/incremental.rs`
introduced by WO-0009 (commit `5c2739a`) and caught for the first time
because WO-0024's gate includes `cargo doc` (earlier WOs did not). The
WO-0024 executor correctly flagged the issue in
`ucil-build/work-orders/0024-ready-for-review.md`; both RCAs
(retry-1 + retry-2 at
`ucil-build/verification-reports/root-cause-WO-0024.md`) recommend the
same **Bucket-D micro-WO** to fix 2 intra-doc links with a
4-character edit each.

## Next step — blocked pending Bucket-D micro-WO emission

**This reject is administrative**, not a rejection of the executor's
work. All quality checks on WO-0024's own deliverable remain green
(critic `CLEAN`, tests 8/8 + 1/1 PASS, 97% coverage, manual mutation
two-step confirms real code, zero stubs, zero forbidden-path diffs,
commit hygiene green).

The remediation path is unchanged from retry 1 and retry 2:

1. **Planner** emits the Bucket-D micro-WO from the RCA's recipe
   (`verification-reports/root-cause-WO-0024.md` — "What a new
   micro-WO should look like") — one commit, +4 / -2 lines on
   `crates/ucil-core/src/incremental.rs` only.
2. Executor implements; critic reviews; verifier passes that micro-WO;
   it merges to `main`.
3. Re-verify WO-0024 (this branch, commit `7aacaa4` remains viable
   as-is) — the gate will turn green on the first pass after the
   micro-WO lands; both P1-W4-F02 and P1-W4-F08 flip to
   `passes=true` with no further executor work on the
   `feat/WO-0024-kg-crud-and-hot-staging` branch.

**This retry-3 reject crosses escalation trigger #1** ("same feature
fails verifier 3 times") and calls for user intervention if the
planner does not self-escalate and emit the Bucket-D micro-WO on its
next pass. The companion escalation at
`ucil-build/escalations/20260418-0844-WO-0024-retry3-same-failure.md`
is the authoritative pointer.

## Features NOT flipped

- P1-W4-F02 → `passes` remains `false` (no verifier-driven `attempts`
  increment occurs on a reject path; per `.claude/agents/verifier.md:43-45`:
  "Do NOT flip anything").
- P1-W4-F08 → `passes` remains `false` (same).

The executor's implementation is preserved; no source code was modified
by this verifier session.
