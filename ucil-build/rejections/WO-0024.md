---
retry: 2
rejected_at: 2026-04-18T08:31:58Z
verifier_session: vrf-6dd0c8a8-46fa-4344-8c5e-85c928a05154
branch: feat/WO-0024-kg-crud-and-hot-staging
head_commit: 7aacaa4da7707e09919130c66a946cd7e34ee9bf
---
# Rejection: WO-0024 (retry 2)

**Verifier session**: vrf-6dd0c8a8-46fa-4344-8c5e-85c928a05154
**Branch**: feat/WO-0024-kg-crud-and-hot-staging
**HEAD**: 7aacaa4da7707e09919130c66a946cd7e34ee9bf
**Rejected at**: 2026-04-18T08:31:58Z

## Scope of reject — state identical to retry 1

17 of 18 verifier acceptance checks pass. The **same one criterion**
that rejected retry 1 still fails: `cargo doc -p ucil-core --no-deps`
emits the same three `^error:` lines driven by the pre-existing
ambiguous intra-doc links in `crates/ucil-core/src/incremental.rs`
(introduced silently by WO-0009 commit `5c2739a`).

Between retry 1 (2026-04-18T08:18:56Z) and this retry 2
(2026-04-18T08:31:58Z) **zero work-order source changed**:

- Branch HEAD is still `7aacaa4` (identical to retry 1).
- `git diff origin/main..HEAD -- crates/ucil-core/src/incremental.rs` = **0 lines**.
- `origin/main` HEAD is now `0d813cd` (only verifier-artifact + RCA chore commits on main since retry 1). `incremental.rs` on `origin/main` is still identical to the content that produced the retry-1 failure.
- The Bucket-D micro-WO that the RCA (`verification-reports/root-cause-WO-0024.md`) and the escalation (`escalations/20260418-0820-pre-existing-incremental-rustdoc-bug.md`) recommended has **not yet been emitted** by the planner.

The verifier has no authority to waive the failing criterion
(`.claude/agents/verifier.md:43-45` — "Any failure → OVERWRITE
rejection") and no authority to edit source (`.claude/agents/verifier.md:51`
"Never edit source code"). Rejecting a second time is the
protocol-correct move.

## Failed criterion

**Criterion (WO-0024 `acceptance_criteria[4]`)**:
```
cargo doc -p ucil-core --no-deps 2>&1 | { ! grep -qE '^(warning|error)'; }
```

**Expected**: exit 0 (no `^warning`/`^error` lines).
**Actual**: exit 1 — three `^error:` lines emitted by `rustdoc`, identical to retry 1.

## Repro (this retry-2 session)

```
$ cd /home/rishidarkdevil/Desktop/ucil-wt/WO-0024
$ cargo clean              # removed 4063 files, 2.4 GiB
$ git clean -fdx -e ucil-build/ -e .env
$ cargo build --workspace  # finished dev profile in 2.98s
$ cargo doc -p ucil-core --no-deps 2>&1 | grep -E '^(warning|error)'
error: `symbol_count` is both a function and a struct
error: `dependent_metric` is both a function and a struct
error: could not document `ucil-core`

$ git diff origin/main..HEAD -- crates/ucil-core/src/incremental.rs | wc -l
0
```

Offending source (`crates/ucil-core/src/incremental.rs:5-6`, unchanged
from retry 1):

```rust
//! ([`FileRevision`]) to two tracked query functions ([`symbol_count`] and
//! [`dependent_metric`]) so the compiler, rustdoc, and the unit-test suite
```

`salsa::tracked fn` creates both a function AND a struct with each name
(`symbol_count` and `dependent_metric`), so `rustdoc` cannot
disambiguate the unadorned `[`symbol_count`]` and `[`dependent_metric`]`
references. `rustdoc`'s own suggested fix is a 4-character edit per
link (append `()` for the function form or prefix `struct@` for the
struct form).

Pre-existence re-confirmed this session — cargo doc on the `origin/main`
tip via an ephemeral worktree `/tmp/main-probe-r2`:

```
$ git worktree add /tmp/main-probe-r2 origin/main
$ (cd /tmp/main-probe-r2 && cargo doc -p ucil-core --no-deps 2>&1 | grep -E '^(warning|error)')
error: `symbol_count` is both a function and a struct
error: `dependent_metric` is both a function and a struct
error: could not document `ucil-core`
$ (cd /tmp/main-probe-r2 && git log -1 --pretty=oneline)
0d813cd97fa2e63510f5a0ff1133af0416b7c6e2 chore(rca): WO-0024 root-cause — administrative reject; remediation is Bucket-D micro-WO
```

(Three-error output on `main`, no WO-0024 commits checked out, confirms
that the 3-error delta WO-0024 introduces over `origin/main` is **0**.)

## All 17 other verifier checks PASSED (unchanged from retry 1)

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| 1 | `cargo nextest run -p ucil-core 'knowledge_graph::'` matches ≥ 7 PASS | **PASS** | 8 tests run, 8 passed (F02 frozen selector, `awk '{exit ($1 < 7)}'` is 0) |
| 2 | `cargo nextest run -p ucil-core knowledge_graph::test_hot_staging_writes` matches exactly 1 PASS | **PASS** | 1 test, 1 passed (F08 frozen selector) |
| 3 | `cargo build --workspace` | **PASS** | finished dev profile in 2.98s |
| 4 | `cargo clippy -p ucil-core --all-targets -- -D warnings` (pedantic + nursery) | **PASS** | 0 warnings/errors |
| **5** | **`cargo doc -p ucil-core --no-deps` no `^(warning\|error)`** | **FAIL** | **pre-existing regression from WO-0009, documented above and in retry 1** |
| 6 | `! grep -rn 'todo!\|unimplemented!\|#\[ignore\]'` in `knowledge_graph.rs` | PASS | 0 matches |
| 7 | Struct anchors — `pub struct Entity`, `Relation`, `HotObservation`, `pub enum WalCheckpointMode` | PASS | all 4 present |
| 8 | `fn upsert_entity` / `get_entity_by_qualified_name` / `get_entity_as_of` / `upsert_relation` / `stage_hot_observation` / `stage_hot_convention_signal` / `stage_hot_architecture_delta` / `checkpoint_wal` | PASS | all 8 present |
| 9 | Test-fn anchors — 7 `fn test_*` entries | PASS | all 7 present |
| 10 | `! grep -qE '^mod\s+tests\s*\{'` (DEC-0005 — no wrapper) | PASS | 0 matches |
| 11 | `chrono` in `crates/ucil-core/Cargo.toml` | PASS | line 24 |
| 12 | `chrono` in workspace `Cargo.toml` | PASS | line 78 |
| 13 | `default-features = false` in workspace `Cargo.toml` | PASS | line 78 |
| 14 | `pub use knowledge_graph::.*{Entity\|HotObservation\|Relation\|WalCheckpointMode}` in `lib.rs` | PASS | lines 27-29 (grouped single-line re-exports) |
| 15 | `! grep -qE 'self\.conn\.execute\('` in `knowledge_graph.rs` | PASS | 0 matches — every writer goes through `execute_in_transaction` |
| 16-18 | Forbidden crate diffs: treesitter / daemon / lsp-diagnostics | PASS | 0 lines each |
| r1 | `bash scripts/reality-check.sh P1-W4-F02` (automated) | FAIL-DOC. | Tripped the documented multi-commit false-positive same as WO-0014 / WO-0015 / WO-0016 / WO-0023 (escalation `20260415-1630-reality-check-per-file-rollback.md`). Rolls back only to the newest-touching-commit's parent; earlier WO-0024 commits leave the module intact. |
| r1 | Manual two-step mutation for P1-W4-F02 | **PASS** | With both `knowledge_graph.rs` and `lib.rs` rolled back to `origin/main` content: F02 selector matches 1 test (pre-existing `test_schema_creation`), fails F02's `≥ 7` floor. Restored: 8/8 PASS. Tests genuinely depend on the WO-0024 code. |
| r2 | `bash scripts/reality-check.sh P1-W4-F08` (automated) | FAIL-DOC. | Same false-positive. |
| r2 | Manual two-step mutation for P1-W4-F08 | **PASS** | Rolled back: F08 selector matches 0 tests, `error: no tests to run`. Restored: 1/1 PASS. |
| cov | `scripts/verify/coverage-gate.sh ucil-core 85 75` | **PASS** | 97.10550887021475% line coverage (floor 85%); branch coverage n/a on stable (nightly-gated); report at `ucil-build/verification-reports/coverage-ucil-core.md` |
| stubs | `grep` / `ast-grep` for stubs over changed files | PASS | 0 occurrences of `todo!()`, `unimplemented!()`, bare-`pass` bodies, single-literal returns |

## Suspected cause — unchanged from retry 1 RCA

Pre-existing rustdoc regression in `crates/ucil-core/src/incremental.rs`
introduced by WO-0009 (commit `5c2739a`) and caught for the first time
because WO-0024's gate includes `cargo doc` (earlier WOs did not). The
WO-0024 executor correctly flagged the issue in
`ucil-build/work-orders/0024-ready-for-review.md`; the RCA
(`ucil-build/verification-reports/root-cause-WO-0024.md`) recommends
**Bucket-D micro-WO** to fix 2 intra-doc links with a 4-character edit
each.

## Next step — blocked pending Bucket-D micro-WO

**This reject is administrative**, not a rejection of the executor's
work. All quality checks remain green (critic `CLEAN`, tests 8/8 +
1/1 PASS, 97.10% coverage, reality-check mutation confirms real code,
zero stubs, zero forbidden-path diffs, commit hygiene green).

The remediation path is unchanged from retry 1:

1. **Planner** emits the Bucket-D micro-WO from the RCA's recipe
   (`verification-reports/root-cause-WO-0024.md` "What a new micro-WO
   should look like") — one commit, +4 / -2 lines on
   `crates/ucil-core/src/incremental.rs` only.
2. Executor implements; critic reviews; verifier passes that micro-WO;
   it merges to `main`.
3. Re-verify WO-0024 (this branch, commit `7aacaa4` remains viable
   as-is) — the 18/18 gate will turn green on the first pass after
   the micro-WO lands; both P1-W4-F02 and P1-W4-F08 flip to
   `passes=true` with no further executor work on the
   `feat/WO-0024-kg-crud-and-hot-staging` branch.

**This retry 2 reject is a monitor event**, not a substantive escalation
— the user-facing note is "still blocked on the same Bucket-D micro-WO
that the retry-1 RCA already prescribed; no progress without a planner
pass that emits it." The companion escalation
(`ucil-build/escalations/20260418-0820-pre-existing-incremental-rustdoc-bug.md`)
remains the authoritative pointer.

## Features NOT flipped

- P1-W4-F02 → `passes` remains `false` (no verifier-driven attempts increment occurs on a reject path; per `.claude/agents/verifier.md:43-45`: "Do NOT flip anything").
- P1-W4-F08 → `passes` remains `false` (same).

The executor's implementation is preserved; no source code was modified
by this verifier session.
