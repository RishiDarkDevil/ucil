---
timestamp: 2026-05-05T18:01:39Z
type: merge-failure
work_order: WO-0052
branch: feat/WO-0052-session-dedup
severity: high
blocks_loop: true
resolved: true
---

# Merge failure for WO-0052

git merge --no-ff feat/WO-0052-session-dedup into main hit conflict(s) even after rebase. Unexpected; manual resolution.

## Repro

```
git checkout main && git pull && git merge --no-ff feat/WO-0052-session-dedup
```

## Recommended action

Human resolves the conflict manually, commits + pushes main, then resolves
this escalation.

## Resolution

Resolved 2026-05-05 by user-authorised monitor session intervention.

Conflict was in `ucil-build/feature-list.json` only (one entry: P2-W7-F04).
The feat branch's verifier-2882192e wrote a PASS flip (passes=true,
attempts=0, last_verified_commit=4ae34085) at 2026-05-05T18:00:19Z while
main concurrently incremented `attempts` to 1 from the retry-1 rejection
cycle (verifier-def33d94 at 2026-05-05T17:29:20Z).

Merge resolution:
- Take feat-branch values for `passes`, `last_verified_ts`,
  `last_verified_by`, `last_verified_commit` (the legitimate retry-2 PASS).
- Preserve `attempts: 1` from main per the harness's monotonically-increasing
  attempts contract — the rejection cycle still counts even though the
  feature ultimately passed.

Merge commit: `18b6798 merge: WO-0052 session-scoped deduplication
(feat → main)`. Pushed to origin/main. P2 advanced 12 → 13 / 25.
