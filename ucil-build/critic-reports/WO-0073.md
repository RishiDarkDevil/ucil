# Critic Report: WO-0073

**Critic session**: crt-WO-0073-20260508
**Branch**: `feat/WO-0073-g4-architecture-parallel-query`
**Ref**: `3b548c98ec645bbd512ae8799181c9405dcf5911`
**Merge base**: `769615c0b98c33e572d6b26cd21493976eb169a7` (`main`)
**Feature(s)**: `P3-W9-F09` — G4 (Architecture) parallel-query orchestrator + edge-union/BFS-blast-radius merger
**Verdict**: **CLEAN**

---

## Diff scope

```
 crates/ucil-daemon/src/executor.rs              | 737 +++++++++++++++++++
 crates/ucil-daemon/src/g4.rs                    | 898 ++++++++++++++++++++++++
 crates/ucil-daemon/src/lib.rs                   |   3 +
 scripts/verify/P3-W9-F09.sh                     | 117 +++
 ucil-build/work-orders/0073-ready-for-review.md | 197 ++++++
 5 files changed, 1952 insertions(+), 0 deletions(-)
```

5 commits on `feat/WO-0073-g4-architecture-parallel-query`:

| sha | type | size (+) | subject |
|-----|------|---------:|---------|
| `78f813f` | `feat(daemon)` | 898 | add g4.rs — G4 architecture parallel orchestrator + edge-union/BFS merger |
| `e08f8fa` | `feat(daemon)` | 3 | wire pub mod g4 + pub use block in lib.rs |
| `5c5b447` | `test(daemon)` | 737 | frozen test_g4_architecture_query at module root in executor.rs |
| `5040685` | `feat(verify)` | 117 | add scripts/verify/P3-W9-F09.sh for G4 architecture frozen test |
| `3b548c9` | `chore(rfr)` | 197 | WO-0073 ready-for-review marker with M1/M2/M3 contract + AC matrix |

Every commit body carries `Phase: 3`, `Feature: P3-W9-F09`, and `Work-order: WO-0073` trailers (3 trailers per commit, 5/5).

---

## Findings

### Blockers (must fix before verifier)

**None.**

### Warnings (should fix before gate)

1. **Soft-target subject-line length over-runs.** Four commit subjects exceed the 70-char "soft target" called out in `.claude/rules/commit-style.md` (`78f813f` 89 chars, `5c5b447` 77, `5040685` 77, `3b548c9` 78). The work-order itself acknowledges 70 as a *soft* target and the body content is descriptive and well-trailered. Same range as the WO-0070 (G3) precedent, which passed verifier. **Not a blocker.**

### OK

1. **Stub detection — clean.**
   - `crates/ucil-daemon/src/g4.rs` — no `todo!()`, `unimplemented!()`, `pass`-only bodies, or trivial-default returns.
   - `crates/ucil-daemon/src/executor.rs` (new region 3724–4459) — clean.
   - The single `None` literal in `g4.rs:879` is the alternative branch of an `if … {Some(...)} else {None}` inside `find_map` — legitimate algorithmic value, not a stub.
   - AC19 grep `rg 'todo!\(|unimplemented!\(|#\[ignore\]'` over `g4.rs` + `executor.rs` returns empty.
2. **No mocks of critical dependencies.**
   - Word-ban grep `rg -i 'mock|fake|stub'` over `crates/ucil-daemon/src/g4.rs` and `scripts/verify/P3-W9-F09.sh` returns empty (AC17 ✓).
   - The new test region in `executor.rs` mentions `mock|fake|stub` only inside `// NOT a mock` doc comments (lines 3737, 3819) — no actual mocking.
   - `TestG4Source` in `executor.rs:3822` is a UCIL-internal `G4Source` impl (DEC-0008 §4 dependency-inversion seam), the same shape as the WO-0047 `TestG1Source` and WO-0070 `TestG3Source` precedents — explicitly carved out by WO-0069 lessons.
   - No new mocks of Serena, LSP servers, SQLite, LanceDB, or Docker.
3. **No skipped / ignored tests.**
   - `rg '#\[ignore\]|\.skip\(|xit\(|@pytest\.mark\.(skip|xfail)'` over the diff returns empty.
4. **No weak assertions.**
   - `rg 'assert!\(true\)|assert_eq!\(true,\s*true\)|expect\(true\)'` over the diff returns empty.
   - All 30+ new `assert!` / `assert_eq!` / `assert!` calls carry meaningful left/right comparisons against literal expected values, plus DEC-0007-format `(SAn) <semantic name>; left: …, right: …` panic bodies.
5. **No hallucinated paths / imports.**
   - `g4.rs` imports — `std::collections::{BTreeMap, BTreeSet, HashMap, VecDeque}`, `std::future::Future`, `std::pin::Pin`, `std::task::Poll`, `std::time::Duration`, `serde::{Deserialize, Serialize}` — all resolve.
   - Attribute deps `async_trait`, `tracing`, `tokio::time::*` — all present in `ucil-daemon` `Cargo.toml` (verified via `cargo metadata`); no new deps added (AC11 ✓).
   - New test region in `executor.rs` imports `crate::g4::{execute_g4, merge_g4_dependency_union, G4BlastRadiusEntry, G4DependencyEdge, G4EdgeKind, G4EdgeOrigin, G4Outcome, G4Query, G4Source, G4SourceOutput, G4SourceStatus, G4_PER_SOURCE_DEADLINE}` — every symbol re-exported via `lib.rs:193` `#[rustfmt::skip] pub use g4::{…}` block.
6. **Feature coverage.**
   - Single feature `P3-W9-F09` covered by the new module-root test `crates/ucil-daemon/src/executor.rs:3790 pub async fn test_g4_architecture_query()`.
   - The frozen `feature-list.json` selector `-p ucil-daemon executor::test_g4_architecture_query` resolves cleanly without a `tests::` intermediate (DEC-0007 module-root placement, AC08 ✓ via `^[[:space:]]*(pub )?(async )?fn test_g4_architecture_query` grep guard in the verify script).
   - Eight DEC-0007-format SA assertions (SA1–SA8) cover parallelism (SA1), edge-union dedup (SA2), disjoint-source emission (SA3), BFS depth-tracking (SA4), multiplicative coupling propagation (SA5), partial-Errored resilience (SA6), partial-TimedOut resilience (SA7), master-deadline-trip (SA8). `git diff main…feat -- executor.rs | grep -cE '^\+.*\(SA[1-9])` reports 37 lines containing SA tags; distinct labels confirmed `(SA1)…(SA8)` (8/8, ≥7 required, AC18 ✓).
7. **Commit hygiene.**
   - 5/5 commits use Conventional Commits (`feat`, `test`, `chore`).
   - 5/5 commits carry `Phase: 3`, `Feature: P3-W9-F09`, `Work-order: WO-0073` trailers (AC28 ✓).
   - `git log feat/WO-0073-g4-architecture-parallel-query ^main --merges | wc -l` = 0 (AC26 ✓).
   - All 5 commits are by `RishiDarkDevil <rishi8001100192@gmail.com>` 2026-05-08; chronological order is monotonic.
   - No `--no-verify` / `--amend` / force-push markers visible in reflog (no published-history rewrite).
   - The two large commits (`78f813f` 898 LOC, `5c5b447` 737 LOC) are explicitly justified in their bodies under DEC-0005 module-coherence + DEC-0007 frozen-test-at-module-root carve-outs — same precedent as WO-0067 / WO-0068 / WO-0070.
8. **Public API documentation — complete.**
   - All 16 `pub (fn|struct|enum|trait|const)` items in `g4.rs` (lines 100, 115, 127, 153, 185, 204, 233, 245, 268, 286, 305, 321, 365, 538, 697) carry rustdoc with cross-references to master-plan §5.4 lines 483-500, DEC-0008 §4, and `# Examples` blocks on `execute_g4` (line 514) + `merge_g4_dependency_union` (line 664).
   - Module-level rustdoc (lines 1–77) covers pipeline shape, merge contract, ground-truth-on-conflict sentinel, and no-substitute-impls policy.
   - The frozen-test function `test_g4_architecture_query` (line 3744) carries an SA1–SA8 enumeration in its rustdoc.
9. **Forbidden paths — clean.**
   - `git diff main -- <forbidden_paths>` empty for: `feature-list.json`, `feature-list.schema.json`, `ucil-master-plan-v2.1-final.md`, `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`, `crates/ucil-core/src/{cross_group.rs,fusion.rs,ceqp.rs}`, `crates/ucil-daemon/src/{g2_search.rs,g3.rs}`, `crates/ucil-daemon/tests/{g3_plugin_manifests.rs,plugin_manifests.rs,g4_plugin_manifests.rs}` (AC20 + AC27 ✓).
10. **Lessons-learned applied.**
    - Per-source deadline as unconditional const not min'd with master (`g4.rs:115`, `:557`, comment block `:543-555` cites WO-0068 + WO-0070 lessons-learned).
    - `#[allow(dead_code)]` on `join_all_g4` (`g4.rs:431`) so the M1 sequential-await mutation compiles cleanly under `#![deny(warnings)]`.
    - SA1 upper bound tightened to 500 ms (NOT round-number 700 ms) per WO-0070 lesson "compute parallelism dual-bound from `max_per_source_sleep_ms × N/2`" — verified at `executor.rs:3946`.
    - `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` for parallelism observability (`executor.rs:3789`).
    - Alphabetical `pub mod` and `pub use` placement (g4 between g3 and lancedb_indexer) at `lib.rs:160` and `lib.rs:193`.
    - Ground-truth-on-conflict TODO sentinel in `merge_g4_dependency_union` at `g4.rs:725-733` per scope_in #22 + AC30 ✓.
    - `#[tracing::instrument(name = "ucil.group.architecture", …)]` decorator at `g4.rs:532-537` per master-plan §15.2 + WO-0067 lesson.
11. **Verify script hygiene.**
    - `scripts/verify/P3-W9-F09.sh` committed with mode `100755` (executable).
    - `set -euo pipefail`, `IFS=$'\n\t'`, no `cd` outside `git rev-parse --show-toplevel`.
    - Rename-drift guards on the 5 frozen public symbols (`G4Source`, `execute_g4`, `merge_g4_dependency_union`, `G4_MASTER_DEADLINE`, `G4_PER_SOURCE_DEADLINE`) at lines 34, 39, 44, 49, 56.
    - Frozen-selector grep `^[[:space:]]*(pub )?(async )?fn test_g4_architecture_query\(\)` at line 71 enforces module-root placement (DEC-0007).
    - Optional shellcheck self-lint at line 107.
    - No mocks / no env-gated short-circuits / no API keys / no plugin-manager invocation — fully synthetic.
12. **`.expect()` on the `join_all_g4` slot fill** (`g4.rs:464`) is the same precedent shape as `executor::join_all_g1:1067` (WO-0047) and `g3::join_all_g3:376` (WO-0070), both of which passed verifier review. The expect is structurally unreachable: the outer `poll_fn` only returns `Ready` when every slot has been filled. **Not a violation** — follows the verified precedent.

---

## Verdict

**CLEAN.**

The diff implements P3-W9-F09 cleanly under the WO-0073 contract: a real
G4 architecture parallel orchestrator (`execute_g4`), a deterministic
edge-union + BFS-blast-radius merger (`merge_g4_dependency_union`), a
DEC-0008 §4 dependency-inversion `G4Source` trait with no mocks of
critical wire formats, and a frozen module-root acceptance test
exercising eight DEC-0007-numbered sub-assertions. M1/M2/M3 mutation
contract is pre-baked and verifier-runnable. The single soft warning
(commit subject length) does not block the verifier.

No blockers. The verifier may proceed.
