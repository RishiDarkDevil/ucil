---
work_order: WO-0021
slug: daemon-lifecycle-and-session-state
branch: feat/WO-0021-daemon-lifecycle-and-session-state
ref: 22db0c26dd6280a8d3cb3101ad872dd48ae01a76
merge_base: 1dcea8ccc4ef37b362b732c8f98149563dcf2920
critic_session: crt-WO-0021-20260418
features: [P1-W3-F01, P1-W4-F07]
verdict: CLEAN
---

# Critic Report: WO-0021 — daemon lifecycle + session state

## Summary

Reviewed 6 authoring commits + 1 ready-for-review marker on
`feat/WO-0021-daemon-lifecycle-and-session-state`, spanning
`crates/ucil-daemon/src/{lib.rs,lifecycle.rs,session_manager.rs,session_ttl.rs}`
and `ucil-build/work-orders/0021-ready-for-review.md`. Every check from
`.claude/agents/critic.md` passes.

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

_None._

### OK

#### 1. Stub detection — CLEAN
No `todo!()`, `unimplemented!()`, `NotImplementedError`, `raise NotImplementedError`,
`pass`-only bodies, or trivial-return stubs were added.

Searched: `git diff main..feat/WO-0021-… -- crates/ucil-daemon/src/` — no
matches for `todo!|unimplemented|NotImplementedError|#[ignore]|\.skip\(|assert!\(true\)`.

The only literal-returning branches in the new code are legitimate:
- `crates/ucil-daemon/src/session_manager.rs:52` — `now_unix_secs()`
  returns `0` via `.unwrap_or(0)` on clock-before-`UNIX_EPOCH`, which
  mirrors the existing `create_session` pattern for the same edge case.
  Documented in the function's rustdoc.

#### 2. Mocked critical dependencies — CLEAN
No mocks / fakes / stubs of Serena, LSP servers, SQLite, LanceDB, or
Docker introduced. `git diff … | grep -iE 'mock|fake|MagicMock|Mockall'`
returns zero matches in the added lines.

Tests use real collaborators:
- `crates/ucil-daemon/src/lifecycle.rs:244` and below — `tempfile::TempDir`
  and real `std::fs` for PID-file I/O; no filesystem mock.
- `crates/ucil-daemon/src/session_manager.rs:432` `test_session_state_tracking`
  — real `git` subprocess via `SessionManager::create_session` → `detect_branch`,
  no mock of `tokio::process::Command`.

#### 3. Skipped / ignored tests — CLEAN
No `#[ignore]`, `.skip()`, `xit`, `xfail`, or `pytest.mark.skip` introduced.

#### 4. Weak assertions — CLEAN
Every added assertion checks a behavioural invariant:
- `lifecycle::test_pid_file_write_creates_file_with_current_pid` — verifies
  file contents equal `std::process::id()`.
- `lifecycle::test_pid_file_drop_removes_file` — verifies file is gone
  after guard drops.
- `session_ttl::test_default_ttl_is_one_hour` is a constant-pin assertion
  `assert_eq!(DEFAULT_TTL_SECS, 3600)` — guards against an accidental edit
  to the policy constant; not a tautology.
- `session_manager::test_session_state_tracking` asserts length, content,
  BTreeSet de-dup, round-trip, and purge-count — no weak checks.

#### 5. Hallucinated imports — CLEAN
All `use` statements in the new files resolve against
`crates/ucil-daemon/Cargo.toml` dependencies:
- `thiserror` — workspace dep (`Cargo.toml:thiserror = "2"`).
- `tokio::signal::unix::{signal, SignalKind}`,
  `tokio::{sync::RwLock, process::Command, time::timeout}` — `tokio`
  declared with `features = ["full"]` at workspace root.
- `serde::{Deserialize, Serialize}`, `uuid::Uuid` — workspace deps.
- `tempfile::TempDir` — `[dev-dependencies]` in `crates/ucil-daemon/Cargo.toml`.
- `std::{collections::{BTreeSet, HashMap}, fs::OpenOptions, io::{Read, Write}, path::{Path, PathBuf}, process::id, time::{SystemTime, UNIX_EPOCH}}` — std.
- `crate::session_ttl::{compute_expires_at, is_expired, DEFAULT_TTL_SECS}`
  — `session_ttl` module is declared at `crates/ucil-daemon/src/lib.rs:25`.

#### 6. Feature coverage — CLEAN
Every feature in the work-order has dedicated test coverage that
references its behaviour:

- **P1-W3-F01** (daemon process lifecycle — PID file + SIGTERM/SIGHUP):
  10 module-root tests in `crates/ucil-daemon/src/lifecycle.rs`
  (`test_pid_file_*`, `test_shutdown_reason_*`, `test_lifecycle_pid_file_path_*`,
  `test_pid_file_error_*`). Frozen selector `lifecycle::` resolves with
  exact path semantics — no `mod tests { }` wrapper in the file
  (`grep -c '^mod tests' lifecycle.rs → 0`).
- **P1-W4-F07** (session state tracking — call history / inferred_domain /
  files_in_context / TTL + purge): `session_manager::test_session_state_tracking`
  at `crates/ucil-daemon/src/session_manager.rs:432`, BEFORE the existing
  `mod tests { }` block at line 500 — satisfies the DEC-0005 frozen-selector
  placement rule. Covers all five WO scenarios (two calls, BTreeSet dedup,
  inferred-domain round-trip, TTL arithmetic, purge + post-purge lookup).

#### 7. Commit hygiene — CLEAN
7 commits on the branch, all following Conventional Commits:

| SHA | Type/scope | Subject | Size | Note |
|-----|-----------|---------|------|------|
| 05bbe9a | feat(daemon) | add lifecycle module with PidFile and ShutdownReason | 381+ | Module-intro exception under DEC-0005, cited in body |
| b283a46 | feat(daemon) | add session state tracking — call history, files, domain, TTL | 197+ | Within DEC-0005 400L soft ceiling |
| 7a2abcf | chore(daemon) | re-export CallRecord + DEFAULT_TTL_SECS + fix intra-doc links | 7+/5- | |
| 62c4696 | docs(daemon) | clarify lifecycle module's role in the library root | 7+/2- | |
| ceee20b | refactor(daemon) | extract session TTL helpers into session_ttl module | 134+/6- | New module, DEC-0005 applies |
| 370ce15 | docs(daemon) | document session_manager + session_ttl roles in library root | 7+ | |
| 22db0c2 | chore(WO-0021) | mark ready-for-review — daemon lifecycle + session state | 73+ | |

- All 7 commits contain `Phase:`, `Feature:`, and `Work-order:` trailers
  (`git log --format=%B | grep -cE '^(Phase\|Feature\|Work-order):' → 22`
  which is 3 trailers × 6 commits + 4 trailers on the ready-for-review
  commit (two Feature lines)).
- Co-authorship trailer present on every commit.
- Largest commit 05bbe9a is 381 lines but is a new-module-introduction
  commit and the body explicitly cites DEC-0005 ("Module-intro commit is
  intentionally larger than 50 lines per DEC-0005 (type + impl + tests
  ship together for review-friendly coherence)"). Consistent with the
  DEC-0005-sanctioned exception.
- No `--amend`-after-push or force-push evidence (linear history from
  merge-base `1dcea8c`, all commits pushed once).

#### 8. Docs + public API — CLEAN
Every new `pub` item has rustdoc with clear intent, and every
`Result`-returning public fn has an `# Errors` section:

- `lifecycle::PidFile`, `lifecycle::PidFileError` (+ `#[non_exhaustive]`),
  `lifecycle::ShutdownReason`, `lifecycle::Lifecycle`, `lifecycle::wait_for_shutdown`,
  `lifecycle::PidFile::{write,read,path}`, `lifecycle::Lifecycle::{new,pid_file_path,run_until_shutdown}`
  — all documented.
- `session_manager::CallRecord`, `session_manager::DEFAULT_TTL_SECS`
  (re-export pointer documented), all four new `SessionInfo` fields,
  and the five new `SessionManager` methods (`record_call`,
  `add_file_to_context`, `set_inferred_domain`, `set_ttl`, `purge_expired`)
  — all documented.
- `session_ttl::DEFAULT_TTL_SECS`, `session_ttl::compute_expires_at`,
  `session_ttl::is_expired` — all documented with `# Examples`.
- `lib.rs` module-level rustdoc updated to describe the new `lifecycle`
  and `session_manager` + `session_ttl` pair.

`#![deny(rustdoc::broken_intra_doc_links)]` is enforced at
`crates/ucil-daemon/src/lib.rs:22` — `cargo doc` would fail if any link
were broken.

## Scope observance

Forbidden-path diff counts (all zero, matching executor claims in
`0021-ready-for-review.md`):
- `crates/ucil-{core,treesitter,lsp-diagnostics,embeddings,agents,cli}` → 0 lines.
- `crates/ucil-daemon/src/{main,storage,server,plugin_manager}.rs` → 0 lines.
- `ucil-build/feature-list.json`, `tests/fixtures/`, `scripts/gate/`,
  `scripts/flip-feature.sh`, `ucil-master-plan-v2.1-final.md` → 0 lines.

## Notes for the verifier

- `reality-check.sh P1-W3-F01` and `reality-check.sh P1-W4-F07` are
  claimed PASS in the ready-for-review marker. Note that the branch
  contains three deliberate documentation commits (62c4696, ceee20b,
  370ce15) specifically to ensure the per-file rollback in
  `scripts/reality-check.sh` lands on a baseline that still declares
  `pub mod lifecycle;` / `pub mod session_ttl;` so stashing the feature
  source files produces compile errors (mutation-style oracle). The
  verifier should independently re-run both `reality-check.sh` invocations
  from a clean worktree.
- Frozen-selector rule (DEC-0005) is satisfied: `fn test_session_state_tracking`
  at line 432 sits strictly before `mod tests {` at line 500 in
  `session_manager.rs`. Verifier should confirm with the awk predicate
  in `acceptance_criteria[7]` of the WO JSON.
- DEC-0007 is the unblocking reason; per-WO cargo-mutants gate is no
  longer part of the verifier cycle.

## Verdict

**CLEAN** — no blockers, no warnings. Work-order is ready for the
verifier in a fresh session.
