---
work_order: WO-0038
slug: lsp-bridge-integration-test-suite
feature_ids: [P1-W5-F08]
branch: feat/WO-0038-lsp-bridge-integration-test-suite
ref: e0b853139e4f9de100dbdec9faf7aadd2e1e5e57
head_commit: 1eafc540accc9d5d40c75b4aa98f240f1f5d2124
critic_session: crt-WO-0038-retry2-2026-04-19
verdict: CLEAN
reviewed_at: 2026-04-19
retry: 2
supersedes: 8d4365b (retry-1 BLOCKED)
---

# Critic Report: WO-0038 — lsp-bridge-integration-test-suite (retry 2)

**Critic session**: `crt-WO-0038-retry2-2026-04-19`
**Branch**: `feat/WO-0038-lsp-bridge-integration-test-suite`
**Commit range**: `main..e0b8531` (8 commits; retry-2 HEAD of the author-authored tree is `1eafc54`)
**Verdict**: **CLEAN**

This report supersedes the retry-1 BLOCKED review. Both retry-1 blockers (mixed-project rustdoc literal + nextest summary regex) are resolved on this retry; all other findings remain as previously analysed.

## Scope of diff

| Path | Change | LOC (net) |
|------|--------|-----------|
| `Cargo.toml` | modified (workspace member append) | +1 |
| `Cargo.lock` | modified (register new crate) | +13 |
| `tests/integration/.gitkeep` | deleted | 0 |
| `tests/integration/Cargo.toml` | new | +25 |
| `tests/integration/src/lib.rs` | new | +5 |
| `tests/integration/test_lsp_bridge.rs` | new | +743 |
| `ucil-build/work-orders/0038-lsp-bridge-integration-test-suite.json` | modified (retry-2 planner remediation) | +1 / −1 |
| `ucil-build/work-orders/0038-ready-for-review.md` | new (ready-for-review retry-2 marker) | +157 |
| `ucil-build/verification-reports/WO-0038.md` | new (retry-1 reject report) | +128 |

Eight commits on the branch (chronological):

1. `754b6b9` — `build(workspace): add tests/integration as workspace member`
2. `6968a0e` — `build(tests-integration): land ucil-tests-integration skeleton`
3. `971f10b` — `test(tests-integration): ship test_lsp_bridge across four fixtures`
4. `8d4365b` — `chore(ready-for-review): WO-0038 marker` (retry 1)
5. `d6f27e5` — `chore(verifier): WO-0038 retry-1 REJECT — mixed-project literal + nextest regex gate`
6. `512004b` — `docs(tests-integration): cite tests/fixtures/mixed-project in test rustdoc` ← RCF Remediation B
7. `1eafc54` — `chore(planner): amend WO-0038 acceptance_criteria[0] per RCF remediation A` ← RCF Remediation A
8. `e0b8531` — `chore(ready-for-review): WO-0038 retry-2 marker — both RCF remediations applied`

## Findings

### Blockers (must fix before verifier)

**None.** Both retry-1 blockers are resolved on this retry (see "Retry-1 blocker resolution" below).

### Warnings (should fix before gate; not blocking verifier)

1. **Commit `971f10b` is 743 LOC of new Rust (+13 LOC `Cargo.lock` churn), well above the ~50-LOC commit-style soft target** (`.claude/rules/commit-style.md`). The other commits on the branch are all appropriately small (1–157 LOC). Splitting `LocalScriptedFake` + helpers first, then each per-fixture test in its own commit, would yield review-friendlier chunks. Not a blocker: the file is cohesive and the tests share helpers through the fixture-coverage guard. Same warning as retry 1; no new code landed that would allow a retrospective split.

    **Cite**: commit `971f10b` (`test(tests-integration): ship test_lsp_bridge across four fixtures`).

2. **Work-order / code deviation — `lsp_types::Uri` vs `lsp_types::Url`.** The WO `plan_summary` and `scope_in` bullets reference `lsp_types::Uri`, but the `SerenaClient` trait at `crates/ucil-lsp-diagnostics/src/diagnostics.rs:156,165,175,184` takes `lsp_types::Url` (re-export of `url::Url`), not `Uri`. The executor correctly used `Url` (the trait cannot be modified per `scope_out`) and documented the deviation in commit `971f10b`'s body and in the module-level rustdoc at `tests/integration/test_lsp_bridge.rs:36-44`. This is a planner-side WO-text bug; the code matches the real trait surface. Known-accepted deviation, unchanged across retries.

    **Cite**: `tests/integration/test_lsp_bridge.rs:36-44`; `tests/integration/test_lsp_bridge.rs:73`; commit `971f10b` body final paragraph.

3. **Role-spanning remediation in commit `1eafc54`.** Commit `1eafc54` modifies `ucil-build/work-orders/0038-lsp-bridge-integration-test-suite.json` (the planner-authored work-order) from the executor retry loop. The subject is `chore(planner): …` to preserve role attribution in the git log, and the commit body cites `verification-reports/root-cause-WO-0038.md §Remediation A` as authority. This is the RCF-sanctioned single-line text fix to `acceptance_criteria[0]`; no ADR required per the RCF. Not a blocker, but worth flagging to the verifier so the role-mixing is visible.

    **Cite**: commit `1eafc54` (`chore(planner): amend WO-0038 acceptance_criteria[0] per RCF remediation A`).

### OK

- **Stub detection** — no `todo!()`, no `unimplemented!()`, no `panic!("stub")`, no `NotImplementedError` anywhere in the diff. Verified via `grep -nE 'todo!\(\)|unimplemented!\(\)|panic!\("stub|NotImplementedError'` in `tests/integration/` → zero hits. The `call_hierarchy_outgoing` + `type_hierarchy_supertypes` trait-method bodies return `Ok(Vec::new())` at `tests/integration/test_lsp_bridge.rs:194,203` with an explanatory comment — this matches the LSP-semantic "empty response" and mirrors the `ScriptedFakeSerenaClient` precedent at `crates/ucil-lsp-diagnostics/src/diagnostics.rs:388-462`. Not a stub of the F08 feature itself (F08 only exercises `diagnostics` + `call_hierarchy_incoming`).

- **Mocked critical collaborators** — none. `LocalScriptedFake` is a concrete `impl SerenaClient` against UCIL's own trait boundary, not a mock of Serena's MCP wire format, nor of an LSP server process, nor of SQLite/LanceDB/Docker. The rustdoc at `tests/integration/test_lsp_bridge.rs:16-25,95-110` explicitly calls this out and cites DEC-0008 + the `ScriptedFakeSerenaClient` precedent already accepted under the P1-W5-F05 / P1-W5-F06 verifier flips. `KnowledgeGraph::open` runs against a real on-disk SQLite DB inside a `tempfile::TempDir` (real `rusqlite`, not mocked).

- **Skipped / ignored tests** — none. `grep -nE '#\[ignore\]|\.skip\(|xfail|xit\(|\.only\('` returns zero hits in `tests/integration/`. All four `#[tokio::test]` + one sync `#[test]` compile into the binary and run unconditionally.

- **Weak assertions** — none. `grep -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)'` → zero hits. All four `#[tokio::test]` bodies make substantive assertions: `assert_eq!(count, 2)` / `assert_eq!(count, 3)` on row counts (`test_lsp_bridge.rs:395,480,557,666`), `assert!(paths.iter().all(|p| p == &expected))` on canonical path equality (`:404,484,561`), `assert!(rel_count >= 1)` + relation-endpoint set inclusion (`:416,497,574,683`). The coverage-guard `#[test] test_suite_covers_four_fixtures` (`:710`) enforces `FIXTURES.len() == 4` plus language/name membership — not a tautology.

- **Hallucinated imports / paths** — all resolve:
    - `ucil_core::KnowledgeGraph` — re-exported at `crates/ucil-core/src/lib.rs`; constructor at `knowledge_graph.rs:651`.
    - `ucil_lsp_diagnostics::diagnostics::{DiagnosticsClient, DiagnosticsClientError, SerenaClient}` — all `pub` at `diagnostics.rs:145,98,206`.
    - `ucil_lsp_diagnostics::types::Language` — `pub enum` at `types.rs:45`.
    - `ucil_lsp_diagnostics::{persist_call_hierarchy_incoming, persist_diagnostics}` — re-exported at `lib.rs:29-34,36-39`.
    - `KnowledgeGraph::conn()` → `&rusqlite::Connection`; `prepare`/`query_map`/`Row::get` are inherent methods on the returned types, so no direct `rusqlite` dev-dep is required.
    - `lsp_types::{Url, Diagnostic, DiagnosticSeverity, NumberOrString, Position, Range, SymbolKind, CallHierarchyItem, CallHierarchyIncomingCall, CallHierarchyOutgoingCall, TypeHierarchyItem}` — all present in `lsp-types 0.95` (workspace `Cargo.toml:99`).
    - `tempfile::TempDir`, `async_trait::async_trait` — workspace dev-deps confirmed at `Cargo.toml:52,104`.
    - Fixture paths all exist on disk: `tests/fixtures/rust-project/src/lib.rs`, `tests/fixtures/python-project/src/python_project/parser.py`, `tests/fixtures/typescript-project/src/task-manager.ts`, `tests/fixtures/mixed-project/src/{main.rs,main.py,index.ts}`.

- **Feature coverage** — WO targets `P1-W5-F08`; diff ships **4 `#[tokio::test]` async tests** (`test_rust_project_diagnostics_and_calls` at `:346`, `test_python_project_diagnostics_and_calls` at `:434`, `test_typescript_project_diagnostics_and_calls` at `:511`, `test_mixed_project_diagnostics_and_calls` at `:590`) plus one sync `#[test]` coverage-guard (`test_suite_covers_four_fixtures` at `:710`). Each tokio test exercises both `persist_diagnostics` and `persist_call_hierarchy_incoming` and asserts on `quality_issues` + `relations` table rows. `grep -c 'async fn test_'` → `4` (≥ 4 required).

- **Timeout discipline** — every `.await` on the bridge entry points is wrapped in `tokio::time::timeout(BRIDGE_AWAIT_BUDGET, ...)` where `BRIDGE_AWAIT_BUDGET = Duration::from_secs(10)` (`test_lsp_bridge.rs:91`). Applied consistently at lines 388, 409, 473, 490, 550, 567, 645, 652, 659, 676. Matches `.claude/rules/rust-style.md` Async § invariant.

- **Commit hygiene** — 8 commits, all Conventional Commits format (`build(workspace):`, `build(tests-integration):`, `test(tests-integration):`, `chore(ready-for-review):`, `chore(verifier):`, `docs(tests-integration):`, `chore(planner):`). Every `feat`/`fix`/`refactor`/`test`/`docs`/`build`/`chore` commit carries `Phase: 1`, `Feature: P1-W5-F08`, `Work-order: WO-0038` trailers plus the `Co-Authored-By` footer. No `--amend` after push, no `--no-verify`, no force-push visible in the reflog. The retry-2 remediation commits (`512004b`, `1eafc54`, `e0b8531`) each reference the prior rejection or RCF artifact in their body.

- **Forbidden-path respect** — `git diff --name-status main..HEAD` shows zero writes under `tests/fixtures/**`, `crates/**`, `scripts/gate/**`, `scripts/flip-feature.sh`, `ucil-master-plan-v2.1-final.md`, or `ucil-build/feature-list.json`. Only `tests/integration/*`, `Cargo.toml`, `Cargo.lock`, and artefacts under `ucil-build/work-orders/`, `ucil-build/verification-reports/`, and `ucil-build/critic-reports/` are touched. The retry-2 JSON amendment (commit `1eafc54`) edits a planner artefact whose allow-list is not enforced by a pre-commit hook — the edit is textually `acceptance_criteria[0]` only and preserves every other field including `id`, `description`, `acceptance_tests`, and `dependencies`.

- **Doc + public API** — `tests/integration/src/lib.rs` exposes no public items (two-line crate doc + attribute block only; `:1-6`). The test binary has no `pub` items (all private to the crate-scoped binary). Rustdoc coverage is thorough on `LocalScriptedFake`, its constructors, every trait-method body, every helper (`fixture_path`, `file_uri`, `path_string`, `open_fresh_kg`, `into_diag_client`, `make_diag`, `make_item`, `wrap_incoming`, `quality_issue_paths`, `relation_endpoint_paths`), every `#[tokio::test]`, and the coverage-guard test. Cross-references to DEC-0008, DEC-0010, and the `ScriptedFakeSerenaClient` precedent are explicit.

- **`cargo fmt --check`** — runs clean in the worktree at HEAD (exit 0).

## Retry-1 blocker resolution

The retry-1 review (`ref: 8d4365b`) raised two blockers: one identified by the critic on retry 1 (mixed-project rustdoc literal) and one identified by the verifier on retry 1 (nextest summary regex). Both are now addressed.

### Blocker 1 — mixed-project rustdoc literal — **RESOLVED**

**Retry 1 finding**: `grep -q 'tests/fixtures/mixed-project' tests/integration/test_lsp_bridge.rs` exited 1 because the `test_mixed_project_diagnostics_and_calls` rustdoc said `mixed-project` without the `tests/fixtures/` prefix.

**Retry 2 remediation**: Commit `512004b` (`docs(tests-integration): cite tests/fixtures/mixed-project in test rustdoc`) added the required literal at `tests/integration/test_lsp_bridge.rs:585`:

```rust
/// P1-W5-F08 — `mixed-project` fixture: three diagnostics (one per
/// language) land as three `quality_issues` rows referencing
/// `tests/fixtures/mixed-project/src/{main.rs,main.py,index.ts}`.  A
```

Verified via `grep -n 'tests/fixtures/mixed-project' tests/integration/test_lsp_bridge.rs` → `585:/// \`tests/fixtures/mixed-project/src/{main.rs,main.py,index.ts}\`.  A`. All four fixture-path literals are now present:

```
344:/// `tests/fixtures/rust-project/src/lib.rs`.
432:/// `tests/fixtures/python-project/src/python_project/parser.py`.
509:/// `tests/fixtures/typescript-project/src/task-manager.ts`.
585:/// `tests/fixtures/mixed-project/src/{main.rs,main.py,index.ts}`.
```

`acceptance_criteria[17]` (alt-enumerated `[20]`) — `grep -q 'tests/fixtures/mixed-project' tests/integration/test_lsp_bridge.rs` — now exits 0.

### Blocker 2 — nextest summary regex — **RESOLVED**

**Retry 1 finding**: `grep -E 'tests run: ([5-9]|[1-9][0-9]+).*[0-9]+ passed' /tmp/nextest-WO-0038.log` never matches nextest v0.9.x's actual summary line (`N tests run: N passed, 0 skipped`). The trailing `.*[0-9]+ passed` over-constrains the line and is unreachable when all tests are green.

**Retry 2 remediation**: Commit `1eafc54` (`chore(planner): amend WO-0038 acceptance_criteria[0] per RCF remediation A`) amended `acceptance_criteria[0]` in `ucil-build/work-orders/0038-lsp-bridge-integration-test-suite.json`:

```diff
-"cd $REPO_ROOT && cargo nextest run --test test_lsp_bridge --no-fail-fast 2>&1 | tee /tmp/nextest-WO-0038.log && grep -E 'tests run: ([5-9]|[1-9][0-9]+).*[0-9]+ passed' /tmp/nextest-WO-0038.log > /dev/null",
+"cd $REPO_ROOT && cargo nextest run --test test_lsp_bridge --no-fail-fast 2>&1 | tee /tmp/nextest-WO-0038.log && grep -E 'tests run: ([5-9]|[1-9][0-9]+) passed' /tmp/nextest-WO-0038.log > /dev/null",
```

The replacement drops the `.*[0-9]+` tokens and matches `tests run: ([5-9]|[1-9][0-9]+) passed` directly. Authorised by `ucil-build/verification-reports/root-cause-WO-0038.md §Remediation A` ("single-line text fix to a planner artifact; … No ADR needed"). Verified via inspection at `ucil-build/work-orders/0038-lsp-bridge-integration-test-suite.json:58`.

## Verdict rationale

The retry-2 diff is substantively correct:

- Implementation reachable from a real entry point (`cargo test --test test_lsp_bridge`; 4 `#[tokio::test]` + 1 `#[test]`).
- No mocks of critical collaborators (Serena MCP wire format, LSP subprocess, SQLite, LanceDB, Docker).
- No stubs of the feature itself.
- Real on-disk SQLite via `KnowledgeGraph::open` on a `tempfile::TempDir`.
- Real on-disk fixtures (every fixture path verified to exist).
- Timeouts on every IO `.await` via `BRIDGE_AWAIT_BUDGET`.
- Substantive assertions: row counts, canonical path equality, relation-endpoint membership, coverage-array length.
- Clean `cargo fmt`, clean rustdoc structure, no hallucinated imports, no forbidden-path writes.
- Commit hygiene is proper; eight commits all carry the required trailers; retry-2 remediation commits (`512004b`, `1eafc54`, `e0b8531`) each cite the prior rejection or RCF artifact.

Both retry-1 blockers are resolved. The two carried-forward warnings (large test-file commit, Uri-vs-Url WO-text deviation) are the same ones flagged on retry 1 and neither is escalated to a blocker for this retry. The retry-2-only note (role-spanning planner amendment from the executor retry loop) is surfaced for verifier visibility but is RCF-authorised.

**Verdict: CLEAN.** Ready for a fresh-session verifier spin.

## Recommended next action

1. Spawn the verifier in a fresh session (`claude -p --no-resume --session-id=<new>`).
2. Verifier runs `cargo clean && cargo test --workspace` (per anti-laziness contract §"Trust your own test output").
3. Verifier executes all 23 `acceptance_criteria` shell gates from `ucil-build/work-orders/0038-lsp-bridge-integration-test-suite.json`.
4. Verifier runs the mutation check on `persist_diagnostics` per `acceptance[7]`.
5. On green, verifier flips `P1-W5-F08.passes = true` via `scripts/flip-feature.sh` and fast-forward merges `feat/WO-0038-lsp-bridge-integration-test-suite` to `main`.
