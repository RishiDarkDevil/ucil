---
work_order: WO-0038
slug: lsp-bridge-integration-test-suite
feature_ids: [P1-W5-F08]
branch: feat/WO-0038-lsp-bridge-integration-test-suite
ref: 8d4365b5c64a87e6358536799d62d5120caa8068
critic_session: crt-WO-0038-2026-04-19
verdict: BLOCKED
reviewed_at: 2026-04-19
---

# Critic Report: WO-0038 — lsp-bridge-integration-test-suite

**Critic session**: `crt-WO-0038-2026-04-19`
**Branch**: `feat/WO-0038-lsp-bridge-integration-test-suite`
**Commit range**: `main..8d4365b` (4 commits, +883 LOC net)
**Verdict**: **BLOCKED**

## Scope of diff

| Path | Change | LOC |
|------|--------|-----|
| `Cargo.toml` | modified (workspace member append) | +1 |
| `Cargo.lock` | modified (register new crate) | +13 |
| `tests/integration/.gitkeep` | deleted | -0 |
| `tests/integration/Cargo.toml` | new | +25 |
| `tests/integration/src/lib.rs` | new | +5 |
| `tests/integration/test_lsp_bridge.rs` | new | +743 |
| `ucil-build/work-orders/0038-ready-for-review.md` | new (ready marker) | +96 |

Four commits: `754b6b9` (workspace member) → `6968a0e` (crate skeleton) → `971f10b` (test file) → `8d4365b` (ready-for-review marker).

## Findings

### Blockers (must fix before verifier)

1. **Acceptance criterion `grep -q 'tests/fixtures/mixed-project'` will fail.**
   `tests/integration/test_lsp_bridge.rs` never contains the literal string `tests/fixtures/mixed-project`. The rustdoc for the rust/python/typescript per-fixture tests each cites the full `tests/fixtures/<name>/...` path (lines 344, 432, 509), but the `test_mixed_project_diagnostics_and_calls` rustdoc at line 583 only names the fixture as `mixed-project` (without the `tests/fixtures/` prefix). References to `mixed-project` are present at lines 5, 583, 587, 591-593, 672, 687, 704, 715, 736 — none of them include the required `tests/fixtures/` prefix.

   Verified:
   ```
   grep -q 'tests/fixtures/rust-project'       → exit 0 (OK)
   grep -q 'tests/fixtures/python-project'     → exit 0 (OK)
   grep -q 'tests/fixtures/typescript-project' → exit 0 (OK)
   grep -q 'tests/fixtures/mixed-project'      → exit 1 (FAIL)
   ```

   Per the work-order `acceptance_criteria[18]`:
   ```
   "grep -q 'tests/fixtures/mixed-project' tests/integration/test_lsp_bridge.rs"
   ```
   This check is part of the verifier's acceptance loop and will block the `passes=true` flip.

   **Fix**: Add a single rustdoc line mirroring the precedent in the other three per-fixture tests (e.g., `/// \`tests/fixtures/mixed-project/src/main.rs\`.`) inside the `test_mixed_project_diagnostics_and_calls` doc block near line 583.

   **Cite**: `tests/integration/test_lsp_bridge.rs:583`.

### Warnings (should fix before gate; not blocking verifier)

1. **Commit `971f10b` is 743 LOC of new Rust (+ 13 LOC Cargo.lock churn), well above the ~50 LOC commit-style soft target** (`.claude/rules/commit-style.md`). The other three commits on the branch are all appropriately small (1–96 LOC). A split where `LocalScriptedFake` + helpers land first, then each per-fixture test lands in its own commit, would yield review-friendlier chunks. The 4 `#[tokio::test]` bodies share helpers, so a split is possible but not trivial. Not a blocker because the file is cohesive and the tests mutually reference one another through the fixture-coverage guard.

   **Cite**: commit `971f10b` (`test(tests-integration): ship test_lsp_bridge across four fixtures`).

2. **Work-order / code deviation — `lsp_types::Uri` vs `lsp_types::Url`.** The WO `plan_summary` and `scope_in` bullets reference `lsp_types::Uri`, but the `SerenaClient` trait's method signatures at `crates/ucil-lsp-diagnostics/src/diagnostics.rs:154,171,188,197` take `lsp_types::Url` (re-export of `url::Url`), not `Uri`. The executor correctly used `Url` (the trait cannot be modified per `scope_out`) and documented the deviation in `971f10b`'s commit body and in the module rustdoc (lines 36–44). This is a planner-side WO-text bug, not an executor deviation; the code matches the real trait surface.

   **Cite**: `tests/integration/test_lsp_bridge.rs:36-44`; commit `971f10b` body final paragraph.

### OK

- **Stub detection** — no `todo!()`, no `unimplemented!()`, no `panic!("stub")`, no `NotImplementedError` anywhere in the diff. The `call_hierarchy_outgoing` + `type_hierarchy_supertypes` trait-method bodies return `Ok(Vec::new())` with an explanatory comment at `test_lsp_bridge.rs:193,202` — this matches the LSP-semantic "empty response" and mirrors the `ScriptedFakeSerenaClient` precedent at `crates/ucil-lsp-diagnostics/src/quality_pipeline.rs:420-462`. Not a stub of the F08 feature itself (F08 only exercises `diagnostics` + `call_hierarchy_incoming`).

- **Mocked critical collaborators** — none. `LocalScriptedFake` is a concrete `impl SerenaClient` against UCIL's own trait boundary, not a mock of Serena's MCP wire format, nor of an LSP server process, nor of SQLite/LanceDB/Docker. The rustdoc at `test_lsp_bridge.rs:16-25,95-110` is explicit on this point and cites DEC-0008 + the `ScriptedFakeSerenaClient` precedent already accepted under the P1-W5-F05 / P1-W5-F06 verifier flips. `KnowledgeGraph::open` runs on a real on-disk SQLite DB inside a `tempfile::TempDir` (real, not mocked).

- **Skipped / ignored tests** — none. `grep -nE '#\[ignore\]|\.skip\(|xfail|xit\(|\.only\('` returns no hits in the diff. Zero `.skip` in the new files.

- **Weak assertions** — none. All 4 `#[tokio::test]` bodies make substantive assertions: `assert_eq!(count, 2)` / `assert_eq!(count, 3)` on row counts, `assert!(paths.iter().all(|p| p == &expected))` on canonical path equality, `assert!(rel_count >= 1)` + relation-endpoint set inclusion. The coverage-guard `#[test] test_suite_covers_four_fixtures` enforces array length == 4 plus language/name membership — not a tautology.

- **Hallucinated imports / paths** — all resolve:
  - `ucil_core::KnowledgeGraph` — re-exported at `crates/ucil-core/src/lib.rs:30`.
  - `ucil_lsp_diagnostics::diagnostics::{DiagnosticsClient, DiagnosticsClientError, SerenaClient}` — all `pub` at `diagnostics.rs:206,98,145`.
  - `ucil_lsp_diagnostics::types::Language` — `pub enum` at `types.rs:45`.
  - `ucil_lsp_diagnostics::{persist_call_hierarchy_incoming, persist_diagnostics}` — re-exported at `lib.rs:29,33`.
  - `KnowledgeGraph::conn()` → `&rusqlite::Connection` (`knowledge_graph.rs:718`). Used in `quality_issue_paths` / `relation_endpoint_paths` helpers without a direct `rusqlite` dev-dep, which is fine because `prepare`/`query_map`/`Row::get` are inherent methods (not trait imports) on the returned types.
  - `lsp_types::{Url, Diagnostic, DiagnosticSeverity, NumberOrString, Position, Range, SymbolKind, CallHierarchyItem, CallHierarchyIncomingCall, CallHierarchyOutgoingCall, TypeHierarchyItem}` — all present in `lsp-types 0.95`.
  - `tempfile::TempDir`, `async_trait::async_trait` — workspace dev-deps confirmed at `Cargo.toml:34,98,51,103`.

- **Feature coverage** — WO targets P1-W5-F08; diff ships 4 `#[tokio::test]` async tests (`test_rust_project_diagnostics_and_calls`, `test_python_project_diagnostics_and_calls`, `test_typescript_project_diagnostics_and_calls`, `test_mixed_project_diagnostics_and_calls` at lines 346/434/511/590) plus one sync `#[test]` coverage-guard (`test_suite_covers_four_fixtures` at line 710). Each tokio test exercises both `persist_diagnostics` and `persist_call_hierarchy_incoming` and asserts on `quality_issues` + `relations` table rows.

- **Timeout discipline** — every `.await` on the bridge entry points is wrapped in `tokio::time::timeout(BRIDGE_AWAIT_BUDGET, ...)` where `BRIDGE_AWAIT_BUDGET = Duration::from_secs(10)` (`test_lsp_bridge.rs:91`). Applied consistently at lines 388, 409, 473, 490, 550, 567, 645, 652, 659, 676. Matches the `.claude/rules/rust-style.md` Async § invariant.

- **Commit hygiene** — 4 commits, all Conventional Commits format (`build(workspace):`, `build(tests-integration):`, `test(tests-integration):`, `chore(ready-for-review):`). Every commit body carries `Phase: 1`, `Feature: P1-W5-F08`, `Work-order: WO-0038` trailers plus the `Co-Authored-By` footer. No `--amend`, no `--no-verify`, no force-push.

- **Forbidden-path respect** — `git diff --name-status main..HEAD` shows zero writes under `tests/fixtures/**`, `crates/**`, `scripts/gate/**`, `scripts/flip-feature.sh`, `ucil-master-plan-v2.1-final.md`, or `ucil-build/feature-list.json`. Only `tests/integration/*`, `Cargo.toml`, `Cargo.lock`, and the ready-for-review marker are touched.

- **Doc + public API** — `tests/integration/src/lib.rs` exposes no public items. The test binary has no `pub` items (all private to the crate-scoped binary). Rustdoc coverage on `LocalScriptedFake`, its constructors, every trait method, every helper, every `#[tokio::test]`, and the coverage-guard test is thorough and cross-references DEC-0008 + DEC-0010 + the approved `ScriptedFakeSerenaClient` precedent.

## Verdict rationale

The diff is substantively correct: implementation reachable from a real entry point (`cargo test --test test_lsp_bridge`), no mocks of critical collaborators, no stubs, real SQLite, real on-disk fixtures, timeouts on every IO `.await`, and the `ScriptedFakeSerenaClient` precedent is correctly extended to a cross-crate integration binary.

The one **blocker** is a trivial rustdoc omission: the `tests/fixtures/mixed-project` string literal is absent from the file, and the WO `acceptance_criteria[18]` greps for it. The fix is one added word (or one added rustdoc line) near `test_lsp_bridge.rs:583`. The executor must revise before the verifier is spawned, because the verifier runs `acceptance_criteria` shell commands as literal checks and this grep will return exit 1.

Once the blocker is addressed, this WO is ready for a fresh-session verifier spin.

## Recommended executor action

1. In `tests/integration/test_lsp_bridge.rs` around line 583, extend the `test_mixed_project_diagnostics_and_calls` rustdoc to include the literal path — for example, mirror the rust/python/typescript precedent with:
   ```rust
   /// P1-W5-F08 — `mixed-project` fixture: three diagnostics (one per
   /// language) land as three `quality_issues` rows pointing at
   /// `tests/fixtures/mixed-project/src/{main.rs,main.py,index.ts}`.
   /// A single incoming-call dispatch against the `.rs` symbol
   /// asserts the relation endpoint path ends with `main.rs` (the
   /// mixed-project rust half).
   ```
2. Commit with subject `docs(tests-integration): cite tests/fixtures/mixed-project in test rustdoc` and the standard Phase/Feature/Work-order trailers.
3. Re-run `cargo doc --workspace --no-deps` locally to confirm the rustdoc still renders clean.
4. Re-push and update `ucil-build/work-orders/0038-ready-for-review.md` `final_commit` to the new sha before spawning the verifier.
