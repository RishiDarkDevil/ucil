---
work_order: WO-0023
slug: lsp-call-and-type-hierarchy-feed
feature: P1-W5-F06
branch: feat/WO-0023-lsp-call-and-type-hierarchy-feed
head_commit: ce039c66e06be3567f8ae0c39d18d70e0713493b
merge_base: fde66433b45328c43261e252cae230910c6a0baf
critic_session: crt-WO-0023
verdict: CLEAN
---

# Critic Report: WO-0023 — lsp-call-and-type-hierarchy-feed

**Critic session**: `crt-WO-0023`
**Branch**: `feat/WO-0023-lsp-call-and-type-hierarchy-feed`
**Merge base**: `fde6643` (main)
**HEAD**: `ce039c6` (`chore(work-order): WO-0023 ready-for-review marker`)
**Diff scope**: 3 commits, 3 files changed, +1625 lines
**Verdict**: **CLEAN**

## Files touched

| File | Status | Lines |
|------|--------|-------|
| `crates/ucil-lsp-diagnostics/src/call_hierarchy.rs` | **new** | +1408 |
| `crates/ucil-lsp-diagnostics/src/lib.rs` | modified | +5 |
| `ucil-build/work-orders/0023-ready-for-review.md` | **new** | +212 |

Forbidden-path acceptance checks verified (0 lines of diff against `main`):
- `crates/ucil-lsp-diagnostics/src/bridge.rs` ✓
- `crates/ucil-lsp-diagnostics/src/diagnostics.rs` ✓
- `crates/ucil-lsp-diagnostics/src/quality_pipeline.rs` ✓
- `crates/ucil-lsp-diagnostics/src/types.rs` ✓
- `crates/ucil-lsp-diagnostics/Cargo.toml` ✓ (no diff — zero new workspace dep, DEC-0008 cycle-free preserved)
- `crates/ucil-core/**`, `crates/ucil-daemon/**`, `crates/ucil-treesitter/**`, `crates/ucil-embeddings/**`, `crates/ucil-cli/**`, `adapters/**`, `ml/**`, `plugins/**` ✓

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Oversized commits (DEC-0005 exemption applies to the feat commit; flagged for verifier review on the test commit)** — `call_hierarchy.rs:entire file`.
   - `be06fb9 feat(lsp-diagnostics): add G4 call/type hierarchy persistence` — +666 lines (call_hierarchy.rs +661, lib.rs +5).
     This is a *module-introduction commit* that introduces `call_hierarchy.rs` with module code + its own `fake_serena_client`/`test_fixtures` submodules. The WO-0016 precedent (`WO-0016.md` Warnings §2) explicitly accepts this shape under
     `DEC-0005-WO-0006-module-coherence-commits.md`. No action required; flagged so the verifier can corroborate under DEC-0005.
   - `e874a54 test(lsp-diagnostics): acceptance tests for call/type hierarchy feed` — +747 lines (call_hierarchy.rs).
     This lands ALL 7 module-root `test_*` functions in one go rather than following the per-function breakdown the WO's `commit_plan` called for (`commit_plan` items 2–6 called for 4 separate test commits of 120–280 lines each). The commit is internally cohesive (all tests live in `call_hierarchy.rs`) and DEC-0005 covers the module-coherence shape, but strictly speaking the 747-line size substantially exceeds the 50-line soft target in `.claude/rules/commit-style.md`. Precedented shape (WO-0016 `d6ab8a6` was 469 churn lines for a test-helper refactor; same envelope) — not a blocker for this WO, but a pattern worth calling out.

2. **Commit-plan deviation — 3 commits delivered vs. `commit_plan.estimated_commits: 6`.** — `ucil-build/work-orders/0023-lsp-call-and-type-hierarchy-feed.json:200`.
   The WO's `commit_plan` specified six commits split by `[module bootstrap, persist_incoming, persist_outgoing, persist_supertypes, extra tests, atomic test]`; the executor delivered two code commits (`be06fb9` module + `e874a54` all tests) plus the marker (`ce039c6`). The feat commit bundles all three persist functions; the test commit bundles all seven tests. The ready-for-review marker (`0023-ready-for-review.md`) does not explicitly acknowledge this deviation — a verifier could reasonably question whether the commit_plan's incremental review value was preserved. Coverage is satisfied (see OK §Feature coverage), so this is a process note rather than a content gap.

3. **Reality-check oracle marked manual (WO-0014 / WO-0015 / WO-0016 precedent)** — `ucil-build/work-orders/0023-ready-for-review.md:55-78`.
   The marker documents that `scripts/reality-check.sh P1-W5-F06` trips the known `zero_tests=1` brand-new-module heuristic (same branch as WO-0014 / WO-0015 / WO-0016). The manual two-step was performed: stashed → `0 tests run: 0 passed, 14 skipped` (+ `error: no tests to run`); restored → `7 tests run: 7 passed, 14 skipped`. The critic cannot flip `passes`, so this is forwarded to the verifier — who should independently re-run the two-step rather than taking the executor's note at face value.

### OK

- **Stub detection** — ripgrep for `todo!()`, `unimplemented!()`, `raise NotImplementedError`, `pass`-only bodies, trivial default returns: **0 hits** in `call_hierarchy.rs`. The four `panic!(...)` calls at `call_hierarchy.rs:897, 909, 931, 1316` are all inside `#[cfg(test)]` helpers or test bodies (allowed by `.claude/rules/rust-style.md`: `.unwrap()` / `.expect()` / `panic!` only forbidden outside `#[cfg(test)]`).
- **Mocked critical collaborators** — `ScriptedFakeSerenaClient` (`call_hierarchy.rs:711`) implements UCIL's own `SerenaClient` trait, defined at `crates/ucil-lsp-diagnostics/src/diagnostics.rs:145` as `pub trait SerenaClient: Send + Sync`. Per `DEC-0008-lsp-bridge-via-serena-mcp-channel.md` §Decision.4 the `SerenaClient` trait is the *dependency-inversion seam* the bridge exposes, explicitly so tests can drive the pipeline without a Serena MCP wire-protocol mock. The `KnowledgeGraph` is opened on a real `tempfile::TempDir` (`test_fixtures::open_fresh_kg`, `call_hierarchy.rs:878`) — real `KnowledgeGraph::open`, real §12.1 DDL, real WAL pragma, real `BEGIN IMMEDIATE`. No `rusqlite::Connection` / `lancedb` / Docker mocks anywhere in the diff.
- **Skipped / ignored tests** — ripgrep for `#[ignore]`, `.skip(`, `xfail`, `xit(`, `pytest.mark.skip`: **0 hits** in diff.
- **Weak assertions** — ripgrep for `assert!(true)`, `assert_eq!(true, true)`, `expect(true).toBe(true)`, `assert True`: **0 hits**. Each of the seven tests makes substantive column-level readbacks:
  - `test_symbol_kind_mapping_covers_serena_emitted_kinds` asserts 17 concrete `SymbolKind → &'static str` cases including fallthrough to `"symbol"` for `FILE` / `OPERATOR` / `EVENT` / `TYPE_PARAMETER`.
  - `test_persist_call_hierarchy_incoming_writes_entities_and_relations` asserts returned count (2), entity COUNT (3), relation COUNT by kind (2 rows with `kind='calls'`), `source_tool` sample (`'lsp:callHierarchy'`), and direction via a `JOIN src, dst ON relations.source_id=src.id AND target_id=dst.id` — the target must equal the root name `parse_header`.
  - `test_persist_call_hierarchy_outgoing_flips_direction` asserts the flipped direction (root-is-source, peer-is-target) via the same SQL join.
  - `test_persist_type_hierarchy_supertypes_writes_inherits_relations` asserts `kind='inherits'` + `source_tool='lsp:typeHierarchy'` + direction (root is source, supertype is target).
  - `test_persist_empty_hierarchy_returns_zero` asserts all three endpoints return 0 AND both tables stay empty (no orphan root entity when the callers vector is empty).
  - `test_non_file_uri_surfaces_typed_error` asserts the typed `CallHierarchyError::NonFileUri { uri }` variant with the offending URI string preserved AND that both tables stay empty (error fires pre-transaction).
  - `test_atomic_transaction_single_scope` asserts the row counts AND `include_str!("call_hierarchy.rs")` grep-count — `needle = concat!("execute_in", "_transaction(")` ensures the source file itself cannot contribute a false-positive match outside the three persist-function call sites.
- **Hallucinated imports** — every `use` in `call_hierarchy.rs` resolves:
  - `lsp_types::{CallHierarchyItem, SymbolKind, TypeHierarchyItem, Url}` (`call_hierarchy.rs:122`) — workspace dep, already used by `diagnostics.rs` and `types.rs`.
  - `thiserror::Error` (`:123`) — workspace dep.
  - `ucil_core::{KnowledgeGraph, KnowledgeGraphError}` (`:124`) — both re-exported at `crates/ucil-core/src/lib.rs:22 pub use knowledge_graph::{KnowledgeGraph, KnowledgeGraphError};`.
  - `crate::diagnostics::{DiagnosticsClient, DiagnosticsClientError}` (`:126`) — both `pub` in `diagnostics.rs`; `SerenaClient::call_hierarchy_incoming` / `call_hierarchy_outgoing` / `type_hierarchy_supertypes` / `diagnostics` exist on the trait at `diagnostics.rs:156-193`; `DiagnosticsClient::call_hierarchy_incoming` / `call_hierarchy_outgoing` / `type_hierarchy_supertypes` exist at `diagnostics.rs:253-304`.
  - `crate::types::Language` (`:127`) — `pub enum Language { Python, Rust, TypeScript, Go, Java, C, Cpp }` at `types.rs`.
  - `async_trait::async_trait` (`:680`, inside `fake_serena_client`) — workspace dep, already used by `diagnostics.rs`.
  - `tempfile::TempDir` (`:803`) — dev-dep in `Cargo.toml:[dev-dependencies] tempfile = { workspace = true }` (no dep-change required, already landed in WO-0016).
  - `std::sync::{Mutex, Arc}` — stdlib.
  - `rusqlite::params!` / `rusqlite::Transaction` / `rusqlite::Error` — workspace dep already landed in `Cargo.toml` during WO-0016.
  - `include_str!("call_hierarchy.rs")` (`:1400`) — path relative to the file itself; legitimate.
  - `concat!("execute_in", "_transaction(")` (`:1401`) — stdlib macro.
- **Feature coverage** — WO-0023 lists exactly one feature (`P1-W5-F06`). The frozen nextest selector at `ucil-build/feature-list.json:987-991` is `-p ucil-lsp-diagnostics call_hierarchy::`. Seven module-root `test_*` functions sit under the `call_hierarchy::` prefix:
  1. `call_hierarchy::test_symbol_kind_mapping_covers_serena_emitted_kinds` (`:946`)
  2. `call_hierarchy::test_persist_call_hierarchy_incoming_writes_entities_and_relations` (`:980`)
  3. `call_hierarchy::test_persist_call_hierarchy_outgoing_flips_direction` (`:1056`)
  4. `call_hierarchy::test_persist_type_hierarchy_supertypes_writes_inherits_relations` (`:1142`)
  5. `call_hierarchy::test_persist_empty_hierarchy_returns_zero` (`:1212`)
  6. `call_hierarchy::test_non_file_uri_surfaces_typed_error` (`:1271`)
  7. `call_hierarchy::test_atomic_transaction_single_scope` (`:1334`)
  Module-root placement respects `DEC-0005` so the selector prefix matches without relying on a `mod tests { … }` wrapper substring. **Feature coverage: satisfied.**
- **Commit hygiene** — every commit in `fde6643..ce039c6` uses Conventional Commits with a proper scope (`feat(lsp-diagnostics)` / `test(lsp-diagnostics)` / `chore(work-order)`), and every commit body contains the required `Phase: 1` / `Feature: P1-W5-F06` / `Work-order: WO-0023` trailers. `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>` present on all three. No `--amend` after push. No force-push. No `--no-verify`. Three commits in order: `be06fb9` → `e874a54` → `ce039c6`.
- **Public-API rustdoc** — every new `pub` item has a doc comment:
  - `CallHierarchyError` enum (`:131-140`) + each of its three variants (`:144-148`, `:150-162`, `:163-166`), `#[non_exhaustive]` per rust-style.md.
  - `symbol_kind_to_entity_kind` (`:172-184`, `#[must_use]` + `const fn`) — includes the full mapping table in rustdoc.
  - `persist_call_hierarchy_incoming` (`:332-370`) — `# Direction`, `# Tracing`, `# Errors` sections with Dispatch / NonFileUri / KnowledgeGraph breakdown.
  - `persist_call_hierarchy_outgoing` (`:453-482`) — `# Direction`, `# Tracing`, `# Errors`.
  - `persist_type_hierarchy_supertypes` (`:558-589`) — `# Direction`, `# Tracing`, `# Errors`.
  - Module-level docstring (`:1-110`) covers the §13.5 G4 contract, the LSP→`entities.kind` projection table, direction semantics for all three edge types, `source_tool` tags, atomicity invariant, re-ingest semantics, timeout discipline (explicit "no second timeout layer" anti-pattern note), and tracing span naming (§15.2).
- **Style invariants**:
  - `thiserror` + `#[non_exhaustive]` on `CallHierarchyError` (rust-style.md Errors §) ✓
  - Single `tokio::time::timeout` layer — the three `.await` sites inside the persist functions go through `DiagnosticsClient::{call_hierarchy_incoming,call_hierarchy_outgoing,type_hierarchy_supertypes}` which already wrap in `LSP_REQUEST_TIMEOUT_MS`; the module deliberately adds no second layer (`call_hierarchy.rs:87-97` rustdoc calls this out as an explicit anti-pattern inheritance from WO-0015). ✓
  - `tracing` spans named `ucil.lsp.persist_call_hierarchy_incoming` / `persist_call_hierarchy_outgoing` / `persist_type_hierarchy_supertypes` per master-plan §15.2 (`call_hierarchy.rs:377-382, 489-494, 596-601`). Each persist function also carries the `#[tracing::instrument]` attribute with `skip(client, kg, root_item)` to keep IO-heavy args out of span fields. ✓
  - `#![warn(clippy::all, clippy::pedantic, clippy::nursery)]` — crate root already has this; the module-scope `#![allow(clippy::module_name_repetitions)]` mirrors the `diagnostics.rs` / `quality_pipeline.rs` escape hatch and is justified inline (`:112-120`). ✓
  - No `ucil-daemon` dependency in `crates/ucil-lsp-diagnostics/Cargo.toml` (Cargo.toml was untouched on this branch; grep for `ucil-daemon` finds only rustdoc references in pre-existing `bridge.rs:13`, `diagnostics.rs:29,129`, `quality_pipeline.rs:44`, and `call_hierarchy.rs` rustdoc — all explaining the cycle rule, not declaring a dep). DEC-0008 cycle rule preserved. ✓
  - All writers route through `KnowledgeGraph::execute_in_transaction` — exactly three call sites at `call_hierarchy.rs:406, 511, 618` (one per persist function). `grep -c 'execute_in_transaction(' == 3` satisfied. ✓
  - No direct writes via `kg.conn().execute(` or `self.conn().execute(` — ripgrep returns **0 hits**. Reads via `kg.conn().query_row(…)` (in `test_fixtures::count_rows`, `sample_relation_source_tool`, `relation_name_pair`) are fine. ✓
  - No `#[cfg(test)] mod tests { }` wrapper — module-root test placement per DEC-0005 (the frozen `call_hierarchy::` selector is a module-prefix match). ✓
  - `unsafe` — none. `unwrap` outside `#[cfg(test)]` — none. ✓

## Verifier handoff notes

- The reality-check oracle (WO acceptance criterion `bash scripts/reality-check.sh P1-W5-F06`) cannot verify mutation for a brand-new module whose tests live in the same file that gets stashed (known harness `zero_tests=1` false-positive documented by WO-0014 / WO-0015 / WO-0016 precedent). Verifier should run the two-step manually:
  1. `git stash -- crates/ucil-lsp-diagnostics/src/call_hierarchy.rs crates/ucil-lsp-diagnostics/src/lib.rs` → confirm `cargo nextest run -p ucil-lsp-diagnostics call_hierarchy::` reports zero matching tests (or `error: no tests to run`).
  2. `git stash pop` → confirm the selector passes 7/7.
- Acceptance criterion requiring `cargo nextest … 'call_hierarchy::'` to match **≥ 7** tests is tight — the module has exactly seven `test_*` functions at module root, with no slack. If any future editor inlines one of the tests into another function or renames a prefix, the selector count will regress.
- Acceptance criteria require **exactly** 3 occurrences of `execute_in_transaction(` in the module (`test "$(grep -c 'execute_in_transaction(' …)" -eq 3`). The test `test_atomic_transaction_single_scope` enforces the same invariant at runtime via `include_str!` + `concat!` so a split-scope regression surfaces as a failed test rather than a silent semantic drift. Verifier re-runs both the grep and the test.
- Critic verified zero-line diffs against the forbidden paths (`bridge.rs`, `diagnostics.rs`, `quality_pipeline.rs`, `types.rs`, `crates/ucil-core/**`, `crates/ucil-daemon/**`, `crates/ucil-treesitter/**`, `crates/ucil-embeddings/**`, `crates/ucil-cli/**`, `adapters/**`, `ml/**`, `plugins/**`, `Cargo.toml`). Verifier should re-confirm at gate time.
- `ScriptedFakeSerenaClient::{call_hierarchy_incoming,call_hierarchy_outgoing,type_hierarchy_supertypes}` each hold a `Mutex` whose lock is cloned on every call (`call_hierarchy.rs:746-791`). The `.expect("ScriptedFakeSerenaClient mutex poisoned")` is the standard test-code expectation — a poisoned mutex would mean a prior test-panic already propagated, and the re-panic here is the intended canary. This is the same pattern `quality_pipeline.rs::fake_serena_client` uses.

## Final verdict

**CLEAN** — no blockers. Three warnings inherited to verifier / gate:
1. Oversized commits (DEC-0005 precedent applies to `be06fb9`; `e874a54` at 747 lines is a process smell worth calling out but not a content issue).
2. 3-commit delivery vs. 6-commit `commit_plan` (process deviation; coverage satisfied).
3. Reality-check oracle marked manual (verifier should re-run the two-step manually per WO-0014/0015/0016 precedent).

Safe to proceed to verifier session.
