# Critic Report: WO-0043

**Critic session**: crt-WO-0043-20260505T0000Z
**Branch**: feat/WO-0043-plugin-hot-reload-and-circuit-breakers
**Tip ref**: 48f8761f97ce090d5d18143b4147d88382f1a3e1
**Merge base**: main
**Range reviewed**: `git log --oneline main..feat/WO-0043-plugin-hot-reload-and-circuit-breakers` (11 commits)
**Features in WO**: P2-W6-F03 (hot-reload), P2-W6-F04 (circuit breakers)
**Files changed**: `crates/ucil-daemon/src/plugin_manager.rs`, `crates/ucil-daemon/src/lib.rs`, `ucil-build/work-orders/0043-ready-for-review.md` (3 files, +675/-3)
**Verdict**: **CLEAN**

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Soft commit-subject length cap exceeded on 5 of 11 commits** (`.claude/rules/commit-style.md` says `<=70 chars`, this is a soft target). Worst offender:
   - `188850c` — `feat(daemon): add MAX_RESTARTS + CIRCUIT_BREAKER_BASE_BACKOFF_MS constants and PluginManager backoff config` (107 chars).
   - Also over: `b83ce15` (89), `4d6756e` (81), `8e206ec` (82), `b60217b` (71).
   - **Not a blocker** — `git log --all` shows 22 prior `feat(daemon)` commits over 70 chars on this repo, established precedent. Flagging only because future commits could trim where the body would carry the detail equally well.

2. **`PluginManager::add` silently degrades on lock contention** (`crates/ucil-daemon/src/plugin_manager.rs:1382-1393`). On `try_write` failure the runtime is NOT registered and only a `tracing::warn!` event fires — the function returns `()`. The work-order explicitly authorised this design (`scope_in[3]`: "implementer's choice between `try_write` panic-on-contention or async `write().await` blocking"), so this is in-spec, but production callers cannot programmatically detect the omission except via a follow-up `registered_runtimes()` snapshot. The acceptance test path is sequential so it cannot exercise the contended branch — verifier will not be able to mutation-cover it. **Soft concern only**, no action required for this WO; flagging for future API-tightening.

### OK

- **Stub detection**: clean.
  - `rg -nE 'todo!\(\)|unimplemented!\(\)|NotImplementedError'` on `crates/ucil-daemon/src/plugin_manager.rs` → no matches.
  - `rg -nE '^\s*return\s+(None|Default::default\(\)|\{\}|true|false|0|"")\s*;?\s*$'` finds two `return true;` hits at lines 346 and 363 — both pre-existing in `PluginManifest::activates_for_language` / `::activates_for_tool` (early-exit fast-paths inside multi-branch logic). Neither line appears as a `+` in the WO-0043 diff (`git diff main..HEAD | grep '^\+.*return true'` → empty). **Not introduced by this WO.**

- **Mocked critical collaborators**: none. The only "mock" tokens in the file refer to the `mock-mcp-plugin` real binary (Cargo-built sibling fixture, identical pattern to the existing `test_hot_cold_lifecycle` per WO-0042). The breaker test uses `command: "/__ucil_test_nonexistent_breaker_binary__"` to trigger a real `tokio::process::Command::spawn` ENOENT — exercising the failure branch through the real `Self::spawn` codepath, not a mock. No matches for `serena|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker` in the diff.

- **Skipped / ignored tests**: clean. `rg -nE '#\[ignore\]|\.skip\(|xfail|xit\(|pytest\.mark\.skip'` → no matches in the diff.

- **Weak assertions**: clean. `rg -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)|assert_eq!\(1,\s*1\)'` → no matches. Every assert in the two new tests checks specific state — `state == Active|Error`, `elapsed >= 100ms`, `elapsed >= 35ms`, `elapsed < 2s`, `restart_attempts == 0|MAX_RESTARTS`, `error_message.contains("circuit breaker")`, the `CircuitBreakerOpen { name, attempts }` pattern match.

- **Hallucinated paths**: clean. Only new `use` statements are `use std::time::Duration as Dur;` inside the two `#[tokio::test]` bodies (lines 1623, 1742) — `std::time::Duration` is already in scope at the file top (line 48). `tokio::sync::RwLock` is already imported (line 56). `Arc`, `Instant`, `tokio::time::timeout`, `tokio::time::sleep`, `tokio::task::yield_now`, `PluginRuntime::mark_error(impl Into<String>)` (line 622) all exist on the branch.

- **Feature coverage**: matches frozen feature-list selectors.
  - P2-W6-F03 → selector `plugin_manager::test_hot_reload` → `async fn test_hot_reload()` at `crates/ucil-daemon/src/plugin_manager.rs:1622` (module root, sibling of `mod tests {}` at line 2126; placement honours DEC-0007).
  - P2-W6-F04 → selector `plugin_manager::test_circuit_breaker` → `async fn test_circuit_breaker()` at `crates/ucil-daemon/src/plugin_manager.rs:1741` (module root).
  - Both tests assert against the work-order's named acceptance behaviours (drain in-flight, breaker trip after MAX_RESTARTS, exponential backoff floor, ERROR-state transition).

- **Commit hygiene**: green.
  - 11/11 commits use Conventional Commits prefixes (`feat`, `test`, `docs`, `chore`).
  - All feat/test/docs commits include `Phase: 2`, `Feature: P2-W6-F03|F04|F03, F04`, `Work-order: WO-0043` trailers in the body. Verified via `git log --reverse --format='%h %s%n%b'`.
  - Per-commit diff sizes (insertions): 27, 52, 19, 100, 117, 102, 2, 91, 139, 1, 139. Largest single commit is 139 lines (test-introduction, allowed by DEC-0005 module-root flat-test commit precedent — explicitly cited in `lessons_applied`). Largest non-test commit is 117 lines (`067cec4` — `reload` + tracing). All well under the critic.md 200-line hard threshold.
  - No `--amend` / `git revert` / `git reset` artefacts on the published branch (`git log --format='%H %P' main..HEAD` shows linear history, single-parent commits only).
  - No `--no-verify` markers.

- **Doc + public API**: green. Every new `pub` item carries a rustdoc:
  - `pub const MAX_RESTARTS: u32 = 3;` (lines 87-92, cites master-plan §14.2).
  - `pub const CIRCUIT_BREAKER_BASE_BACKOFF_MS: u64 = 1_000;` (lines 94-103, links `[Self::with_circuit_breaker_base]` and `[MAX_RESTARTS]`).
  - `PluginRuntime.in_flight: Arc<RwLock<()>>` (lines 488-491).
  - `PluginRuntime.restart_attempts: u32` (lines 492-495).
  - `PluginError::NotFound { name }` and `CircuitBreakerOpen { name, attempts }` (lines 790-808) — variant docs plus per-field docs and `#[error(...)]` payloads.
  - `PluginManager::with_circuit_breaker_base` (lines 836-848, includes `#[must_use]`).
  - `PluginManager::reload` (lines 1137-1190, includes a `# Errors` section).
  - `PluginManager::restart_with_backoff` (lines 1253-1280, includes a `# Errors` section).
  - `PluginManager::add` (lines 1372-1393, documents the contended-lock fall-through).
  - The new private field `circuit_breaker_base: Duration` carries no doc but is `pub(crate)`-equivalent (no `pub`), so the rustdoc rule does not apply.
  - No new `pub` symbols in `lib.rs` beyond the two re-exports already documented at the source (`MAX_RESTARTS`, `CIRCUIT_BREAKER_BASE_BACKOFF_MS`).
  - rustdoc-link discipline (`#![deny(rustdoc::broken_intra_doc_links)]` per crate, prior phase-1 landmine): all intra-doc references use full paths (`[`PluginManager::reload`]`, `[`Self::with_circuit_breaker_base`]`) or plain backticks. Commit `257a1e1` already remediated the one bare `MAX_RESTARTS` identifier flagged by `clippy::doc_markdown`.

- **WO-0042 lessons applied**: confirmed at the source level.
  - Module-root test placement (DEC-0007): both new tests sit OUTSIDE `mod tests {}` (which begins at line 2126), so nextest reports paths matching the frozen feature-list selectors verbatim.
  - Additive struct-field updates: `PluginRuntime` gained `in_flight` and `restart_attempts`; `PluginRuntime::new` (line 511) initialises both. The existing `test_hot_cold_lifecycle` does not construct `PluginRuntime` via struct literal (it goes through `mgr.activate(...)` which calls `PluginRuntime::new` internally), so no struct-literal fix-ups were needed — confirmed by reading lines 1500-1620.
  - No global tracing-subscriber install: `rg -n 'tracing_subscriber::|init\(\)'` against the new test bodies → no matches.
  - Test wall-time budget: `test_circuit_breaker` configures `with_circuit_breaker_base(Dur::from_millis(5))` so the {1, 2, 4} s production backoffs collapse to {5, 10, 20} ms; assertion `elapsed < 2s` is the ceiling.
  - Pre-baked mutation entries are present in `acceptance` (work-order lines 58-59) for each feature, naming the specific function bodies the verifier should stash.

- **Forbidden-paths discipline**: clean. The diff touches only `crates/ucil-daemon/src/plugin_manager.rs`, `crates/ucil-daemon/src/lib.rs`, and `ucil-build/work-orders/0043-ready-for-review.md`. None of the WO's 35 forbidden-paths entries are touched (no other crate, no `tests/`, no fixtures, no `feature-list.json`, no `scripts/gate/**`, no `.githooks/**`, no master-plan).

## Recommendation

Proceed to the verifier. The two pre-baked mutation checks (`reload` body → `Ok(())`; `restart_with_backoff` body → `Ok(())`) are both well-targeted at the function bodies that uniquely cause the asserted-behaviour invariants — verifier should be able to execute them without inventing patches.
