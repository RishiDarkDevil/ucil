# Critic Report: WO-0070

**Critic session**: crt-WO-0070-2026-05-08
**Branch**: feat/WO-0070-g3-parallel-merge
**Tip**: 1e0f29760c1bdc2ab8f0cd201e84f5f1fecf7458
**Merge base**: b8d3852fc60e5cd88cb37880cf8c3c8c13936de1 (main)
**Feature**: P3-W9-F07 (G3 (Knowledge) parallel query merging by entity with temporal priority)
**Commits reviewed**: 6 (4a63eae, f2d2e65, b52dd47, 27938bb, cf24e87, 1e0f297)
**Verdict**: **CLEAN**

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix or acknowledge before gate)

1. **SA1 upper-bound spec deviation (acknowledged + justified)** —
   `crates/ucil-daemon/src/executor.rs:3328`
   The frozen-test SA1 upper bound is `< 500 ms`, not the WO scope_in
   #9 literal `< 700 ms`. The executor tightened it in commit `cf24e87`
   because a sequential 3×200 ms = ~603 ms execution slips through
   `< 700` and would have made the M1 mutation contract toothless.
   The deviation is fully disclosed in the commit body, in the RFR's
   "Disclosed deviations" §3, and follows WO-0068 lessons-learned
   "For executor #4" (follow the spirit when the literal sabotages the
   intent). The lower-bound 180 ms matches spec. **This is a stronger
   assertion than spec, not a weaker one** — flagging only so the
   verifier sees the deviation when reading the test against the WO.
   No action required; verifier should re-confirm M1 reproduction
   produces the documented panic body
   `(SA1) parallel wall < 500 ms; left: 603, right: 500`.

2. **`#[allow(dead_code)]` on `join_all_g3`** —
   `crates/ucil-daemon/src/g3.rs:343-378`
   The helper is annotated `#[allow(dead_code)]` so that an in-place
   M1 mutation (which replaces the parallel-fan-out site with a
   sequential `for ... .await` loop and leaves `join_all_g3`
   unreferenced) compiles cleanly under the crate's `#![deny(warnings)]`.
   Without the allow, M1 would surface as a compile error rather than
   the SA1 panic the contract expects. The rustdoc on the function
   explicitly cites this rationale and the executor disclosed it in
   commit `cf24e87`. The verifier should treat this as the intended
   shape of the M1 contract — restore via `git checkout -- g3.rs`
   between mutations. No action required.

### OK

#### 1. Stub detection (CLEAN)
- `git diff main..HEAD | grep -E '^\+.*(todo!\(|unimplemented!\(|panic!\(.*not.*implem)'` → empty.
- `grep -nE '^\s*pass\s*$|return None\s*$|return Default::default\(\)\s*$|return \{\}\s*$'` over `/tmp/g3.rs` → empty.
- `pub fn merge_g3_by_entity` (g3.rs:585-721) is a 137-line pure-merge implementation with non-trivial cluster partitioning, agreement-cluster + conflict-cluster resolution, and full tie-break ordering (confidence → ts → lex source_id) — no skeleton, no shortcut.
- `pub async fn execute_g3` (g3.rs:447-518) is a real fan-out orchestrator with `tokio::time::timeout` master + per-source deadlines, in-order placeholder synthesis, and `tracing::instrument` — no skeleton.

#### 2. Mocked critical dependencies (CLEAN)
- Production-side word-ban grep `grep -niE 'mock|fake|stub' crates/ucil-daemon/src/g3.rs scripts/verify/P3-W9-F07.sh` → exit 1 (zero matches).
- Test-side `TestG3Source` impl in `crates/ucil-daemon/src/executor.rs:3194-3276` is a UCIL-internal trait impl per `DEC-0008` §4 (the dependency-inversion seam, NOT a mock of any external wire format). The carve-out is explicitly documented in the WO scope_in #12 (precedent: WO-0048 line 363 + WO-0068 lessons-learned For executor #5).
- No `Mockall`, `MagicMock`, `mockito`, or `httpmock` references in the diff.
- No `tokio::process::Command` interception, no MCP-stdio fake, no `pyright`/`rust-analyzer`/`SQLite`/`LanceDB`/Docker substitution.

#### 3. Skipped / ignored tests (CLEAN)
- `git diff main..HEAD | grep -E '^\+.*(#\[ignore\]|\.skip\(|xit\(|xfail|@pytest\.mark\.skip)'` → empty.
- `test_g3_parallel_merge` is `#[cfg(test)] #[tokio::test(flavor = "multi_thread", worker_threads = 4)]` — runs unconditionally under `cargo test -p ucil-daemon`.
- No env-gated short-circuits in the verify script (`scripts/verify/P3-W9-F07.sh`) — the test is fully synthetic.

#### 4. Weak assertions (CLEAN)
- `grep -E 'assert!\(true\)|assert_eq!\(true,\s*true\)'` over the diff → empty.
- All 31 SA-numbered assertions in `executor.rs` (8 SAs × multi-assertion blocks) carry semantic panic bodies of the form `(SAn) <semantic name>; left: ..., right: ...` per `DEC-0007`.
- Every assertion compares against a derived value (e.g.
  `outcome_sa1.wall_elapsed_ms`, `merged_sa2.merged.len()`,
  `m_sa3.winning_confidence`), not a literal-vs-literal tautology.
- Float comparisons use `(x - y).abs() < 1e-9` (e.g.
  `executor.rs:3437`).

#### 5. Hallucinated paths / imports (CLEAN)
- `g3.rs` imports: `std::collections::BTreeMap`, `std::future::Future`,
  `std::pin::Pin`, `std::task::Poll`, `std::time::Duration`,
  `serde::{Deserialize, Serialize}`. All resolve.
- `g3.rs` references: `tokio::time::timeout`, `async_trait::async_trait`,
  `tracing::instrument`. All workspace deps already in
  `crates/ucil-daemon/Cargo.toml` (`tokio.workspace = true:33`,
  `tracing.workspace = true:34`, `serde.workspace = true:36`,
  `async-trait = { workspace = true }:71`).
- `executor.rs:3170-3173`: `use crate::g3::{ execute_g3, merge_g3_by_entity, G3FactObservation, G3Outcome, G3Query, G3Source, G3SourceOutput, G3SourceStatus, G3_PER_SOURCE_DEADLINE }` — every symbol is defined in `g3.rs` (verified at lines 92, 104, 136, 170, 183, 206, 278, 447, 585).
- `lib.rs:159` (`pub mod g3;`) and `lib.rs:189-190` (`pub use g3::{...}`) reference `g3.rs` which exists.
- No imports of non-existent crates, no orphan `mod` declarations, no broken intra-doc links.

#### 6. Feature coverage (CLEAN)
- WO `feature_ids = ["P3-W9-F07"]`. The frozen test `test_g3_parallel_merge` (executor.rs:3164-3722) carries 8 SA-numbered sub-assertions covering every behaviour the WO goal enumerates:

  | SA | Behaviour | Location |
  |----|-----------|----------|
  | SA1 | Parallelism | executor.rs:3284-3344 |
  | SA2 | Newest-wins-on-conflict | executor.rs:3346-3397 |
  | SA3 | Highest-confidence-on-agreement | executor.rs:3399-3452 |
  | SA4 | Different-entities-union | executor.rs:3454-3492 |
  | SA5 | Partial-Errored | executor.rs:3494-3561 |
  | SA6 | Partial-TimedOut | executor.rs:3563-3625 |
  | SA7 | Master-deadline-trip | executor.rs:3627-3688 |
  | SA8 | Empty-source | executor.rs:3690-3721 |

- `scripts/verify/P3-W9-F07.sh` exists and gates on the cargo summary
  regex `^test executor::test_g3_parallel_merge \.\.\. ok$`.
- Pre-baked M1/M2/M3 mutation contract in `ucil-build/work-orders/0070-ready-for-review.md`
  with file/line/patch/expected-panic/restore for each row, plus a
  pre-mutation md5sum (`69356f91cb321fe9f63903187a9c410c`) for snapshot
  integrity.

#### 7. Commit hygiene (CLEAN)
- 6 commits, all Conventional Commits subjects ≤ 70 chars:
  - `4a63eae feat(daemon): land G3 parallel orchestrator + entity-keyed merger`
  - `f2d2e65 test(daemon): frozen G3 parallel-merge selector at module root`
  - `b52dd47 feat(scripts): add P3-W9-F07 verify script for G3 parallel-merge`
  - `27938bb docs(daemon): scrub word-ban triggers from g3.rs + verify script`
  - `cf24e87 fix(daemon): tighten SA1 bound + atomic M1 mutation contract`
  - `1e0f297 chore(rfr): WO-0070 ready-for-review marker`
- Every commit body carries `Phase: 3`, `Feature: P3-W9-F07`,
  `Work-order: WO-0070`, `Co-Authored-By: Claude Opus 4.6 (1M context)`
  trailers per `.claude/rules/commit-style.md`.
- Two commits exceed the soft 50-line target: `4a63eae` (717 insertions)
  and `f2d2e65` (603 insertions). Both are explicitly authorized by
  `DEC-0005` module-coherence carve-out + WO scope_in #19 ("commit 1 —
  NEW g3.rs in ONE cohesive feat commit, likely 600-800 LOC; commit 3 —
  test: frozen test, likely 400-500 LOC, single coherent selector"). The
  test is a single `#[tokio::test]` function and cannot be split; the
  module is a single new file. Precedent: WO-0067 ceqp.rs (548 LOC),
  WO-0068 cross_group.rs (788 LOC). Acceptable.
- `git log feat/WO-0070-g3-parallel-merge..main --oneline | wc -l` → 0
  (branch is fast-forwardable from main).
- No `--amend`-after-push, no force-push, no `--no-verify`.
- No forbidden_paths touched: tests/fixtures/**, scripts/gate/**,
  scripts/flip-feature.sh, ucil-{core/cross_group,fusion,ceqp}.rs,
  ucil-daemon/g2_search.rs, ucil-daemon/tests/{g3_plugin_manifests,plugin_manifests}.rs,
  feature-list.json + schema, master-plan, frozen prior WOs (0001/0002/0044/0067/0068/0069),
  .githooks/**, decisions/DEC-*.md — verified via `git diff` filter (zero matches).
- Branch tip pushed: `git ls-remote origin feat/WO-0070-g3-parallel-merge`
  resolves to `1e0f29760c1bdc2ab8f0cd201e84f5f1fecf7458` (matches local HEAD).

#### 8. Doc + public API (CLEAN)
- Every new `pub` item in `g3.rs` carries a rustdoc comment:
  - `pub const G3_MASTER_DEADLINE` (g3.rs:67-77) — full docstring with §6.1 reference
  - `pub const G3_PER_SOURCE_DEADLINE` (g3.rs:79-92) — full docstring documenting the unconditional-const rationale
  - `pub struct G3Query` (g3.rs:96-119) — struct + every field
  - `pub struct G3FactObservation` (g3.rs:121-160) — struct + every field including the reserved `validity_window` future hook
  - `pub enum G3SourceStatus` (g3.rs:162-179) — enum + every variant
  - `pub struct G3SourceOutput` (g3.rs:181-197) — struct + every field
  - `pub struct G3Outcome` (g3.rs:199-214) — struct + every field
  - `pub struct G3MergedFact` (g3.rs:216-241) — struct + every field
  - `pub struct G3MergeOutcome` (g3.rs:243-257) — struct + every field
  - `pub trait G3Source` (g3.rs:261-293) — trait + every method
  - `pub async fn execute_g3` (g3.rs:382-518) — full docstring with `# Examples` block (no_run) and `tracing::instrument` reference
  - `pub fn merge_g3_by_entity` (g3.rs:522-721) — full docstring with `# Examples` block (runnable doctest)
- The crate-level deny `#![deny(rustdoc::broken_intra_doc_links)]` (lib.rs:154) implies the doctests must pass — local `cargo test --doc -p ucil-daemon` not run by critic, but verifier should re-confirm.
- Module-level rustdoc on `g3.rs:1-54` documents the pipeline shape, merge contract, and "no-substitute-impls policy" (renamed from "Forbidden-pattern declaration" in commit `27938bb` to clear the AC16 word-ban).

## Mutation contract (pre-baked, verifier-applicable)

The RFR (`ucil-build/work-orders/0070-ready-for-review.md`) documents
M1/M2/M3 with `file:line:patch:expected-panic:restore-command` rows.
The critic confirms the patch sites map to live code:

- M1: g3.rs:473-485 (parallel-fan-out block in `execute_g3`) — verified live
- M2: g3.rs:685-686 (conflict-cluster `>` / `<` swap in `merge_g3_by_entity`) — verified live (lines 685-688 in tip snapshot)
- M3: g3.rs:650-653 (agreement-cluster `>` / `<` swap in `merge_g3_by_entity`) — verified live

`md5sum crates/ucil-daemon/src/g3.rs` (tip snapshot) matches the
`69356f91cb321fe9f63903187a9c410c` in the RFR — verifier can use this
as the snapshot integrity check before applying mutations.

## Summary

`feat/WO-0070-g3-parallel-merge` lands a clean, well-documented G3
(Knowledge) parallel-execution orchestrator + entity-keyed temporal-
priority merger in `crates/ucil-daemon/src/g3.rs` plus a frozen
8-SA acceptance test at module root in
`crates/ucil-daemon/src/executor.rs::test_g3_parallel_merge`. The
implementation is real (no `todo!()`, no `unimplemented!()`, no stub
returns), no critical-dep mocks, all forbidden_paths respected, all
public items documented, all 6 commits carry the required
Phase/Feature/Work-order trailers, and the M1/M2/M3 mutation contract
is pre-baked with line-accurate patches.

The two warnings (SA1 upper-bound tightening from 700 → 500 ms; the
`#[allow(dead_code)]` on `join_all_g3` to keep M1 atomic) are
**deliberate executor decisions to make the test load-bearing under
mutation**, fully disclosed in commit `cf24e87` and the RFR's
"Disclosed deviations" section. They strengthen the test rather than
weaken it.

**Verdict**: CLEAN — proceed to verifier.
