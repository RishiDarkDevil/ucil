# Critic Report: WO-0078

**Critic session**: crt-2026-05-08-WO-0078
**Branch**: `feat/WO-0078-g6-platform-aggregation`
**Final commit**: `e012bed9d931bc81f97f9269a52e3aedbcf2f5ce`
**Merge base vs main**: `851e716831abd551e67a09d420f2232073070137`
**Verdict**: **CLEAN**

## Scope of review

Diff `851e716..feat/WO-0078-g6-platform-aggregation` (5 implementation commits + 1 RFR-marker commit, all carrying valid `Phase: 3 / Feature: P3-W10-F08 / Work-order: WO-0078` trailers):

| Commit | LOC | Subject |
|--------|-----|---------|
| `dedb06e` | 954 | feat(daemon/g6): add G6 (Platform) parallel orchestrator + simple aggregator |
| `5e17835` | 3 | feat(daemon): wire pub mod g6 + pub use g6::... re-exports |
| `b3da93b` | 709 | test(daemon): add frozen test_g6_platform_aggregation at module root |
| `c65c00e` | 119 | feat(scripts): add scripts/verify/P3-W10-F08.sh |
| `c79456d` | 181 | test(daemon): reorder SA6 before SA2..SA5 in test_g6_platform_aggregation |
| `e012bed` | 112 | chore(wo-0078): ready-for-review marker with M1/M2/M3 mutation contract |

Files touched (all within scope_in / none in `forbidden_paths`):

- `crates/ucil-daemon/src/g6.rs` (+954 / NEW)
- `crates/ucil-daemon/src/lib.rs` (+3, two re-export lines)
- `crates/ucil-daemon/src/executor.rs` (+720 / purely additive — 0 lines removed; appended below the existing G4 region)
- `scripts/verify/P3-W10-F08.sh` (+119 / NEW, chmod +x verified)
- `ucil-build/work-orders/0078-ready-for-review.md` (+112 / NEW marker)

Cargo.toml + Cargo.lock unchanged. `tests/fixtures/**` unchanged.

## Findings

### Blockers (must fix before verifier)

**None.**

### Warnings (should consider before gate)

1. **Three commit subjects exceed the 70-char soft target** at `crates/ucil-daemon/src/g6.rs` commit-style.md:
   - `dedb06e` 76 chars: `feat(daemon/g6): add G6 (Platform) parallel orchestrator + simple aggregator`
   - `c79456d` 73 chars: `test(daemon): reorder SA6 before SA2..SA5 in test_g6_platform_aggregation`
   - `e012bed` 71 chars: `chore(wo-0078): ready-for-review marker with M1/M2/M3 mutation contract`

   Per `commit-style.md` the limit is `<=70 chars` (soft). The WO scope_in #20 also calls it a soft target. Not a blocker; carry-over from prior WOs (WO-0073/0074/0075/0076/0077 all shipped with the same minor overage).

2. **Two large commits exceed the ~50-line/200-line soft caps** intentionally per scope_in #20 + DEC-0005 module-coherence carve-out:
   - `dedb06e` 954 LOC (g6.rs single-feat commit; cited DEC-0005 + WO-0067/0068/0070/0073 module-coherence precedent in body).
   - `b3da93b` 709 LOC (frozen test single-test commit; cited DEC-0007 + WO-0047/0070/0073 module-root placement in body).

   Both commits cite the relevant ADR / WO precedent in their bodies. Not a blocker — the WO explicitly authorises the pattern.

3. **RFR marker says "Commits (5)"** at `ucil-build/work-orders/0078-ready-for-review.md:7` but the branch carries 6 commits — the marker itself is the 6th. The "5" refers to the implementation commits before the marker. Cosmetic; consistent with WO-0073/0074/0075/0076/0077 markers.

### OK

#### 1. Stub detection — CLEAN
- `grep -nE 'todo!\(\)|unimplemented!\(\)' crates/ucil-daemon/src/g6.rs scripts/verify/P3-W10-F08.sh` → no hits.
- WO-0078-additive lines in `crates/ucil-daemon/src/executor.rs` → no `todo!()`, `unimplemented!()`, `raise NotImplementedError`, bare `pass`, or trivial `Default::default()` returns.
- The `LongSleep` fallback at `crates/ucil-daemon/src/executor.rs:4608-4614` returns a real `G6SourceOutput` after the orchestrator's per-source timeout has already fired — comment notes the path is unreachable on the happy path; not a stub.

#### 2. Mocked critical dependencies — CLEAN
- `grep -niE '(mock|fake|MagicMock|Mockall).*(serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker)'` on WO-0078-additive lines → no hits.
- Production-side word-ban grep `grep -niE 'mock|fake|stub' crates/ucil-daemon/src/g6.rs scripts/verify/P3-W10-F08.sh` → empty (AC18).
- Test-side `TestG6Source` impl at `crates/ucil-daemon/src/executor.rs:4562-4625` is a UCIL-internal trait impl per DEC-0008 §4 dependency-inversion seam — not a mock of any external wire format. Same precedent as `TestG1Source` (WO-0047), `TestG3Source` (WO-0070), `TestG4Source` (WO-0073). Word-ban exemption applies.
- The two "mock" string hits in WO-0078-additive `executor.rs` lines are explanatory comments stating "**NOT** a mock" (`crates/ucil-daemon/src/executor.rs:4476`, `crates/ucil-daemon/src/executor.rs:4558`). Documentation, not implementation.

#### 3. Skipped / ignored tests — CLEAN
- `grep -nE '#\[ignore\]|\.skip\(|xfail|@pytest\.mark\.(skip|xfail)' on additive lines → no hits.
- AC20 `rg 'todo!\(|unimplemented!\(|#\[ignore\]' crates/ucil-daemon/src/g6.rs crates/ucil-daemon/src/executor.rs` exits 1 (verified locally).

#### 4. Weak assertions — CLEAN
- 34 SA-tagged assertions across `test_g6_platform_aggregation` (AC19 floor ≥7).
- Every `assert!`/`assert_eq!` carries a verbatim `(SAn) <semantic>; left: ..., right: ...` panic body per DEC-0007.
- No `assert!(true)`, `assert_eq!(1, 1)`, or empty test bodies.
- 8 in-module unit tests in `crates/ucil-daemon/src/g6.rs:751-954` (`mod tests`) covering truncation edge cases, dedup behaviour, errored-source handling, empty-source recording, max_blocks tail-truncation, max_chars_per_block truncation, and `G6QueryKind::default`.

#### 5. Hallucinated paths / imports — CLEAN
All imports in `crates/ucil-daemon/src/g6.rs:81-87` resolve to real targets:
- `std::collections::{BTreeMap, BTreeSet}` — std.
- `std::future::Future`, `std::pin::Pin`, `std::task::Poll`, `std::time::Duration` — std.
- `serde::{Deserialize, Serialize}` — workspace dep (`serde.workspace = true` in `crates/ucil-daemon/Cargo.toml`).

In test imports at `crates/ucil-daemon/src/executor.rs:4533-4536`:
- `crate::g6::{execute_g6, merge_g6_platform_aggregation, G6BlockKind, G6Outcome, G6PlatformBlock, G6Query, G6QueryKind, G6Source, G6SourceOutput, G6SourceStatus, G6_PER_SOURCE_DEADLINE}` — every symbol verified present in `crates/ucil-daemon/src/g6.rs` and re-exported via `crates/ucil-daemon/src/lib.rs:196`.
- `async_trait::async_trait` — workspace dep (`async-trait = { workspace = true }` in `crates/ucil-daemon/Cargo.toml`).

#### 6. Feature coverage — CLEAN
WO-0078 declares one feature: `P3-W10-F08`. Frozen acceptance test `executor::test_g6_platform_aggregation` lives at module root of `crates/ucil-daemon/src/executor.rs:4529` (AC08).

Eight semantic sub-assertions (SA1..SA8) covering the full work-order behaviour:
- SA1 — Parallelism (`crates/ucil-daemon/src/executor.rs:4646-4705`): wall ∈ [180, 500) ms.
- SA6 — Partial-Errored (`crates/ucil-daemon/src/executor.rs:4707-4801`): merger excludes errored source, sources_contributing == 2 (placed before SA2..SA5 so M3 trips here first per `c79456d` reorder).
- SA2 — Stable input ordering (`crates/ucil-daemon/src/executor.rs:4803-4874`): aggregated_blocks[0..3] in `[git, github, filesystem]`.
- SA3 — Disjoint blocks aggregated, no dedup (`crates/ucil-daemon/src/executor.rs:4876-4942`).
- SA4 — sources_contributing accuracy with empty source (`crates/ucil-daemon/src/executor.rs:4944-4995`).
- SA5 — total_blocks + total_payload_chars (`crates/ucil-daemon/src/executor.rs:4997-5038`).
- SA7 — Partial-TimedOut (`crates/ucil-daemon/src/executor.rs:5040-5115`).
- SA8 — Master-deadline-trip (`crates/ucil-daemon/src/executor.rs:5117-5179`).

#### 7. Commit hygiene — CLEAN
- All 6 commits use Conventional Commits format (`feat(scope):`, `test(scope):`, `chore(scope):`).
- All 6 commits carry `Phase: 3`, `Feature: P3-W10-F08`, `Work-order: WO-0078` trailers verified via `git show -s --format='%B'`.
- All 6 commits carry `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>`.
- `git log feat/WO-0078-g6-platform-aggregation ^main --merges | wc -l` → 0 (AC27).
- `git rev-parse origin/feat/WO-0078-g6-platform-aggregation` → `e012bed9d931bc81f97f9269a52e3aedbcf2f5ce` matches local HEAD (AC30).
- No `--amend`, no force-push, no `--no-verify`.
- Two large commits (`dedb06e` 954 LOC g6.rs, `b3da93b` 709 LOC test) exceed the 200-line soft cap but cite DEC-0005 / DEC-0007 carve-outs in their bodies — see Warning #2 above.

#### 8. Doc + public API — CLEAN
Every `pub` item in `crates/ucil-daemon/src/g6.rs` has a rustdoc comment with explanatory text:
- `crates/ucil-daemon/src/g6.rs:91-100` — `G6_MASTER_DEADLINE` (master-plan §5.6 + §6.1 cited).
- `crates/ucil-daemon/src/g6.rs:102-115` — `G6_PER_SOURCE_DEADLINE` (WO-0068/0070/0073 lessons cited inline).
- `crates/ucil-daemon/src/g6.rs:119-128` — `G6QueryKind` (rustdoc precedes `#[derive(...)]`).
- `crates/ucil-daemon/src/g6.rs:142-148` — `G6Query` with field-level docs.
- `crates/ucil-daemon/src/g6.rs:165-193` — `G6BlockKind` with per-variant docs.
- `crates/ucil-daemon/src/g6.rs:195-217` — `G6PlatformBlock` with field-level docs.
- `crates/ucil-daemon/src/g6.rs:219-237` — `G6SourceStatus` with per-variant docs.
- `crates/ucil-daemon/src/g6.rs:239-256` — `G6SourceOutput` with field-level docs.
- `crates/ucil-daemon/src/g6.rs:258-273` — `G6Outcome` with field-level docs.
- `crates/ucil-daemon/src/g6.rs:275-304` — `G6AggregationOutcome` with field-level docs.
- `crates/ucil-daemon/src/g6.rs:308-326` — `pub trait G6Source` with full per-method rustdoc + `# Examples` callout.
- `crates/ucil-daemon/src/g6.rs:435-501` — `pub async fn execute_g6` with full algorithm rustdoc + `# Examples` (no_run).
- `crates/ucil-daemon/src/g6.rs:598-657` — `pub fn merge_g6_platform_aggregation` with full algorithm rustdoc + `# Examples` (compile-tested).

Public re-exports at `crates/ucil-daemon/src/lib.rs:196` covered transitively by item-level docs.

## Mutation contract sanity

The pre-baked M1/M2/M3 contract is grep-verifiable:

- **M1 site** — parallel join at `crates/ucil-daemon/src/g6.rs:537` (`tokio::time::timeout(master_deadline, join_all_g6(futures))`). The orphaned-helper compile guard `#[allow(dead_code)]` is in place at `crates/ucil-daemon/src/g6.rs:396` (AC17).
- **M2 site** — `.collect()` at `crates/ucil-daemon/src/g6.rs:703` closing the `outputs.iter().filter(...).flat_map(...)` chain.
- **M3 site** — `matches!(o.status, G6SourceStatus::Available)` predicate at `crates/ucil-daemon/src/g6.rs:701` (block-inclusion filter); a second occurrence at `crates/ucil-daemon/src/g6.rs:684` (empty-source detection) is annotated by the comment block `crates/ucil-daemon/src/g6.rs:668-677`. Either single-site or dual-site Edit application by the verifier still trips SA6 with `(SA6) sources_contributing == 2; left: 0_or_1, right: 2`.

The RFR marker reports all three mutation panics confirmed locally (executor's self-report — verifier will independently re-run).

## Carve-outs respected

- **Standing-protocol coverage workaround**: WO claims 90.15% line coverage via `env -u RUSTC_WRAPPER cargo llvm-cov -p ucil-daemon` per scope_in #17 (≥80% floor, satisfied). 33+ WOs deep — verifier may re-confirm.
- **AC30/AC31 phase-1/phase-2 effectiveness-gate flake escalations** — three pre-existing items remain standing scope_out per scope_in #18.
- **No new deps** — Cargo.toml diff is empty (AC verified).

## Sibling stable code paths (scope_in #19) — UNTOUCHED

All 14 protected files at HEAD = main (verified via `git diff 851e716..HEAD -- <files> | wc -l` → 0):
- `crates/ucil-core/src/{cross_group.rs, fusion.rs, ceqp.rs}`
- `crates/ucil-daemon/src/{g2_search.rs, g3.rs, g4.rs}`
- `crates/ucil-daemon/tests/{plugin_manifests.rs, g3_plugin_manifests.rs, g4_plugin_manifests.rs, g5_plugin_manifests.rs, g6_plugin_manifests.rs, g7_plugin_manifests.rs, g8_plugin_manifests.rs}`

## Summary

The diff is faithful to the work-order. Every acceptance criterion (AC01–AC31) has a corresponding artefact in the changed files. The mutation contract is grep-verifiable from the production code. The frozen test selector resolves cleanly via the module-root placement. No critical dependencies are mocked. No anti-laziness markers are introduced. No forbidden paths are touched. Three minor warnings (commit subject overage, large module-coherence commits, RFR-count cosmetic) are explicitly authorised by the work-order's scope_in #20 + DEC-0005/DEC-0007 + commit-style.md soft-target wording.

**Verdict: CLEAN. Verifier may proceed.**
