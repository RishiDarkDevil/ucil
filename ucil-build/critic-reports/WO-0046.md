---
work_order: WO-0046
feature: P2-W6-F08
branch: feat/WO-0046-plugin-lifecycle-integration-suite
ref: 76307ffcc581cc94bfb28e2e6770bbed9241caef
critic_session: crt-WO-0046-2026-05-05
verdict: CLEAN
ready_for_verifier: true
---

# Critic Report: WO-0046 — plugin-lifecycle-integration-suite

**Critic session**: `crt-WO-0046-2026-05-05`
**Branch**: `feat/WO-0046-plugin-lifecycle-integration-suite`
**Ref**: `76307ffcc581cc94bfb28e2e6770bbed9241caef`
**Diff range**: `main..feat/WO-0046-plugin-lifecycle-integration-suite`
**Verdict**: **CLEAN** — ready for verifier.

## Diff summary

```
$ git diff --name-only main..HEAD
Cargo.lock
scripts/verify/P2-W6-F08.sh
tests/integration/Cargo.toml
tests/integration/test_plugin_lifecycle.rs
ucil-build/work-orders/0046-ready-for-review.md
```

5 files, +738 / −0 across 7 commits. Allow-list grep — every diffed
path matches the WO acceptance whitelist; ZERO files under
`crates/*/src/**`, `plugins/**`, `tests/fixtures/**`, `crates/ucil-daemon/tests/**`,
or `crates/ucil-cli/**` (forbidden_paths invariants honoured).

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Commit 78d1945 size — 252 insertions, slightly over the 200-LOC soft target.**
   `git show --stat 78d1945` reports 252 insertions for the
   `test(integration): add test_plugin_hot_cold_round_trip end-to-end`
   commit. The WO scope_in (line 24) explicitly authorises
   "One commit per test is acceptable; one combined test commit is
   acceptable up to the 200-LOC threshold per DEC-0005 module-coherence."
   The 252 figure includes the file-level `//!` docstring (~40 LOC),
   `#![deny(warnings)]` + `#![warn(clippy::*)]` attributes, `use`
   statements (8 LOC), the `mock_mcp_plugin_path` helper (~25 LOC),
   the `healthy_manifest` helper (~17 LOC), and the test body
   (~105 LOC). The executor's per-test-commit layout matches the WO
   framing ("Helpers ... land with their first caller in commit 2 to
   avoid the dead-code intermediate state"), so this is a soft
   warning only — DEC-0005 module-coherence is honoured because
   every line in the commit lands the file framing + first
   selector together. Verifier may waive without action.

### OK (passing every critic gate)

#### 1. Stub detection — clean
`grep -nE 'todo!\(\)|unimplemented!\(\)|NotImplementedError'` against
`tests/integration/test_plugin_lifecycle.rs` and `scripts/verify/P2-W6-F08.sh`
returns no matches. No `pass`-only bodies (Rust file). No
`Default::default()` or bare-literal stubs as feature implementations
— `mock_mcp_plugin_path:75` and the two `*_manifest` helpers all
return real, fully-populated values; the only `Default::default()`
call is `CapabilitiesSection::default()` at lines 117 + 143
(`tests/integration/test_plugin_lifecycle.rs`), which is the canonical
constructor for the public re-export type.

#### 2. Mocked critical dependencies — clean
`grep -nE 'mock|fake|stub|MagicMock|Mockall'` filtered by
`(serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker)`
returns no matches in either changed file. Every `mock` token in
`tests/integration/test_plugin_lifecycle.rs` falls into one of:

  - Lines 3, 8 (`//!` prohibition docstring) — the file's mock-of-
    critical-dep prohibition contract per WO-0044 lessons line 169.
  - Lines 6, 16, 55–95, 104, 110, 160, 172, 175, 183, 243, 378, 392
    — references to the real `mock-mcp-plugin` binary (a real
    artifact owned by `ucil-daemon`, built unconditionally by
    `cargo build -p ucil-daemon --bin mock-mcp-plugin`; this binary
    name is the same `mock-mcp-plugin` shipped with WO-0042/0043/0044).
  - Line 110: `fn healthy_manifest(name: &str, mock_path: &Path)` —
    parameter name `mock_path` referencing the real binary's path.
  - Line 131: `/// No second mock binary is introduced` — docstring.

All three test fns drive a real `PluginManager` against either the
real `mock-mcp-plugin` subprocess (healthy path) or the real-ENOENT
`/__ucil_test_nonexistent_breaker_binary__` placeholder (failing
path). Zero `MockCommand`, `spawn_mock`, or `mock_command`
identifiers in the test file (`grep -qE 'MockCommand|spawn_mock|mock_command' …`
returns no matches).

#### 3. Skipped / ignored tests — clean
`grep -nE '#\[ignore\]|\bit\.skip\(|\bxit\(|@pytest\.mark\.skip|@pytest\.mark\.xfail|\.skip\('`
returns no matches in either changed file. The three module-root
tests (`#[tokio::test]` at lines 173, 300, 390 of
`tests/integration/test_plugin_lifecycle.rs`) have no `#[ignore]`
attribute, no `#[cfg(ignore)]`, and no `.skip(` invocation — all
three run unconditionally on `cargo test --test test_plugin_lifecycle`.

#### 4. Weak assertions — clean
`grep -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)'` returns no
matches. Every assertion is non-trivial:

  - `assert_eq!(runtime.state, PluginState::Active, "...")` at lines
    184, 207, 212, 256, 263, 403, 450 — exact-variant pinning per
    WO-0042 lessons line 92.
  - `assert_eq!(snapshot[0].restart_attempts, MAX_RESTARTS, "...")`
    at line 343 — pins the field to a `pub const`, not a literal.
  - `assert!(elapsed_to_idle >= Duration::from_millis(50), "...")`
    at line 230 — dual-bound timing assertion (lower behaviour
    bound) per WO-0043 lessons line 134.
  - `assert!(elapsed < Duration::from_secs(2), "...")` at lines
    234, 364 — dual-bound timing assertion (upper fast-test bound).
  - `match err { PluginError::CircuitBreakerOpen { ref name, attempts } => { … } other => panic!("…", other) }`
    at lines 313–325 + 417–429 — explicit-shape error matching per
    WO-0042 lessons line 92 (b); `matches!()` is correctly avoided
    because the variant carries diagnostic context.
  - `error_message.as_deref().is_some_and(|m| m.contains("circuit breaker"))`
    at lines 348–355 + 482–490 — substring assertion against the
    breaker-trip diagnostic; this is the same shape the daemon's
    own `test_circuit_breaker` uses (`crates/ucil-daemon/src/plugin_manager.rs:1770`).

Every panic message includes the runtime debug print (`got {:?}; runtime={:?}`),
matching the WO-0046 scope_in assertion idiom requirement (line 21).

#### 5. Hallucinated imports — clean
The single `use ucil_daemon::{...}` at lines 48–51 of
`tests/integration/test_plugin_lifecycle.rs` references:

  - `CapabilitiesSection` — re-exported at `crates/ucil-daemon/src/lib.rs:107`.
  - `PluginError` — re-exported at `crates/ucil-daemon/src/lib.rs:107`.
  - `PluginManager` — re-exported at `crates/ucil-daemon/src/lib.rs:107`.
  - `PluginManifest` — re-exported at `crates/ucil-daemon/src/lib.rs:107`.
  - `PluginRuntime` — re-exported at `crates/ucil-daemon/src/lib.rs:107`.
  - `PluginSection` — re-exported at `crates/ucil-daemon/src/lib.rs:107`.
  - `PluginState` — re-exported at `crates/ucil-daemon/src/lib.rs:107`.
  - `TransportSection` — re-exported at `crates/ucil-daemon/src/lib.rs:107`.
  - `MAX_RESTARTS` — re-exported at `crates/ucil-daemon/src/lib.rs:108`.

Every method called against these types resolves on the daemon
branch:

  - `PluginManager::new` → `plugin_manager.rs:847`
  - `PluginManager::with_circuit_breaker_base` → `plugin_manager.rs:861`
  - `PluginManager::activate` → `plugin_manager.rs:1103`
  - `PluginManager::add` → `plugin_manager.rs:1392`
  - `PluginManager::restart_with_backoff` → `plugin_manager.rs:1263`
  - `PluginManager::registered_runtimes` → `plugin_manager.rs:1369`
  - `PluginRuntime::new` → `plugin_manager.rs:503`
  - `PluginRuntime::with_idle_timeout` → `plugin_manager.rs:534` (`#[must_use]`)
  - `PluginRuntime::tick` → `plugin_manager.rs:558`

Every public field accessed (`runtime.state`, `runtime.idle_timeout`,
`runtime.restart_attempts`, `runtime.error_message`, `runtime.manifest`)
is declared `pub` at `plugin_manager.rs:469–493`. Every nested
`PluginManifest` field (`plugin`, `capabilities`, `transport`,
`resources`, `lifecycle`) is `pub` at `plugin_manager.rs:107–121`,
nested `PluginSection` fields (`name`, `version`, `description`)
are `pub` at lines 128–134, and nested `TransportSection` fields
(`kind`, `command`, `args`) are `pub` at lines 148–155.

The `PluginError::CircuitBreakerOpen { ref name, attempts }`
match-arm shape at lines 314 + 418 of the test exactly matches the
production variant declaration at `plugin_manager.rs:803–807` (fields:
`name: String`, `attempts: u32`).

The `mock_mcp_plugin_path` helper anchors the mock binary via
`env!("CARGO_MANIFEST_DIR")` (resolves at compile time per
DEC-0010) and joins `../../target/<profile>/mock-mcp-plugin`. The
path resolution honours `CARGO_TARGET_DIR` if set (line 87), uses
`debug` vs `release` from `cfg!(debug_assertions)` (lines 76–80),
and falls through to a panic on missing binary with an operator-
actionable hint pointing at `cargo build -p ucil-daemon --bin mock-mcp-plugin`
(lines 93–98) — the warm-build precondition is enforced upstream
by `scripts/verify/P2-W6-F08.sh` (lines 32–48).

#### 6. Feature coverage — clean
WO-0046 declares `feature_ids: ["P2-W6-F08"]`. The diff adds
**three** module-root `#[tokio::test]` selectors that each cover
one slice of the P2-W6-F08 plugin-lifecycle contract:

| Test fn (file:line) | Slice covered | WO acceptance bullet |
|---------------------|---------------|----------------------|
| `test_plugin_hot_cold_round_trip` (`tests/integration/test_plugin_lifecycle.rs:174`) | HOT/COLD round-trip — `Active → Idle → Active` | scope_in line 16 |
| `test_plugin_crash_recovery_via_circuit_breaker` (`tests/integration/test_plugin_lifecycle.rs:301`) | Crash-recovery via circuit breaker — `MAX_RESTARTS` ENOENT spawns trip the breaker | scope_in line 17 |
| `test_plugin_independent_lifecycle_two_runtimes` (`tests/integration/test_plugin_lifecycle.rs:391`) | Independent-runtime isolation — failing plugin's breaker does not poison healthy peer | scope_in line 18 |

`grep -cE '^#\[(tokio::)?test\]' tests/integration/test_plugin_lifecycle.rs`
returns `3`, which satisfies acceptance bullet 1 (file exists, ≥ 3
module-root selectors, no `mod tests {` wrapper). Verified module-
root placement: `! grep -q 'mod tests {' tests/integration/test_plugin_lifecycle.rs`
— no nested test module, so frozen-selector `cargo test --test
test_plugin_lifecycle <fn_name>` resolves directly per DEC-0007.

Both pre-baked mutations (idle-timeout assignment drop, circuit-
breaker-base assignment drop) are documented in the ready-for-review
marker (`ucil-build/work-orders/0046-ready-for-review.md` lines
84–139) with exact line numbers, expected failure assertions, and
runtime-only variants for the case where `#![deny(warnings)]`
turns the spec sed into a compile-failure cascade. Two verifier-
extension hints are also documented (lines 110–111, 132–133).

#### 7. Commit hygiene — clean
All 7 commits in `git log main..HEAD`:

  - `3b99574` — `build(tests-integration): wire test_plugin_lifecycle [[test]] + ucil-daemon dev-dep` (5 insertions)
  - `78d1945` — `test(integration): add test_plugin_hot_cold_round_trip end-to-end` (252 insertions; see Warning above)
  - `47e7733` — `test(integration): add test_plugin_crash_recovery_via_circuit_breaker` (118 insertions, 2 deletions)
  - `d12cce2` — `test(integration): add test_plugin_independent_lifecycle_two_runtimes` (123 insertions)
  - `ae15540` — `build(verify): add scripts/verify/P2-W6-F08.sh` (77 insertions)
  - `e6b7b48` — `build(lock): regenerate Cargo.lock for ucil-tests-integration ucil-daemon dep` (1 insertion)
  - `76307ff` — `docs(work-order): WO-0046 ready-for-review marker` (164 insertions)

  - **Conventional Commits subjects**: ✓ all 7 commits use
    `<type>(<scope>): <imperative summary>` per `.claude/rules/commit-style.md`.
  - **`Phase: 2` trailer**: ✓ all 7 commits.
  - **`Feature: P2-W6-F08` trailer**: ✓ all 7 commits.
  - **`Work-order: WO-0046` trailer**: ✓ all 7 commits.
  - **`Co-Authored-By: Claude Opus 4.6 (1M context)` footer**: ✓ all 7 commits.
  - **No `--amend` after push**: ✓ — branch `feat/WO-0046-…` is the
    canonical branch and 76307ff is its tip; no rebase / amend
    artefacts visible in `git log`.
  - **No force-push artefacts**: ✓ — `git log main..HEAD` is linear
    and contiguous.
  - **No `--no-verify`**: ✓ — no signed-off-as-bypass markers.
  - **Commit-size sanity**: 6 of 7 commits are ≤ 164 LOC (well within
    DEC-0005 module-coherence). Commit 78d1945 is 252 LOC (the
    file framing + first test); see Warning #1 above. The WO
    explicitly authorises the "one commit per test" layout
    (scope_in line 24); the warning is informational, not a blocker.

#### 8. Doc + public API — clean (N/A)
The diff introduces zero new `pub` items. All three test fns
(`test_plugin_hot_cold_round_trip`, `test_plugin_crash_recovery_via_circuit_breaker`,
`test_plugin_independent_lifecycle_two_runtimes`) are private
module-root fns, and the three helpers (`mock_mcp_plugin_path`,
`healthy_manifest`, `failing_manifest`) are also private. The
`tests/integration/Cargo.toml` change is purely additive metadata
(`[[test]]` entry + `[dev-dependencies]` entry). The
`scripts/verify/P2-W6-F08.sh` shell script, the `Cargo.lock`
regeneration, and the `ucil-build/work-orders/0046-ready-for-review.md`
marker have no Rust public-surface impact.

The doc-markdown pre-flight grep
`rg -nE '^\s*///.*\b[A-Z][A-Z_0-9]+\b' tests/integration/test_plugin_lifecycle.rs`
returns hits, but every match falls into one of:

  - Backticked tokens (e.g. `` `CARGO_MANIFEST_DIR` ``,
    `` `MAX_RESTARTS` ``, `` `ENOENT` ``) — clippy::doc_markdown
    skips backticked content.
  - Hyphenated tokens (e.g. `WO-0046`, `DEC-0010`, `P2-W6-F08`) —
    not Rust identifiers, clippy::doc_markdown does not flag them.

The executor's ready-for-review marker (line 65) reports
`cargo clippy -p ucil-tests-integration --all-targets -- -D warnings → exit 0`,
confirming the doc-markdown pre-flight is satisfied.

## Verifier checklist

Verifier should run (per WO-0046 acceptance_criteria, with optional
verifier-extension on a third symmetric mutation per WO-0045 lessons
line 199):

```
# 1. Existence + grep gates (mechanical, runs in seconds)
test -f tests/integration/test_plugin_lifecycle.rs
test -x scripts/verify/P2-W6-F08.sh
! grep -q 'mod tests {' tests/integration/test_plugin_lifecycle.rs
[ "$(grep -cE '^#\[(tokio::)?test\]' tests/integration/test_plugin_lifecycle.rs)" -ge 3 ]

# 2. Build + test (clean state)
cargo clean
cargo build -p ucil-daemon --bin mock-mcp-plugin
cargo test --test test_plugin_lifecycle -- --test-threads=1
cargo clippy -p ucil-tests-integration --all-targets -- -D warnings

# 3. Regression sentinels (no Phase 0/1/2 regression)
cargo test -p ucil-daemon plugin_manager::test_hot_cold_lifecycle
cargo test -p ucil-daemon plugin_manager::test_circuit_breaker
cargo test -p ucil-daemon --test plugin_manager
cargo test -p ucil-daemon --test plugin_manifests
cargo test --test test_lsp_bridge
cargo test --workspace --no-fail-fast

# 4. Pre-baked mutation A (idle-timeout assignment drop)
#    Note: under #![deny(warnings)] use the runtime-only variant
#    documented in the ready-for-review marker:
#       fn with_idle_timeout(self, idle_timeout: Duration) -> Self {
#           let _ = idle_timeout; self
#       }
#    Run `cargo test --test test_plugin_lifecycle test_plugin_hot_cold_round_trip` —
#    expect panic on tests/integration/test_plugin_lifecycle.rs:207
#    (Some(Idle) vs None). Restore via `git checkout --
#    crates/ucil-daemon/src/plugin_manager.rs`.

# 5. Pre-baked mutation B (circuit-breaker-base assignment drop)
#    Same variant pattern. Expect panic on
#    tests/integration/test_plugin_lifecycle.rs:364
#    (`fast-test budget must hold (got 7.0s)`). Restore.

# 6. Optional verifier-extension (per WO-0045 lessons line 199):
#    Replace the for-loop in restart_with_backoff body with
#    `return Ok(());` — expect test #2 to fail at the `expect_err`
#    line because the breaker never trips. Restore.

# 7. End-to-end driver
bash scripts/verify/P2-W6-F08.sh    # → [OK] P2-W6-F08

# 8. Allow-list invariants
[ -z "$(git diff --name-only main..HEAD | grep -E '^crates/.*/src/')" ]
[ -z "$(git diff --name-only main..HEAD | grep -E '^plugins/')" ]
[ -z "$(git diff --name-only main..HEAD | grep -E '^tests/fixtures/')" ]
```

## Summary

CLEAN — no blockers, one informational warning on commit 78d1945
size that the WO scope_in already authorises. Verifier may proceed
in a fresh session; pre-baked mutation patterns are documented in
the ready-for-review marker with exact line numbers and runtime-
only variants for the `#![deny(warnings)]` cascade case.

— UCIL critic
