---
wo_id: WO-0093
slug: warm-processors-agent-scheduler
phase: 3
week: 10
features: ["P3-W10-F13"]
branch: feat/WO-0093-warm-processors-agent-scheduler
ref: 5b16ee2b9266ed227a99679f5c39aa0277bae6c8
merge_base: d04112cb2440ccd488a2f41a5ada23cc2fbf09ee
critic_session: critic-WO-0093-2026-05-09
verdict: CLEAN
generated_at: 2026-05-09T11:30:00Z
---

# Critic Report: WO-0093

**Critic session**: critic-WO-0093-2026-05-09
**Branch**: feat/WO-0093-warm-processors-agent-scheduler
**Diff range**: `main..feat/WO-0093-warm-processors-agent-scheduler` (6 commits, 5 files, +2102 / -0)
**Verdict**: **CLEAN**

## Summary

WO-0093 lands the four warm-tier promotion processors (`Observation`, `ConventionSignal`, `ArchitectureDelta`, `DecisionLinker`) plus the `WarmProcessorSource` dependency-inversion trait and an `AgentScheduler` orchestrator inside a NEW `crates/ucil-daemon/src/agent_scheduler.rs` (1834 LOC) — exactly the shape called for by the work-order, the master-plan §10 / §11 / §15.2 / §17.2 / §18 references, and the SEXTUPLE-validated G-backbone precedent (WO-0047 / WO-0070 / WO-0083 / WO-0085 / WO-0089). All anti-laziness checks pass; the 8 sub-acceptance criteria (SA1..SA8) are exercised by the frozen `pub async fn test_warm_processors` at module root with `#[tokio::test(flavor = "current_thread", start_paused = true)]`. No blockers.

## Findings

### Blockers (must fix before verifier)

*None.*

### Warnings (should fix before gate)

1. **Bundled commit sizes exceed the soft 50-LOC target** —
   `crates/ucil-daemon/src/agent_scheduler.rs:1-1834` is split across 4 feat commits with these sizes (per `git show --stat`):
   - `8fc3fcd` *scaffold agent_scheduler module skeleton* — 532 insertions.
   - `b7c7bd5` *land 4 warm-tier promotion processor functions* — +472 / −11.
   - `9131c80` *add AgentScheduler orchestrator + handle* — 199 insertions.
   - `57d0ab0` *land WO-0093 frozen acceptance test (SA1..SA8)* — +655 / −3 (test file + Cargo.toml dev-dep).

   This is **explicitly authorized** by the planner per WO-0093 `scope_in[13]` ("Module-coherence per DEC-0005 — bundle … in ONE cohesive feat commit, even if it crosses 700-900 LOC") and consistent with WO-0067 / WO-0068 / WO-0089 precedent (`cross_group.rs` 788 LOC, `g8.rs` ~700 LOC). Recorded as a soft warning per critic.md §7 to surface the deviation; not a blocker.

2. **`crates/ucil-daemon/src/agent_scheduler.rs:1213-1222` carries 7 `#[allow(...)]` attributes on the frozen test** — `clippy::too_many_lines`, `missing_panics_doc`, `uninlined_format_args`, `items_after_statements`, `significant_drop_in_scrutinee`, `significant_drop_tightening`, `no_effect_underscore_binding`. All seven are scoped to the `#[cfg(test)]` `test_warm_processors` function and are well-explained by the test's structure (SA1..SA8 + 4 in-memory state fixtures + tracing-subscriber `Layer` impl + 12-method `WarmProcessorSource` impl all in one body). Same shape as the WO-0089 G8 frozen test. Surfaced as a Warning per critic.md §8 doc + lint expectation; not a blocker.

### OK

- **§1 Stub detection** —
  `grep -E 'todo!\(\)|unimplemented!\(\)|NotImplementedError|raise NotImplementedError' crates/ucil-daemon/src/agent_scheduler.rs` → 0 matches.
  No `pass`-only Python bodies (this is a Rust file).
  No bare `Default::default()` / `{}` / `None` returned as the entire body of any non-test function — every shipped function has a real implementation reachable from the frozen test.

- **§2 Mocked critical dependencies** —
  `grep -iE 'mock|fake|stub' crates/ucil-daemon/src/agent_scheduler.rs scripts/verify/P3-W10-F13.sh` → 0 production-side matches. The lone `tokio::time::MissedTickBehavior::Skip` at `crates/ucil-daemon/src/agent_scheduler.rs:1042` is a legitimate `tokio` API call, not a test skip. The `#[cfg(test)] TestWarmProcessorSource` impl at `crates/ucil-daemon/src/agent_scheduler.rs:1274-1447` is exempt under WO-0048 line 363's `#[cfg(test)]` carve-out and DEC-0008 §4 (UCIL-owned trait dep-inversion seam — production impls own a real `KnowledgeGraph` handle; the test impl is in-memory and does NOT shadow `Serena`, LSP, `SQLite`, `LanceDB`, or `Docker`). Also: NO `MagicMock` / `Mockall` references anywhere on the diff.

- **§3 Skipped / ignored tests** —
  `grep -E '#\[ignore\]|\.skip\(\s*\)|xfail|xit|@pytest\.mark\.skip' crates/ucil-daemon/src/agent_scheduler.rs scripts/verify/P3-W10-F13.sh` → 0 matches. The frozen test runs unconditionally.

- **§4 Weak assertions** —
  `grep -E 'assert!\(true\)|assert_eq!\(true,\s*true\)' crates/ucil-daemon/src/agent_scheduler.rs` → 0 matches.
  Every assertion in `test_warm_processors` carries a `(SAn) <semantic name>; left: ..., right: ...` panic-message body (`crates/ucil-daemon/src/agent_scheduler.rs:1524, 1538, 1550, 1562, 1568, 1584, 1590, 1595, 1606, 1612, 1617, 1626, 1637, 1643, 1659, 1671, 1683, 1695, 1712, 1718, 1722, 1728, 1735, 1756, 1772, 1786, 1796, 1810, 1818, 1830`) per DEC-0007 + WO-0067/68/69/70 precedent.

- **§5 Hallucinated paths** —
  All `use` / fully-qualified paths in `crates/ucil-daemon/src/agent_scheduler.rs` resolve:
  - `std::collections::{BTreeMap, BTreeSet, HashSet, HashMap}` ✓
  - `std::time::Duration`, `std::sync::{Arc, Mutex, MutexGuard}`, `std::sync::atomic::{AtomicBool, AtomicU64, Ordering}`, `std::future::Future`, `std::fmt::{Debug, Formatter, Result}` ✓
  - `serde::{Deserialize, Serialize}` — `serde.workspace = true` at `crates/ucil-daemon/Cargo.toml:38` ✓
  - `serde_json::to_string` — `serde_json.workspace = true` at `crates/ucil-daemon/Cargo.toml:39` ✓
  - `thiserror::Error` — `thiserror.workspace = true` at `crates/ucil-daemon/Cargo.toml:33` ✓
  - `tokio::{sync::{RwLock, watch}, task::JoinSet, time::{Instant, MissedTickBehavior, interval_at, timeout}}` — `tokio.workspace = true` at `crates/ucil-daemon/Cargo.toml:34` ✓
  - `tracing::{instrument, info_span, span::{Attributes, Id}, subscriber::set_default, Subscriber}` — `tracing.workspace = true` at `crates/ucil-daemon/Cargo.toml:35` ✓
  - `tracing_subscriber::{layer::{SubscriberExt, Context, Layer}, registry}` — `tracing-subscriber.workspace = true` at `crates/ucil-daemon/Cargo.toml:36` ✓
  - `async_trait::async_trait` — `async-trait = { workspace = true }` at `crates/ucil-daemon/Cargo.toml:73` ✓
  - `tokio::time::pause / advance / yield_now` — gated on `test-util` dev-feature added at `crates/ucil-daemon/Cargo.toml:128-136` ✓

- **§6 Feature coverage** —
  Only feature in WO is `P3-W10-F13` (`feature-list.json` selector `-p ucil-daemon agent_scheduler::test_warm_processors`). The frozen `pub async fn test_warm_processors` at `crates/ucil-daemon/src/agent_scheduler.rs:1224` is an EXACT selector match and exercises SA1..SA8 explicitly:
  - SA1 (`crates/ucil-daemon/src/agent_scheduler.rs:1538-1601`) — observation processor fires at t=60s; 1 warm row from 1 hot row; `mark_promoted([1])`.
  - SA2 (`crates/ucil-daemon/src/agent_scheduler.rs:1604-1632`) — 3 P1 rows promote (`evidence_count = 3`); 2 P2 rows do not.
  - SA3 (`crates/ucil-daemon/src/agent_scheduler.rs:1683-1741`) — architecture-delta fires at t=120s (NOT 60s); 2 warm states; summary mentions both `add` and `src/a.rs`.
  - SA4 (`crates/ucil-daemon/src/agent_scheduler.rs:1634-1649`) — affected_files=`Some(...)` row promotes; `None` row stays unpromoted.
  - SA5 (`crates/ucil-daemon/src/agent_scheduler.rs:1659-1706`) — `ticks_observed = {Obs:2, Conv:2, Arch:1, Dec:2}` at t=120s.
  - SA6 (`crates/ucil-daemon/src/agent_scheduler.rs:1750-1800`) — injected error → tick still fires; subsequent tick recovers cleanly with `error.is_none()`.
  - SA7 (`crates/ucil-daemon/src/agent_scheduler.rs:1802-1814`) — `≥ 4` `ucil.agent.warm_processor.tick` spans observed via `SpanCounter` `tracing-subscriber` Layer.
  - SA8 (`crates/ucil-daemon/src/agent_scheduler.rs:1816-1832`) — `running_tasks() == 4` pre-shutdown; `shutdown().await` returns `Ok(())`.

- **§7 Commit hygiene** —
  6 commits, all Conventional Commits with scope (`chore(rfr)`, `feat(daemon) × 4`, `test(daemon)`); subjects 43-64 chars (all ≤ 70).
  Every feat / test commit body carries `Phase: 3`, `Feature: P3-W10-F13`, `Work-order: WO-0093` trailers AND a `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>` trailer per `.claude/rules/commit-style.md`.
  No merge commits (`git log feat/WO-0093... ^main --merges` → empty).
  No evidence of `--amend` after push or force-push (linear history; commit timestamps monotonic).
  Bundled commit sizes >50 LOC are explicitly authorized per scope_in[13] (DEC-0005 module-coherence) — see Warning #1.

- **§8 Doc + public API** —
  Every `pub` item carries a rustdoc:
  - 8 `pub const`s (`crates/ucil-daemon/src/agent_scheduler.rs:83, 92, 101, 108, 117, 128, 136, 146`) — all documented with master-plan citations.
  - 1 `pub enum WarmProcessorError` with 4 documented variants (`crates/ucil-daemon/src/agent_scheduler.rs:159-178`).
  - 1 `pub enum WarmProcessorKind` with 4 documented variants + 2 `pub const fn` impls (`crates/ucil-daemon/src/agent_scheduler.rs:203-240`).
  - 8 `pub struct`s (`WarmPromotionResult`, `AgentSchedulerStats`, `HotObservationRow`, `HotConventionSignalRow`, `HotArchitectureDeltaRow`, `HotDecisionMaterialRow`, `WarmObservationRow`, `WarmConventionRow`, `WarmArchitectureStateRow`, `WarmDecisionRow`) — every field documented.
  - `pub trait WarmProcessorSource` with 12 documented methods (`crates/ucil-daemon/src/agent_scheduler.rs:450-531`).
  - 4 `pub async fn run_*_processor` (`crates/ucil-daemon/src/agent_scheduler.rs:656, 766, 858, 945`) — each with `# Errors` doc-section + `#[tracing::instrument(name = "ucil.agent.warm_processor", ...)]`.
  - `pub struct AgentScheduler` + 5 `pub` methods (`new`, `stats`, `start`, `running_tasks`, `shutdown`) — all documented (`crates/ucil-daemon/src/agent_scheduler.rs:1071-1192`).
  - `pub async fn test_warm_processors` — documented as the frozen acceptance test for `P3-W10-F13` (`crates/ucil-daemon/src/agent_scheduler.rs:1207-1212`).
  - lib.rs re-export block lists all 18 public re-exported symbols alphabetically (`crates/ucil-daemon/src/lib.rs:188`).

- **§Forbidden paths** — 0 violations: only files touched are `crates/ucil-daemon/Cargo.toml` (dev-dep, not in forbidden list — workspace `Cargo.toml` was not touched), `crates/ucil-daemon/src/agent_scheduler.rs` (NEW, allowed), `crates/ucil-daemon/src/lib.rs` (allowed; only `pub mod agent_scheduler;` + `pub use` lines added), `scripts/verify/P3-W10-F13.sh` (NEW, allowed), `ucil-build/work-orders/0093-ready-for-review.md` (RFR marker, allowed).

- **§Anti-laziness contract** — 0 violations: no `passes` flips, no phase-done declarations, no test-skip annotations, no stubbed bodies, no edits to `feature-list.json` / `tests/fixtures/**` / `flip-feature.sh`, no `--no-verify` / `--no-edit` / `--amend` after push / force-push patterns, no benchmark removal / target relaxation / coverage loosening.

- **§Trait dependency-inversion seam (DEC-0008 §4)** — `WarmProcessorSource` is UCIL-owned (`crates/ucil-daemon/src/agent_scheduler.rs:449-531`); the production-impl wiring is **explicitly deferred to a follow-up WO** per scope_out[1] ("NO production wiring of a real-`KnowledgeGraph` `WarmProcessorSource` impl"). The module-header rustdoc (`crates/ucil-daemon/src/agent_scheduler.rs:50-65`) carries the "no substitute impls" forbidden-pattern declaration per scope_in[25].

- **§Mutation contract (M1 / M2 / M3)** — pre-baked in the RFR marker (`ucil-build/work-orders/0093-ready-for-review.md`) with verbatim diff patches, expected SA failures, and `git checkout --` restore commands. md5 (`054996053c367ad1a3fbe361d6ba878b`) recorded for round-trip verification per WO-0067 / WO-0068 / WO-0089 precedent.

- **§Verify script** — `scripts/verify/P3-W10-F13.sh` (44 LOC, mode 100755) carries `set -euo pipefail`, sanity-greps `pub mod agent_scheduler;`, asserts `cargo test ... -- --list` resolves to exactly 1 test, then runs the test asserting `test result: ok\. 1 passed`. Mirrors `scripts/verify/P3-W11-F09.sh` (WO-0089) shape. Cargo 1.94+ `-- --list` separator form per WO-0089 §B verifier note correctly applied at line 30.

- **§§Phase-3 standing carve-outs** — scope_out[27..30] disclose the AC36 / AC37 phase-N gate runs flake escalations (`20260507T0357Z-effectiveness-nav-rust-symbol-rs-line-flake.md`, `20260507T1629Z-effectiveness-refactor-rename-python-fixture-missing-symbol.md`, `20260507T1930Z-effectiveness-nav-rust-symbol-doctest-caller-flake.md`) and the standing coverage-gate `RUSTC_WRAPPER` workaround (use `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon …`) — the verifier is expected to re-run those gates from a clean session per WO-0067 / WO-0068 / WO-0069 precedent.

## Verdict

**CLEAN** — no blockers; 2 advisory warnings (bundled commit sizes per scope_in[13] DEC-0005 carve-out, frozen-test `#[allow(...)]` annotations consistent with G7 / G8 precedent) flagged for transparency only. The verifier may proceed.
