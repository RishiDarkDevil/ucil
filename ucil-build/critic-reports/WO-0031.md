# Critic Report: WO-0031

**Critic session**: crt-wo-0031-symbol-resolution
**Branch**: feat/WO-0031-symbol-resolution
**HEAD**: 906507b3a6b5ad9c0c8f99893422648433f6dc58
**Merge base**: cd40d240ac0af25b4db03ae057b3f0c3d746e9a6 (main)
**Feature**: P1-W4-F03 (symbol resolution)
**Verdict**: CLEAN

## Diff scope

```
crates/ucil-core/src/knowledge_graph.rs  | 337 ++++++++++++++++++++++++
crates/ucil-core/src/lib.rs              |   1 +
ucil-build/work-orders/0031-ready-for-review.md |  62 +++++
3 files changed, 400 insertions(+), 0 deletions(-)
```

5 commits on top of main:

| sha | subject | +/- |
|-----|---------|-----|
| `ac9d07c` | feat(core): add SymbolResolution struct | +42 |
| `906783e` | feat(core): add KnowledgeGraph::resolve_symbol | +116 / −6 |
| `6e93542` | test(core): cover symbol resolution | +185 |
| `1905ffa` | docs(core): re-export SymbolResolution from lib | +1 |
| `906507b` | chore(wo): WO-0031 ready-for-review marker | +62 |

No forbidden path (`ucil-build/feature-list.json`, master plan, `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`, `crates/ucil-core/src/incremental.rs`) is touched.

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate — non-blocking)

1. **Commit-size soft-target exceeded** — `906783e feat(core): add KnowledgeGraph::resolve_symbol` is +116/−6 LoC. The commit-style rule (`~50 LoC soft target`) and the WO's own suggested split (`~50 LoC`) both aimed lower. The commit is still logically coherent (rustdoc block ~40 lines, method body ~50 lines, `resolution_from_row` helper ~22 lines) and commit-style.md permits this as a soft target, so this is a warning, not a blocker. Would be reviewed more easily if the `resolution_from_row` row decoder had landed as a separate commit between the struct and the method.
2. **Test commit above planner estimate** — `6e93542 test(core): cover symbol resolution` is +185 LoC, versus the WO estimate of `~60 LoC — DEC-0005 allows one larger test commit`. DEC-0005 and the WO explicitly allow a larger test commit, so non-blocking, but the actual size is ~3× the estimate. Contributing factors: six sub-cases (a-f) plus a bonus qualified-name path, a `mk_resolver_fixture` helper (~26 lines), and extensive rustdoc/block comments (~35 lines) — all reasonable. Flagging for awareness only.

### OK

- **(1) Stub detection** — `rg` for `todo!()`, `unimplemented!()`, `NotImplementedError`, `raise NotImplementedError` returned no matches in `knowledge_graph.rs`. No trivial default-return stubs in the new `resolve_symbol` body — it prepares real SQL, binds params, and maps rows via `resolution_from_row`.
- **(2) Mocked critical dependencies** — the one new test (`test_symbol_resolution`, `knowledge_graph.rs:2145`) opens a real `rusqlite` connection via `KnowledgeGraph::open(tempdir/kg.db)` (`knowledge_graph.rs:2149-2150`), calls real `kg.upsert_entity` three times (`knowledge_graph.rs:2153,2172,2183`), and asserts on real `kg.resolve_symbol` results. No mocks of SQLite, LanceDB, Serena, LSP, or Docker. The three `mock`-containing hits found in the file (`knowledge_graph.rs:1331,1912,2013,2141`) are **documentation strings** explicitly calling out "No mocks of rusqlite / sqlite" — not actual mock code.
- **(3) Skipped / ignored tests** — no `#[ignore]`, `.skip(`, `xfail`, `it.skip`, `xit`, or `pytest.mark.skip` anywhere in the diff.
- **(4) Weak assertions** — every assertion in `test_symbol_resolution` checks a concrete value (file_path strings, start_line integers, parent_module `Some(...)` / `None`, `missing.is_none()`). No `assert!(true)`, no `assert_eq!(1, 1)`, no empty test bodies. Each assertion carries a rationale string.
- **(5) Hallucinated paths** — the only new import introduced is `tempfile::TempDir` inside `test_symbol_resolution` (`knowledge_graph.rs:2146`); `tempfile.workspace = true` is already declared at `crates/ucil-core/Cargo.toml:32`. Re-export target `pub use knowledge_graph::SymbolResolution;` at `crates/ucil-core/src/lib.rs:27` resolves to the new pub struct at `crates/ucil-core/src/knowledge_graph.rs:261`.
- **(6) Feature coverage** — the single feature `P1-W4-F03` is exercised by `test_symbol_resolution` (`knowledge_graph.rs:2145`), which maps one-to-one to the WO's acceptance list:
  - (a) `KnowledgeGraph::open` via tempdir — `knowledge_graph.rs:2149`
  - (b) Three upserts with the exact fixture data the WO specified — `knowledge_graph.rs:2153, 2172, 2183`
  - (c) `resolve_symbol("parse", None)` → newest-wins, parent_module `Some("ucil_core::types")` — `knowledge_graph.rs:2194-2209`
  - (d) Scoped resolve returns the treesitter row with `start_line=Some(42)` and parent_module `Some("ucil_treesitter::parser")` — `knowledge_graph.rs:2212-2232`
  - (e) `resolve_symbol("disambiguous", None)` with qualified_name=NULL → parent_module == None — `knowledge_graph.rs:2235-2244`
  - (f) `resolve_symbol("nonexistent", None)` → `Ok(None)` — `knowledge_graph.rs:2247-2253`
  - bonus: full-qualified-name match `"ucil_treesitter::parser::parse"` exercising the `qualified_name = ?1` / `LIKE '%::' || ?1` branches — `knowledge_graph.rs:2258-2266`.
- **(7) Commit hygiene** — all 5 commits are Conventional Commits (`feat/test/docs/chore(core|wo): …`), subject lines ≤72 chars, every commit carries the required trailers `Phase: 1`, `Feature: P1-W4-F03`, `Work-order: WO-0031` (verified: 5×5 matches). No `--amend`, no force-push, no bypass-hook markers (`--no-verify`, `-n`).
- **(8) Doc + public API** — both new `pub` items carry rustdoc:
  - `pub struct SymbolResolution` (`knowledge_graph.rs:261`): module-level rustdoc cites master-plan §12.1 lines 1130-1145 and §18 Phase 1 Week 4 line 1749; every public field has its own doc string explaining the NOT NULL / NULL semantics.
  - `pub fn resolve_symbol` (`knowledge_graph.rs:1008`): rustdoc covers the three match shapes, the `file_scope` narrowing, the `t_ingested_at DESC LIMIT 1` tie-break, the `parent_module` derivation, and an `# Errors` block enumerating `KnowledgeGraphError::Sqlite`. Intra-doc links use the unambiguous `[KnowledgeGraph::resolve_symbol]` / `[KnowledgeGraph::get_entity_by_qualified_name]` form required by `#![deny(rustdoc::broken_intra_doc_links)]`.
  - Internal helper `resolution_from_row` is not `pub`, but also carries a rustdoc block describing the `parent_module` derivation rule.

## Summary

The branch lands a clean, spec-conforming addition: one read-only method (`KnowledgeGraph::resolve_symbol`), one projection struct (`SymbolResolution`), one module-root test (`test_symbol_resolution` — the frozen F03 selector), and one re-export — with no changes to any frozen file, the entities DDL, or the F02 method surface. Tests hit real SQLite via tempfile (no mocks of critical collaborators). All master-plan citations the WO required are present. Commit hygiene is clean; only the `resolve_symbol` method commit exceeds the ~50 LoC soft target, and that is a non-blocking warning per commit-style.md.

**Recommendation**: proceed to verifier.
