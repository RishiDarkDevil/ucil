---
work_order: WO-0049
feature: P2-W7-F05
branch: feat/WO-0049-find-references-and-g1-source-production-wiring
ref: f197ad1389c0e2ef67b67e5364b45fc25970bcfd
merge_base: 3b51c78c2fdfb05bf56ccebd154760e495f63f3a
critic_session: crt-WO-0049-2026-05-05-74ac9bb7
verdict: BLOCKED
ready_for_verifier: false
---

# Critic Report: WO-0049 — find-references-and-g1-source-production-wiring

**Critic session**: `crt-WO-0049-2026-05-05-74ac9bb7`
**Branch**: `feat/WO-0049-find-references-and-g1-source-production-wiring`
**Ref**: `f197ad1389c0e2ef67b67e5364b45fc25970bcfd`
**Diff range**: `main..feat/WO-0049-find-references-and-g1-source-production-wiring` (merge-base `3b51c78`)
**Verdict**: **BLOCKED** — work-order is overwhelmingly under-delivered. The single landed commit closes only 2 of the 16 `scope_in` steps; the headline feature (the `find_references` MCP tool with 4 production-shape `G1Source` impls) is entirely absent.

## Diff summary

```
$ git log --oneline main..feat/WO-0049-find-references-and-g1-source-production-wiring
f197ad1 refactor(daemon): eliminate .expect in join_all_g1 + per-source tracing

$ git diff --name-only main...feat/WO-0049-find-references-and-g1-source-production-wiring
crates/ucil-daemon/src/executor.rs

$ git diff main...feat/WO-0049-find-references-and-g1-source-production-wiring --stat
 crates/ucil-daemon/src/executor.rs | 29 +++++++++++++++++++++++++----
 1 file changed, 25 insertions(+), 4 deletions(-)
```

1 file, 1 commit, +25 / −4 across the branch.

The work-order's `estimated_commits: 6` and 16-step `scope_in` describe a multi-file delivery — `crates/ucil-daemon/src/g1_sources.rs` (NEW), the
`McpServer.with_g1_sources` builder + the `find_references` route + `handle_find_references` method on `crates/ucil-daemon/src/server.rs`,
3 frozen module-root acceptance tests in `server.rs`, re-exports + a preamble paragraph in `crates/ucil-daemon/src/lib.rs`, and `scripts/verify/P2-W7-F05.sh`.
None of these have landed on the branch. The executor produced exactly one refactor-only commit closing scope_in steps 7 and 8 (per-source tracing + `.expect` cleanup on `executor.rs`).

No `ucil-build/work-orders/0049-ready-for-review.md` marker exists either — the executor never declared the WO complete, which is consistent with the body of work being absent.

## Findings

### Blockers (must fix before verifier)

1. **Feature deliverable missing — `crates/ucil-daemon/src/g1_sources.rs` NEW file does not exist on the branch.**
   `scope_in` step 1 names the file plus 4 production-shape `G1Source` impls (`KgG1Source`, `SerenaG1Source`, `AstGrepG1Source`, `DiagnosticsG1Source`).
   `git ls-tree feat/WO-0049-find-references-and-g1-source-production-wiring -- crates/ucil-daemon/src/g1_sources.rs` returns empty.
   Without these impls, the WO's named feature (P2-W7-F05) has no implementation.

2. **`G1SourceFactory` struct missing.** Required by `scope_in` step 2 (`pub struct G1SourceFactory` + `pub fn build()` returning `Vec<Box<dyn G1Source + Send + Sync + 'static>>`).
   `git grep G1SourceFactory feat/WO-0049-find-references-and-g1-source-production-wiring -- 'crates/ucil-daemon/src/**.rs'` returns empty.

3. **`McpServer.g1_sources` field + `with_g1_sources` builder missing.** Required by `scope_in` step 3.
   `git show feat/WO-0049-...-wiring:crates/ucil-daemon/src/server.rs | grep -E 'g1_sources|with_g1_sources'` returns empty — `crates/ucil-daemon/src/server.rs` is byte-identical to main.

4. **`find_references` route in `handle_tools_call` missing.** Required by `scope_in` step 5.
   `git show feat/WO-0049-...-wiring:crates/ucil-daemon/src/server.rs | grep -nE '"find_references"'` returns only the catalog row at `ucil_tools()` (frozen WO-0010 surface), no new route in `handle_tools_call`.

5. **`handle_find_references` method missing.** Required by `scope_in` step 6 — implements arg parsing, `G1Query` construction, `execute_g1` + `fuse_g1` orchestration, BTreeMap usage_type bucketing, and JSON-RPC envelope emission.
   `git grep handle_find_references feat/WO-0049-find-references-and-g1-source-production-wiring -- 'crates/ucil-daemon/src/**.rs'` returns empty.

6. **`lib.rs` re-exports for 5 new public symbols missing.** Required by `scope_in` step 4 / AC27.
   `git show feat/WO-0049-...-wiring:crates/ucil-daemon/src/lib.rs | grep -E 'KgG1Source|SerenaG1Source|AstGrepG1Source|DiagnosticsG1Source|G1SourceFactory'` returns zero hits. The lib.rs preamble paragraph naming F05's deliverable is also absent (the file is byte-identical to main).

7. **Frozen acceptance test `test_find_references_tool` missing.** AC03/AC04/AC05–AC09 cannot be satisfied.
   `git show feat/WO-0049-...-wiring:crates/ucil-daemon/src/server.rs | grep -nE 'pub async fn test_find_references_tool'` returns empty. The phase-2 invariant #3 ("frozen `#[test]` selectors") is therefore unmet for P2-W7-F05.

8. **Negative-path tests missing.** Required by `scope_in` step 10 / AC10–AC11 (`test_find_references_tool_unknown_symbol`, `test_find_references_tool_missing_name_param`).
   `git show feat/WO-0049-...-wiring:crates/ucil-daemon/src/server.rs | grep -nE 'test_find_references_tool_unknown_symbol|test_find_references_tool_missing_name_param'` returns empty.

9. **`scripts/verify/P2-W7-F05.sh` missing.** Required by `scope_in` step 11.
   `git ls-tree feat/WO-0049-...-wiring -- scripts/verify/P2-W7-F05.sh` returns empty. The standing per-feature verify-script convention (every prior P2 feature ships one) is broken.

10. **Feature coverage = 0 for `P2-W7-F05`.** The only feature_id in the work-order has zero new or modified tests referencing its name or behavior. Per critic.md check #6, "no test for feature X" → **BLOCK**. The single landed commit only edits the existing private helpers `run_g1_source` and `join_all_g1`; existing `executor::test_g1_parallel_execution` and `executor::test_g1_result_fusion` cover those helpers but do NOT exercise the absent `find_references` MCP tool path.

11. **Commit-cadence floor unmet (AC34 ≥4 commits).** Branch has exactly 1 commit. Per the work-order's `estimated_commits: 6` and AC34's "at least 4 commits", the cadence floor is missed by 3 commits.

12. **Pre-baked mutations AC28–AC30 unsatisfiable.** All three target code that does not exist on the branch:
    - AC28 mutates the routing condition `name == "find_references" && self.g1_sources.is_some()` — that condition was never added.
    - AC29 mutates `KgG1Source::execute` — `KgG1Source` does not exist.
    - AC30 mutates the `BTreeMap` group-by loop in `handle_find_references` — that handler does not exist.
    The verifier cannot run any of the WO's anti-laziness mutations; this is the authoritative anti-laziness layer per DEC-0007 and its absence is itself a blocker.

13. **No ready-for-review marker.** `ucil-build/work-orders/0049-ready-for-review.md` does not exist. The executor never declared the work-order complete; per CLAUDE.md workflow step 4, the marker is the explicit hand-off to verifier and its absence corroborates the under-delivery.

### Warnings (should fix before gate)

1. **Allow-list paths under-occupied (AC25 partial).** `git diff --name-only main...feat/WO-0049-...-wiring` returns only `crates/ucil-daemon/src/executor.rs` — that path IS in the AC25 allow-list, but the WO requires diffs to additionally cover `crates/ucil-daemon/src/server.rs`, `crates/ucil-daemon/src/lib.rs`, `crates/ucil-daemon/src/g1_sources.rs`, `scripts/verify/P2-W7-F05.sh`, and `ucil-build/work-orders/0049-ready-for-review.md`. The allow-list is satisfied vacuously by the absence of work, not by correct coverage.

2. **Single-commit body conflates two scope steps under one subject.** The lone commit `f197ad1` couples scope_in step 7 (per-source tracing instrumentation, ~12 LOC) and step 8 (the `.expect` cleanup, ~13 LOC) into one commit. Per `.claude/rules/commit-style.md` "one logical change per commit" plus DEC-0005 module-coherence framing, these are two logically distinct refactors — the tracing edit is observability, the `flatten()` swap is a panic-removal — and would normally be split. Borderline-acceptable under DEC-0005 because both edits sit in the same private helper neighborhood and total +25/−4, but worth flagging.

### OK

- **Stub scan (check #1) clean on the delivered diff.** The 25 added lines on `executor.rs` introduce no `todo!()`, `unimplemented!()`, `NotImplementedError`, bare `pass`, or trivial-default-return stubs. `git show f197ad1 -- crates/ucil-daemon/src/executor.rs | grep -nE 'todo!\(\)|unimplemented!\(\)|raise NotImplementedError'` → empty.

- **Critical-collaborator mocks (check #2) clean.** No tests added on this branch; therefore no opportunity for mocking Serena / LSP / SQLite / LanceDB / Docker. `git diff main...feat/WO-0049-...-wiring -- 'crates/ucil-daemon/tests/**' 'crates/ucil-daemon/src/**' | grep -iE 'mock|fake|stub' | grep -iE 'serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker'` → empty.

- **Skipped / ignored tests (check #3) clean.** `git diff main...feat/WO-0049-...-wiring | grep -nE '#\[ignore\]|\.skip\(|xfail|@pytest\.mark\.skip|it\.skip\('` → empty.

- **Weak assertions (check #4) clean.** `git diff main...feat/WO-0049-...-wiring | grep -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)|expect\(true\)\.toBe\(true\)'` → empty.

- **Hallucinated paths (check #5) clean on the delivered diff.** The `executor.rs` patch only adds a `#[tracing::instrument]` attribute (uses already-imported `tracing` macro) and rearranges existing `slots`/`flatten` calls — no new `use` statements. The `tracing` crate is already a dependency of `crates/ucil-daemon` (already used elsewhere in the file).

- **`.expect()` outside `#[cfg(test)]` cleared (AC24 partial-positive).** The original `executor.rs:1057` call has been replaced with `flatten() + debug_assert_eq!`. Remaining `.expect(...)` occurrences in the modified file (lines 1564–2636) are all inside `#[cfg(test)]` blocks (verified by inspecting the file structure) — permitted by root CLAUDE.md.

- **Commit hygiene (check #7) green for what landed.**
  - Conventional Commits subject: `refactor(daemon): eliminate .expect in join_all_g1 + per-source tracing` ✓
  - Body has `Phase: 2`, `Feature: P2-W7-F05`, `Work-order: WO-0049` trailers ✓
  - `Co-Authored-By: Claude Opus 4.6 (1M context)` trailer ✓
  - Size: 29 insertions / 4 deletions, well under the 50-LOC soft target ✓
  - No `--amend` after push, no force-push (single commit pushed as-is, `origin/feat/WO-0049-...-wiring` matches `feat/WO-0049-...-wiring` at `f197ad1`) ✓

- **Doc + public API (check #8) green for what landed.** The single commit's only public-surface change is implicit — `run_g1_source` is `async fn` not `pub`, and the new rustdoc paragraph above the `#[tracing::instrument]` attribute already documents the span name + the WO-0047 deferral closure rationale. No new `pub` items were introduced (no docs to demand).

- **Branch sync (AC35) green.** `git rev-parse origin/feat/WO-0049-...-wiring` matches `git rev-parse feat/WO-0049-...-wiring` (`f197ad1`). Working tree is clean.

- **Forbidden paths untouched (AC31–AC33).** `git diff --name-only main...feat/WO-0049-...-wiring -- 'tests/fixtures/**' 'ucil-build/feature-list.json' 'ucil-build/feature-list.schema.json' 'ucil-master-plan-v2.1-final.md'` → empty.

- **No `Cargo.toml` / `Cargo.lock` modifications (AC26).** Diff touches only `executor.rs`; no manifest changes.

## Recommendation to executor

Resume work on the branch and land the missing 14 of 16 `scope_in` items in additional commits before writing the ready-for-review marker:

1. Add `crates/ucil-daemon/src/g1_sources.rs` with the 4 `G1Source` impls + `G1SourceFactory` (≈250 LOC, split per impl as 4–5 commits).
2. Extend `crates/ucil-daemon/src/server.rs`: `McpServer.g1_sources` field, `with_g1_sources` builder, `find_references` route, `handle_find_references` method (≈150 LOC, 1–2 commits).
3. Add the 3 module-root acceptance tests on `server.rs` per DEC-0007 (≈300 LOC, 1 commit per DEC-0005 module-coherence allowance).
4. Update `crates/ucil-daemon/src/lib.rs`: `mod g1_sources;`, the 5 re-exports on a single line, and the WO-0049 preamble sentence appended to the WO-0048 paragraph (≈10 LOC, 1 commit).
5. Add `scripts/verify/P2-W7-F05.sh` per the standing convention (≈30 LOC, 1 commit).
6. Write `ucil-build/work-orders/0049-ready-for-review.md` once `cargo test -p ucil-daemon server::test_find_references_tool` is green locally.

The two refactors that already landed (`run_g1_source` instrumentation + `join_all_g1` `.expect` cleanup) are correct and can stay — they should NOT be reverted; they form the foundation for the missing work.

## Next steps

- **Verifier**: do NOT run. The work-order is incomplete; running tests would only confirm the absence of the feature.
- **Executor**: resume on `feat/WO-0049-find-references-and-g1-source-production-wiring`, land the missing scope_in items in fresh commits, and only write `0049-ready-for-review.md` after every AC03–AC11 / AC27 / AC34 check is locally green.
- **Triage / orchestrator**: this is best classified as an executor-incomplete state, not a feature-failure. The drift counter should NOT be incremented yet (the executor never declared completion); but if the executor goes idle for another iteration without progress, escalate per CLAUDE.md trigger #6 (drift counter ≥4).
