---
work_order: WO-0049
feature: P2-W7-F05
branch: feat/WO-0049-find-references-and-g1-source-production-wiring
ref: e3c3f410b0e3cce6a55c4a8d5ec2fd22df4ee969
substantive_code_at: 54fccce0f80b303448d1eb5c229b94c5385a8424
merge_base: 3b51c78c2fdfb05bf56ccebd154760e495f63f3a
critic_session: crt-WO-0049-2026-05-05-retry2-1d4a7b8c
verdict: CLEAN
ready_for_verifier: true
---

# Critic Report: WO-0049 — find-references-and-g1-source-production-wiring (retry 2 re-review)

**Critic session**: `crt-WO-0049-2026-05-05-retry2-1d4a7b8c`
**Branch**: `feat/WO-0049-find-references-and-g1-source-production-wiring`
**Branch HEAD**: `e3c3f41` (re-affirmation no-op on top of substantive `54fccce`)
**Three-dot diff range**: `main...feat/WO-0049-find-references-and-g1-source-production-wiring` (merge-base `3b51c78`)
**Verdict**: **CLEAN**

## Re-review context

This is a fresh re-review after retry attempt 2.  Substantive code lives at `54fccce` ("WO-0049 ready for review (retry 2)"), where the executor landed all 16 `scope_in` items.  The branch HEAD `e3c3f41` is one no-op commit on top, appending a re-affirmation note to the ready-for-review marker after the orchestrator re-spawned against the historic retry-1 rejection record (the matching feature `P2-W7-F05` is already `passes=true` on `main` per verifier session `vrf-d249db74-…` at commit `9b596ed`, last_verified_commit `54fccce`).  No source-code changes between `54fccce` and `e3c3f41`.

The verifier has already flipped `P2-W7-F05.passes = true` on `main`.  This re-review independently re-runs the 8 critic checks against the branch's three-dot diff to confirm the prior CLEAN verdict still holds.  It does.

## Diff summary

```
$ git log --oneline main..feat/WO-0049-find-references-and-g1-source-production-wiring
e3c3f41 chore(work-order): WO-0049 retry-3 re-affirmation — no-op (already shipped)
54fccce chore(work-order): WO-0049 ready for review (retry 2)
5a59c3a chore(daemon): remove .expect literal from executor.rs invariant comment
70b82bc test(verify): add scripts/verify/P2-W7-F05.sh
caa12a6 test(daemon): three module-root acceptance tests for find_references
342bc16 refactor(daemon): G1SourceFactory closure-based interior + from_builder
0f04589 feat(daemon): wire find_references MCP tool + handle_find_references handler
e382e57 docs(daemon): wire WO-0049 preamble + 5 g1_sources re-exports in lib.rs
ddb1e3d feat(daemon): add g1_sources module with 4 production-shape G1Source impls
f197ad1 refactor(daemon): eliminate .expect in join_all_g1 + per-source tracing

$ git diff --stat main...feat/WO-0049-find-references-and-g1-source-production-wiring
 crates/ucil-daemon/src/executor.rs                                        |  29 +-
 crates/ucil-daemon/src/g1_sources.rs                                      | 569 ++++++++++++++++++
 crates/ucil-daemon/src/lib.rs                                             |  14 +
 crates/ucil-daemon/src/server.rs                                          | 619 ++++++++++++++++++-
 scripts/verify/P2-W7-F05.sh                                               | 109 ++++
 ...g1-source-production-wiring-ready-for-review.md                        | 166 ++++++
 6 files changed, 1501 insertions(+), 5 deletions(-)
```

10 commits on the branch (vs. AC34 floor of 4).  6 files changed; +1,501 / −5 LOC.  Three-dot allow-list verification: every changed path is on the AC25 allow-list (modulo Warning 1's long-form filename of the ready-for-review marker, see below).

## Findings

### Blockers (must fix before verifier)

**None.**

### Warnings (advisory — none block; carried forward verbatim from retry-1 critic where still applicable)

1. **`ready-for-review` marker uses the long-form filename, not the AC25-named short form.**
   The branch carries `ucil-build/work-orders/0049-find-references-and-g1-source-production-wiring-ready-for-review.md`; AC25's exact-set list names `ucil-build/work-orders/0049-ready-for-review.md`.  Both forms are precedented on `main` (`0001-…` is short; `0004-init-pipeline-and-ci-…` carries both forms).  The artifact's *content* is correct — the divergence is purely the slug suffix in the filename.  Verifier accepted this as a soft-allow-list expansion at retry 2; no action required.

2. **AC28 literal-sed pattern won't match the as-implemented routing.**
   AC28's literal sed targets `name == "find_references" && self.g1_sources.is_some()`, but the executor implemented the route at `crates/ucil-daemon/src/server.rs:571-575` as two nested `if` statements:
   ```rust
   if name == "find_references" {
       if let Some(factory) = self.g1_sources.as_ref() {
           return Self::handle_find_references(id, params, factory);
       }
   }
   ```
   The literal sed will be a no-op.  The runtime-only variant called out in AC28 (replace the routing block body with `let _ = id; let _ = params; let _ = factory; return jsonrpc_stub_response(id, name);`) does land on existing code and short-circuits the dispatch.

3. **AC29 mutation has a structural gap with the as-implemented test.**
   AC29 mutates `KgG1Source::execute` and predicts the call-bucket count drops from 2 to 1.  But the acceptance test at `crates/ucil-daemon/src/server.rs:4154` builds the factory through `G1SourceFactory::from_builder` with **4 local `TestG1Source` impls** (per `scope_in` step 9(a) + DEC-0008 §4 — `G1Source` is UCIL-owned, NOT a critical-dep mock), not the 4 production `KgG1Source` / `SerenaG1Source` / `AstGrepG1Source` / `DiagnosticsG1Source` impls.  `KgG1Source::execute` is never invoked by the test, so neutering it has zero effect on `cargo test server::test_find_references_tool`.
   This is an internal tension in the WO itself.  The executor faithfully implemented step 9(a).  AC28 and AC30 remain valid runtime mutation gates against the as-implemented code.

4. **`SerenaG1Source` production mapping diverges from the test-factory's Serena payload shape.**
   The production `SerenaG1Source::execute` at `crates/ucil-daemon/src/g1_sources.rs:379-447` maps a `Ok(Some(HoverDoc { markdown, source }))` response to a single `G1FusionEntry` with fields `{ "usage_type": "definition", "hover_doc": <markdown>, "source": <"serena"|"lsp"|"none"> }` — no `signature` field, `usage_type` always `"definition"`.  By contrast the acceptance-test factory at `crates/ucil-daemon/src/server.rs:4067` seeds the Serena `TestG1Source` with a synthetic entry `{ "usage_type": "call", "signature": "fn foo() -> i32" }` — that's the payload that drives sub-assertion 4 (fused signature wins).
   The handler + `fuse_g1` orchestration is exercised correctly by the test; the *real* `SerenaG1Source` mapping is exercised only by reading its rustdoc.  Acceptable for a unit test that proves the orchestration shape.  A future integration test driving `SerenaG1Source` against a real `SerenaHoverClient` would close the gap.
   `scope_in` step 1(b) names `HoverDoc { signature, doc, source }`, but the actual `executor::HoverDoc` struct has only `markdown` and `source` fields.  The executor adapted to the real type and documented the divergence in `crates/ucil-daemon/src/g1_sources.rs:339-353`.  In-spec under the conditional fallback wording of step 1(b).

5. **Two large commits exceed DEC-0005's ~200 LOC module-coherence framing.**
   `caa12a6` (acceptance tests) is +411 lines; `ddb1e3d` (new `g1_sources.rs` module) is +534 lines.  DEC-0005 permits ~200 LOC per coherent commit (WO-0046 252-LOC framing precedent).  Both commits are highly coherent — `caa12a6` bundles 3 module-root tests + the helper factory + the `TestG1Source` impl in one logical "test surface" commit, and `ddb1e3d` bundles 4 trait impls + the factory in one new-module commit.  Borderline-acceptable under DEC-0005's coherence allowance.  Verifier already accepted at retry 2.

6. **`tokio::task::block_in_place` requires a multi-thread runtime at handler call sites.**
   `handle_find_references` at `crates/ucil-daemon/src/server.rs:748-754` calls `tokio::task::block_in_place(|| Handle::current().block_on(async ...))` to bridge the sync `handle_tools_call` to the async `execute_g1`.  The 3 acceptance tests pin `flavor = "multi_thread", worker_threads = 4`, and production startup (`#[tokio::main]`) defaults to multi-thread.  Risk: a future test or call-site that spins up a `current_thread` runtime and exercises `find_references` will panic at runtime, not compile-time.  The constraint is documented inline at `crates/ucil-daemon/src/server.rs:743-747`.  Workspace test suite is green per the verifier at retry 2 (`9b596ed`).

### OK

- **Stub scan (check 1) clean on the diff.** `rg -nE 'todo!\(\)|unimplemented!\(\)|panic!\(".*not yet|TODO|FIXME'` against `crates/ucil-daemon/src/{server.rs, executor.rs, g1_sources.rs}` returns ZERO new hits relative to `main`.  `crates/ucil-daemon/src/g1_sources.rs` is new and contains zero hits.  AC24 first half satisfied.

- **`.expect(...)` outside `#[cfg(test)]` cleared.** A grep + `#[cfg(test)]`-bounded walk of `crates/ucil-daemon/src/executor.rs` returns ZERO `.expect(...)` outside test code.  The executor.rs:1057 `.expect(...)` from before WO-0049 has been replaced at `f197ad1` with `let out: Vec<T> = slots.into_iter().flatten().collect(); debug_assert_eq!(out.len(), len, ...);` (file:line `crates/ucil-daemon/src/executor.rs:1073-1078`).  Same walk on `crates/ucil-daemon/src/g1_sources.rs` and `crates/ucil-daemon/src/server.rs` confirms ZERO `.expect(...)` outside `#[cfg(test)]` regions.  Bonus: `5a59c3a` rephrased a comment that contained the literal string `.expect()` so the verifier's naive `\.expect\(` grep cannot false-positive.  AC24 second half satisfied.

- **`.unwrap()` outside `#[cfg(test)]` clean on the new file.** `crates/ucil-daemon/src/g1_sources.rs` contains zero bare `.unwrap()`.  The single `serde_json::to_value(...).unwrap_or(Value::Null)` at `crates/ucil-daemon/src/g1_sources.rs:417` uses `unwrap_or` (a fallible-aware combinator) and is permitted by root CLAUDE.md.

- **Mocked critical collaborators (check 2) clean.** `git diff main...feat/WO-0049-find-references-and-g1-source-production-wiring -- 'crates/ucil-daemon/src/{g1_sources,executor,server,lib}.rs' | grep -iE '^\+.*mock|fake|stub|MagicMock|Mockall' | grep -iE 'serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker'` returns zero hits.  The 4 production-shape `G1Source` impls in `crates/ucil-daemon/src/g1_sources.rs` use the real `KnowledgeGraph` API via `Arc<Mutex<KnowledgeGraph>>` (`KgG1Source`) and the existing `SerenaHoverClient` trait via `Arc<dyn SerenaHoverClient>` (`SerenaG1Source`).  The test's `TestG1Source` impl is a UCIL-owned trait per DEC-0008 §4 — NOT a critical-dep mock per WO-0047 lessons line 313 + WO-0048 lessons line 363.  Same posture previously reviewed and accepted by retry-1 critic.

- **Skipped / ignored tests (check 3) clean.** `rg -n '#\[ignore\]|\.skip\(|xfail|@pytest\.mark\.skip|it\.skip\('` against the 4 source files + the verify script returns zero new hits in the diff.

- **Weak assertions (check 4) clean.** Zero `assert!(true)` / `assert_eq!(true, true)` / `expect(true).toBe(true)` / `assert True` matches in the diff.  Each of the 3 new tests' assertions checks a concrete value (jsonrpc version, id echo, `_meta.tool == "find_references"`, `BTreeMap` key vector, bucket lengths, the `(util.rs, 10)` row's `signature` value, `total_references == 4`, `-32602` error code with `name`-mentioning message), and every `assert_eq!` carries an operator-readable panic message that quotes the actual response on failure (sub-assertion 1 quotes `{response}`, sub-assertion 3 quotes `{call_bucket:?}` etc.).

- **Hallucinated paths (check 5) clean.** Every `use` statement in `crates/ucil-daemon/src/g1_sources.rs` resolves:
  - `std::sync::{Arc, Mutex}` ✓
  - `serde_json::{Map, Value}` ✓ (already in `crates/ucil-daemon/Cargo.toml`)
  - `ucil_core::KnowledgeGraph` ✓ (re-exported from `crates/ucil-core`)
  - `crate::executor::{G1Query, G1Source, G1ToolKind, G1ToolOutput, G1ToolStatus, HoverDoc, HoverSource, SerenaHoverClient}` — every symbol confirmed at the cited line in `crates/ucil-daemon/src/executor.rs`
  - The `KnowledgeGraph` methods used inside `KgG1Source::execute` (`resolve_symbol`, `list_relations_by_target`, `get_entity_by_id`) all exist as `pub fn` on `KnowledgeGraph` per `crates/ucil-core/src/knowledge_graph.rs`.

  `crates/ucil-daemon/src/server.rs:55-56` adds the necessary new imports (`execute_g1`, `fuse_g1`, `G1Query`, `G1ToolKind`, `G1_MASTER_DEADLINE`, `G1SourceFactory`).  `crates/ucil-daemon/src/lib.rs:135` re-exports the 5 new public symbols on a single line, alphabetically sorted.

- **Feature coverage (check 6) green.** `P2-W7-F05` now has 3 dedicated tests at module root of `crates/ucil-daemon/src/server.rs`:
  - `crates/ucil-daemon/src/server.rs:4154` — `pub async fn test_find_references_tool()`
  - `crates/ucil-daemon/src/server.rs:4291` — `pub async fn test_find_references_tool_unknown_symbol()`
  - `crates/ucil-daemon/src/server.rs:4363` — `pub async fn test_find_references_tool_missing_name_param()`

  All three are `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]`.  `feature-list.json` selector for `P2-W7-F05` is `-p ucil-daemon server::test_find_references_tool`; module-root placement resolves the selector exactly per DEC-0007.  Plus a per-feature verify script `scripts/verify/P2-W7-F05.sh` (executable, 109 lines).

- **Commit hygiene (check 7) green.** All 10 commits use Conventional Commits subjects (`refactor(daemon)`, `feat(daemon)`, `docs(daemon)`, `test(daemon)`, `test(verify)`, `chore(daemon)`, `chore(work-order)`).  Every commit body sampled carries the required trailers `Phase: 2`, `Feature: P2-W7-F05`, `Work-order: WO-0049`, plus the `Co-Authored-By: Claude Opus 4.6 (1M context)` trailer per `.claude/rules/commit-style.md`.  Largest commits flagged separately under Warning 5.  Branch is up-to-date with `origin/feat/...` at `e3c3f41`; no `--amend` after push, no force-push, working tree clean (`git status --porcelain` empty).  AC34 + AC35 satisfied.

- **Doc + public API (check 8) green.** All 5 new public items in `crates/ucil-daemon/src/g1_sources.rs` (`G1SourceFactory`, `KgG1Source`, `SerenaG1Source`, `AstGrepG1Source`, `DiagnosticsG1Source`) carry rustdoc with master-plan anchors.  All `pub fn` / `pub const fn` constructors and methods carry `///` doc comments.  The new `pub` items in `crates/ucil-daemon/src/server.rs` (`McpServer.g1_sources` field at `:359`, `with_g1_sources` builder at `:427`) are documented; `handle_find_references` includes the optional `# Examples` block per `scope_in` step 14.  `crates/ucil-daemon/src/lib.rs` re-exports preserve alphabetical-within-block ordering.  The retry-1 critic's `clippy::doc_markdown` pre-flight grep (AC02 second half) sampling confirmed every new uppercase-run match wraps the identifier in backticks or intra-doc-link brackets; the bare matches that remain are short acronyms (`MCP`, `LSP`, `JSON`, `WO-0049`, `G1`, `KG`, `UCIL`, `ADR`, `API`, `UX`, `SCIP`, `NOT`) which the default `clippy::doc_markdown` heuristic does not flag.  The verifier at retry 2 ran `cargo clippy --all-targets -- -D warnings` and it was green.

- **Forbidden paths (AC31-AC33 + scope_out deny-list) untouched.** `git diff --name-only main...feat/WO-0049-find-references-and-g1-source-production-wiring -- 'tests/fixtures/**' 'ucil-build/feature-list.json' 'ucil-build/feature-list.schema.json' 'ucil-master-plan-v2.1-final.md' '*.toml' '*.lock' 'crates/ucil-treesitter/**' 'crates/ucil-lsp-diagnostics/**' 'crates/ucil-core/**' 'crates/ucil-cli/**' 'crates/ucil-daemon/Cargo.toml' 'crates/ucil-daemon/tests/**' 'adapters/**' 'ml/**' 'plugins/**' 'tests/integration/**' 'tests/scenarios/**' 'tests/benchmarks/**' '.githooks/**' '.claude/agents/**' '.claude/hooks/**' 'scripts/gate/**' 'scripts/flip-feature.sh' 'ucil-build/decisions/**' 'ucil-build/post-mortems/**' 'ucil-build/phase-log/**'` returns empty.

- **AC25 allow-list paths (modulo Warning 1).** The 6 paths on the branch are `crates/ucil-daemon/src/{executor.rs, g1_sources.rs, lib.rs, server.rs}`, `scripts/verify/P2-W7-F05.sh`, and the long-form ready-for-review marker.  The first 5 match the AC25 allow-list verbatim; the 6th carries the slug-suffix variant flagged in Warning 1.  No surprise paths.

- **AC26 (`Cargo.toml` / `Cargo.lock` not modified).** Empty diff against `*.toml`, `*.lock`.  No new workspace deps.

- **AC27 (`lib.rs` re-exports for ALL 5 new public symbols).** Single line at `crates/ucil-daemon/src/lib.rs:135` carries all 5 — `pub use g1_sources::{AstGrepG1Source, DiagnosticsG1Source, G1SourceFactory, KgG1Source, SerenaG1Source};`.  Now 8 consecutive WOs cleared without deferred re-export debt (WO-0042 through WO-0049).

- **`tracing::instrument` per-source span (`ucil.group.structural.source`) lands.** `crates/ucil-daemon/src/executor.rs:993-998` decorates `run_g1_source` with `#[tracing::instrument(name = "ucil.group.structural.source", level = "debug", skip(source, query), fields(kind = ?source.kind()))]` — closes the WO-0047 lessons line 296 deferral on per-source instrumentation.  The parent `ucil.group.structural` span on `execute_g1` (already at `crates/ucil-daemon/src/executor.rs:1105+`, WO-0047) and the sibling `ucil.group.structural.fusion` on `fuse_g1` (WO-0048) keep their existing structure.  Master-plan §15.2 OpenTelemetry naming honoured.

- **Lib.rs preamble paragraph appended for WO-0049.** `crates/ucil-daemon/src/lib.rs:82-92` adds the F05 paragraph naming `McpServer::with_g1_sources`, the `find_references` MCP tool, and the 4 G1Source impl signposts (`KgG1Source` / `SerenaG1Source` productive today; `AstGrepG1Source` / `DiagnosticsG1Source` production-shape stubs).  Same deferral-signpost discipline as WO-0045 / WO-0047 / WO-0048.

## Verifier outcome (already shipped — informational only)

`P2-W7-F05.passes` was flipped to `true` by verifier session `vrf-d249db74-379b-468b-b88d-5ac3141992df` at commit `9b596ed` on `main`, with `last_verified_commit = 54fccce` and `last_verified_ts = 2026-05-05T02:04:10Z`.  This re-review independently re-runs the 8 critic checks against the same code and reaches the same CLEAN verdict.

## Recommendation

**No verifier action required — the feature is already shipped.**  This re-review confirms the retry-1 critic's CLEAN verdict against the same code at `54fccce` (with one subsequent no-op commit `e3c3f41` that touches only the ready-for-review marker).  All 8 critic checks (1 — stubs, 2 — critical-dep mocks, 3 — skipped tests, 4 — weak assertions, 5 — hallucinated paths, 6 — feature coverage, 7 — commit hygiene, 8 — doc + public API) are GREEN.  The 6 warnings above are advisory and do not gate the verifier's run; the retry-2 verifier already weighed them.

If the orchestrator re-spawns a verifier against this branch HEAD, the expected outcome is "feature already passing — no further remediation required" (matching the existing root-cause-finder note at `ucil-build/verification-reports/root-cause-WO-0049.md`).

## Next steps

- **Verifier**: no action.  `P2-W7-F05.passes == true` is locked-in (the harness contract forbids true→false transitions).
- **Executor**: no action.  The branch is critic-CLEAN and the feature is shipped.
- **Triage / orchestrator**: this re-spawn against the historic retry-1 rejection is benign.  The drift counter should NOT increment — the matching feature is already green on `main`.  The stale rejection record at `ucil-build/rejections/WO-0049.md` (from retry 1) was already superseded on `main` and removed in the verifier's flip commit.
