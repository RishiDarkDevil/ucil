# Critic Report: WO-0003

**Critic session**: crt-0003-retry1-20260415
**Branch**: feat/0003-init-fixtures
**Retry attempt**: 1
**Commits reviewed** (full branch, `main..feat/0003-init-fixtures`):
- `b5f270b feat(cli): implement ucil init command with language detection`
- `d41556d feat(fixtures): add rust-project test fixture (~5.8K LOC)`
- `a3d0754 chore(verifier): WO-0003 verification REJECT — F12/F13/F14 absent, F03/F11 blocked by cargo test compile error`
- `6592a7a chore(verifier): escalate stop-hook blocking verifier session after WO-0003 rejection`
- `58c2bc8 fix(cli): add tempfile dev-dep + fix otel doctest Span import`
- `d513330 fix(fixtures): F11 — add SLOW-TEST #[ignore] + DEC-0003 + hook update`

**Verdict**: BLOCKED

---

## Findings

### Blockers (must fix before verifier)

**1. F12 (python-project fixture) still entirely absent — no code committed**

The work order requires:
- `tests/fixtures/python-project/` directory with pyproject.toml, ≥ 5 000 LOC of typed Python, and passing pytest tests.
- `tests/fixtures/python_project/test_fixture_valid.py` (underscore path).
- `scripts/verify/P0-W1-F12.sh` (not in the pre-seeded scripts — no such file exists on the branch).

`git ls-tree -r feat/0003-init-fixtures -- tests/fixtures/` shows no `python-project` subtree.
No verify script for F12 exists on the branch.
Acceptance criterion 3:
```
cd tests/fixtures/python-project && uv run pytest ../python_project/test_fixture_valid.py
```
is **untestable** — both directories are missing.

---

**2. F13 (typescript-project fixture) still entirely absent — no code committed**

The work order requires:
- `tests/fixtures/typescript-project/` with package.json, tsconfig.json, ≥ 5 000 LOC of typed TypeScript, and vitest tests.
- `adapters/tests/fixtures/typescript-project.test.ts`.
- `adapters/vitest.config.ts` (or equivalent) for test discovery.

`git ls-tree -r feat/0003-init-fixtures -- tests/fixtures/` shows no `typescript-project` subtree.
`git ls-tree -r feat/0003-init-fixtures -- adapters/` shows no `tests/` subdirectory.
Acceptance criterion 4:
```
pnpm --filter adapters vitest run adapters/tests/fixtures/typescript-project.test.ts
```
is **untestable** — neither the test file nor the fixture directory exist.

---

**3. F14 (mixed-project fixture) still entirely absent; verify script is an unmodified `exit 1` stub**

The work order requires:
- `tests/fixtures/mixed-project/` with Rust/TypeScript/Python sources containing intentional lint defects.
- `scripts/verify/P0-W1-F14.sh` that asserts file existence and at least one clippy warning.

`git ls-tree -r feat/0003-init-fixtures -- tests/fixtures/` shows no `mixed-project` subtree.
`git diff main feat/0003-init-fixtures -- scripts/verify/P0-W1-F14.sh` is **empty** — the script
is identical to the pre-seeded stub on `main`:
```bash
#!/usr/bin/env bash
# Acceptance test for P0-W1-F14
# Executor agents will implement this script during the feature's work-order.
echo "TODO: implement acceptance test for P0-W1-F14"
exit 1
```
Acceptance criterion 5:
```
bash scripts/verify/P0-W1-F14.sh exits 0
```
fails immediately on line 4.

---

**4. No `0003-ready-for-review.md` marker written**

`git ls-tree -r feat/0003-init-fixtures -- ucil-build/work-orders/` shows no
`0003-ready-for-review.md` file. The executor has not self-declared this retry
ready for verifier review. The critic review was requested externally despite the
absence of this marker. The marker must be present before the verifier is spawned
(per `ucil-build/CLAUDE.md` workflow step 4).

---

### Resolved since first critic report (for the record)

The following issues from the first critic report (`crt-0003-20260415`) are now
**CLEAN** in the retry diff:

- **[First-report Blocker 2 resolved]** F11 acceptance criterion gap: `rust_project_loads`
  now has `// SLOW-TEST:` comment + `#[ignore]` attribute (commit `d513330`).
  ADR `DEC-0003-slow-test-ignore-allowlist.md` filed and merged.
  Pre-commit hook updated to detect the `SLOW-TEST` exemption pattern.

- **[First-report W1 resolved]** Commit `d513330` body includes `Ref: DEC-0001`.

- **[First-report W2 resolved]** `pub fn rust_project_loads` → `fn rust_project_loads`
  (commit `d513330`).

- **[First-report W4 resolved]** `# Examples` block added to `detect_languages()`
  (commit `d513330`).

- **[Verifier compile error resolved]** `tempfile` added to `[dev-dependencies]` in
  `crates/ucil-cli/Cargo.toml`; otel doctest `Span as _` import fixed
  (commit `58c2bc8`).

---

### Warnings (carry-over, should fix before gate)

**W1 (carry-over): `std::process::Command` in `crates/ucil-core/tests/fixture.rs:40`**

`std::process::Command::new("cargo")` is used in a non-async test. No strict violation
today (the test is `#[test]`, not `#[tokio::test]`), but if the test is ever made async
the CLAUDE.md rule requires `tokio::process::Command`. Flagging for awareness.

---

### OK

- No `todo!()`, `unimplemented!()`, stub-returning functions in any changed file.
- No mocked critical collaborators (Serena, LSP, SQLite, LanceDB, Docker).
- No `.skip()`, `xfail`, or commented-out assertions outside the DEC-0003–approved
  `// SLOW-TEST:` + `#[ignore]` pattern.
- No weak assertions (`assert!(true)` etc.).
- No hallucinated imports: `ucil_core::schema_migration::SCHEMA_VERSION` confirmed
  present in `crates/ucil-core/src/schema_migration.rs`.
- `feature-list.json` diff only touches whitelisted fields (`last_verified_ts`,
  `last_verified_by`, `last_verified_commit`, `attempts`). Frozen fields (`id`,
  `description`, `acceptance_tests`, `dependencies`, `passes`) are unchanged.
- Commit hygiene for `58c2bc8` and `d513330`: Conventional Commits subject ✓,
  `Phase: 0`, `Feature:`, `Work-order: WO-0003` trailers ✓.
- `init.rs` implementation is complete and non-trivial: language detection, idempotent
  `.ucil/` creation, `ucil.toml` serialisation with `SCHEMA_VERSION`. No stubs.
- Unit tests in `init.rs` cover all four language detections and empty-dir case.
- `scripts/verify/P0-W1-F03.sh` is complete and tests idempotency.

---

## Required actions before verifier (retry 2)

1. **Implement F12 (python-project fixture)** — `tests/fixtures/python-project/`
   (≥ 5 K LOC real Python code, `pyproject.toml`, passing pytest tests),
   `tests/fixtures/python_project/test_fixture_valid.py`, and
   `scripts/verify/P0-W1-F12.sh`.

2. **Implement F13 (typescript-project fixture)** — `tests/fixtures/typescript-project/`
   (≥ 5 K LOC real TypeScript, `package.json`, `tsconfig.json`, vitest tests),
   `adapters/tests/fixtures/typescript-project.test.ts`, and
   `adapters/vitest.config.ts` (or equivalent) with test discovery configured.

3. **Implement F14 (mixed-project fixture)** — `tests/fixtures/mixed-project/` with
   intentional lint defects in Rust/TypeScript/Python, and update
   `scripts/verify/P0-W1-F14.sh` to assert file existence + ≥ 1 clippy warning
   (currently still the pre-seeded `exit 1` stub — **no changes made in retry 1**).

4. **Write `ucil-build/work-orders/0003-ready-for-review.md`** after all acceptance
   criteria pass locally.
