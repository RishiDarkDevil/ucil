# Critic Report: WO-0003

**Critic session**: crt-0003-retry2-20260415
**Branch**: feat/0003-init-fixtures
**Retry attempt**: 2
**Commits reviewed** (`main..feat/0003-init-fixtures` as of `d264d86`):

```
d264d86 chore(executor): WO-0003 ready-for-review — all 8 acceptance criteria green
7e792fc feat(fixtures): add F13 typescript-project fixture (~4.2K LOC task-query engine)
8379a06 feat(fixtures): add F12 python-project fixture (~4.8K LOC expression interpreter)
b1a2fdb feat(fixtures): add F14 mixed-project fixture with intentional lint defects
f6c7502 chore(verifier): WO-0003 retry-2 rejection — F12/F13/F14 still absent + harness bug
d513330 fix(fixtures): F11 — add SLOW-TEST #[ignore] + DEC-0003 + hook update
58c2bc8 fix(cli): add tempfile dev-dep + fix otel doctest Span import
e4828df chore(verifier): WO-0003 rejection escalation — executor must complete F12/F13/F14
6592a7a chore(verifier): escalate stop-hook blocking verifier session after WO-0003 rejection
a3d0754 chore(verifier): WO-0003 verification REJECT — F12/F13/F14 absent, F03/F11 blocked by cargo test compile error
d41556d feat(fixtures): add rust-project test fixture (~5.8K LOC)
b5f270b feat(cli): implement ucil init command with language detection
```

**Verdict**: CLEAN

---

## Findings

### Blockers (must fix before verifier)

_None._

All three blockers from the retry-1 report (F12/F13/F14 absent) are resolved in this
retry. See **Resolved** section below for details.

---

### Warnings (should fix before gate)

**W1 (carry-over, unchanged): `std::process::Command` in `crates/ucil-core/tests/fixture.rs:40`**

`std::process::Command::new("cargo")` is used in a synchronous `#[test]` body (not an
async path), so the CLAUDE.md rule ("use `tokio::process::Command` in async paths") is
not technically violated. However, if this test is ever converted to `#[tokio::test]`
the rule would trigger. Flagging for continuity.

---

**W2 (carry-over, unchanged): Large fixture commits do not cite DEC-0001**

`8379a06` (+6 334 lines, F12) and `7e792fc` (+7 111 lines, F13) both exceed the 50-line
soft target without citing `DEC-0001-WO-0002-large-commits` in their commit bodies.
`DEC-0001` was listed in the WO's `context_refs` and the earlier fix commit `d513330`
did reference it, so this is the same omission flagged in retry-1. The commits are
otherwise clearly cohesive (single fixture per commit), so this is a hygiene issue, not
a correctness issue.

---

**W3 (new): `assert_eq!(2 + 2, 4)` in `tests/fixtures/mixed-project/tests/integration_test.rs:13`**

The passing test in the mixed-project Rust fixture is a bare `assert_eq!(2+2, 4)` smoke
test. This would be a blocker if it appeared in UCIL's own test suite (Check 4:
trivially-true assertion). It appears inside a defect fixture whose purpose is to test
that UCIL's diagnostic tooling can detect fixture defects — the smoke test exists only
to prove the integration test binary compiles, which is a legitimate purpose. Flagging
as a low-severity warning rather than a blocker because:
  - `tests/fixtures/mixed-project/` is a defect fixture, not UCIL source.
  - The fixture is documented (`// Smoke test: just confirm the integration test harness
    itself compiles.`).
  - No feature's acceptance test reads this assertion as a quality signal.

---

### Resolved since retry-1 (for the record)

**[Retry-1 Blocker 1 resolved] F12 — python-project fixture committed and complete**

`8379a06` adds `tests/fixtures/python-project/` — a full expression-language interpreter:
- `src/python_project/types.py` (1 138 L), `lexer.py` (1 090 L), `parser.py` (1 049 L),
  `evaluator.py` (1 405 L) — total 4 682 source lines.
- 146 pytest tests in `tests/` (all passing per executor self-report).
- `tests/fixtures/python_project/test_fixture_valid.py` — 4 tests that verify directory
  existence, TOML validity, type annotations, and `total_lines ≥ 2 000` (actual: 4 682).
- `scripts/verify/P0-W1-F12.sh` — asserts fixture structure then runs validator via
  `python3 -m pytest`.

_Criterion 3 (`pytest` on `test_fixture_valid.py`) is now testable. ✓_

---

**[Retry-1 Blocker 2 resolved] F13 — typescript-project fixture committed and complete**

`7e792fc` adds `tests/fixtures/typescript-project/` — a task-management query engine:
- `src/types.ts` (1 120 L), `repository.ts` (1 006 L), `filter-engine.ts` (954 L),
  `task-manager.ts` (1 087 L) — total 4 167 source lines; strict-mode TypeScript.
- 134 vitest tests in `tests/` (task-manager: 45, filter-engine: 51, repository: 38).
- `adapters/tests/fixtures/typescript-project.test.ts` — 15 structural-validation tests.
- `adapters/vitest.config.ts` — root set to workspace root so `pnpm --filter adapters
  vitest run adapters/tests/fixtures/typescript-project.test.ts` resolves correctly.
- `adapters/package.json` — adds `vitest: "vitest"` script alias and `vitest@^2.1.8`
  dev dependency.

_Criterion 4 (`pnpm --filter adapters vitest run ...`) is now testable. ✓_

---

**[Retry-1 Blocker 3 resolved] F14 — mixed-project fixture committed and complete**

`b1a2fdb` adds `tests/fixtures/mixed-project/` with intentional defects in all three
languages and `scripts/verify/P0-W1-F14.sh`:

- **Rust** (`src/main.rs`): unused import (`use std::collections::BTreeSet`), dead
  function without `#[allow]`, `.unwrap()` on `Option`, SQL-injection-style concatenation
  comment — all of which `cargo clippy` warns on.
- **TypeScript** (`src/index.ts`): implicit `any` via `@ts-ignore`, `console.log` in
  library function.
- **Python** (`src/main.py`): B006 mutable default argument, bare `except:` clause,
  `print()` in library code.
- Each language has one intentionally-failing test skipped in CI:
  - `tests/integration_test.rs:19` — `#[ignore = "SLOW-TEST: intentionally failing..."]`
  - `tests/index.test.ts:28` — `it.skip("intentionally failing...")`
  - `tests/test_main.py:25` — `@pytest.mark.skip(reason="Intentionally failing...")`
  All three are exempt from the pre-commit hook via `ALLOW_PREFIX` (DEC-0004).
- `scripts/verify/P0-W1-F14.sh` — asserts all 9 expected files exist, runs `cargo clippy
  --manifest-path ...` and verifies at least one `warning:` line appears (exits 0).
- `ucil-build/decisions/DEC-0004-mixed-project-fixture-skip-allowlist.md` filed.
- Pre-commit hook updated to add `tests/fixtures/mixed-project/` to `ALLOW_PREFIX`.

_Criterion 5 (`bash scripts/verify/P0-W1-F14.sh`) is now testable. ✓_

---

### OK (all checks)

| Check | Result |
|-------|--------|
| 1. Stub detection (`todo!`, `unimplemented!`, `pass`-only, bare returns) | CLEAN — none found in any changed file |
| 2. Mocked critical deps (Serena, LSP, SQLite, LanceDB, Docker) | CLEAN — no mocks in `tests/`, `crates/`, `adapters/` |
| 3. Skipped / ignored tests | CLEAN — `#[ignore]` on `rust_project_loads` has `// SLOW-TEST:` marker (DEC-0003); mixed-project skips exempt by DEC-0004 |
| 4. Weak assertions | CLEAN (UCIL code) — W3 above covers the fixture smoke-test; no weak assertions in UCIL's own suite |
| 5. Hallucinated paths | CLEAN — `ucil_core::schema_migration::SCHEMA_VERSION` confirmed at `crates/ucil-core/src/schema_migration.rs:18`; `clap::Args`, `serde::Serialize`, `tempfile` all in Cargo.toml |
| 6. Feature coverage | CLEAN — F03: `init.rs` unit tests + `P0-W1-F03.sh`; F11: `fixture.rs::rust_project_loads`; F12: `test_fixture_valid.py` + `P0-W1-F12.sh`; F13: `typescript-project.test.ts`; F14: `P0-W1-F14.sh` |
| 7. Commit hygiene | CLEAN (subjects) — W2 above covers missing DEC-0001 citation in F12/F13 bodies; all commits have Conventional Commits subjects; feat/fix commits have `Phase:`, `Feature:`, `Work-order:` trailers |
| 8. Public API docs | CLEAN — `detect_languages()` has full rustdoc with `# Examples` block; `InitArgs`, `Language`, `run()` all have `///` doc comments; no undocumented `pub` items added |
| `feature-list.json` frozen fields | CLEAN — only `last_verified_by`, `attempts` changed; `id`, `description`, `acceptance_tests`, `dependencies`, `passes` all unmodified by executor |
| `passes=true` set by executor | CLEAN — all five features remain `"passes": false` |
| `0003-ready-for-review.md` marker | PRESENT — written as `ucil-build/work-orders/0003-ready-for-review.md` (commit `d264d86`) |

---

## Summary

All three retry-1 blockers are resolved. F12 (python-project), F13 (typescript-project),
and F14 (mixed-project) are now fully implemented with real, non-trivial code, passing
tests, and verify scripts. The three remaining items above are warnings only and do not
need to be fixed before the verifier is spawned.

**Verifier may be spawned.**
