# Critic Report: WO-0080

**Critic session**: crt-wo-0080-2026-05-08T1340Z
**Branch**: feat/WO-0080-ruff-quality-plugin-manifest
**Ref**: 66e1780ddb9ba9f9f397a410375552b419db00c2
**Merge base**: 560fe82e24f9b2de2f8d4128744477bd6c825946
**Verdict**: CLEAN

## Findings

### Blockers (must fix before verifier)

_(none)_

### Warnings (should fix before gate, non-blocking)

1. **Commit size soft-cap (599fbd9 — `feat(scripts-verify): replace P3-W11-F03 TODO-stub with end-to-end ruff smoke`)** — diff is 478 lines, well over the ~50-line soft target in `.claude/rules/commit-style.md`. The commit replaces a 5-line TODO-stub with the canonical-shape verify script (mirrors `scripts/verify/P3-W11-F02.sh` + `scripts/verify/P3-W11-F04.sh`); the python smoke driver is a coherent unit and splitting it would yield non-runnable intermediate states. DEC-0005 module-coherence carve-out arguably applies. Non-blocking — the WO scope_in #18 cites DEC-0005 explicitly for the additive test commit (~106 LOC), and the verify-script replacement falls in the same coherence class.

2. **Commit size soft-cap (641e33c — `feat(plugins-quality): land plugins/quality/ruff/plugin.toml ...`)** — diff is 184 lines for a single TOML file with extensive top-of-file rustdoc (manifest body itself is ~30 lines; the rest is mandated rustdoc per WO scope_in #5 and §"Live tool name capture" + §"PyPI vs serverInfo.name" documentation requirements). Coherent unit; splitting would harm reviewability. Non-blocking.

3. **Commit size soft-cap (66e1780 — `chore(rfr): WO-0080 ready-for-review marker`)** — diff is 230 lines for the RFR document. Documentation-only file that the harness consumes as a single artifact; splitting is not meaningful. Non-blocking.

### OK

- **Stub detection (CLEAN)**: No `todo!()`, `unimplemented!()`, `raise NotImplementedError`, or trivial-default returns introduced. The three `+pass` matches in the diff (`scripts/verify/P3-W11-F03.sh`) are inside `try/except Exception: pass` cleanup blocks for `proc.stdin.close()` / `proc.kill()` / `smoke_log.close()` (lines visible in the inline-Python heredoc) — acceptable Python idiom for best-effort resource cleanup, not implementation stubs. See `scripts/verify/P3-W11-F03.sh` (the three `pass` occurrences in the python smoke driver's `finally:` block).

- **Mocked critical collaborators (CLEAN)**: No `mock|fake|stub|MagicMock|Mockall` referencing `serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker` in any added test or production-side file. The integration test spawns `uvx mcp-server-analyzer@0.1.2` as a real subprocess via `PluginManager::health_check_with_timeout` — full real-IO path.

- **Skipped/ignored tests (CLEAN)**: No `#[ignore]`, `.skip()`, `it.skip(`, `xit(`, `@pytest.mark.skip`, or `@pytest.mark.xfail` introduced. The `if skip_via_env() { return; }` guard at `crates/ucil-daemon/tests/g7_plugin_manifests.rs:454` and the `[SKIP]` echo in `scripts/verify/P3-W11-F03.sh` are allowed per WO scope_in #6 + WO-0076 precedent (single-gate `UCIL_SKIP_QUALITY_PLUGIN_E2E` env short-circuit).

- **Weak assertions (CLEAN)**: No `assert!(true)`, `assert_eq!(true, true)`, `expect(true).toBe(true)`, `assert True`, or `assert 1` introduced. New test asserts:
  - `crates/ucil-daemon/tests/g7_plugin_manifests.rs:464` — `manifest.plugin.name == "ruff"`.
  - `crates/ucil-daemon/tests/g7_plugin_manifests.rs:465` — `transport.kind == "stdio"`.
  - `crates/ucil-daemon/tests/g7_plugin_manifests.rs:467` — `!provides.is_empty()`.
  - `crates/ucil-daemon/tests/g7_plugin_manifests.rs:471` — all capabilities under `quality.*` namespace.
  - `crates/ucil-daemon/tests/g7_plugin_manifests.rs:499` — `health.name == "ruff"`.
  - `crates/ucil-daemon/tests/g7_plugin_manifests.rs:500` — `health.status == HealthStatus::Ok`.
  - `crates/ucil-daemon/tests/g7_plugin_manifests.rs:506` — `!health.tools.is_empty()`.
  - `crates/ucil-daemon/tests/g7_plugin_manifests.rs:526` — `(SA3) ruff-check` tool presence in advertised set.

- **Hallucinated paths/imports (CLEAN)**: All symbols used in the new test resolve to module-level definitions already present in `crates/ucil-daemon/tests/g7_plugin_manifests.rs` (lines 79, 92, 97, 119, 193, 224 per the existing two test peers): `PluginManifest`, `PluginManager`, `HealthStatus`, `FIRST_RUN_TIMEOUT_MS`, `repo_root()`, `skip_via_env()`, `copy_fixture_to_tmpdir()`, `assert_dir_exists()`. Fixture `tests/fixtures/python-project` exists on `main` (verified `pyproject.toml`, `src/`, `tests/` present at HEAD).

- **Feature coverage (CLEAN)**: P3-W11-F03 (the WO's sole feature) is exercised by:
  - `crates/ucil-daemon/tests/g7_plugin_manifests.rs:452` — new `#[tokio::test] async fn ruff_manifest_health_check` (cargo-test path; tools/list handshake).
  - `scripts/verify/P3-W11-F03.sh` — replaces TODO-stub with end-to-end smoke (cargo-test wrapper + tools/list assertion + tools/call ruff-check on a fabricated `bad.py` deliberate-violation file with rule/line/file structured-finding assertion per F03 spec).

- **Commit hygiene (CLEAN)**:
  - All 5 commits use Conventional Commits headers: `feat(plugins-quality):`, `feat(devtools):`, `test(daemon):`, `feat(scripts-verify):`, `chore(rfr):` — all under <=70 chars.
  - All 5 commits carry the mandated trailers `Phase: 3` / `Feature: P3-W11-F03` / `Work-order: WO-0080`.
  - Zero merge commits on the feat branch (`git log feat/WO-0080-ruff-quality-plugin-manifest ^main --merges` returns empty — AC11 satisfied).
  - No `--amend` after push, no force-push markers in the reflog of the branch tip.
  - Three commits exceed the ~50-line soft target — see Warnings 1-3 above; non-blocking.

- **Forbidden-paths (CLEAN)**: Diff touches only `crates/ucil-daemon/tests/g7_plugin_manifests.rs` (additive only, zero deletions confirmed via `git diff main -- ... | grep '^-' | grep -v '^---' | wc -l` → 0), `plugins/quality/ruff/plugin.toml` (NEW), `scripts/devtools/install-ruff-mcp.sh` (NEW), `scripts/verify/P3-W11-F03.sh` (replacing TODO-stub), `ucil-build/work-orders/0080-ready-for-review.md` (NEW). No edits to `ucil-build/feature-list.json`, master plan, `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`, peer test files (`plugin_manifests.rs`, `g3_…`, `g4_…`, `g5_…`, `g6_…`, `g8_…`), peer verify scripts (`P3-W11-F02.sh`, `P3-W11-F04.sh`, `P3-W11-F08.sh`), or peer plugin directories.

- **Pinned-immutable-ref (CLEAN)**: `plugins/quality/ruff/plugin.toml` pins `mcp-server-analyzer@0.1.2`. No floating refs (`main|latest|master|head|develop|dev|nightly`) anywhere in the manifest. Per master-plan §13.

- **No new `unsafe` blocks (CLEAN)**: Per WO scope_in #17 + #34. Diff hits on the word `unsafe` are confined to doc-comment cross-references in the new test rustdoc + manifest rustdoc + RFR acceptance-table cell — no new `unsafe { ... }` blocks introduced. Verified via `git diff ... | grep -E '^\+.*unsafe '` returning only doc-comment lines and the AC-table cell that reads "OK (no NEW unsafe block)".

- **No `std::process::Command` (CLEAN)**: New test uses no subprocess machinery directly — the `PluginManager::health_check_with_timeout` helper handles spawn. Verified `! grep -qE 'std::process::Command' crates/ucil-daemon/tests/g7_plugin_manifests.rs` returns empty for the entire file (existing tests already use tokio::process per WO-0076 carve-out).

- **Doc + public API (CLEAN)**: No new `pub` items introduced (test-only Rust addition + TOML manifest + scripts). The new `ruff_manifest_health_check` test carries a 28-line rustdoc covering DEC-0023 Tier-1 selection, PyPI-vs-serverInfo.name asymmetry, self-resolving-server classification, and scope_in cross-references.

- **Cargo selector uniqueness (CLEAN)**: `git show feat/WO-0080-ruff-quality-plugin-manifest:crates/ucil-daemon/tests/g7_plugin_manifests.rs | grep -c 'async fn ruff_manifest_health_check'` → 1. The selector `cargo test -p ucil-daemon --test g7_plugin_manifests g7_plugin_manifests::ruff_manifest_health_check` resolves uniquely (substring-match).

- **Mutation contract (CLEAN)**: RFR §"Mutation contract" (`ucil-build/work-orders/0080-ready-for-review.md:132-167`) carries both M1 (transport.command poison) and M2 (expected-tool-name regression) contracts in the canonical 7-WO shape: file + patch + targeted selector + expected panic + restore command + pre-mutation md5 path + post-restore verification — mirroring WO-0067/0068/0069/0070/0072/0077/0079.

- **Feature attempts counter (CLEAN)**: P3-W11-F03 has `attempts=0` per `ucil-build/feature-list.json`. WO scope §plan_summary advertises this as the first WO attempt (DEC-0020 deferral consumed no executor cycles). No 3-strike or attempts>=10 escalation territory.

## Summary

5 commits, 4 production files (1 modified, 3 new) + 1 RFR doc, 106 additive lines on the integration-test peer file with zero deletions. Tier-1 install path selected per DEC-0023 §Decision point 2(Tier 1) — `mcp-server-analyzer@0.1.2` on PyPI (Anselmoo's wrapper combining Ruff + Vulture). M1/M2 mutation contracts reversibly verified with md5-clean restoration per the 7-WO mature pattern. Mirrors the WO-0079 (graphiti G3 solo-feature deferral-revival) shape verbatim, retargeted from G3-knowledge to G7-quality namespace.

Verdict: **CLEAN** — proceed to verifier.
