# Critic Report: WO-0041

**Critic session**: crt-WO-0041-20260419
**Branch**: feat/WO-0041-mcp-stdio-repo-kg-bootstrap
**Ref**: cfb95fa84606043d1e2e09b785fe1dd1de5093ba
**Merge base**: main
**Verdict**: CLEAN

## Scope of review

6 commits reviewed (`main..feat/WO-0041-mcp-stdio-repo-kg-bootstrap`):

| SHA       | Subject                                                                         | Size (LOC) |
|-----------|---------------------------------------------------------------------------------|------------|
| aa6b977   | build(daemon): promote tempfile to [dependencies] for --repo code path          | +6         |
| 2342b47   | feat(daemon): add --repo arg parser to mcp subcommand in main.rs                | +78        |
| 098fead   | feat(daemon): add walk_supported_source_files helper in main.rs                 | +189 / -3  |
| 6531ad3   | feat(daemon): bootstrap KnowledgeGraph + IngestPipeline when --repo present     | +39 / -6   |
| d870d50   | test(daemon): e2e integration — spawn binary with --repo, assert real find_definition | +135 |
| cfb95fa   | chore(work-order): WO-0041 ready-for-review marker                              | +79 (md)   |

Changed files: `crates/ucil-daemon/Cargo.toml`, `crates/ucil-daemon/src/main.rs`, `crates/ucil-daemon/tests/e2e_mcp_with_kg.rs`, `ucil-build/work-orders/0041-ready-for-review.md`. No `forbidden_paths` entries touched.

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

1. **Commit size over soft target** (`~50 LOC/commit`):
   - `098fead` introduces `walk_supported_source_files` at +189 LOC. The walker body itself is ~80 LOC; the remainder is the embedded `#[cfg(test)] mod tests` block with 5 unit tests. A cleaner split would have been one commit for the helper + SKIP_DIRS/SUPPORTED_EXTENSIONS consts and a second `test(daemon):` commit for the unit tests. Within `DEC-0005` spirit (module-introduction test bundling) but noticeably above cadence.
   - `2342b47` is +78 LOC for the arg parser + 5 unit tests — same shape, smaller magnitude.
   - `d870d50` at +135 LOC is the test-introduction commit and is explicitly permitted to exceed the soft target under `DEC-0005`. Not a warning — cited here for completeness.

   Recommendation: no rewrite required (branch is already pushed and `--amend` after push is forbidden). Flagged for executor style-awareness on the next WO.

### OK

- **Stub detection** (`crates/ucil-daemon/src/main.rs`, `crates/ucil-daemon/tests/e2e_mcp_with_kg.rs`): no `todo!()`, `unimplemented!()`, `raise NotImplementedError`, or `pass`-only bodies introduced. The `parse_repo_arg` helper's `None` return is the documented "no `--repo` provided" signal, not a stub. Per-file `ingest_file` failures in `main.rs:172-177` are `tracing::warn!`-logged and swallowed intentionally per the work-order's resilience requirement — not a silent-stub pattern.
- **Mocked critical collaborators**: no mocks of Serena, LSP servers, SQLite, LanceDB, or Docker in `tests/e2e_mcp_with_kg.rs`. The integration test spawns the real compiled binary via `env!("CARGO_BIN_EXE_ucil-daemon")` and exercises a real SQLite `KnowledgeGraph` at a `TempDir`-owned `knowledge.db`. In-process unit tests in `main.rs:212-325` use `tempfile::tempdir` on the filesystem — no mocked FS.
- **Skipped / ignored tests**: no `#[ignore]`, `.skip()`, `xfail`, `xit(`, `@pytest.mark.skip`, or commented-out assertions introduced.
- **Weak assertions**: every new `assert!` / `assert_eq!` compares against meaningful values (parsed arg content, sorted file lists, JSON-RPC response field values). No `assert!(true)` / `expect(true).toBe(true)` variants.
- **Hallucinated paths / imports**:
  - `use ucil_core::KnowledgeGraph` at `main.rs:12` → resolves via `crates/ucil-core/src/lib.rs:30` (`pub use knowledge_graph::{KnowledgeGraph, KnowledgeGraphError};`).
  - `use ucil_daemon::executor::IngestPipeline` at `main.rs:13` → resolves via `crates/ucil-daemon/src/lib.rs:79` (`pub mod executor;`) and `executor.rs:359` (`pub struct IngestPipeline`).
  - `ucil_daemon::server::McpServer::with_knowledge_graph` call site at `main.rs:183` → matches signature at `crates/ucil-daemon/src/server.rs:396` (`pub fn with_knowledge_graph(kg: Arc<Mutex<KnowledgeGraph>>) -> Self`).
  - `ucil_daemon::server::McpServer::new()` fallback at `main.rs:188` → matches `server.rs:366`.
  - `tempfile::TempDir::new` / `tempfile::tempdir` → `tempfile.workspace = true` is present under `[dependencies]` at `Cargo.toml:65` (promoted in `aa6b977`).
  - Fixture `tests/fixtures/rust-project/src/util.rs` exists with `pub fn evaluate(expr: &Expr)` at line 128 — matches the e2e test's `file_path.ends_with("src/util.rs")` and positive-`start_line` assertions.
  - `cargo check -p ucil-daemon --tests` on the branch head compiles cleanly (verified locally).
- **Feature coverage**: WO-0041's `feature_ids: []` is the documented gate-closure pattern (matches WO-0007 / WO-0025 / WO-0040 precedent per `lessons_applied`). No feature requires a named test. The WO's acceptance-test burden is carried by the new `e2e_mcp_stdio_with_repo_returns_real_find_definition` test plus the 8 new unit tests in `main.rs`.
- **Commit hygiene**:
  - All 6 commits follow Conventional Commits (`build` / `feat` / `test` / `chore`).
  - All carry `Phase: 1`, `Feature: (none — gate-closure)`, `Work-order: WO-0041` trailers and the expected `Co-Authored-By:` line.
  - No `--amend` after push, no force-push signals in `git log --format='%H %P'`.
  - Scope discipline: no commit touches a path in `forbidden_paths` (verified against `scope_out` list — `server.rs`, `executor.rs`, `lib.rs`, `lifecycle.rs`, `.claude/settings.json`, `tests/fixtures/**`, cross-crate sources all untouched).
- **Doc + public API**: all new module-level items in `main.rs` carry rustdoc:
  - `SUPPORTED_EXTENSIONS` const (`main.rs:13-17`), `SKIP_DIRS` const (`main.rs:22-33`), `WALK_FILE_CAP` const (`main.rs:38-41`): documented.
  - `parse_repo_arg` fn (`main.rs:48-60`): documented with semantics of `--repo` / `--repo=<VAL>` / unknown-flag passthrough.
  - `walk_supported_source_files` fn (`main.rs:70-87`): documented including skip rules, ordering guarantee, and the cap.
  - Crate-root `//!` extended per plan with a two-sentence `--repo` description that uses plain backticks (no intra-doc-link hazard — matches `WO-0038/0039` lesson).
  - The private helper `visit` (`main.rs:89-128`) is undocumented but is not `pub`, so rustdoc requirements do not apply.

## Summary

The diff cleanly implements the three changes in the WO's `plan_summary`: the hand-rolled `--repo` arg parser, the `std`-only recursive source-file walker with SKIP_DIRS / SUPPORTED_EXTENSIONS filters and a 50 000-file cap, and the KG + IngestPipeline bootstrap that attaches to `McpServer::with_knowledge_graph` before `serve`. The stub-path fallback for absent / invalid `--repo` is preserved, keeping the WO-0040 regression guard (`e2e_mcp_stdio`) intact. The new e2e integration test (`e2e_mcp_with_kg.rs`) spawns the real compiled binary, drives the real JSON-RPC handshake over real stdio pipes, and asserts on the real-data envelope (`_meta.source == "tree-sitter+kg"`, `_meta.found == true`, etc.) — proving the wiring end-to-end with no in-process shortcut.

**Verifier may proceed.**
