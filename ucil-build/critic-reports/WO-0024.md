# Critic Report: WO-0024

**Critic session**: crt-WO-0024-20260418
**Work-order**: `ucil-build/work-orders/0024-kg-crud-and-hot-staging.json`
**Branch**: `feat/WO-0024-kg-crud-and-hot-staging`
**HEAD**: `7aacaa4da7707e09919130c66a946cd7e34ee9bf`
**Merge base**: `main` (`823700a`)
**Commits under review**: 10 (`d13cfc9` → `7aacaa4`)
**Features claimed**: `P1-W4-F02` (CRUD + bi-temporal), `P1-W4-F08` (hot staging)
**Verdict**: **CLEAN**

---

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

1. **Large commit — `418bf98`**: `feat(core): knowledge_graph: upsert_entity + get_entity_by_qualified_name + list_entities_by_file + 3 module-root tests` is **403 insertions** in a single commit. The commit-plan in the WO targeted 140 lines for this step (2.88x overrun). The 200-line "no good reason" soft ceiling in `.claude/agents/critic.md` §Check 7 is exceeded. That said:
    * It is not a "kitchen-sink" commit — all additions live in one module (`crates/ucil-core/src/knowledge_graph.rs`) and concern one coherent increment (entity CRUD + three tests + two internal helpers `entity_from_row` / `absent_to_none`).
    * Substantial portion of the 403 lines is rustdoc + test bodies (mandatory for pedantic + nursery).
    * Splitting into two commits (e.g. read helpers; upsert + tests) would have helped reviewability.
    * **Recommendation**: tolerate for this WO; flag in docs-writer post-mortem so the next planner splits more aggressively.

### OK

- ✅ **No stubs** — `knowledge_graph.rs` contains 0 occurrences of `todo!()`, `unimplemented!()`, `NotImplementedError`, bare-literal returns, or trivial-default returns. Every new method has a real `rusqlite` body routed through `execute_in_transaction` or `self.conn.prepare`.
- ✅ **No mocked critical deps** — all 8 tests open a **real tempfile-backed SQLite db** via `tempfile::TempDir::new()` + `KnowledgeGraph::open(...)` (lines 1208, 1324, 1396, 1454, 1534, 1630, 1767, 1869). `grep -iE 'mock|fake|stub|MagicMock|Mockall'` hits are three comments explicitly affirming _absence_ of mocks (lines 1179, 1760, 1861) — no instances of the patterns themselves.
- ✅ **No skipped / ignored tests** — 0 matches for `#[ignore]`, `.skip(`, `xfail`, `xit`, `pytest.mark.skip` across the diff.
- ✅ **No weak assertions** — 0 matches for `assert!(true)`, `assert_eq!(true, true)`, `expect(true).toBe(true)`. Every test asserts specific field values, row counts, or monotonic invariants (e.g. `test_entity_unique_constraint_updates` asserts `access_count` strictly increases across repeat upserts at line 1520-1523; `test_wal_checkpoint_truncates` asserts the WAL file shrinks to 0 bytes at line 1926-1929).
- ✅ **No hallucinated imports** — `use chrono::{DateTime, Utc};` (line 50), `use rusqlite::{Connection, Transaction, TransactionBehavior};` (line 51), `use serde::{Deserialize, Serialize};` (line 52), `use thiserror::Error;` (line 53) all resolve. `cargo check -p ucil-core --tests` and `cargo clippy -p ucil-core --all-targets -- -D warnings` both complete cleanly against this branch. The `chrono` workspace dep is wired at `Cargo.toml:78` (default-features = false, features = ["clock", "serde", "std"]) and pulled into `crates/ucil-core/Cargo.toml:24`.
- ✅ **Feature coverage — P1-W4-F02 (CRUD + bi-temporal)**: 6 new tests reference the feature's behaviour:
    * `test_upsert_and_get_entity` (line 1321) — upsert + round-trip read.
    * `test_list_entities_by_file` (line 1393) — document-order read.
    * `test_entity_unique_constraint_updates` (line 1451) — ON CONFLICT DO UPDATE branch.
    * `test_upsert_relation_and_list` (line 1531) — relation CRUD + no-UNIQUE append semantics.
    * `test_bi_temporal_as_of` (line 1626) — half-open valid-time window lookup with 4 distinct query points.
    * `test_wal_checkpoint_truncates` (line 1865) — F02 supporting primitive.
- ✅ **Feature coverage — P1-W4-F08 (hot staging)**: the **frozen F08 selector** `knowledge_graph::test_hot_staging_writes` (line 1764) is present and exercises all three stagers (`stage_hot_observation`, `stage_hot_convention_signal`, `stage_hot_architecture_delta`) with per-table `COUNT(*)` read-backs (cross-table-INSERT-typo catcher).
- ✅ **Frozen acceptance selectors resolve** — `cargo nextest run -p ucil-core 'knowledge_graph::' --status-level=pass --hide-progress-bar` reports **8 tests run, 8 passed** (≥ 7 required for F02). `cargo nextest run -p ucil-core knowledge_graph::test_hot_staging_writes` reports **1 test run, 1 passed** (F08).
- ✅ **Module-root flat tests (DEC-0005)** — 0 matches for `^mod\s+tests\s*\{`. Every `fn test_*` sits at module root under `#[cfg(test)] #[test]` — matches the `test_schema_creation` precedent (line 1183) set by WO-0011.
- ✅ **Every writer routes through `execute_in_transaction`** — 0 matches for `self\.conn\.(execute|execute_batch)\(` against the module. The only `conn.execute_batch` calls are the three `PRAGMA` applications in `KnowledgeGraph::open` at lines 545-547 (pre-existing from WO-0011; not DML writes). The INIT_SQL DDL runs inside a `BEGIN IMMEDIATE` transaction at lines 579-581.
- ✅ **Commit hygiene — Conventional Commits** — every commit subject matches `<type>(<scope>): <subject>`. Type distribution: 1 × `build`, 7 × `feat`, 1 × `refactor`, 1 × `chore`.
- ✅ **Commit hygiene — trailers** — every commit carries `Phase: 1`, `Feature: P1-W4-F<02|08>`, `Work-order: WO-0024` (3 trailer lines per commit × 10 commits = 30 trailer lines confirmed).
- ✅ **No `--amend` after push / no force-push** — `git rev-parse HEAD` == `git rev-parse @{upstream}`; working tree clean; branch matches `origin/feat/WO-0024-kg-crud-and-hot-staging`.
- ✅ **Forbidden-path audit** — `git diff origin/main..HEAD -- crates/ucil-treesitter/ crates/ucil-daemon/ crates/ucil-lsp-diagnostics/ crates/ucil-embeddings/ crates/ucil-agents/ crates/ucil-cli/ adapters/ ml/` is 0 bytes. `ucil-build/feature-list.json`, the master plan, and `tests/fixtures/**` are untouched.
- ✅ **Doc + public API** — every new `pub` item has a rustdoc block:
    * `KnowledgeGraphError::InvalidTimestamp` (line 99-104).
    * `Entity` + 15 public fields (lines 110-176).
    * `Relation` + 10 public fields (lines 178-214).
    * `HotObservation` + 4 public fields (lines 216-241).
    * `WalCheckpointMode` + both variants + `as_sql()` (lines 243-276).
    * `KnowledgeGraph::upsert_entity` (line 633-656), `get_entity_by_qualified_name` (line 693-706), `list_entities_by_file` (line 736-746), `upsert_relation` (line 775-793), `list_relations_by_source` (line 822-830), `get_entity_as_of` (line 861-886), `stage_hot_observation` (line 915-931), `stage_hot_convention_signal` (line 956-975), `stage_hot_architecture_delta` (line 997-1016), `checkpoint_wal` (line 1047-1071).
    * Every method with a `Result` return carries an `# Errors` section.
- ✅ **Tracing spans** — every writer is `#[tracing::instrument(…, name = "ucil.<layer>.<op>")]` per master-plan §15.2: `ucil.core.kg.upsert_entity`, `ucil.core.kg.upsert_relation`, `ucil.core.kg.get_entity_as_of`, `ucil.core.kg.stage_hot_observation`, `ucil.core.kg.stage_hot_convention_signal`, `ucil.core.kg.stage_hot_architecture_delta`, `ucil.core.kg.checkpoint_wal`.
- ✅ **INIT_SQL unchanged** — the `INIT_SQL` string constant at lines 290-485 is byte-for-byte identical to the WO-0011 delivery; the diff on that region is 0 lines. Confirmed via `git diff origin/main..feat/WO-0024-kg-crud-and-hot-staging -- crates/ucil-core/src/knowledge_graph.rs` inspection — all insertions are outside the DDL region.
- ✅ **Safe `format!` in `checkpoint_wal`** — the PRAGMA string uses `format!("PRAGMA wal_checkpoint({});", mode.as_sql())` (line 1080). `mode.as_sql()` returns one of two hard-coded `&'static str` values (`"PASSIVE"` / `"TRUNCATE"`) — no user-controlled input reaches the format, no SQL-injection surface. The comment at line 1076-1079 correctly documents why placeholder binding is not possible for PRAGMAs.
- ✅ **No `.unwrap()` / `.expect()` outside `#[cfg(test)]`** — all uses of `.expect(...)` land in the test bodies (lines 1208+). Production code uses `?` throughout.

---

## Verdict

**CLEAN** — the diff is production-quality. All eight acceptance selectors pass locally; no stubs; no mocks of critical collaborators; no hallucinated paths; feature coverage is solid for both P1-W4-F02 and the P1-W4-F08 frozen selector; commit hygiene is green; every new public item has rustdoc.

The sole Warning is the 403-line `upsert_entity` commit — tolerable for this WO (coherent single-module increment), recommend flagging in the post-mortem for planner guidance on future multi-method commits.

**Forward to verifier.**
