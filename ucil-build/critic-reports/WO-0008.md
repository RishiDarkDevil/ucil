# Critic Report: WO-0008

**Critic session**: crt-wo-0008
**Branch**: `feat/WO-0008-daemon-lifecycle-session-state`
**Ref reviewed**: `50f053d01ec9266e19988f3f36fc15907a5a74b8`
**Merge base**: `ff5431a3b7d6237576607d048050d2aec64ea4a8`
**Diff**: 4 files, +583/-2
  - `crates/ucil-daemon/src/lib.rs` (+5/-1)
  - `crates/ucil-daemon/src/lifecycle.rs` (+312, new)
  - `crates/ucil-daemon/src/session_manager.rs` (+193/-1)
  - `ucil-build/work-orders/0008-ready-for-review.md` (+73, new)
**Features claimed**: `P1-W3-F01` (process lifecycle), `P1-W4-F07` (session state tracking)

**Verdict**: CLEAN (with one non-blocking warning)

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

1. **Commit `55159c3` is 203 lines** — 3 lines over the critic-protocol 200-line ceiling.
   The commit introduces `crates/ucil-daemon/src/lifecycle.rs` as a single atomic module
   (types + impl + `#[cfg(test)] mod tests`), which is exactly the pattern already
   blessed by `ucil-build/decisions/DEC-0005-WO-0006-module-coherence-commits.md`
   ("single new `*.rs` file containing coherent type + impl + `#[cfg(test)] mod tests`
   may exceed the soft limit when splitting would produce non-compiling or dead-code
   intermediate states"). The executor's ready-for-review marker explicitly cites
   DEC-0005. Margin is tiny (1.5 %) and justification is pre-existing, so this is a
   warning rather than a blocker.

### OK

- **Stubs**: no `todo!()`, `unimplemented!()`, `NotImplementedError`, bare `pass`,
  or trivial `return None` / `Default::default()` stubs anywhere in
  `crates/ucil-daemon/src/lifecycle.rs` or `crates/ucil-daemon/src/session_manager.rs`.
  Every new public function has a real body that calls into real stdlib / tokio APIs.
- **Mocked critical collaborators**: none.
  `rg 'mock|fake|stub|MagicMock|Mockall'` against `crates/ucil-daemon/` returns zero
  hits. The session-manager tests shell out to real `git` via
  `tokio::process::Command`; the lifecycle PID-file tests hit the real filesystem via
  `tempfile::TempDir`. No Serena / LSP / SQLite / LanceDB / Docker touched at all
  (correctly — this WO is scoped to pure in-process state).
- **Skipped / ignored tests**: none.
  `rg '#\[ignore\]|\.skip\(|xit\(|@pytest\.mark\.skip|@pytest\.mark\.xfail'` against
  the diff returns zero hits. All 16 new/changed test functions are active.
- **Weak assertions**: none.
  `rg 'assert!\(true\)|assert_eq!\(true,\s*true\)'` returns zero hits. Every test
  body carries real semantic assertions (PID round-trip via `std::process::id()`,
  file existence before/after drop, BTreeSet dedup count, `Option::is_none()` after
  purge, etc.).
- **Hallucinated imports**: none.
  `lifecycle.rs` imports `std::io::Write`, `std::path::{Path, PathBuf}`,
  `thiserror::Error`, and `tokio::signal::unix::{signal, SignalKind}` (feature
  `signal` is part of `tokio = { features = ["full"] }` in the workspace
  `Cargo.toml:26`). `session_manager.rs` imports `serde`, `thiserror`, `tokio::sync`,
  `tokio::process`, `tokio::time`, `uuid` — all listed in
  `crates/ucil-daemon/Cargo.toml` or the workspace root. Test code uses
  `tempfile::TempDir` (dev-dependency, `crates/ucil-daemon/Cargo.toml:25`).
- **Feature coverage**:
  - `P1-W3-F01` — acceptance selector `-p ucil-daemon lifecycle::`. Covered by
    seven new tests under `lifecycle::tests::…` (lines 215-293 of
    `crates/ucil-daemon/src/lifecycle.rs`): `pid_file_write_creates_file_with_current_pid`,
    `pid_file_read_returns_written_pid`, `pid_file_drop_removes_file`,
    `pid_file_double_write_is_idempotent_or_errors_cleanly`,
    `pid_file_read_of_garbage_returns_stale_error`, `shutdown_reason_debug_is_readable`,
    `lifecycle_holds_pid_file_and_removes_on_drop`. Module-prefix selector matches
    the `tests::` sub-path (confirmed per WO-0006 learning).
  - `P1-W4-F07` — frozen exact selector
    `session_manager::test_session_state_tracking`. Covered by the standalone
    `#[cfg(test)] #[tokio::test] async fn test_session_state_tracking` at
    `crates/ucil-daemon/src/session_manager.rs:422` (module-level) — crucially
    placed BEFORE the inner `mod tests {` at line 491. This satisfies the exact-match
    selector and dodges the trap documented in escalation
    `20260415-1856-wo-WO-0006-attempts-exhausted` §Root cause 2. The test
    exercises all five required scenarios: `create_session`, two `record_call`s
    (asserts history length and tool names), three `add_file_to_context` calls with
    one duplicate (asserts BTreeSet dedup to 2), `set_inferred_domain` round-trip,
    and `set_ttl(1) + purge_expired(created_at + 2)` with explicit `removed == 1`
    and `get_session(...).is_none()` checks.
- **Commit hygiene** — green on every axis:
  - Seven commits on branch, all subject-line Conventional Commits:
    `feat(daemon)` ×3, `test(daemon)` ×1, `chore(daemon)` ×1,
    `chore(work-order)` ×1 plus the ready-for-review marker commit.
  - All seven carry `Phase: 1`, `Feature: P1-W3-F01` or `P1-W4-F07` (the
    marker commit lists both), and `Work-order: WO-0008` trailers.
  - Sizes: `55159c3` 203L (see warning above), `9badca1` 119L, `18230b0` 45L,
    `99e3919` 71L, `63b50ed` 78L, `8b7b1fd` 4L, `50f053d` 73L — six of seven
    under the 150L soft target.
  - No `--amend`, no force-push, no `--no-verify` — `git log --format='%h %s'`
    shows a clean forward-only sequence.
- **Doc + public API**:
  - Every new `pub` item in `lifecycle.rs` has a rustdoc:
    `PidFileError` (+per-variant + per-field), `PidFile`, `PidFile::write`
    (with `# Errors`), `PidFile::read` (with `# Errors`), `PidFile::path`,
    `ShutdownReason` (+per-variant), `wait_for_shutdown` (with `# Errors`),
    `Lifecycle`, `Lifecycle::new`, `Lifecycle::run_until_shutdown`.
  - Every new `pub` item in `session_manager.rs` has a rustdoc:
    `CallRecord` (+per-field), `DEFAULT_TTL_SECS`, the four new `SessionInfo`
    fields (`call_history`, `inferred_domain`, `files_in_context`,
    `expires_at`), and the five new `SessionManager` methods (`record_call`,
    `add_file_to_context`, `set_inferred_domain`, `set_ttl`, `purge_expired`).
  - Module-level docstrings on both files; `lifecycle.rs` includes a
    working `# Examples` block (`no_run`).
  - `#![allow(clippy::module_name_repetitions)]` is justified in a code
    comment in both files.
- **Scope discipline**: the executor honored every `scope_out` entry.
  `git diff --name-only ff5431a..50f053d` shows zero changes to
  `crates/ucil-daemon/src/main.rs`, `crates/ucil-treesitter/**`,
  `crates/ucil-daemon/src/storage.rs`, `tests/fixtures/**`, or
  `ucil-build/feature-list.json`. No `notify`, `salsa`, plugin.toml, or
  SQLite-schema work slipped in.
- **Frozen-selector placement check**: `fn test_session_state_tracking` at
  line 422, `mod tests {` at line 491 — 422 < 491 ✅ (matches
  `acceptance_criteria[5]` of the work-order).

## Recommendation

Proceed to verifier. The single warning (commit 55159c3 at 203 lines) is covered
by DEC-0005 and does not warrant rework; the margin is 3 lines and the executor
pre-acknowledged the ADR in the ready-for-review marker.
