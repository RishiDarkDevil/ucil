# Critic Report: WO-0035

**Critic session**: crt-wo-0035-20260418
**Branch**: `feat/WO-0035-search-code-tool`
**Head commit**: `0ce203ec04ea135146498e3c08abbca282ab5d67` (`chore(wo): WO-0035 ready-for-review marker`)
**Merge base**: `cf559004` (`chore(planner): WO-0035 search_code MCP tool + DEC-0009`)
**Work-order**: `ucil-build/work-orders/0035-search-code-tool.json`
**Feature(s)**: `P1-W5-F09` — `search_code` MCP tool
**Verdict**: **CLEAN**

## Scope of review

True diff (merge-base → branch head) — 8 files, +2095/-12:

```
 Cargo.lock                                      |  108 +++
 Cargo.toml                                      |   21 +
 crates/ucil-core/src/knowledge_graph.rs         |  254 +++
 crates/ucil-daemon/Cargo.toml                   |    7 +
 crates/ucil-daemon/src/lib.rs                   |    1 +
 crates/ucil-daemon/src/server.rs                | 1170 ++
 crates/ucil-daemon/src/text_search.rs           |  370 +++
 ucil-build/work-orders/0035-ready-for-review.md |  176 ++
```

Note: `git diff main..feat/WO-0035-search-code-tool --stat` *looks* like
it deletes `scripts/gate-check.sh`, `scripts/run-integration-tester.sh`,
and three `scripts/verify/*.sh` files (471 LoC). Those are NOT deletions
introduced by this WO — they are branch-age artefacts: main advanced
five harness commits (`5173610`, `adc734f`, `119bb6d`, `1160f52`,
`52d9bfe`) after the feature branch was cut, and a two-dot range
naturally shows "files present on one side but not the other" as
phantom deletes. No harness path appears in `git diff cf559004..HEAD`,
so this is a merge-ordering concern (verifier will need to rebase or
trivially merge), **not** a forbidden-path breach.

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

1. **Commit size exceeds the ≤50-LoC soft target on the two main
   commits** — CLAUDE.md → commit-style.md flags ~50 LoC/commit as a
   soft target. Here:
   - `1aef49a feat(daemon): wire search_code MCP tool (P1-W5-F09, DEC-0009)`
     — 3 files, **+860/-12** (dispatch branch + `handle_search_code` +
     parse/clamp/cwd helpers + `SearchCodeResult` + `merge_search_results`
     + helper rustdoc + 6 `text_search` unit tests in one commit).
   - `5e17349 test(daemon): add search_code acceptance + 5 negative tests (WO-0035)`
     — 1 file, **+497**.

   Soft target, not a hard rule. Bundling keeps the implementation
   compilable at every intermediate commit, which matters more than the
   target. Flag only.

2. **Feature branch is 5 commits behind `main`** (`cf559004..52d9bfe`).
   None of the late-main commits touch files that this WO edits, and
   none of WO-0035's files live under `scripts/verify/**` or
   `scripts/gate-check.sh`, so the merge should be a trivial
   fast-forward-with-unrelated-histories. Verifier/merge step must
   confirm — not a critic blocker.

3. **`expect()` used inside `#[cfg(test)]` blocks only** — file and
   Vec access inside acceptance tests uses `.expect("…")` with
   explanatory messages. This is explicitly allowed by
   `.claude/rules/rust-style.md` ("Never `.unwrap()` / `.expect()`
   outside `#[cfg(test)]`") and every `.expect` I checked sits under a
   `#[cfg(test)]` gate. Verified, no action.

### OK

- **Stub detection (check 1).** Zero hits for `todo!()`,
  `unimplemented!()`, `NotImplementedError`, `raise
  NotImplementedError`, or bare-`pass` bodies across the three changed
  `.rs` files (`server.rs`, `text_search.rs`, `knowledge_graph.rs`).
  Trivial-default-return scan clean — `Ok(vec![])` on the KG empty
  path is a documented spec behaviour (scope_in bullet 2:
  "never `Err` on empty set").

- **Mocked critical deps (check 2).** Zero hits in the diff for
  `mock`, `fake`, `Mockall`, or `MagicMock` patterns against the
  forbidden collaborators (Serena, LSP, SQLite, rusqlite, LanceDB,
  Docker). The only "mock" substrings in the diff are rustdoc
  commentary affirming the absence of mocks:
  - `crates/ucil-daemon/src/server.rs:1138` — "No mocks of `SQLite`,
    `ignore`, or `grep-searcher` — both halves run against real state…"
  - `crates/ucil-daemon/src/text_search.rs:1547` — module-level rustdoc
    citing the "no mocked critical deps" rule.
  Every acceptance path hits a real `KnowledgeGraph::open` on a
  `tempfile::TempDir`-backed SQLite file, and `text_search` runs the
  real `ignore::WalkBuilder` + `grep_searcher::Searcher` against real
  filesystem trees.

- **Skipped / ignored tests (check 3).** Zero hits for `#[ignore]`,
  `.skip(`, `xit`, `pytest.mark.skip`, `pytest.mark.xfail` in the
  changed files.

- **Weak assertions (check 4).** Zero hits for `assert!(true)`,
  `assert_eq!(true, true)`, `expect(true).toBe(true)`, or empty-body
  tests. Every test body carries concrete assertions against real
  data (e.g. `meta.get("symbol_match_count") == Some(1)`,
  `hits.iter().any(|p| p == "a.rs")`, `any_both` check for
  `source == "both"` merge).

- **Hallucinated paths / imports (check 5).**
  - `use grep_regex::RegexMatcherBuilder;` — `grep-regex = "0.1"`
    present in `Cargo.toml:[workspace.dependencies]` and `Cargo.lock`.
  - `use grep_searcher::{BinaryDetection, Searcher, SearcherBuilder, Sink, SinkMatch};`
    — `grep-searcher = "0.1"` present.
  - `use ignore::WalkBuilder;` — `ignore = "0.4"` present.
  - `use thiserror::Error;` — pre-existing workspace dep.
  - `use crate::text_search::{self, TextMatch, TextSearchError};` in
    `server.rs` — `pub(crate) mod text_search;` declared in
    `crates/ucil-daemon/src/lib.rs:69`.
  All 8 crates referenced by `crates/ucil-daemon/Cargo.toml`
  (`ignore`, `grep-searcher`, `grep-regex`, `grep-matcher`,
  `rusqlite`, `ucil-core`, `ucil-treesitter`, `tempfile`) resolve.

- **Feature coverage (check 6).** `P1-W5-F09` frozen selector
  `server::test_search_code_basic` present at module root
  (`crates/ucil-daemon/src/server.rs:2810`), **not** nested under
  `mod tests { … }` — satisfies DEC-0005 / DEC-0007 /  WO-0006
  test-selector rule. Coverage breakdown:
  - 1 acceptance test (`test_search_code_basic`).
  - 5 negative tests (`empty_query`, `no_kg_returns_stub`,
    `non_string_query`, `nonexistent_root`, `max_results_clamp`) —
    matches scope_in bullet 9 (i, ii, iii, iv, v).
  - 1 text-only regression (`test_search_code_tool_only_text_no_symbol`)
    — matches scope_in bullet 9 (vi).
  - 4 pure-function merge unit tests
    (`test_merge_search_results_{symbol_only,text_only,collision_flips_to_both,respects_max_results}`).
  - 6 `text_search::tests::*` unit tests covering .gitignore respect,
    line-number indexing, result cap, zero-cap, invalid-regex, and
    empty-result.
  - 6 `knowledge_graph.rs` unit tests on `search_entities_by_name`
    (exact, substring, qualified_name match, limit, ordering, empty).

- **Commit hygiene (check 7).** 9 commits:
  - All subjects match Conventional Commits
    (`build` / `feat` / `test` / `refactor` / `docs` / `chore`).
  - Every `feat` / `fix` / `refactor` / `test` / `docs` / `build`
    commit body carries the required trailers `Phase: 1`,
    `Feature: P1-W5-F09`, `Work-order: WO-0035`
    (verified via `git log --format=%b | grep -E 'Phase:|Feature:|Work-order:' | sort -u`).
  - Every commit carries
    `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>`.
  - No `--amend`, no force-push evidence in `reflog` spots of the
    remote branch.
  - Per-commit diff stats look incremental: build-deps → KG helper →
    KG tests → daemon wiring → merge tests → spec-conformance refactor
    → acceptance tests → clippy doc fix → ready-for-review marker.
    Cadence is linear; the two outliers are called out under Warning #1.

- **Doc + public API (check 8).** Every new public / `pub(crate)` /
  `pub struct`-field item carries rustdoc:
  - `SEARCH_CODE_DEFAULT_MAX_RESULTS` — `server.rs:87-92`.
  - `SEARCH_CODE_MAX_RESULTS` — `server.rs:95-101`.
  - `KnowledgeGraph::search_entities_by_name` + `# Errors` —
    `knowledge_graph.rs:926-945`.
  - `TextMatch` struct + `file_path` / `line_number` / `line_text`
    fields — `text_search.rs:35-48`.
  - `TextSearchError` + each variant — `text_search.rs:52-75`.
  - `text_search` fn + `# Behaviour` + `# Errors` —
    `text_search.rs:89-119`.
  - Internal helpers (`SearchCodeResult`, `SearchCodeArgs`,
    `SearchCodeReadError`, `handle_search_code`, `parse_search_code_args`,
    `read_symbol_matches`, `merge_search_results`, `search_code_response`,
    `clamp_max_results`, `cwd_or_dot`, `CollectSink`,
    `trim_line_terminator`) all carry rustdoc, satisfying scope_in
    bullet 10. Intra-doc links (e.g. `[`grep_regex::Error`]`,
    `[`SEARCH_CODE_MAX_RESULTS`]`) use typed paths.

- **Forbidden-path guard.** No files touched under
  `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`,
  `ucil-build/feature-list.json`, `ucil-build/feature-list.schema.json`,
  the master plan, or DEC-0001..DEC-0008.

- **DEC-0009 present.**
  `ucil-build/decisions/DEC-0009-search-code-in-process-ripgrep.md`
  exists on both `main` (seeded by planner as `cf559004`) and on the
  feature branch — rationale for the in-process `grep-*` + `ignore`
  decision. `acceptance_criteria` command #14 satisfied.

- **`ready-for-review.md` present.**
  `ucil-build/work-orders/0035-ready-for-review.md` — 176 lines, lists
  all 14 acceptance criteria as PASS, references the reality-check
  evidence (stashing the merge path empties the result vec ⇒ test
  FAILs; restoring ⇒ GREEN). `acceptance_criteria` command #13
  satisfied.

- **Phase-1 invariant #9 preserved.** `search_code` without an attached
  KG still returns the `_meta.not_yet_implemented: true` stub — the
  `test_search_code_tool_no_kg_returns_stub` test asserts it. The 22-tool
  catalog (`test_all_22_tools_registered`, `test_ceqp_params_on_all_tools`)
  and the existing real handlers (`test_find_definition_tool`,
  `test_get_conventions_tool`) all remain untouched on the happy path.

## Verdict: CLEAN

No blockers. Two soft-warnings noted (commit size, branch staleness)
that the verifier and merge step can decide to tolerate or enforce.
Acceptance-test wiring looks honest: both halves exercise real
dependencies (SQLite + `ignore` + `grep-searcher`) against real
filesystem state, the merge is a pure function with its own dedicated
unit tests, and the `source == "both"` invariant is asserted at the
wire level.

Suggest the verifier proceed with the fresh-session run of the full
`acceptance_criteria[]` list.
