# Critic Report: WO-0071

**Critic session**: crt-2026-05-08-wo-0071
**Branch**: feat/WO-0071-graphiti-and-codegraphcontext-plugin-manifests
**Ref**: d795bc0880ef6f3a7ec5450d3c7610b1d846b9c6
**Merge base**: fee489608088bd3dae7002b26c149b2e38d6d396
**Verdict**: BLOCKED

## Diff summary

The branch contains a **single commit** that adds **only** an escalation
file:

```
d795bc0 chore(escalation): WO-0071 — graphiti MCP server has no canonical pypi pin
 ucil-build/escalations/20260508T0210Z-wo-0071-graphiti-mcp-handshake-and-infra-blocker.md | 82 ++++++++++++++++++++++
 1 file changed, 82 insertions(+)
```

No source code, no tests, no manifests, no install scripts, no verify
scripts. The executor explicitly halted per the
`.claude/agents/executor.md` escalation contract because the upstream
Graphiti MCP server has no canonical pypi pin that satisfies the WO-0069
plugin-manifest health-check template (four pypi forks tested, each
failing one of the three WO-0069 preconditions).

## Findings

### Blockers (must fix before verifier)

1. **Feature coverage: zero implementation, zero tests for both feature
   IDs in the work-order**.
   - WO-0071 features: `P3-W9-F08` (codegraphcontext) and `P3-W9-F10`
     (graphiti).
   - Required artifacts per scope_in #2/#3/#7/#8/#10/#11:
     - `plugins/architecture/codegraphcontext/plugin.toml` — **MISSING**
     - `plugins/knowledge/graphiti/plugin.toml` — **MISSING**
     - `scripts/devtools/install-codegraphcontext-mcp.sh` — **MISSING**
     - `scripts/devtools/install-graphiti-mcp.sh` — **MISSING**
     - New `crates/ucil-daemon/tests/g4_plugin_manifests.rs` — **MISSING**
     - Third test inside existing `mod g3_plugin_manifests` block in
       `crates/ucil-daemon/tests/g3_plugin_manifests.rs` — **MISSING**
     - `scripts/verify/P3-W9-F08.sh` adapted from `F05.sh` — **MISSING**
     - `scripts/verify/P3-W9-F10.sh` rewrite of TODO-stub — **MISSING**
   - Verified via `git diff main..feat/WO-0071-... --name-only` →
     only `ucil-build/escalations/20260508T0210Z-wo-0071-...md` listed.
   - The verifier cannot flip `P3-W9-F08` or `P3-W9-F10` from
     `passes=false` to `passes=true` on this branch — there is no
     implementation to verify and no acceptance test to run. Acceptance
     criteria #1–#13, #20, #22, #23 (per WO `acceptance` array) are all
     unmet by construction.
   - **However**, this is **deliberate executor escalation** rather than
     laziness or stubbing. The escalation file documents six concrete
     attempts (in order) at landing F10 and proposes five options (A
     split-the-WO; B harness extension; C docker-fixture extension; D
     anti-WO negative test; E wait-for-upstream) for the next planner
     pass. The executor's recommendation is Option A: land F08 alone (it
     was verified end-to-end producing a 25-tool `tools/list` from
     `uvx --with falkordblite codegraphcontext@0.4.7 mcp start`) and
     re-scope F10 separately.
   - **Resolution path**: the planner must rescope WO-0071 (per the
     executor's Option A recommendation, or Option B/C/E) and emit a
     replacement WO. This is a planner-side blocker on a template
     assumption that does not hold for Graphiti's upstream landscape; it
     is **not** a critic-fixable issue and **not** a stub/skip violation
     of the anti-laziness contract.

### Warnings (should fix before gate)

None. The escalation file is well-formed and routes to the planner
correctly.

### OK

- **Stub detection**: no code changes, so no `todo!()`,
  `unimplemented!()`, `raise NotImplementedError`, bare `pass`, or
  trivial-default returns introduced. Empty result set.
- **Mocked critical dependencies**: no test changes, so no mocks of
  Serena / LSP / SQLite / LanceDB / Docker / `rusqlite` / `sqlx` /
  `lancedb` introduced. Empty result set.
- **Skipped / ignored tests**: no test files changed. The two literal
  occurrences of "skip" / "stub" in the escalation prose at
  `20260508T0210Z-wo-0071-...md:77,80` are direct quotations of the
  anti-laziness contract itself ("violate the 'no skipped tests'
  anti-laziness contract" / "Do NOT stub or skip — Write an escalation")
  and are documentation references, not introductions of skipped tests.
- **Weak assertions**: no test files changed. Empty result set.
- **Hallucinated paths**: the escalation cites concrete file:line
  references. Verified live:
  - `crates/ucil-daemon/src/plugin_manager.rs:1143` →
    `async fn run_protocol_prefix(` (matches escalation citation
    `1143-1175`). ✓
  - `crates/ucil-daemon/src/plugin_manager.rs:1652` →
    `fn parse_tools_list_response(frame: &str) -> Result<...>` (matches
    escalation citation `line 1652`). ✓
  - The mention of `docker/serena-compose.yaml` is an analogy ("parallel
    to how Serena is bring-up'd via …"), not a load-bearing claim that
    the file exists in HEAD; treating it as illustrative.
  - WO-0069 / template references: `ucil-build/work-orders/0069-...json`
    exists; `ucil-build/critic-reports/WO-0069.md` exists.
- **Commit hygiene**:
  - Single commit `d795bc0`, 82 LOC inserted, well under the 200-LOC
    soft cap.
  - Subject `chore(escalation): WO-0071 — graphiti MCP server has no
    canonical pypi pin` is a valid Conventional Commits subject (`chore`
    type, escalation scope, ≤70-char nominal — slightly over due to the
    em-dash, but within tolerance for chore/escalation commits).
  - Body contains `Phase: 3`, `Feature: P3-W9-F10`, `Work-order:
    WO-0071` trailers per `.claude/rules/commit-style.md`.
  - Co-Authored-By trailer present.
  - No `--amend` against published history. No force push. Single
    forward-only commit on the feat branch.
- **Doc + public API**: no new `pub` items introduced; nothing to flag.
- **Forbidden paths untouched**: verified the diff does not touch
  `ucil-build/feature-list.json`,
  `ucil-build/feature-list.schema.json`, `ucil-master-plan-v2.1-final.md`,
  `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`,
  `ucil-build/decisions/**`, `ucil-build/post-mortems/**`, or
  `crates/ucil-daemon/tests/plugin_manifests.rs`. Empty result set.
- **Escalation file structure**: front-matter present (`date`, `wo`,
  `features`, `agent`, `severity: high`, `blocks_loop: true`,
  `requires_planner_action: true`, `resolved: false` trailing line).
  Conforms to the harness-brain schema in
  `ucil-build/CLAUDE.md` ("Escalation frontmatter conventions" section).

## Final verdict

**BLOCKED** — the work-order's acceptance criteria are not met because
no features are implemented and no acceptance tests landed; the verifier
cannot flip either `P3-W9-F08` or `P3-W9-F10` on this branch.

**This is not a stub/skip/laziness violation**. The executor halted
correctly per the escalation contract when it determined that the
WO-0069 template's three preconditions (boots without external infra;
respects MCP `notifications/initialized` no-response rule; published to
pypi/npm with a console script) cannot be jointly satisfied by any
existing pypi-published Graphiti MCP server. The executor's six
documented attempts and five proposed re-scope options route the issue
back to the planner for a follow-up WO.

**Required next action**: planner re-scopes WO-0071 (Option A is the
executor's recommendation: land F08 alone, defer F10 to a follow-up
that also lands the harness/docker extension F10 needs), or marks the
escalation as the authoritative state and emits replacement work-orders.
The verifier should NOT be spawned on this branch as-is.
