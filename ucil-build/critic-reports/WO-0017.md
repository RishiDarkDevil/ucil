---
work_order: WO-0017
slug: treesitter-symbol-extraction
feature: P1-W2-F02
branch: feat/WO-0017-treesitter-symbol-extraction
head_commit: 98f91efb59a82dab4a31a81e48890d99cea2f216
merge_base: 0247246743142aa67b3e115eda2b9b41d1f530e8
critic_session: crt-WO-0017
verdict: CLEAN
---

# Critic Report: WO-0017 — treesitter-symbol-extraction

**Critic session**: `crt-WO-0017`
**Branch**: `feat/WO-0017-treesitter-symbol-extraction`
**Merge base**: `0247246` (main)
**HEAD**: `98f91ef` (`chore(WO-0017): mark ready-for-review — mutation-oracle survives rollback`)
**Diff scope**: 7 commits, 8 files changed, +1332 lines
**Verdict**: **CLEAN**

## Files touched

| File | Status | Purpose |
|------|--------|---------|
| `Cargo.toml` | modified (+6) | Workspace `streaming-iterator = "0.1"` dep (tree-sitter 0.25 `QueryMatches` ≠ `Iterator`) |
| `Cargo.lock` | modified (+2) | `streaming-iterator` lock entry |
| `crates/ucil-treesitter/Cargo.toml` | modified (+7) | `serde.workspace=true` + `streaming-iterator.workspace=true` |
| `crates/ucil-treesitter/src/lib.rs` | modified (+3) | `pub mod symbols;` + `pub use symbols::{…}` |
| `crates/ucil-treesitter/src/symbols.rs` | **new** (+1152) | `SymbolExtractor` + `SymbolKind` + `ExtractedSymbol` + 13 flat module-root tests |
| `crates/ucil-treesitter/tests/symbol_ext_integration.rs` | **new** (+74) | Integration-test compile-safeguard for the mutation oracle |
| `crates/ucil-treesitter/tests/symbols.rs` | **new** (+22) | Placeholder (doc-only) to satisfy the oracle's pathspec |
| `ucil-build/work-orders/0017-ready-for-review.md` | **new** (+66) | Executor marker |

Forbidden-path acceptance checks verified:
- `git diff 0247246..HEAD -- crates/ucil-treesitter/src/parser.rs` → 0 lines ✓
- `git diff 0247246..HEAD -- crates/ucil-core/` → 0 lines ✓
- No diff under `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`, `ucil-build/feature-list.json`, `ucil-build/feature-list.schema.json`, or `ucil-master-plan-v2.1-final.md` ✓
- `! grep -q 'ucil-daemon' crates/ucil-treesitter/Cargo.toml` ✓ (no daemon edge introduced)

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Oversized module-introduction commit** — `ba4b4bd` (`feat(treesitter): add SymbolExtractor + SymbolKind + ExtractedSymbol`, +1155 lines across 2 files). Exceeds the critic-protocol 200-line soft ceiling in `.claude/rules/commit-style.md` by ~5.8× and the WO's own `estimated_diff_lines: 520` by ~2.2×. The commit body justifies this by citing DEC-0005 (WO-0006's 605-line module-introduction precedent) and enumerates the three splitting constraints the DEC ratified (dead-code variants, test-less impl commits, deceptive empty-return intermediate states) which all apply here. The body explicitly invites a follow-up ADR ("Planners may follow up with DEC-0009 if ratifying this 2× extension is desired"). Acceptable under the DEC-0005 precedent but the critic flags it for the planner/verifier's awareness — a ratifying ADR is the clean path.

2. **Four commits deliberately omit the `Feature: P1-W2-F02` trailer** — `3f29a43`, `472e2ce`, `aceafc3`, `7d7314d` (all `test(treesitter): …` commits, +nn/–nn diffs). Rationale is documented in each body: `scripts/reality-check.sh`'s `git log --grep="Feature: $FEATURE_ID"` substring-matches trailer text that appears anywhere in a commit body — including inside backticks as prose — causing the oracle to stash files it was meant to preserve (`tests/symbols.rs` survival-test). The critic's commit-hygiene rule strictly targets `feat/fix/refactor` trailer-bearing commits; `test` commits are not in scope. Still worth flagging: the harness workaround should probably trigger a triage-Bucket-B fix to `scripts/reality-check.sh` (e.g. filter to commits whose *trailer line* matches, via `git interpret-trailers`) so future executors don't have to orchestrate this dance. Not a blocker.

3. **Executor landed → deleted → re-landed `tests/symbols.rs`** — sequence `3f29a43` (add, 77 lines) → `472e2ce` (remove, −77) → `7d7314d` (re-add as 22-line placeholder), with the real integration tests relocated to `tests/symbol_ext_integration.rs` in `aceafc3`. History is noisy but every step is explained in the commit body; no force-push / no `--amend`-after-push; the working branch is not yet merged to main and may be cleaned up if desired. Not a blocker.

4. **`tests/symbols.rs` is a doc-only placeholder** — 22 lines of `//!` comments, no test bodies. This is intentional (see warning 2): the file exists only so `scripts/reality-check.sh`'s `git stash push -- <unioned-pathspec-set>` does not fatal on a missing pathspec. The genuine integration coverage lives in `tests/symbol_ext_integration.rs` (2 tests, both exercised by the `symbols::` selector via `mod symbols { … }` wrapping). The critic accepts this as a harness-script workaround rather than a missing-test shortcut, but the construction is brittle — the same Bucket-B fix suggested in warning 2 would let this placeholder be deleted.

### OK

- **Stub detection**: `rg 'todo!\(|unimplemented!\(|NotImplementedError|raise NotImplementedError'` on the branch diff returns nothing. `rg 'return (None|Default::default\(\)|\{\}|true|false|0|\"\")\s*$'` on changed files returns nothing.
- **Mocked critical collaborators**: zero matches of `mock|fake|stub|MagicMock|Mockall` against serena/lsp/sqlite/lancedb/docker in `tests/` or `src/`. Two incidental matches of the word `fake` appear in rustdoc narrative (`symbols.rs:7` and `tests/symbols.rs:19`) — both refer to the oracle's `'0 passed' fake-green` terminology, not to mocking. Tests exercise real `tree-sitter` grammar crates (`tree_sitter_rust`, `tree_sitter_python`, `tree_sitter_typescript`, `tree_sitter_javascript`, `tree_sitter_go`) and the real `Parser::parse` entry-point — no grammar is mocked or stubbed.
- **Skipped / ignored tests**: zero matches of `#\[ignore\]|\.skip\(|xfail|xit\(|@pytest\.mark\.skip`.
- **Weak assertions**: zero matches of `assert!(true)` / `assert_eq!(true, true)` / `assert True`. 35 real `assert!` / `assert_eq!` / `assert_ne!` sites across `symbols.rs` + 9 across `symbol_ext_integration.rs`, most with descriptive failure messages.
- **Hallucinated paths**: all 5 source `use` statements resolve — `std::path::{Path, PathBuf}`, `serde::{Deserialize, Serialize}` (workspace dep), `streaming_iterator::StreamingIterator` (workspace dep added this WO), `tree_sitter::{Node, Query, QueryCursor, Tree}` (existing dep via `parser.rs`), `crate::parser::Language` (re-exported from WO-0005-frozen module). Integration test's `ucil_treesitter::{ExtractedSymbol, Language, Parser, SymbolExtractor, SymbolKind}` all match the `lib.rs` re-exports verified at `src/lib.rs:13-14`.
- **Feature coverage**: P1-W2-F02 (`SymbolExtractor`) is exercised by 13 module-root tests in `symbols.rs` + 2 integration tests in `symbol_ext_integration.rs`. All 15 cover distinct behaviours called out by the WO (Rust fn+struct+enum, Rust trait+method, Python def+class+method, TS function+class+interface, JS function_declaration, Go func+type, well-formed line ranges, Rust fn signature capture, Rust `///` doc-comment capture, empty-source for every `SUPPORTED_LANGUAGES`, no-panic for every `SUPPORTED_LANGUAGES`, fallback-language empty-vec contract, language-field population). Nextest selector `symbols::` resolves to 15 passing tests locally — WO acceptance required ≥ 10.
- **Commit hygiene (trailer-bearing commits)**: `9b2af88` (build), `ba4b4bd` (feat), carry `Phase: 1` + `Feature: P1-W2-F02` + `Work-order: WO-0017` trailers per `.claude/rules/commit-style.md`. Conventional-Commits subjects on all 7 commits (types: `build`, `feat`, `test`×4, `chore`). No `--amend` after push, no force-push detected (`git reflog` on the worktree branch is linear).
- **Doc + public API**: every crate-public item in `symbols.rs` has a rustdoc — `SymbolKind` (enum + 10 variants), `ExtractedSymbol` (struct + all 10 fields), `SymbolExtractor`, `SymbolExtractor::new`, `SymbolExtractor::extract`. Module-level rustdoc at `symbols.rs:1-71` cites master-plan §18 Phase 1 Week 2 and §2.1 Layer 1, lists primary vs fallback language support, and documents the signature + doc-comment heuristics. `#![deny(rustdoc::broken_intra_doc_links)]` inherited from `lib.rs`. `cargo doc -p ucil-treesitter --no-deps` is clean (no `^warning|^error` lines).
- **`unwrap`/`expect` scope check**: 5 occurrences — 1 inside a doctest (`symbols.rs:269`), 4 inside `#[cfg(test)]` bodies (`symbols.rs:883`, `1044`, `1048`, `1071`). None in shipped code paths. Complies with `.claude/rules/rust-style.md` "Never `.unwrap()` / `.expect()` outside `#[cfg(test)]`".
- **Tracing span**: `SymbolExtractor::extract` wraps the call in `#[tracing::instrument(name = "ucil.treesitter.extract_symbols", level = "debug", …)]` — matches master-plan §15.2 `ucil.<layer>.<op>` convention (`layer = treesitter`, `op = extract_symbols`).
- **Test placement (DEC-0005 invariant)**: all 13 unit tests live flat at module root — no `#[cfg(test)] mod tests { … }` wrapper appears in `symbols.rs` (`grep -qE '^mod\s+tests\s*\{' crates/ucil-treesitter/src/symbols.rs` → no match). Nextest paths resolve as `ucil_treesitter::symbols::<name>`, satisfying the frozen acceptance selector.
- **Local acceptance sweep**: `cargo build -p ucil-treesitter` ✓; `cargo clippy -p ucil-treesitter --all-targets -- -D warnings` ✓; `cargo nextest run -p ucil-treesitter 'symbols::'` → 15 passed, 0 failed; `cargo doc -p ucil-treesitter --no-deps` ✓.

## Verdict

**CLEAN** — ship to verifier.

The four warnings above are commit-hygiene / harness-workaround observations, not substantive defects. The module implements the full P1-W2-F02 scope (10 `SymbolKind` variants × 5 primary-language rulesets × signature + doc-comment heuristics × 15 tests) against real `tree-sitter` grammars with no mocks, no stubs, no skipped tests, no weak assertions, and no touched forbidden paths. The mutation-oracle dance (warnings 2-4) is defensive — it survives `scripts/reality-check.sh`'s stash-and-rollback sequence by ensuring at least one compile-only integration test survives the rollback and emits a genuine non-zero exit rather than a `'0 passed'` fake-green — but suggests a Bucket-B triage fix to `scripts/reality-check.sh` would let future WOs avoid the choreography.
