# Critic Report: WO-0095

**Critic session**: crt-WO-0095-incremental-computation-integration-test
**Branch**: `feat/WO-0095-incremental-computation-integration-test`
**Tip commit**: `8d7748fdcb11fc85d76b19cee6380966e4677bf9`
**Merge base**: `main`
**Feature**: P3-W9-F11 (Salsa early-cutoff integration test)
**Verdict**: **CLEAN**

## Diff summary

```
4 commits, 5 files changed, 389 insertions(+), 0 deletions(-)
  Cargo.lock                                       |   1 +
  scripts/verify/P3-W9-F11.sh                      |  34 +
  tests/integration/Cargo.toml                     |   5 +
  tests/integration/test_incremental.rs            | 178 +
  ucil-build/work-orders/0095-ready-for-review.md  | 171 +
```

Commits:
1. `fbedcdf` — `test(integration): add test_incremental.rs LoggingDatabase + early-cutoff frozen tests` (+178)
2. `bd6f17a` — `build(tests-integration): add salsa dev-dep + [[test]] entry for test_incremental` (+6)
3. `428f3fa` — `build(scripts): add verify shell script for P3-W9-F11` (+34)
4. `8d7748f` — `chore(work-orders): WO-0095 ready for review` (+171)

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

_None._

### OK

- **Stub detection** — No `todo!()`, `unimplemented!()`, `NotImplementedError`, `pass`-only bodies, or trivial-default returns in `tests/integration/test_incremental.rs` or `scripts/verify/P3-W9-F11.sh`. Both `pub fn test_*` bodies execute substantive Salsa flow with multiple typed assertions.

- **Mocked critical collaborators** — `! grep -qiE 'mock|fake|stub'` is true for both `tests/integration/test_incremental.rs` and `scripts/verify/P3-W9-F11.sh`. The local `LoggingDatabase` (`tests/integration/test_incremental.rs:43-55`) is a UCIL-owned `impl salsa::Database` event-hook harness, the standard Salsa observability seam — explicitly NOT a substitute for any deny-listed external dep (Serena/LSP/SQLite/LanceDB/Docker). The module rustdoc (`tests/integration/test_incremental.rs:9-26`) calls this out in positive phrasing per WO-0093 lesson.

- **Skipped / ignored tests** — No `#[ignore]`, `.skip(`, `xit`, `xfail`, `pytest.mark.skip`, or commented-out assertions anywhere in the diff. Both new tests run unconditionally as `#[test]`.

- **Weak assertions** — All eight assertions across the two frozen tests use `assert_eq!` or `assert!(...)` against substantive runtime values with `(SAn) ...; left: ..., right: ...` panic messages per DEC-0007. No `assert!(true)`, `assert_eq!(true, true)`, or empty-body tests. SA1 (`tests/integration/test_incremental.rs:127-130`) is the load-bearing early-cutoff assertion (`!dependent_reran`); SA0/SA2/SA3 are sanity canaries.

- **Hallucinated paths** — Every import resolved against the source tree:
  - `salsa::Setter` (`tests/integration/test_incremental.rs:32`) — used identically at `crates/ucil-core/src/incremental.rs:` test mod, salsa 0.22 public API.
  - `salsa::Storage`, `salsa::EventKind::WillExecute`, `salsa::Database`, `#[salsa::db]` — public salsa 0.22 surface used in production at `crates/ucil-core/src/incremental.rs`.
  - `ucil_core::incremental::{dependent_metric, FileRevision}` (`tests/integration/test_incremental.rs:33`) — confirmed at `crates/ucil-core/src/lib.rs:52` (re-export) and `crates/ucil-core/src/incremental.rs:134` (`pub fn dependent_metric`) / `crates/ucil-core/src/incremental.rs:82` (`pub trait UcilDb`). The executor correctly imported only the two names actually used (omitting `symbol_count` and `UcilDatabase` from the WO's prescriptive list to satisfy `#![deny(warnings)]`).
  - `salsa = "0.22"` dev-dep in `tests/integration/Cargo.toml:44` matches the pin in `crates/ucil-core/Cargo.toml`.

- **Feature coverage** — P3-W9-F11 has two new tests directly probing its acceptance behaviour, both at module root per DEC-0007:
  - `tests/integration/test_incremental.rs:93` — `pub fn test_incremental_whitespace_only_change_skips_downstream_recompute` (load-bearing early-cutoff invariant).
  - `tests/integration/test_incremental.rs:146` — `pub fn test_incremental_semantic_change_invalidates_downstream` (control case so the early-cutoff test cannot pass trivially).
  Module-root rustdoc cites `P3-W9-F11` + master-plan §10/§17.2/§18.

- **Commit hygiene** — All four commits are Conventional Commits with valid scopes (`integration`, `tests-integration`, `scripts`, `work-orders`). All four bodies carry `Phase: 3`, `Feature: P3-W9-F11`, `Work-order: WO-0095` trailers (4/4 each, verified via `git log --format='%h%n%b' | grep -c`). Every commit ends with `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>` (4/4). Diff sizes per commit: 178 / 6 / 34 / 171 — the 178-line `fbedcdf` commit is the new module-coherent test file, within the DEC-0005 carve-out cited in WO scope_in #21. No merge commits (`git log --merges` empty). No `--amend`, `--no-verify`, or force-push patterns in commit subjects.

- **Doc + public API** — Both `pub fn` items have rustdoc with `# Panics` sections referencing DEC-0007 and the SAn convention (`tests/integration/test_incremental.rs:80-92` and `tests/integration/test_incremental.rs:135-145`). Module-root `//!` rustdoc explains the file's purpose, master-plan citations, and the `LoggingDatabase` test-harness rationale (`tests/integration/test_incremental.rs:9-26`). The non-`pub` items (`LoggingDatabase` struct, its `Default`/`salsa::Database`/method impls, `fixture_revision`) carry rustdoc despite being private, exceeding the requirement.

- **Forbidden-paths** — No file under `crates/`, `tests/fixtures/`, `scripts/gate/`, `scripts/flip-feature.sh`, the master plan, `feature-list.json`, or any pre-existing test file in `tests/integration/test_*.rs` was touched. The diff is confined to NEW files (`tests/integration/test_incremental.rs`, `scripts/verify/P3-W9-F11.sh`, `ucil-build/work-orders/0095-ready-for-review.md`), one append-only `[[test]]` + dev-dep insertion in `tests/integration/Cargo.toml`, and the auto-regenerated `Cargo.lock`.

- **No `unsafe`, no `std::process::Command`** — Both AC11/AC12 greps return empty against the test file.

- **Verify script shape** — `scripts/verify/P3-W9-F11.sh` carries `#!/usr/bin/env bash` shebang (`scripts/verify/P3-W9-F11.sh:1`), `set -euo pipefail` (`scripts/verify/P3-W9-F11.sh:13`), invokes `cargo test --test test_incremental` (`scripts/verify/P3-W9-F11.sh:24`), and asserts `test result: ok.` via `grep -qE` (`scripts/verify/P3-W9-F11.sh:28`). Mirrors WO-0094's `P3-W11-F13.sh` shape verbatim per scope_in #14.

- **RFR marker completeness** — `ucil-build/work-orders/0095-ready-for-review.md` exists with `## Summary`, `## Files changed`, `## Commits`, and a 32-row `## Acceptance criteria — local verification` table. M1 mutation contract, file path, exact patch (`"alpha   beta\tgamma"` → `"alpha beta gamma delta"`), expected SA failure, and `git checkout --` restore command are all documented (per AC19).

## Recommendation

The work-order is **CLEAN**. Spawn the verifier in a fresh session.
