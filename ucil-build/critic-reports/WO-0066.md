# Critic Report: WO-0066

**Critic session**: crt-2026-05-07T-WO-0066
**Branch**: `feat/WO-0066-find-similar-mcp-tool`
**HEAD reviewed**: `1f20c3bac6348cfb50d2ffd008ad0a1e6282a7fb` (`docs(work-orders): update WO-0066 RFR with final commit sha`)
**Merge base**: `4c6f1fe87f90a15275b2c9b7e44b09908122a4f0` (`main` — `plan(p2-w8): emit WO-0066 find-similar MCP tool (closes Phase 2)`)
**Diff scope**: 9 commits, 10 paths, +1567 / -164 (Cargo.toml +13/-6, executor.rs +24/-8, server.rs +1116/-0, P2-W8-F08.sh +75 NEW, coverage-*.md +60/-23 gate-side artefacts, effectiveness-phase-1.md +163/-133 gate-side artefact, RFR 0066-ready-for-review.md +133 NEW)
**Verdict**: **CLEAN with warnings**

The WO lands the `find_similar` MCP tool — closing Phase 2 Week 8 and the entire Phase 2 envelope per master-plan §3.2 line 219 + §18 Phase 2 Week 8 line 1791 ("Vector search works"). The implementation comprises:

1. **Test-helper visibility promotion** in `crates/ucil-daemon/src/executor.rs` (`1664074`): four `#[cfg(test)]` items (`TestEmbeddingSource` struct + `dim` field, `build_synthetic_chunker_for_lancedb_f04`, `read_table_rows_for_lancedb_f04`, `SYNTHETIC_TOKENIZER_JSON_FOR_LANCEDB_F04`) promoted from private to `pub(crate)` per the WO-0064 deferred carve-out hint — `crate::server::test_find_similar_tool` is the second consumer of the WO-0064 fixture pattern.
2. **`FindSimilarExecutor` aggregate** + three frozen `pub const`s (`FIND_SIMILAR_DEFAULT_MAX_RESULTS = 10`, `FIND_SIMILAR_MAX_RESULTS_CAP = 100`, `FIND_SIMILAR_QUERY_TIMEOUT = 5s`) + `McpServer::with_find_similar_executor(...)` builder (`87af673`).
3. **`handle_find_similar`** + helpers `parse_find_similar_max_results`, `parse_find_similar_branch`, `execute_find_similar`, `project_find_similar_rows`, `find_similar_success_envelope`, `find_similar_error_envelope` + dispatch wiring in `handle_tools_call` (`16831a8`). The handler embeds via the injected `EmbeddingSource`, opens `lancedb::connect(uri).execute().await` → `conn.open_table("code_chunks").execute().await` per the WO-0064 production pattern at `lancedb_indexer.rs:660-672`, runs `Table::query().nearest_to(query_vec.as_slice())?.limit(N).execute().await` per the WO-0065 bench precedent, drains the `RecordBatchStream` via `futures::TryStreamExt::try_collect`, projects 12 columns + the synthetic `_distance` column onto `FindSimilarHit { file_path, start_line, end_line, content, language, symbol_name, symbol_kind, similarity_score }`, applies a defence-in-depth sort by `similarity_score` DESC, and emits `tracing::info_span!("ucil.daemon.find_similar", branch, max_results, query_dim)` per master-plan §15.2.
4. **`pub async fn test_find_similar_tool`** at module root (DEC-0007 frozen-selector convention), 8 sub-assertions SA1..SA8 — happy-path envelope, `_meta` shape, hits-count + projection structure, IDENTITY query (snippet byte-identical to baz.rs's chunk → top hit is `baz.rs`), monotonic similarity ordering, missing-snippet → `-32602`, nonexistent-branch → `_meta.error_kind ∈ {branch_not_found, table_not_found}`, fall-through stub when no executor attached (`8e3ff40`).
5. **`scripts/verify/P2-W8-F08.sh`** — frozen-selector grep + frozen-handler grep + frozen-builder grep + `cargo nextest run` (or `cargo test --exact` fallback) + `[OK]/[FAIL]` exit semantics (`762bd5d`).
6. **Cargo.toml hoist**: `futures.workspace = true` moved from `[dev-dependencies]` to `[dependencies]` — first production consumer of `futures::TryStreamExt`; no NEW external crate enters the dep tree (`futures` was already transitively present via `lancedb` / `lance-*`).

I locally re-grepped the diff for every check in `.claude/agents/critic.md`. No blockers. Five warnings noted below — none gating; all are documented divergences with WO precedent.

## Findings

### Blockers (must fix before verifier)

**None.**

### Warnings (should be acknowledged before gate)

1. **`feat(daemon)` commit `16831a8` lands 591 LOC in a single commit** — the wiring of `handle_find_similar` + helper functions (`parse_find_similar_max_results`, `parse_find_similar_branch`, `execute_find_similar`, `project_find_similar_rows`, `find_similar_success_envelope`, `find_similar_error_envelope`, `FindSimilarHit`, `FindSimilarError`, `FindSimilarArgError` types) + dispatch wiring + Cargo.toml hoist arrived together. WO scope_in[25] anticipated four separate `feat(daemon):` commits (handler + dispatch as two of them); the executor consolidated handler + dispatch + helpers + Cargo into one commit. The 591 LOC is dominated by (a) ~140 LOC of doc comments on `handle_find_similar` (master-plan + DEC-0008 + DEC-0007 cites + per-arg shape + result envelope + error kinds), (b) ~70 LOC of helper rustdoc, (c) the 250-LOC `execute_find_similar` body with explicit error-kind discrimination on each `match`. WO-0065 set the precedent with a 327-LOC `feat(embeddings)` cite of DEC-0005 module-coherence; the WO-0066 commit body cites `WO-0066 scope_in 25 + DEC-0005` for the test-file commit but does NOT explicitly cite DEC-0005 for the feat commit's 591 LOC. Splitting helper-by-helper would have produced stub-shaped intermediate states. Soft target violation with implicit precedent — **accept with note**.

2. **`test(daemon)` commit `8e3ff40` lands 425 LOC in a single commit** — `pub async fn test_find_similar_tool` with 8 sub-assertions (SA1..SA8) + fixture-builder (tempdir + branch creation + 3 file writes + `LancedbChunkIndexer::index_paths` invocation + table-row sanity check). WO scope_in[25] explicitly authorizes this single-commit shape with the carve-out "the test fixture + 8 sub-assertions WILL exceed 200 LOC; cite DEC-0005 + WO-0064 precedent". The commit body cites the WO scope_in number directly. Pre-stamped DEC-0005 carve-out — **accept**.

3. **Out-of-scope phase-1 effectiveness re-run commit on the WO branch** — `5499b2d chore(effectiveness): re-run phase-1 nav-rust-symbol at 762bd5d` modifies `ucil-build/verification-reports/effectiveness-phase-1.md` (+163 / -133 lines, signed by Claude Opus 4.7 rather than 4.6). The trailer is `Phase: 1 / Feature: (effectiveness-gate) / Work-order: (effectiveness-evaluator re-run)`. Path is NOT in WO-0066 `forbidden_paths`; per WO-0066 scope_in[32] explicit anticipation, `ucil-build/verification-reports/**` updates are EXPECTED on the WO-0066 feat branch and NOT a forbidden_paths violation. The substantive verdict is unchanged (resolved-deferred escalation `20260507T0357Z-effectiveness-nav-rust-symbol-rs-line-flake.md`; substantive judge-tie within noise). Mirrors WO-0061 / WO-0065 anti-pattern observation — **accept with note**, consistent with the standing protocol.

4. **`chore(verification-reports)` commit `931eea4` is a gate-side artefact** — refresh of `ucil-build/verification-reports/coverage-{ucil-core, ucil-daemon, ucil-lsp-diagnostics, ucil-treesitter}.md` produced by `bash scripts/gate/phase-1.sh` during WO-0066 acceptance verification. The commit body explicitly disclosure-stamps the `ucil-daemon line=0%` line as a known carry from the WO-0058+ standing coverage workaround (real coverage is 88.94% under `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --summary-only --json`; above the AC24 informational floor `>= 80.0`). Per WO-0066 scope_in[32] standing pattern — **accept with note**.

5. **Cargo.toml `futures` dev-dep → prod-dep hoist** — the WO scope_in[24] declares "NO new dependencies in `crates/ucil-daemon/Cargo.toml`" and AC30 frames the verification regex as `git diff main -- crates/ucil-daemon/Cargo.toml | grep -E '^\+[a-z_-]+\s*=' | grep -v '^---'` returning zero matches. The actual added line is `+futures.workspace = true` — the AC30 regex `^\+[a-z_-]+\s*=` does NOT match `+futures.workspace = true` (the `.` in `.workspace` is not in `[a-z_-]+`'s character class), so the regex check passes. Substantively the dep was previously in `[dev-dependencies]` (test-only) and is now in `[dependencies]` (production-visible). The commit body documents this as a "visibility-tier hoist" rather than a new external crate entering the tree (`futures` was already transitively present via `lancedb` / `lance-*`). Strict reading of "no new dependencies" includes hoists between dep-tier; the WO authored this exact regex to permit the hoist. AC30's letter is satisfied; spirit is debatable. Verifier should explicitly check `cargo tree -p ucil-daemon -e features` before/after to confirm no new external crate. **Accept with note** — recommend verifier confirm via `cargo tree`.

### OK

- **Stub detection** — `git diff main..feat/WO-0066-find-similar-mcp-tool -- 'crates/**'` shows zero matches for `todo!()`, `unimplemented!()`, `raise NotImplementedError`, or trivial-default returns in the new code. The "stub" string appears only in (a) doc comments referencing the phase-1 `_meta.not_yet_implemented: true` fall-through path (the existing behaviour preserved per invariant #9), and (b) test panic messages — neither is reachable code that returns a stub.
- **No mocked critical collaborators** — `git diff main..feat -- 'crates/**' | grep -iE 'mock|fake|stub'` matches only doc-comment references (carve-out from WO-0048 line 363 word-ban; "no mocking" disclaimers; `not_yet_implemented` stub-path explanations) and test panic strings ("(SA8) no-executor stub path...", "(SA8) stub envelope must..."). `TestEmbeddingSource` is the canonical UCIL-internal trait-impl per DEC-0008 §4 + WO-0048 line 363 + WO-0064 line 270 — explicitly carved out from the critical-dep mock ban. `LanceDB` is exercised end-to-end (real `lancedb::connect` → `open_table` → `Table::query().nearest_to(...).limit(N).execute()` → `RecordBatchStream::try_collect` against a tempdir-rooted table populated by the real `LancedbChunkIndexer::index_paths`). `BranchManager` is the real `crates/ucil-daemon/src/branch_manager.rs` — no mocking. No Serena, LSP, SQLite, or Docker references in the diff.
- **No skipped/ignored tests** — `git diff main..feat | grep -iE '#\[ignore\]|\.skip\(|xfail|xit\(|pytest\.mark\.skip|it\.skip\('` returns zero hits in code paths. The only `xit\b` matches are bash `exit` statements in `scripts/verify/P2-W8-F08.sh`, false positives from word-boundary alone — not test-skip annotations.
- **No weak assertions** — `git diff main..feat -- 'crates/**' | grep -iE 'assert!\(true\)|assert_eq!\(true,\s*true\)|expect\(true\)|assert True\b|assert 1\b'` returns zero hits. Every assertion in `test_find_similar_tool` is substantive: `jsonrpc == "2.0"`, `id == 401`, `error.is_none()`, `result.isError == false`, `_meta.tool == "find_similar"`, `_meta.query_dim == 768`, `hits.len() == hits_count`, `hits[0].file_path.ends_with("baz.rs")`, monotonic descending similarity, `error.code == -32602`, `error.message.contains("snippet")`, `_meta.error_kind ∈ {branch_not_found, table_not_found}`, `_meta.not_yet_implemented == true`.
- **No `.unwrap()`/`.expect()` in production code paths** — the eight `.expect()` sites in the diff (`tempfile::tempdir().expect("tmp repo")`, `tokio::fs::create_dir_all(...).expect("mkdir branches")`, etc.) all live inside the `#[cfg(test)]`-gated `pub async fn test_find_similar_tool()` at `server.rs:5028`. The `unwrap_or` at `server.rs:527` (`usize::try_from(max_results).unwrap_or(usize::MAX)`) is `unwrap_or`, not `.unwrap()` — that's permitted in production code per `.claude/rules/rust-style.md`. Production-side `match` arms cover every error path explicitly.
- **No hallucinated imports** — `use crate::branch_manager::BranchManager;` resolves to `crates/ucil-daemon/src/branch_manager.rs:286` (`pub fn branch_vectors_dir`); `use crate::lancedb_indexer::EmbeddingSource;` resolves to `lancedb_indexer.rs:285` (`pub trait EmbeddingSource`); `use crate::executor::{build_synthetic_chunker_for_lancedb_f04, read_table_rows_for_lancedb_f04, TestEmbeddingSource};` resolves to the post-promotion `pub(crate)` items at `executor.rs:2710/2748/2766`; `use crate::lancedb_indexer::LancedbChunkIndexer;` resolves to `lancedb_indexer.rs:522`. `arrow_array::cast::AsArray::as_string::<i32>`, `arrow_array::Int32Array`, `arrow_array::Float32Array`, `arrow_array::RecordBatch` are all from the existing `arrow-array.workspace = true` dependency. `lancedb::connect`, `lancedb::query::{ExecutableQuery, QueryBase}` are from `lancedb.workspace = true`. `futures::TryStreamExt` is from the hoisted `futures.workspace = true`. All paths verified against the `git show feat/WO-0066-find-similar-mcp-tool:Cargo.toml` snapshot.
- **Feature coverage** — WO declares `feature_ids: ["P2-W8-F08"]`. The new test `pub async fn test_find_similar_tool` at `crates/ucil-daemon/src/server.rs:5028` covers the feature with 8 explicitly-numbered sub-assertions. The 8 sub-assertions map 1:1 to AC15..AC18 + AC11..AC13 + AC05 from the WO. The frozen-selector grep `^[[:space:]]*pub async fn test_find_similar_tool\(\)` resolves at column 0 — DEC-0007 module-root convention upheld.
- **Commit hygiene** — all 9 commits are Conventional Commits with `<type>(<scope>): <subject>` (each subject ≤ 70 chars). All 9 carry the `Phase: 2 / Feature: P2-W8-F08 / Work-order: WO-0066` trailers (with the warning #3 effectiveness commit's `Phase: 1 / Feature: (effectiveness-gate) / Work-order: (effectiveness-evaluator re-run)` carve-out, consistent with the standing gate-side-artefact pattern). Commit subjects: `1f20c3b docs(work-orders)` / `931eea4 chore(verification-reports)` / `34b0ef5 docs(work-orders)` / `5499b2d chore(effectiveness)` / `762bd5d chore(scripts)` / `8e3ff40 test(daemon)` / `16831a8 feat(daemon)` / `87af673 feat(daemon)` / `1664074 refactor(daemon)`. No `--amend` after push, no force push, no `--no-verify` flags. Per-commit LOC: `1664074=27` ✅, `87af673=135` ⚠ soft, `16831a8=591` ⚠ (warning #1), `8e3ff40=425` ✅ (carve-out per WO-0066 scope_in 25), `762bd5d=75` ✅, `5499b2d=167` ✅, `34b0ef5=131` ✅, `931eea4=36` ✅, `1f20c3b=5` ✅.
- **Doc + public API** — every NEW `pub` item carries rustdoc:
  - `pub const FIND_SIMILAR_DEFAULT_MAX_RESULTS: u64` (`server.rs:127`) — 8 lines of rustdoc citing master-plan §3.2.
  - `pub const FIND_SIMILAR_MAX_RESULTS_CAP: u64` (`server.rs:136`) — 7 lines of rustdoc.
  - `pub const FIND_SIMILAR_QUERY_TIMEOUT: Duration` (`server.rs:150`) — 12 lines of rustdoc.
  - `pub struct FindSimilarExecutor` (`server.rs:401`) — 28 lines of rustdoc citing master-plan §3.2 line 219 + §18 line 1791 + §12.2 + DEC-0008.
  - `pub fn FindSimilarExecutor::new(...)` (`server.rs:423`) — 4 lines of rustdoc.
  - `pub fn FindSimilarExecutor::default_branch(...)` (`server.rs:438`) — 2 lines of rustdoc.
  - `pub find_similar: Option<Arc<FindSimilarExecutor>>` field on `McpServer` (`server.rs:485`) — 11 lines of rustdoc.
  - `pub fn McpServer::with_find_similar_executor(...)` (`server.rs:581`) — 16 lines of rustdoc citing master-plan §3.2 + WO-0010 invariant #9 + DEC-0008.
- **Per-crate `#![deny(rustdoc::broken_intra_doc_links)]` discipline** — all rustdoc cross-refs use `[`Type`]` linkability syntax against existing public items: `[`BranchManager`]`, `[`BranchManager::branch_vectors_dir`]`, `[`EmbeddingSource`]`, `[`McpServer::handle_find_similar`]`, `[`Self::new`]`, `[`Self::with_knowledge_graph`]`, `[`Self::with_g2_sources`]`, `[`FIND_SIMILAR_DEFAULT_MAX_RESULTS`]`, `[`FIND_SIMILAR_MAX_RESULTS_CAP`]`, `[`FIND_SIMILAR_QUERY_TIMEOUT`]`, `[`FindSimilarExecutor::default_branch`]`, `[`McpServer::handle_find_similar`]`. All targets exist in the new code or pre-existing surface.
- **Tracing discipline** — `#[tracing::instrument(name = "ucil.daemon.find_similar", level = "debug", skip(id, params, executor))]` on `handle_find_similar` plus the inner `tracing::info_span!("ucil.daemon.find_similar", branch, max_results, query_dim)` per master-plan §15.2 span-naming convention. Error paths emit `tracing::warn!` with structured `error_kind` field. No `println!`/`eprintln!` in the diff.
- **Async timeout discipline** — `tokio::time::timeout(FIND_SIMILAR_QUERY_TIMEOUT, execute_find_similar(...)).await` wraps every `.await` that touches IO (the embedding call + LanceDB connect/open/query/drain). Named const `FIND_SIMILAR_QUERY_TIMEOUT = Duration::from_secs(5)` per language-agnostic invariant.
- **Forbidden paths** — `git diff main..feat -- 'ucil-build/feature-list.json' 'ucil-build/feature-list.schema.json' 'ucil-master-plan-v2.1-final.md' 'tests/fixtures/' 'scripts/gate/' 'scripts/flip-feature.sh' 'crates/ucil-embeddings/' 'crates/ucil-treesitter/' 'crates/ucil-cli/' 'crates/ucil-core/' 'adapters/' 'ml/'` returns zero changes. WO `forbidden_paths` upheld.
- **Verify script `scripts/verify/P2-W8-F08.sh`** — shebang `#!/usr/bin/env bash` ✅, `set -euo pipefail` ✅, `IFS=$'\n\t'` ✅, executable bit `100755` ✅. Frozen-selector grep `^[[:space:]]*pub async fn test_find_similar_tool\(\)` ✅, frozen-handler grep `fn handle_find_similar` ✅, frozen-builder grep `fn with_find_similar_executor` ✅. Falls back to `cargo test -p ucil-daemon --lib -- --exact server::test_find_similar_tool` if `cargo-nextest` is absent. `[OK]` / `[FAIL]` exit semantics implemented per AC28.
- **Pre-flight word-ban (WO-0061 line 686)** — `git diff main..feat -- 'crates/**'` mentions `mock|fake|stub` only in (a) doc-comment carve-out citations to WO-0048 line 363 / WO-0066 carve-out, (b) `not_yet_implemented` "stub path" disclaimers (existing phase-1 invariant #9 behaviour preserved), and (c) test panic messages ("(SA8) no-executor stub path..."). Per WO-0066 AC29 + WO-0048 line 363, test code is exempt and doc-comment carve-out citations are the canonical pattern. **No production code path returns a mock/fake/stub value as a feature implementation**.

## Mutation contract delegation (M1/M2/M3)

Per WO-0066 scope_in[22] + WO-0061 line 690 standing pattern, mutations M1/M2/M3 are explicitly DELEGATED to the verifier. The RFR `0066-ready-for-review.md` documents the literal `sed`-able edits + expected failure modes + restoration commands:

- **M1** — replace `nearest_to(query_vec.as_slice())` with `nearest_to(&[0.0f32; 768])` at `server.rs:530`. Expected: SA4 fails (top hit no longer ends with `baz.rs`).
- **M2** — replace `executor.embedding_source.embed(snippet).await?` body with `Ok::<Vec<f32>, _>(vec![0.0f32; executor.embedding_source.dim()])` at `server.rs:460`. Expected: SA4 fails.
- **M3** — comment out the `hits.sort_by(...)` block at `server.rs:567-571`. Expected: SA5 fails on the LanceDB stream-order leak.

**Verifier MUST apply each mutation, run `bash scripts/verify/P2-W8-F08.sh`, confirm the expected failure mode, and restore via `git checkout -- crates/ucil-daemon/src/server.rs` before proceeding to the next mutation.** Critic recommends verifier apply M3 with extra care: LanceDB's `nearest_to` results are typically already distance-ordered upstream, so SA5 may transiently pass even with the explicit sort removed if upstream ordering happens to coincide with the desired DESC order. The defence-in-depth is real but the green-stays-green case must be inspected, not assumed.

## Summary

WO-0066 lands the `find_similar` MCP tool end-to-end with a robust 8-sub-assertion frozen test, real LanceDB exercising, an injected `EmbeddingSource` per DEC-0008 trait-seam discipline, and full master-plan §3.2 + §12.2 + §15.2 + §18 citation traceability. Five warnings are documented and consistent with WO-0061 / WO-0064 / WO-0065 standing precedent (large feat commit, large test commit pre-stamped under DEC-0005, gate-side effectiveness/coverage artefacts, dev-dep → prod-dep hoist within the AC30 regex's letter). No blockers.

**Verdict**: **CLEAN with warnings** — proceed to verifier. Verifier should apply mutations M1/M2/M3 per the standing pattern, confirm `cargo tree -p ucil-daemon -e features` shows no new external crate, and confirm SA1..SA8 from a clean build (`cargo clean && cargo nextest run -p ucil-daemon server::test_find_similar_tool`).
