# Critic Report: WO-0032

**Critic session**: crt-wo-0032-treesitter-to-kg-pipeline
**Branch**: feat/WO-0032-treesitter-to-kg-pipeline
**HEAD**: ebb046dc05948a821dda9207f220d942d10fe97a
**Merge base**: main (linear history, 4 commits ahead)
**Feature**: P1-W4-F04 (wire tree-sitter extraction → knowledge-graph population)
**Verdict**: CLEAN

## Diff scope

```
 Cargo.lock                                      |   2 +
 crates/ucil-daemon/Cargo.toml                   |   7 +
 crates/ucil-daemon/src/executor.rs              | 877 ++++++++++++++++++++++++
 crates/ucil-daemon/src/lib.rs                   |   9 +
 ucil-build/work-orders/0032-ready-for-review.md | 117 ++++
 5 files changed, 1012 insertions(+)
```

4 commits on top of main:

| sha | subject | +/− |
|-----|---------|-----|
| `46103ae` | `build(daemon): add ucil-treesitter + rusqlite deps for executor pipeline` | +7 |
| `91a408d` | `feat(daemon): add executor module — tree-sitter → KG ingest pipeline` | +667 |
| `8307fbf` | `test(daemon): add supplementary unit coverage for executor helpers` | +221 |
| `ebb046d` | `chore(wo-0032): mark ready for review` | +117 |

No forbidden path is touched (`ucil-build/feature-list.json`, `ucil-build/feature-list.schema.json`, `ucil-master-plan-v2.1-final.md`, `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh` — all clean per `git diff --name-only main..HEAD`).

History is linear (no merges; every commit has exactly one parent).

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate — non-blocking)

1. **Commit-size soft-target exceeded — `91a408d feat(daemon)` is +667 LoC.** The `commit-style.md` rule (`~50 LoC soft target`) and the critic-protocol hard ceiling (200 LoC) are both exceeded. The commit body cites **DEC-0005** (`ucil-build/decisions/DEC-0005-WO-0006-module-coherence-commits.md`) as precedent for new-module-introduction commits, and the rationale matches: splitting types from impl would leave intermediate states that fail `clippy::dead_code` (the crate-root sets `#![deny(warnings)]` and `#![warn(clippy::pedantic)]`); splitting tests from impl would violate "every feat commit has a test." DEC-0005 explicitly extends to "new module file containing type + impl + unit-test-mod in a single commit," which is exactly the shape here. Non-blocking.
2. **`8307fbf test(daemon)` is +221 LoC, slightly over 200.** Contains 7 supplementary unit tests grouped into one coherent batch (multi-file isolation, unsupported-extension error path, extension table fence, kind-tag table fence, qualified-name shape, source-hash determinism, Default impl). Splitting per-test would yield 7 sub-50-LoC commits but lose review coherence. Non-blocking; flagging for awareness.

### OK

- **(1) Stub detection** — `rg -n 'todo!\(\)|unimplemented!\(\)' crates/ucil-daemon/src/executor.rs` returned no matches. No `raise NotImplementedError`, no bare `pass`, no `panic!("not yet…")`. The two `_ => "unknown"` arms in `kind_tag` (`executor.rs:201`) and `language_tag` (`executor.rs:230`) are wildcard-exhaustiveness arms required by `SymbolKind` / `Language` being `#[non_exhaustive]` (verified at `crates/ucil-treesitter/src/symbols.rs:107` and `crates/ucil-treesitter/src/parser.rs:36`). They are NOT trivial-default-return stubs — the explicit variant matches above them are real, exhaustive over every currently-known variant, and the wildcard is documented as the forward-compat fallback. The "return literal" rule does not fire (no bare `return None`, no bare `return Default::default()`, no bare `return 0`).
- **(2) Mocked critical dependencies** — the test selector `executor::test_treesitter_to_kg_pipeline` (`executor.rs:544`) opens a **real** `KnowledgeGraph` against a real on-disk SQLite tempdir (`executor.rs:547-549`), invokes the **real** `ucil_treesitter::Parser` + `SymbolExtractor` via `IngestPipeline::new()` + `ingest_file()` (`executor.rs:558-561`), and asserts on real `KnowledgeGraph::list_entities_by_file` rows (`executor.rs:567-569`). All seven supplementary tests follow the same pattern. `rg -ni 'mock|fake|stub|MagicMock|Mockall' /tmp/executor.rs` matches only **one** line — `executor.rs:50`, a documentation string explicitly stating "no mocks of tree-sitter, `SQLite`, or the knowledge graph are permitted." No mocks of Serena, LSP, SQLite, LanceDB, or Docker.
- **(3) Skipped / ignored tests** — `git diff main..HEAD -- '*.rs' | grep '^+'` filtered for `#[ignore]`, `.skip(`, `xfail`, `it.skip`, `xit`, `pytest.mark.skip` returned **no matches**.
- **(4) Weak assertions** — every assertion in `test_treesitter_to_kg_pipeline` and the seven supplementary tests checks a concrete value (entity count > 0, `source_tool` == `Some("tree-sitter")`, `language` == `Some("rust")`, `t_valid_from` == `Some(TREE_SITTER_VALID_FROM)`, `start_line >= 1`, `qualified_name.is_some()`, kind ∈ {function, method}, idempotent row-count equality, `access_count` ≥ entity count, exact extension → `Language` table, hash length == 16 + hex chars, qualified-name shape `src/a.rs::foo@10:1`). No `assert!(true)`, no `assert_eq!(1, 1)`, no empty test bodies. Most assertions carry a rationale string.
- **(5) Hallucinated paths** — every `use` resolves on this branch:
  - `ucil_core::knowledge_graph::{KnowledgeGraph, KnowledgeGraphError}` (`executor.rs:75`) → defined at `crates/ucil-core/src/knowledge_graph.rs:539` and `:65`.
  - `ucil_treesitter::parser::{Language, ParseError, Parser}` (`executor.rs:76`) → defined at `crates/ucil-treesitter/src/parser.rs:38, 14, 119`.
  - `ucil_treesitter::symbols::{ExtractedSymbol, SymbolExtractor, SymbolKind}` (`executor.rs:77`) → defined at `crates/ucil-treesitter/src/symbols.rs:147, 277, 109`.
  - `KnowledgeGraph::execute_in_transaction` closure type `FnOnce(&Transaction) -> Result<_, rusqlite::Error>` matches the `executor.rs:445` call site exactly (`crates/ucil-core/src/knowledge_graph.rs:652`).
  - `KnowledgeGraph::list_entities_by_file(&str) -> Result<Vec<Entity>, _>` matches `executor.rs:568` (`crates/ucil-core/src/knowledge_graph.rs:789`).
  - `KnowledgeGraph::conn() -> &Connection` matches `executor.rs:643` (`crates/ucil-core/src/knowledge_graph.rs:635`).
  - `Entity` fields touched by the test (`source_tool`, `language`, `t_valid_from`, `start_line`, `qualified_name`, `kind`) all exist (`crates/ucil-core/src/knowledge_graph.rs:127-178`).
  - `rusqlite.workspace = true` (`crates/ucil-daemon/Cargo.toml:38`) resolves to the workspace declaration at root `Cargo.toml:37`.
  - `ucil-treesitter = { path = "../ucil-treesitter" }` (`crates/ucil-daemon/Cargo.toml:43`) is a real sibling crate with `Cargo.toml`.
  - The fixture file `tests/fixtures/rust-project/src/util.rs` exists on this branch (verified via `git ls-tree`); `src/parser.rs` referenced by `test_ingest_multi_file_isolation` also exists.
  - Re-exports at `crates/ucil-daemon/src/lib.rs:65` (`pub use executor::{ExecutorError, IngestPipeline, SOURCE_TOOL, TREE_SITTER_VALID_FROM};`) all resolve to the new pub items at `executor.rs:113, 359, 87, 101`.
- **(6) Feature coverage** — the single feature `P1-W4-F04` ("Wire tree-sitter extraction → knowledge-graph population") is exercised by the **frozen acceptance selector** `executor::test_treesitter_to_kg_pipeline` (`executor.rs:544`), which maps one-to-one to the WO's `acceptance` list:
  - "Pipeline runs end-to-end against a real fixture rust file and inserts entities" → `executor.rs:558-573` (real `IngestPipeline.ingest_file` against `tests/fixtures/rust-project/src/util.rs`, then `list_entities_by_file` retrieval).
  - "`source_tool = "tree-sitter"` on every inserted entity" → `executor.rs:576-581` (assertion loop over every retrieved entity).
  - "Pipeline upserts within a single `execute_in_transaction` per file" → reachable from `executor.rs:445` (single `kg.execute_in_transaction(|tx| …)` call per `ingest_file` invocation, looping inserts inside the closure rather than calling `upsert_entity` per row).
  - "Running the pipeline twice on an unchanged file is idempotent" → `executor.rs:614-634` (second `ingest_file` call, asserts `second == first` insert count and `after.len() == count_before`).
  - Plus seven supplementary tests at `executor.rs:662-877` covering multi-file isolation (each file gets its own transaction scope), unsupported-extension error path, extension-table regression fence, kind-tag table fence, qualified-name shape stability, `compute_source_hash` determinism + 16-hex shape, and `Default` impl availability.
- **(7) Commit hygiene** — all 4 commits are Conventional Commits (`build/feat/test/chore(<scope>): …`), subject lines ≤72 chars, every commit carries the required trailers `Phase: 1`, `Feature: P1-W4-F04`, `Work-order: WO-0032` (4×3 matches verified). No `--amend`, no force-push, no bypass-hook markers (`--no-verify`, `-n`). Linear history (every commit has exactly one parent).
- **(8) Doc + public API** — every new `pub` item carries rustdoc:
  - `pub const SOURCE_TOOL: &str` (`executor.rs:87`) — explains the downstream-fusion role and the rationale for exposing as a const.
  - `pub const TREE_SITTER_VALID_FROM: &str` (`executor.rs:101`) — explains the bi-temporal pinning rationale and the migration path.
  - `pub enum ExecutorError` (`executor.rs:113`) — explains `#[non_exhaustive]` rationale; every variant (`Io`, `UnsupportedExtension`, `Parse`, `KnowledgeGraph`) and every variant field has its own doc string.
  - `pub struct IngestPipeline` (`executor.rs:359`) — explains the per-thread reuse pattern, the borrow-vs-own design choice, and includes a working `# Examples` block.
  - `pub fn IngestPipeline::new` (`executor.rs:370`) — documents equivalence to `Default`.
  - `pub fn IngestPipeline::ingest_file` (`executor.rs:416`) — covers transaction scope, idempotency, and an `# Errors` block enumerating all four `ExecutorError` variants.
  - Module-level `//!` block (`executor.rs:1-61`) cites master-plan §18 Phase 1 Week 4 line 1759 and §11 line 1117 + phase-log invariants 1, 7, 8 as required by the WO `scope_in` rustdoc clause.
  - Crate-level `lib.rs` doc block updated at `crates/ucil-daemon/src/lib.rs:50-55` to register the new module.
  - Internal helpers (`language_from_extension`, `kind_tag`, `language_tag`, `build_qualified_name`, `compute_source_hash`, `symbol_to_row`, `EntityRow`) are not `pub` but each carries an explanatory rustdoc block — exceeds the bar.

## Summary

The branch lands a coherent, spec-conforming addition: one new public module (`ucil_daemon::executor`) wiring `ucil_treesitter::Parser` + `SymbolExtractor` into `ucil_core::KnowledgeGraph` per master-plan §18 Phase 1 Week 4 line 1759, with one frozen acceptance test plus seven supplementary unit tests and four crate-root re-exports. Tests hit real `tree-sitter` and real on-disk `rusqlite` (no mocks of critical collaborators). All public APIs are documented; all `use` paths resolve on the branch; no forbidden file is touched. Idempotency is enforced by synthesising a non-NULL `qualified_name` of shape `{file}::{name}@{line}:{col}` plus a constant `t_valid_from`, threading the existing `ON CONFLICT(qualified_name, file_path, t_valid_from) DO UPDATE` path. Single-transaction-per-file (WAL-batching invariant, master-plan §11 line 1117) is reachable from the test by code inspection — `ingest_file` issues exactly one `kg.execute_in_transaction(|tx| …)` call.

The two `_ => "unknown"` wildcard arms (`kind_tag`, `language_tag`) are required by upstream `#[non_exhaustive]` enums and are documented as forward-compat fallbacks, not stubs.

Commit hygiene is clean. The two oversized commits (`91a408d` at 667 LoC, `8307fbf` at 221 LoC) are non-blocking per **DEC-0005**, which the executor cites in the `91a408d` body. Both are noted as warnings only.

**Recommendation**: proceed to verifier.
