# Critic Report: WO-0087

**Critic session**: crt-wo-0087-aider-repo-map-pagerank
**Branch**: feat/WO-0087-aider-repo-map-pagerank
**Ref**: d236016918f68fb01fbe9fdad2339313fc4e7f22
**Diff scope**: main..feat/WO-0087-aider-repo-map-pagerank (3 commits, +695 LOC, -0 LOC, 3 files)
**Verdict**: BLOCKED

## Summary

The branch lands a clean, well-documented implementation of the Aider repo-map
algorithm (skeleton + types + PageRank kernel + KG enumeration + recency-bias
personalization + budget-fit) but is **structurally incomplete**: commits (4)
and (5) of the planner's own 5-commit plan never materialised. The frozen unit
test `test_repo_map_pagerank` is absent and the ready-for-review marker file
does not exist, so the verifier cannot exercise the load-bearing acceptance
test or the M1/M2/M3 mutation contract that anchors the feature's correctness
proof. Production code is otherwise clean — no stubs, no mocked critical
collaborators, no skipped tests, no weak assertions, no hallucinated paths,
commit hygiene is green.

## Findings

### Blockers (must fix before verifier)

1. **Missing frozen unit test `fn test_repo_map_pagerank`**
   `crates/ucil-core/src/context_compiler.rs` is 607 lines but contains
   **zero** `#[cfg(test)]` blocks and **zero** `#[test]` functions. The string
   `test_repo_map_pagerank` appears only inside a doc-comment line at
   `crates/ucil-core/src/context_compiler.rs:88`. This violates:
   - AC13 (`grep -cE '#\[cfg\(test\)\]'` must return ≥1)
   - AC14 (frozen test placement at module root)
   - AC15 (cargo selector must resolve to exactly 1 test)
   - AC16 (test must pass — it cannot pass if it does not exist)
   - AC21 (`! grep -qE '#\[ignore\]…'` is vacuously satisfied but the
     anti-laziness intent is violated)
   - AC23, AC24, AC25 (M1/M2/M3 mutation contracts — each requires the
     SA-tagged panic body in the missing test)
   - Critic check #6 (feature coverage): WO-0087 lists `feature_ids:
     ["P3-W10-F01"]`, and that feature's acceptance test in
     `feature-list.json` is the cargo_test `context_compiler::test_repo_map_pagerank`.
     No test in the diff references this feature's behaviour.

2. **Missing ready-for-review file**
   `ucil-build/work-orders/0087-ready-for-review.md` does not exist. The
   work-order's scope_in #25 + AC27 require commit (5) `docs(work-orders):
   WO-0087 ready-for-review` with the M1/M2/M3 mutation contract section,
   convergence diagnostics, path-#4(a)-vs-#4(b) decision log, and disclosed
   deviations. The file is a hard prerequisite for verifier dispatch:
   `ucil-build/work-orders/` shows 0086 RFR present but no 0087.

3. **Commit-plan shortfall — 3 of 5 planned commits landed**
   The work-order's scope_in #25 specifies 4-5 commits; the first commit's
   own body (5fe4f02) explicitly lists the 5-commit shape:
   ```
   (1) skeleton + types + lib.rs mod-decl [this commit];
   (2) personalized_page_rank iteration kernel;
   (3) build_repo_map + KG enumeration helpers + 50x recency bias +
       token-budget fitting;
   (4) test_repo_map_pagerank with 6-entity DAG + 3 SA-tagged assertions;
   (5) RFR.
   ```
   `git log --oneline main..feat/WO-0087-aider-repo-map-pagerank` shows only
   commits (1)/(2)/(3); commit (4) test and commit (5) RFR are absent.
   Cross-references AC35 (commit cadence 4-6).

### Warnings (should fix before gate, not strictly blocking)

(None — all other categories are clean.)

### OK

- **Stub detection**: `grep -nE 'todo!|unimplemented!|NotImplementedError'`
  on the new files returns no hits. No stub bodies in production code.
- **Mocked critical deps**: no `mock|fake|stub` strings in the production-side
  diff (`context_compiler.rs`, `lib.rs`, `knowledge_graph.rs`); no test files
  added so no test-side mocks to scrutinise.
- **Skipped tests**: no `#[ignore]`, `.skip()`, `xfail`, `it.skip`,
  `pytest.skip` strings anywhere in the diff (vacuous because no tests added).
- **Weak assertions**: no `assert!(true)`, `expect(true)`, `assert_eq!(true,
  true)` patterns (vacuous because no tests added).
- **Hallucinated paths / imports**: `context_compiler.rs:96` imports
  `crate::knowledge_graph::{Entity, KnowledgeGraph, KnowledgeGraphError}` —
  all three are pre-existing public types in `knowledge_graph.rs`. The new
  KG read helpers `list_all_entities`
  (`crates/ucil-core/src/knowledge_graph.rs:1003-1019`) and
  `list_all_calls_relations` (`crates/ucil-core/src/knowledge_graph.rs:1166-1184`)
  consumed by `build_repo_map` (`crates/ucil-core/src/context_compiler.rs:507,511`)
  exist as `pub fn` on `impl KnowledgeGraph` with full column projections that
  match the existing `entity_from_row` / `relation_from_row` row-mappers. The
  `pub use context_compiler::{RankedSymbol, RepoMap, RepoMapError, RepoMapOptions};`
  re-export at `crates/ucil-core/src/lib.rs:36` resolves cleanly to the four
  declared types.
- **Feature coverage (production-side)**: every feature_id in the work-order
  (`P3-W10-F01`) has an entry-point implementation reachable from the public
  API surface re-exported in `lib.rs:36`. The implementation gap is on the
  test side (Blocker #1), not the production side.
- **Commit hygiene**: 3/3 commits use Conventional Commits subjects
  (`feat(core): …`); 3/3 carry the `Phase: 3 / Feature: P3-W10-F01 /
  Work-order: WO-0087` trailer block; 3/3 carry the
  `Co-Authored-By: Claude Opus 4.6` trailer; no commit exceeds the 200-LOC
  rule of thumb (commit 1: skeleton + types ≈ 240 LOC of mostly rustdoc;
  commit 2: kernel ≈ 145 LOC; commit 3: entry point + KG helpers ≈ 215 LOC).
  No `--amend`-after-push artefact in the log; no merge commits
  (`git log feat/WO-0087-aider-repo-map-pagerank ^main --merges` is empty —
  satisfies AC28). No force-push residue.
- **Doc + public API**: every new `pub` item has a rustdoc preamble.
  `RepoMapError` (`context_compiler.rs:111`), `RepoMapOptions`
  (`context_compiler.rs:160`, with `# Examples` block at lines 146-156),
  `RankedSymbol` (`context_compiler.rs:208`), `RepoMap`
  (`context_compiler.rs:231`), `entity_token_estimate`
  (`context_compiler.rs:265`, marked `#[must_use]`), `personalized_page_rank`
  (`context_compiler.rs:329`, marked `#[must_use]`), `build_repo_map`
  (`context_compiler.rs:501`) all carry full rustdoc with master-plan
  citations and (for the algorithmic functions) `# Update rule` /
  `# Convergence` / `# Errors` sections per `.claude/rules/rust-style.md
  §Docs`. The two new KG methods carry rustdoc explaining one-shot warm-up
  scope and citing master-plan §3.5 + §4.5.
- **No `unsafe`**: `! grep -qE 'unsafe\s*\{|unsafe\s+fn'` on
  `context_compiler.rs` (satisfies AC33).
- **No `std::process::Command`**: `! grep -qE 'std::process::Command'` on
  `context_compiler.rs` (satisfies AC32).
- **No new Cargo deps**: `git diff main…HEAD -- Cargo.toml
  crates/ucil-core/Cargo.toml` returns empty (satisfies AC22).
- **Forbidden-paths discipline**: diff touches only
  `crates/ucil-core/src/{context_compiler.rs,knowledge_graph.rs,lib.rs}` —
  none of the work-order's `forbidden_paths` are touched (satisfies AC29,
  AC30, AC31 transitively).
- **Tracing**: `personalized_page_rank` carries
  `#[tracing::instrument(..., name = "ucil.core.context_compiler.page_rank")]`
  per scope_in #14 + AC12 (`context_compiler.rs:322-327`). The two new KG
  helpers carry `ucil.core.kg.list_all_entities` /
  `ucil.core.kg.list_all_calls_relations` matching the existing
  `ucil.core.kg.<op>` shape.
- **Errors**: `RepoMapError` is `#[derive(Debug, thiserror::Error)]
  #[non_exhaustive]` with a `#[from] KnowledgeGraphError` bridge per
  `.claude/rules/rust-style.md §Errors`.

## Verifier disposition

Do NOT spawn the verifier in its current state. The branch is missing the
load-bearing acceptance test that AC15/AC16/AC23/AC24/AC25/AC26 all pivot
on, and the RFR marker that the workflow's `executor → critic → verifier`
hand-off requires. The executor must:

1. Land commit (4): `test(core): seed KG with 6-entity DAG and assert
   structural+recency+budget invariants` — module-root placement of
   `test_repo_map_pagerank` per DEC-0007 (NOT inside `mod tests { … }`),
   with the SA1/SA2/SA3 panic bodies named in scope_in #8 so the M1/M2/M3
   mutation contract surfaces the regressions.
2. Land commit (5): `docs(work-orders): WO-0087 ready-for-review` with the
   canonical mutation-contract section + convergence diagnostics + path
   #4(a) decision log per AC27 / scope_in #23.

Re-spawn the critic on the updated tip after both commits land.

---

*Critic methodology: read-only review per `.claude/agents/critic.md`. Checks
1-8 executed against `git diff main…origin/feat/WO-0087-aider-repo-map-pagerank`
+ `git log --oneline main..origin/feat/WO-0087-aider-repo-map-pagerank` +
work-order JSON (`ucil-build/work-orders/0087-aider-repo-map-pagerank.json`).
No source-side edits performed.*
