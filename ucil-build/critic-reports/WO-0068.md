# Critic Report: WO-0068

**Critic session**: crt-WO-0068
**Branch**: `feat/WO-0068-cross-group-executor-and-fusion`
**Ref**: `4a609f25cec041c013ca65a792088373843865b7`
**Merge base**: `main`
**Verdict**: CLEAN

## Scope reviewed

```
Cargo.lock                                         |    1 +
crates/ucil-core/Cargo.toml                        |    5 +
crates/ucil-core/src/cross_group.rs                | 1380 ++++++++++++++++++++
crates/ucil-core/src/lib.rs                        |    7 +
scripts/verify/P3-W9-F03.sh                        |  103 ++
scripts/verify/P3-W9-F04.sh                        |   95 ++
ucil-build/verification-reports/*.md               |  748 +/-
ucil-build/work-orders/0068-ready-for-review.md    |  310 +
```

12 commits; substantive UCIL-source diff is in 5 (`feat` / `test` /
`refactor` / `chore` / `docs`). Remaining 7 are gate-side artefact
refreshes and the RFR doc, all permitted under WO-0068 scope_in #46.

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **`.expect()` in production code (style-rule advisory)**
   `crates/ucil-core/src/cross_group.rs:521` — `.expect("join_all_cross_group: every slot must be filled before returning")` lives in the private `join_all_cross_group` helper (outside `#[cfg(test)]`).
   The expect is structurally unreachable: the surrounding `poll_fn` (lines 497-518) only resolves with `Poll::Ready(())` when `any_pending == false`, i.e. every slot is `Some(T)`. The expect message documents this invariant explicitly.
   This mirrors the existing `join_all_g1` precedent shipped under WO-0047 (`crates/ucil-daemon/src/executor.rs:1067`), which carries the identical `.expect("join_all_g1: every slot must be filled before returning")` pattern and passed verifier on its original merge.
   `.claude/rules/rust-style.md` prefers `?` + typed error, but the function returns `Vec<T>` (not `Result`), so propagating would require a return-type change with no caller-visible benefit. Soft advisory only — accept under the daemon precedent.

2. **Two single-commit additions exceed the ≤50 LOC soft target**
   - `2fde7d03 feat(core): add cross_group module with execute + fuse APIs` — 788 insertions across 4 files (the new module + lib.rs wiring + Cargo.toml dep + Cargo.lock).
   - `ecac8b09 test(core): add cross_group frozen tests SA1..SA7` — 597 insertions in one file.
   Both commit bodies cite DEC-0005 module-coherence carve-out and reference WO-0067 ceqp.rs (548 LOC, single commit) as the recent precedent. The work-order pre-emptively allowed this in `scope_in` #39 ("cite DEC-0005 in the commit body when a single commit … crosses 100-150 LOC because the function/test is one cohesive unit"). Splitting helper-by-helper would produce stub-shaped intermediate states and break the M1/M2/M3 mutation-restoration contract. Acceptable under the carve-out.

3. **`lib.rs` re-export block is non-alphabetical (planner-ambiguity)**
   `crates/ucil-core/src/lib.rs:34` places `pub use cross_group::{...}` AFTER `pub use fusion::{...}` (line 28), even though `cross_group` precedes `fusion` alphabetically.
   The executor followed `scope_in` #18 verbatim ("Place the line BETWEEN the `fusion::*` re-export block (lines 26-27) and the `incremental::*` re-export (line 28)"). The same scope_in entry also asserts "alphabetical-by-module ordering is preserved", which is internally contradictory — the literal placement directive overrides the alphabetical claim. The `pub mod` declaration at `lib.rs:11` IS alphabetical (between `ceqp;` and `fusion;`). Planner ambiguity, not an executor failure; flagged for future planner attention.

### OK

- **Stub detection**: No `todo!()`, `unimplemented!()`, `raise NotImplementedError`, `pass`-only bodies, or trivial-default-return stubs anywhere in the diff. (`grep -nE 'todo!\(\)|unimplemented!\(\)|raise NotImplementedError'` empty across `cross_group.rs` and the verify scripts.)
- **Mocked critical collaborators**: None. The module is pure orchestration + math; no Serena / LSP / SQLite / LanceDB / Docker references in the new test code (`grep -nE 'serena|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker'` empty in `cross_group.rs`).
- **Skipped / ignored tests**: None. (`grep -nE '#\[ignore\]|\.skip\(|xfail|xit\(|@pytest\.mark\.skip'` empty.)
- **Weak assertions**: None. (`grep -nE 'assert!\(true\)|assert_eq!\(true|expect\(true\)'` empty.) Each frozen-test assertion carries an `(SAn) <semantic name>; left: …; right: …` panic body per DEC-0007.
- **Hallucinated imports**: Verified.
  - `crate::fusion::{group_weights_for, QueryType}` — both public in `crates/ucil-core/src/fusion.rs:374` (`QueryType`) and in the same module (`group_weights_for`).
  - `tokio::time::{timeout, sleep, Instant}` — already present via `tokio.workspace = true`.
  - `async_trait::async_trait` — added at `crates/ucil-core/Cargo.toml:24` as `async-trait.workspace = true`; workspace dep declared at root `Cargo.toml:120`.
  - `tracing::instrument` — `tracing.workspace = true` at `crates/ucil-core/Cargo.toml:13`.
  - `serde::{Serialize, Deserialize}`, `serde_json`, `std::collections::BTreeMap`, `std::path::PathBuf`, `std::time::Duration` — all already in scope.
- **Feature coverage**:
  - `P3-W9-F03` ↔ `cross_group::test_cross_group_parallel_execution` (line 900, `#[tokio::test]`, SA1..SA7 inline-rustdoc-numbered).
  - `P3-W9-F04` ↔ `cross_group::test_cross_group_rrf_fusion` (line 1129, `#[test]`, SA1..SA7 inline-rustdoc-numbered).
  - Both tests live at module root per DEC-0007 frozen-test-at-module-root, so the frozen selectors `cargo test -p ucil-core cross_group::test_cross_group_parallel_execution` and `cargo test -p ucil-core cross_group::test_cross_group_rrf_fusion` substring-match correctly.
- **Commit hygiene**: Conventional Commits format on every commit; every substantive feat/test/refactor commit carries `Phase: 3`, `Feature: P3-W9-F03` and/or `Feature: P3-W9-F04`, `Work-order: WO-0068`, and `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>` trailers. Gate-side `chore(verification-reports)` commits carry `Phase: 1` / `Phase: 2` correctly (the gate runs targeted those phases). No `--amend` after push detected; no force-push; no `--no-verify`.
- **Doc + public API**: Every `pub` item in `cross_group.rs` carries rustdoc — `Group` enum + each `G1..G8` variant; `GroupStatus` enum + each variant; `GroupHit`, `GroupResult`, `CrossGroupQuery`, `CrossGroupExecution`, `CrossGroupFusedHit`, `CrossGroupFusedOutcome` structs + every field; `GroupExecutor` trait + `group()` + `execute()`; the three constants `CROSS_GROUP_MASTER_DEADLINE`, `CROSS_GROUP_PER_GROUP_DEADLINE`, `CROSS_GROUP_RRF_K`; the two top-level functions `execute_cross_group` and `fuse_cross_group` (both with `# Examples` sections per scope_in #40). Module-level rustdoc cites master-plan §6.1 lines 585-641, §6.2 lines 643-658, §4 lines 113-116.
- **Word-ban contract (scope_in #37)**: Production-code (non-`#[cfg(test)]`) lines contain zero `mock|fake|stub` substrings (case-insensitive). Test helpers (`AvailableExec`, `SleepingExec`, `ErroringExec`, `dummy_query`, `dummy_hit`) all carry per-item `#[cfg(test)]` and live in the test partition of the file.
- **Constants and `#[default]` rationale**: `Group::G1` has the `understand_code → G1, G3, G5` master-plan §3.2 row 1 fallback rationale doc-comment (lines 41-44). `GroupStatus::Unavailable` has the §6.1 line 606 "safest pre-execution status" rationale (lines 130-132). Both per scope_in #41 + WO-0067 lessons-learned.
- **Frozen constants**: `CROSS_GROUP_MASTER_DEADLINE = Duration::from_millis(5_000)` (line 283), `CROSS_GROUP_PER_GROUP_DEADLINE = Duration::from_millis(4_500)` (line 294), `CROSS_GROUP_RRF_K: u32 = 60` (line 305). All carry §6.1 line 606 / §6.2 line 645 citations. Distinct naming from `G2_RRF_K` per scope_in #15 to prevent silent tuning-propagation.
- **Verify scripts**: `scripts/verify/P3-W9-F03.sh` and `scripts/verify/P3-W9-F04.sh` both carry `#!/usr/bin/env bash`, `set -euo pipefail`, `IFS=$'\n\t'`. Both run rename-drift greps for the frozen public symbols + frozen test selector AND `cargo test -p ucil-core cross_group::<test_name>`. Both end with `[OK] P3-W9-F0X …` and `exit 0` on green; both emit `[FAIL] …` with last-40-line tail of the test log on red.
- **Cargo.toml dep budget**: AT MOST one new dep (`async-trait.workspace = true`) added to `crates/ucil-core/Cargo.toml`. No `regex`, no `futures` standalone import. `git diff main -- crates/ucil-core/Cargo.toml | grep -E '^\+[a-z_-]+\s*=' | grep -v '^---' | wc -l` returns 1, satisfying AC37.
- **Disclosed deviations** (per `0068-ready-for-review.md`):
  - `per_group_deadline = CROSS_GROUP_PER_GROUP_DEADLINE` unconditionally (NOT capped by `master_deadline`), so SA4's tight 100 ms master deadline resolves deterministically. Documented inline at `cross_group.rs:404-416` and in the RFR.
  - Test relocation refactor: tests hoisted from `mod tests` to module root in commit `07436d5f` per DEC-0007, so the `cargo test cross_group::test_*` substring selectors resolve.
- **`tracing::instrument` posture**: `execute_cross_group` carries `#[tracing::instrument(name = "ucil.daemon.classify_and_dispatch", level = "debug", skip(executors), fields(query_target = %query.target, executor_count = executors.len()))]` per scope_in #43. `fuse_cross_group` (pure deterministic math) intentionally has no tracing per the same scope_in.

## Notes for the verifier

- M1 mutation: replace `tokio::time::timeout(per_group_deadline, executor.execute(query))` at `cross_group.rs:323` with `Ok(executor.execute(query).await)` directly — expected to fail SA2 (per-group timeout disposition) and SA4 (master-deadline trip) of `test_cross_group_parallel_execution`. Restore via `git checkout -- crates/ucil-core/src/cross_group.rs`.
- M2 mutation: in `fuse_cross_group` at `cross_group.rs:699`, replace `f64::from(CROSS_GROUP_RRF_K) + f64::from(*rank)` with `f64::from(CROSS_GROUP_RRF_K)` (drops the rank term) — expected to fail SA1 of `test_cross_group_rrf_fusion`. Restore via `git checkout --`.
- M3 mutation: in `fuse_cross_group` at `cross_group.rs:698`, replace `weights[*g as usize]` with `weights[(*g as usize) - 1]` — expected to fail SA6 OR panic on `usize::MAX` underflow for `Group::G1` (`0_usize - 1`). Restore via `git checkout --`.
- Verifier MUST run `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-core --summary-only --json | jq '.data[0].totals.lines.percent'` per AC29 standing-protocol coverage workaround (24th consecutive WO under this carve-out per WO-0067 lessons-learned).

## Verdict

**CLEAN** — no blockers. Three Warnings are advisory-only; all are pre-disclosed in either the WO scope_in directives or the RFR. Verifier may proceed.
