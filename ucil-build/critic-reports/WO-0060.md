# Critic Report: WO-0060

**Critic session**: crt-2026-05-07T-WO-0060
**Branch**: `feat/WO-0060-ucil-embeddings-chunker`
**HEAD reviewed**: `e7a5b2ba5ca5eb8de2e1fe32993c2c27cbcaeb4c` (`chore(wo): WO-0060 ready-for-review marker`)
**Merge base**: `90db4684e03baa28afb4689a8f57c26d319b28b2` (`main`)
**Diff scope**: 7 commits, 7 paths, +1148 / -0 (Cargo.lock 1, Cargo.toml 1, chunker.rs 920, lib.rs 13, sample.rs 51, verify script 83, RFR 79)
**Verdict**: **CLEAN with warnings**

The WO lands `crates/ucil-embeddings/src/chunker.rs` (NEW, 920 lines) implementing the real-tokenizer `EmbeddingChunker` per master-plan §12.2 line 1339. The pipeline is `parser.parse → ucil_treesitter::Chunker::chunk → re-tokenize via tokenizers::Tokenizer → collapse_to_signature for oversize` with a final source-order sort on `start_line`. The frozen acceptance test `chunker::test_embedding_chunker_real_fixture` lives at module root (DEC-0007 compliant) and carries 5 sub-assertions (chunk count, cap, real-tokenizer count, metadata round-trip, source order). Twelve additional unit tests inside `#[cfg(test)] mod tests {}` exercise every error variant, the oversize-fallback, the byte-budget truncation, and the helper functions.

I locally re-ran `cargo build -p ucil-embeddings --tests` (clean), `cargo clippy -p ucil-embeddings --all-targets -- -D warnings` (clean), `cargo test -p ucil-embeddings chunker::` (13 passed; 0 failed; 0 ignored), and `bash scripts/verify/P2-W8-F05.sh` (`[OK] P2-W8-F05`). No stubs, no critical-collaborator mocks, no skipped/ignored tests, no weak assertions, no hallucinated imports, no forbidden-path edits. The synthetic tokenizer is built via real `tokenizers::Tokenizer::from_str` parsing of a hand-crafted `tokenizer.json` payload — per DEC-0008 this is an internal trait/struct boundary and **not** a critical-dep mock.

## Findings

### Blockers (must fix before verifier)

**None.**

### Warnings (should be acknowledged before gate)

1. **Single 503-LOC commit** — `d43c6ce feat(embeddings): add EmbeddingChunker skeleton + helpers` lands +503 LOC (490 chunker.rs + 13 lib.rs) in one commit, well over the ~50-line soft target in `.claude/rules/commit-style.md`. The commit body explicitly cites DEC-0005 module-coherence + WO-0048 297-LOC + WO-0056 542-LOC + WO-0058 305-LOC + WO-0059 425-LOC NEW-module precedents. Splitting would leave dead-code / unused-import warnings under `#![deny(warnings)]`. Soft target with documented justification consistent with prior accepted precedents — accept.

2. **Single 289-LOC test commit** — `13656ae test(embeddings): add chunker negative-path unit tests for coverage` lands +289 / −2 in one commit (the `mod tests {}` block plus a refactor of `collapse_to_signature` to iterative byte-budget shrink). The body documents the iterative-shrink change inline. Tests-only commit with paired refactor — same shape as WO-0059's coverage-driven retry; soft-target deviation, accept.

3. **AC25 unbacked-uppercase rustdoc filter false-positives** — guidance grep `rg -nE '^\s*///.*\b[A-Z][A-Z_0-9]+\b' crates/ucil-embeddings/src/chunker.rs` after filtering out lines whose all-caps token is inside a backticked anchor still returns 14 hits (`chunker.rs:135,143,161,162,207,234,243,246,285,339,418,527,533,569`). All 14 are English prose tokens (`NOT`, `AST`, `JSON`, `UNK`, `OR`, `SHOULD`, `AC09`) — none are Rust identifiers. The authoritative gate `cargo clippy -p ucil-embeddings --all-targets -- -D warnings` (which actually runs `clippy::doc_markdown`) passes clean per my local rerun. Same false-positive shape as WO-0059 critic warning #4. Guidance grep, not authoritative — accept.

4. **`Cargo.lock` 1-line additive diff** — adding `ucil-treesitter` as a path dep produced exactly `+ "ucil-treesitter",` inside the `ucil-embeddings` deps block at `Cargo.lock:2738`. No version bumps in unrelated packages. Pre-flight in scope_in step 1 anticipated this; AC26 explicitly admits Cargo.lock to the allow-list. Accept.

5. **`collapse_to_signature` iterative byte-budget shrink (inline divergence from scope_in)** — the WO scope_in step 9 prescribed a single hard-truncation step. The executor changed it to `loop { signature = truncate_to_byte_budget(...); ... if count <= cap || budget == 0 break; budget /= 2 }` because the synthetic tokenizer's tight 2-bytes-per-token ratio (one space + one ASCII char per token) blew through the static `4`-bytes-per-token budget on the `collapse_to_signature_handles_single_line_oversize_content` test. Documented inline at `chunker.rs:427-443` and in the RFR's "Notable design decisions". Inline upstream-API-shape adaptation per WO-0058 lessons line 543 precedent; not novel design warranting an ADR. Accept.

6. **Source-order sort in `chunk()` (inline divergence from scope_in)** — `ucil_treesitter::Chunker` returns chunks in tree-sitter query-match order (interleaving classes and methods); the SA5 source-order assertion in the frozen test forced a `sort_by_key(|c| c.start_line)` post-pass. Documented inline at `chunker.rs:361-367` and in the RFR. Inline adaptation; required for downstream `LanceDB` indexer (P2-W8-F04) chunk-id stability. Accept.

7. **Manual `Debug` impl for `EmbeddingChunker` (inline divergence from scope_in)** — `ucil_treesitter::Parser` does not derive `Debug`, so `#[derive(Debug)]` on `EmbeddingChunker` was replaced with a hand-rolled formatter eliding parser internals. Documented inline at `chunker.rs:259-271` and in the RFR. Upstream-surface-forced adaptation; accept.

### OK

- **Stubs**: zero hits for `todo!()`, `unimplemented!()`, `raise NotImplementedError`, bare `pass`, or trivial-default returns in shipped code. `rg -nE 'todo!\(\)|unimplemented!\(\)|panic!\(".*not yet|TODO|FIXME' crates/ucil-embeddings/src/chunker.rs` → 0 matches. The mutation entries M1/M2/M3 in the RFR document early-return `Ok(Vec::new())` patches as **mutation tests**, not as shipped code. `unwrap()` appears only inside `#[cfg(test)]` blocks (`u32::try_from(...).unwrap_or(...)` is upper-bound saturation, not a panic point).
- **Mocked critical collaborators**: zero. The frozen acceptance test exercises real `tokenizers 0.23` `Tokenizer::from_str`/`encode` and real `ucil_treesitter::{Parser, Chunker}` against a real on-disk Rust source fixture. The synthetic tokenizer is constructed via `tokenizer_json.parse::<Tokenizer>()` — i.e. real `tokenizers` crate API parsing a synthetic JSON vocab. Per DEC-0008 + WO-0048 lessons line 363 + WO-0059 critic precedent, this is an internal trait/struct boundary, NOT a mock. `tempfile::NamedTempFile` is used for the invalid-JSON test — `tempfile` is not on the no-mock critical-collaborator list. `rg -i 'mock|fake|stub|MagicMock|Mockall' crates/ucil-embeddings/src/chunker.rs` returns only 3 doc-comment hits, all of which explicitly state "**NOT** a critical-dep mock".
- **Skipped/ignored tests**: zero. `rg -n '#\[ignore\]|\.skip\(|xit\(|@pytest\.mark\.skip|@pytest\.mark\.xfail' crates/ucil-embeddings/` returns zero. All 13 chunker tests are `#[test]` with no skip/ignore attributes.
- **Weak assertions**: zero hits for `assert!(true)`, `assert_eq!(true, true)`, `expect(true).toBe(true)` in any new test. Every `assert_eq!` / `assert!` carries an operator-readable panic message per WO-0051 lessons line 405 (e.g. `"oversize chunk must collapse below cap; got token_count={} for content {:?}"`).
- **Hallucinated imports**: zero. All `use` items in `chunker.rs` resolve: `std::path::{Path, PathBuf}` (std), `thiserror::Error` (workspace dep), `tokenizers::Tokenizer` (workspace dep), `ucil_treesitter::{Chunk, ChunkError, Chunker, Language, ParseError, Parser}` (path dep declared in `crates/ucil-embeddings/Cargo.toml:18`; all six items are re-exported by `crates/ucil-treesitter/src/lib.rs:15-16`). `mod tests {}` adds `std::io::Write` and `tempfile::NamedTempFile` — both resolve (std + workspace dev-dep at `Cargo.toml:20`). `cargo build -p ucil-embeddings --tests` passes clean per my local rerun.
- **Feature coverage**: `P2-W8-F05` (the only feature in the WO) is covered by the frozen `#[test] fn test_embedding_chunker_real_fixture()` at module root of `crates/ucil-embeddings/src/chunker.rs:576`, matching the frozen `feature-list.json:P2-W8-F05.acceptance_tests[0].selector = "-p ucil-embeddings chunker::"` (substring match) per DEC-0007. Test exercises 5 sub-assertions (SA1 chunk count positive; SA2 every chunk respects `MAX_CHUNK_TOKENS`; SA3 `chunks[0].token_count == content.split_whitespace().count()` — the real tokenizer guard; SA4 metadata round-trip; SA5 source-order monotonic). The 12 additional unit tests in `chunker::tests::*` add coverage on every error variant + helper.
- **Commit-message trailers**: 7/7 commits have `Phase: 2`, 7/7 have `Feature: P2-W8-F05`, 7/7 have `Work-order: WO-0060`. All 7 are Conventional Commits with valid types: `build` × 1, `feat` × 3, `test` × 2, `chore` × 1.
- **Commit-subject lengths**: 7/7 ≤ 70 chars (longest 67 chars: `test(embeddings): add chunker negative-path unit tests for coverage`). WO-0059 lessons line 607 ≤70 cap respected for all subjects on this WO.
- **Allow-list discipline**: `git diff --name-only main..feat/WO-0060-ucil-embeddings-chunker` returns exactly 7 paths, all in WO scope_in / AC26 allow-list:
  1. `Cargo.lock` (1-line additive)
  2. `crates/ucil-embeddings/Cargo.toml` (path dep)
  3. `crates/ucil-embeddings/src/chunker.rs` (NEW)
  4. `crates/ucil-embeddings/src/lib.rs` (re-exports + preamble)
  5. `crates/ucil-embeddings/tests/data/sample.rs` (NEW fixture)
  6. `scripts/verify/P2-W8-F05.sh` (NEW)
  7. `ucil-build/work-orders/0060-ready-for-review.md` (RFR marker)

  Zero forbidden-path touches: root `Cargo.toml` clean (AC27), `tests/fixtures/**` untouched (AC29), `ucil-build/feature-list.json` untouched (AC31), `ucil-master-plan-v2.1-final.md` untouched (AC32), all sibling crates / `.githooks/` / `.claude/` / `scripts/gate/` / `scripts/flip-feature.sh` untouched.
- **Public-API rustdoc**: every `pub` item in `chunker.rs` carries a doc comment — `MAX_CHUNK_TOKENS` (line 93-102), `EmbeddingChunkerError` + each of its 5 variants (line 125-187), `EmbeddingChunk` + each of its 5 fields (line 191-225), `EmbeddingChunker` (line 229-254), `from_tokenizer` (line 274-287), `from_tokenizer_path` with `# Errors` (line 295-313), `chunk` with `# Errors` (line 326-348), `pub(crate) retokenize_chunk` with `# Errors` (line 372-389), `pub(crate) collapse_to_signature` with `# Errors` (line 404-422). Module-level `//!` preamble cites master-plan §12.2 line 1339 verbatim + master-plan §18 Phase 2 Week 8 line 1786 + the F04/F08 deferral pattern. `lib.rs` preamble extends with a paragraph on `EmbeddingChunker` per the cumulative re-export-discipline.
- **`unsafe` discipline**: zero `unsafe` blocks introduced.
- **Tracing instrumentation**: `from_tokenizer_path` carries `#[tracing::instrument(name = "ucil.embeddings.chunker.load", level = "debug", ...)]`; `chunk` carries `#[tracing::instrument(name = "ucil.embeddings.chunker.chunk", level = "debug", ...)]` per master-plan §15.2 `ucil.<layer>.<op>` naming. Inner `tracing::debug!(count = out.len(), ...)` adds run-time observability per the WO scope_in.
- **`#![deny(warnings)]` + `clippy::pedantic` + `clippy::nursery`** preserved at the crate root; `cargo clippy -p ucil-embeddings --all-targets -- -D warnings` exits 0 per my local rerun. The single `#![allow(clippy::module_name_repetitions)]` at `chunker.rs:80` is documented inline (mirrors `ucil-treesitter::chunker`'s identical escape).
- **Mutation prep**: M1 (`chunk()` body neutered to early `Ok(Vec::new())`) → frozen test panics at SA1 chunk-count assertion; M2 (oversize-fallback neutered) → `retokenize_chunk_collapses_oversize_to_signature` panics at `token_count <= MAX_CHUNK_TOKENS`; M3 (`token_count = ast_chunk.token_count` byte-estimate) → frozen test panics at SA3 word-count assertion. RFR documents all 3 with restoration via `git checkout -- crates/ucil-embeddings/src/chunker.rs`.
- **Re-export discipline**: `lib.rs:41` is a single `pub use chunker::{EmbeddingChunk, EmbeddingChunker, EmbeddingChunkerError, MAX_CHUNK_TOKENS};` line covering all 4 new public symbols; line stays under rustfmt's 100-col budget without `#[rustfmt::skip]`. AC28 cumulative re-export-discipline streak: now 14 consecutive WOs cleared (WO-0042 → WO-0058 → WO-0059 → WO-0060).
- **Verify script integrity**: `scripts/verify/P2-W8-F05.sh` is `chmod +x`, fail-fasts on missing fixture (step 1), fail-fasts on missing module-root selector (step 2), runs `cargo test -p ucil-embeddings chunker:: -- --nocapture` with tolerant cargo-test/nextest summary regex (step 3), and self-lints via shellcheck with PATH-fallback (step 4). My local rerun: `[OK] P2-W8-F05`.
- **Stop-hook compliance**: `git status --porcelain` empty; `git rev-parse HEAD` (`e7a5b2b`) matches `git rev-parse @{u}` (`e7a5b2b`). Working tree clean, branch in sync with origin.
- **Inline divergences from WO scope_in are all upstream-API-shape adaptations** per WO-0058 lessons line 543 precedent: (1) manual `Debug` impl (Parser doesn't derive Debug); (2) synthetic tokenizer via JSON deserialisation (avoids `ahash` dep); (3) source-order sort post-pass (Chunker emits in query-match order); (4) iterative byte-budget shrink in `collapse_to_signature` (synthetic tokenizer's tight token-per-byte ratio defeats static budget). None constitute novel design decisions warranting an ADR — all four are documented inline + in the RFR's "Notable design decisions".

## Critic checklist

| # | Check | Result |
|---|-------|--------|
| 1 | Stub detection (`todo!()`, `unimplemented!()`, `NotImplementedError`, bare `pass`, trivial-default returns) | **CLEAN** |
| 2 | Mocked critical deps (Serena, LSP, SQLite, LanceDB, Docker, ONNX, tokenizers, ucil-treesitter) | **CLEAN** |
| 3 | Skipped/ignored tests (`#[ignore]`, `.skip()`, `xfail`, `xit`, `pytest.skip`) | **CLEAN** |
| 4 | Weak assertions (`assert!(true)`, `expect(true)`, etc.) | **CLEAN** |
| 5 | Hallucinated imports (`use` not resolving) | **CLEAN** |
| 6 | Feature coverage (`P2-W8-F05` test references behaviour) | **CLEAN** (frozen test + 12 unit tests) |
| 7 | Commit hygiene (Conventional Commits + Phase/Feature/Work-order trailers + size sanity) | **WARN** (one 503-LOC commit, one 289-LOC test commit — both with documented DEC-0005 carve-out) |
| 8 | Doc + public API (every `pub` has rustdoc) | **CLEAN** |

## Verdict

**CLEAN with warnings.** No blockers. All 7 warnings are either documented inline divergences with prior-WO precedent (1, 2, 5, 6, 7), guidance-grep false-positives (3), or expected by the WO scope_in (4). The diff is ready for the verifier.
