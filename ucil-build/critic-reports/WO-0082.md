# Critic Report: WO-0082

**Critic session**: crt-wo-0082-2026-05-08T2110Z
**Branch**: feat/WO-0082-test-runner-mcp-revival
**Ref**: f3aa92f35b5a8659aea4cb393f0c223675d54037
**Merge base**: `git merge-base main feat/WO-0082-test-runner-mcp-revival`
**Verdict**: CLEAN

## Findings

### Blockers (must fix before verifier)

_(none)_

### Warnings (should fix before gate, non-blocking)

1. **AC13 regex / rustfmt newline collision (carry-pattern from WO-0080)** —
   `acceptance_criteria` item #13 in `ucil-build/work-orders/0082-test-runner-mcp-revival.json`
   reads
   ```
   grep -qE '#\[tokio::test\][[:space:]]*async fn test_runner_manifest_health_check' \
       crates/ucil-daemon/tests/g8_plugin_manifests.rs
   ```
   Default `grep` operates per-line; rustfmt enforces a newline + indent between
   the `#[tokio::test]` attribute (line 248) and the `async fn
   test_runner_manifest_health_check() {` signature (line 249), so the literal
   ERE alternation never matches across that line break. The test IS
   correctly placed — `cargo test -p ucil-daemon --test g8_plugin_manifests
   g8_plugin_manifests::test_runner_manifest_health_check` resolves uniquely
   (substring match without `--exact`). Verifier should treat AC13 as the
   cargo selector resolution per the WO-0080 carry-pattern; the executor's
   RFR (`ucil-build/work-orders/0082-ready-for-review.md` §"Acceptance
   criteria sweep" row 13) flags this explicitly. Non-blocking.

2. **Test-file deletion-count tension with WO scope_in #8** — scope_in #8
   contains internally inconsistent guidance: sentence 2 demands
   `git diff main -- crates/ucil-daemon/tests/g8_plugin_manifests.rs |
   grep -E '^-' | grep -vE '^---' | wc -l` returns 0, but sentence 3 explicitly
   permits "leading file-level rustdoc comment block ... amended ONLY to update
   the file-level overview" (a, b, c sub-items). The actual deletion count is
   21 lines, all confined to (a) the leading `//!` rustdoc preamble at lines
   1–96 (file overview / DEC-0024+DEC-0025 revival narrative / dual-test
   selector citation per scope_in #8 (a)/(b)/(c)) and (b) the
   `FIRST_RUN_TIMEOUT_MS` doc-comment block at
   `crates/ucil-daemon/tests/g8_plugin_manifests.rs:108-118` (extending the
   doc to mention the npx fetch alongside the existing uvx fetch). The
   `mcp_pytest_runner_manifest_health_check` test BODY itself is untouched
   (verified — no `^-` deletions inside lines 132–197 of the new file). The
   executor's RFR §"What I verified locally" → "Test-body zero-deletions
   check" calls this out explicitly. The strict-literal sentence-2 reading
   is structurally incompatible with sentence-3's permission, so the
   verifier should treat the test-body-zero-deletions invariant (the spirit
   of scope_in #8) as the load-bearing check, not the file-wide
   `wc -l` literal. Non-blocking.

3. **Per-commit LOC over the ~50-line soft target** — three commits exceed
   the `.claude/rules/commit-style.md` soft target:
   - `f5c9e77 feat(plugins/testing): add test-runner G8 manifest ...` —
     +273 / -0. Single TOML file; ~30 lines of [plugin]/[capabilities]/
     [transport]/[lifecycle] tables and ~243 lines of mandated top-of-file
     comment block (per scope_in #3 + scope_in #36 §Provenance + scope_in #32
     npm-vs-serverInfo.name asymmetry). Splitting would yield a manifest
     without provenance — non-meaningful.
   - `93132a6 feat(scripts-verify): replace P3-W11-F07 TODO-stub ...` —
     +355 / -4. Replaces a 4-line TODO-stub with the full canonical-shape
     verify script (mirrors `scripts/verify/P3-W11-F08.sh` +
     `scripts/verify/P3-W11-F03.sh`); the python smoke driver inside the
     `python3 - <<'PY' ... PY` heredoc is one coherent unit and splitting
     it across multiple commits would yield non-runnable intermediate states.
   - `f3aa92f chore(rfr): WO-0082 ready-for-review marker` — +257 / -0. RFR
     document is a single artifact consumed by the harness; splitting is not
     meaningful.
   DEC-0005 module-coherence carve-out is cited in the WO scope_in #18 and
   in the additive-test commit (`4718120` body). Same precedent as
   WO-0072/0077/0079/0080. Non-blocking.

### OK

- **Stub detection (CLEAN)**: No `todo!()`, `unimplemented!()`, `raise
  NotImplementedError`, or trivial-default-return body introduced. The three
  `pass` matches in `scripts/verify/P3-W11-F07.sh` (lines 314, 322, 326) are
  all inside `try/except Exception: pass` cleanup blocks in the python smoke
  driver's `finally:` clause (closing `proc.stdin`, killing `proc`, closing
  `smoke_log` — best-effort resource cleanup), not implementation stubs.
  Same idiom as WO-0080 `scripts/verify/P3-W11-F03.sh` precedent.

- **Mocked critical collaborators (CLEAN)**: No `mock|fake|stub|MagicMock|
  Mockall` referencing `serena|SerenaClient|pyright|rust-analyzer|
  typescript-language-server|gopls|rusqlite|sqlx|lancedb|docker` in any
  added test, production manifest, install script, or verify script. The
  single `Mocking` token in `crates/ucil-daemon/tests/g8_plugin_manifests.rs:11`
  is in a forbidden-pattern declaration ("Mocking `tokio::process::Command`,
  the spawned MCP server, or the JSON-RPC dialogue is forbidden") inside the
  file-level rustdoc — exempt under the WO-0048 line 363 / WO-0069
  carve-out. The integration test spawns `npx -y
  @iflow-mcp/mcp-test-runner@0.2.1` as a real subprocess via
  `PluginManager::health_check_with_timeout` — full real-IO JSON-RPC over
  stdio.

- **Skipped/ignored tests (CLEAN)**: No `#[ignore]`, `.skip(`, `it.skip(`,
  `xit(`, `@pytest.mark.skip`, or `@pytest.mark.xfail` introduced. The
  `if skip_via_env() { return; }` guard at
  `crates/ucil-daemon/tests/g8_plugin_manifests.rs:127` and the `[SKIP]`
  echo at `scripts/verify/P3-W11-F07.sh:181` are explicitly permitted per
  WO scope_in #6 + WO-0077 precedent (single-gate
  `UCIL_SKIP_TESTING_PLUGIN_E2E` short-circuit; verifier MUST NOT set
  per scope_in #25).

- **Weak assertions (CLEAN)**: No `assert!(true)`, `assert_eq!(true, true)`,
  `expect(true).toBe(true)`, `assert True`, or `assert 1` introduced. New
  test asserts on substantive predicates:
  - `crates/ucil-daemon/tests/g8_plugin_manifests.rs:255` — `manifest.plugin.name == "test-runner"`.
  - `crates/ucil-daemon/tests/g8_plugin_manifests.rs:256` — `manifest.transport.kind == "stdio"`.
  - `crates/ucil-daemon/tests/g8_plugin_manifests.rs:258` — `!manifest.capabilities.provides.is_empty()`.
  - `crates/ucil-daemon/tests/g8_plugin_manifests.rs:262-269` — all capabilities under `testing.*` namespace.
  - `crates/ucil-daemon/tests/g8_plugin_manifests.rs:280` — `health.name == "test-runner"`.
  - `crates/ucil-daemon/tests/g8_plugin_manifests.rs:281-286` — `health.status == HealthStatus::Ok`.
  - `crates/ucil-daemon/tests/g8_plugin_manifests.rs:294-299` — `health.tools.len() == 1` (DEC-0025 §Decision point 3 single-tool dispatcher).
  - `crates/ucil-daemon/tests/g8_plugin_manifests.rs:304-308` — `health.tools[0] == "run_tests"` (canonical upstream tool name; M2 mutation target).
  Verify-script python driver asserts `tools[]` len == 1, `tools[0].name ==
  "run_tests"`, `inputSchema.framework.enum` len ≥6, and enum subset of
  canonical set `{bats, pytest, flutter, jest, go, rust, cargo, generic,
  vitest}` per AC#3.

- **Hallucinated paths (CLEAN)**: All `use` targets in
  `crates/ucil-daemon/tests/g8_plugin_manifests.rs` resolve against the
  branch state:
  - `use std::path::PathBuf` — std.
  - `use ucil_daemon::{HealthStatus, PluginManager, PluginManifest}` —
    re-exported from `crates/ucil-daemon/src/plugin_manager.rs` via
    `crates/ucil-daemon/src/lib.rs:pub use plugin_manager::{ ... HealthStatus,
    ... PluginManager, PluginManifest, ... }`.
  All sibling-script and ADR references in the manifest comments + RFR are
  verified present:
  `scripts/devtools/install-eslint-mcp.sh` (100755),
  `scripts/devtools/install-mcp-pytest-runner.sh` (100755),
  `scripts/verify/P3-W11-F03.sh` (100755),
  `scripts/verify/P3-W11-F08.sh` (100755),
  `ucil-build/decisions/DEC-0024-revive-test-runner-mcp-via-npm.md`,
  `ucil-build/decisions/DEC-0025-correct-test-runner-mcp-tool-surface.md` —
  all present on the branch.

- **Feature coverage (CLEAN)**: Sole feature `P3-W11-F07` (description:
  "test-runner-mcp plugin installed; unified test execution covers Rust
  (cargo nextest), Python (pytest), and TypeScript (vitest) via MCP
  protocol") is covered by:
  - `crates/ucil-daemon/tests/g8_plugin_manifests.rs::test_runner_manifest_health_check`
    (NEW; line 248) — drives `npx -y @iflow-mcp/mcp-test-runner@0.2.1` as
    real subprocess and asserts the canonical `run_tests` dispatcher tool
    is present.
  - `scripts/verify/P3-W11-F07.sh` (replaces TODO-stub) — drives independent
    JSON-RPC `tools/list` capture and asserts `tools[]` len == 1 +
    `tools[0].name == "run_tests"` + framework enum has ≥6 values from
    canonical set including {rust, pytest, jest} (the F07 master-plan
    §4.8-line-405-mandated frameworks).
  Feature description's key nouns (test-runner-mcp, run_tests, framework,
  Rust/Python/TypeScript) and verbs (run, test, execute) all directly
  exercised by both the cargo test and the verify script's tool-level
  smoke.

- **Commit hygiene (CLEAN)**: All 5 commits on `main..f3aa92f` carry
  Conventional Commits subjects (`feat(plugins/testing):`,
  `feat(scripts/devtools):`, `test(daemon):`, `feat(scripts-verify):`,
  `chore(rfr):`) and the canonical UCIL trailer block:
  ```
  Phase: 3
  Feature: P3-W11-F07
  Work-order: WO-0082
  Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
  ```
  No merge commits (`git log feat/WO-0082-test-runner-mcp-revival ^main
  --merges` returns 0 matches per AC#19 / WO-0070 §planner #4). No
  `--amend`-looking history rewrites. Three commit-size soft-cap warnings
  enumerated above.

- **Doc + public API (CLEAN)**: No new `pub fn`, `pub struct`, `pub enum`,
  `pub mod`, or `pub use` items introduced. The new `async fn
  test_runner_manifest_health_check` (line 249) is module-private (test
  function inside `mod g8_plugin_manifests`). Existing `pub` re-exports in
  `crates/ucil-daemon/src/lib.rs` are untouched. The
  `test_runner_manifest_health_check` function itself carries a
  comprehensive `///`-doc block (lines 222–246) explaining the DEC-0025
  §Decision point 3 contract; the function-level doc satisfies the
  spirit of the soft Doc check even though tests don't strictly require
  doc comments.

- **Forbidden paths untouched (CLEAN)**: `git diff main..feat/WO-0082-...`
  produces no output for any of the WO `forbidden_paths` entries
  (`ucil-build/feature-list.json`, `ucil-build/feature-list.schema.json`,
  `ucil-master-plan-v2.1-final.md`, `tests/fixtures/**`, `scripts/gate/**`,
  `scripts/flip-feature.sh`, `crates/ucil-daemon/tests/plugin_manifests.rs`,
  `crates/ucil-daemon/tests/g{3..7}_plugin_manifests.rs`,
  `scripts/verify/P3-W11-F0{2,3,4,8}.sh`, `plugins/quality/**`,
  `plugins/knowledge/**`, `plugins/architecture/**`, `plugins/context/**`,
  `plugins/platform/**`, `plugins/testing/mcp-pytest-runner/**`).

- **Word-ban grep (CLEAN)**: `grep -qiE 'mock|fake|stub'` against
  production-side files (`plugins/testing/test-runner/plugin.toml`,
  `scripts/devtools/install-test-runner-mcp.sh`,
  `scripts/verify/P3-W11-F07.sh`) returns no matches — manifest, install
  script, and verify script are clean. Test-file `Mocking ... is
  forbidden` declaration exempt under WO-0069 carve-out.

- **No `std::process::Command` in g8 tests (CLEAN)**: `grep -nE
  'std::process::Command' /tmp/wo0082-g8_plugin_manifests.rs` returns no
  matches. Tokio variant only — per `.claude/rules/rust-style.md`
  §Async + WO-0075 §executor W1 lesson.

- **No `unsafe { ... set_var }` in g8 tests (CLEAN)**: `grep -nE
  'unsafe[\s]*\{[^}]*set_var'` returns no matches. test-runner-mcp is
  self-resolving at the MCP-server level (npx-managed package bundles
  Node + @modelcontextprotocol/sdk + transitive deps); no parent-process
  env-var mutation needed per scope_in #17 + scope_in #34. WO-0076
  W1+W2 unsafe-set_var debt does NOT apply.

- **Pinned-immutable-ref policy (CLEAN)**: `grep -qE '"(main|latest|
  master|head|develop|dev|nightly)"' plugins/testing/test-runner/plugin.toml`
  returns no matches. Frozen semver `0.2.1` per DEC-0025 §Decision point 1
  + master-plan §13.

- **Bare `test-runner-mcp` name absent (CLEAN)**: `grep -qE
  '"test-runner-mcp"' plugins/testing/test-runner/plugin.toml` returns no
  matches. DEC-0025 §Context Error 1 honoured — only the third-party
  scoped mirror `@iflow-mcp/mcp-test-runner@0.2.1` is referenced in
  `transport.args`.

- **Verify-script fixtures invariant (CLEAN)**: `scripts/verify/
  P3-W11-F07.sh` does not write to `tests/fixtures/**` (uses `mktemp -d`
  for the SMOKE_TMPDIR per scope_in #11 + WO-0074 §executor #5 +
  WO-0076 scope_in #5/#16 invariant).

- **Executable bits (CLEAN)**: `git ls-tree feat/...` confirms
  `scripts/devtools/install-test-runner-mcp.sh` and
  `scripts/verify/P3-W11-F07.sh` are both mode 100755 per AC#5 + AC#6.

- **Mutation contract documented (CLEAN)**: RFR
  (`ucil-build/work-orders/0082-ready-for-review.md`) §"Mutation contract"
  documents both M1 (transport.command poison) and M2 (expected-tool-name
  regression) per scope_in #26 with the canonical 6-row contract table
  shape (file path, exact patch, targeted selector, expected panic,
  restore command, pre-mutation md5). Mirrors WO-0067/0068/0069/0070/0072/
  0077/0079/0080 verbatim. Mutation contract is a mandatory M1+M2 set per
  WO scope_in #12/#13.

## Summary

WO-0082 is a clean implementation of the DEC-0025-corrected test-runner G8
plugin manifest revival. The deferral chain (`DEC-0019 → DEC-0020 →
DEC-0021`) is fully discharged by the matching revival chain (`DEC-0022 →
DEC-0023 → DEC-0024+DEC-0025`); WO-0081's empirical-validation work
correctly redirected the planner to the live-captured upstream surface
(single-tool `run_tests` dispatcher, framework enum 7 values) instead of
the DEC-0024 misread (6 separate framework-specific tools). The 4
production-side commits (manifest, install script, additive test, replaced
verify script) plus the RFR marker total 5 well-formed commits with
correct UCIL trailers. Three Warnings are documentation/interpretation
issues consistent with the WO-0080 carry-pattern set; no Blockers.

**Verdict: CLEAN — proceed to verifier.**
