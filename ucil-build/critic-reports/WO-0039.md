# Critic Report: WO-0039

**Critic session**: crt-20260419-WO-0039
**Branch**: feat/WO-0039-watchman-backend-retry-with-pathguard
**Ref**: ce5ea6aea5fd431ee15c26e4bfb49eee4607c558
**Merge base**: 4f6a8de (effective main)
**Work-order**: `ucil-build/work-orders/0039-watchman-backend-retry-with-pathguard.json`
**Features**: P1-W3-F03
**Verdict**: CLEAN

## Diff overview

```
Cargo.lock                                |  26 ++
Cargo.toml                                |   6 +
crates/ucil-daemon/Cargo.toml             |   2 +
crates/ucil-daemon/src/lib.rs             |  13 +-
crates/ucil-daemon/src/session_manager.rs |  24 +
crates/ucil-daemon/src/test_support.rs    |  88 ++++ (new file)
crates/ucil-daemon/src/watcher.rs         | 754 +++++++++++++++++++++++++++---
7 files changed, 853 insertions(+), 60 deletions(-)
```

10 commits; `forbidden_paths` clean (no touches to master plan, feature-list, gate scripts, fixtures, disallowed crates, disallowed daemon modules). `session_manager.rs` edits are confined to test bodies — runtime methods `create_session`, `detect_branch`, `discover_worktrees` are byte-identical.

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should consider before gate)

1. **Commit size over soft cap** — two commits exceed the `.claude/rules/commit-style.md` ~50 LOC soft target and the critic >200-line threshold:
   - `5322352` `feat(daemon): FileWatcher::new_with_backend dispatch to 3 backends …` — **354 LOC** in `watcher.rs`. Justified: introduces three private constructors (`new_notify_debounced`, `new_poll`, `new_watchman`) + `BackendHandle` enum + `WatchmanEvent` struct + shared `spawn_debouncer_forwarder` helper; splitting further would break the public-API introduction across half-broken intermediate states (WO-0026 call sites must remain byte-identical, which requires the delegate + dispatch in one commit). DEC-0001 / DEC-0005 module-coherence precedent applies.
   - `e76e356` `test(daemon): add 7 watcher tests under PathRestoreGuard + doc polish` — **211 LOC** in `watcher.rs`. Explicitly permitted by WO scope_in §29 ("test-introduction commit may exceed under DEC-0001/DEC-0005 precedent") and matches the pattern used by WO-0023's `call_hierarchy` test bundle.

   Not a blocker — both commits have clear module-coherence rationales in their bodies.

2. **Cosmetic commit `ce5ea6a`** (`test(daemon): annotate PATH-mutation lines with PathRestoreGuard reference`) exists solely to satisfy the line-local acceptance grep at criterion #22 (`! grep 'std::env::(set_var|remove_var).*"PATH"' … | grep -vE 'PathRestoreGuard|test_support'`). All five mutation sites were already inside `PathRestoreGuard::new()`-fenced tests; the trailing `// under PathRestoreGuard` comments document that invariant adjacent to each call rather than relying on the `let _guard = …` three lines above. Content is accurate (not deceptive) but the commit is essentially spec-test-driven — acceptable, flagged for transparency.

### OK

- **Stub detection**: no `todo!()`, `unimplemented!()`, `NotImplementedError`, `raise NotImplementedError`, bare `pass`, or trivial `return None/Default/0/""` stubs in `crates/ucil-daemon/src/{watcher.rs,test_support.rs,session_manager.rs,lib.rs}`. The `Err(err) => { tracing::debug!(…); None }` arm inside `detect_watchman` (watcher.rs:182-188) is the correct `Option::None` return on a genuine `which::which` failure, not a stub.
- **Mocking of critical collaborators**: no Serena, LSP, `pyright`, `rust-analyzer`, `typescript-language-server`, `gopls`, `rusqlite`, `sqlx`, `lancedb`, or Docker mocks in any changed test. The `create_watchman_shim` helper (watcher.rs:1081-1103) writes a real executable shell script to a tempdir so `which::which("watchman")` resolves it on a scoped `PATH` — this is a filesystem shim, not a mock of a critic-restricted collaborator. (Watchman is not on the forbidden-mock list in `.claude/agents/critic.md`.)
- **Skipped/ignored tests**: grep for `#[ignore]`, `.skip(`, `xfail`, `xit(`, `@pytest.mark.skip`, `it.skip` on the diff — zero hits.
- **Weak assertions**: no `assert!(true)`, `assert_eq!(true, true)`, `assert True`, or `expect(true).toBe(true)` in the diff. Every new test asserts a non-trivial post-condition (kind mapping, count bounds, backend enum equality, source equality, path suffix match, canonicalised equality, empty-result bounds).
- **Hallucinated imports**: all new `use` / module references resolve.
  - `use crate::test_support::PathRestoreGuard as RestorePath` (watcher.rs:1077) → exists at `crates/ucil-daemon/src/test_support.rs:58-80`.
  - `crate::test_support::env_guard` (session_manager.rs:×6) → exists at `test_support.rs:49-56`.
  - `which::which("watchman")` (watcher.rs:178) → `which = "7"` added at workspace root Cargo.toml:94-95 and `which = { workspace = true }` at `crates/ucil-daemon/Cargo.toml` deps.
  - `walkdir::WalkDir` (watcher.rs:213) → already at workspace root Cargo.toml:92, newly added to daemon deps.
  - `notify_debouncer_full::new_debouncer_opt` (watcher.rs:67) → existing dep, new API surface used.
  - `tokio::io::{AsyncBufReadExt, AsyncWriteExt, BufReader}` (watcher.rs:662) → tokio already depends on `io-util` transitively through existing usage.
  - `serde::Deserialize` / `serde_json::json!` / `serde_json::from_str` → existing workspace deps.
  - `std::os::unix::fs::PermissionsExt` (watcher.rs:1084) → inside `#[cfg(all(test, unix))]`; target-gated correctly.
- **Feature coverage for P1-W3-F03**: seven new tests reference the feature's behaviour:
  - `watcher::test_watchman_detection` (watcher.rs:1114) — frozen F03 selector, probes `detect_watchman` negative + positive.
  - `watcher::test_count_files_capped_below_cap` (watcher.rs:1141).
  - `watcher::test_count_files_capped_stops_early` (watcher.rs:1152).
  - `watcher::test_auto_select_backend_returns_notify_when_watchman_absent` (watcher.rs:1170).
  - `watcher::test_auto_select_backend_returns_watchman_when_available_and_above_threshold` (watcher.rs:1184).
  - `watcher::test_auto_select_backend_returns_notify_when_below_threshold` (watcher.rs:1202).
  - `watcher::test_poll_backend_delivers_events` (watcher.rs:1221) — exercises `FileWatcher::new_with_backend(…, WatcherBackend::Poll)`.
- **Commit hygiene**: all 10 commits use Conventional Commits format with a `<type>(<scope>): <summary>` subject and `Phase: 1 / Feature: P1-W3-F03 / Work-order: WO-0039` trailers. Types are `build`, `feat` (×6), `test` (×3). No `--amend` against published history; no force-push; no `--no-verify`. Commit bodies explain motivation (WO-0027 race, DEC-0011 fix, scope decisions).
- **Doc + public API**: every new `pub` item has a rustdoc:
  - `pub const WATCHMAN_AUTO_SELECT_THRESHOLD` (watcher.rs:94) — docs cite master-plan §2 line 138.
  - `pub const POLL_WATCHER_INTERVAL` (watcher.rs:105) — docs justify the 2 s choice vs. notify's 30 s default.
  - `pub enum WatcherBackend` (watcher.rs:129) with per-variant docs on `NotifyDebounced`, `Watchman`, `Poll`.
  - `pub struct WatchmanCapability { pub binary: PathBuf }` (watcher.rs:152) with struct-level and field-level docs.
  - `pub fn detect_watchman` (watcher.rs:174) — behaviour, tracing span, caching note.
  - `pub fn count_files_capped` (watcher.rs:211) — `O(cap)` complexity note + return-range contract.
  - `pub fn auto_select_backend` (watcher.rs:243) — decision matrix + rationale for never returning `Poll`.
  - `pub fn FileWatcher::new_with_backend` (watcher.rs:546) — per-backend dispatch, tracing span, `# Errors` section.
  - Two new `WatcherError` variants (`WatchmanSpawn`, `WatchmanDecode`) have per-variant rustdoc explaining why `#[from]` conversions are deliberately absent.
- **Scope compliance**: `forbidden_paths` clean; no edits to `ucil-master-plan-v2.1-final.md`, `feature-list.json`, `scripts/gate/**`, `scripts/flip-feature.sh`, `tests/fixtures/**`, `.githooks/**`, `.claude/hooks/**`, disallowed crates, or the twelve named disallowed daemon modules. Runtime methods in `session_manager.rs` are byte-identical — only test bodies receive the `let _g = crate::test_support::env_guard();` fence.
- **DEC-0011 pattern compliance**: every `std::env::set_var("PATH", …)` site in `watcher.rs` is inside a test that opens with `let _guard = RestorePath::new();` (shadow-import for `PathRestoreGuard`). Every `SessionManager::*` call in the fenced tests is preceded by `let _g = crate::test_support::env_guard();` with a `// DEC-0011` justification comment. The `clippy::await_holding_lock` allow on the six fenced tests carries a one-line justification referencing `test_session_state_tracking`'s docblock (single-thread tokio runtime, `!Send` guard cannot cross threads, the hold across `.await` is the fence itself).

## Verdict

**CLEAN** — no blockers. Diff adheres to the WO-0039 scope (WO-0027 re-land on main + DEC-0011 crate-scoped `test_support` baked in). Two warnings flagged for transparency (large commits are well-justified; cosmetic comment-annotation commit documents a real invariant rather than deceiving the grep).

Verifier may proceed.
