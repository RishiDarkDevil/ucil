# Critic Report: WO-0039 (retry attempt 1 re-review)

**Critic session**: crt-20260419T0130-WO-0039-retry1
**Branch**: feat/WO-0039-watchman-backend-retry-with-pathguard
**Ref (branch tip)**: ce5ea6aea5fd431ee15c26e4bfb49eee4607c558
**Merge base (effective main)**: 4f6a8de
**Work-order**: `ucil-build/work-orders/0039-watchman-backend-retry-with-pathguard.json`
**Features**: P1-W3-F03
**Verdict**: **CLEAN**

## Context — retry attempt 1 produced zero code edits

The executor was re-invoked on WO-0039 under the "a PRIOR verifier attempt rejected
your work" contract, but **none of the preconditions held**:

- `ucil-build/rejections/WO-0039.md` — **does not exist**.
- `ucil-build/verification-reports/root-cause-WO-0039.md` — **does not exist**.
- `ucil-build/verification-reports/WO-0039.md` — **exists, verdict PASS** (vrf-fb23f4aa, 2026-04-18T19:46:57Z).
- `ucil-build/critic-reports/WO-0039.md` (prior critic review `017cedf`) — **verdict CLEAN**.
- Main has `9618fbd chore(verifier): WO-0039 PASS — flip P1-W3-F03 to passes=true`.

The executor correctly halted and wrote
`ucil-build/escalations/20260419-0051-executor-retry-WO-0039-premise-wrong.md`
rather than mutating code or cherry-picking already-present impl commits. That
escalation landed on `main` as `fdacd40`; it does NOT touch the feat branch.

**Branch tip is byte-identical to the prior CLEAN review**:

```
$ git log --oneline 4f6a8de..ce5ea6a
ce5ea6a test(daemon): annotate PATH-mutation lines with PathRestoreGuard reference
6dbb7e9 feat(daemon): re-export WO-0039 watcher public surface from lib.rs
6bfbb74 test(daemon): fence session_manager git-spawning tests with env_guard (DEC-0011)
e76e356 test(daemon): add 7 watcher tests under PathRestoreGuard + doc polish
5322352 feat(daemon): FileWatcher::new_with_backend dispatch to 3 backends (notify/poll/watchman)
f4bf4ca feat(daemon): extend WatcherError with WatchmanSpawn + WatchmanDecode variants
89743ba feat(daemon): add detect_watchman + count_files_capped + auto_select_backend
301efbe feat(daemon): add WatcherBackend enum + WatchmanCapability + constants
4d3f33b feat(daemon): add crate-scoped test_support module for PATH mutex (DEC-0011)
4034bc3 build(workspace): add which workspace dep + walkdir to daemon deps
```

Same 10 commits, same tip `ce5ea6a`. All retry-attempt-1 activity was
meta/admin (escalation file on main); the artifact under critic review is
unchanged.

## Diff overview (unchanged since prior CLEAN review)

```
 Cargo.lock                                |  26 ++
 Cargo.toml                                |   6 +
 crates/ucil-daemon/Cargo.toml             |   2 +
 crates/ucil-daemon/src/lib.rs             |  13 +-
 crates/ucil-daemon/src/session_manager.rs |  24 +
 crates/ucil-daemon/src/test_support.rs    |  88 ++++ (new file)
 crates/ucil-daemon/src/watcher.rs         | 754 +++++++++++++++++++++++++++---
 7 files changed, 853 insertions(+), 60 deletions(-)
```

`forbidden_paths` clean — no touches to master plan, `feature-list.json`,
`feature-list.schema.json`, `scripts/gate/**`, `scripts/flip-feature.sh`,
`tests/fixtures/**`, `.githooks/**`, `.claude/hooks/**`, disallowed crates,
or the twelve named disallowed daemon modules. `session_manager.rs` edits
are confined to test bodies — runtime methods `create_session`,
`detect_branch`, `discover_worktrees` are byte-identical.

## Findings

### Blockers (must fix before verifier)

_None._ (The feature has in fact already been verified PASS and flipped; this
re-review exists only to discharge the critic step of the retry cycle.)

### Warnings (transparency flags, unchanged from prior review)

1. **Commit size over soft cap** — two commits exceed the
   `.claude/rules/commit-style.md` ~50 LOC soft target:
   - `5322352` — **295 insertions** in `watcher.rs`. Justified: introduces
     three private constructors (`new_notify_debounced`, `new_poll`,
     `new_watchman`) + `BackendHandle` enum + `WatchmanEvent` struct +
     shared `spawn_debouncer_forwarder` helper. Splitting further would
     break the public-API introduction across half-broken intermediate
     states (WO-0026 call sites must stay byte-identical → delegate +
     dispatch must land in one commit). DEC-0001 / DEC-0005 module-
     coherence precedent applies.
   - `e76e356` — **222 insertions** introducing 7 watcher tests under
     `PathRestoreGuard`. Explicitly permitted by WO scope_in §29 ("test-
     introduction commit may exceed under DEC-0001/DEC-0005 precedent")
     and matches the pattern used by WO-0023's `call_hierarchy` test
     bundle.

   Not a blocker — both commits have clear module-coherence rationales.

2. **Cosmetic tip commit `ce5ea6a`** (5 insertions) appends `// under
   PathRestoreGuard` trailing comments on each of the 5
   `std::env::set_var("PATH", …)` sites so that the line-local
   acceptance grep at WO-0039 `acceptance_criteria[21]`
   (`! grep -nE 'std::env::(set_var|remove_var).*"PATH"' … | grep -vE
   'PathRestoreGuard|test_support'`) passes without false positives. All
   five mutation sites are already inside `PathRestoreGuard::new()`-fenced
   tests — the trailing comments DOCUMENT that invariant adjacent to each
   call rather than relying on the `let _guard = …` three lines above.
   Not deceptive; flagged for transparency.

3. **Retry-loop meta anomaly** (out of critic jurisdiction, noted for
   triage). The retry that spawned this review was fired by the harness
   even though the feature was already flipped `passes=true` on main and
   no `rejections/WO-0039.md` existed. Bucket B harness work is needed
   (see escalation `20260419-0051-…`); the `feat/WO-0039-…` → main
   fast-forward merge also appears to have been skipped (verifier
   responsibility per root CLAUDE.md workflow step 6). Neither issue
   alters the critic verdict on the code diff.

### OK

- **Stub detection** — no `todo!()`, `unimplemented!()`,
  `NotImplementedError`, `raise NotImplementedError`, bare `pass`, or
  trivial `return None / Default::default() / {} / literal` stubs in any
  of `crates/ucil-daemon/src/{watcher.rs,test_support.rs,session_manager.rs,lib.rs}`.
  The `Err(err) => { tracing::debug!(…); None }` arm in `detect_watchman`
  (watcher.rs:182-188) is a legitimate `Option::None` return on a
  `which::which` failure, exercised by the mutation check and by the
  negative branch of `test_watchman_detection`.

- **Mocked critical collaborators** — no Serena, LSP, `pyright`,
  `rust-analyzer`, `typescript-language-server`, `gopls`, `rusqlite`,
  `sqlx`, `lancedb`, or Docker mocks in any changed test. The
  `create_watchman_shim` helper (watcher.rs:1081-1103) writes a real
  executable shell script to a tempdir so that `which::which("watchman")`
  resolves it under a scoped `PATH` — this is a filesystem shim, not a
  mock of a critic-restricted collaborator. (`watchman` is not on the
  forbidden-mock list in `.claude/agents/critic.md`; an integration-tier
  subscription test is explicitly deferred by WO-0039 `scope_out`.)

- **Skipped / ignored tests** — `grep -nE '#\[ignore\]|\.skip\(|xfail|
  it\.skip|xit\(|@pytest\.mark\.(skip|xfail)'` on the diff returns zero
  hits.

- **Weak assertions** — no `assert!(true)`, `assert_eq!(true, true)`,
  `assert True`, or `expect(true).toBe(true)` in the diff. Every new test
  asserts a non-trivial post-condition (kind mapping, count bounds,
  backend enum equality, source equality, path suffix match,
  canonicalised equality, empty-result bounds).

- **Hallucinated imports** — all new `use` / path references resolve:
  - `use crate::test_support::PathRestoreGuard as RestorePath`
    (watcher.rs:1077) → `crates/ucil-daemon/src/test_support.rs:58-80`.
  - `crate::test_support::env_guard` (session_manager.rs, 6 sites) →
    `test_support.rs:49-56`.
  - `which::which("watchman")` (watcher.rs:178) ← `which = "7"` added at
    workspace root `Cargo.toml:94-95` and `which = { workspace = true }`
    at `crates/ucil-daemon/Cargo.toml` deps.
  - `walkdir::WalkDir` (watcher.rs:213) ← `walkdir = "2"` already at
    workspace root `Cargo.toml:92`, newly added to daemon deps.
  - `notify_debouncer_full::new_debouncer_opt` (watcher.rs:67) ←
    existing dep, new API surface used.
  - `tokio::io::{AsyncBufReadExt, AsyncWriteExt, BufReader}`
    (watcher.rs:662) ← tokio `io-util` feature already enabled
    transitively.
  - `serde::Deserialize`, `serde_json::{json, from_str}` ← existing
    workspace deps.
  - `std::os::unix::fs::PermissionsExt` (watcher.rs:1084) inside
    `#[cfg(all(test, unix))]` — target-gated correctly.

- **Feature coverage for P1-W3-F03** — seven new tests reference the
  feature's behaviour; each asserts a genuine post-condition:
  - `watcher::test_watchman_detection` (watcher.rs:1114) — **frozen F03
    selector**, probes `detect_watchman` negative + positive branches.
  - `watcher::test_count_files_capped_below_cap` (watcher.rs:1141).
  - `watcher::test_count_files_capped_stops_early` (watcher.rs:1152).
  - `watcher::test_auto_select_backend_returns_notify_when_watchman_absent`
    (watcher.rs:1170).
  - `watcher::test_auto_select_backend_returns_watchman_when_available_and_above_threshold`
    (watcher.rs:1184).
  - `watcher::test_auto_select_backend_returns_notify_when_below_threshold`
    (watcher.rs:1202).
  - `watcher::test_poll_backend_delivers_events` (watcher.rs:1221) —
    exercises `FileWatcher::new_with_backend(…, WatcherBackend::Poll)`.

- **Commit hygiene** — all 10 commits on `feat/WO-0039-…` use
  Conventional Commits with `<type>(<scope>): <summary>` subject and
  `Phase: 1 / Feature: P1-W3-F03 / Work-order: WO-0039` trailers (grep
  over commit bodies finds all three trailers on every commit). Types:
  `build`, `feat` (×6), `test` (×3). No `--amend` against published
  history; no force-push; no `--no-verify`. Bodies explain motivation
  (WO-0027 race, DEC-0011 fix, scope decisions).

- **Doc + public API** — every new `pub` item has a rustdoc:
  - `pub const WATCHMAN_AUTO_SELECT_THRESHOLD` (watcher.rs:94) — cites
    master-plan §2 line 138.
  - `pub const POLL_WATCHER_INTERVAL` (watcher.rs:105) — justifies 2 s
    vs notify's 30 s default.
  - `pub enum WatcherBackend` (watcher.rs:129) with per-variant docs on
    `NotifyDebounced`, `Watchman`, `Poll`.
  - `pub struct WatchmanCapability { pub binary: PathBuf }`
    (watcher.rs:152) with struct-level and field-level docs.
  - `pub fn detect_watchman` (watcher.rs:174) — behaviour, tracing span,
    no-caching note.
  - `pub fn count_files_capped` (watcher.rs:211) — `O(cap)` complexity
    note + `[0, cap + 1]` return-range contract.
  - `pub fn auto_select_backend` (watcher.rs:243) — decision matrix +
    rationale for never auto-returning `Poll`.
  - `pub fn FileWatcher::new_with_backend` (watcher.rs:546) — per-backend
    dispatch, tracing span, `# Errors` section.
  - Two new `WatcherError` variants (`WatchmanSpawn`, `WatchmanDecode`)
    have per-variant rustdoc explaining the deliberately absent `#[from]`.

- **Scope compliance** — `forbidden_paths` clean; no edits to
  `ucil-master-plan-v2.1-final.md`, `feature-list.json`,
  `feature-list.schema.json`, `scripts/gate/**`, `scripts/flip-feature.sh`,
  `tests/fixtures/**`, `.githooks/**`, `.claude/hooks/**`, disallowed
  crates, or the twelve named disallowed daemon modules. Runtime methods
  in `session_manager.rs` are byte-identical; only test bodies receive
  the `let _g = crate::test_support::env_guard();` fence.

- **DEC-0011 pattern compliance** — every `std::env::set_var("PATH", …)`
  site in `watcher.rs` is inside a test that opens with
  `let _guard = RestorePath::new();` (shadow-import for
  `PathRestoreGuard`). Every `SessionManager::*` call in the fenced
  tests is preceded by `let _g = crate::test_support::env_guard();`
  with a `// DEC-0011: fence PATH mutations in watcher tests`
  justification comment. The `#[allow(clippy::await_holding_lock)]` on
  each of the six fenced tests carries a one-line justification pointing
  back to `test_session_state_tracking`'s docblock (single-thread tokio
  runtime, `!Send` guard cannot cross threads, the hold across `.await`
  is the intentional fence).

- **Test shape** — the 7 new watcher tests live at module root under
  `#[cfg(test)] #[test]` / `#[cfg(test)] #[tokio::test]`, matching the
  existing 8 WO-0026 watcher tests and the DEC-0005 frozen-selector
  requirement. No new `mod tests { }` wrapper was introduced for
  watcher. The existing `mod tests { }` block in `session_manager.rs` is
  preserved (predates DEC-0005, out of scope for this WO).

- **`tokio::process::Command` in async paths** — `new_watchman` uses
  `tokio::process::Command` (not `std::process::Command`) per
  `rust-style.md`; `kill_on_drop(true)` is set so the subprocess dies
  with the watcher.

- **`#[non_exhaustive]` on error enum** — `WatcherError` keeps its
  `#[non_exhaustive]` attribute; new variants `WatchmanSpawn` and
  `WatchmanDecode` land without `#[from]` to keep call-site conversion
  explicit and deliberate.

## Verdict

**CLEAN** — the diff under review (same 10 commits, same tip `ce5ea6a`)
adheres to the WO-0039 scope (re-land of WO-0027 on current main with
the crate-scoped `test_support` / `ENV_GUARD` / `PathRestoreGuard`
DEC-0011 pattern baked in). Retry attempt 1 made no code changes; it
recognised the retry premise was false and halted via escalation, which
is the correct agent behaviour.

No blockers. Two warnings carried forward from the prior review (well-
justified large commits, transparent cosmetic comment-annotation
commit). One new transparency note about the retry-loop meta anomaly —
that is harness/orchestrator territory, not critic jurisdiction, and
does not affect the code diff verdict.

Verifier may proceed (or, given the feature is already flipped on main,
triage may close the retry as Bucket A spurious fire per the
`20260419-0051-…` escalation's recommendation).
