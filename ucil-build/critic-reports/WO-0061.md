# Critic Report: WO-0061

**Critic session**: crt-2026-05-07T-WO-0061
**Branch**: `feat/WO-0061-ucil-embeddings-throughput-bench`
**HEAD reviewed**: `93a120b91d228551b03fdd367889eb0e586285ec` (`chore(wo-0061): mark ready-for-review (P2-W8-F06)`)
**Merge base**: `5aa02f7a1dd53003e4ab5e93a28ac7ee0161e1c2` (`main`)
**Diff scope**: 7 commits, 7 paths, +877 / -2 (Cargo.lock 174, root Cargo.toml 10, embeddings Cargo.toml 10, throughput.rs 246, bench-embed-throughput.sh 136, verify/P2-W8-F06.sh 89, RFR 212)
**Verdict**: **CLEAN with warnings**

The WO lands a criterion-based throughput bench for `CodeRankEmbed::embed` per master-plan §4.2 line 303 + §18 Phase 2 Week 8 line 1789. The implementation comprises (1) `criterion = 0.5` workspace dep with `default-features = false` + `cargo_bench_support` + `plotters`; (2) the dev-dep + `[[bench]] harness = false` table in `crates/ucil-embeddings/Cargo.toml`; (3) `crates/ucil-embeddings/benches/throughput.rs` (246 LOC) with a `pub const SNIPPETS: [&str; 100]` array of mixed Rust/Python/TypeScript/plain-text snippets, a `locate_model_dir()` upward-walk helper, and a `bench_embed_100_snippets` criterion function that loads the production 137M Int8 model once and iterates the `SNIPPETS` slice per outer sample with `Throughput::Elements(SNIPPETS.len() as u64)` and `sample_size(10)`; (4) `scripts/bench-embed-throughput.sh` (136 LOC) — installer pre-flight + release build cache warm + `cargo bench` + `jq` parse of `target/criterion/embed_100_snippets/embed_100_snippets/new/estimates.json` + wall-time floor `MEAN_NS >= 1e9` + throughput floor `cpu_emb_per_sec >= 50` + canonical `cpu_emb_per_sec=<N>` stdout emit; (5) `scripts/verify/P2-W8-F06.sh` (89 LOC) — frozen-script wrapper that re-asserts `>= 50` independently per WO-0061 mutation #3 defence-in-depth.

I locally re-grepped the diff for every check in `.claude/agents/critic.md` (stubs, mocks of critical collaborators, skipped/ignored tests, weak assertions, hallucinated imports, feature coverage, commit hygiene, public-API docs). No blockers. The only divergence from a literal AC reading is AC08 — see Warning 1 below — but the regex passes via the rustdoc preamble, the runtime is provably 100, and the executor disclosed the deviation in the RFR map.

## Findings

### Blockers (must fix before verifier)

**None.**

### Warnings (should be acknowledged before gate)

1. **AC08 grep matches rustdoc, not the call site** — `crates/ucil-embeddings/benches/throughput.rs:229` calls `group.throughput(Throughput::Elements(SNIPPETS.len() as u64))`, not the literal `Throughput::Elements(100)` form prescribed by WO scope_in[6]. The AC08 regex `Throughput::Elements\(\s*100\s*\)` still matches at lines 24 (module rustdoc) and 212 (function rustdoc). `SNIPPETS` is `pub const SNIPPETS: [&str; 100]` so the runtime value is provably 100 (compile-time constant); `as u64` widens the `usize` length. The executor disclosed this in the RFR's AC08 row. The deviation is arguably better engineering (single source of truth between array length and throughput marker), and the grep regex passes via the rustdoc citation. Borderline — accept since the AC's authoritative grep returns ≥1 hit and the spirit (100 elements per outer iteration) is preserved. Verifier may want to confirm the runtime path under `cargo bench` produces the canonical `embed_100_snippets/embed_100_snippets/new/estimates.json` directory shape.

2. **`expect()` calls in bench code outside `#[cfg(test)]`** — four `.expect()` sites at `crates/ucil-embeddings/benches/throughput.rs:192,194,226,237` (`crates/ parent of ucil-embeddings`, `workspace root parent of crates/`, `CodeRankEmbed::load on ml/models/coderankembed`, `CodeRankEmbed::embed on baked-in snippet`). `.claude/rules/rust-style.md` states "Never `.unwrap()` / `.expect()` outside `#[cfg(test)]`. Use `?` and a typed error." Bench code is not `#[cfg(test)]`, but criterion's `criterion_main!` returns `()` so `?`-propagation is not viable here — `.expect()` is the canonical idiom for setup failures in criterion benches. WO scope_in[6] explicitly authorizes the `.expect("CodeRankEmbed::load")` form at the model-load site. WO authority overrides the general style rule per the oracle hierarchy in root CLAUDE.md. Accept with note — first criterion bench in the project, sets a precedent for all future `crates/*/benches/`.

3. **Single-commit LOC > 50 (soft target)** — three commits exceed the `.claude/rules/commit-style.md` ~50-LOC soft target: `4c73836 feat(embeddings): add throughput criterion bench (P2-W8-F06)` lands the 246-LOC bench file (97% of which is the `SNIPPETS: [&str; 100]` array literal); `03643ff feat(scripts): add bench-embed-throughput.sh asserter` lands a 136-LOC bash script; `93a120b chore(wo-0061): mark ready-for-review (P2-W8-F06)` lands the 212-LOC RFR marker. The bench-file commit body explicitly cites `DEC-0005` module-coherence + WO-0048 297-LOC + WO-0056 542-LOC + WO-0058 305-LOC + WO-0059 425-LOC + WO-0060 503-LOC NEW-module precedents; splitting the SNIPPETS array from the bench function would have triggered `#![deny(warnings)]` cascade through `unused_imports`. Soft target with documented justification consistent with prior accepted precedents — accept.

4. **`Cargo.lock` +174/-2 churn from criterion transitive deps** — `dc52b44 build(embeddings): add criterion dev-dep + [[bench]] throughput` adds `anes`, `tinytemplate`, `criterion-plot`, `oorandom`, `ciborium`, `ciborium-io`, `ciborium-ll`, `cast`, and ~15 other transitive crates from criterion 0.5's dep tree. `default-features = false` correctly drops `rayon`/`rayon-core` from the chain. WO scope_in[17] explicitly anticipates this churn with WO-0058 lessons line 569 + WO-0060 lessons line 646 ("transitive Cargo.lock churn is non-blocking for the W-1 200-LOC critic cadence") precedent. Accept.

5. **Bench script comments use the word "mock"** — `scripts/bench-embed-throughput.sh:33` ("likely mock-shaped") and line 115 ("this is the mock-shape sentinel") use "mock" as a descriptor of the implementation SHAPE the wall-time floor catches (no-op return / 1-element array). These are NOT mocks of critical collaborators; the script's job is to detect such mock-shaped implementations via the `MEAN_NS >= 1e9` floor. AC11 + AC28 mock-scans apply only to `crates/ucil-embeddings/benches/throughput.rs`, not the shell script. Verifier may grep the shell script if the AC contract is interpreted broadly — flag for awareness. Accept on the strict reading; the wording is metaphor for the asserter's intent.

6. **Three pre-baked mutations (AC34/35/36) delegated to verifier** — the WO scope_in steps 14/15/16 prescribe pre-baked mutations on `models.rs`, `benches/throughput.rs`, and `scripts/bench-embed-throughput.sh`. The executor's RFR row maps AC34-AC36 as "Pre-baked mutations (verifier-applied) | trust design". Reading the WO carefully, the prescription is the design contract for what each mutation MUST cause — the WO does not require the executor to commit then revert each mutation in-line. Same delegation pattern as WO-0048 / WO-0056 / WO-0060. Verifier should apply each mutation literally (the WO names the exact `sed`-able edit), confirm the expected failure mode, restore via `git checkout --`, and confirm green again. Accept.

### OK

- **Stubs**: zero hits in shipped code. `git show <branch>:crates/ucil-embeddings/benches/throughput.rs | grep -nE 'todo!\(\)|unimplemented!\(\)|panic!\(".*not yet|TODO|FIXME|raise NotImplementedError'` returns 0 matches. The `panic!()` in `locate_model_dir` (lines 198-204) is REAL filesystem code per AC27 — it gates on `model_onnx.exists() || tokenizer_json.exists()` returning false and emits an operator-readable message naming the installer script. Per `DEC-0015` D3 precedent, this is a path-existence check, not a stub. The bench script and verify script have zero `todo`/`TODO`/`FIXME`/`HACK` mentions. The two `BENCH_LOG`/`BENCH_STDOUT` mktemp lines triggered an `XXXXXX` regex hit but those are `mktemp` template placeholders — not stubs.
- **Mocked critical collaborators**: zero in the bench `.rs` file. `git show <branch>:crates/ucil-embeddings/benches/throughput.rs | grep -inE '\bmock|\bfake|\bstub|\bfixture|\bdummy|\bnoop'` returns 0 matches post-`19035d4`. The bench loads the real production 137M Int8 `CodeRankEmbed` model from `ml/models/coderankembed/model.onnx` via the public `CodeRankEmbed::load` API and exercises the public `CodeRankEmbed::embed` surface — no `ort`/`tokenizers`/`CodeRankEmbed` mocks per `.claude/rules/rust-style.md`. (Bench script has metaphorical "mock-shape" mentions — see Warning 5.)
- **Skipped/ignored tests**: zero. `grep -nE '#\[ignore\]|\.skip\(|xfail|it\.skip|@pytest\.mark\.skip|@pytest\.mark\.xfail'` on the bench file returns 0. The bench function has no skip/ignore attributes. The bench is gated only by the verify script's `install-coderankembed.sh` pre-flight (which is real installer code, not a skip).
- **Weak assertions**: zero. `grep -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)|assert_eq!\(1,\s*1\)'` returns 0. The bench has no `#[test]` blocks (it's a `criterion_main!` binary), so the WO's mutation contract (AC34-AC36) carries the "real assertion" semantics: wall-time floor `MEAN_NS >= 1e9` and throughput floor `cpu_emb_per_sec >= 50` in the bash asserter, plus the verify script's independent `>= 50` re-check.
- **Hallucinated imports**: zero. The three `use` items in `throughput.rs` all resolve:
  - `use std::path::PathBuf;` — std.
  - `use criterion::{black_box, criterion_group, criterion_main, Criterion, Throughput};` — workspace dep `criterion = 0.5` declared at root `Cargo.toml:56`; all five symbols are part of criterion 0.5's public API (`black_box`, `criterion_group!`, `criterion_main!` macros + `Criterion`, `Throughput` types).
  - `use ucil_embeddings::CodeRankEmbed;` — re-exported by `crates/ucil-embeddings/src/lib.rs:23` (`pub use models::{CodeRankEmbed, CodeRankEmbedError, EMBEDDING_DIM};`). The `CodeRankEmbed::load(&Path)` and `CodeRankEmbed::embed(&str)` methods exist at `models.rs:258` and `models.rs:351` respectively (per F02/WO-0059).
  - The bench script references `scripts/devtools/install-coderankembed.sh` which exists on this branch (122 LOC), `scripts/verify/coverage-gate.sh` (referenced via AC26, exists), and the criterion estimates path which is criterion 0.5's documented output shape.
- **Feature coverage**: `P2-W8-F06` (the only feature in the WO) is covered end-to-end by:
  1. The bench function `bench_embed_100_snippets` at `benches/throughput.rs:223` exercises real `CodeRankEmbed::embed` across 100 baked-in snippets per outer iteration;
  2. The `criterion_group!`/`criterion_main!` pair at lines 245-246 wires `cargo bench --bench throughput` to the function;
  3. `scripts/bench-embed-throughput.sh` parses criterion's emitted `estimates.json` and asserts `>= 50` emb/sec;
  4. `scripts/verify/P2-W8-F06.sh` re-asserts `>= 50` independently and prints the canonical `[OK] P2-W8-F06 cpu_emb_per_sec=<N>` line.
  Verifier confirms via `bash scripts/verify/P2-W8-F06.sh` (executor's RFR row 21 reports `[OK] P2-W8-F06 cpu_emb_per_sec=70.67` locally, well within the master-plan §4.2 line 303 50-150 emb/sec band).
- **Commit-message trailers**: 7/7 commits have `Phase: 2`, 7/7 have `Feature: P2-W8-F06`, 7/7 have `Work-order: WO-0061`. All 7 are Conventional Commits with valid types: `build` × 2, `feat` × 3, `docs` × 1, `chore` × 1.
- **Commit-subject lengths**: 7/7 ≤ 70 chars (longest 64 chars: `build(workspace): add criterion 0.5 dev-dep for embeddings bench`). Per WO-0059 lessons line 607 + WO-0060 lessons line 645 ≤70 cap.
- **Allow-list discipline**: `git diff --name-only main..feat/WO-0061-ucil-embeddings-throughput-bench` returns exactly 7 paths, all in WO scope_in / AC29 allow-list:
  1. `Cargo.lock` (transitive deps from criterion 0.5)
  2. `Cargo.toml` (workspace `criterion` dep)
  3. `crates/ucil-embeddings/Cargo.toml` (`criterion.workspace = true` dev-dep + `[[bench]] name = "throughput" harness = false` table)
  4. `crates/ucil-embeddings/benches/throughput.rs` (NEW)
  5. `scripts/bench-embed-throughput.sh` (NEW, 0755)
  6. `scripts/verify/P2-W8-F06.sh` (NEW, 0755)
  7. `ucil-build/work-orders/0061-ready-for-review.md` (RFR marker)

  Zero forbidden-path touches: `crates/ucil-embeddings/src/**` clean (AC29 confirms `git diff --name-only main...HEAD -- 'crates/ucil-embeddings/src/'` is empty), all sibling crates / `adapters/**` / `ml/**` / `plugins/**` / `tests/fixtures/**` / `.githooks/**` / `.claude/**` / `scripts/gate/**` / `scripts/flip-feature.sh` untouched. `ucil-build/feature-list.json` and `ucil-master-plan-v2.1-final.md` untouched.
- **Public-API rustdoc**: the only `pub` item introduced in the bench file is `pub const SNIPPETS: [&str; 100]` at line 72; it carries a 19-line rustdoc block (lines 56-71) documenting the mix breakdown (30 Rust / 25 Python / 25 TypeScript / 20 plain text), per-snippet byte range, and load-bearing 100-element invariant. The non-`pub` helper `locate_model_dir()` (line 191) and the bench function `bench_embed_100_snippets` (line 223) also carry rustdoc. The module-level `//!` preamble cites master-plan §4.2 line 303 verbatim and §18 Phase 2 Week 8 line 1789 verbatim, names `P2-W8-F06` / `WO-0061`, and explains the bench shape. `lib.rs` is unchanged — no new `pub` re-exports.
- **No new public API surface on `ucil-embeddings::lib.rs`**: `git diff main...HEAD -- crates/ucil-embeddings/src/` returns empty per AC29. The 14-WO `lib.rs` re-export streak (WO-0042 → WO-0058 → WO-0059 → WO-0060) is preserved by construction — bench code lives in a separate compilation unit.
- **`unsafe` discipline**: zero `unsafe` blocks introduced.
- **Frozen identifiers**: the bench's `criterion::Criterion::benchmark_group("embed_100_snippets")` (line 228) + `group.bench_function("embed_100_snippets", ...)` (line 231) match the criterion-output path `target/criterion/embed_100_snippets/embed_100_snippets/new/estimates.json` that the bench script reads at line 75. AC10 grep `bench_function\(\s*"embed_100_snippets"` returns exactly 1 hit at line 231. Frozen-bench-name selector check in the verify script (line 39) re-greps the same identifier. Identifier consistency is load-bearing — the verify script depends on it for criterion-output-path resolution.
- **Mutation prep (AC34/35/36)**: WO scope_in steps 14/15/16 prescribe three pre-baked mutations. M1 (`CodeRankEmbed::embed` body neutered to `Ok(vec![0.0_f32; EMBEDDING_DIM])`) → bench script's `MEAN_NS >= 1e9` floor catches the sub-millisecond runtime; restore via `git checkout -- crates/ucil-embeddings/src/models.rs`. M2 (`SNIPPETS` shrunk to 1 element) → wall-time floor catches the ~5-15ms single-call runtime; restore via `git checkout -- crates/ucil-embeddings/benches/throughput.rs`. M3 (bench script's `>= 50` neutered to `>= 0`) → verify script's independent `>= 50` re-check catches it; restore via `git checkout -- scripts/bench-embed-throughput.sh`. RFR documents all 3 with restoration. Verifier-applied per Warning 6.
- **AC25 — workspace regression**: the executor's RFR claims `cargo test --workspace --no-fail-fast` exits 0. Critic does not re-run the full workspace suite (verifier scope); commit hygiene + structural checks all clean.

