# Critic Report: WO-0090

**Critic session**: crt-WO-0090-mcp-tool-quality-and-type-check
**Branch**: `feat/WO-0090-mcp-tool-quality-and-type-check`
**Reviewed ref**: `561ba3be4d7b768d0250c573e326f2d078d0d3ab`
**Merge base**: `d938acf` (main)
**Commit range**: `git log --oneline main..feat/WO-0090-mcp-tool-quality-and-type-check` → 6 commits
**Work-order**: `ucil-build/work-orders/0090-mcp-tool-quality-and-type-check.json`
**Feature(s)**: `P3-W11-F10` (`check_quality`) + `P3-W11-F15` (`type_check`)
**Verdict**: **CLEAN**

## Summary of changes

| File | Change | LOC |
|---|---|---|
| `crates/ucil-daemon/Cargo.toml` | EDIT (+`ucil-lsp-diagnostics` path dep, +`lsp-types` workspace dep) | +16 |
| `crates/ucil-daemon/src/server.rs` | EDIT (3 fields + 3 builders + 2 handlers + 6 helper fns + 2 frozen tests) | +1196 |
| `Cargo.lock` | EDIT (lock-only) | +2 |
| `ucil-build/work-orders/0090-ready-for-review.md` | NEW (RFR) | +161 |
| **Total** | 4 files changed | +1375 |

Commits (6):
- `4a8b7c4` `build(daemon): add ucil-lsp-diagnostics + lsp-types deps for WO-0090`
- `fccc803` `feat(daemon): wire check_quality + type_check MCP tool handlers`
- `bf121cb` `test(daemon): frozen test_check_quality_tool — F10 SA1..SA6`
- `8de7d0b` `test(daemon): frozen test_type_check_tool — F15 SA1..SA5`
- `2c8966a` `refactor(daemon): single-line tracing instrument + AC14 evidence comment`
- `561ba3b` `chore(work-orders): WO-0090 ready-for-review marker`

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

1. **Large `feat` commit — `fccc803` is 641 lines** (`crates/ucil-daemon/src/server.rs` only). The `.claude/rules/commit-style.md` "~50 lines of diff per commit is a soft target" guidance is exceeded. RFR §"Commit-cadence vs scope_in #12 alignment" justifies the bundling as "splitting … would require maintaining two sets of struct fields / Default impls / Debug impls in mid-state, producing exactly the stub-shaped intermediate the work-order forbids." Test commits (`bf121cb`, `8de7d0b`) are also each above the soft target (267 / 292 lines), but they are mostly trait-impl boilerplate (`TestG7Source`, `TestG8Source`, `ScriptedFakeSerenaClient`) for the dependency-inversion seam. AC24 commit-count budget is met (6/6). Soft warning only — no blocker.

2. **AC14 grep evidence lives on a comment line, not the actual macro invocation**. `crates/ucil-daemon/src/server.rs:1905` carries the inline comment `// PARALLEL fan-out via tokio::join!(execute_g7(...), execute_g8(...))` which is what `grep -E 'tokio::join!|tokio::try_join!' | grep -E 'execute_g7|execute_g8'` matches; the actual invocation at `crates/ucil-daemon/src/server.rs:1912-1915` spans four lines because rustfmt wraps the macro arguments. Run-time semantics are correct (`tokio::join!` is the real call site). Disclosed in RFR §3 of "Disclosed deviations". Soft warning only.

3. **`scope_in` #9 wire-type re-exports not added**. The plan called for `pub use server::{CheckQualityResponse, TypeCheckResponse, QualityIssueWire, UntestedFunctionWire, TypeError};` in `crates/ucil-daemon/src/lib.rs`. The executor projected the response shapes via `serde_json::json!(...)` directly rather than introducing named wire structs, so there are no new public types to re-export. Simplification is reasonable; the response shapes are still asserted end-to-end through the JSON-RPC envelope in the frozen tests. Diff to `crates/ucil-daemon/src/lib.rs` is empty. Soft warning only.

4. **`scope_out` #8 inaccuracy disclosed but worth flagging**. The work-order claimed `ucil-lsp-diagnostics` was already a daemon dep (`scope_out #8`: "`ucil-lsp-diagnostics` (which is already a `ucil-daemon` workspace dep)"). It was not — the executor had to add it as a sibling-crate path dep in `crates/ucil-daemon/Cargo.toml:115`. The addition does not introduce a new external workspace member (`ucil-lsp-diagnostics` is `crates/ucil-lsp-diagnostics`) but it is a real new dep on the daemon crate. Disclosed in RFR §1 of "Disclosed deviations". Soft warning only.

### OK

#### 1. Stub detection — CLEAN
- `grep -nE '^\+.*todo!\(\)|unimplemented!\(\)|raise NotImplementedError|NotImplementedError'` against the full diff returned **zero hits**.
- `grep -nE '^\+.*pass\s*$'` returned **zero hits**.
- `grep -nE '^\+\s*return\s+(None|Default::default\(\)|true|false|0|"")\s*$'` returned **zero hits**.
- Production-side `.unwrap()` enumeration in RFR is honest:
  - `crates/ucil-daemon/src/server.rs:1916` `u64::try_from(start.elapsed().as_millis()).unwrap_or(u64::MAX)` — saturating `u128 → u64` cast, not `unwrap()`.
  - `crates/ucil-daemon/src/server.rs:2008` and `:2118` `serde_json::to_string(&payload).unwrap_or_else(|_| summary.clone())` — degraded textual fallback, not panic.
- Both new handlers (`handle_check_quality` at `crates/ucil-daemon/src/server.rs:1870`, `handle_type_check` at `:2047`) have real bodies that call into `crate::g7::execute_g7` / `crate::g8::execute_g8` / `DiagnosticsClient::diagnostics` and project results into the canonical wire shape.

#### 2. Mocked critical dependencies — CLEAN
- `grep -ni 'mock|fake|stub'` against the full diff hits **only**:
  - **Production-side doc comments** describing the legitimate phase-1 `_meta.not_yet_implemented: true` "stub path" (the architectural concept inherited from `crates/ucil-daemon/src/server.rs:18`, `:455`, `:459`, `:473–778`, `:906–977`). Not introduced by this WO; no production-code stub functions added.
  - **Test-side `ScriptedFakeSerenaClient`** at `crates/ucil-daemon/src/server.rs:1062` — UCIL-internal `SerenaClient` trait impl per DEC-0008 §4 dependency-inversion seam, structurally identical to `quality_pipeline.rs::test_fixtures::ScriptedFakeSerenaClient` carried forward through WO-0048 / WO-0085 / WO-0089. **Not a mock of the real Serena MCP server** — UCIL owns the `SerenaClient` trait definition.
  - **Test-side `TestG7Source` / `TestG8Source`** at `crates/ucil-daemon/src/server.rs:7648` / `:7693` — UCIL-internal `G7Source` / `G8Source` trait impls per DEC-0008 §4. Not mocks of any external service.
- No SQLite / LanceDB / Docker mocking introduced in tests; LanceDB and SQLite are not exercised by either handler.

#### 3. Skipped / ignored tests — CLEAN
- `grep -nE '^\+.*#\[ignore\]'` → zero hits.
- `grep -nE '^\+.*(\.skip\(|xfail|@pytest\.mark\.skip|@pytest\.mark\.xfail|it\.skip|xit\()'` → zero hits.

#### 4. Weak assertions — CLEAN
- `grep -nE '^\+.*(assert!\(true\)|assert_eq!\(true,\s*true\)|assert True\b|expect\(true\)\.toBe\(true\))'` → zero hits.
- The two frozen tests carry SA-numbered panic-body shapes per DEC-0007 + WO-0067 mature pattern. Spot-check:
  - `crates/ucil-daemon/src/server.rs:7900` `assert_eq!(issues_arr.len(), 3, "(SA1) issues[] length; left: {}, right: 3", issues_arr.len());`
  - `crates/ucil-daemon/src/server.rs:8093` `assert_eq!(errors_arr.len(), 3, "(SA1) errors[] length; left: {}, right: 3", errors_arr.len());`
- Each `assert!`/`assert_eq!` carries `(SAn) <semantic name>` prefixes — verifier mutation panic-body shape lookups can pattern-match cleanly.

#### 5. Hallucinated paths — CLEAN
- New `use` statements verified to resolve:
  - `crate::g7::{execute_g7, merge_g7_by_severity, G7Query, G7Source, G7_DEFAULT_MASTER_DEADLINE}` → `crates/ucil-daemon/src/g7.rs:104, 116, 322, 491, 627`. ✓
  - `crate::g8::{execute_g8, merge_g8_test_discoveries, G8Query, G8Source, G8_DEFAULT_MASTER_DEADLINE}` → `crates/ucil-daemon/src/g8.rs:105, 241, 301, 496, 616`. ✓
  - `ucil_lsp_diagnostics::{DiagnosticsClient, Language}` → `crates/ucil-lsp-diagnostics/src/diagnostics.rs:206` + `crates/ucil-lsp-diagnostics/src/types.rs` (re-exported via `lib.rs:41`). ✓
  - `ucil_lsp_diagnostics::SerenaClient` (test-only) → `crates/ucil-lsp-diagnostics/src/diagnostics.rs:145`. ✓
  - `lsp_types::{Diagnostic, DiagnosticSeverity, NumberOrString, Url, Position, Range, ...}` → workspace dep `lsp-types = "0.95"` in root `Cargo.toml:115`. ✓
- `cargo build -p ucil-daemon` exits 0 — locally confirmed.
- `cargo clippy -p ucil-daemon -- -D warnings` exits 0 — locally confirmed.

#### 6. Feature coverage — CLEAN
- **F10 (`P3-W11-F10`)**: `crates/ucil-daemon/src/server.rs:7637` `async fn test_check_quality_tool()` — at MODULE ROOT (not inside any `mod tests {}` — `grep -nE '^mod tests\b'` against `server.rs` returns zero hits). Selector `cargo test -p ucil-daemon server::test_check_quality_tool` resolves uniquely (locally verified: 1 passed, 0 failed; 171 filtered out). Asserts SA1-SA6 covering issue count, lowercase severity vocabulary canary, untested-function count + path, master-timed-out flag, and parallelism wall-clock < 5000 ms.
- **F15 (`P3-W11-F15`)**: `crates/ucil-daemon/src/server.rs:7899` `async fn test_type_check_tool()` — at MODULE ROOT. Selector `cargo test -p ucil-daemon server::test_type_check_tool` resolves uniquely (locally verified: 1 passed, 0 failed). Asserts SA1-SA5 covering type-error count (filtering Warning + clippy non-type errors), severity vocabulary, language coverage, and `meta.files_checked` / `meta.files_skipped`.
- M1 / M2 / M3 mutation contract pre-baked in RFR §"M1 / M2 / M3 mutation contract" with patch + restore commands and md5sum snapshot path (`/tmp/wo-0090-server-orig.md5sum` `4760f6c6dfa983b03e8e93b596a68af9`). The contract maps M1 → SA1 issues[] length; M2 → SA3 untested_functions[] length; M3 → SA1 errors[] length — all with the verifier's expected `left: X, right: Y` panic-body shape.

#### 7. Commit hygiene — CLEAN
- Conventional Commits subject lines on every commit (`build`, `feat`, `test`, `refactor`, `chore`).
- Every commit body carries `Phase: 3`, `Feature: <id>`, `Work-order: WO-0090` trailers + `Co-Authored-By` line.
- No merge commits introduced: `git log feat/WO-0090-mcp-tool-quality-and-type-check ^main --merges` → 0 (AC23).
- No `--amend` after push detected; no force-push.
- Branch is a clean linear sequence on top of merge-base `d938acf`.
- See Warning #1 above for the soft size deviation on `fccc803` / `bf121cb` / `8de7d0b`.

#### 8. Doc + public API — CLEAN
- New `pub` items all carry rustdoc:
  - `pub g7_sources: Option<Arc<...>>` at `crates/ucil-daemon/src/server.rs:537` — 18 lines of rustdoc above (`crates/ucil-daemon/src/server.rs:519-537`).
  - `pub g8_sources: Option<Arc<...>>` at `:548` — 10 lines of rustdoc (`:538-547`).
  - `pub diagnostics_client: Option<Arc<DiagnosticsClient>>` at `:561` — 12 lines of rustdoc (`:549-560`).
  - `pub fn with_g7_sources(...)` at `:745` — 18 lines of rustdoc (`:726-743`) including `#[must_use]` annotation.
  - `pub fn with_g8_sources(...)` at `:761` — 10 lines of rustdoc (`:750-759`) + `#[must_use]`.
  - `pub fn with_diagnostics_client(...)` at `:780` — 13 lines of rustdoc (`:766-778`) + `#[must_use]`.
- New `async fn handle_check_quality(...)` at `:1870` — 38 lines of rustdoc (`:1832-1867`) including parameter table and parallelism rationale.
- New `async fn handle_type_check(...)` at `:2047` — 33 lines of rustdoc (`:2012-2044`) including filter-predicate spec.
- New free helper functions (`boxed_g7_sources`, `boxed_g8_sources`, `language_from_path`, `language_to_str`, `path_to_file_url`, `is_type_error_diagnostic`) all carry rustdoc (`crates/ucil-daemon/src/server.rs:2287-2455`). `is_type_error_diagnostic` doc enumerates the type-checker source allow-list and the M3 mutation flow.
- Adapter structs `G7SourceArcAdapter` (`:2305`) / `G8SourceArcAdapter` (`:2347`) are crate-private (no `pub`) so rustdoc is optional; both nonetheless carry doc comments.

## Auxiliary checks

- `grep -niE 'mock|fake|stub'` on production-side (lines < 3896, the first `#[cfg(test)]`) returns only the inherited "stub path" doc/comment hits already discussed under Check 2 — no new production-side mock/fake/stub identifiers introduced by this WO.
- AC10 (`"check_quality"` ≥ 2 lines): 7 hits in `crates/ucil-daemon/src/server.rs` (descriptor at `:343`, dispatch at `:963`, doc/comment lines, ToolDescriptor block at `:3922`, test JSON at `:7769`).
- AC11 (`"type_check"` ≥ 2 lines): 7 hits (descriptor at `:359`, dispatch at `:971`, doc/comment lines, ToolDescriptor block at `:3926`, test JSON at `:8062`).
- AC12 (`#[tracing::instrument(name = "ucil.tool.check_quality")]` literal): 1 hit at `crates/ucil-daemon/src/server.rs:1868`.
- AC13 (`#[tracing::instrument(name = "ucil.tool.type_check")]` literal): 1 hit at `:2045`.
- AC15 (`with_g7_sources` / `with_g8_sources` / `with_diagnostics_client`): 3 hits at `:745`, `:761`, `:780`.
- AC23 (no merge commits): 0.
- AC24 (≤ 6 commits): 6.

## Test-tier observations

- `cargo test -p ucil-daemon --lib server::` → 37 passed, 0 failed (no regression on the existing server-module tests).
- Both new selectors execute under `tokio::test(flavor = "multi_thread", worker_threads = 4)` — appropriate for the parallel `tokio::join!` fan-out.
- The F15 test fixture (`ScriptedFakeSerenaClient`) implements the full `SerenaClient` trait (4 methods: `diagnostics`, `call_hierarchy_incoming`, `call_hierarchy_outgoing`, `type_hierarchy_supertypes`); the unused methods return empty results — clean dependency-inversion-seam carry-forward, not a partial-impl stub.
- F10 SA6 wall-clock canary (`meta.wall_elapsed_ms < 5000`) is the parallelism mutation surface: M1 / M2 trip on SA1 / SA3 (count assertions) directly, but the wall-clock canary is an additional regression net for any future executor that switches `tokio::join!` to sequential `.await`s.

## Verdict rationale

All 8 critic checks pass with **zero blockers**. The 4 warnings are:
- (1) commit-size soft target exceeded — but AC24 commit-count budget is met and the bundling rationale is plausible;
- (2) AC14 evidence on a comment line — disclosed, runtime semantics correct;
- (3) wire-type re-exports skipped — simplification, no acceptance impact;
- (4) `scope_out #8` dep inaccuracy — disclosed honestly in RFR.

None of these obstruct verifier work or change the substantive correctness of the F10 / F15 dispatch handlers. **Approving for verifier dispatch.**
