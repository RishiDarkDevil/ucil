# Critic Report: WO-0089

**Critic session**: crt-WO-0089-g8-test-discovery-backbone
**Branch**: `feat/WO-0089-g8-test-discovery-backbone`
**Reviewed ref**: `364bf22ba8ed922973ca0a8105e92df4f4503b2c`
**Merge base**: `8c764d9` (main)
**Commit range**: `git log --oneline main..feat/WO-0089-g8-test-discovery-backbone` → 5 commits
**Work-order**: `ucil-build/work-orders/0089-g8-test-discovery-backbone.json`
**Feature(s)**: `P3-W11-F09` (G8 Testing parallel-execution backbone)
**Verdict**: **CLEAN**

## Summary of changes

| File | Change | LOC |
|---|---|---|
| `crates/ucil-daemon/src/g8.rs` | NEW | +659 |
| `crates/ucil-daemon/src/lib.rs` | EDIT (`+pub mod g8;`) | +1 |
| `crates/ucil-daemon/src/executor.rs` | EDIT (frozen test at module root) | +315 |
| `ucil-build/work-orders/0089-ready-for-review.md` | NEW (RFR) | +111 |
| **Total** | 4 files changed | +1086 |

Commits (5):
- `62268a0` `feat(daemon): scaffold g8 module with G8Source trait + execute_g8 + merger`
- `c7fac9d` `chore(daemon): wire g8 module through lib.rs`
- `2d281eb` `test(daemon): executor::test_g8_test_discovery_all_methods frozen test`
- `790350d` `feat(daemon): add Ord+PartialOrd derives to TestDiscoveryMethod`
- `364bf22` `docs(work-orders): WO-0089 ready-for-review`

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

_None._

### OK

#### 1. Stub detection — CLEAN
- `grep -nE 'todo!\(\)|unimplemented!\(\)|raise NotImplementedError|NotImplementedError'` against `crates/ucil-daemon/src/g8.rs` and the executor.rs diff returned **zero hits**.
- `grep -nE '(^|\s)return (None|Default::default\(\)|\{\}|true|false|0|"")\s*$'` returned **zero hits**.
- Every public surface in `g8.rs` has a real body:
  - `execute_g8` (g8.rs:496) builds boxed futures, polls them via `join_all_g8`, wraps in master `tokio::time::timeout`, synthesises ordered `TimedOut` placeholders on `Err(Elapsed)`.
  - `run_g8_source` (g8.rs:339) wraps `source.execute(query)` in `tokio::time::timeout(per_source_deadline, …)` and maps each arm to `Available` / `Errored` / `TimedOut`.
  - `merge_g8_test_discoveries` (g8.rs:616) groups by `test_path` via `BTreeMap`, unions methods via `HashSet`, takes `max(confidence)`, unions source paths via `BTreeSet` — pure deterministic.
  - `join_all_g8` (g8.rs:403) is an explicit `poll_fn`-based fan-out (not a `todo!`).

#### 2. Mocked critical dependencies — CLEAN
- `grep -nEi 'mock|fake|stub'` against `crates/ucil-daemon/src/g8.rs`: **zero hits** (production-side word-ban clean).
- `grep -nEi 'mock|fake|stub'` against the executor.rs diff: only documentation comments at executor.rs diff lines 23 + 93 saying "**NOT** a mock of any external wire format" — these are explicit disclaimers, not mocks of critical collaborators.
- `grep -nE '(serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker)'` across the diff: **zero hits**.
- The `TestG8Source` impl (executor.rs diff:95-132) is a UCIL-internal trait impl per DEC-0008 §4 dependency-inversion seam — same shape as `TestG3Source` / `TestG4Source` / `TestG7Source` precedents. The `Test` prefix is permitted per scope_in #28 + WO-0083 / WO-0085 precedent.
- The two pre-existing `lib.rs` word-ban hits at lines 144 + 150 (`22-tool stub catalog` / `implemented: true stub`) exist on `main` ahead of WO-0089 (verified via `git show main:crates/ucil-daemon/src/lib.rs | grep -niE 'mock|fake|stub'`); the `+pub mod g8;` edit adds **zero** new word-ban hits. RFR explicitly discloses this carry-forward in §Disclosed deviations #1.

#### 3. Skipped / ignored tests — CLEAN
- `grep -nE '#\[ignore\]|\.skip\(|xit\b|@pytest\.mark\.skip|@pytest\.mark\.xfail'` across the diff: **zero hits**.
- Frozen test `test_g8_test_discovery_all_methods` (executor.rs diff:60) runs unconditionally under `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]`.

#### 4. Weak assertions — CLEAN
- `grep -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)|assert_eq!\(1,\s*1\)'` across the diff: **zero hits**.
- All eight SA-tagged assertions reference real outcome state with operator-readable `(SAn) <name>; left: …, right: …` panic bodies (executor.rs diff:240-322).
- SA1 `outcome.results.len() == 5`, SA2 `!outcome.master_timed_out`, SA3 three `Available` checks, SA4 `outcome.results[3].status == TimedOut` (M1-target), SA5 dedup-count + `methods_found_by` ordering (M2-target), SA6 `wall_elapsed_ms < 6_500`, SA7 `Errored` status + message preservation, SA8 snake_case serde canary on all three variants.

#### 5. Hallucinated paths / imports — CLEAN
- All `use` statements in `g8.rs` (lines 71-78) point to existing crates:
  - `std::collections::{BTreeMap, BTreeSet, HashSet}` / `std::future::Future` / `std::path::PathBuf` / `std::pin::Pin` / `std::task::Poll` / `std::time::Duration` — stdlib.
  - `serde::{Deserialize, Serialize}` — verified in `crates/ucil-daemon/Cargo.toml` (`serde.workspace = true`).
- `#[async_trait::async_trait]` (g8.rs:300, executor.rs diff:101) — verified in `crates/ucil-daemon/Cargo.toml` (`async-trait = { workspace = true }`).
- `#[tracing::instrument(...)]` (g8.rs:333, 490) — verified (`tracing.workspace = true`).
- `tokio::time::timeout`, `tokio::time::sleep` — verified (`tokio.workspace = true`).
- `serde_json::to_string` (executor.rs diff:138, 143, 148) — verified (`serde_json.workspace = true`).
- `crate::g8::{…}` import in test (executor.rs diff:64-67) — every name (`execute_g8`, `merge_g8_test_discoveries`, `G8Query`, `G8Source`, `G8SourceStatus`, `G8TestCandidate`, `TestDiscoveryMethod`) is a `pub` item declared in g8.rs.
- `lib.rs` `pub mod g8;` edit (lib.rs diff:162) is in alphabetical position between `g7` and `lancedb_indexer` per §17.2 canonical layout.

#### 6. Feature coverage — CLEAN
- WO-0089 declares one feature: `P3-W11-F09`. `feature-list.json` line 2483 confirms entry exists; lines 2520, 2611, 3753, 5187 reference it.
- The frozen test `test_g8_test_discovery_all_methods` (executor.rs diff:60) directly drives the feature's behaviour:
  - References `execute_g8` + `merge_g8_test_discoveries` (the prescribed F09 surface).
  - References all three `TestDiscoveryMethod` variants (Convention/Import/KgRelations) per master-plan §5.8 lines 572-574.
  - Asserts the §5.8 line 577 "concurrently, then merge" contract via SA1-SA5.
- Selector `cargo test -p ucil-daemon executor::test_g8_test_discovery_all_methods` resolves uniquely per RFR §What I verified locally + AC18 disclosure.

#### 7. Commit hygiene — CLEAN
- All 5 commits use Conventional Commits (`feat(daemon)` ×2, `chore(daemon)` ×1, `test(daemon)` ×1, `docs(work-orders)` ×1).
- Every commit body carries `Phase: 3`, `Feature: P3-W11-F09`, `Work-order: WO-0089` trailers verified via `git log --format='%B' main..feat/WO-0089-g8-test-discovery-backbone`.
- Every commit ends with `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>`.
- Two commits exceed the ~50-line soft target:
  - `62268a0` (g8.rs scaffold, +659): a single new module landed atomically. Splitting g8.rs into multiple commits would create non-compiling intermediate states and is explicitly authorised by scope_in #22 ("Commits (1)+(2)+(3) MAY be folded per DEC-0005 module-coherence") and the SEXTUPLE-validated G-backbone precedent (WO-0047 / WO-0070 / WO-0083 / WO-0085).
  - `2d281eb` (frozen test, +315): a single SA1-SA8 acceptance test with 5 `TestG8Source` impls — splitting reduces test cohesion. Same DEC-0005 module-coherence justification.
- 4 non-merge commits / 0 merge commits within the AC30 4-7 inclusive range (RFR also lists 4; the `docs(work-orders): WO-0089 ready-for-review` is the 5th and is the canonical RFR commit).
- No `--amend` after push, no `--no-verify`, no force-push detected via `git log --reflog` shape.

#### 8. Doc + public API — CLEAN
- Every `pub` item in `g8.rs` carries a rustdoc comment:
  - `pub const G8_PER_SOURCE_DEADLINE` (g8.rs:94) — docs at lines 82-93.
  - `pub const G8_DEFAULT_MASTER_DEADLINE` (g8.rs:105) — docs at lines 96-104.
  - `pub enum TestDiscoveryMethod` (g8.rs:130) — docs at lines 109-127, plus per-variant docs at 131, 133, 135.
  - `pub struct G8TestCandidate` (g8.rs:145) — docs at lines 139-143, plus per-field docs at 146-162.
  - `pub enum G8SourceStatus` (g8.rs:176) — docs at lines 165-174, plus per-variant docs at 177-184.
  - `pub struct G8SourceOutput` (g8.rs:193) — docs at lines 187-191, plus per-field docs.
  - `pub struct G8Outcome` (g8.rs:223) — docs at lines 216-221, plus per-field docs.
  - `pub struct G8Query` (g8.rs:241) — docs at lines 233-239, plus per-field docs.
  - `pub struct MergedG8TestCandidate` (g8.rs:257) — docs at lines 247-255, plus per-field docs.
  - `pub trait G8Source` (g8.rs:301) — docs at lines 274-299, plus per-method docs at 302-322.
  - `pub async fn execute_g8` (g8.rs:496) — docs at lines 438-489 incl. `# Examples`.
  - `pub fn merge_g8_test_discoveries` (g8.rs:616) — docs at lines 558-614 incl. `# Examples`.
- File-level `//!` doc (g8.rs:1-66) cites master-plan §3.2 / §5.8 / §6.1 / §15.2 / §17.2 / §18 anchors and references DEC-0008 §4.
- Frozen test `pub async fn test_g8_test_discovery_all_methods` (executor.rs diff:60) carries a rustdoc (executor.rs diff:30-56) summarising SA1-SA8.
- `#![deny(rustdoc::broken_intra_doc_links)]` (g8.rs:69) enforces the no-broken-link guard.

## Mutation-contract sanity (read-only confirmation)

The RFR §Mutation contracts presents M1 + M2 in the canonical six-row shape with both observed FAILED outcomes and md5 restoration verified. M1 (per-source timeout bypass in `run_g8_source` g8.rs:347) and M2 (merger dedup-key inversion in `merge_g8_test_discoveries` g8.rs:619-637) target distinct load-bearing source lines and trip distinct SA panics (SA4 vs SA5a). Critic observation: the actual M1 anchor line `tokio::time::timeout(per_source_deadline, source.execute(query))` exists at g8.rs:347, and the M2 anchor `BTreeMap<PathBuf, (HashSet<TestDiscoveryMethod>, f64, BTreeSet<PathBuf>)>` exists at g8.rs:619 — both prescribed mutations are structurally apply-able as described in the RFR.

## Verdict

**CLEAN** — work-order is ready for the verifier. Zero blockers; zero warnings. All eight critic categories pass.
