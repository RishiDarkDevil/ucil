# Critic Report: WO-0058

**Critic session**: crt-2026-05-06T23-WO-0058
**Branch**: `feat/WO-0058-ucil-embeddings-onnx-session`
**HEAD reviewed**: `14cdf0e` (`chore(wo): mark WO-0058 ready for review (P2-W8-F01)`)
**Merge base**: `main` (no overlap with `WO-0057` per disjoint allow-lists)
**Verdict**: **CLEAN with warnings**

The diff implements P2-W8-F01 (`ucil-embeddings` ONNX Runtime session)
across 6 commits, ~1012 lines of insertions (Cargo.lock auto-bloat
dominates: 330 of 1012 lines). The frozen test
`onnx_inference::test_onnx_session_loads_minimal_model` lands at
module root of `crates/ucil-embeddings/src/onnx_inference.rs` per
DEC-0007. The fixture (`tests/data/minimal.onnx`, 177 bytes) is
generated by a committed Python script for reproducibility. No
critical-collaborator mocks, no stubs, no `#[ignore]` / `.skip()`,
no weak assertions, no `unsafe`, no forbidden-path edits.

## Findings

### Blockers (must fix before verifier)

**None.**

### Warnings (should be acknowledged before gate)

1. **AC20 commit-subject violation**:
   `crates/ucil-embeddings/src/...`-irrelevant — `git log %s` for
   commit `d9d6d84` is **72 chars** (`docs(embeddings): drop assert!
   from rustdoc examples for AC17 compliance`), 2 chars over the
   `commit-style.md` 70-char hard cap. The executor flagged this in
   `ucil-build/work-orders/0058-ready-for-review.md` (`## Documented
   AC deviation` section) and notes it cannot be retro-fixed without
   `--amend after push` or `--force` — both forbidden by root
   CLAUDE.md + `.claude/agents/executor.md`. The other 4 subjects are
   59–70 chars (compliant). Verifier must decide whether the
   2-char overshoot is acceptable, escalate via ADR, or reject.

2. **Two `ndarray` major-versions in Cargo.lock**:
   `Cargo.lock:1115` (`ndarray 0.16.1`, the workspace dep) and
   `Cargo.lock:1130` (`ndarray 0.17.2`, pulled transitively by
   `ort 2.0.0-rc.12` via its internal `ndarray` feature). The
   ready-for-review note (`## Documented divergences #4`) explains
   why the WO's `array.view() → ort::inputs!{}` form was replaced by
   `Tensor::from_array((shape, data))` — type identity across
   `ndarray@0.16` vs `ndarray@0.17` would otherwise break. Compile
   times pay a small cost; runtime correctness is unaffected. No
   functional blocker, but flag for the F02 / F05 follow-ups in case
   they want to align on `ndarray 0.17` to drop the duplicate.

3. **Two commits exceed the 200-LOC critic soft-cap with reason**:
   - `e599ff3` (`feat(embeddings): add OnnxSession + OnnxSessionError
     module`) — 305 insertions across `onnx_inference.rs` + `lib.rs`.
     Reason cited inline: DEC-0005 module-coherence carve-out +
     WO-0056 542-LOC NEW-module precedent + `#![deny(warnings)]`
     prohibits intermediate states with unused imports / dead-code
     warnings.
   - `ed85004` (`build(embeddings): add ort + ndarray workspace
     deps...`) — 354 insertions, of which 330 are auto-generated
     `Cargo.lock` churn from the new transitive graph. Cargo.lock is
     not human-authored; the human-edit surface is the 24-line
     `Cargo.toml` block.
   Both reasons are honest and consistent with prior precedent.

4. **`OnnxSession::infer` signature deviates from WO scope_in[19]**:
   `&mut self` vs the WO-prescribed `&self`. Reason cited inline +
   in `## Documented divergences #3`: `ort 2.0.0-rc.12`'s
   `Session::run` takes `&mut self`. Documented in the
   `OnnxSession::infer` rustdoc + struct-level "Not Clone" rustdoc.
   Consumers that need shared inference must wrap in `Mutex` (also
   documented). Not a novel design choice — purely fitting upstream
   per the WO's "MAY adapt the call shape" clause.

5. **`OnnxSession::input_names` / `output_names` lack `# Examples`
   blocks**: `.claude/rules/rust-style.md` says `# Examples` is
   required "when non-trivial". Both are 1-line accessor methods
   over a `Vec<String>` cache; the trivial-accessor exemption seems
   to apply, but flag as a soft warning so the verifier can confirm.

### OK

- **Stub scan** (`/tmp/wo0058-onnx_inference.rs` for `todo!()`,
  `unimplemented!()`, `panic!("...not yet`, `TODO`, `FIXME`,
  `raise NotImplementedError`, bare `pass`): zero matches.
- **Mocked critical-deps scan** (`mock|fake|stub|MagicMock|Mockall`
  in `onnx_inference.rs` — case-insensitive): zero matches. The
  test wraps `OnnxSession::from_path` against the on-disk
  `tests/data/minimal.onnx` (177 bytes) which exercises the real
  `ort::Session::commit_from_file` + a real CPU execution provider
  (download-binaries pulls a real `libonnxruntime.so` into the build
  cache). No mock of `ort::Session`, `ort::SessionBuilder`,
  `ort::Value`, or `ndarray::Array` anywhere.
- **Skipped/ignored tests**: zero matches for `#[ignore]`,
  `.skip(`, `xfail`, `xit(`, `@pytest.mark.skip` across the diff.
- **Weak assertions**: zero matches for `assert!(true)`,
  `assert_eq!(true, true)`, `expect(true)`, `assert True`,
  `assert 1`. All test assertions follow the WO's prescribed
  `assert!(<predicate>, "<plain-English description>; got {:?}",
  <actual_value>)` shape with operator-readable panic messages
  (`onnx_inference.rs:266-294`).
- **Hallucinated paths**: every `use` in the changed file resolves:
  `std::path::Path` (stdlib), `ndarray::Array2` (workspace dep added
  in `Cargo.toml:191`), `ort::session::Session` (workspace dep
  added in `Cargo.toml:184`, pinned to `=2.0.0-rc.12`),
  `ort::value::Tensor` (same crate). `Cargo.lock:1115,1130,1285`
  confirms both crates resolve at the pinned version line.
- **Feature coverage**: P2-W8-F01 has the new module-root test
  `test_onnx_session_loads_minimal_model` at
  `crates/ucil-embeddings/src/onnx_inference.rs:263` with 4
  sub-assertions (SA1 session loads / SA2 input metadata / SA3
  output metadata / SA4 infer round-trip). Test selector matches the
  frozen `feature-list.json:P2-W8-F01.acceptance_tests[0].selector`
  of `-p ucil-embeddings onnx_inference::`.
- **Module-name discipline (AC24)**:
  `crates/ucil-embeddings/src/onnx_inference.rs` exists,
  `crates/ucil-embeddings/src/ort_session.rs` does NOT — module
  name corrected from the abandoned WO-0054. ✓
- **Test placement (DEC-0007)**: `fn
  test_onnx_session_loads_minimal_model(` is at column 0 (module
  root), NOT inside `mod tests {}`. ✓
  (`onnx_inference.rs:263`).
- **Re-export discipline (cumulative-debt-avoidance)**:
  `crates/ucil-embeddings/src/lib.rs:16-17` declares both
  `pub mod onnx_inference;` and `pub use
  onnx_inference::{OnnxSession, OnnxSessionError};`. Cumulative
  re-export-discipline streak: 13 consecutive WOs cleared (WO-0042
  → WO-0058, excluding the abandoned WO-0054).
- **Mutation entries pre-baked (AC15 / AC16)**: M1 (literal sed AND
  runtime-only variant) for `from_path` → SA1 panic; M2 for `infer`
  → SA4 panic. Both verified by the executor and restored cleanly
  per the ready-for-review marker.
- **Commit hygiene — Conventional Commits format**: all 6 subjects
  match `<type>(<scope>): <summary>` shape (`build(embeddings)`,
  `feat(embeddings)`, `test(embeddings)`, `ci(verify)`,
  `docs(embeddings)`, `chore(wo)`). All have `Phase: 2 / Feature:
  P2-W8-F01 / Work-order: WO-0058` trailers (verified via grep on
  `git log` body output).
- **Commit hygiene — no `--amend` / no force-push**: branch tip
  `14cdf0e` has 6 commits ahead of main with strict-linear ancestry
  (no rewrites detected).
- **Doc / public API**: every `pub` item in `onnx_inference.rs` has a
  rustdoc block (`OnnxSessionError` enum + each variant +
  `OnnxSession` struct + `from_path` + `input_names` + `output_names`
  + `infer`). `from_path` and `infer` include `# Errors` and
  `# Examples` (the rustdoc examples were rewritten in `d9d6d84` to
  drop `assert!` lines for AC17 compliance — neutral `let _names =
  ...` / `println!(...)` substitutes preserve compilable
  rustdoc-test).
- **Module-level `//!` preamble (AC18)**: cites master-plan §18 line
  1786 verbatim, names `OnnxSession`, lists F02 / F03 / F05 / F06
  consumer pattern, documents the sync API decision.
- **Tracing spans (master-plan §15.2)**: `from_path` instruments
  `ucil.embeddings.onnx_session.load`; `infer` instruments
  `ucil.embeddings.onnx_session.infer`. Span names follow the
  `ucil.<layer>.<op>` convention.
- **No `unsafe` blocks** in `onnx_inference.rs` (AC22). ✓
- **No forbidden-path edits**: `git diff --name-only main...HEAD`
  returns only `Cargo.toml`, `Cargo.lock`,
  `crates/ucil-embeddings/Cargo.toml`,
  `crates/ucil-embeddings/src/lib.rs`,
  `crates/ucil-embeddings/src/onnx_inference.rs`,
  `crates/ucil-embeddings/tests/data/build_minimal_onnx.py`,
  `crates/ucil-embeddings/tests/data/minimal.onnx`,
  `scripts/verify/P2-W8-F01.sh`,
  `ucil-build/work-orders/0058-ready-for-review.md`. None of the WO's
  forbidden_paths is touched (no `feature-list.json`,
  `tests/fixtures/**`, `scripts/gate/**`,
  `crates/ucil-{core,cli,treesitter,lsp-diagnostics,daemon,agents}/**`,
  `adapters/**`, `ml/**`, etc.).
- **Verify script**: `scripts/verify/P2-W8-F01.sh` is `+x`,
  shellcheck-fallback present (per WO-0044 onwards), tolerant test-
  result regex (`test result: ok\. .* 0 failed|[0-9]+ tests? passed`)
  per the WO-0042+ standing shape. Frozen-selector module-root
  invariant pre-checked before running cargo.
- **Fixture authenticity**: `crates/ucil-embeddings/tests/data/
  build_minimal_onnx.py` constructs a real ONNX graph (`Cast(int64
  → float32)`) via the official `onnx` Python package; the
  resulting protobuf is checked in (`minimal.onnx` 177 bytes). The
  fixture is in the per-crate `tests/data/` (NOT repo-root
  `tests/fixtures/**` deny-list).

## Recommended verifier action

Proceed to verification. The AC20 commit-subject overshoot is the
only friction point; the verifier should evaluate whether the
2-char overshoot warrants escalation per `.claude/rules/commit-
style.md`'s hard `<=70` cap, or accept-and-document given the
post-push amendment ban. Everything else is clean.

---

*Critic is read-only. No source files were edited. This report was
authored on the `feat/WO-0058-ucil-embeddings-onnx-session` branch
and committed/pushed alongside the existing WO commit ladder.*
