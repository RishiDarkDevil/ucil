---
work_order: WO-0047
feature: P2-W7-F01
branch: feat/WO-0047-g1-parallel-execution-orchestrator
ref: da6aa2f34a32ce0f32e180c1d288419436761231
critic_session: crt-WO-0047-2026-05-05
verdict: CLEAN
ready_for_verifier: true
---

# Critic Report: WO-0047 — g1-parallel-execution-orchestrator

**Critic session**: `crt-WO-0047-2026-05-05`
**Branch**: `feat/WO-0047-g1-parallel-execution-orchestrator`
**Ref**: `da6aa2f34a32ce0f32e180c1d288419436761231`
**Diff range**: `main...feat/WO-0047-g1-parallel-execution-orchestrator` (three-dot per WO-0046 lessons line 250)
**Verdict**: **CLEAN** — ready for verifier.

## Diff summary

```
$ git diff --name-only main...HEAD
crates/ucil-daemon/src/executor.rs
crates/ucil-daemon/src/lib.rs
scripts/verify/P2-W7-F01.sh
ucil-build/work-orders/0047-ready-for-review.md
```

4 files, +884 / −1 across 6 commits (4 feat/test, 2 chore).
Allow-list grep — every diffed path matches the WO acceptance whitelist
(`crates/ucil-daemon/src/executor.rs`, `crates/ucil-daemon/src/lib.rs`,
`scripts/verify/P2-W7-F01.sh`, `ucil-build/work-orders/0047-ready-for-review.md`)
per AC17. Zero hits in `forbidden_paths` (no `Cargo.toml`, no
`tests/fixtures/**`, no `crates/ucil-daemon/src/{lifecycle,main,plugin_manager,
priority_queue,server,session_manager,session_ttl,startup,storage,test_support,
text_search,understand_code,watcher}.rs`, no `crates/ucil-daemon/tests/**`,
no `feature-list*.json`, no master plan, no decisions, no phase-log).

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Production-code `.expect(...)` in `join_all_g1` (`crates/ucil-daemon/src/executor.rs:1056`).**
   The private helper `async fn join_all_g1` ends with
   `slots.into_iter().map(|r| r.expect("join_all_g1: every slot must be filled before returning")).collect()`.
   This is reachable from non-test code (called by `execute_g1` at `executor.rs:1128`).
   `.claude/rules/rust-style.md` Errors section says "Never `.unwrap()` / `.expect()`
   outside `#[cfg(test)]`. Use `?` and a typed error." The `.expect` here encodes
   a structurally-enforced invariant — the `poll_fn` loop cannot return `Poll::Ready(())`
   while any slot is `None` (lines 1031–1052) — so the panic is unreachable in
   practice. Soft warning: an `if let` / `let else` pattern or a `debug_assert!`
   would honour the rule literally. Verifier may waive.

2. **Commit `1e5febbe` size — 274 insertions, above the 200-LOC soft target.**
   `git show --stat 1e5febbe` reports 274 insertions for the
   `test(daemon): add test_g1_parallel_execution at module root (DEC-0007)`
   commit. WO `acceptance` AC25 explicitly notes "DEC-0005 module-coherence
   permits up to ~200 LOC for a coherent test+helper commit; 252 LOC was
   the WO-0046 cap (informational warning, not a blocker)." 274 LOC is one
   coherent test fn + one `TestG1Source` impl + one `TestBehaviour` enum
   + scenario sub-blocks for the three frozen sub-assertions, all bound
   to the single frozen selector — module-coherence honoured. Soft
   warning, not a blocker.

3. **Commit `73535f13` size — 203 insertions, marginally above 200-LOC soft target.**
   `git show --stat 73535f13` reports 203 insertions for the
   `feat(daemon): add G1 source trait, query, kind/status/output/outcome types`
   commit. The WO `scope_in` enumerates 7 new public types plus 2
   `pub const`s in this commit; landing each type with its rustdoc + per-field
   docs in one coherent commit is the WO's prescribed shape. 3 LOC over
   the 200-LOC soft target is informational only.

### OK (passing every critic gate)

#### 1. Stub detection — clean
`grep -nE 'todo!\(\)|unimplemented!\(\)|panic!\("[^"]*not yet|TODO|FIXME'`
against `crates/ucil-daemon/src/executor.rs` returns no matches. The
baseline on `main` is also 0, so AC16 (zero new stub hits) holds.
No `raise NotImplementedError` (Rust file, N/A). No `pass`-only
bodies (Rust file, N/A). No bare-literal stubs as feature
implementations — every constructor call returns a fully populated
value:
  - `G1Outcome` literals at `executor.rs:1146` (master-timeout
    branch) and `:1152` (success branch) carry real `results`,
    `wall_elapsed_ms`, `master_timed_out` fields.
  - `G1ToolOutput` literals at `executor.rs:1001`, `:1135`, `:1833`,
    `:1844`, `:1858` carry real `kind`, `status`, `elapsed_ms`,
    `payload`, `error` fields driven by the per-source behaviour.
  - The only `Default::default()` reference is in the WO's AC20
    mutation patch documentation (`ucil-build/work-orders/0047-ready-for-review.md:82`),
    NOT in shipped code. `derive(Default)` on `G1Outcome` is
    additive and only consumed by the verifier's mutation patch.

#### 2. Mocked critical dependencies — clean
`grep -nEi 'mock|fake|stub|MagicMock|Mockall' | grep -Ei '(serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker)'`
returns no matches in the WO-0047 added lines. Every `mock` /
`fake` token in the diff falls into one of:

  - Doc comments referencing pre-existing
    `fake_serena_hover_client::ScriptedFakeSerenaHoverClient` (the
    WO-0037 dependency-inversion seam, NOT a mock of Serena's wire
    format — DEC-0008 §4 authorises UCIL-internal trait impls in
    tests).
  - The new `TestG1Source` impl at `executor.rs:1816–1868` is a local
    impl of UCIL's own `G1Source` trait (introduced in this same
    WO at `executor.rs:965`). Per DEC-0008 §4 a UCIL-owned
    dependency-inversion seam is NOT a critical-dep mock; it is the
    same shape as the pre-existing `SerenaHoverClient` pattern
    (`executor.rs:640`) the critic explicitly cleared in WO-0037.

The `G1ToolKind::Serena`, `G1ToolKind::AstGrep`,
`G1ToolKind::TreeSitter`, `G1ToolKind::Diagnostics` enum variant
*names* reference real production-wiring targets (per the WO's
production-wiring rustdoc at `executor.rs:843–853`), but the test
uses local `TestG1Source` impls that never instantiate Serena, an
LSP, SQLite, LanceDB, or Docker — production wiring of the four
sources is deferred to P2-W7-F02 / P2-W7-F05 per the WO `scope_out`
line 30.

#### 3. Skipped / ignored tests — clean
`grep -nE '#\[ignore\]|\.skip\(|xit\(|xfail|@pytest\.mark\.skip|@pytest\.mark\.xfail|skipif'`
against `crates/ucil-daemon/src/executor.rs` returns no matches.
The acceptance test at `executor.rs:1793` is annotated only with
`#[cfg(test)]`, `#[allow(clippy::too_many_lines, clippy::missing_panics_doc)]`,
and `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]`
— no `#[ignore]`, no `#[cfg(ignore)]`, no `.skip(` invocation.
The verify script (`scripts/verify/P2-W7-F01.sh`) contains no
`--skip` or `--ignored` flags.

#### 4. Weak assertions — clean
`grep -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)|assert!\(1\b|assert_eq!\(1,\s*1\)'`
returns no matches. Every assertion in `test_g1_parallel_execution`
is non-trivial:

  - **Dual-bound parallel-timing assertion** (AC05) at
    `executor.rs:1896–1908`: lower bound `wall_elapsed_ms >= 180`
    AND upper bound `wall_elapsed_ms < 600`. Lower bound proves at
    least one 200 ms sleep elapsed; upper bound proves serial
    4×200 ms = 800 ms did not happen → parallelism confirmed. Both
    panic messages cite the dual-bound discipline and quote the
    actual measured value. Per WO-0043 lessons line 109.
  - **Master-timeout sentinel** at `executor.rs:1909–1912`:
    `assert!(!outcome_a.master_timed_out, ...)` — pinned to `false`,
    not a tautology.
  - **Length pin** at `executor.rs:1913–1917`: `assert_eq!(outcome_a.results.len(), 4, ...)`.
  - **Per-source variant pin** at `executor.rs:1918–1926`: `assert_eq!(r.status, G1ToolStatus::Available, ...)`
    inside a `for r in &outcome_a.results { ... }` loop with the
    panic message capturing `{:?} for kind {:?}`.
  - **Partial-Errored count** (AC06) at `executor.rs:1958–1969`:
    `assert_eq!(errored_b, 1, ...)` AND `assert_eq!(available_b, 3, ...)`,
    both with debug-print panic messages.
  - **Partial-TimedOut count** (AC07) at `executor.rs:2001–2012`:
    `assert_eq!(timed_out_c, 1, ...)` AND `assert_eq!(available_c, 3, ...)`,
    both with debug-print panic messages.
  - **Wall-time budget** at `executor.rs:2013–2018`:
    `assert!(outcome_c.wall_elapsed_ms < 5_500, ...)`.
  - **Master-timeout sentinel for scenario c** at `executor.rs:2019–2023`:
    `assert!(!outcome_c.master_timed_out, ...)`.

Every panic message includes operator-actionable diagnostic context
(measured values, kind, results vector debug print).

#### 5. Hallucinated imports — clean
The new top-level `use` statements in `crates/ucil-daemon/src/executor.rs`:

  - `std::future::Future` — std lib, stable.
  - `std::pin::Pin` — std lib, stable.
  - `std::task::Poll` — std lib, stable.
  - `std::time::Duration` — std lib, stable.

`#[async_trait::async_trait]` is provided by `async-trait = { workspace = true }`
already declared in `crates/ucil-daemon/Cargo.toml:62` (existing dep,
no new addition — WO AC18 honoured). `#[tracing::instrument]` is
provided by `tracing` already declared in `crates/ucil-daemon/Cargo.toml`.
`tokio::time::timeout`, `tokio::time::sleep`, `tokio::task::yield_now`
are provided by `tokio = { workspace = true, features = ["full"] }`
(existing dep). `serde::Serialize`, `serde::Deserialize`,
`serde_json::Value`, `serde_json::json!` are provided by `serde` /
`serde_json` (existing deps).

The lib.rs `pub use executor::{...}` line at `crates/ucil-daemon/src/lib.rs:112`
re-exports all 9 new public symbols (`G1Source`, `G1Query`,
`G1ToolKind`, `G1ToolStatus`, `G1ToolOutput`, `G1Outcome`,
`execute_g1`, `G1_MASTER_DEADLINE`, `G1_PER_SOURCE_DEADLINE`); each
target exists in `crates/ucil-daemon/src/executor.rs` (lines 810,
819, 829, 861, 883, 904, 936, 965, 1110 respectively).

`grep -nE 'G1Source|G1Query|G1ToolKind|G1ToolStatus|G1ToolOutput|G1Outcome|execute_g1|G1_MASTER_DEADLINE|G1_PER_SOURCE_DEADLINE'`
on `crates/ucil-daemon/src/lib.rs` finds all 9 symbols on the
re-export line — AC19 (zero deferred re-export debt) honoured.

`cargo build -p ucil-daemon` exits 0 (`env -u RUSTC_WRAPPER cargo build -p ucil-daemon`
at `Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.08s`) —
no unresolved imports.

#### 6. Feature coverage — clean
WO-0047 declares `feature_ids: ["P2-W7-F01"]`. The diff adds
**one** module-root `#[cfg(test)] #[tokio::test(flavor = "multi_thread", worker_threads = 4)]`
selector — `pub async fn test_g1_parallel_execution` at
`crates/ucil-daemon/src/executor.rs:1793` — that covers the three
sub-slices of the P2-W7-F01 contract:

| Sub-assertion | Slice covered | WO acceptance bullet |
|---------------|---------------|----------------------|
| (a) Parallel timing | `wall_elapsed_ms` ∈ [180, 600) → parallelism confirmed | AC05 |
| (b) Partial-Errored | 1 of 4 sources `Errored` → 1 Errored + 3 Available | AC06 |
| (c) Partial-TimedOut | 1 of 4 sources sleeps 6 s → 1 TimedOut + 3 Available; wall < 5500 ms | AC07 |

Module-root placement verified: `grep -nE '^pub async fn test_g1_parallel_execution'`
returns `1793:pub async fn test_g1_parallel_execution() {` — at column
0, NOT inside any `mod tests {}` block. The only `mod` declaration in
the file (`mod fake_serena_hover_client { … }` at `executor.rs:1571–1633`)
ends well before line 1793, and it does NOT contain the test fn.
Per DEC-0007 the frozen `feature-list.json` selector
`-p ucil-daemon executor::test_g1_parallel_execution` resolves directly
to `ucil_daemon::executor::test_g1_parallel_execution` without a
`tests::` intermediate. AC04 honoured.

The `feature-list.json` entry for P2-W7-F01 (acceptance_tests
`-p ucil-daemon executor::test_g1_parallel_execution`) matches the
selector verbatim.

`scripts/verify/P2-W7-F01.sh` ran end-to-end during critic review:

```
$ env -u RUSTC_WRAPPER bash scripts/verify/P2-W7-F01.sh
[INFO] P2-W7-F01: running cargo test executor::test_g1_parallel_execution...
[INFO] P2-W7-F01: shellcheck not on PATH; skipping lint.
[OK] P2-W7-F01
```

`cargo test -p ucil-daemon --lib executor::test_g1_parallel_execution`
exits 0 (1 passed; 0 failed; 4.75 s wall — sub-scenario c's 4.5 s
per-source ceiling fires before the master 5 s deadline as the
WO predicts).

Both pre-baked mutations (AC20 master-deadline guard neutered;
AC21 per-source-timeout wrapper neutered) are documented in the
ready-for-review marker (`ucil-build/work-orders/0047-ready-for-review.md`
lines 61–115) with **runtime-only variants** for the
`#![deny(warnings)]` cascade case (per WO-0046 lessons line 245).
Each entry includes the exact code substitution, the expected
runtime panic line, and a `git checkout --` restore command.
Verifier-extension hint: scenario (c)'s LongSleep behaviour
(`executor.rs:1851–1865`) is a runtime canary — if the timeout
wrapper regresses, the `Available` fallback at `:1858–1864` fires
and AC07 panics with `got 0` for the TimedOut count.

#### 7. Commit hygiene — clean
All 6 commits in `git log main..feat/WO-0047-g1-parallel-execution-orchestrator`:

  - `73535f13` — `feat(daemon): add G1 source trait, query, kind/status/output/outcome types` (203 insertions; see Warning #3)
  - `5442276` — `feat(daemon): add execute_g1 orchestrator with per-source + master deadlines` (183 insertions)
  - `1e5febbe` — `test(daemon): add test_g1_parallel_execution at module root (DEC-0007)` (274 insertions; see Warning #2)
  - `5c7dfd1d` — `feat(daemon): re-export G1 surface + add P2-W7-F01 verify script` (81 insertions, 1 deletion)
  - `aeea4375` — `chore(WO-0047): write ready-for-review marker` (142 insertions)
  - `da6aa2f3` — `chore(WO-0047): pin tip commit sha in ready-for-review marker` (2 insertions, 1 deletion)

  - **Conventional Commits subjects**: ✓ all 6 commits use
    `<type>(<scope>): <imperative summary>` per `.claude/rules/commit-style.md`
    (`feat(daemon)`, `test(daemon)`, `chore(WO-0047)`).
  - **`Phase: 2` trailer**: ✓ all 6 commits.
  - **`Feature: P2-W7-F01` trailer**: ✓ all 6 commits.
  - **`Work-order: WO-0047` trailer**: ✓ all 6 commits.
  - **`Co-Authored-By: Claude Opus 4.6 (1M context)` footer**: ✓ all 6 commits.
  - **No `--amend` after push**: ✓ — branch
    `feat/WO-0047-g1-parallel-execution-orchestrator` is the canonical
    branch and `da6aa2f3` is its tip; no rebase / amend artefacts in
    `git log --oneline main..HEAD`.
  - **No force-push artefacts**: ✓ — `git log main..HEAD` is linear
    and contiguous. `da6aa2f3` is the published HEAD on
    `origin/feat/WO-0047-g1-parallel-execution-orchestrator`.
  - **No `--no-verify`**: ✓ — no signed-off-as-bypass markers.
  - **Commit cadence**: 4 source-touching commits + 2 chore
    (review-marker) commits = 6 total. WO AC25 minimum is "at least
    3 commits"; honoured with margin.
  - **Commit-size sanity**: 4 of 6 commits are ≤ 183 LOC (well within
    DEC-0005 module-coherence). Two commits (203 + 274 LOC) exceed
    the 200-LOC soft target; both are flagged as Warnings #2 + #3
    above and are explicitly authorised by the WO's DEC-0005 carve-out.

#### 8. Doc + public API — clean
The diff introduces **9 new `pub` items** in `crates/ucil-daemon/src/executor.rs`:

| Item | File:line | Doc comment |
|------|-----------|-------------|
| `pub const G1_MASTER_DEADLINE: Duration` | `executor.rs:810` | ✓ lines 802–809 |
| `pub const G1_PER_SOURCE_DEADLINE: Duration` | `executor.rs:819` | ✓ lines 812–818 |
| `pub struct G1Query` | `executor.rs:829` | ✓ lines 821–828 (struct doc); per-field docs at lines 830–837 |
| `pub enum G1ToolKind` | `executor.rs:861` | ✓ lines 840–860 (enum doc); per-variant docs at lines 862–872 |
| `pub enum G1ToolStatus` | `executor.rs:883` | ✓ lines 875–882 (enum doc); per-variant docs at lines 884–893 |
| `pub struct G1ToolOutput` | `executor.rs:904` | ✓ lines 896–903 (struct doc); per-field docs at lines 905–919 |
| `pub struct G1Outcome` | `executor.rs:936` | ✓ lines 922–935 (struct doc); per-field docs at lines 937–943 |
| `pub trait G1Source` | `executor.rs:965` | ✓ lines 946–964 (trait doc); per-method docs at lines 966, 970–977 |
| `pub async fn execute_g1<S>` | `executor.rs:1110` | ✓ lines 1060–1103 (incl. `# Examples` block) |

The `pub async fn test_g1_parallel_execution` at `executor.rs:1793`
is `#[cfg(test)]`-gated, so its visibility is irrelevant for
production rustdoc — but it carries a full doc comment regardless
(lines 1769–1789).

The lib.rs preamble paragraph for WO-0037 (`lib.rs:51–62`) gains a
new trailing sentence at `lib.rs:63–72` documenting WO-0047's
contribution and citing DEC-0008 + the F02 / F05 deferral note —
the load-bearing signpost the WO scope_in line 23 explicitly
required.

The `# Examples` rustdoc block on `execute_g1` at `executor.rs:1085–1103`
is `no_run`-gated (compiles but not executed in `cargo test`),
which avoids needing a real `Vec<Box<dyn G1Source>>` fixture in
doc-tests. Every uppercase identifier in the new rustdoc lives
inside backticks (e.g. `` `G1Outcome` ``, `` `G1_MASTER_DEADLINE` ``,
`` `G1ToolStatus::TimedOut` ``) per the AC02 pre-flight grep — no
clippy::doc_markdown hits.

The executor's ready-for-review marker (line 12) reports
`cargo clippy -p ucil-daemon --all-targets -- -D warnings → exit 0`,
confirming the doc-markdown pre-flight is satisfied. Three clippy
hits surfaced and were fixed during the executor loop
(`single_match_else` / `option_if_let_else` /
`unnecessary_result_map_or_else` on the `tokio::time::timeout` match;
`needless_collect` on the `kinds` Vec; `missing_panics_doc` /
`too_many_lines` on the test fn — all per the marker.

## Verifier checklist

Verifier should run (per WO-0047 acceptance_criteria, with optional
verifier-extension on a third symmetric mutation per WO-0045
lessons line 199):

```
# 1. Existence + grep gates (mechanical, runs in seconds)
test -f crates/ucil-daemon/src/executor.rs
test -f scripts/verify/P2-W7-F01.sh
grep -nE '^pub async fn test_g1_parallel_execution' crates/ucil-daemon/src/executor.rs | grep -v 'mod tests'
grep -cE 'G1Source|G1Query|G1ToolKind|G1ToolStatus|G1ToolOutput|G1Outcome|execute_g1|G1_MASTER_DEADLINE|G1_PER_SOURCE_DEADLINE' crates/ucil-daemon/src/lib.rs   # ≥ 1 (re-export line)

# 2. Build + clippy + test (clean state)
cargo clean
env -u RUSTC_WRAPPER cargo build -p ucil-daemon
env -u RUSTC_WRAPPER cargo clippy -p ucil-daemon --all-targets -- -D warnings
env -u RUSTC_WRAPPER cargo test -p ucil-daemon executor::test_g1_parallel_execution -- --nocapture

# 3. Phase 2 Week 6 regression sentinels
env -u RUSTC_WRAPPER cargo test -p ucil-daemon plugin_manager::test_hot_cold_lifecycle plugin_manager::test_manifest_parser plugin_manager::test_lifecycle_state_machine plugin_manager::test_hot_reload plugin_manager::test_circuit_breaker
env -u RUSTC_WRAPPER cargo test -p ucil-daemon --test plugin_manager
unset UCIL_SKIP_EXTERNAL_PLUGIN_TESTS
env -u RUSTC_WRAPPER cargo test -p ucil-daemon --test plugin_manifests
env -u RUSTC_WRAPPER cargo test --test test_plugin_lifecycle

# 4. Phase 1 regression sentinels
env -u RUSTC_WRAPPER cargo test -p ucil-daemon --test e2e_mcp_stdio --test e2e_mcp_with_kg
env -u RUSTC_WRAPPER cargo test --test test_lsp_bridge

# 5. Workspace pre-flight
env -u RUSTC_WRAPPER cargo test --workspace --no-fail-fast

# 6. Coverage gate (manual workaround per WO-0046 lessons line 270)
bash scripts/verify/coverage-gate.sh ucil-daemon 85 75 || \
  ( env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --summary-only ; \
    LLVM_PROFDATA="$(rustc --print target-libdir)/../bin/llvm-profdata" ; \
    find target/llvm-cov-target -name '*.profraw' -size 0 -delete ; \
    find target/llvm-cov-target -name '*.profraw' -print0 | while IFS= read -r -d '' f; do "$LLVM_PROFDATA" show "$f" >/dev/null 2>&1 || rm -f "$f"; done ; \
    env -u RUSTC_WRAPPER cargo llvm-cov report --package ucil-daemon --summary-only )

# 7. Pre-baked mutation A (master-deadline guard neutered) — runtime-only variant
#    Per WO ready-for-review marker lines 69–98; replace execute_g1 body with
#       { let _ = query; let _ = sources; let _ = deadline;
#         tokio::task::yield_now().await; G1Outcome::default() }
#    Drop the `#[tracing::instrument(...)]` attribute.
#    Run `cargo test -p ucil-daemon --lib executor::test_g1_parallel_execution`
#    Expect: panic at executor.rs:1896 (parallel-timing lower bound) OR
#    executor.rs:1913 (results.len() != 4). Restore via
#    `git checkout -- crates/ucil-daemon/src/executor.rs`.

# 8. Pre-baked mutation B (per-source timeout wrapper neutered) — runtime-only variant
#    Per WO ready-for-review marker lines 100–115; replace
#       tokio::time::timeout(per_source_deadline, source.execute(query)).await
#    with
#       { let _ = per_source_deadline;
#         Ok::<_, tokio::time::error::Elapsed>(source.execute(query).await) }
#    Run `cargo test -p ucil-daemon --lib executor::test_g1_parallel_execution`
#    Expect: panic at executor.rs:2001 (TimedOut count = 0) and
#    executor.rs:2013 (wall-time exceeds 5500 ms ceiling).
#    Restore via `git checkout -- crates/ucil-daemon/src/executor.rs`.

# 9. Optional verifier-extension (per WO-0045 lessons line 199):
#    Replace `pub async fn run_g1_source<...>` body inner future construction
#    `tokio::time::timeout(per_source_deadline, source.execute(query))` with
#    `std::future::ready(G1ToolOutput { kind: source.kind(), status: G1ToolStatus::TimedOut,
#       elapsed_ms: 0, payload: serde_json::Value::Null, error: Some("forced".into()) })`
#    Expect: scenarios (a) and (b) both fail because every source returns
#    TimedOut regardless of behaviour. Restore.

# 10. End-to-end driver
bash scripts/verify/P2-W7-F01.sh    # → [OK] P2-W7-F01

# 11. Allow-list invariants (three-dot diff per WO-0046 lessons line 250)
[ "$(git diff --name-only main...HEAD | sort)" = "$(printf 'crates/ucil-daemon/src/executor.rs\ncrates/ucil-daemon/src/lib.rs\nscripts/verify/P2-W7-F01.sh\nucil-build/work-orders/0047-ready-for-review.md\n' | sort)" ]
[ -z "$(git diff --name-only main...HEAD -- '*.toml')" ]
[ -z "$(git diff --name-only main...HEAD -- 'tests/fixtures/**')" ]
[ -z "$(git diff --name-only main...HEAD -- 'ucil-build/feature-list.json' 'ucil-build/feature-list.schema.json')" ]
[ -z "$(git diff --name-only main...HEAD -- 'ucil-master-plan-v2.1-final.md')" ]
```

## Summary

CLEAN — no blockers. Three soft warnings (one production-code
`.expect(...)` in a private helper with a structurally-enforced
invariant; two commits 3 LOC and 74 LOC over the 200-LOC soft
target, both authorised by DEC-0005 module-coherence). Verifier
may proceed in a fresh session; pre-baked mutation patterns are
documented in the ready-for-review marker with runtime-only
variants for the `#![deny(warnings)]` cascade case (now 6th
consecutive WO using the WO-0046 runtime-only mutation pattern).

— UCIL critic
