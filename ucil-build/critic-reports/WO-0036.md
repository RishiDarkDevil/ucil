# Critic Report: WO-0036

**Critic session**: crt-WO-0036
**Branch**: `feat/WO-0036-understand-code-tool`
**Head commit**: `3432d1530041b397e8c39681b83352fe94b77a17`
**Merge base**: `6e90d54c11aa86f983badb06cd1879ed99a3ef21`
**Feature**: `P1-W4-F09` — `understand_code` MCP tool (tree-sitter AST + KG fusion).
**Verdict**: **CLEAN**

## Scope of review

Commits (6, all pushed):

| SHA | Subject | Diff lines |
|-----|---------|-----------|
| `5712d52` | feat(treesitter): add Language::from_extension helper | +93 / -0 |
| `c568eea` | feat(daemon): wire understand_code MCP tool (KG+tree-sitter) | +1,195 / -1 |
| `fbe3238` | test(daemon): add frozen acceptance test for understand_code (file mode) | +300 / -2 |
| `aefd46d` | test(daemon): add supplementary understand_code tests (7 variants) | +305 / -0 |
| `3d2a0dd` | refactor(daemon): satisfy clippy pedantic/nursery on understand_code | +264 / -231 |
| `3432d15` | chore(wo): WO-0036 ready-for-review marker | +226 / -0 |

Files touched (7 total, no forbidden paths):
`Cargo.lock`, `crates/ucil-daemon/Cargo.toml`, `crates/ucil-daemon/src/lib.rs`,
`crates/ucil-daemon/src/server.rs`, `crates/ucil-daemon/src/understand_code.rs`
(new, 1,224 lines), `crates/ucil-treesitter/src/parser.rs`,
`ucil-build/work-orders/0036-ready-for-review.md`.

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Broader visibility than `scope_in` specified.**
   WO §scope_in bullets 2, 3, 7 call for `pub(crate) struct UnderstandCodeFileSummary`,
   `pub(crate) struct FileSymbolRow`, `pub(crate) struct UnderstandCodeSymbolSummary`,
   `pub(crate) enum UnderstandCodeError`. The implementation declares them all as
   `pub` (see `crates/ucil-daemon/src/understand_code.rs:92, 147, 173, 205, 252, 278,
   325, 421, 474, 695, 833` and the `pub mod understand_code;` re-export in
   `crates/ucil-daemon/src/lib.rs:76`). Functionally equivalent at the crate
   boundary because the module itself is public, but the surface API is wider than
   the WO contracted. Not a correctness issue; recommend tightening to
   `pub(crate) mod understand_code;` + `pub(crate) ...` or documenting the
   deviation in the `ready-for-review.md` notes so the verifier knows it was
   intentional.

2. **Commit cadence deviates from WO `estimated_commits: 10` target and `≤50 LOC`
   soft rule.**
   - `c568eea` lands the entire `understand_code.rs` module (1,177 lines added) in
     a single commit. The WO's acceptance criterion #12 listed 9 separable logical
     steps (helper → enum → count_imports → explain_file → explain_symbol →
     handler → error paths → tests → polish). Only 6 commits landed.
   - `fbe3238` (+279 lines of test support + main acceptance test in one commit)
     and `aefd46d` (+305 lines, seven supplementary tests in one commit) also
     exceed the 50-LOC target.
   - `3d2a0dd` is a 635-line refactor (264+/231-) that mixes three independent
     concerns (delegate split, `collect_edges` extraction, assert helper split +
     clippy rename).
   None of these are hard violations of the anti-laziness contract, but they do
   make a clean bisect harder and contradict `.claude/rules/commit-style.md` “One
   logical change per commit. ~50 lines of diff per commit is a soft target.”

3. **`ready-for-review.md` miscounts commits.**
   `ucil-build/work-orders/0036-ready-for-review.md:30` header says
   *“Commits (5, all pushed)”* and the table lists 5 rows, but the branch has 6
   commits (the review marker itself is commit `3432d15`). Self-referential gap —
   the marker commit obviously post-dates the body — but the numbering is wrong
   and should be regenerated or annotated as "5 implementation + 1 marker".

### OK

- **No stubs introduced.** `rg -nE 'todo!\(|unimplemented!\(|NotImplementedError|\braise NotImplementedError'` across the diff → zero hits in shipped code. Phase-1 invariant #9 preserved for the 18 still-stubbed tools; `test_understand_code_tool_no_kg_returns_stub` explicitly pins that behaviour.
- **No skipped / ignored tests.** `rg -nE '#\[ignore\]|\bit\.skip\(|\bxit\(|@pytest\.mark\.skip|@pytest\.mark\.xfail|\.skip\('` across the diff → zero hits.
- **No mocks of critical collaborators.** The sole `mock|fake|MagicMock|Mockall` hits in `server.rs` are rustdoc lines explicitly stating *"No mocks of serde_json / tokio::io / SQLite / ignore / grep-searcher"*. `build_understand_code_fixture` uses a real `KnowledgeGraph::open` over a real `tempfile::TempDir`, a real `IngestPipeline::ingest_file` running real tree-sitter grammars against `tests/fixtures/rust-project/src/util.rs`, and real `upsert_entity` / `upsert_relation` to seed the synthetic caller. `McpServer::with_knowledge_graph` dispatches against that live KG.
- **No weak assertions.** `rg -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)'` → zero hits. Every assertion in the new tests names a concrete expected value and renders the response on failure.
- **No panicking `.unwrap()` / `.expect()` outside `#[cfg(test)]`** in shipped code. All unwraps in `understand_code.rs` are `unwrap_or` / `unwrap_or_else` with well-reasoned fallbacks (`understand_code.rs:486, 500, 513, 746, 841, 859, 1038, 1066, 1073`). Tests may use `.expect()` freely per style rules.
- **Imports are all real.** Cross-checked every `use` in `understand_code.rs` and the new `server.rs` test block:
  - `ucil_core::{Entity, KnowledgeGraph, KnowledgeGraphError, Relation}` — re-exported at `crates/ucil-core/src/lib.rs:28-30`.
  - `ucil_treesitter::{Language, ParseError, Parser, SymbolExtractor, SymbolKind}` — re-exported at `crates/ucil-treesitter/src/lib.rs:16-17`.
  - `crate::server::{jsonrpc_error, JSONRPC_VERSION}` — both exist (`server.rs:61` const, `server.rs:979` fn, promoted to `pub(crate)` by this WO).
  - `crate::executor::{rust_project_fixture, IngestPipeline, SOURCE_TOOL, TREE_SITTER_VALID_FROM}` — all four exist (`executor.rs:87, 101, 359, 511`).
  - `tree-sitter` + `streaming-iterator` added to `crates/ucil-daemon/Cargo.toml:45-52` and resolve via workspace roots (`Cargo.toml:66, 109`).
- **Feature coverage.** Feature `P1-W4-F09` maps to the frozen selector
  `server::test_understand_code_tool` (feature-list §P1-W4-F09 acceptance_tests).
  That test is present at `server.rs:3440`. Seven supplementary tests land
  alongside it — `test_understand_code_tool_symbol_mode`, `…_auto_detect_file`,
  `…_missing_target`, `…_empty_target`, `…_invalid_kind`, `…_unknown_symbol`,
  `…_no_kg_returns_stub` — covering every acceptance bullet in the WO.
- **Commit hygiene otherwise green.**
  - All 6 commits follow Conventional Commits (`feat(scope): …`, `test(…)`,
    `refactor(…)`, `chore(…)`).
  - Every feat/fix/refactor/test commit body carries `Phase: 1`, `Feature:
    P1-W4-F09`, `Work-order: WO-0036` trailers and the required
    `Co-Authored-By: Claude Opus 4.6 (1M context)` line.
  - No `--amend` or force-push traces in `git reflog` / `git log --pretty`.
- **Rustdoc on new public API.** Every `pub` item in `understand_code.rs` has a
  preceding doc comment referencing the master-plan anchor and/or the WO
  (`UNDERSTAND_CODE_MAX_EDGES`, `UNDERSTAND_CODE_MAX_TOP_LEVEL_SYMBOLS`,
  `UnderstandCodeError`, `FileSymbolRow`, `UnderstandCodeFileSummary`,
  `EntitySummary`, `EntitySummary::from_entity`, `RelationEdge`,
  `UnderstandCodeSymbolSummary`, `count_imports`, `language_tag`, `explain_file`,
  `explain_symbol`, `handle_understand_code`). `Language::from_extension` in
  `parser.rs:82` carries rustdoc with a runnable `# Examples` block plus
  references to master-plan §18 Phase 1 Week 2 line 1744 and feature P1-W4-F09.
- **No forbidden-path edits.** `git diff --name-only main..HEAD` confirms zero
  touches to `tests/fixtures/**`, `scripts/gate/**`,
  `scripts/flip-feature.sh`, `ucil-build/feature-list.json`,
  `ucil-build/feature-list.schema.json`, the master plan, or any DEC-000[1-9]
  ADR.
- **Phase-1 invariants preserved.**
  - Invariant #5 (`ucil-treesitter` depends only on `ucil-core` + tree-sitter —
    no daemon crates): the helper lands inside `ucil-treesitter`, no new upstream
    dep added.
  - Invariant #9 (other 18 tools keep the stub): `test_understand_code_tool_no_kg_returns_stub` asserts the fallback; the dispatch branch is additive, surrounded by `if let Some(kg) = self.kg.as_ref()`.
  - `cargo nextest run -p ucil-daemon server::test_all_22_tools_registered` is pinned in the ready-for-review marker as PASS.

## Reality-check evidence (from ready-for-review marker)

The ready-for-review marker at `ucil-build/work-orders/0036-ready-for-review.md`
reports that stashing `handle_understand_code` + the dispatch branch and re-running
`cargo nextest run -p ucil-daemon server::test_understand_code_tool` causes the
acceptance test to FAIL (happy-path assertions break because the stub response
drops `_meta.summary`). Popping restores GREEN. The verifier should re-run this
mutation step independently per DEC-0007.

## Recommendation

Proceed to verifier. Warnings 1-3 are non-blocking and can be addressed either
in this WO's follow-up polish commit or deferred to a subsequent cleanup WO.
