# Critic Report: WO-0034

**Critic session**: crt-WO-0034-20260418
**Branch**: `feat/WO-0034-get-conventions-tool`
**Ref reviewed**: `7aaac3cad813c7080feeeab1102a12024f9f9444`
**Range**: `main..feat/WO-0034-get-conventions-tool` (6 commits, +1087 / –16 LOC)
**Feature(s)**: `P1-W4-F10` — `get_conventions` MCP tool (master-plan §3.2 row 7)
**Verdict**: **CLEAN**

## Summary

WO-0034 promotes `get_conventions` from the Phase-1 22-tool stub
catalog (second tool after WO-0033's `find_definition`) to a real
knowledge-graph-backed handler that reads the SQLite `conventions`
table. Diff is confined to three source files plus a ready-for-review
marker:

- `crates/ucil-core/src/knowledge_graph.rs` — +1 `pub struct Convention`
  (10 fields mirroring the §12.1 DDL), +2 `pub` helpers
  (`insert_convention`, `list_conventions`), +1 private row decoder
  (`convention_from_row`), +4 module-root unit tests.
- `crates/ucil-core/src/lib.rs` — +1 re-export
  (`pub use knowledge_graph::{Convention, Entity, Relation}`).
- `crates/ucil-daemon/src/server.rs` — +1 dispatch branch in
  `handle_tools_call`, new `handle_get_conventions` method, 3 private
  helpers (`read_conventions`, `convention_to_json`,
  `get_conventions_found_response`), +1 `GetConventionsReadError`
  struct, +1 `build_get_conventions_fixture` helper, +4 module-root
  tests (acceptance + empty + no-KG stub + non-string category).
- `ucil-build/work-orders/0034-ready-for-review.md` — new marker.

All 8 anti-laziness checks pass. No stubs, no mocks of critical
collaborators, no skipped tests, no weak assertions, no hallucinated
imports, every new `pub` item carries rustdoc, and the frozen
acceptance selector `server::test_get_conventions_tool` resolves at
the module root per DEC-0005.

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate — or justify now)

1. **Commits above the ≤50-LOC soft target.**
   - `77a0134 feat(core): add Convention struct` — 68 insertions.
   - `3e54fa6 feat(core): add insert_convention + list_conventions
     helpers` — 144 insertions.
   - `df657a2 test(core): cover conventions CRUD helpers` — 165
     insertions.
   - `0cc81ce feat(daemon): wire get_conventions MCP tool` — 187 / –15.
   - `f3a9b29 test(daemon): cover get_conventions tool end-to-end` —
     379 insertions.

   `critic.md` rule is "no commit > 200 lines of diff without a good
   reason." Only `f3a9b29` (379 LOC) exceeds that hard line, but it
   bundles the `build_get_conventions_fixture` helper with the four
   tests that share it plus `#[allow(clippy::too_many_lines)]` doc —
   splitting would leave a transiently-unused fixture or fail
   `-D warnings` between intermediate commits. The other commits
   range 68–187 LOC, dominated by rustdoc blocks (every new `pub`
   item has ≥10 lines of doc plus inline field docs). Acceptable per
   the commit-style.md "soft target" framing, but flagging so the
   verifier sees the estimated 8 commits were met with 6.

2. **Ready-for-review commit count mismatch.** The marker at
   `ucil-build/work-orders/0034-ready-for-review.md:19` says
   "Commits (5)" listing the 5 source commits; the branch carries a
   6th commit (`7aaac3c docs(work-order): mark WO-0034 ready for
   review`) which is the marker-commit itself and thus self-referentially
   uncounted. Not a blocker — the WO estimated 7–8 commits, actual
   is 6 source-inclusive. No action required.

### OK

- **Stub detection** (check 1): `todo!()`, `unimplemented!()`,
  `NotImplementedError`, `raise NotImplementedError`, and `pass`-only
  bodies → **0 hits** across `git diff main..HEAD -- '*.rs'`
  (`grep -nE 'todo!\(|unimplemented!\(|NotImplementedError|panic!\('`).
  The handler returns real JSON envelopes built from real
  `KnowledgeGraph::list_conventions` reads on a real tempfile-backed
  SQLite, not default-literal placeholders. The phrase "stub"
  appears only in comments and doc-strings documenting phase-1
  invariant #9 for the remaining 20 tools — never in a reachable
  code path.

- **Mocked critical deps** (check 2): No matches for
  `mock|Mock|fake|Fake|MagicMock|Mockall` against
  `serena|lsp|pyright|rust-analyzer|rusqlite|sqlx|lancedb|docker`
  in any new test. Textual hits of "mock" in the diff are
  affirmations that tests do **not** mock (e.g.
  `knowledge_graph.rs:2144 "No mocks of rusqlite"`). The acceptance
  fixture `build_get_conventions_fixture` at `server.rs:1680–1725`
  opens a **real** tempfile-backed SQLite via
  `KnowledgeGraph::open(tmp.path().join("knowledge.db"))` and inserts
  rows via the just-added real `insert_convention` helper. Four KG
  unit tests at `knowledge_graph.rs:2745–2873` also use real
  `tempfile::TempDir` + `KnowledgeGraph::open`.

- **Skipped / ignored tests** (check 3): 0 new `#[ignore]` / `.skip(`
  / `xfail` / `xit(` / `@pytest.mark.skip` in any of the 8 new tests
  (4 in `knowledge_graph.rs`, 4 in `server.rs`). No pre-existing
  skipped tests in the changed files.

- **Weak assertions** (check 4): 0 hits for `assert!(true)`,
  `assert_eq!(true, true)`, `assert_eq!(1, 1)`, `expect(true).toBe(true)`.
  Every assertion in the 8 new tests checks a concrete projection of
  the JSON-RPC response envelope, Convention struct, or KG row —
  `_meta.tool == "get_conventions"`, `_meta.source == "kg"`,
  `_meta.count == {0,1,2}`, `_meta.category == "naming" | null`,
  `_meta.conventions[i].category`, `_meta.conventions[i].pattern`,
  `result.isError == false`, `result._meta.not_yet_implemented` is
  present (no-KG) or absent (KG happy path), `error.code == -32602`,
  round-trip of `confidence` (f64), `evidence_count` (i64), `scope`,
  and `t_ingested_at` populated by schema default.

- **Hallucinated paths** (check 5): All new `use` statements resolve
  to symbols that exist on the branch —
  - `ucil_core::{Convention, KnowledgeGraph}` —
    `crates/ucil-core/src/lib.rs:28,30` (Convention newly added).
  - `tempfile::TempDir`, `serde_json::{json, Value}`,
    `std::sync::{Arc, Mutex}` — standard deps already in
    `crates/ucil-daemon/Cargo.toml:42–43` (`[dev-dependencies]
    tempfile.workspace = true`) and the `serde_json` / `tokio` deps
    from previous WOs.
  - `rusqlite::params![]` in `insert_convention` — `rusqlite` is
    already a declared dep in `crates/ucil-core/Cargo.toml`.
  - No new `ucil_core::*` or `ucil_daemon::*` names invented — every
    referenced helper (`execute_in_transaction`, `list_conventions`,
    `insert_convention`, `jsonrpc_error`, `JSONRPC_VERSION`,
    `McpServer::new`, `McpServer::with_knowledge_graph`,
    `handle_line`) either pre-existed or is added in the same diff.

- **Feature coverage** (check 6): `WO-0034` declares
  `feature_ids: ["P1-W4-F10"]`. The frozen selector
  `server::test_get_conventions_tool` exists at module root
  (`server.rs:1753` on the branch, inside neither `mod tests { }`
  nor `mod <anything> { }` — direct `#[cfg(test)] #[test] fn …`).
  Plus three negative-path tests (`server::test_get_conventions_
  tool_empty` — empty-table; `server::test_get_conventions_tool_
  no_kg_returns_stub` — phase-1 invariant #9 for no-KG path;
  `server::test_get_conventions_tool_non_string_category` — JSON-RPC
  `-32602`) and four KG read-helper unit tests
  (`knowledge_graph::test_insert_and_list_conventions_all`,
  `::test_list_conventions_category_filter`,
  `::test_list_conventions_unknown_category_returns_empty`,
  `::test_list_conventions_empty_table`) that exercise the new
  helpers the handler depends on. Every code path introduced by the
  WO is covered.

- **Commit hygiene** (check 7): 6 commits, all Conventional-Commits
  subjects (`feat(core):` ×2, `feat(daemon):` ×1, `test(core):` ×1,
  `test(daemon):` ×1, `docs(work-order):` ×1). Every `feat/test/
  docs` commit body carries the required `Phase: 1` /
  `Feature: P1-W4-F10` / `Work-order: WO-0034` trailers **and** the
  `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic
  .com>` line mandated by `.claude/rules/commit-style.md`. No
  `--amend` / `--no-verify` / `git push --force` patterns in
  `git reflog` for this branch. See Warning #1 for the sole
  over-200-LOC commit (test bundle, justified).

- **Doc + public API** (check 8): Every new `pub` item has rustdoc —
  - `pub struct Convention` — `knowledge_graph.rs:304–326` (22-line
    struct-level doc referencing master-plan §12.1 + §3.2 row 7) plus
    per-field rustdoc on all 10 fields (`id`, `category`, `pattern`,
    `examples`, `counter_examples`, `confidence`, `evidence_count`,
    `t_ingested_at`, `last_verified`, `scope`).
  - `pub fn insert_convention` — `knowledge_graph.rs:1207–1228` with
    `# Errors` section.
  - `pub fn list_conventions` — `knowledge_graph.rs:1251–1287` with
    `# Errors` section plus explicit "empty list if none yet
    extracted" contract reference.
  - `Convention` re-export at `lib.rs:28` sits in the existing
    `pub use` block with the same module-doc already present.
  - Private `handle_get_conventions` / `read_conventions` /
    `convention_to_json` / `get_conventions_found_response` /
    `GetConventionsReadError` all carry rustdoc even though they are
    crate-private — matches the `find_definition` helper-doc
    precedent from WO-0033.
  - `McpServer::kg` field doc and `McpServer::with_knowledge_graph`
    doc were **extended** (not replaced) to mention `get_conventions`
    alongside `find_definition` (`server.rs:317–324`, `349–367`).

## Cross-WO invariants

- **Phase-1 invariant #9 (20 stub tools)**: preserved. The dispatch
  guard at `server.rs:502–509` (`if name == "get_conventions" { if
  let Some(kg) = … }`) only routes when a KG handle is attached; the
  default-constructed `McpServer::new()` path still produces
  `_meta.not_yet_implemented: true` for `get_conventions`. The
  dedicated `test_get_conventions_tool_no_kg_returns_stub` test at
  `server.rs:1983` asserts this explicitly. The pre-existing
  `test_all_22_tools_registered` at `server.rs:974` (unchanged) still
  exercises a default-constructed server and continues to observe
  the stub envelope for all 22 tools. Invariant count drops from 21
  → 20 still-stubbed tools, which matches the updated McpServer
  docstring.
- **DEC-0005 (module-root test placement)**: preserved. All 4 new
  server tests live at module root (`#[cfg(test)] #[test]` /
  `#[tokio::test]` without a `mod tests { … }` wrapper), matching
  the placement of the frozen WO-0010 selector and the WO-0033
  `test_find_definition_tool`.
- **DEC-0007 (no per-WO cargo-mutants)**: respected. `get_
  conventions` extends an existing module; `reality-check.sh` handles
  mutation verification (stashing `handle_get_conventions` + its
  dispatch branch should cause `test_get_conventions_tool` to FAIL
  per the verifier step 6b contract).
- **DEC-0008 (Serena LSP bridge)**: respected. WO-0034 does **not**
  wire Serena anywhere; conventions are a cold-table read that needs
  no symbol-resolution layer. Acceptance selectors remain
  `cargo nextest`-resolvable without Serena in the loop.
- **Forbidden paths**: zero touches on `ucil-build/feature-list
  .json`, `feature-list.schema.json`, `ucil-master-plan-v2.1-final
  .md`, `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-
  feature.sh`, `.githooks/**`, or `ucil-build/decisions/DEC-000{1..8}
  -*.md` (verified via `git diff --name-only`).

## Verdict

**CLEAN** — no blockers. Recommend proceeding to verifier.

The two warnings are informational: one large (379-LOC) test-bundle
commit carries its justification inline (fixture-and-tests atomicity
under `-D warnings`), and a self-referential commit-count discrepancy
in the ready-for-review marker is harmless. Phase-1 invariant #9,
DEC-0005, DEC-0007, DEC-0008, and every forbidden-path rule are
preserved. Every new `pub` item carries rustdoc; every new test uses
real SQLite via `tempfile::TempDir` + `KnowledgeGraph::open`; no
mocks of rusqlite, serde_json, tokio, Serena, LSP, LanceDB, or
Docker anywhere in the diff.
