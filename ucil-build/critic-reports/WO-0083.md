---
work_order: WO-0083
slug: architecture-mcp-tools
phase: 3
week: 10
features: [P3-W10-F16, P3-W10-F17, P3-W10-F18]
branch: feat/WO-0083-architecture-mcp-tools
ref: 4c0907954acbaa758bcf14db624c66f7a82e720f
merge_base: 238e7bd110b858ca2f2775015684148d1e640247
---

# Critic Report: WO-0083 — `get_architecture` / `trace_dependencies` / `blast_radius` MCP tools

**Critic session**: crt-wo-0083-pre-verifier
**Branch**: `feat/WO-0083-architecture-mcp-tools`
**Verdict**: **CLEAN**

## Diff at a glance

```
crates/ucil-daemon/src/server.rs                                                | +1573 / -18
ucil-build/work-orders/0083-ready-for-review.md                                 |  +120
ucil-build/escalations/20260508T1635Z-merge-wo-skip-verifier-on-main-branch.md  |     0 / -235  (branch-positioning artifact, see Warning #1)
```

Three commits, zero merge commits:

```
4c09079 chore(work-order): WO-0083 ready-for-review marker
a400673 test(daemon): frozen tests for G4 architecture MCP tools (P3-W10-F16/F17/F18)
63b9ebd feat(daemon): wire G4 architecture MCP tools (P3-W10-F16/F17/F18)
```

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (verifier should be aware; not blocking)

1. **Feat branch is one commit behind main — `git diff main..feat/...` shows a phantom escalation deletion**
   - `git merge-base main feat/WO-0083-architecture-mcp-tools` → `238e7bd` (planner WO-0083 commit).
   - `main` advanced to `a594e6f` after divergence, adding `ucil-build/escalations/20260508T1635Z-merge-wo-skip-verifier-on-main-branch.md`.
   - The full diff therefore shows that escalation file as `-235`, but `git show --numstat 63b9ebd` proves the **feat commit only edits `crates/ucil-daemon/src/server.rs`** (no file deletions, no escalation touched by any commit on the branch).
   - Action for verifier: rebase the feat branch onto `main` (or merge with a 3-way merge that preserves files added on `main` post-divergence) before fast-forward merging. The escalation file will be carried forward correctly under either path.
   - Not an executor mistake; the merge-base is just stale.

2. **`McpServer::with_g4_sources` rustdoc has no `# Examples` block** (`server.rs:635-661`)
   - `.claude/rules/rust-style.md` requires "Every public item has a rustdoc with `# Examples` when non-trivial."
   - The body is a one-line `self.g4_sources = Some(sources); self` so this is on the trivial side; the existing peer builders (`with_g2_sources`, `with_find_similar_executor`) also ship without `# Examples`, so this matches local precedent.
   - The three handler methods (`handle_get_architecture` `server.rs:1353`, `handle_trace_dependencies` `server.rs:1475`, `handle_blast_radius` `server.rs:1625`) each carry an `# Examples` `ignore`-doctest per scope_in #34 — the load-bearing rustdoc requirement is satisfied.
   - Soft suggestion: a follow-up `docs:` commit could add an example referencing the daemon-startup-orchestrator wiring shape `McpServer::new().with_g4_sources(Arc::new(vec![g4_a, g4_b]))`. Not blocking.

### OK (passed)

#### 1. Stub detection — clean
- No `todo!()` / `unimplemented!()` / `NotImplementedError` / `raise NotImplementedError` / pass-only bodies in the new production code.
- The literal word `stub` appears 14× in the diff but every occurrence is a documentation reference to the **phase-1 invariant #9 stub fall-through path** (`_meta.not_yet_implemented: true`) that the new dispatch wiring deliberately preserves when `g4_sources.is_none()` — that is the spec'd behaviour, not a stub implementation. (`server.rs:820-836`, doc-comments at `server.rs:486-488`, `503-507`, `546-549`, `622-624`, `645-650`, `787-789`, `821`, `838-841`, `854`.)
- No trivial-default returns (no `return None;` / `return Default::default();` / `return {};` shaped feature implementations).

#### 2. Mocked critical dependencies — clean
- The `TestG4Source` (`server.rs:6407-6428`) impls the **UCIL-internal `G4Source` trait** per DEC-0008 §4 dependency-inversion seam — explicitly endorsed by scope_in #8 + #12 + #40 of the work-order, and aligned with the WO-0070 `TestG3Source` precedent.
- `G4Source` is **not** a critical-collaborator surface (Serena MCP, LSP servers, SQLite, LanceDB, Docker) — it is an internal Rust trait whose production-side adapters (`CodeGraphContextG4Source`, `LSPCallHierarchyG4Source`) are deferred to a follow-up production-wiring WO per scope_out #1 / #2.
- Grep `mock|fake` over the new test region (`/tmp/wo-0083-server.rs:6380..6953`) returns empty.

#### 3. Skipped / ignored tests — clean
- Zero `#[ignore]`, `.skip()`, `xfail`, `xit`, `@pytest.mark.skip`, or `@pytest.mark.xfail` in the diff.
- The existing `#[cfg(test)]` carve-out for the `TestG4Source` helper + `make_g4_test_edge` builder + `test_g4_source_list` factory is the correct way to gate test-only items per scope_in #12 + WO-0048 line 363.
- Three frozen tests run unconditionally inside `#[tokio::test]`.

#### 4. Weak assertions — clean
- 30 substantive `assert!` / `assert_eq!` calls across the three frozen tests (`server.rs:6481-6952`).
- 63 `SAn`-numbered panic-message lines in the canonical `(SAn) <semantic name>; left: ..., right: ...` format per DEC-0007 + WO-0067/0068/0070/0072 precedent — verifier can read a single panic line and identify which test, which SA, and what failed.
- No `assert!(true)` / `assert_eq!(true, true)` / `expect(true).toBe(true)` patterns.

#### 5. Hallucinated paths / imports — clean
All `use crate::g4::{...}` items verified against `crates/ucil-daemon/src/g4.rs`:
- `execute_g4` → `g4.rs:697` (and the public function above).
- `G4Query` → `g4.rs:127`.
- `G4Source` trait → `g4.rs:365`.
- `G4SourceOutput` → `g4.rs:245`.
- `G4SourceStatus` → `g4.rs:233`.
- `G4UnifiedEdge` → `g4.rs:286`.
- `G4UnionOutcome` → `g4.rs:321`.
- `G4_MASTER_DEADLINE` → `g4.rs:100`.
- `G4DependencyEdge` (used as `crate::g4::G4DependencyEdge`) → `g4.rs:204`.
- `G4EdgeKind` (used as `crate::g4::G4EdgeKind`) → `g4.rs:153` (variants `Import`, `Call`, `Inherits`, `Implements`, `Other(String)`).
- `G4EdgeOrigin` (used as `crate::g4::G4EdgeOrigin::Inferred`) → `g4.rs:185`.
- `G4BlastRadiusEntry` (used as `crate::g4::G4BlastRadiusEntry`) → `g4.rs:305`.
- `merge_g4_dependency_union` (used as `crate::g4::merge_g4_dependency_union`) → `g4.rs:697`.
- External crates (`serde_json`, `tokio`, `tracing`, `async_trait`, `Arc`, `BTreeMap`, `BTreeSet`, `VecDeque`) are all already in `crates/ucil-daemon/Cargo.toml` per scope_out #6 — no new dependencies.

#### 6. Feature coverage — clean
Every `feature_id` in the work-order has a dedicated new frozen test that references the feature's tool-name behaviour:
- **P3-W10-F16** → `server::test_get_architecture_tool` (`server.rs:6481`) — exercises `handle_get_architecture` end-to-end with 4 edges over 4 nodes; SA1-SA8 cover `_meta.tool` / `_meta.source` / `_meta.modules` / `_meta.edges` / max coupling_weight / `_meta.master_timed_out` / `_meta.source_results`.
- **P3-W10-F17** → `server::test_trace_dependencies_tool` (`server.rs:6613`) — exercises `handle_trace_dependencies` with chain+fork graph; SA1-SA7 cover the directional BFS in both `direction == "both"` and `direction == "upstream"` (load-bearing absence-of-`_meta.downstream` invariant).
- **P3-W10-F18** → `server::test_blast_radius_tool` (`server.rs:6787`) — exercises `handle_blast_radius` with 5-node BFS tree; SA1-SA8 cover single-string→array lifting, seed exclusion, descending sort invariant, array-target preservation, dependency_chain shape, and master-deadline canary.

Each test resolves uniquely under substring-match per DEC-0007:
- `cargo test -p ucil-daemon server::test_get_architecture_tool` → 1 match
- `cargo test -p ucil-daemon server::test_trace_dependencies_tool` → 1 match
- `cargo test -p ucil-daemon server::test_blast_radius_tool` → 1 match

(The three are placed at module root, not inside any inner `mod tests { … }` — confirmed by `grep -n 'fn test_(get_architecture|trace_dependencies|blast_radius)_tool' /tmp/wo-0083-server.rs`.)

#### 7. Commit hygiene — clean
- 3 commits total, all Conventional-Commits-formatted: `feat(daemon)` / `test(daemon)` / `chore(work-order)`.
- Every commit body carries the required trailers: `Phase: 3`, `Feature: P3-W10-F16` / `F17` / `F18`, `Work-order: WO-0083`.
- Zero merge commits per AC17: `git log feat/WO-0083-architecture-mcp-tools ^main --merges` is empty.
- Per-commit sizes: feat 982/-1, test +574, chore +120.
- The feat commit is large (~982 LOC), but **explicitly endorsed by scope_in #13 + #38** of the work-order: "the entire suite SHOULD ship as fewer larger cohesive commits — so an alternative shape is `feat(daemon): wire three architecture MCP tools (P3-W10-F16/F17/F18)` as a SINGLE commit at ~700-900 LOC. The single-commit shape is preferred for the M1/M2/M3 mutation-restoration contract since all three sites coexist in one diff." DEC-0005 module-coherence carve-out is cited in the commit body. The tests landed in a sibling commit, not folded in — that is the cleaner shape.
- No `--amend` after push, no force-push (verified by walking the linear feat-branch log).

#### 8. Doc + public API — clean
New `pub` items, all documented:
- `pub g4_sources: Option<Arc<Vec<Arc<dyn G4Source>>>>` field (`server.rs:513`) — 23-line rustdoc covering DEC-0008 §4 dependency-inversion, phase-1 invariant #9 fall-through, and the production-wiring deferral.
- `pub fn with_g4_sources(...)` (`server.rs:661`) — 26-line rustdoc citing master-plan §3.2 rows 8/9/10 + §5.4 + DEC-0008 §4 + WO-0010 catalog-count invariance. (See Warning #2 — no `# Examples` section, but matches local builder precedent.)
- Manual `impl std::fmt::Debug for McpServer` (`server.rs:516-533`) — replaces the prior `#[derive(Debug)]` on the struct (`#[derive(Clone)]` retained). The reason is correct: the `G4Source` trait does not require `Debug`, so an auto-derived `Debug` would not compile on `Arc<Vec<Arc<dyn G4Source>>>`. The manual impl surfaces the source-list length only — a sane operator-readable substitute.
- Three `async fn handle_*` methods (private to `impl McpServer`, but each carries a full rustdoc with `# Examples` ignore-doctest per scope_in #15 + #34): `handle_get_architecture` (`server.rs:1284-1417`), `handle_trace_dependencies` (`server.rs:1419-1564`), `handle_blast_radius` (`server.rs:1566-1696`). Each cites master-plan §3.2 row + feature ID + WO ID + DEC-0008 §4 + the underlying orchestrator.
- Twelve helper functions / consts (`collect_modules`, `project_unified_edges`, `format_edge_kind`, `project_source_results`, `format_g4_source_status`, `estimate_token_count`, `directional_bfs`, `project_bfs_chain`, `project_blast_radius_impacted`, `build_dependency_chains`, plus the six `*_DEFAULT_*` consts and the two `G4_MAX_*_CAP` consts) all carry rustdoc.
- Tracing: each handler is annotated with `#[tracing::instrument(name = "ucil.tool.<tool>", level = "debug", skip(id, params, sources))]` per master-plan §15.2 + scope_in #14 (`server.rs:1348-1352`, `1470-1474`, `1620-1624`).

#### Other invariants checked
- **No `unsafe`** in the new diff (grep over the new region returns empty).
- **No production `.unwrap()` / `.expect("...")`** in new handlers + helpers (`server.rs:1284..2162`); only `unwrap_or` (safe default) calls (9×). Test code uses `unwrap`/`expect` per the standard test-code carve-out (scope_in #27).
- **No `std::process::Command`**, no shell escapes, no network calls in the new code.
- **No new external crate dependencies** — `Cargo.toml` of `ucil-daemon` is untouched (verified via `git diff main..feat/... -- crates/ucil-daemon/Cargo.toml` returning empty).
- **Catalog count preserved at 22** — `grep -c 'ToolDescriptor::new' /tmp/wo-0083-server.rs` returns `22`, matching `/tmp/main-server.rs`. Three new dispatch arms wire **existing** tool names (`get_architecture` / `trace_dependencies` / `blast_radius`) per scope_in #25.
- **Phase-1 invariant #9 preserved** — when `g4_sources.is_none()`, the three new dispatch arms (`server.rs:822-836`) fall through to the `_meta.not_yet_implemented: true` branch unchanged.
- **Mutation contract M1/M2/M3** is documented in the RFR with file:line locations, exact patches, targeted SAs, expected panics, and md5 restoration commands. The disclosed deviation (M3 trips SA4 before SA5) is explicitly invoked under scope_in #20's "or analogous" + scope_in #21's spirit-over-literal carve-out — verifier-reviewable.

## Final verdict

**CLEAN** — no blockers. Two soft warnings (one branch-positioning bookkeeping note for the verifier's merge step, one minor rustdoc style suggestion). The executor implemented the full work-order surface with substantive tests, real `G4Source` impls (no critical-collaborator mocking), conventional commits, and a well-documented mutation contract. Forward to verifier.
