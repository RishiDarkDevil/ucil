# Critic Report: WO-0040

**Critic session**: crt-WO-0040-2026-04-19
**Branch**: `feat/WO-0040-ucil-daemon-mcp-stdio-subcommand`
**Head**: `c4316589eacd1ceec8634b1f2ae014c01ad8b6e5`
**Merge base**: `816e4b23c281eda9deb9b151ab4b8630249afc16`
**Verdict**: **CLEAN**

## Summary

WO-0040 is a gate-closure WO that wires `ucil-daemon mcp --stdio` into
`main.rs` and adds a black-box integration test. The diff is tight (22 /
-6 in `main.rs`, one new 146-line test file, plus the ready-for-review
marker) and does not touch any forbidden path. All acceptance_criteria
grep sentinels are present. No blockers found.

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Commit `Feature:` trailer is `none` on all three commits**
   (`070204a`, `5418513`, `c431658`).
   `.claude/rules/commit-style.md` states "Every feature commit includes
   `Feature: <ID>` referencing a real entry in `feature-list.json`".
   This WO legitimately has `feature_ids: []` (gate-closure, not feature
   delivery) and `lessons_applied` cites the WO-0007 / WO-0025 precedent
   for empty-feature WOs, so this is consistent with planner intent —
   flagging only for downstream awareness. Not blocking.

### OK

- **Stub detection** — clean. No `todo!()`, `unimplemented!()`,
  `NotImplementedError`, `raise NotImplementedError`, or bare-`pass`
  bodies in the changed files. The `_` arm of `match` in
  `crates/ucil-daemon/src/main.rs:21-25` preserves the existing Phase-0
  `tracing_subscriber::fmt::init() + tracing::info! + Ok(())` path
  byte-for-byte per `scope_in` — deliberate, not a stub.

- **Mocked critical deps** — clean. Grep of the new test file for
  `mock|fake|stub|MagicMock|serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker`
  matches only rustdoc context at
  `crates/ucil-daemon/tests/e2e_mcp_stdio.rs:14-16` ("does not start
  docker, does not rely on Serena … `McpServer::new()` is the stub
  path") — these are negative assertions describing what the test
  deliberately avoids, not instances of mocking critical collaborators.

- **Skipped / ignored tests** — clean. No `#[ignore]`, `.skip(`, `xit(`,
  `xfail`, `pytest.mark.skip` anywhere in the new test file.

- **Weak assertions** — clean. Every assertion in
  `crates/ucil-daemon/tests/e2e_mcp_stdio.rs` is substantive:
  - `:82` `output.status.success()` — process exit code
  - `:89` `!stdout.is_empty()` — the fail-closed fingerprint for the
    old Phase-0 skeleton main.rs (which emitted 0 bytes of JSON on
    stdout)
  - `:110` `tools.len() == 22` — exact count match for frozen surface
  - `:121-124` each of 22 `FROZEN_TOOLS` names present
  - `:137-140` each of 4 `CEQP_KEYS` on every tool's
    `inputSchema.properties`
  No `assert!(true)`, `assert_eq!(true, true)`, `expect(true)`.

- **Hallucinated imports / paths** — all references resolve:
  - `ucil_daemon::server::McpServer` in
    `crates/ucil-daemon/src/main.rs:16` → `pub mod server;` at
    `crates/ucil-daemon/src/lib.rs:83`, `pub struct McpServer` at
    `crates/ucil-daemon/src/server.rs:334`, `pub fn new()` at
    `server.rs:366`, `pub async fn serve` at `server.rs:420`.
  - `serde_json::Value` used in the integration test — `serde_json`
    is in `crates/ucil-daemon/Cargo.toml [dependencies]` (workspace
    entry); Cargo makes `[dependencies]` visible to integration tests
    in `tests/`, consistent with the executor's claim that the test
    compiles without a dev-deps bump.
  - `env!("CARGO_BIN_EXE_ucil-daemon")` — Cargo-injected for any
    `[[bin]] name = "ucil-daemon"` (declared at `Cargo.toml:11`).
  - `std::io::Write`, `std::process::{Command, Stdio}` — stdlib.

- **Feature coverage** — N/A. WO `feature_ids: []` (see warning #1);
  the single integration test directly exercises the behavior called
  out in the WO goal (the `mcp --stdio` dispatch in `main.rs`) and
  asserts the 22-tool + CEQP contract from master-plan §3 / §3.1.

- **Commit hygiene** — clean.
  - `070204a feat(daemon): wire mcp --stdio subcommand into main.rs` —
    Conventional Commits ✓, `Phase: 1 / Feature: none / Work-order:
    WO-0040` trailers ✓, +22/-6 LOC (≤50 soft target).
  - `5418513 test(daemon): e2e integration test — spawn binary, assert
    22 tools + CEQP` — Conventional Commits ✓, trailers ✓, +146 LOC
    (exceeds 50 soft target but falls under DEC-0005 module-introduction
    precedent, and is cited in the WO's `lessons_applied`).
  - `c431658 chore(work-orders): WO-0040 ready for review` —
    Conventional Commits ✓, trailers ✓, +38 LOC.
  - No `--amend`, no `--no-verify`, no force-push indicators in the
    log.

- **Doc + public API** — clean. No new `pub` items in `main.rs`
  (private `fn main` only). The module-level rustdoc on `main.rs` was
  refreshed to `Dispatches on the first positional argument …` — a
  plain-English single sentence with no intra-doc links, matching the
  rustdoc-link-landmine-avoidance lesson in the WO (no repeat of the
  WO-0009 / WO-0025 / WO-0027 / WO-0039 failure mode). The new test
  file has a thorough module rustdoc covering scope, intent, and the
  exact assertions made.

- **Forbidden-path check** — clean. Only touched files:
  `crates/ucil-daemon/src/main.rs`, `crates/ucil-daemon/tests/e2e_mcp_stdio.rs`,
  `ucil-build/work-orders/0040-ready-for-review.md`. None intersect
  the WO's `forbidden_paths` list (which covers every other daemon
  source module, all forbidden scripts/gates, `feature-list.json`,
  and the master plan).

- **Acceptance-criteria sentinels** — every `grep -q` sentinel in the
  WO's `acceptance_criteria` block is present on HEAD:
  `"mcp"`, `McpServer::new`, `.serve(`, `with_writer` in `main.rs`;
  `fn e2e_mcp_stdio_handshake_returns_22_tools_with_ceqp`,
  `CARGO_BIN_EXE_ucil-daemon` in the test file.

- **Stdout-pollution trap** — guarded. `main.rs:14-16` explicitly
  calls `tracing_subscriber::fmt().with_writer(std::io::stderr).init()`
  on the `mcp` arm, so tracing will not interleave with the JSON-RPC
  frames on stdout. This matches the acceptance-criteria requirement
  and the `lessons_applied` call-out.

## Notes for the verifier

- The verifier should still run `cargo clean && cargo test -p
  ucil-daemon --test e2e_mcp_stdio` from a fresh worktree to confirm
  the executor's local-green claim (root CLAUDE.md: "Trust your own
  test output — if you are the verifier, run `cargo clean && cargo
  test` yourself").
- The WO's `acceptance[7]` requires a mutation check (stash the
  `.serve(stdin, stdout).await` call → test fails; restore → test
  passes). The new test's `!stdout.is_empty()` guard at line 89 is
  the fail-fingerprint for the Phase-0 skeleton regression, so the
  mutation check should trip cleanly.
