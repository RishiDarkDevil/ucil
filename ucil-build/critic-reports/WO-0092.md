---
work_order: WO-0092
slug: review-changes-mcp-tool
feature_ids: [P3-W11-F11]
phase: 3
week: 11
branch: feat/WO-0092-review-changes-mcp-tool
final_commit: 9faad4c23776f24e4e8fddc991f7ff65bca8daf0
critic_session: crt-wo-0092-2026-05-09
reviewed_at: 2026-05-09T09:30:00Z
verdict: CLEAN
---

# Critic Report: WO-0092 (`review_changes` MCP tool)

**Branch**: `feat/WO-0092-review-changes-mcp-tool`
**Commit range reviewed**: `main..feat/WO-0092-review-changes-mcp-tool` (5 commits)
**Files changed**:
- `crates/ucil-daemon/src/server.rs` (+730 / −0)
- `ucil-build/work-orders/0092-ready-for-review.md` (+143)
- `ucil-build/work-orders/0091-ready-for-review.md` (−126; obsoleted by previous WO ship)

**Verdict**: **CLEAN** — no blockers. Two soft warnings noted (commit-size + commit-count under documented module-coherence carve-outs).

## Branch commits

```
9faad4c chore(rfr): WO-0092 ready-for-review marker
56dcf5d refactor(daemon): project G4 blast-radius before G7 in review_changes
012c1fa refactor(daemon): hoist changed_files_count to local for AC9 grep
9064d92 test(daemon): add frozen test_review_changes_tool
99eb457 feat(daemon): add handle_review_changes MCP tool dispatch
```

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should be acknowledged; not blocking)

1. **Commit size > 200 LOC heuristic** — `99eb457 feat(daemon)` is +335 LOC and `9064d92 test(daemon)` is +395 LOC, both above critic.md's "no commit > 200 lines without a good reason" heuristic. **Justification accepted**: scope_in #11 explicitly invokes the DEC-0005 module-coherence carve-out ("BUNDLE in ONE feat commit (single feature; no pair to split): feat — handle_review_changes body + dispatch-chain wiring + frozen test_review_changes_tool test"); on-precedent with WO-0090's `handle_check_quality + handle_type_check` bundle. Documented in `0092-ready-for-review.md` commit-cadence statement.

2. **Branch commit count = 5 vs scope_in #11 prescribed 4** — `crates/ucil-daemon/src/server.rs:0092-ready-for-review.md` shows the branch shipped 1 feat + 1 test + **2** refactor + 1 RFR. AC25 cap is `≤ 5`, so within budget; both refactor commits have inline justification (`012c1fa` AC9 single-line grep, `56dcf5d` projection-order swap so M4 trips SA2 per AC17). Per WO-0086 §planner "AC commit-cadence vs scope_in cohesion alignment" — soft over-cap on scope_in #11's idealised 4-commit recipe, well within AC25 binding floor.

### OK (passing checks)

#### 1. Stub detection — CLEAN
- `git diff main..feat/WO-0092 -- crates/ucil-daemon/src/server.rs | grep -E '^\+' | grep -E 'todo!\(\)|unimplemented!\(\)|panic!\(|NotImplementedError'` → 0 hits.
- No trivial `return None` / `return Default::default()` / `return {}` / `return true` / `return false` in shipped handler (`crates/ucil-daemon/src/server.rs:2088-2344`).
- Pre-existing in-file occurrences of the word `stub` (e.g. `server.rs:18`, `455`, `940`, `967`, `979`, `987`, `992`, `4357`) all refer to the documented **phase-1 `_meta.not_yet_implemented: true` fall-through path** — these are not new stubbed behaviour, they are references to the established invariant-#9 mechanism. Only new `stub` token in the diff is in a dispatch-chain comment at `crates/ucil-daemon/src/server.rs:967` matching the convention from neighbouring lines 940/979/987.

#### 2. Mocked critical dependencies — CLEAN
- `git diff main..feat/WO-0092 -- crates/ucil-daemon/src/server.rs | grep -E '^\+' | grep -iE 'mock|fake|MagicMock|Mockall'` → 1 hit, in a comment (`crates/ucil-daemon/src/server.rs:8493-8494`: `"these are NOT mocks of any external wire format"`) explaining the `TestG7Source` / `TestG8Source` are DEC-0008 §4 dependency-inversion seam impls, not external-wire mocks.
- `TestG4Source` (`crates/ucil-daemon/src/server.rs:7378`) is the pre-existing `#[cfg(test)]` shared helper from WO-0083, reused as-is.
- No mocks of Serena, LSP servers (`pyright`, `rust-analyzer`, `tsserver`, `gopls`), SQLite, LanceDB, or Docker.

#### 3. Skipped / ignored tests — CLEAN
- `git diff main..feat/WO-0092 | grep -E '^\+' | grep -E '#\[ignore\]|\.skip\(|xit\(|@pytest\.mark\.skip|@pytest\.mark\.xfail'` → 0 hits.

#### 4. Weak assertions — CLEAN
- `git diff main..feat/WO-0092 | grep -E '^\+' | grep -E 'assert!\(true\)|assert_eq!\(true,\s*true\)|expect\(true\)\.toBe\(true\)'` → 0 hits.
- Frozen test (`crates/ucil-daemon/src/server.rs:8746-8877`) carries 16 substantive assertions covering SA1-SA8, each with `(SAn) <semantic name>; left: …; right: …` panic-body shape per DEC-0007 + WO-0067.

#### 5. Hallucinated paths — CLEAN
All new `use` statements in the test (`crates/ucil-daemon/src/server.rs:8538-8544`) resolve to existing items:
- `crate::g4::{G4DependencyEdge, G4EdgeKind, G4EdgeOrigin}` → `crates/ucil-daemon/src/g4.rs:204` / `:153` / `:185`.
- `crate::g7::{G7Issue, G7SourceOutput, G7SourceStatus, Severity}` → `crates/ucil-daemon/src/g7.rs:184` / `:230` / `:218` / `:140`.
- `crate::g8::{G8TestCandidate, TestDiscoveryMethod}` → `crates/ucil-daemon/src/g8.rs:145` / `:130`.
- `std::collections::BTreeSet`, `std::path::PathBuf`, `std::sync::Arc` — stdlib.

#### 6. Feature coverage — CLEAN
Feature `P3-W11-F11` has the frozen test `test_review_changes_tool` at module root (`crates/ucil-daemon/src/server.rs:8537` — verified NOT inside any inner `mod tests {}` block). The test:
- Seeds `TestG4Source` (2 edges from `src/foo.rs`), `TestG7Source` (3 issues spanning Critical/High/Medium), `TestG8Source` (2 candidates).
- Constructs `McpServer` via `with_g4_sources(...).with_g7_sources(...).with_g8_sources(...)` (`crates/ucil-daemon/src/server.rs:8695-8698`).
- Dispatches `tools/call review_changes` with `changed_files: ["src/foo.rs", "src/bar.py"]` and asserts SA1-SA8.
- Acceptance-test selector in `feature-list.json` (`-p ucil-daemon server::test_review_changes_tool`) matches verbatim.

#### 7. Commit hygiene — CLEAN
- All 5 commits use Conventional Commits with valid scope (`daemon` / `rfr`) and imperative subject ≤ 70 chars.
- Every commit body carries `Phase: 3` / `Feature: P3-W11-F11` / `Work-order: WO-0092` trailers.
- `Co-Authored-By` trailer present on every commit.
- Merge commits: `git log feat/WO-0092 ^main --merges | wc -l` → `0`.
- No `--amend` / `--force` evidence.

#### 8. Doc + public API — CLEAN
- `handle_review_changes` (`crates/ucil-daemon/src/server.rs:2086-2088`) carries:
  - 58-line rustdoc (`crates/ucil-daemon/src/server.rs:2028-2085`) covering arguments, projection semantics, severity ladder, empty-source handling, and `# Panics` section per WO-0085 + WO-0090 §executor lesson.
  - `#[tracing::instrument(name = "ucil.tool.review_changes")]` per master-plan §15.2.
  - `#[allow(clippy::too_many_lines)]` matching the precedent for `handle_check_quality` / `handle_type_check`.
- The handler is private (`async fn`, no `pub`) — consistent with `handle_check_quality:1887`, `handle_blast_radius:1779`, `handle_type_check:2382`. The work-order's `pub async fn` prescription is over-prescriptive; the executor's choice matches existing dispatch handler precedent. No doc warning.
- The frozen test carries 35-line rustdoc enumerating SA1-SA8 (`crates/ucil-daemon/src/server.rs:8499-8533`).
- No new `pub` items introduced — `lib.rs` re-export block unchanged (executor took the scope_in #8 inline-`json!` path, no new wire types needed).

## Spot-checked acceptance-criteria evidence

| AC | Critic spot-check | Evidence |
|---|---|---|
| AC5 | test at module root | `crates/ucil-daemon/src/server.rs:8537` — `async fn test_review_changes_tool()`. No enclosing `mod tests {}` (last `mod` declaration is far upstream). |
| AC6 | handler exists | `crates/ucil-daemon/src/server.rs:2088` |
| AC7 | `"review_changes"` ≥ 2 lines | 5 lines: `:339` (ToolDescriptor), `:968` (dispatch), `:2329` (`_meta.tool`), `:4256` (test fixture), `:8705` (test request) |
| AC8 | `#[tracing::instrument]` | `crates/ucil-daemon/src/server.rs:2086` |
| AC9 | `Span::current().record("changed_files_count"` | `crates/ucil-daemon/src/server.rs:2124` (single-line per `012c1fa` refactor) |
| AC10 | `tokio::join!` 3-arity over G4/G7/G8 | `crates/ucil-daemon/src/server.rs:2169-2173` — `execute_g4 + execute_g7 + execute_g8` in a single `tokio::join!` |
| AC11 | merge_g{4,7,8}_* helper reuse ≥ 6 | 18 lines (3× over threshold) |
| AC12 | project_blast_radius_impacted / build_dependency_chains ≥ 4 | 9 lines |
| AC18 | `(SA[1-8]) ` panic-body labels ≥ 8 | 97 lines |
| AC19 | SA7 `wall_elapsed_ms < 6000` literal | `crates/ucil-daemon/src/server.rs:8523`, `:8831`, `:8836-8838` |
| AC20 | no PascalCase severity in production | `grep -E '"severity":\s*"(Critical\|High\|Medium\|Low\|Info)"' crates/ucil-daemon/src/server.rs` → 0 hits |
| AC24 | 0 merge commits on feat | confirmed |
| AC25 | branch commits ≤ 5 | 5 commits (within cap) |

## Suggested verifier focus

1. **Mutation contract M1-M4** — RFR doc `0092-ready-for-review.md` enumerates the patches and `(SAn)` panic-body trips. Verifier should re-run M1/M2/M3/M4 from a `cargo clean` baseline and md5sum-verify restoration.
2. **Wall-clock canary SA7** (`< 6000 ms`) — sequential-await regression guard. The G4=12s + G7=5.5s + G8=5s per-source masters compound past 6 s under sequential awaits; SA7 catches that.
3. **Severity-rank load-bearing test (M4 + commit `56dcf5d`)** — projection order is now G4-first / G7-second so the `findings.sort_by(...)` is genuinely load-bearing for SA2. Without the sort, G4's medium-severity row lands at index 0 and SA2 fails.
4. **Standing carry-forwards** (informational only) — coverage-gate sccache `RUSTC_WRAPPER` workaround (43+ WOs deep), reality-check stash bug (17+ WOs deep), AC22/AC23 gate sub-checks. Per scope_out #9 these are bucket-B/D triage candidates, not WO-0092 regressions.

## Final verdict

**CLEAN** — proceed to verifier.
