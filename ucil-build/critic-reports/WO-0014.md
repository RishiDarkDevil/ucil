# Critic Report: WO-0014

**Critic session**: crt-WO-0014-20260418
**Branch**: `feat/WO-0014-lsp-diagnostics-bridge-skeleton`
**Ref reviewed**: `fd963e8b18c57db8a6a2d6efc3b46f46b8654286`
**Merge base**: `main` (`9c7160b618f39a198a86502bc061b9669f6bb3a2` at time of review)
**Commits reviewed**:

```
9c98ab3 build(lsp-diagnostics): add serde workspace dep for Language enum derives
869c82e feat(lsp-diagnostics): add Language, LspEndpoint, LspTransport, Diagnostic types
109bea4 feat(lsp-diagnostics): add LspDiagnosticsBridge skeleton with serena_managed state
c00e76b test(lsp-diagnostics): add module-root bridge:: acceptance tests for F03
fd963e8 chore(wo): ready-for-review marker for WO-0014
```

**Diff**: 6 files changed, 616 insertions(+), 1 deletion(-)
**Verdict**: **CLEAN**

---

## Findings

### Blockers (must fix before verifier)

_None._ Every mechanical blocker from `.claude/agents/critic.md` §1–8 passes:

1. **Stub detection (§1)** — `grep -nE 'todo!\(|unimplemented!\(|NotImplementedError|raise NotImplementedError'` on `crates/ucil-lsp-diagnostics/src/`: **0 hits**. No bare `return None` / `Default::default()` / trivial-literal stubs dressed up as feature implementations; the two accessor methods that legitimately return `None` in the empty-map branches (`endpoint_for`, `endpoints().get`) do so because the endpoint map is spec'd empty at F03 per DEC-0008, and the module-root tests assert this invariant rather than hiding behind it.
2. **Mocked critical collaborators (§2)** — `grep -Ein 'mock|fake|stub|MagicMock|Mockall'` on `crates/ucil-lsp-diagnostics/src/`: **0 hits**. The four tests construct a real `LspDiagnosticsBridge::new(bool)` and exercise accessor methods directly — no Serena mock, no LSP-protocol mock, no SQLite/LanceDB/Docker touch at all. This is exactly what phase-log invariant #1 and WO `acceptance[9]` call for.
3. **Skipped / ignored tests (§3)** — `grep -nE '#\[ignore\]|\.skip\(|xfail|it\.skip|@pytest\.mark\.skip|@pytest\.mark\.xfail'` on changed files: **0 hits**.
4. **Weak assertions (§4)** — `grep -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)|assert_eq!\(1,\s*1\)'` on changed files: **0 hits**. Every `assert!` / `assert_eq!` carries a meaningful predicate; `test_bridge_error_duplicate_endpoint_render` asserts the `Display` output contains the substring `"duplicate endpoint"` AND the colliding language name with failure-mode diagnostic messages.
5. **Hallucinated paths (§5)** — Every `use` in the new code resolves:
   - `std::collections::HashMap`, `std::path::PathBuf`, `serde::{Serialize, Deserialize}` — standard/workspace.
   - `thiserror::Error` — already in `crates/ucil-lsp-diagnostics/Cargo.toml` line 11.
   - `crate::types::{Diagnostic, Language, LspEndpoint}` in `bridge.rs:29` — resolves to the new `types.rs` (lines 47, 110, 127).
   - `super::{BridgeError, LspDiagnosticsBridge}` / `crate::types::{Language, LspEndpoint, LspTransport}` in the `tests` module — all present.
   - Re-exports in `lib.rs:22-23`: `bridge::{BridgeError, LspDiagnosticsBridge}` + `types::{Diagnostic, DiagnosticSeverity, Language, LspEndpoint, LspTransport}` — all seven referenced items exist.
6. **Feature coverage (§6)** — WO declares `features: ["P1-W5-F03"]`. Four new tests reference its behaviour, two at module root aligned with the frozen selector `bridge::`:
   - Module-root: `bridge::test_bridge_with_serena_managed_has_no_own_endpoints` (`bridge.rs:208`) — asserts the DEC-0008 invariant "no UCIL-owned LSP endpoints while Serena is active".
   - Module-root: `bridge::test_bridge_without_serena_has_no_endpoints_until_f07` (`bridge.rs:223`) — asserts the degraded-mode branch is also empty at F03 (F07's territory).
   - Supporting (`mod tests`, non-selector-frozen): `test_insert_endpoint_round_trip` (`bridge.rs:246`) exercises `insert_endpoint` → `endpoint_for` → re-insert-returns-prior.
   - Supporting: `test_bridge_error_duplicate_endpoint_render` (`bridge.rs:286`) exercises the `BridgeError::DuplicateEndpoint` Debug + Display surface so the placeholder variant is not dead code.
7. **Commit hygiene (§7)** — Every commit uses Conventional Commits with a valid scope (`lsp-diagnostics`, `wo`). All `feat`/`test`/`build`/`chore` commits carry the required `Phase: 1 / Feature: P1-W5-F03 / Work-order: WO-0014` trailers plus the `Co-Authored-By: Claude Opus 4.6 (1M context)` footer per `.claude/rules/commit-style.md`. Commit sizes: `9c98ab3` = 6 lines, `869c82e` = 188 lines, `109bea4` = 188 lines, `c00e76b` = 120 lines, `fd963e8` = 114 lines (ready-for-review doc). All under the 200-line DEC-0005 ceiling. No `--amend`, no force-push signatures in `git log`.
8. **Doc + public API (§8)** — Every new `pub` item carries rustdoc:
   - `BridgeError` (`bridge.rs:35-47`) + variant `DuplicateEndpoint { language }` (`bridge.rs:56-59`).
   - `LspDiagnosticsBridge` struct (`bridge.rs:67-85`), every field doc'd individually (`serena_managed`, `endpoints`, `diagnostics_cache`).
   - All six `pub fn` / `pub const fn`: `new` (WO-mandated DEC-0008 cite in the doc), `is_serena_managed`, `endpoint_for`, `endpoints`, `insert_endpoint`, `diagnostics_cache` — each doc'd; `new` / `is_serena_managed` / `endpoint_for` / `endpoints` / `diagnostics_cache` carry `#[must_use]`.
   - `Language` + all 7 variants (`types.rs:45-61`), `LspTransport` + both variants with fields (`types.rs:77-99`), `LspEndpoint` + both fields (`types.rs:111-122`), `Diagnostic` + all 6 fields (`types.rs:138-152`), `DiagnosticSeverity` + all 4 variants (`types.rs:158-167`).
   - No `Result`-returning pub fn exists at F03, so the `# Errors` section requirement from `.claude/rules/rust-style.md` is vacuously satisfied; the `#[non_exhaustive] BridgeError` enum still anchors the crate-wide error surface for F04/F07.

### Warnings (should confirm with verifier, not blocking)

1. **Pre-existing unused deps in `Cargo.toml`** — `tokio`, `tracing`, and `ucil-core` are declared in `crates/ucil-lsp-diagnostics/Cargo.toml` (lines 10, 12, 19) but this WO introduces no code that uses them. WO `scope_in` explicitly says _"Keep the existing `thiserror`, `tokio`, `tracing`, `ucil-core` lines byte-for-byte"_ so the executor correctly left them alone — this is a pre-WO-0014 condition, and clippy with `-D warnings` passes locally, confirming there's no `unused-dependencies` lint active for this crate. Verifier may wish to note this for the F04 / F07 planner who will actually consume these deps.

2. **Reality-check harness interaction** — The ready-for-review marker (`ucil-build/work-orders/0014-ready-for-review.md`, lines ~58-75) documents that `scripts/reality-check.sh P1-W5-F03` tripped the pre-existing stash-stack bug from escalation `20260415-1630-reality-check-per-file-rollback.md` during the "pop" phase (popped an unrelated WO-0012 stash entry, producing a `Cargo.toml` merge conflict). The stash-code phase correctly reported tests failing while the bridge code was stashed, which is the load-bearing half of the mutation oracle. This is a harness condition the verifier already handles per the WO's `acceptance_criteria[13]` — flagging here only so the verifier knows it should perform the documented two-file manual stash check rather than treating the reality-check output as a blocker.

3. **`DashMap` choice deferred** — Master-plan §13.3 lists `DashMap<PathBuf, Vec<Diagnostic>>` for the diagnostics cache; the WO justifies `std::collections::HashMap` (no concurrent access at F03; promotion reserved for F04). The rationale is captured in the `LspDiagnosticsBridge` struct doc (`bridge.rs:93-100`) and the `new` doc references DEC-0008 per the WO requirement. Verifier should confirm the DEC-0008 cite is present in the rustdoc (it is, `bridge.rs:110-113`) — no action needed.

### OK

- **No skipped tests** anywhere in the diff.
- **No mocked critical collaborators** — Serena/LSP/SQLite/LanceDB/Docker untouched; tests are pure in-crate units.
- **Module-root test discipline honoured** — the two frozen-pattern-aligned tests sit at module root (`bridge.rs:208, 223`), outside the `mod tests { … }` block that begins at `bridge.rs:235`. Grep confirms (`mod tests` vs. `fn test_bridge_…` line numbers).
- **All 7 public types present per WO `acceptance_criteria[10]`**: `bridge.rs` = 2 (`BridgeError`, `LspDiagnosticsBridge`), `types.rs` = 5 (`Language`, `LspTransport`, `LspEndpoint`, `Diagnostic`, `DiagnosticSeverity`). Grep returns exactly 7.
- **No `ucil-daemon` dep in `Cargo.toml`** — DEC-0008 cycle-free invariant holds.
- **`serde = { workspace = true }` dep added** at `Cargo.toml:18` per the WO's first commit.
- **Local tooling green**: `cargo build --workspace` exit 0; `cargo nextest run -p ucil-lsp-diagnostics` = 4 passed / 0 skipped; `cargo clippy -p ucil-lsp-diagnostics --all-targets -- -D warnings` exit 0 (pedantic + nursery); `cargo doc -p ucil-lsp-diagnostics --no-deps` exit 0. Verifier must re-run from a fresh `cargo clean` per the harness contract.
- **Commit cadence matches the plan**: 4 implementation commits (build → types → skeleton → tests) + 1 ready-for-review marker. No kitchen-sink commits, no `wip:` saves.
- **No edits to forbidden paths**: the 6-file diff touches only `Cargo.lock`, `crates/ucil-lsp-diagnostics/{Cargo.toml,src/lib.rs,src/bridge.rs,src/types.rs}`, and `ucil-build/work-orders/0014-ready-for-review.md`. No `feature-list.json`, no `crates/ucil-daemon/**`, no `tests/fixtures/**`, no prior work-order edits.
- **`#[non_exhaustive] BridgeError`** per `.claude/rules/rust-style.md`; `new(serena_managed: bool) -> Self` constructor shape frozen per DEC-0008.

---

## Summary

WO-0014 is a tight, single-crate skeleton contribution with clear module boundaries, proper commit cadence, and full test coverage for its (narrow) F03 acceptance surface. DEC-0008 is cited in the rustdoc where required, and the module-root/mod-tests split matches the project convention set by WO-0006 / WO-0010 / WO-0013. No blockers; the verifier should proceed directly to the fresh-session gate check.

**Verdict: CLEAN**
