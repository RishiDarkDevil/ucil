# Critic Report: WO-0030

**Critic session**: crt-wo-0030-20260418
**Work-order**: `ucil-build/work-orders/0030-kg-crud-hot-staging-retry.json`
**Branch**: `feat/WO-0030-kg-crud-hot-staging-retry`
**Tip commit**: `4fbe743708e4cfffcbd3af5632db0daec6a18125`
**Merge base**: `main` (tip `bafebd5`, parent of the WO planner-emit commit)
**Commits reviewed**: `main..HEAD` — 10 commits (`8dc79ac..4fbe743`)
**Files changed**: `Cargo.toml`, `Cargo.lock`, `crates/ucil-core/Cargo.toml`, `crates/ucil-core/src/knowledge_graph.rs`, `crates/ucil-core/src/lib.rs`, `ucil-build/work-orders/0030-ready-for-review.md` — 6 paths, +1514 lines
**Features claimed**: `P1-W4-F02`, `P1-W4-F08`
**Verdict**: **CLEAN**

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix or justify before gate)

1. **Work-order trailers point at WO-0024 on 9 of 10 commits** —
   `git log main..HEAD --format='%h %s %n%b'` shows every implementation
   commit (`8dc79ac`, `81f012c`, `46bb793`, `6b91c90`, `3a83f04`,
   `46ecb2a`, `d24b944`, `1192423`, `6a394e2`) carries
   `Work-order: WO-0024` in its body; only the marker commit
   `4fbe743` uses `Work-order: WO-0030`. The WO-0030
   `plan_summary` and `scope_in[0]` explicitly instruct the executor
   to **cherry-pick** commits `d13cfc9..60eab33` from the stale
   `feat/WO-0024-kg-crud-and-hot-staging` branch, so the historical
   trailer is an expected artefact of the documented preferred path,
   not an executor error. Feature trailers (`P1-W4-F02`, `P1-W4-F08`)
   are correct and every Feature id resolves in `feature-list.json`.
   Recording for verifier visibility only — not a blocker.

2. **Commit `46bb793` is 404 lines (soft target ≤200)** —
   "upsert_entity + get_entity_by_qualified_name + list_entities_by_file
   + 3 module-root tests" introduces the entity CRUD surface plus the
   `entity_from_row` decoder and the `absent_to_none` helper shared by
   all three reads (and by the bi-temporal read in a later commit).
   The commit body explains the grouping; splitting the reads from the
   shared helpers would force forward references. The commit is
   coherent (one logical unit), the commit-style rule is a soft target
   ("~50 lines of diff per commit is a soft target"), and the single
   breach is justified. Noting for gate visibility.

### OK

- **Stubs**: `rg -n 'todo!\(\)|unimplemented!\(\)|NotImplementedError'`
  across `crates/ucil-core/src/knowledge_graph.rs` — zero hits.
  No `pass`-only bodies and no trivial-default-return shortcut patterns
  reachable from the acceptance tests.
- **Mocked critical collaborators**: No `mock` / `fake` / `MagicMock` /
  `Mockall` references in the diff's test bodies. The three matches of
  `mock` in `knowledge_graph.rs` are all in documentation comments
  ("No mocks of rusqlite …") on `test_schema_creation`,
  `test_hot_staging_writes`, and `test_wal_checkpoint_truncates`.
  Each of the 7 new tests opens a real `tempfile::TempDir`-backed
  SQLite db through `KnowledgeGraph::open(...)` — real
  `rusqlite::Connection`, real `BEGIN IMMEDIATE` transactions, real
  `PRAGMA wal_checkpoint`. No Serena / LSP / Docker paths touched.
- **Skipped tests**: `rg -n '#\[ignore\]|\.skip\(|xfail|xit|pytest\.mark\.(skip|xfail)'`
  on the changed files — zero hits.
- **Weak assertions**: No `assert!(true)`, `assert_eq!(true, true)`,
  `expect(true).toBe`, or `assert True`. Every new test has multiple
  concrete assertions on real fetched values (full-field round-trip,
  `start_line ASC` ordering, `access_count` monotonicity,
  `t_last_verified` set post-conflict, bi-temporal window selection,
  per-table `COUNT(*)` checks, WAL file size post-TRUNCATE).
- **Hallucinated imports / paths**: All five top-level `use`s in the
  changed `knowledge_graph.rs` resolve — `std` (stdlib),
  `chrono::{DateTime, Utc}` (added to workspace deps in `8dc79ac` and
  wired into `crates/ucil-core/Cargo.toml`), `rusqlite` (workspace
  dep), `serde::{Deserialize, Serialize}` (workspace dep),
  `thiserror` (workspace dep). Test-local `use tempfile::TempDir` and
  `use chrono::TimeZone` resolve against existing dev-deps. The three
  new `pub use knowledge_graph::{...}` lines in
  `crates/ucil-core/src/lib.rs` reference `Entity`, `Relation`,
  `HotObservation`, `WalCheckpointMode`, `KnowledgeGraph`,
  `KnowledgeGraphError` — every name exists at module root on this
  branch.
- **Feature coverage**:
    * `P1-W4-F02` acceptance selector `-p ucil-core knowledge_graph::`
      resolves to all 7 new module-root tests plus the pre-existing
      `test_schema_creation` — the whole `knowledge_graph` module.
      Each of the six scope_in-required `test_*` names from the WO
      (`test_upsert_and_get_entity`, `test_bi_temporal_as_of`,
      `test_list_entities_by_file`, `test_upsert_relation_and_list`,
      `test_entity_unique_constraint_updates`,
      `test_wal_checkpoint_truncates`) is present at module root with
      `#[cfg(test)] #[test]` attributes.
    * `P1-W4-F08` frozen selector
      `-p ucil-core knowledge_graph::test_hot_staging_writes` resolves
      to the test at `crates/ucil-core/src/knowledge_graph.rs:1764`.
      The body calls `stage_hot_observation`,
      `stage_hot_convention_signal`, and
      `stage_hot_architecture_delta` exactly once each, then
      `SELECT COUNT(*)` on each target table — satisfying the WO's
      "each COUNT(*) is 1 and returned ids are positive" contract.
- **Commit hygiene**: All 10 commits follow Conventional Commits
  (`feat(core):` / `build(workspace):` / `refactor(core):` /
  `chore(harness):` prefixes, subject ≤72 chars). Every commit body
  carries `Phase: 1`, a resolving `Feature:` id, and a `Work-order:`
  id. Branch tip matches `origin/feat/WO-0030-kg-crud-hot-staging-retry`
  (no divergence). No evidence of `--amend` or force-push in the
  reflog-visible history on this branch.
- **Doc + public API**: Every new `pub` item has a rustdoc
  immediately preceding it. Cited master-plan sections per
  `.claude/rules/rust-style.md § Docs`:
    * `Entity`, `Relation`, `HotObservation`, `WalCheckpointMode`
      cite §12.1 lines 1130-1145 / 1147-1156 / 190-198 for their row
      schemas; every pub field has its own rustdoc line.
    * `WalCheckpointMode::as_sql` has a one-line rustdoc and
      `#[must_use]`.
    * Every new `KnowledgeGraph` method (`upsert_entity`,
      `get_entity_by_qualified_name`, `list_entities_by_file`,
      `upsert_relation`, `list_relations_by_source`,
      `get_entity_as_of`, `stage_hot_observation`,
      `stage_hot_convention_signal`, `stage_hot_architecture_delta`,
      `checkpoint_wal`) has a rustdoc block that describes behaviour,
      cites master-plan §11 line 1117 (`BEGIN IMMEDIATE`) where
      relevant, names the `#[tracing::instrument]` span per §15.2, and
      documents the `# Errors` it can return.
    * `KnowledgeGraphError::InvalidTimestamp` variant carries its own
      rustdoc explaining the reserved-for-future-use contract.
    * `#![deny(rustdoc::broken_intra_doc_links)]` remains active in
      `crates/ucil-core/src/lib.rs:8` and intra-doc links use
      unambiguous `[`path`]` forms
      (`[`KnowledgeGraph::upsert_entity`]`,
      `[`chrono::DateTime::to_rfc3339`]`, etc.).
- **Scope respected**: No edits under `ucil-build/feature-list.json`,
  `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`,
  `ucil-master-plan-v2.1-final.md`, or
  `crates/ucil-core/src/incremental.rs` (the WO's `forbidden_paths`
  list). No edits under `crates/ucil-daemon/` or
  `crates/ucil-lsp-diagnostics/` (WO `scope_out`).
- **Acceptance marker**: `ucil-build/work-orders/0030-ready-for-review.md`
  is present on the branch.
- **Test isolation**: Every new test allocates its own `TempDir` and
  opens a fresh `KnowledgeGraph::open(tmp.path().join("kg.db"))` — no
  shared on-disk state, no `#[serial]` needed, no environment-variable
  stomping.

## Summary

The implementation is a clean re-land of the WO-0024 cherry-picks onto
a main that already carries the `incremental.rs` rustdoc fix
(`f6ec86e`) which previously rejected WO-0024. Every acceptance-test
surface the WO pins (P1-W4-F02's `knowledge_graph::` module and
P1-W4-F08's `knowledge_graph::test_hot_staging_writes` frozen
selector) is present, real (no mocks of the SQLite collaborator),
non-trivially asserted, and documented. Two warnings are recorded for
verifier visibility — both are documented consequences of the
planner-blessed cherry-pick path rather than executor shortcuts.

Verdict: **CLEAN** — proceed to verifier.
