---
work_order: WO-0072
slug: codegraphcontext-plugin-manifest
phase: 3
week: 9
features: ["P3-W9-F08"]
branch: feat/WO-0072-codegraphcontext-plugin-manifest
ref: a7da4341aee35155f2f52598eb3c1d3a1360b6be
merge_base: 05d5d4736ea16e9f6045da339218d87924a72b0f
verdict: CLEAN
---

# Critic Report: WO-0072

**Critic session**: crt-2026-05-08T0700Z
**Branch**: `feat/WO-0072-codegraphcontext-plugin-manifest`
**Ref**: `a7da4341aee35155f2f52598eb3c1d3a1360b6be`
**Merge base**: `05d5d4736ea16e9f6045da339218d87924a72b0f`
**Verdict**: **CLEAN**

## Diff under review

5 commits, 5 files, 727 lines (4 production files + 1 RFR):

```
a7da434 docs(rfr): WO-0072 ready-for-review — codegraphcontext F08 alone (DEC-0019)
a45ccbe fix(verify/P3-W9-F08): copy fixture to tmpdir + use correct query_type
09aa39c test(verify): replace P3-W9-F08 stub with codegraphcontext acceptance
79cbd94 test(daemon): add g4 plugin manifest health-check suite (codegraphcontext)
50dc542 feat(plugins/architecture): land codegraphcontext MCP manifest at v0.4.7
```

| File | LOC | Kind |
|------|----:|------|
| `crates/ucil-daemon/tests/g4_plugin_manifests.rs` | 156 | new integration test |
| `plugins/architecture/codegraphcontext/plugin.toml` | 122 | new data manifest |
| `scripts/devtools/install-codegraphcontext-mcp.sh` | 63 | new helper script |
| `scripts/verify/P3-W9-F08.sh` | 203 (was ~30 stub) | rewritten verify script |
| `ucil-build/work-orders/0072-ready-for-review.md` | 183 | RFR (not shipped) |

## Findings

### Blockers (must fix before verifier)

**None.** Diff passes every adversarial check.

### Warnings (should fix before gate; non-blocking)

1. **Cosmetic `wo-0071` carryover in the rewritten verify script** — `scripts/verify/P3-W9-F08.sh:34`, `:55` (`CARGO_LOG="/tmp/wo-0071-f08-cargo.log"`), `:77` (`CGC_TMPDIR="$(mktemp -d -t wo-0071-f08-cgc-XXXXXX)"`), `:78` (`SMOKE_LOG="/tmp/wo-0071-f08-smoke.log"`), `:85` (comment), `:122` (`clientInfo":{"name":"ucil-wo-0071"...`). These are non-functional naming carryovers from the cherry-picked source. Same spirit as the explicit trailer-carryover allowance in scope_in #3 but not explicitly covered by a planner carve-out. Verifier-side correctness is unaffected — the script still operates against the correct fixture and asserts the correct selectors. Suggest a follow-up cosmetic-fix WO to retire the `wo-0071` strings.

2. **Historical WO-0071 references inside test rustdoc** — `crates/ucil-daemon/tests/g4_plugin_manifests.rs:31` (`matching the WO-0071 acceptance selector`), `:84` (`carried by WO-0071 scope_in #28`). These are intentional provenance documentation in `//!`-prefixed module rustdoc, not bugs. Acceptable per scope_in #11 carve-out (rustdoc-side commentary on `#[cfg(test)]` files).

3. **M3 patch divergence from planner spec — explicitly disclosed** — RFR §"Disclosed Deviations §1" replaces the planner-prescribed M3 (`remove --with falkordblite from transport.args`) with `mcp → mcpz` typo because operator state `~/.codegraphcontext/.env` (created on first install-script run) configures `database=falkordb` in such a way that removing `--with falkordblite` no longer flips the test PASS→FAIL on systems that have ever run the install script. Substituted M3 still flips the test (`StdioTransport(Os { kind: BrokenPipe })`) and produces a substantively distinct failure mode from M1 (pre-spawn ENOENT) and M2 (post-handshake test-side panic). Empirically grounded planner-spec correction per WO-0068 lessons §planner ("Empirical reality wins"). Verifier should apply the substituted M3 from RFR §M3, not the planner's M3 from WO scope_in #7 / AC9. Not a critic-side blocker; flagging for verifier awareness.

4. **Soft-target LOC overshoot on two commits** — `79cbd94` adds 219 LOC across two files (g4 test 156 + install script 63) and `09aa39c` adds 182 LOC to one file (verify script). Both exceed the ~50 LOC soft target from `.claude/rules/commit-style.md`. DEC-0005 module-coherence carve-out applies (single cohesive change per commit, single file or two-file pair landing as a unit) and is acknowledged in scope_in #12. Acceptable; flagging for completeness only.

### OK (passing checks)

#### 1. Stub detection — clean
- No `todo!()`, `unimplemented!()`, `raise NotImplementedError`, bare `pass`, or trivial-default returns in any changed file.
- `g4_plugin_manifests.rs` has full assertion bodies and a complete handshake flow.
- `plugin.toml` is a concrete data file with all required sections.
- Both shell scripts have full implementations with no placeholder comments.

#### 2. Mocked critical dependencies — clean
- `rg -i 'mock|fake|stub'` over the four production files returns the single match `g4_plugin_manifests.rs:10` `//! Mocking tokio::process::Command, the spawned MCP server, ... is forbidden.` This is a NEGATIVE assertion forbidding mocking, inside a `//!` rustdoc. Explicitly carved out per scope_in #11 + WO-0048 line 363 + WO-0069 §executor.
- The test drives a real `uvx --with falkordblite codegraphcontext@0.4.7 mcp start` subprocess via `PluginManager::health_check_with_timeout`. No `mockall`, no fake transport, no stubbed JSON-RPC.
- Verify-script smoke spawns the real codegraphcontext binary and validates JSON-RPC responses with python3.

#### 3. Skipped / ignored tests — clean
- No `#[ignore]`, `.skip()`, `xfail`, `it.skip`, `xit`, or `@pytest.mark.skip` anywhere in the diff.
- `skip_via_env()` in `g4_plugin_manifests.rs:80-83` is the documented `UCIL_SKIP_EXTERNAL_PLUGIN_TESTS` / `UCIL_SKIP_ARCHITECTURE_PLUGIN_E2E` opt-out pattern per WO-0069. Per WO-0069 §verifier #3 (carried by scope_in #20), the verifier MUST NOT set either env. This is the canonical opt-out pattern for offline-CI runners, NOT a hidden test-disable.

#### 4. Weak assertions — clean
- All seven assertions in `g4_plugin_manifests.rs` (lines 100, 101, 102, 106, 120, 121, 127, 147) are substantive:
  - `assert_eq!(manifest.plugin.name, "codegraphcontext")`
  - `assert_eq!(manifest.transport.kind, "stdio")`
  - non-empty capabilities check
  - `architecture.*` namespace prefix check on every capability
  - `assert_eq!(health.name, "codegraphcontext")`
  - `assert_eq!(health.status, HealthStatus::Ok, ...)`
  - `!health.tools.is_empty()` with descriptive panic
  - `health.tools.iter().any(|t| t == "analyze_code_relationships")` with full live-tool-list panic
- No `assert!(true)`, no `assert_eq!(true, true)`, no `expect(true).toBe(true)`, no `assert True`.

#### 5. Hallucinated paths — clean
- `use ucil_daemon::{HealthStatus, PluginManager, PluginManifest};` (line 50) — verified at `crates/ucil-daemon/src/lib.rs:195-200` (re-exports from `plugin_manager` module).
- `PluginManager::health_check_with_timeout` — verified at `crates/ucil-daemon/src/plugin_manager.rs:1057`.
- `tests/fixtures/rust-project/src/util.rs::fn evaluate` referenced by the verify script — verified at line 128 of that file.
- `plugins/architecture/codegraphcontext/plugin.toml` referenced by the test — exists at the path it loads.

#### 6. Feature coverage — clean
- WO-0072 ships a single feature: `P3-W9-F08` (CodeGraphContext plugin manifest).
- Test `g4_plugin_manifests::codegraphcontext_manifest_health_check` directly exercises the feature: parses the manifest, drives the MCP handshake via the real subprocess, asserts the live `tools/list` reply contains the canonical blast-radius tool name `analyze_code_relationships`.
- Verify script `scripts/verify/P3-W9-F08.sh` invokes the cargo-test selector AND runs an end-to-end dependency-graph + analyze smoke against a tmpdir copy of `tests/fixtures/rust-project`.
- M1/M2/M3 mutation contract documented in RFR with verbatim observed-panic strings.

#### 7. Commit hygiene — clean
- All five commits use Conventional Commits with proper `<type>(<scope>): <summary>` form.
- All five commits carry the required `Phase: 3` and `Feature: P3-W9-F08` trailers.
- `Work-order` trailer:
  - 50dc542, 79cbd94, 09aa39c → `WO-0071` (carryover from cherry-picked source — explicitly permitted per scope_in #3 carve-out, documented in RFR Provenance section)
  - a45ccbe → `WO-0072` (rewritten — partial cherry-pick required fresh commit, per RFR Provenance row 4)
  - a7da434 → `WO-0072` (new RFR)
- `git log feat ^main --merges` is empty — zero merge commits on the feat branch (AC25 satisfied per WO-0070 reword).
- All five subject lines are under or at 75 chars (soft target 70 — acceptable margin).
- All `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>` trailers present.

#### 8. Doc + public API — clean
- No new `pub` items. Per scope_in #13, this WO ships only data (TOML manifest), integration tests, helper scripts, and a verify script — no new orchestration code, no new tracing-instrument annotations.
- Test file has comprehensive `//!`-prefixed module rustdoc explaining the test contract, the `UCIL_SKIP_*` opt-out semantics, and the WO-0044/WO-0069/DEC-0007 lineage.
- TOML manifest has 73 lines of leading `#`-prefixed documentation explaining the pinned version, launch invariants, advertised tools, transport, API-key requirements, and the `architecture.*` namespace establishment.
- Install script and verify script both have multi-line top-of-file documentation explaining behaviour and exit codes.

#### 9. Forbidden_paths discipline — clean
Verified empty diff against every entry in WO `forbidden_paths`:
- `git diff main HEAD -- crates/ucil-daemon/src/` → empty (NO daemon source changes — DEC-0019 spurious-frame-drain deferred to Phase 7)
- `git diff main HEAD -- plugins/knowledge/graphiti/` → empty
- `git diff main HEAD -- scripts/devtools/install-graphiti-mcp.sh scripts/verify/P3-W9-F10.sh` → empty
- `git diff main HEAD -- ucil-build/decisions/` → empty (no new ADR; the prior attempt-2 forbidden-paths violation is NOT repeated)
- `git diff main HEAD -- crates/ucil-daemon/tests/plugin_manifests.rs crates/ucil-daemon/tests/g3_plugin_manifests.rs` → empty (existing regression suites untouched)
- `git diff main HEAD -- ucil-build/feature-list.json tests/fixtures/ scripts/gate/ scripts/flip-feature.sh ucil-master-plan-v2.1-final.md` → empty

#### 10. Pinned-immutable-tag policy — clean
- `! grep -qE '"(main|latest|master|head|develop|dev|nightly)"'` over `plugin.toml` returns no matches.
- The version pin `codegraphcontext@0.4.7` is intact at `plugin.toml:111` (transport.args).

#### 11. DEC-0007 frozen-test placement — clean
- `mod g4_plugin_manifests { ... }` declared at module root (`g4_plugin_manifests.rs:46`).
- `#[tokio::test] async fn codegraphcontext_manifest_health_check()` at line 91 inside the module.
- Selector `g4_plugin_manifests::codegraphcontext_manifest_health_check` resolves correctly.

## Verdict

**CLEAN.** Verifier may proceed.

This WO is a textbook re-packaging via cherry-pick: clean F08 work isolated from the F10/graphiti scope per DEC-0019 §4, no daemon source touched, no forbidden ADR re-introduced, no scope creep. The single substantive divergence (M3 patch substitution) is empirically grounded and disclosed in the RFR with full reproduction steps.

The verifier should:
1. Apply M1/M2/M3 from the RFR (NOT the planner's M3 from WO scope_in #7 / AC9 — see Warning #3).
2. Pre-install the coderankembed model via `bash scripts/devtools/install-coderankembed.sh` before running `cargo test --workspace` (per WO-0068 §verifier #4 standing carry).
3. Run `cargo test -p ucil-daemon --test g4_plugin_manifests g4_plugin_manifests::codegraphcontext_manifest_health_check` from a clean session.
4. Run `bash scripts/verify/P3-W9-F08.sh` end-to-end with `UCIL_SKIP_ARCHITECTURE_PLUGIN_E2E` UNSET and `UCIL_SKIP_EXTERNAL_PLUGIN_TESTS` UNSET (per scope_in #20).
5. Measure ucil-daemon coverage via `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --summary-only --json | jq '.data[0].totals.lines.percent'` (per scope_in #17 standing protocol — the gate-script's `[FAIL] coverage gate` line is the known sccache `RUSTC_WRAPPER` interaction).
