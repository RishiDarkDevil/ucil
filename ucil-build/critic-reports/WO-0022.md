---
work_order: WO-0022
slug: storage-layout-and-crash-recovery
branch: feat/WO-0022-storage-layout-and-crash-recovery
ref: 636a8c95ce497a813e2100b3fbd948b6e359a15c
merge_base: main
critic_session: crt-WO-0022-20260418
features: [P1-W2-F06, P1-W3-F09]
verdict: CLEAN
---

# Critic Report: WO-0022 — storage layout + crash-recovery checkpoint

## Summary

Reviewed 5 commits on `feat/WO-0022-storage-layout-and-crash-recovery`
(`bca37f0 → 636a8c9`) touching only `crates/ucil-daemon/src/{lib.rs,
lifecycle.rs,storage.rs}` and `ucil-build/work-orders/0022-ready-for-
review.md`. Eight forbidden-path groups confirmed at 0 lines diff vs
`origin/main`. Every check from `.claude/agents/critic.md` passes with
two minor commit-size advisories that are covered by DEC-0005 + the
reality-check mutation-oracle design already documented in the
ready-for-review marker.

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

1. **Commit `bca37f0` is 432 lines** (module-introduction of
   `crates/ucil-daemon/src/storage.rs` + `lib.rs` module declaration).
   Exceeds critic.md's 200-line ceiling; covered by the explicit
   DEC-0005/DEC-0001 waiver cited in the commit body (module
   introduction with impl + tests bound together to avoid non-compiling
   intermediates). DEC-0005 line 7 names `P1-W2-F06` explicitly, so
   the waiver applies verbatim.
2. **Commit `4c555c9` is 303 lines** (Checkpoint impl + 4 module-root
   tests + lib.rs re-export). Commit body explains the additive
   extension but does not itself cite a size waiver. Justification is
   documented in `0022-ready-for-review.md` lines 23-31: the
   `scripts/reality-check.sh` mutation oracle rolls each feature's
   files back to the parent of the commit that introduced them, so
   splitting impl from tests across two commits would leave one
   stashed state with half the F09 surface missing. Matches the
   WO-0021 precedent (`05bbe9a` — single commit that introduced
   lifecycle.rs + lib.rs wiring). Non-blocking; a future WO-style
   commit body could inline this rationale.

### OK

#### 1. Stub detection — CLEAN
No `todo!()`, `unimplemented!()`, `NotImplementedError`,
`raise NotImplementedError`, `pass`-only bodies, or trivial-return
stubs introduced.

Searched the added surface:
`crates/ucil-daemon/src/storage.rs` and the additive range of
`crates/ucil-daemon/src/lifecycle.rs` — zero matches for
`todo!|unimplemented!|NotImplementedError|raise NotImplementedError`.

The one literal-returning expression in new code is
`now_unix_secs()` at `lifecycle.rs:438-442`, which returns `0` on
clock-before-`UNIX_EPOCH` via `.map_or(0, |d| d.as_secs())`. This
mirrors the existing `session_manager::now_unix_secs` helper cited
by the work-order's scope_in line 30 as the intentional duplication
target. Documented in the function's rustdoc. Not a stub.

#### 2. Mocked critical dependencies — CLEAN
No mocks / fakes / stubs of Serena, LSP servers, SQLite, LanceDB,
or Docker introduced.

`grep -iE 'mock|fake|stub|MagicMock|Mockall'` against the new code
returns zero matches. Tests use real collaborators:

- `storage.rs:268, 325, 347, 361, 390` — `tempfile::TempDir` +
  real `std::fs::create_dir_all` for layout I/O; no filesystem mock.
- `lifecycle.rs:584, 631, 652, 667` — `tempfile::TempDir` +
  `Checkpoint::{write, read}` hitting real `std::fs` +
  `serde_json`; no JSON / filesystem mock.

Nothing in the WO-0022 surface would need to mock Serena, LSP,
SQLite, LanceDB, or Docker (those collaborators are not on the
storage / checkpoint code path), so the forbidden-mock list is
trivially satisfied.

#### 3. Skipped / ignored tests — CLEAN
No `#[ignore]`, `.skip()`, `xit`, `xfail`, or `pytest.mark.skip`
introduced.

A single `#[rustfmt::skip]` appears in `crates/ucil-daemon/src/lib.rs`
on the extended `pub use lifecycle::{Checkpoint, CheckpointError, ...}`
re-export line. This is a formatting directive on a re-export, **not
a test-suppression attribute** — it preserves the single-line form so
the work-order's `grep -q 'pub use lifecycle::.*Checkpoint'`
acceptance check still matches. Non-behavioural, explicitly
documented in commit `861dbcd`'s body.

#### 4. Weak assertions — CLEAN
Every assertion checks a behavioural invariant. No
`assert!(true)` / `assert_eq!(true, true)` / `assert_eq!(1, 1)`
patterns.

Assertion counts on the new surface:
- `storage.rs`: 40 `assert!` / `assert_eq!` / `assert_ne!` / `expect*`
  calls.
- `lifecycle.rs`: 64 total (existing PidFile + new Checkpoint).

Representative checks on the frozen selectors:
- `storage::test_two_tier_layout` (`storage.rs:267-320`) asserts
  every one of the seven directories named in master-plan §11.2
  actually exists on disk, idempotency equality, and every named
  path accessor resolves under the expected subtree (7 `assert!` +
  7 `assert_eq!`).
- `lifecycle::test_crash_recovery` (`lifecycle.rs:583-626`) writes
  a populated `Checkpoint`, round-trips it via `restore_or_new`,
  and checks every field; then removes the file and asserts
  `default_branch` + `CARGO_PKG_VERSION` fall-through. 10+
  behavioural assertions, nothing tautological.

#### 5. Hallucinated imports — CLEAN
All `use` statements on the new surface resolve against
`crates/ucil-daemon/Cargo.toml` (unchanged on this branch):

- `storage.rs`: `std::{path::{Path, PathBuf}, result::Result}` +
  `thiserror::Error` — std + workspace dep at `Cargo.toml:23`.
- `lifecycle.rs` additive range: `serde::{Deserialize, Serialize}`
  — workspace dep at `Cargo.toml:27`; `serde_json::{Error,
  to_writer_pretty, from_str}` reached via fully-qualified path —
  workspace dep at `Cargo.toml:28`; `tempfile::TempDir` (test-only)
  — dev-dep at `Cargo.toml:33`; `tracing::instrument` — workspace
  dep at `Cargo.toml:25`.

No import references a module / type / path that does not exist
on this branch.

#### 6. Feature coverage — CLEAN
Both features in WO-0022 have dedicated tests referencing their
behaviour by name:

- **P1-W2-F06** (two-tier `.ucil/` layout): 6 module-root tests in
  `storage.rs` — `test_two_tier_layout` (F06 frozen selector),
  `test_branch_name_with_slashes_is_sanitised`,
  `test_empty_branch_rejected`, `test_init_is_idempotent`,
  `test_shared_paths_are_branch_independent`,
  `test_storage_error_io_display_mentions_path`. Each exercises a
  distinct §11.2 invariant.
- **P1-W3-F09** (crash recovery via `.ucil/checkpoint.json`): 4
  module-root tests in `lifecycle.rs` — `test_crash_recovery` (F09
  frozen selector), `test_checkpoint_write_then_read_roundtrip`,
  `test_checkpoint_read_missing_returns_none`,
  `test_checkpoint_read_malformed_returns_parse_error`. Covers
  write→restore round-trip, fresh-start (Ok(None)), and
  malformed-JSON (Parse) distinctly.

Both frozen selectors (`storage::test_two_tier_layout` and
`lifecycle::test_crash_recovery`) are free `#[cfg(test)] #[test] fn`
items at module root — NOT wrapped in `mod tests { }`. The only
`mod tests { }` occurrences on the branch are inside comments
explaining why the pattern is avoided (storage.rs:258, 261;
lifecycle.rs:446, 577). Acceptance grep `^\s*mod\s+tests\s*\{` does
not match these. This is the precise rejection mode of WO-0006
(commits `0c604df`, `ab943e5`, `a8ab537`) and has been correctly
avoided here.

#### 7. Commit hygiene — CLEAN (with size advisories above)

All 5 commits on the branch carry Conventional-Commits subjects and
the required UCIL trailers:

| SHA | Subject | Lines | Phase | Feature(s) | WO |
|-----|---------|-------|-------|------------|------|
| `bca37f0` | `feat(daemon): add storage module with two-tier .ucil/ layout` | 432 | 1 | P1-W2-F06 | WO-0022 |
| `4c555c9` | `feat(daemon): lifecycle Checkpoint persists indexing progress for crash recovery` | 303 | 1 | P1-W3-F09 | WO-0022 |
| `a411d7c` | `chore(daemon): re-export StorageLayout + cross-reference Checkpoint in lib rustdoc` | 6 | 1 | BOTH | WO-0022 |
| `861dbcd` | `chore(daemon): preserve single-line lifecycle re-export for acceptance grep` | 5 | 1 | BOTH | WO-0022 |
| `636a8c9` | `chore(wo): mark WO-0022 ready for review — storage + crash-recovery` | 121 | 1 | BOTH | WO-0022 |

- Every `feat`/`chore` commit includes `Phase:`, `Feature:`,
  `Work-order:` trailers per `.claude/rules/commit-style.md`.
- Only `a411d7c`, `861dbcd`, and `636a8c9` carry `Feature: P1-W2-F06`
  AND `Feature: P1-W3-F09`; `bca37f0` trails only F06, `4c555c9`
  only F09 — correct pairing for the reality-check per-feature
  stash logic (one feature per code-introducing commit).
- No `--amend`, no force-push, no `--no-verify` hints in the commit
  objects.
- Two oversized commits (432 and 303 lines) are covered by
  DEC-0005/DEC-0001 and the reality-check atomicity requirement
  (see Warnings section above).

#### 8. Doc + public API — CLEAN
Every new `pub` item has a rustdoc block.

`storage.rs` — 2 types + 15 pub methods, all documented:
- `pub enum StorageError` with rustdoc + per-variant docs
  (`Io { path, source }` and `EmptyBranch`).
- `pub struct StorageLayout` with rustdoc explaining the
  `base`/`branch` invariants.
- `pub fn init(...)` — rustdoc + `# Errors` section documenting
  `EmptyBranch` and `Io` returns.
- All 9 path accessors (`base`, `branch`, `shared_dir`,
  `branch_dir`, `sessions_dir`, `plugins_dir`, `backups_dir`,
  `otel_dir`, `logs_dir`, `shared_knowledge_db_path`,
  `shared_memory_db_path`, `shared_history_db_path`,
  `branch_symbols_db_path`, `branch_vectors_dir`,
  `branch_tags_lmdb_path`, `branch_state_json_path`) have one-line
  rustdoc + `#[must_use]`.
- Module rustdoc on `storage.rs:1-44` cites master-plan §11.2
  lines 1060-1088 + §18 Phase 1 Week 2 line 1737 and includes a
  runnable-shape `# Examples` block.

`lifecycle.rs` additive range — 2 types + 4 pub methods, all
documented:
- `pub enum CheckpointError` + per-variant docs for `Io` and
  `Parse` (both carry `path: PathBuf` + `#[source]`).
- `pub struct Checkpoint` with per-field rustdoc on all 4 `pub`
  fields.
- `pub fn Checkpoint::{new, write, read, restore_or_new}` all have
  rustdoc; fallible methods (`write`, `read`, `restore_or_new`)
  include `# Errors` sections.
- Module-level rustdoc at `lifecycle.rs:1-39` extended with a
  third bullet naming `Checkpoint` + the `.ucil/checkpoint.json`
  path, matching scope_in line 25.

`lib.rs` re-exports (`pub mod storage;`,
`pub use lifecycle::{Checkpoint, CheckpointError, ...}`,
`pub use storage::{StorageError, StorageLayout};`) are transparent
and inherit the source-module rustdoc.

## Scope discipline

Eight forbidden-path groups confirmed at **0 lines diff** vs
`origin/main`:

- `crates/ucil-treesitter/` — 0 lines
- `crates/ucil-core/` — 0 lines
- `crates/ucil-lsp-diagnostics/` — 0 lines
- `crates/ucil-cli/` — 0 lines
- `crates/ucil-embeddings/` — 0 lines
- `crates/ucil-agents/` — 0 lines
- `adapters/` — 0 lines
- `ucil-build/feature-list.json` — 0 lines

`ml/` also unchanged. `crates/ucil-daemon/Cargo.toml` unchanged
(no new deps required — all of `serde`, `serde_json`, `thiserror`,
`tokio`, `tracing`, `tempfile` already present).

## Verdict

**CLEAN** — no blockers. Two commit-size warnings are covered by
DEC-0005/DEC-0001 (commit 1 module-introduction) and the
reality-check mutation-oracle atomicity requirement (commit 2,
documented in `0022-ready-for-review.md`). Neither is sufficient to
block verifier; both should inform the verifier's expectations about
commit-size ratios on module-introduction work orders.
