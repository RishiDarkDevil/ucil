# Critic Report: WO-0086

**Critic session**: crt-2026-05-09-wo0086
**Branch**: `feat/WO-0086-deps-cruiser-and-zoekt-manifests`
**Ref reviewed**: `7544fe9` (40-char SHA truncated to disambiguate from secret-scan)
**Range**: `main..feat/WO-0086-deps-cruiser-and-zoekt-manifests`
**Verdict**: CLEAN

## Scope of review

Diff range covers 9 files (891 insertions / 13 deletions) across 3 commits:

1. `90f61ef feat(plugins): add dependency-cruiser G4 architecture plugin manifest` (F14)
2. `8d2ad95 feat(plugins): add zoekt G2 search plugin manifest with trigram smoke` (F15)
3. `7544fe9 chore(rfr): WO-0086 ready-for-review marker` (RFR only)

Files touched:
- NEW `plugins/architecture/dependency-cruiser/plugin.toml`
- NEW `plugins/search/zoekt/plugin.toml`
- NEW `scripts/devtools/install-dependency-cruiser.sh`
- NEW `scripts/devtools/install-zoekt.sh`
- REPLACED `scripts/verify/P3-W10-F14.sh` (was TODO-stub)
- REPLACED `scripts/verify/P3-W10-F15.sh` (was TODO-stub)
- MODIFIED `crates/ucil-daemon/tests/g4_plugin_manifests.rs` (+65 lines; +1 test)
- MODIFIED `crates/ucil-daemon/tests/plugin_manifests.rs` (+57 lines; +1 test)
- NEW `ucil-build/work-orders/0086-ready-for-review.md`

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate, non-blocking)

1. **Per-feature feat commit size exceeds critic.md soft 200-LOC target** —
   `90f61ef` (F14) is +270/-6 across 4 files; `8d2ad95` (F15) is +341/-7
   across 4 files. critic.md §commit-hygiene rule reads "No commit >200
   lines of diff without a good reason." The reason here is documented:
   WO-0086 scope_in #16 + DEC-0005 module-coherence carve-out treats
   each feature's (manifest + install script + parse-only test +
   verify-script) as ONE cohesive unit. Acceptable per scope_in #16
   "DO NOT split the F14 OR F15 deliverables across multiple feat
   commits beyond the per-feature module-coherence carve-out — each
   feature's manifest + install script + verify script + parse-only
   test is ONE coherent unit per DEC-0005 (~150-200 LOC each commit)."
   The actual sizes are slightly over the ~150-200 LOC self-quoted
   target inside scope_in #16 itself, mostly because the verify scripts
   ballooned past ~100 LOC each (cycle fabrication for F14, full
   trigram-index + correctness invariant for F15). Soft warning, not a
   blocker — the cohesion rationale is documented in both commit
   messages.

2. **AC28 nominal commit cadence (4-6) vs actual (3)** — AC28 reads
   "Commit cadence: 4-6 commits matching DEC-0005 module-coherence
   (per-feature feat commit + per-feature test/verify commit + final
   RFR commit; F14 and F15 deliverables in separate coherent commits)."
   The actual is 3 commits (2 cohesive feat + 1 RFR). scope_in #16
   prescribes ONE coherent unit per feature, which is tension with
   AC28's 4-6 nominal range. Branch chose scope_in #16's interpretation;
   this is a known WO-internal inconsistency. The RFR's "Commit cadence"
   section documents the interpretation but does NOT call out the
   AC28 deviation in the "Disclosed deviations" enumeration. Soft
   warning — verifier should accept under scope_in #16's specificity
   but executor could amend the RFR's deviation list for tidiness.
   `crates/ucil-daemon/tests/g4_plugin_manifests.rs` — leading
   rustdoc was rewritten across 3 lines (the diff shows 5 minus lines,
   2 of them are headers). scope_in #7 carved this out: "The leading
   file-level rustdoc comment blocks may be amended ONLY to update the
   file overview." Permitted; flagging only because the per-test
   bodies inside `mod g4_plugin_manifests { ... }` and `mod
   plugin_manifests { ... }` were not deleted (✅ scope_in #7 honoured).

### OK

- **Stub detection**: zero hits for `todo!()`, `unimplemented!()`,
  `raise NotImplementedError`, bare `pass`, or trivial-default returns
  in any file added/modified by the diff. Test bodies invoke
  `PluginManifest::from_path` and assert on real parsed fields with
  SA-tagged operator-readable panics.
- **Mocked critical collaborators**: zero hits for `mock|fake|stub`
  keywords across the F14/F15 manifests, install scripts, verify
  scripts, AND test additions. Verify scripts spawn real `depcruise`,
  `zoekt`, `zoekt-index`, `rg`, `jq` binaries — no `MagicMock` /
  `Mockall` / fake harness anywhere on the diff.
- **Skipped / ignored tests**: zero hits for `#[ignore]`, `.skip(`,
  `xit(`, `pytest.mark.skip`, `pytest.mark.xfail` in the new test
  bodies (`crates/ucil-daemon/tests/g4_plugin_manifests.rs:167-216`,
  `crates/ucil-daemon/tests/plugin_manifests.rs:198-244`).
- **Weak assertions**: zero hits for `assert!(true)`,
  `assert_eq!(true, true)`, `assert True`, etc. All asserts inspect
  parsed-manifest field values and emit SA-tagged failure messages
  quoting the observed value.
- **Hallucinated imports**: `PluginManifest`, `repo_root()`, and the
  three `manifest.{plugin,capabilities,transport,lifecycle}` field
  paths all resolve to real entities at
  `crates/ucil-daemon/src/plugin_manager.rs:113-254`
  (struct definitions verified line-by-line). The `lifecycle:
  Option<LifecycleSection>` access via `.as_ref().expect(...)` is
  type-correct. No new `use` lines were added — both tests reuse the
  existing `use ucil_daemon::{HealthStatus, PluginManager,
  PluginManifest};` already at
  `crates/ucil-daemon/tests/g4_plugin_manifests.rs:54` and
  `crates/ucil-daemon/tests/plugin_manifests.rs:42`.
- **Feature coverage**: each WO-feature has at least one new test
  whose name + body reference the manifest under test:
  - P3-W10-F14 → `dependency_cruiser_manifest_parses`
    (`crates/ucil-daemon/tests/g4_plugin_manifests.rs:172`)
    parses `plugins/architecture/dependency-cruiser/plugin.toml` and
    asserts on `architecture.dependency` + `javascript`/`typescript`.
  - P3-W10-F15 → `zoekt_manifest_parses`
    (`crates/ucil-daemon/tests/plugin_manifests.rs:209`) parses
    `plugins/search/zoekt/plugin.toml` and asserts on `search.text` +
    language-agnostic + `transport.kind == "stdio"`.
- **Commit hygiene — Conventional Commits + trailers**: all 3 commits
  use `feat(plugins): ...` or `chore(rfr): ...` subjects ≤ 70 chars
  and carry `Phase: 3`, `Feature: ...`, `Work-order: WO-0086`
  trailers (verified via `git log --format='%b'` per-commit).
  - `90f61ef`: `Feature: P3-W10-F14` ✅
  - `8d2ad95`: `Feature: P3-W10-F15` ✅
  - `7544fe9`: `Feature: P3-W10-F14, P3-W10-F15` ✅ (RFR commit
    references both features; permitted for non-feat chore).
- **Commit hygiene — no merges / no force-push / no `--amend`**:
  `git log feat/WO-0086-deps-cruiser-and-zoekt-manifests ^main
  --merges` returns empty (AC23 ✅).
- **Forbidden paths untouched**: `git diff --name-only main...HEAD --
  tests/fixtures/** ucil-build/feature-list.json
  ucil-build/feature-list.schema.json ucil-master-plan-v2.1-final.md
  scripts/gate/** scripts/flip-feature.sh` returns empty (AC24/25/26
  ✅).
- **Pinned-immutable refs**: `! grep -qE
  '"(latest|next|main|master|head|develop|dev|nightly)"'
  plugins/.../plugin.toml` returns empty for both manifests. F14 pins
  `dependency-cruiser` to `17.4.0` (npm `dist-tags.latest`
  2026-05-09); F15 pins Zoekt to commit-SHA
  `2a1cee1ac057de4d43c0ce316b11fd3648c9ee25` <!-- gitleaks:allow --> (40-char
  SHA annotated `# gitleaks:allow` in the manifest to disambiguate from a
  token; sourcegraph/zoekt
  has no formal semver releases per RFR — verified via `gh api
  repos/sourcegraph/zoekt/releases` returning empty).
- **Production-side word-ban**: `! grep -qiE 'mock|fake|stub'
  plugins/architecture/dependency-cruiser/plugin.toml
  plugins/search/zoekt/plugin.toml
  scripts/devtools/install-dependency-cruiser.sh
  scripts/devtools/install-zoekt.sh scripts/verify/P3-W10-F14.sh
  scripts/verify/P3-W10-F15.sh` returns empty (AC09 ✅).
- **`std::process::Command` regression-guard**: zero hits in either
  test file (scope_in #14 ✅). The test additions are PARSE-ONLY (no
  subprocess invocation, no async/IO).
- **`unsafe` block regression-guard**: zero hits in either test file
  (scope_in #15 ✅). No new `unsafe std::env::set_var` introduced.
- **Doc + public API**: zero new `pub` items introduced under
  `crates/`. The two new tests are private `#[test]` items inside
  existing private `mod g4_plugin_manifests { ... }` and `mod
  plugin_manifests { ... }` integration-test wrappers (DEC-0007
  frozen-selector module-root placement). No rustdoc obligation.
- **DEC-0009 declarative-sentinel rationale**: each new test is
  preceded by an inline `//` comment block citing DEC-0009 + the
  reason this test is parse-only (dep-cruiser is CLI; Zoekt is HTTP
  API / external service); the runtime path lives in the verify
  script. Mirrors the WO-0051 ripgrep precedent verbatim.
- **Mutation contracts (M1-M4)**: documented in RFR with file path,
  exact patch, selector, expected SA-tagged panic body, observed
  fail line:column, restore command, and md5sum verification path
  per mutation. M4 selected an alternate form (`TOLERANCE_NS =
  -100s`) per scope_in #11's permissive clause; the alternate form
  preserves bash syntactic validity (the WO-proposed form orphans
  `then`/`fi`). Documented in RFR's "Disclosed deviations" §3.
- **RFR file**: `ucil-build/work-orders/0086-ready-for-review.md`
  exists; contains summary, live `depcruise --version` /
  `zoekt --version` / `rg --version` capture transcript with
  timestamp `2026-05-08T20:05:15Z`, M1-M4 mutation contracts in the
  canonical shape, UCIL_SKIP_SEARCH_PLUGIN_E2E new env-var rationale,
  bundle-shape rationale citing WO-0044 + WO-0051, and 5 disclosed
  deviations enumerated (AC22 ✅).
- **Bundle-shape rationale**: cited in `90f61ef` commit body
  ("WO-0086 bundles F14 + F15 since both are CLI/external-service
  plugins without stdio JSON-RPC transport, mirroring the WO-0044
  ast-grep+probe pair shape and WO-0051 ripgrep parse-only template")
  — DEC-0005 module-coherence + WO-0044/WO-0051 precedent both
  honoured (scope_in #29 ✅).

## Verdict

**CLEAN.**

All 8 critic-checklist items pass; only two soft warnings flagged
(commit-size exceeds critic.md's 200-LOC soft target with documented
DEC-0005 module-coherence rationale; AC28 nominal commit cadence
deviation undocumented in the RFR's deviation list). Neither warning
blocks the verifier. The diff is ready for verifier intake.
