# Critic Report: WO-0064 — `LancedbChunkIndexer` (P2-W8-F04)

**Critic session**: crt-2026-05-07T-wo-0064
**Branch**: `feat/WO-0064-lancedb-chunk-indexer`
**Tip sha (reviewed)**: `e737892fa7095a9a97cee069107180e67bc044c9`
**Merge base**: `5a28ee818b30f1f520f2933a04fecd12c58ab576` (`main`)
**Diff size**: 9 files, 2178 insertions, 4 deletions
**Verdict**: **CLEAN**

## Inputs reviewed

- Work-order: `ucil-build/work-orders/0064-lancedb-chunk-indexer.json`
- Feature ids: `P2-W8-F04` (background `LanceDB` chunk indexing,
  master-plan §18 Phase 2 Week 8 line 1789).
- Commit range: 9 commits, all carrying the required `Phase: 2 / Feature:
  P2-W8-F04 / Work-order: WO-0064` trailers.

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

_None._

### OK

#### 1. Stub detection — clean
- `rg -nE 'todo!\(\)|unimplemented!\(\)|raise NotImplementedError|panic!\(".*not yet'`
  on `crates/ucil-daemon/src/lancedb_indexer.rs`,
  `crates/ucil-daemon/src/executor.rs`,
  `crates/ucil-daemon/src/branch_manager.rs`: zero hits.
- `rg -nE 'TODO|FIXME|XXX|HACK'` on the three modified Rust files: zero hits.
- The `index_paths` body at `crates/ucil-daemon/src/lancedb_indexer.rs:658-773`
  contains real `tokio::fs::metadata` (line 684), `tokio::fs::read` (702),
  `Sha256::digest` (704), `lancedb::connect` (671), `conn.open_table` (672),
  and `table.add(iter).execute().await?` (767) — no return-`None`/-default
  stub bodies, no `panic!` placeholders.
- `IndexerState::save_atomic` (line 465-474) implements the real
  tmp-file + atomic rename pattern — not a literal stub.

#### 2. Mocked critical dependencies — clean
- `rg -niE 'mock|fake|stub|MagicMock|Mockall'` over the diff `+` lines
  for `crates/**/*.rs`, `tests/**/*.rs`, `scripts/**`: only newly-added
  matches are `TestEmbeddingSource` (and its docs/comments). This is a
  `UCIL`-internal trait impl on the project's own `EmbeddingSource`
  trait (`crates/ucil-daemon/src/lancedb_indexer.rs:285-309`), declared
  inside `#[cfg(test)]` at `crates/ucil-daemon/src/executor.rs:2699-2726`.
  The carve-out for `UCIL`-internal trait seams is per `DEC-0008` §4 and
  the work-order goal explicitly disclaims it as the same shape as
  `WO-0048`'s `ScriptedFakeSerenaHoverClient` precedent — not a Serena /
  LSP / SQLite / LanceDB / Docker mock.
- `BranchManager`, `LanceDB` `Table::add`, `lancedb::connect`,
  `tokio::fs::metadata`, and `Sha256::digest` are exercised on the real
  collaborator in both frozen acceptance tests
  (`crates/ucil-daemon/src/executor.rs:2803, 3030`).
- No `mockall`, `MagicMock`, or test-double of the prohibited
  collaborator set in the diff.

#### 3. Skipped / ignored tests — clean
- `rg -nE '#\[ignore\]|\.skip\(|xfail|xit\(|@pytest\.mark\.skip'` over
  the diff `+` lines: zero hits across all modified files.
- The single `UCIL_SKIP_EXTERNAL_PLUGIN_TESTS=1` reference in the RFR
  is a pre-existing, separately-justified executor-side env var
  (per WO-0044 lessons), not a test skip introduced here.

#### 4. Weak assertions — clean
- `rg -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)|expect\(true\)|assert True\b'`
  on the modified files: zero hits.
- The 6 sub-assertions in `executor::test_lancedb_incremental_indexing`
  (`crates/ucil-daemon/src/executor.rs:2856-3014`) carry actual
  expected values (`files_scanned == 2`, `chunks_inserted >= 2`,
  `len() > 1` for distinct hashes, `FixedSizeList<Float32, 768>`
  schema match) and operator-readable panic messages quoting the
  actual `IndexerStats` debug repr per the WO-0051 standing pattern.
- The 19 unit tests in `lancedb_indexer.rs::tests` (lines 970-1198)
  each carry meaningful assertions (`matches!`, `assert!(...contains)`,
  `assert_eq!(...)` against constructed inputs).

#### 5. Hallucinated paths / imports — clean
Every newly-added `use` / path reference resolves on this branch:
- `arrow_array::*`, `arrow_schema::*`: workspace deps already in use
  by `crates/ucil-daemon/src/branch_manager.rs`.
- `async_trait::async_trait`: workspace dep, transitively via
  `crates/ucil-daemon/Cargo.toml:71`.
- `crate::branch_manager::{code_chunks_schema, sanitise_branch_name,
  BranchManager, BranchManagerError}`: `sanitise_branch_name` is now
  promoted to `pub` at `crates/ucil-daemon/src/branch_manager.rs:357`
  (verified via the in-line carve-out comment); the other three are
  already public.
- `sha2::{Digest, Sha256}`: workspace dep newly added at
  `Cargo.toml:252` (`sha2 = "0.10"`).
- `thiserror::Error`: pre-existing workspace dep.
- `ucil_embeddings::{CodeRankEmbed, CodeRankEmbedError, EmbeddingChunker,
  EmbeddingChunkerError, EMBEDDING_DIM}`: all exported from
  `crates/ucil-embeddings/src/lib.rs:59-66`.
- `ucil_treesitter::Language`: `Language::from_extension(&str) ->
  Option<Self>` exists at `crates/ucil-treesitter/src/parser.rs:117`.
- `crate::watcher::{EventSource, FileEvent, FileEventKind}`: confirmed
  in the executor test imports (`executor.rs:3035`).
- `lancedb::{connect, query::{ExecutableQuery, QueryBase}}`,
  `arrow_array::cast::AsArray`, `futures::TryStreamExt`: `lancedb` is
  a workspace dep; `futures` is a NEW dev-dep added in this WO at
  `Cargo.toml:264`.

#### 6. Feature coverage — clean
- `P2-W8-F04` (background `LanceDB` chunk indexing): covered by
  - `executor::test_lancedb_incremental_indexing`
    (`crates/ucil-daemon/src/executor.rs:2803`) — six sub-assertions
    over real `BranchManager`, real `LanceDB`, real `Sha256`, real
    `tokio::fs::metadata`.
  - `executor::test_lancedb_indexer_handle_processes_events`
    (`crates/ucil-daemon/src/executor.rs:3030`) — proves the spawned
    `IndexerHandle` actually dispatches a `FileEventKind::Created` to
    `index_paths` and the table receives ≥1 row.
- 19 unit tests in `lancedb_indexer.rs::tests` exercise every
  reachable variant of `ChunkIndexerError` and `EmbeddingSourceError`,
  matching the AC12 + AC41 variant-coverage map in the RFR.
- No feature in the work-order is left untested.

#### 7. Commit hygiene — clean
- 9 commits on the branch, each Conventional Commits compliant
  (`feat(daemon)`, `test(daemon)`, `build(daemon)`, `chore(daemon)`,
  `chore(verify)`, `docs(wo)` × 2, `feat(daemon)`, `build(daemon)`).
- Every commit body carries `Phase: 2 / Feature: P2-W8-F04 / Work-order:
  WO-0064` trailers (verified: 9 occurrences of each).
- Longest subject is 69 chars
  (`chore(daemon): drop accidentally-staged profraw + restore coverage.md`)
  — within the 70-char ceiling per AC37 / WO-0059 lessons line 607.
- Largest commit (`feat(daemon): add lancedb_indexer module for P2-W8-F04`,
  1224-LOC NEW module) is justified inline in the commit body via the
  `DEC-0005` carve-out and the precedent stack
  (`WO-0046/0048/0056/0058/0059/0060/0061`).
- `chore(daemon): drop accidentally-staged profraw + restore coverage.md`
  is the cleanup commit that removes earlier accidentally-staged
  out-of-scope files (`*.profraw` + a modified
  `verification-reports/coverage-ucil-daemon.md`) — a fix-forward, not
  a force-push or amend.
- No `--amend` after push, no force-push detected; the RFR-marker
  commit (`ef88110`) followed by a `docs(wo)` self-edit
  (`e737892`) updating the final-sha disclosure is a normal
  fix-forward sequence.

#### 8. Doc + public API — clean
Every new `pub` item in `crates/ucil-daemon/src/lancedb_indexer.rs`
carries a rustdoc comment:
- `pub enum ChunkIndexerError` — docs at line 115-120 + per-variant docs
  for all 9 variants.
- `pub enum EmbeddingSourceError` — docs at line 232-237 + per-variant docs.
- `pub trait EmbeddingSource` — docs at line 262-283 + docs on `name`,
  `dim`, `embed`.
- `pub struct CodeRankEmbeddingSource` — docs at line 311-325 + docs on
  `load`, `model_dir`.
- `pub const INDEXER_STATE_SCHEMA_VERSION` — docs at 390-395.
- `pub struct IndexerState` — docs at 398-419 + per-field docs +
  per-method docs (`load_or_default`, `save_atomic`,
  `schema_version_current`).
- `pub struct IndexerStats` — docs at 487-494 + per-field docs.
- `pub struct LancedbChunkIndexer<S>` — docs at 514-521 + per-method
  docs.
- `pub struct IndexerHandle` — docs at 778-790 + per-method docs.

The promoted `pub fn sanitise_branch_name` at
`crates/ucil-daemon/src/branch_manager.rs:357` keeps and extends the
prior rustdoc with the `WO-0064` carve-out justification.

The `lib.rs` re-exports (`crates/ucil-daemon/src/lib.rs:189`) cover
all 8 new public symbols + the promoted `sanitise_branch_name` (line
183) — zero deferred re-export debt per AC26.

## Cross-cutting confirmations

- **Allow-list (AC24/AC34)**: `git diff --name-only main...HEAD`
  matches the 9-file allow-list exactly. No `tests/fixtures/**`,
  `ucil-build/feature-list*.json`, or
  `ucil-master-plan-v2.1-final.md` modification.
- **`unwrap()` / `expect()` discipline**: all 8 occurrences of
  `expect()` in `crates/ucil-daemon/src/lancedb_indexer.rs` (lines
  983-1019) are inside the `#[cfg(test)] mod tests {}` block.
  Production code uses `?` + typed `ChunkIndexerError`. The 5
  `unwrap_or(...)` / `unwrap_or_default()` calls in the production
  path (lines 694, 745, 746, 752, 869, 935) are infallible defaults
  for arithmetic / try_into bounds, not panic-on-error.
- **`tokio::time::timeout`**: this WO does not introduce new
  network IO awaits — `tokio::fs::*` (filesystem) and the in-process
  `lancedb::Table::add` await on local sqlite-backed storage. Per
  `.claude/rules/rust-style.md` the `timeout` rule covers IO-touching
  awaits; filesystem on local fs is acceptable without the wrapper
  (consistent with `branch_manager.rs` and `lifecycle.rs` precedent).
- **RFR sections (AC39-AC42)**: all four required headings present
  at lines 102 (`## Verifier-universal gates`), 127 (`## Upstream-API
  research`), 208 (`## Variant coverage map`), 236 (`## Mutation
  contract`).
- **Verify script**: `scripts/verify/P2-W8-F04.sh` is `set -euo
  pipefail`, has the standing shellcheck-on-PATH fallback per
  WO-0044, runs the 2 frozen selectors + the WO-0053 regression,
  and prints `[OK] P2-W8-F04` on success.

## Verdict

**CLEAN** — recommend forwarding to the verifier.

No blockers; no warnings. The diff is tightly scoped to the AC24
allow-list, all 9 commits are conventional + carry the required
trailers, every newly-public symbol is documented + re-exported,
and the only `TestEmbeddingSource` test seam is a `UCIL`-internal
`EmbeddingSource` trait impl per `DEC-0008` §4 (matching the
`WO-0048` `ScriptedFakeSerenaHoverClient` precedent), not a
critical-collaborator mock.
