# Critic Report: WO-0064 — `LancedbChunkIndexer` (P2-W8-F04)

**Critic session**: crt-2026-05-07T-wo-0064-r2
**Branch**: `feat/WO-0064-lancedb-chunk-indexer` (already merged → `main` at `bbe645d`)
**Tip sha (reviewed)**: `e737892fa7095a9a97cee069107180e67bc044c9`
**Merge base**: `5a28ee818b30f1f520f2933a04fecd12c58ab576` (`main`)
**Commit range**: `5a28ee8..e737892` — 9 commits, 9 files, 2178 insertions, 4 deletions
**Verdict**: **CLEAN**

## Re-review context

This is the third critic pass on the same diff — original (`ed09fe1`)
and retry-1 (`4c08d29`) both reached CLEAN. Retry-2 was triggered
by the harness re-dispatching a stale critic prompt for an
already-verified-PASS / merged work-order; same root cause
documented in:

- `ucil-build/escalations/20260507T0156Z-wo-0064-stale-rejection-prompt-post-merge.md`
- `ucil-build/escalations/20260507T0205Z-wo-0064-stale-verifier-prompt-post-merge.md`
- `ucil-build/escalations/20260507T0207Z-wo-0064-stale-executor-prompt-post-merge-r2.md`

The underlying state at the moment of review:

| Fact | Evidence |
|------|----------|
| `P2-W8-F04.passes` | `true` — `last_verified_by: verifier-7f7ea48e-8254-4118-b031-ea2a01b5d1d5`, `last_verified_commit: e737892…`, `last_verified_ts: 2026-05-07T01:52:36Z`, `attempts: 0` |
| Verifier verdict | `ucil-build/verification-reports/WO-0064.md` — `**Verdict**: **PASS**` (commit `5c36efd`) |
| Branch merged | commit `bbe645d merge: WO-0064 lancedb-chunk-indexer (orphan → main) — P2-W8-F04 closure` |
| Prior critic verdicts | `ed09fe1` (orig CLEAN), `4c08d29` (retry-1 CLEAN re-confirmed) — both at the same tip-sha `e737892` |
| Diff identity | `git diff --stat 5a28ee8..e737892` reports the same 9 files / 2178 insertions as the prior reviews |
| `git log e737892..HEAD` | only meta-housekeeping commits (`ed09fe1`, `5c36efd`, `bbe645d`, `8969512`, `4c08d29`, `33f26f3`, `6231d40`) — no source-tree changes since last review |

There is **no rejection artefact** to remediate from
(`ucil-build/rejections/WO-0064.md` does not exist) and **no new
diff** since retry-1. This re-review re-runs the eight critic
checks against the same range and re-confirms the prior CLEAN
verdict.

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

_None._

### OK

#### 1. Stub detection — clean

- `Grep todo!\(\)|unimplemented!\(\)|raise NotImplementedError`
  on `crates/ucil-daemon/src/lancedb_indexer.rs`: **0 hits**.
- `Grep #\[ignore\]|\.skip\(|#\[cfg_attr\(.*ignore`
  on `crates/ucil-daemon/src/lancedb_indexer.rs`: **0 hits**.
- `index_paths` body (`crates/ucil-daemon/src/lancedb_indexer.rs:658-773`)
  is real code: `tokio::fs::metadata` (684), `tokio::fs::read` (702),
  `Sha256::digest` (704), `lancedb::connect` (671), `conn.open_table`
  (672), `table.add(iter).execute().await?` (767). No literal-stub
  returns and no `panic!` placeholders.
- `IndexerState::save_atomic` (lines 465-474) implements the real
  tmp-file + `tokio::fs::rename` atomic pattern.
- `infer_language` / `language_name` / `build_record_batch` /
  `now_micros` (`lancedb_indexer.rs:883-963`) are real
  implementations exercised by the unit tests at lines 1172-1197.

#### 2. Mocked critical dependencies — clean

- `Grep -i mock|fake|MagicMock|Mockall` over
  `crates/ucil-daemon/src/lancedb_indexer.rs`: **0 hits**.
- `Grep -i mock|fake|MagicMock|Mockall` over
  `crates/ucil-daemon/src/executor.rs` surfaces only pre-existing
  references to `fake_serena_hover_client::ScriptedFakeSerenaHoverClient`
  (lines 50, 517-519, 523, 625-626, 726, 1893, 1898) — all
  introduced by `WO-0048`/`WO-0058` for the call-hierarchy fusion
  test seam, NOT by this WO. They are explicitly carved out under
  `DEC-0008 §4` for hand-written project-trait fakes (distinct from
  Serena MCP wire-format mocks, which remain forbidden).
- The two frozen acceptance tests at module root of `executor.rs`
  (`test_lancedb_incremental_indexing` line 2803,
  `test_lancedb_indexer_handle_processes_events` line 3030) exercise
  the **real** collaborators end-to-end:
  - real `BranchManager::create_branch_table("main", None)` (2818, 3044)
  - real `lancedb::connect(...).execute().await` (2754, 2991)
  - real `tokio::fs::metadata` / `tokio::fs::write` / `tokio::fs::rename`
    (no fs mock)
  - real `Sha256::digest` for `file_hash` (production path at
    `lancedb_indexer.rs:704`)
  - real `IndexerHandle::spawn` driving an `mpsc::Receiver<FileEvent>`
    (3072-3084)
- The only test-side substitution in this WO is
  `TestEmbeddingSource` (`executor.rs:2700-2726`) — a `#[cfg(test)]`
  impl of UCIL's own `EmbeddingSource` trait. Per `DEC-0008 §4` and
  the precedent stack (`WO-0048 ScriptedFakeSerenaHoverClient`,
  `WO-0061 *Source` test impls), in-repo trait impls are explicitly
  distinct from the prohibited critical-dep substitution layers
  (`Serena` / `LSP` / `SQLite` / `LanceDB` / `Docker`).

#### 3. Skipped / ignored tests — clean

- `Grep #\[ignore\]|\.skip\(|xfail|xit\(|@pytest\.mark\.skip`
  across `crates/ucil-daemon/src/`: **0 hits**.
- The `UCIL_SKIP_EXTERNAL_PLUGIN_TESTS=1` reference in the RFR is a
  pre-existing executor-side env var (per `WO-0044` lessons), not a
  test skip introduced here.
- Both frozen acceptance selectors are
  `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]`
  and run unconditionally.

#### 4. Weak assertions — clean

- `Grep assert!\(true\)|assert_eq!\(true,\s*true\)|expect\(true\)`
  across `crates/ucil-daemon/src/`: **0 hits**.
- The 6 sub-assertions in `test_lancedb_incremental_indexing`
  (`executor.rs:2856-3014`) carry actual expected values —
  `files_scanned == 2`, `files_skipped_unchanged == 2`,
  `chunks_inserted >= 2`, `foo_hashes.len() > 1`, `bar_hashes.len()
  == 1`, `FixedSizeList<Float32, 768>` schema match — and
  operator-readable panic messages quoting the actual `IndexerStats`
  debug repr per the `WO-0051` standing pattern.
- The handle test (`executor.rs:3087-3091`) asserts `count >= 1`
  with a precise panic-on-fail message describing the watcher →
  indexer dispatch wiring being tested.
- The 19 unit tests in `lancedb_indexer.rs::tests` (970-1197) each
  carry meaningful assertions: `matches!(e, ChunkIndexerError::Foo
  { .. })` for variant coverage, `assert!(msg.contains("..."))` for
  Display content, `assert_eq!` against constructed inputs.

#### 5. Hallucinated paths / imports — clean

Every newly-added `use` / path reference still resolves on this branch:

- `arrow_array::*`, `arrow_schema::*`: workspace deps
  (`Cargo.toml:246-247`, already used by `branch_manager.rs`).
- `async_trait::async_trait`: workspace dep
  (`Cargo.toml:120`, daemon `Cargo.toml:71`).
- `sha2::{Digest, Sha256}`: workspace dep
  (`Cargo.toml:254`, daemon `Cargo.toml:96`).
- `thiserror::Error`: pre-existing workspace dep.
- `ucil_embeddings::{CodeRankEmbed, CodeRankEmbedError,
  EmbeddingChunker, EmbeddingChunkerError, EMBEDDING_DIM}`: all
  exported from `crates/ucil-embeddings/src/lib.rs:59-63`.
- `ucil_treesitter::Language`: `Language::from_extension(&str)`
  exists at `crates/ucil-treesitter/src/parser.rs:117`.
- `crate::branch_manager::{code_chunks_schema, sanitise_branch_name,
  BranchManager, BranchManagerError}`: `sanitise_branch_name` is
  promoted to `pub` at `crates/ucil-daemon/src/branch_manager.rs:357`
  (with the in-line carve-out comment justifying the visibility
  bump for `lancedb_indexer.rs` as the third caller).
- `crate::watcher::{EventSource, FileEvent, FileEventKind}`:
  resolved in the executor handle test (`executor.rs:3035`).
- `lancedb::{connect, query::{ExecutableQuery, QueryBase}}`,
  `arrow_array::cast::AsArray`, `futures::TryStreamExt`: `lancedb`
  is a workspace dep (`Cargo.toml:238`); `futures` is a workspace
  dep added in this WO at `Cargo.toml:264`.

#### 6. Feature coverage — clean

`P2-W8-F04` (background `LanceDB` chunk indexing — master-plan §18
Phase 2 Week 8 line 1789, §12.2 lines 1321-1346) has the following
coverage:

- `executor::test_lancedb_incremental_indexing`
  (`crates/ucil-daemon/src/executor.rs:2803`) — six sub-assertions
  over real `BranchManager`, real `LanceDB`, real `Sha256`, real
  `tokio::fs::metadata` covering: SA1 first-pass index-all, SA2
  unchanged-skip, SA3 touched-only-reindex, SA4 file_hash divergence
  on touched file, SA5 state JSON round-trip, SA6
  `FixedSizeList<Float32, 768>` schema match. This is the
  acceptance selector frozen in `feature-list.json`.
- `executor::test_lancedb_indexer_handle_processes_events`
  (`executor.rs:3030`) — proves the spawned `IndexerHandle`
  dispatches a `FileEventKind::Created` event to `index_paths` and
  the table receives ≥1 row.
- 19 unit tests in `lancedb_indexer.rs::tests` exercise every
  reachable variant of `ChunkIndexerError` and
  `EmbeddingSourceError` plus state round-trip + corrupt-JSON +
  missing-file + language inference + Display formatters.
- The `WO-0053` regression
  (`branch_manager::test_lancedb_per_branch`) is invoked by
  `scripts/verify/P2-W8-F04.sh` to prove the
  `pub fn sanitise_branch_name` promotion did not regress
  `P2-W7-F09` lifecycle.

No feature in the work-order is left untested.

#### 7. Commit hygiene — clean

```
e737892 docs(wo): update RFR final-sha to include the marker commit         (3 LOC)
ef88110 docs(wo): RFR for WO-0064 P2-W8-F04 lancedb chunk indexer         (326 LOC, RFR-doc by spec)
d118dea chore(daemon): drop accidentally-staged profraw + restore coverage.md (102 LOC, fix-forward cleanup)
223202d build(daemon): drop duplicate dev-dep sha2 + async-trait           (111 LOC)
0d25124 chore(verify): add scripts/verify/P2-W8-F04.sh                     (123 LOC)
3c7ca8d test(daemon): add executor::test_lancedb_incremental_indexing      (471 LOC, two frozen tests + dev-deps, justified inline per DEC-0007)
be64ab8 feat(daemon): add lancedb_indexer module for P2-W8-F04            (1224 LOC NEW module, justified inline per DEC-0005 + precedent stack)
6d76af3 feat(daemon): promote sanitise_branch_name to pub                  (15 LOC)
c66f8be build(daemon): add ucil-embeddings + sha2 deps for P2-W8-F04       (21 LOC)
```

- 9 commits, each Conventional Commits compliant
  (`feat(daemon)` ×2, `test(daemon)`, `build(daemon)` ×2,
  `chore(daemon)`, `chore(verify)`, `docs(wo)` ×2).
- Every commit body carries `Phase: 2 / Feature: P2-W8-F04 /
  Work-order: WO-0064` trailers (verified — 9 occurrences of each).
- Longest subject is 69 chars
  (`chore(daemon): drop accidentally-staged profraw + restore coverage.md`)
  — within the 70-char ceiling.
- The two >200-LOC commits each carry an in-body justification:
  - `be64ab8` (1224 LOC) cites `DEC-0005` carve-out for new-module
    commits and the seven-WO precedent stack
    (`WO-0046/0048/0056/0058/0059/0060/0061`).
  - `3c7ca8d` (471 LOC) cites `DEC-0007` (frozen acceptance tests at
    `executor.rs` module root) plus the dev-dep additions for the
    test seam.
  - `ef88110` (326 LOC) is a `docs(wo)` RFR marker file — by-spec
    ~300-line size.
- `d118dea` is a fix-forward cleanup that removes earlier
  accidentally-staged out-of-scope files (`*.profraw` + a modified
  `verification-reports/coverage-ucil-daemon.md`) — not a
  force-push or amend.
- No `--amend` after push, no force-push detected; the RFR-marker
  commit (`ef88110`) followed by a `docs(wo)` self-edit
  (`e737892`) updating the final-sha disclosure is a normal
  fix-forward sequence.

#### 8. Doc + public API — clean

`Grep pub fn|pub async fn|pub struct|pub enum|pub trait|pub const`
on `crates/ucil-daemon/src/lancedb_indexer.rs` returns 21 public
items, every one of which carries a rustdoc comment with
per-variant docs on enums and per-field docs on structs:

| Symbol | Line | Docs |
|--------|------|------|
| `pub enum ChunkIndexerError` | 121 | doc comment + per-variant docs (9 variants) |
| `pub enum EmbeddingSourceError` | 238 | doc comment + per-variant docs (2 variants) |
| `pub trait EmbeddingSource` | 285 | doc comment + per-method docs (`name`, `dim`, `embed`) |
| `pub struct CodeRankEmbeddingSource` | 326 | doc comment + Debug impl + `pub fn load`, `pub fn model_dir` |
| `pub const INDEXER_STATE_SCHEMA_VERSION` | 396 | doc comment |
| `pub struct IndexerState` | 421 | doc comment + per-field docs + `pub fn load_or_default`, `pub async fn save_atomic`, `pub const fn schema_version_current` |
| `pub struct IndexerStats` | 496 | doc comment + per-field docs |
| `pub struct LancedbChunkIndexer<S>` | 528 | doc comment + per-method docs (`new`, `state_path`, `state`, `load_state`, `index_paths`) |
| `pub struct IndexerHandle` | 798 | doc comment + per-method docs (`spawn`, `shutdown`) |

The promoted `pub fn sanitise_branch_name` at
`crates/ucil-daemon/src/branch_manager.rs:357` keeps and extends
the prior rustdoc with the `WO-0064` carve-out justification (lines
350-355).

The `lib.rs` re-exports
(`crates/ucil-daemon/src/lib.rs:189`) cover all 8 new public
symbols + the promoted `sanitise_branch_name` (line 183) — zero
deferred re-export debt.

## Cross-cutting confirmations

- **Allow-list**: `git diff --name-only 5a28ee8..e737892` matches
  the 9-file set exactly. No `tests/fixtures/**`,
  `ucil-build/feature-list.json`, or
  `ucil-master-plan-v2.1-final.md` modification.
- **`unwrap()` / `expect()` discipline**: all 8 occurrences of
  `expect(...)` in `lancedb_indexer.rs` are inside
  `#[cfg(test)] mod tests {}` (lines 983-1019). Production code
  uses `?` + typed `ChunkIndexerError`. The 5
  `unwrap_or(...)` / `unwrap_or_default()` calls in the production
  path (lines 694, 745, 746, 752) are infallible defaults for
  arithmetic / `try_into` saturation, not panic-on-error.
- **`tokio::time::timeout`**: this WO does not introduce new
  network IO awaits — `tokio::fs::*` (filesystem) and the
  in-process `lancedb::Table::add` await on local sqlite-backed
  storage. Per `.claude/rules/rust-style.md` the `timeout` rule
  covers IO-touching network awaits; filesystem on local fs is
  acceptable without the wrapper (consistent with
  `branch_manager.rs` and `lifecycle.rs` precedent).
- **RFR sections (AC39-AC42)**: all four required headings present
  in `0064-ready-for-review.md` (verifier-universal gates,
  upstream-API research, variant coverage map, mutation contract).
- **Verify script**: `scripts/verify/P2-W8-F04.sh` is `set -euo
  pipefail`, has the standing shellcheck-on-PATH fallback per
  `WO-0044`, runs the 2 frozen selectors + the `WO-0053`
  regression, and prints `[OK] P2-W8-F04` on success.

## Verdict

**CLEAN** — re-confirms both the original (`ed09fe1`) and the
retry-1 (`4c08d29`) CLEAN verdicts at the same tip-sha
(`e737892`). All 8 mechanical checks pass; no blockers, no
warnings.

The work-order is already fully verified-PASS and merged at
`main @ bbe645d`; this retry-2 re-review re-runs the checks
against the same diff and reaches the same outcome. Per the open
post-merge stale-prompt escalations
(`20260507T0156Z`, `20260507T0205Z`, `20260507T0207Z`), the
underlying harness bug — re-dispatching critic / verifier /
executor prompts for a verified-PASS / merged work-order — is
the recurring stale-prompt issue previously documented for
`WO-0063`, and is the appropriate target for the
`harness-fixer` agent, not for further critic / executor /
verifier turns on this WO.
