# Critic Report: WO-0059

**Critic session**: crt-2026-05-07T-WO-0059
**Branch**: `feat/WO-0059-ucil-embeddings-coderankembed`
**HEAD reviewed**: `6cd94abde6e74a55d153c56bbdcb71f3dd9d10df` (`chore(rfr): document AC24 commit-count deviation`)
**Merge base**: `82ad8267d0504dc3796d0b51870e41a0ad6b2e34` (`main`; tip is the WO-0059 emit-commit, no overlap with the in-flight WO-0057)
**Diff scope**: 10 commits, 9 paths, +1216 / −2 (`Cargo.lock` auto-bloat dominates: 313 of 1216 insertion lines)
**Verdict**: **CLEAN with warnings**

The diff lands `P2-W8-F02` (CodeRankEmbed default CPU embedding model)
on top of the foundational `OnnxSession` from WO-0058. NEW module
`crates/ucil-embeddings/src/models.rs` declares `pub struct
CodeRankEmbed`, `pub enum CodeRankEmbedError`, `pub const
EMBEDDING_DIM: usize = 768`, the frozen acceptance test
`models::test_coderankembed_inference` at module root per DEC-0007,
and an idempotent installer + verify-script pair. The 138MB model +
712KB tokenizer are gitignored and downloaded by
`scripts/devtools/install-coderankembed.sh` from a sha256-pinned
HuggingFace mirror. No critical-collaborator mocks, no stubs, no
`#[ignore]` / `.skip()`, no weak assertions, no `unsafe`, no
forbidden-path edits, no commits of the 137MB binary.

## Findings

### Blockers (must fix before verifier)

**None.**

### Warnings (should be acknowledged before gate)

1. **AC22 commit-subject 70-char overshoot** — commit `c4d375a build(embeddings): add tokenizers workspace dep for HuggingFace tokenizer` is **73 chars** (3 over the `.claude/rules/commit-style.md` cap). The other 9 subjects are 43–68 chars (compliant). Constraint chain documented in `ucil-build/work-orders/0059-ready-for-review.md` (`## Documented AC deviations` → `#### AC22 — commit subject 73 chars`): the WO scope_in[16] template itself was 82 chars; the executor shortened to 73; further trimming would require `--amend after push` (forbidden by root CLAUDE.md) or `git push --force` (also forbidden). WO-0058 set a 2-char-overshoot precedent; this is 3-char. Verifier judgment call — accept as documented deviation, escalate via ADR, or reject.

2. **AC24 commit-count soft-ceiling overshoot** — 10 commits vs ceiling 8 (floor `>= 3` met). All 10 carry the conventional `Phase: 2 / Feature: P2-W8-F02 / Work-order: WO-0059` trailer (10/10/10 — AC23 fully clean). Constraint chain documented in the RFR (`#### AC24 — commit count 10 vs soft ceiling 8`):
   - `c4d375a` (build dep)
   - `5e1d640` (NEW module + frozen test)
   - `4e86bb9` (lib.rs re-export)
   - `fedfb06` (refactor: drop `OnnxSession` composition for direct `ort::Session` — forced by the production model declaring dual inputs `input_ids` + `attention_mask`, single-input `OnnxSession::infer` insufficient, `crates/ucil-embeddings/src/onnx_inference.rs` is in `forbidden_paths`)
   - `02a4293` (`.gitignore`)
   - `ab68f77` (installer)
   - `7533b13` (verify script)
   - `f3069bf` (rustdoc backtick cleanup for AC25)
   - `e9f6845` (RFR marker)
   - `6cd94ab` (RFR amendment for AC24 deviation — required because the deviation was discovered after the initial RFR push, and `--amend after push` is forbidden).

   Soft ceiling, not hard gate. Accept-as-documented per WO-0058 line 568 precedent.

3. **Single 425-LOC commit** — `5e1d640 feat(embeddings): add CodeRankEmbed model + frozen acceptance test` lands the entire NEW `models.rs` (425 insertions) in one commit, well over the ~50-line soft target in `.claude/rules/commit-style.md`. The commit body explicitly cites DEC-0005 module-coherence + WO-0048 297-LOC + WO-0056 542-LOC + WO-0058 305-LOC precedents. Splitting would have triggered `#![deny(warnings)]` cascade between intermediate states. Soft target, documented justification — accept.

4. **AC25 unbacked-uppercase rustdoc filter returns 10 lines** vs expected `0`. The RFR analysis (`#### AC25 / AC28 — 10 unbacked uppercase rustdoc lines`) notes the filter regex `` `[A-Z][A-Z_0-9]+` `` rejects hyphenated backticked references like `` `P2-W8-F02` ``, so lines whose only all-caps tokens are inside hyphenated backticked anchors register as "unbacked" even though every uppercase identifier is correctly fenced. Comparable to the WO-0058 verifier-accepted `crates/ucil-embeddings/src/onnx_inference.rs` which returns 11 with the same grep. The actual enforcement gate `cargo clippy -p ucil-embeddings --tests -- -D warnings` (the `clippy::doc_markdown` lint) passes clean per the RFR. Guidance grep, not authoritative — accept.

5. **Variant-shape soft smell** at `models.rs:377-382` — when the ONNX outputs map lacks a `sentence_embedding` key, the code returns `CodeRankEmbedError::DimensionMismatch { expected: EMBEDDING_DIM, got: 0 }`. Strictly this is a missing-output category, not a dimension-mismatch. The `#[non_exhaustive]` enum can grow a `MissingOutput { name: String }` variant in a follow-up WO without breaking downstream. Not load-bearing — accept.

### OK

- **Stubs**: zero hits for `todo!()`, `unimplemented!()`, `raise NotImplementedError`, bare `pass`, or trivial-default returns (`return None`, `Default::default()`, `Vec::new()`, `true`, `false`, `0`, `""`) in shipped code (`crates/ucil-embeddings/src/models.rs`). The mutation entries M1/M2 in the RFR document early-return `Err(MissingModelFile)` / `Ok(Vec::new())` patches as **mutation tests**, not as shipped code.
- **Mocks of critical collaborators**: zero. The test exercises real `ort 2.0.0-rc.12` `Session::run`, real `tokenizers 0.23` `Tokenizer::from_file`/`encode`, real on-disk 138MB CodeRankEmbed Int8 ONNX model, real 712KB HuggingFace `tokenizer.json`. No `mock`/`fake`/`stub`/`MagicMock`/`Mockall` references anywhere in the changed sources. ONNX Runtime is on the no-mock list per `.claude/rules/rust-style.md`; tokenizers is implicitly covered.
- **Skipped/ignored tests**: zero. The single `#[ignore]` substring match (`models.rs:430`) is a rustdoc paragraph **explaining why** the executor used a hard `panic!` pre-flight assertion instead of `#[ignore]` (per WO-0055 lessons line 458); not an actual `#[ignore]` attribute.
- **Weak assertions**: zero hits for `assert!(true)`, `assert_eq!(true, true)`, or `assert!(1 == 1)`. The `assert_eq!(embedding.len(), 768, ...)` at line 483 is the load-bearing master-plan-frozen invariant.
- **Hallucinated imports**: zero. All `use` items in `models.rs` (`std::path::{Path, PathBuf}`, `ndarray::Array2`, `ort::session::Session`, `ort::value::Tensor`, `tokenizers::Tokenizer`) resolve — `ndarray` and `ort` are pre-existing workspace deps; `tokenizers = "0.23"` is the newly-added workspace dep declared in `Cargo.toml:193-204` and pulled into `crates/ucil-embeddings/Cargo.toml:14`. `Cargo.lock` shows the `tokenizers 0.21.x` resolution chain (313 line additions).
- **Feature coverage**: `P2-W8-F02` (the only feature in the WO) is covered by the new `#[test] fn test_coderankembed_inference()` at module root of `crates/ucil-embeddings/src/models.rs:435-491`, matching the frozen `feature-list.json:P2-W8-F02.acceptance_tests[0].selector = "-p ucil-embeddings models::test_coderankembed_inference"` per DEC-0007. Test exercises 5 sub-assertions (SA1 load, SA2 tokenizer round-trip, SA3 embed succeeds, SA4 `len() == 768`, SA5 all floats finite) per WO scope_in[8].
- **Operator-friendly missing-binary precondition**: pre-flight `assert!(model_onnx.exists() && tokenizer_json.exists(), ...)` at `models.rs:447-454` panics with an actionable message pointing at `bash scripts/devtools/install-coderankembed.sh`, not a runtime-skip via early `return` (which is functionally `#[ignore]` per WO-0055 lesson).
- **Commit-message trailers**: 10/10 commits have `Phase: 2`, 10/10 have `Feature: P2-W8-F02`, 10/10 have `Work-order: WO-0059` (AC23 fully green). All 10 are Conventional Commits with valid types: `build`, `feat`, `feat`, `feat`, `refactor`, `build`, `feat`, `test`, `docs`, `chore`, `chore`.
- **Allow-list discipline**: `git diff --name-only main..HEAD` returns exactly 9 paths, 100% inside the WO allow-list — `Cargo.lock`, `Cargo.toml`, `crates/ucil-embeddings/Cargo.toml`, `crates/ucil-embeddings/src/lib.rs`, `crates/ucil-embeddings/src/models.rs` (NEW), `ml/models/coderankembed/.gitignore` (NEW), `scripts/devtools/install-coderankembed.sh` (NEW), `scripts/verify/P2-W8-F02.sh` (NEW), `ucil-build/work-orders/0059-ready-for-review.md` (NEW). Zero forbidden-path touches: `crates/ucil-embeddings/src/onnx_inference.rs` (forbidden), `crates/ucil-embeddings/tests/**` (forbidden), all sibling-crate paths, `ucil-build/feature-list.json`, master plan, hooks — all untouched.
- **No 137MB binary committed**: `git ls-tree feat/WO-0059-ucil-embeddings-coderankembed -- ml/models/coderankembed/` returns exactly one entry: `ml/models/coderankembed/.gitignore`. The `model.onnx` (138081004 bytes) and `tokenizer.json` (711649 bytes) are gitignored per AC26 + scope_out[10].
- **Devtool + verify-script integrity**: both scripts committed with mode `100755` (executable). Installer is idempotent (sha256 short-circuit + curl-with-retry + verify-then-commit pattern). Verify script fail-fasts on missing real binary BEFORE any cargo invocation per WO-0055 lesson 458, runs the installer if needed, greps the frozen module-root selector, runs `cargo test` and matches both cargo-test (`test result: ok. ... 0 failed`) and nextest (`N tests passed`) summary-line shapes per WO-0042+ reusable shape.
- **Public-API rustdoc**: every `pub` item in `models.rs` carries a doc comment — `EMBEDDING_DIM` (88), `CodeRankEmbedError` (98) + each variant (99-110, 118-121, 129-142, 149, 157-166, 173-182), `CodeRankEmbed` (214), `load` (258) with `# Errors` + `# Examples`, `embed` (351) with `# Errors` + `# Examples`. Module-level `//!` preamble cites master-plan §13 line 1332, §17.6 lines 2028-2029, §18 Phase 2 Week 8 line 1787, plus the upstream-fit divergence rationale for the dropped `OnnxSession` composition.
- **`unsafe` discipline**: zero `unsafe` blocks introduced.
- **Tracing instrumentation**: both `load` and `embed` carry `#[tracing::instrument(name = "ucil.embeddings.coderankembed.<op>", level = "debug", ...)]` per master-plan §15.2 span-naming convention.
- **`#![deny(warnings)]` + `clippy::pedantic` + `clippy::nursery`** preserved at the crate root; the RFR claims `cargo clippy -p ucil-embeddings -- -D warnings` exits 0.
- **Mutation prep** documented in the RFR (`### Mutation test analysis`): M1 (load early-Err) → SA1 panics; M2 (embed early-empty-Vec) → SA4 panics on len=0; M3 (`EMBEDDING_DIM = 768 → 100`) → embed's post-normalisation invariant fires `DimensionMismatch`, SA3 panics with that error chain. All three patches each fail SOME assertion in the test per the WO-0048+ verifier-accepts standing rule.
- **Five upstream-API-shape adaptations** per WO-0058 line 543 precedent are all documented inline + in the RFR: (1) `tokenizers 0.20 → 0.23` (closest stable semver, API surface unchanged); (2) `Tokenizer` error field `source → message` (thiserror auto-treats `source`-named fields as `&dyn Error` chain links, `String` does not implement `Error`); (3) drop `OnnxSession` composition for direct `ort::Session` (production model is dual-input, single-input `OnnxSession::infer` insufficient, `onnx_inference.rs` in forbidden_paths); (4) read pre-pooled `sentence_embedding` output, no manual mean-pool (graph bakes mean-pooling-over-attention-mask via upstream `1_Pooling/config.json`); (5) single-line `assert_eq!` to fit AC09's regex + rustfmt 100-col budget. None of these constitute novel design decisions warranting an ADR per WO-0058 scope_out[6] precedent.

## Critic checklist

| # | Check | Result |
|---|-------|--------|
| 1 | Stub detection (`todo!()`, `unimplemented!()`, `NotImplementedError`, bare `pass`, trivial-default returns) | **CLEAN** |
| 2 | Mocked critical deps (Serena, LSP, SQLite, LanceDB, Docker, ONNX, tokenizers) | **CLEAN** |
| 3 | Skipped/ignored tests (`#[ignore]`, `.skip()`, `xfail`, `xit`, `pytest.skip`) | **CLEAN** |
| 4 | Weak assertions (`assert!(true)`, `expect(true)`, etc.) | **CLEAN** |
| 5 | Hallucinated imports (`use` not resolving) | **CLEAN** |
| 6 | Feature coverage (`P2-W8-F02` test references behaviour) | **CLEAN** |
| 7 | Commit hygiene (Conventional Commits + Phase/Feature/Work-order trailers + size sanity) | **WARN** (subject 73 chars; count 10; one 425-LOC commit) |
| 8 | Doc + public API (every `pub` has rustdoc) | **CLEAN** |

---

Critic-report authored 2026-05-07. Verdict **CLEAN with warnings**: the
4 warnings (AC22 73-char subject, AC24 10-commit ceiling overshoot,
425-LOC single commit, AC25 hyphenated-backtick filter false-positives)
are all soft-target deviations explicitly documented in
`ucil-build/work-orders/0059-ready-for-review.md` with constraint
chains tracing back to the root-CLAUDE-forbidden `--amend after push`
+ `--force` rules and the WO-prescribed `forbidden_paths` carve-out.
Each follows precedents the verifier already accepted on prior WOs
(WO-0048 / WO-0056 / WO-0058). Forwarded to verifier without
remediation requirement.
