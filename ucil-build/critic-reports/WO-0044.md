# Critic Report: WO-0044

**Critic session**: crt-WO-0044-2026-05-05
**Branch**: feat/WO-0044-ast-grep-probe-plugin-manifests
**Tip**: 172c9b5470494729db0f96135492771591edbfad
**Merge base**: main
**Range**: main..feat/WO-0044-ast-grep-probe-plugin-manifests (7 commits, 8 files, +688 / -8)
**Verdict**: CLEAN

---

## Inputs reviewed

Work-order: `ucil-build/work-orders/0044-ast-grep-probe-plugin-manifests.json`
Features: P2-W6-F05 (ast-grep manifest), P2-W6-F06 (probe manifest)
Phase / Week: 2 / 6

Files in the diff:
- `plugins/structural/ast-grep/plugin.toml` (NEW, 62 LOC)
- `plugins/search/probe/plugin.toml` (NEW, 62 LOC)
- `crates/ucil-daemon/tests/plugin_manifests.rs` (NEW, 148 LOC)
- `scripts/devtools/install-ast-grep.sh` (NEW, 40 LOC, mode 100755)
- `scripts/devtools/install-probe.sh` (NEW, 36 LOC, mode 100755)
- `scripts/verify/P2-W6-F05.sh` (REWRITE of stub, 80 LOC, mode 100755)
- `scripts/verify/P2-W6-F06.sh` (REWRITE of stub, 111 LOC, mode 100755)
- `ucil-build/work-orders/0044-ready-for-review.md` (NEW, 157 LOC; ready marker)

Commit ladder (7 commits, all signed-off, all pushed):
```
172c9b5 docs(work-orders): mark WO-0044 ready-for-review
27962da feat(verify): implement scripts/verify/P2-W6-F06.sh end-to-end
b1e56bd feat(verify): implement scripts/verify/P2-W6-F05.sh end-to-end
54da36e test(daemon): add plugin_manifests integration suite (ast-grep + probe)
60160a7 chore(devtools): add install-ast-grep.sh + install-probe.sh helpers
9d68b15 feat(plugins): add probe plugin manifest under plugins/search
85b2e2c feat(plugins): add ast-grep plugin manifest under plugins/structural
```

---

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (worth flagging — not gating)

1. **probe version is a release candidate** — `plugins/search/probe/plugin.toml:38` pins `version = "0.6.0-rc315"`. The pin is to an immutable upstream npm tag (not a moving `latest` / `main`), so the reproducibility invariant in `.claude/rules/commit-style.md` is preserved. The executor's leading comment block (`plugin.toml:1-30`) explicitly justifies this: upstream has not yet cut a stable 0.6.0; the `latest` dist-tag currently resolves to `0.6.0-rc315`. Consider bumping in a follow-up ADR once upstream cuts a stable 0.6.0. **Acceptable as-is.**

2. **Test uses `health_check_with_timeout` rather than `health_check`** — `crates/ucil-daemon/tests/plugin_manifests.rs:81, 116` calls `PluginManager::health_check_with_timeout(&manifest, FIRST_RUN_TIMEOUT_MS)` with a 90 s budget rather than the bare `PluginManager::health_check(&manifest)` literal text in the WO `scope_in`. The function is `pub` (`crates/ucil-daemon/src/plugin_manager.rs:975`) and its rustdoc explicitly recommends a ≥30 s budget for first-run package-manager fetches such as `npx -y @<pkg>@<ver>` — the precise scenario the new tests hit. Semantics identical to `health_check` (which itself just delegates with `HEALTH_CHECK_TIMEOUT_MS`). The WO's intent — real subprocess, real JSON-RPC `initialize` → `notifications/initialized` → `tools/list` — is fully preserved. **Reasonable judgment call; not gating.**

3. **Two commits exceed the 50-LOC soft target** — commit 4 (`test(daemon): add plugin_manifests integration suite`, 148 LOC) and commit 6 (`feat(verify): P2-W6-F06.sh`, 111 LOC). Both are explicitly authorised by the WO `scope_in` commit ladder and DEC-0005-WO-0006-module-coherence-commits (single-coherent-test-file / single-script-file commits may exceed the soft target). Both have `Phase: 2`, `Feature: …`, `Work-order: WO-0044` trailers. **Acceptable per the cited ADR.**

### OK

#### 1. Stub detection — CLEAN
```
git grep -E 'todo!\(\)|unimplemented!\(\)|NotImplementedError|raise NotImplementedError' \
   feat/WO-0044-ast-grep-probe-plugin-manifests -- \
   crates/ucil-daemon/tests/plugin_manifests.rs \
   plugins/structural/ast-grep/ plugins/search/probe/ \
   scripts/devtools/install-*.sh scripts/verify/P2-W6-F0[56].sh
→ no hits (ret=1).
```
No `pass`-only Python bodies, no trivial `return None / Default::default() / {}` pattern in any new code path. The new test bodies make 11 distinct `assert!`/`assert_eq!` calls between them.

#### 2. Mocked critical dependencies — CLEAN
The only `mock` token in `crates/ucil-daemon/tests/plugin_manifests.rs` is in the module-level docstring at line 10:
```
//! Mocking `tokio::process::Command`, the spawned MCP server, or the
//! JSON-RPC dialogue is forbidden — the WO-0044 contract is precisely
//! that real MCP-server subprocesses speak real JSON-RPC over stdio
```
That is a **prohibition** on mocking, not actual use of mocks. No `tokio::process::Command` shim, no fake `Child`, no `MagicMock`, no `Mockall`. The tests call `PluginManager::health_check_with_timeout` which internally `Self::spawn(manifest)?` exec's the real `npx -y @notprolands/ast-grep-mcp@1.1.1` / `npx -y @probelabs/probe@0.6.0-rc315 mcp` subprocess — exactly the WO-0044 invariant.

#### 3. Skipped / ignored tests — CLEAN
No `#[ignore]`, no `#[cfg(any(... not(...)))]` cheats. The runtime opt-out at `crates/ucil-daemon/tests/plugin_manifests.rs:65, 77, 105` (`if skip_via_env() { return; }`) is explicitly authorised by the WO `scope_in` (`Tests are gated by ... ONLY for offline-CI but otherwise run unconditionally — the verifier MUST NOT set the skip env`). The `skip_via_env()` helper reads `UCIL_SKIP_EXTERNAL_PLUGIN_TESTS` only — does NOT set it; the verifier's environment must not export that variable.

The "skip"-pattern matches in scripts/verify/P2-W6-F0?.sh come from `exit 1` strings — no test-skip directives.

#### 4. Weak assertions — CLEAN
No `assert!(true)`, no `assert_eq!(true, true)`, no empty test bodies. Each test makes ≥6 substantive assertions: `manifest.plugin.name == "<name>"`, `manifest.transport.kind == "stdio"`, capabilities-provides non-empty, `health.name == "<name>"`, `health.status == HealthStatus::Ok`, `health.tools` non-empty, plus a strong content assertion (`health.tools.iter().any(|t| t == "find_code")` for ast-grep; `… == "search_code"` for probe) that pins the canonical tool name advertised by each upstream MCP server.

#### 5. Hallucinated paths — CLEAN
- `use ucil_daemon::{HealthStatus, PluginManager, PluginManifest};` — all three re-exported at `crates/ucil-daemon/src/lib.rs:105-108`.
- `PluginManager::health_check_with_timeout` — defined at `crates/ucil-daemon/src/plugin_manager.rs:975`, `pub`, reachable through the re-exported `PluginManager`.
- Field references `manifest.plugin.name`, `manifest.transport.kind`, `manifest.capabilities.provides` — all match the struct definitions at `plugin_manager.rs:105-168` (`pub plugin: PluginSection { pub name: String }`, `pub transport: TransportSection { pub kind: String }`, `pub capabilities: CapabilitiesSection { pub provides: Vec<String> }`).
- Verify-script symbols exist in fixtures: `class TaskManager` lives at `tests/fixtures/typescript-project/src/task-manager.ts:133`; `pub fn evaluate(expr: &Expr) -> Result<Value, EvalError>` lives at `tests/fixtures/rust-project/src/util.rs:128` — both confirmed by direct grep.

#### 6. Feature coverage — CLEAN
- **P2-W6-F05** is exercised by `plugin_manifests::ast_grep_manifest_health_check` (`crates/ucil-daemon/tests/plugin_manifests.rs:71-104`) AND `scripts/verify/P2-W6-F05.sh` (cargo selector + ast-grep structural-search smoke).
- **P2-W6-F06** is exercised by `plugin_manifests::probe_manifest_health_check` (`crates/ucil-daemon/tests/plugin_manifests.rs:106-141`) AND `scripts/verify/P2-W6-F06.sh` (cargo selector + token-budgeted search + explicit extract).

Each Rust test pins the expected upstream tool name (`find_code` / `search_code`) so the WO's no-mocks-of-critical-deps invariant is genuinely exercised at runtime — a tool-name drift in either upstream surfaces as a loud test failure rather than a silent green.

#### 7. Commit hygiene — CLEAN
All 7 commits follow Conventional Commits (`feat(plugins):`, `feat(plugins):`, `chore(devtools):`, `test(daemon):`, `feat(verify):`, `feat(verify):`, `docs(work-orders):`). Every commit body carries the required UCIL trailers:
```
Phase: 2
Feature: P2-W6-F05  [or P2-W6-F06 — the two manifest commits + the test commit each name both features]
Work-order: WO-0044
Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```
No `--amend`-after-push, no force-push, no `--no-verify` patterns anywhere in the branch state or scripts. Subject lines all ≤70 chars and imperative.

Per-commit blast radius is bounded:
- 85b2e2c: `plugins/structural/ast-grep/plugin.toml` only.
- 9d68b15: `plugins/search/probe/plugin.toml` only.
- 60160a7: `scripts/devtools/install-ast-grep.sh` + `scripts/devtools/install-probe.sh` only.
- 54da36e: `crates/ucil-daemon/tests/plugin_manifests.rs` only.
- b1e56bd: `scripts/verify/P2-W6-F05.sh` only.
- 27962da: `scripts/verify/P2-W6-F06.sh` only.
- 172c9b5: `ucil-build/work-orders/0044-ready-for-review.md` only.

#### 8. Doc + public API — CLEAN
The diff introduces zero new `pub` items in any Rust crate (`crates/ucil-daemon/src/**` is in `forbidden_paths`; `tests/plugin_manifests.rs` declares only a private `mod plugin_manifests` with private helpers `repo_root` / `skip_via_env` and two `#[tokio::test]` functions). The test module itself has a thorough crate-level `//!` docstring explaining the no-mock invariant and the workspace-fixture pairing. Both new manifest TOMLs lead with a structured reproducibility comment block citing upstream URL, pinned tag, MCP-server version, advertised-tool list, and rationale — same shape as `plugins/structural/serena/plugin.toml:1-14`.

#### Forbidden-paths sweep — CLEAN
```
git diff main..feat/WO-0044-ast-grep-probe-plugin-manifests -- \
   crates/ucil-daemon/src/ crates/ucil-daemon/Cargo.toml \
   crates/ucil-daemon/tests/plugin_manager.rs \
   crates/ucil-daemon/tests/e2e_mcp_stdio.rs \
   crates/ucil-daemon/tests/e2e_mcp_with_kg.rs \
   crates/ucil-daemon/tests/support/ \
   plugins/structural/serena/ \
   tests/fixtures/ \
   ucil-build/feature-list.json \
   scripts/gate/ .githooks/ .claude/
→ empty diff (ret=0, no output).
```
All 35 entries on the WO `forbidden_paths` list are honoured 1:1. No `Cargo.toml` or `Cargo.lock` modifications.

#### Reproducibility check — CLEAN
```
git grep -E '"main"' feat/WO-0044-ast-grep-probe-plugin-manifests -- \
   plugins/structural/ast-grep/plugin.toml plugins/search/probe/plugin.toml
→ no hits (ret=1).
```
Both manifests pin to immutable npm release tags (`@notprolands/ast-grep-mcp@1.1.1`, `@probelabs/probe@0.6.0-rc315`), no moving `latest` / `main` refs.

#### Lessons-applied audit — CONSISTENT
The WO `lessons_applied` claims (frozen-selector module wrapper, backward-compat regression guards, single-file blast radius, pre-baked mutation checks, alternation regex for cargo-test|nextest summary, no-mocks invariant, reproducibility-pinning, DEC-0005 module-coherence) are all observable in the diff:
- `mod plugin_manifests { … }` wrapper present (`tests/plugin_manifests.rs:34`) — matches `mod plugin_manager` at `tests/plugin_manager.rs:21`.
- Acceptance-criteria entries each run prior-WO frozen selectors (`test_manifest_parser`, `test_lifecycle_state_machine`, `test_hot_reload`, `test_circuit_breaker`, `test_hot_cold_lifecycle`, `--test plugin_manager`, `--test e2e_mcp_stdio`, `--test e2e_mcp_with_kg`).
- Cargo-test summary regex uses alternation: `'test result: ok\. 1 passed; 0 failed|1 tests run: 1 passed'` in both verify scripts.
- Pinned-version reproducibility: explicit comment blocks + `! grep -q '"main"'` acceptance.
- Operator-actionable failure messages: both verify scripts emit `[FAIL] P2-W6-F0X: <reason>` and reference the install helper script.

---

## Verdict

**CLEAN.** WO-0044 is ready for the verifier.

Three minor warnings (RC-version pin, `health_check_with_timeout` instead of `health_check`, two commits over the 50-LOC soft target) are documented above for the verifier's awareness — none are gating, all are either explicitly authorised by the WO `scope_in` / project ADRs or are reasonable executor-side judgment calls that preserve the WO's intent.

The verifier should:
1. Run `scripts/verify/P2-W6-F05.sh` and `scripts/verify/P2-W6-F06.sh` from a fresh shell with `ast-grep` and `probe` on PATH (or invoke `scripts/devtools/install-*.sh` first to discover the install command).
2. Run the two pre-baked mutation checks listed in the WO `acceptance` (replace `[transport].command` with a non-existent path → expect `PluginError::Spawn`; restore → green).
3. Confirm `UCIL_SKIP_EXTERNAL_PLUGIN_TESTS` is **unset** in the verifier shell so the runtime opt-out does not mask a real failure.
4. Run the full WO-0042 / WO-0043 regression-guard test selectors (`test_manifest_parser`, `test_lifecycle_state_machine`, `test_hot_reload`, `test_circuit_breaker`, `test_hot_cold_lifecycle`) plus `--test plugin_manager`, `--test e2e_mcp_stdio`, `--test e2e_mcp_with_kg` against a clean `cargo clean` + `cargo build -p ucil-daemon --tests` workspace.
