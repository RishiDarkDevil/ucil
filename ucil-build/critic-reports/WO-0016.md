---
work_order: WO-0016
slug: diagnostics-quality-pipeline
feature: P1-W5-F05
branch: feat/WO-0016-diagnostics-quality-pipeline
head_commit: bcffe30f76857944be23ccd662b672f539c68f40
merge_base: dc194d2785709dd8d00c18d74f97c6acfe385dca
critic_session: crt-WO-0016
verdict: CLEAN
---

# Critic Report: WO-0016 — diagnostics-quality-pipeline

**Critic session**: `crt-WO-0016`
**Branch**: `feat/WO-0016-diagnostics-quality-pipeline`
**Merge base**: `dc194d2` (main)
**HEAD**: `bcffe30` (`chore(wo): WO-0016 ready-for-review marker`)
**Diff scope**: 5 commits, 5 files changed, +1023 lines
**Verdict**: **CLEAN**

## Files touched

| File | Status | Purpose |
|------|--------|---------|
| `Cargo.lock` | modified (+2) | `rusqlite` + `tempfile` added to `ucil-lsp-diagnostics` deps |
| `crates/ucil-lsp-diagnostics/Cargo.toml` | modified (+16) | `rusqlite` workspace dep + `tempfile` dev-dep |
| `crates/ucil-lsp-diagnostics/src/lib.rs` | modified (+5) | `pub mod quality_pipeline;` + re-exports |
| `crates/ucil-lsp-diagnostics/src/quality_pipeline.rs` | **new** (+840) | Module + 5 module-root tests |
| `ucil-build/work-orders/0016-ready-for-review.md` | **new** (+160) | Executor's local-verdict marker |

Forbidden-path acceptance checks verified:
- `git diff dc194d2..HEAD -- crates/ucil-lsp-diagnostics/src/diagnostics.rs` → 0 lines ✓
- `git diff dc194d2..HEAD -- crates/ucil-lsp-diagnostics/src/bridge.rs` → 0 lines ✓
- `git diff dc194d2..HEAD -- crates/ucil-lsp-diagnostics/src/types.rs` → 0 lines ✓
- No diff under `tests/fixtures/**`, `scripts/gate/**`, `ucil-build/feature-list.json`, `ucil-master-plan-v2.1-final.md`, `crates/ucil-core/src/knowledge_graph.rs`, `crates/ucil-core/src/schema_migration.rs`, or `ucil-build/decisions/DEC-0008-*` ✓

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Misleading commit-body claim** — `crates/ucil-lsp-diagnostics/src/quality_pipeline.rs` (`c97cb5c`).
   The body of commit `c97cb5c` ("`feat(lsp-diagnostics): add quality_pipeline module for G7 feed`")
   ends with *"Tests land in a follow-on commit to keep the diff review-friendly."*
   The commit actually lands the module **and** all 5 `#[tokio::test]` / `#[test]`
   items (913-line file). The follow-on `d6ab8a6` is a test-fixture-helper
   *refactor*, not a tests-landing commit. Not a behavioural issue, but the
   commit body will mislead a future archaeologist. Consider a clarifying
   amendment note in `0016-ready-for-review.md` or in the verifier's log (the
   commit itself is published and `--amend` after push is forbidden).

2. **Oversized commits (DEC-0005 precedent applies)** — `c97cb5c` (+928 lines
   across 4 files) and `d6ab8a6` (+198 / −271 = 469 churn lines, single
   file). Both exceed the critic-protocol 200-line soft ceiling in
   `.claude/rules/commit-style.md`.
   - `c97cb5c` is a textbook *module-introduction commit* — it creates
     `quality_pipeline.rs` with the module + its tests together, mirroring
     the WO-0006 commits explicitly accepted by
     `DEC-0005-WO-0006-module-coherence-commits.md`. No action required;
     flagged so the verifier can corroborate under the DEC-0005 exemption.
   - `d6ab8a6` is a test-helper extraction refactor (introduce `test_fixtures`
     submod, rewrite the five test bodies to call the helpers). Strictly
     speaking this is a *refactor*, not a module introduction, so DEC-0005
     does not auto-cover it. The change is internally cohesive and every
     diffed line stays inside `quality_pipeline.rs`, but future work-orders
     of this shape should prefer two commits (add helpers / adopt helpers).
     Not a blocker for this WO.

3. **Reality-check oracle marked manual (WO-0014 / WO-0015 precedent)** —
   `ucil-build/work-orders/0016-ready-for-review.md`.
   The ready-for-review marker documents that
   `scripts/reality-check.sh P1-W5-F05` cannot confirm mutation because a
   brand-new module's tests vanish with the stashed code (zero-tests heuristic
   false-positive). Two prior WOs (WO-0014 / WO-0015) hit the same limitation
   and their verifiers accepted a manual two-step. The verifier should
   independently re-run the manual mutation (stash → 0 tests; pop → 5/5 pass)
   rather than taking the executor's note at face value — the critic cannot
   flip `passes`, so this is an inherited concern to forward to the verifier.

### OK

- **Stub detection** — ripgrep for `todo!()`, `unimplemented!()`,
  `raise NotImplementedError`, `pass`-only bodies, trivial default returns
  across the diff: **0 hits**. The only `unwrap_or_else(|e| panic!(...))`
  calls are inside `#[cfg(test)] mod test_fixtures` helpers (allowed by
  `rust-style.md` — `.expect()`/`.unwrap()` only forbidden outside
  `#[cfg(test)]`).
- **Mocked critical collaborators** — `ScriptedFakeSerenaClient`
  (`quality_pipeline.rs:420`) implements UCIL's own `SerenaClient` trait
  (defined in `crates/ucil-lsp-diagnostics/src/diagnostics.rs` at
  `pub trait SerenaClient: Send + Sync`). Per
  `DEC-0008-lsp-bridge-via-serena-mcp-channel.md` §Decision.4, the
  `SerenaClient` trait is the *dependency-inversion seam* the bridge
  exposes, explicitly so tests can drive the pipeline without a Serena MCP
  wire-protocol mock. The `KnowledgeGraph` is opened on a real
  `tempfile::TempDir` (`test_fixtures::open_fresh_kg`,
  `quality_pipeline.rs:517`) — real `KnowledgeGraph::open`, real §12.1
  DDL, real WAL pragma, real `BEGIN IMMEDIATE`. No `rusqlite` /
  `lancedb` / Docker mocks anywhere in the diff.
- **Skipped / ignored tests** — ripgrep for `#[ignore]`, `.skip(`, `xfail`,
  `xit(`, `pytest.mark.skip`: **0 hits** in diff.
- **Weak assertions** — ripgrep for `assert!(true)`, `assert_eq!(true, true)`,
  `expect(true).toBe(true)`, `assert True`: **0 hits**. The five tests each
  assert substantive column-level readbacks (`test_diagnostics_to_quality_issues`
  asserts all 8 `quality_issues` columns per row across 3 rows + a
  `COUNT(*)` + a `first_seen IS NULL OR first_seen = ''` guard;
  `test_source_tool_falls_back_to_language_default` covers `source=None` on
  both Python and Rust URIs plus `source=Some("ruff")` preservation;
  mapping tests cover every LSP severity variant exhaustively).
- **Hallucinated imports** — every `use` in `quality_pipeline.rs` resolves:
  - `lsp_types::{Diagnostic, DiagnosticSeverity, NumberOrString, Url}` —
    workspace dep, already used by `diagnostics.rs`.
  - `thiserror::Error` — workspace dep.
  - `ucil_core::{KnowledgeGraph, KnowledgeGraphError}` — both re-exported
    from `crates/ucil-core/src/lib.rs:pub use knowledge_graph::{KnowledgeGraph, KnowledgeGraphError};`.
  - `crate::diagnostics::{DiagnosticsClient, DiagnosticsClientError, SerenaClient}`
    — all three `pub` in `crates/ucil-lsp-diagnostics/src/diagnostics.rs`.
  - `crate::types::Language` — `pub enum Language` with all seven variants
    (`Python`, `Rust`, `TypeScript`, `Go`, `Java`, `C`, `Cpp`) used by
    `language_default_server` (`quality_pipeline.rs:201-210`). The `match`
    is exhaustive.
  - `KnowledgeGraph::execute_in_transaction` and `KnowledgeGraph::conn`
    both exist at `crates/ucil-core/src/knowledge_graph.rs:430` and `:~conn()`
    respectively.
  - `quality_issues` DDL has `file_path TEXT NOT NULL`, `line_start INTEGER`,
    `line_end INTEGER`, `category TEXT NOT NULL`, `severity TEXT NOT NULL`,
    `message TEXT NOT NULL`, `rule_id TEXT`, `source_tool TEXT`,
    `first_seen TEXT NOT NULL DEFAULT (datetime('now'))`
    (`crates/ucil-core/src/knowledge_graph.rs:174-188`). The 8-column
    INSERT at `quality_pipeline.rs:354-378` matches this schema; the
    `first_seen IS NULL` COUNT assertion at `:699` confirms the default
    fires.
- **Feature coverage** — WO-0016 lists exactly one feature (`P1-W5-F05`).
  `test_diagnostics_to_quality_issues` (the frozen nextest selector for
  `P1-W5-F05`) lives at module root (`quality_pipeline.rs:601`), exercises
  `persist_diagnostics` end-to-end with three canned diagnostics across two
  files, and asserts every §12.1 column's projection. Module-root placement
  respects `DEC-0005` so the selector matches without relying on `mod tests {…}`
  substring match. **Feature coverage: satisfied.**
- **Commit hygiene** — every commit in `dc194d2..bcffe30` uses Conventional
  Commits with a proper scope, and every commit body contains the required
  `Phase: 1` / `Feature: P1-W5-F05` / `Work-order: WO-0016` trailers.
  Co-author line present on all five. No `--amend` after push. No
  force-push. No `--no-verify`. Five commits in order:
  `ec2a67b` → `c97cb5c` → `d6ab8a6` → `02ec559` → `bcffe30`.
  `estimated_commits: 5` in the work-order matches exactly.
- **Public-API rustdoc** — every new `pub` item has a doc comment:
  - `QualityPipelineError` enum + each variant (`:102-125`)
  - `severity_to_quality` (`:143`, with `#[must_use]`)
  - `category_from_severity` (`:168`, with `#[must_use]`)
  - `language_default_server` (`:201`, with `#[must_use]`)
  - `persist_diagnostics` (`:322`, with `# Re-ingest behaviour`,
    `# Tracing`, `# Errors` sections)
  - Module-level docstring (`:1-73`) covers the LSP-4→quality-5 collapse,
    §12.1 reference, re-ingest policy, timeout discipline, and tracing
    convention.
  The `02ec559` commit proactively fixed the `redundant_explicit_links`
  rustdoc-lint errors so `cargo doc --no-deps` is clean under
  `#![deny(warnings)]` + `#![deny(rustdoc::broken_intra_doc_links)]`.
- **Style invariants**:
  - `thiserror` + `#[non_exhaustive]` on `QualityPipelineError` (rust-style.md
    Errors §) ✓
  - Single `tokio::time::timeout` layer — the `.await` inside
    `persist_diagnostics` goes through `DiagnosticsClient::diagnostics`
    which already wraps in `LSP_REQUEST_TIMEOUT_MS`; the module deliberately
    adds no second layer (`quality_pipeline.rs:55-65` rustdoc calls this
    out; WO `lessons_applied[3]` required it) ✓
  - `tracing` span named `ucil.lsp.persist_diagnostics` per master-plan §15.2
    (`quality_pipeline.rs:328-332`) ✓
  - `#![warn(clippy::all, clippy::pedantic, clippy::nursery)]` — crate root
    already has this; the module-scope `#![allow(clippy::module_name_repetitions)]`
    mirrors the `diagnostics.rs` escape hatch and is justified inline
    (`:75-82`) ✓
  - No `ucil-daemon` dependency in `crates/ucil-lsp-diagnostics/Cargo.toml`
    (`! grep -q 'ucil-daemon' Cargo.toml` satisfied; DEC-0008 cycle rule
    preserved) ✓
  - All writes go through `KnowledgeGraph::execute_in_transaction`
    (exactly one call per invocation at `:353`) ✓

## Verifier handoff notes

- The reality-check oracle (WO acceptance criterion 13,
  `bash scripts/reality-check.sh P1-W5-F05`) cannot verify mutation for
  a brand-new module (known harness limitation, documented by WO-0014 /
  WO-0015 precedent). Verifier should run the two-step manually:
  1. `git stash -- crates/ucil-lsp-diagnostics/src/quality_pipeline.rs
     crates/ucil-lsp-diagnostics/src/lib.rs
     crates/ucil-lsp-diagnostics/Cargo.toml` → confirm
     `cargo nextest run -p ucil-lsp-diagnostics test_diagnostics_to_quality_issues`
     reports zero matching tests.
  2. `git stash pop` → confirm the selector passes 1/1 and the
     `quality_pipeline::` prefix passes 5/5.
- Acceptance criterion 3 depends on the nextest `quality_pipeline::` prefix
  selector matching ≥ 5 tests. The module has exactly five
  `test_*` functions at module root — the selector has no slack.
- Criterion 10 (`git diff == 0` for `diagnostics.rs`, `bridge.rs`,
  `types.rs`) is already verified by the critic; verifier should
  re-confirm against origin/main at gate time.
- The `c97cb5c` commit body's "tests land in a follow-on commit" claim
  is inaccurate (see Warning #1); verifier should not let this block
  progress — it is a commit-message nit only.

## Final verdict

**CLEAN** — no blockers. Three warnings inherited to verifier / gate (all
precedented by DEC-0005 or prior WO manual-mutation-check procedure).
Safe to proceed to verifier session.
