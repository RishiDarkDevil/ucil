# Critic Report: WO-0009 — salsa + plugin-manager skeleton

**Critic session**: crt-wo0009-2026-04-17
**Branch**: `feat/WO-0009-salsa-and-plugin-manager`
**Head commit**: `18f97e3d63fb9a6437614d7239a38c444e3f4d7f`
**Merge base**: `4be4a8a13f17aee60bbca41aac792c5ca378cfc4` (main)
**Features in scope**: `P1-W3-F04` (ucil-core Salsa incremental skeleton),
`P1-W3-F05` (ucil-daemon plugin-manager skeleton)
**Verdict**: **CLEAN** (with 5 non-blocking warnings)

## Scope reviewed

```
git diff --name-status main..feat/WO-0009-salsa-and-plugin-manager
M   Cargo.lock
M   crates/ucil-core/Cargo.toml
A   crates/ucil-core/src/incremental.rs
M   crates/ucil-core/src/lib.rs
M   crates/ucil-core/tests/smoke.rs
M   crates/ucil-daemon/Cargo.toml
M   crates/ucil-daemon/src/lib.rs
A   crates/ucil-daemon/src/plugin_manager.rs
A   crates/ucil-daemon/tests/plugin_manager.rs
A   crates/ucil-daemon/tests/support/mock_mcp_plugin.rs
A   ucil-build/work-orders/0009-ready-for-review.md
```

Total: 4 commits, 1 441 insertions / 2 deletions.

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate, but not verifier-blocking)

1. **Conventional-Commits scope missing** — `crates/ucil-core/Cargo.toml`
   build commit `45d3d41` uses `build:` (no scope). Per
   `.claude/rules/commit-style.md`, UCIL commits follow
   `<type>(<scope>): <summary>`; precedent on main (`git log main --format=%s | grep ^build`)
   uses `build(cargo): track .cargo/config.toml`. Soft violation; not
   blocking but worth correcting on the next similar commit (cannot be
   amended post-push per anti-laziness contract).

2. **Large feat commit bundles src + integration-test file** —
   `46abfaf feat(daemon): plugin-manager skeleton` totals 754 insertions
   across two new files:
   - `crates/ucil-daemon/src/plugin_manager.rs` (620 LOC, contains
     `#[cfg(test)] mod tests` per DEC-0005) — coherent single-file
     type+impl+unit-tests, allowed exception.
   - `crates/ucil-daemon/tests/plugin_manager.rs` (128 LOC integration
     tests) — could have been a separate `test(daemon): …` commit and
     would have compiled stand-alone once the module existed.
   DEC-0001/DEC-0005 permits the module-introduction commit to exceed
   the 50-line soft target; bundling a second file is a soft deviation.
   Flag, not block.

3. **Timeout test depends on `sleep` being on `PATH`** —
   `crates/ucil-daemon/src/plugin_manager.rs:573-609` test
   `health_check_timeout_fires_when_command_hangs` invokes
   `Command::new("sleep").args(["30"])`. On CI images that strip
   `coreutils` or restrict `/usr/bin`, this unit test will fail.
   Executor's ready-for-review marker (line 73) already calls this out.
   Worth noting so the verifier reproduces on a matching image.

4. **Ready-for-review marker cites a wrong test name** —
   `ucil-build/work-orders/0009-ready-for-review.md:54-56` claims the
   timeout path is exercised by
   `health_check_times_out_when_plugin_does_not_respond`; the actual
   test name at `crates/ucil-daemon/src/plugin_manager.rs:573` is
   `health_check_timeout_fires_when_command_hangs`. The behaviour
   described is accurate; the name string is not.

5. **AC9 self-declared as "known harness limitation"** —
   `ucil-build/work-orders/0009-ready-for-review.md:61-74` flags
   `scripts/reality-check.sh WO-0009` as failing in a pattern
   consistent with already-passing features `P1-W2-F01` and
   `P1-W2-F05`. This is visibility-only for the critic — the verifier
   decides whether the precedent is dispositive. Recorded here so the
   verifier does not miss the explicit acknowledgement.

### OK

- **Stub detection** — no `todo!()`, `unimplemented!()`,
  `NotImplementedError`, `raise NotImplementedError`, pass-only bodies,
  or trivial bare-literal feature returns in any of the four new/edited
  source files.
  - Scans:
    `git show <ref>:crates/ucil-core/src/incremental.rs`,
    `…/plugin_manager.rs`,
    `…/tests/plugin_manager.rs`,
    `…/tests/support/mock_mcp_plugin.rs` — zero hits for
    `todo!\(|unimplemented!\(|NotImplementedError|raise NotImplementedError`.

- **Mocked critical dependencies** — zero hits for
  `rg -i '(mock|fake|stub|MagicMock|Mockall)' …| grep -Ei '(serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker)'`
  across the diff. The only "mock" in the tree is the **real in-tree
  MCP plugin binary** (`crates/ucil-daemon/tests/support/mock_mcp_plugin.rs`),
  which is a legitimate integration fixture invoked via
  `env!("CARGO_BIN_EXE_mock-mcp-plugin")` — this is a real OS
  subprocess, not a test double of `tokio::process::Command`.

- **Skipped / ignored tests** — zero hits for
  `#\[ignore\]|\.skip\(|xfail|xit\(` across `incremental.rs`,
  `plugin_manager.rs` (both src and integration), and
  `mock_mcp_plugin.rs`.

- **Weak assertions** — zero hits for
  `assert!\(true\)|assert_eq!\(true,\s*true\)|assert!\(1\)|assert_eq!\(1,\s*1\)`.
  Tests assert concrete values (`3`, `4`, `6`, `8`), concrete
  enum-variant matches (`HealthStatus::Ok`,
  `PluginError::HealthCheckTimeout`, `PluginError::Spawn`,
  `PluginError::UnsupportedTransport`), and log-content presence
  (`drain_log().iter().any(|e| e.contains("symbol_count"))`).

- **No `.unwrap()` / `.expect()` outside `#[cfg(test)]`** — verified by
  slicing each file above its `mod tests` line:
  - `incremental.rs` `mod tests` begins at line 141; lines 1–140 grep
    clean for `.unwrap(\)|.expect\(`.
  - `plugin_manager.rs` `mod tests` begins at line 428; lines 1–427
    grep clean for `.unwrap(\)|.expect\(`.
  - `mock_mcp_plugin.rs` is a `[[bin]]` test-support binary; no
    `.unwrap()`, no `.expect()`.

- **Hallucinated imports** — every `use`/`extern` target exists:
  - `salsa::Storage`, `salsa::Setter`, `salsa::Database`,
    `salsa::EventKind`, `salsa::db`, `salsa::input`, `salsa::tracked`
    — provided by `salsa = "0.22"` added to
    `crates/ucil-core/Cargo.toml` in `45d3d41`.
  - `serde::Deserialize`, `thiserror::Error`, `tokio::{io::…,
    process::{Child, Command}, time::timeout}`, `serde_json::{json,
    Value}` — all workspace deps in root `Cargo.toml`
    (`[workspace.dependencies]`: `serde`, `thiserror`, `tokio`,
    `serde_json`, `toml`, `tempfile`).
  - `tempfile` — `[dev-dependencies] tempfile.workspace = true` in
    both `ucil-core` and `ucil-daemon` `Cargo.toml`.
  - `ucil_daemon::{HealthStatus, PluginError, PluginManager,
    PluginManifest}` — re-exported from
    `crates/ucil-daemon/src/lib.rs:11-14`.

- **Feature coverage** — every WO feature has ≥1 new/modified test
  referencing its behaviour:
  - **P1-W3-F04** (salsa incremental DAG) — 5 dedicated tests in
    `crates/ucil-core/src/incremental.rs:155-280`:
    `memoise_skips_second_call_with_identical_input` (memoisation),
    `invalidate_on_mtime_change_forces_recompute` (invalidation),
    `early_cutoff_skips_downstream_recompute` (early cutoff),
    `contents_change_propagates_through_downstream` (propagation),
    `concrete_database_default_constructs` (production DB smoke).
    Early cutoff is observed via
    `salsa::EventKind::WillExecute` logging — not a proxy. ✓
  - **P1-W3-F05** (plugin-manager skeleton) — 10 unit tests in
    `crates/ucil-daemon/src/plugin_manager.rs:440-608`
    (`manifest_parse_reads_plugin_and_transport_sections`,
    `manifest_parse_rejects_missing_required_field`,
    `manifest_parse_reports_io_error_for_missing_file`,
    `discover_lists_only_toml_files_sorted_by_name`,
    `discover_errors_when_dir_missing`,
    `spawn_rejects_non_stdio_transport`,
    `parse_tools_list_response_happy_path`,
    `parse_tools_list_response_rejects_jsonrpc_error`,
    `parse_tools_list_response_rejects_empty_frame`,
    `health_check_timeout_fires_when_command_hangs`) plus 3 integration
    tests in `crates/ucil-daemon/tests/plugin_manager.rs:53-128`
    (`spawn_and_health_check_returns_mock_tools`,
    `discover_finds_mock_manifest_and_health_check_succeeds`,
    `spawn_fails_cleanly_when_command_is_missing`). ✓

- **Commit trailer hygiene** — all four commits carry
  `Phase: 1`, `Feature: P1-W3-F04`/`P1-W3-F05`, `Work-order: WO-0009`
  trailers. `Co-Authored-By` present on every commit.

- **Commit size sanity** — Cargo.lock (173 auto-generated lines) and
  single-new-module introductions (`incremental.rs` + tests,
  `plugin_manager.rs` + tests) are covered by DEC-0001/DEC-0005
  exceptions. Integration-test-file bundling noted as Warning 2.

- **Forbidden paths untouched** —
  `git diff --name-only main..feat/WO-0009-salsa-and-plugin-manager`
  shows **no** edits to
  `ucil-build/feature-list.json`, `ucil-build/feature-list.schema.json`,
  `ucil-master-plan-v2.1-final.md`, `tests/fixtures/**`,
  `scripts/gate/**`, `scripts/flip-feature.sh`,
  `ucil-build/decisions/**`, or `ucil-build/work-orders/000[1-8]-*`.

- **`tokio::time::timeout` wraps every IO `.await` in `health_check`**
  — verified at
  `crates/ucil-daemon/src/plugin_manager.rs:312` (`timeout(HEALTH_CHECK_TIMEOUT,
  async { Self::run_tools_list(&mut child).await }).await`) with the
  named const `HEALTH_CHECK_TIMEOUT_MS = 5_000` at line 67.
  Matches `.claude/rules/rust-style.md` (async IO timeout invariant)
  and the WO-0009 acceptance criterion.

- **Public API docs** — every `pub` item has rustdoc
  (54 `///` lines in `incremental.rs`, 125 in `plugin_manager.rs`).
  Spot-check confirmed on every `pub (fn|struct|trait|enum|const)` hit
  reported by grep. The `#![deny(rustdoc::broken_intra_doc_links)]`
  crate-root lint remains active in both crates' `src/lib.rs`.

- **Real subprocess in integration tests** — the integration tests at
  `crates/ucil-daemon/tests/plugin_manager.rs:53,85,110` invoke the
  compiled `mock-mcp-plugin` binary via
  `env!("CARGO_BIN_EXE_mock-mcp-plugin")`. The binary is declared as
  `[[bin]]` in `crates/ucil-daemon/Cargo.toml:15-17`. No mocking of
  `tokio::process::Command`, stdin, or stdout.

## Summary

The diff satisfies every anti-laziness check in `.claude/agents/critic.md`:
no stubs, no mocked critical collaborators, no skipped tests, no weak
assertions, no hallucinated imports, full feature coverage, clean
commit-trailer hygiene, forbidden paths untouched, and all public items
documented. The five warnings are cosmetic / procedural (one missing
scope, one fat commit, one PATH-dependent test, one name mismatch in
the marker, one self-declared AC9 limitation the verifier needs to
adjudicate) and none of them is grounds to block the verifier handoff.

**Verdict: CLEAN — proceed to verifier.**
