---
work_order: WO-0026
slug: file-watcher-notify-debounce
feature: P1-W3-F02
branch: feat/WO-0026-file-watcher-notify-debounce
head_commit: 9308d33e1bbce6889d2f4e10dcbd1f39e4e323cc
merge_base: main
critic_session: crt-WO-0026
verdict: CLEAN
---

# Critic Report: WO-0026 — file-watcher-notify-debounce

**Critic session**: `crt-WO-0026`
**Branch**: `feat/WO-0026-file-watcher-notify-debounce`
**Merge base**: `main`
**HEAD**: `9308d33` (`chore(work-order): WO-0026 ready for review`)
**Diff scope**: 6 commits, 6 files changed, +1072/-12 lines
**Verdict**: **CLEAN**

## Files touched

| File | Status | Lines added |
|------|--------|-------------|
| `Cargo.lock` | modified | +304 / -12 |
| `Cargo.toml` | modified | +10 |
| `crates/ucil-daemon/Cargo.toml` | modified | +2 |
| `crates/ucil-daemon/src/lib.rs` | modified | +11 |
| `crates/ucil-daemon/src/watcher.rs` | **new** | +638 |
| `ucil-build/work-orders/0026-ready-for-review.md` | **new** | +107 |

Forbidden-path acceptance checks verified (0 lines of diff against `main`):

- `crates/ucil-core/**`, `crates/ucil-treesitter/**`, `crates/ucil-lsp-diagnostics/**`, `crates/ucil-embeddings/**`, `crates/ucil-agents/**`, `crates/ucil-cli/**`, `adapters/**`, `ml/**` ✓
- `ucil-build/feature-list.json`, `ucil-master-plan-v2.1-final.md`, `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`, `.claude/agents/**`, `.claude/settings.json` ✓

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Deviation from `scope_in` — `FileIdMap` cache used instead of `NoCache`** — `crates/ucil-daemon/src/watcher.rs:70,184`.
   The WO's `scope_in` bullet at `work-orders/0026-file-watcher-notify-debounce.json:23` specifies the field type as `notify_debouncer_full::Debouncer<notify::RecommendedWatcher, notify_debouncer_full::NoCache>`, but the executor chose `Debouncer<notify::RecommendedWatcher, FileIdMap>` because `new_debouncer` (the helper the same bullet mandates on line 24) returns `Debouncer<_, FileIdMap>`. The ready-for-review marker at `ucil-build/work-orders/0026-ready-for-review.md:88-92` explicitly acknowledges this conflict and argues the helper pick is idiomatic and the `FileIdMap` cache is harmless for F02 (no rename-tracking). Verifier should corroborate that the `FileIdMap` cache does not violate any F02 acceptance semantics.

2. **Commit-plan deviation — 5 code commits delivered vs. `commit_plan.estimated_commits: 6`.** — `ucil-build/work-orders/0026-file-watcher-notify-debounce.json:156-198`.
   The WO called for six commits split by `[deps → types → new+notify-tests → hook-tests → map_notify_kind+test → shutdown-test]`; the executor delivered four feat commits plus the deps commit (+ the review marker). The module-introduction commit `f348b27` bundles `types + FileWatcher::new + notify_hook_event + map_notify_kind + test_event_kind_mapping_covers_create_modify_remove` into a single 396-line commit rather than splitting module bootstrap from the per-helper tests. DEC-0005 covers module-coherence commits (WO-0011 / WO-0016 / WO-0023 precedent), and the ready-for-review marker at `ucil-build/work-orders/0026-ready-for-review.md:74-82` justifies the bundling on dead-code grounds. Not a blocker, flagged for verifier awareness.

3. **Oversized module-introduction commit (DEC-0005 exemption applies)** — `f348b27` — 396 lines (`watcher.rs +385`, `lib.rs +11`).
   Exceeds the 50-line soft target in `.claude/rules/commit-style.md` but precedented under `DEC-0005-WO-0006-module-coherence-commits.md` (WO-0023 `be06fb9` was 666 lines). No action required.

4. **Reality-check oracle marked manual (brand-new-module precedent)** — `ucil-build/work-orders/0026-ready-for-review.md:44-66`.
   The marker documents that `scripts/reality-check.sh P1-W3-F02` trips the brand-new-module false-positive (escalation `20260415-1630-reality-check-per-file-rollback.md`, precedent WO-0014 / WO-0015 / WO-0016 / WO-0023). The manual two-step mutation probe was performed by the executor: stashed → `watcher::` selector returned 0 tests; restored → 6 tests pass. Critic cannot flip `passes`; forwarded to verifier — who should independently re-run the two-step rather than take the executor's note at face value. WO `acceptance[10]` and `lessons_applied[11]` anticipate this fallback.

### OK

- **Stub detection** — ripgrep for `todo!()`, `unimplemented!()`, `#[ignore]`, `NotImplementedError`, `raise NotImplementedError`, bare `pass` bodies, trivial default returns (`return None`, `Default::default()`, `{}`, `true`, `false`, `0`, `""`): **0 hits** in `crates/ucil-daemon/src/watcher.rs`.
  The three `panic!(...)` / `.expect(...)` sites in the module are all inside `#[cfg(test)]` test bodies (`watcher.rs:625`, plus the many `.expect(...)` call sites in the test fns starting at `watcher.rs:355+`), which is allowed by `.claude/rules/rust-style.md` (`unwrap`/`expect` forbidden only outside `#[cfg(test)]`).

- **Mocked critical dependencies** — ripgrep for `mock|fake|stub|MagicMock|Mockall` matching `notify|notify-debouncer-full|serena|lsp|sqlite|lancedb|docker` in the diff: **0 hits**. Every test uses real `tempfile::TempDir` + `std::fs::write` against a real `notify::RecommendedWatcher` driven by the kernel inotify / FSEvents backend — no mocking of `notify` or `notify-debouncer-full`. Phase-log invariant 1 satisfied.

- **Skipped / ignored tests** — ripgrep for `#[ignore]|\.skip\(|xfail|xit|pytest\.skip`: **0 hits**.

- **Weak assertions** — ripgrep for `assert!\(true\)|assert_eq!\(true,\s*true\)|expect\(true\)`: **0 hits**. Test assertions are substantive: event source comparisons, bounded debounce counts (`<= 3` from 5 rapid writes), event-kind equalities, timing budgets against `DEBOUNCE_WINDOW`.

- **Hallucinated imports / paths** — each `use` statement resolves to a real crate:
  - `std::*` — stdlib ✓
  - `notify::{RecursiveMode, Watcher}` — `Cargo.lock:name = "notify" version = "6.1.1"` ✓
  - `notify_debouncer_full::{new_debouncer, DebounceEventResult, Debouncer, FileIdMap}` — `Cargo.lock:name = "notify-debouncer-full" version = "0.3.2"` ✓
  - `thiserror::Error` — existing workspace dep ✓
  - `tokio::{sync::mpsc, task::JoinHandle}` — existing workspace dep ✓
  `lib.rs` re-export `pub use watcher::{EventSource, FileEvent, FileEventKind, FileWatcher, WatcherError, DEBOUNCE_WINDOW};` — every target exists in `watcher.rs` ✓.

- **Feature coverage** — WO `features: ["P1-W3-F02"]`; all six named tests present at module root (frozen `watcher::test_*` selector per DEC-0005):
  - `watcher.rs:354 fn test_event_kind_mapping_covers_create_modify_remove`
  - `watcher.rs:412 async fn test_notify_emits_event_after_debounce`
  - `watcher.rs:455 async fn test_notify_debounces_editor_writes`
  - `watcher.rs:502 async fn test_post_tool_use_hook_bypasses_debounce`
  - `watcher.rs:548 async fn test_hook_event_source_is_distinct`
  - `watcher.rs:601 async fn test_watcher_shutdown_is_clean`
  No `#[cfg(test)] mod tests { }` wrapper — DEC-0005 module-root flat tests honoured.

- **Timeouts on every IO `.await`** — every receiver-side await wraps `rx.recv()` in `tokio::time::timeout` with an explicit budget (`Duration::from_secs(2)`, `DEBOUNCE_WINDOW`, `500 ms`). The test-only `drain_until_quiet` helper bounds each `recv` await with a `per_recv` timeout (`watcher.rs:395`). Rust-style invariant + phase-log invariant 7 satisfied.

- **Error handling** — `WatcherError` enum uses `thiserror::Error` with `#[non_exhaustive]` and `#[from]` conversions for `notify::Error` + `std::io::Error`. No `.unwrap()` / `.expect()` outside `#[cfg(test)]` in the production path (`watcher.rs:1-353` is the non-test region — clean).

- **Tracing** — `#[tracing::instrument(name = "ucil.daemon.watcher.new", ...)]` at `watcher.rs:229`, `#[tracing::instrument(name = "ucil.daemon.watcher.hook", ...)]` at `watcher.rs:317`. Span names follow master-plan §15.2 `ucil.<layer>.<op>` pattern.

- **Commit hygiene** — every commit subject is a Conventional Commit with scope:
  - `build(workspace): add notify + notify-debouncer-full workspace deps`
  - `feat(daemon): add watcher module with FileEvent types + FileWatcher`
  - `feat(daemon): watcher notify-path tests with real tempdir writes`
  - `feat(daemon): watcher hook-bypass tests pin down §14 fast path`
  - `feat(daemon): watcher shutdown test closes the F02 contract`
  - `chore(work-order): WO-0026 ready for review`
  Every feat/build/chore commit body contains `Phase: 1`, `Feature: P1-W3-F02`, `Work-order: WO-0026` trailers. No `--amend` after push, no force push observable. Sizes: 41 / 99 / 113 / 396 / 316 (deps incl. Cargo.lock) / 107 lines — only the module-introduction commit exceeds the 50-line soft target, under DEC-0005 exemption.

- **Doc + public API** — every new public item carries rustdoc:
  - `DEBOUNCE_WINDOW` (`watcher.rs:73-82`), `FileEventKind` + all 5 variants (`watcher.rs:84-108`), `EventSource` + both variants (`watcher.rs:110-132`), `FileEvent` + all 3 fields (`watcher.rs:134-147`), `WatcherError` + all 4 variants (`watcher.rs:149-172`), `FileWatcher` (`watcher.rs:174-188`), `FileWatcher::new` (`watcher.rs:214-233`) with `# Errors` section, `FileWatcher::notify_hook_event` (`watcher.rs:302-321`) with `# Errors` section, module-level docs (`watcher.rs:1-52`) with `# Examples` block.
  `lib.rs` module-level rustdoc updated at `lib.rs:24-30` to describe the new `watcher` module. `#![deny(rustdoc::broken_intra_doc_links)]` already active in `ucil-daemon/src/lib.rs:27` — intra-doc links (`[`FileEvent`]`, `[`EventSource::PostToolUseHook`]`, `[`DEBOUNCE_WINDOW`]`, `[`FileWatcher::notify_hook_event`]`) all resolve to real items.

- **Dependency hygiene** — `notify = "6"` + `notify-debouncer-full = "0.3"` placed in workspace `Cargo.toml` with an explanatory block comment citing master-plan §18 Phase 1 Week 3 line 1741 + §14 lines 1024-1025; `crates/ucil-daemon/Cargo.toml` pulls both via `{ workspace = true }`. No other crate touched. Versions pinned as major, per WO `scope_in[0]`.
