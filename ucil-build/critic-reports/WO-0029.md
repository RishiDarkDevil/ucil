---
work_order: WO-0029
branch: feat/WO-0029-lsp-fallback-server-spawner
ref: 9601547fd1a4d7192e04b374a212e2a08d2bec8e
merge_base: 4164c0dc7820adb6c9922661fb7298dddfe70145
reviewed_at: 2026-04-18T20:45:00Z
critic_session: crt-wo-0029
verdict: CLEAN
---

# Critic Report: WO-0029 — LSP fallback server spawner (P1-W5-F07)

**Branch**: `feat/WO-0029-lsp-fallback-server-spawner`
**Final commit**: `9601547`
**Merge base**: `4164c0d`
**Verdict**: **CLEAN**

## Commits reviewed

```
9601547 chore(work-order): WO-0029 ready for review
16d4ead fix(lsp-diagnostics): parallelize shutdown_all + fix rustdoc links
1e5c7d1 feat(lsp-diagnostics): add server_sharing module with FallbackSpawner
```

## Files changed

```
crates/ucil-lsp-diagnostics/src/bridge.rs         |  64 ++
crates/ucil-lsp-diagnostics/src/lib.rs            |   2 +
crates/ucil-lsp-diagnostics/src/server_sharing.rs | 918 +++++
ucil-build/work-orders/0029-ready-for-review.md   | 115 +++
4 files changed, 1099 insertions(+)
```

All paths are inside `crates/ucil-lsp-diagnostics/` or `ucil-build/work-orders/` — none of
the WO's `forbidden_paths` (`feature-list.json`, `.githooks/**`, `scripts/gate/**`,
other crate trees) are touched.

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

1. **Large single commit** — `1e5c7d1` adds 893 lines to
   `crates/ucil-lsp-diagnostics/src/server_sharing.rs` in one commit
   (vs the ~50-line soft target in `commit-style.md`). The content is a
   cohesive new module (struct, impls, reaper, tests, rustdoc), so
   splitting post-hoc is low-value, but future WOs of comparable size
   should split into "scaffold + impl + tests" commits. Not a blocker.

### OK

- **Stub detection**: `server_sharing.rs` has no `todo!()`,
  `unimplemented!()`, `NotImplementedError`, bare `pass`, or trivial
  default-literal returns. Every `pub fn` body calls into real
  lifecycle code; every `Result`-returning path surfaces a concrete
  `ServerSharingError` variant via `thiserror`. Acceptance assertion
  `! grep -RnE 'todo!\\(|unimplemented!\\(|#\\[ignore\\]'` holds.
- **Mocked critical collaborators**: None. The WO scope explicitly
  sanctions `sleep 60` as a real stand-in binary (WO `scope_out`:
  "No mocking of `tokio::process::Child` — use a real stand-in
  binary (`sleep`) to drive the lifecycle machinery"). Tests at
  `server_sharing.rs:689`, `:850`, `:866`, `:896` all spawn real
  `tokio::process::Child` instances — no `mock`, `fake`,
  `MagicMock`, or `mockall` anywhere in the diff.
  `grep -nE 'mock|fake|stub|MagicMock|Mockall' /tmp/server_sharing.rs`
  returned zero hits.
- **Skipped/ignored tests**: No `#[ignore]`, `.skip()`, `xfail`, `xit`,
  or `@pytest.mark.skip` anywhere in the diff.
- **Weak assertions**: No `assert!(true)`, `assert_eq!(true, true)`,
  `expect(true).toBe(true)`. Every assertion in
  `test_fallback_spawn` (`:689`-`:779`) binds a real predicate —
  PID > 0, endpoint count, transport variant match, `last_used`
  monotonicity, live-child status, shutdown count. The sole
  `panic!` at `:731` is inside an unreachable match arm on
  `LspTransport::DelegatedToSerena` — a legitimate exhaustiveness
  guard, not a no-op assertion.
- **Hallucinated imports / paths**:
  - `crate::bridge::LspDiagnosticsBridge` — verified present in
    `bridge.rs:104` (`pub struct LspDiagnosticsBridge`).
  - `crate::types::{Language, LspEndpoint, LspTransport}` — verified
    present in `types.rs` (Language enum, LspEndpoint struct,
    LspTransport enum with `Standalone { command, args }` variant).
  - `tokio::process::{Child, Command}`, `tokio::sync::Notify`,
    `tokio::task::{JoinHandle, JoinSet}`, `tokio::time::{timeout,
    sleep}` — all within `tokio`'s stable public surface.
  - `thiserror::Error` — workspace dep, already in
    `ucil-lsp-diagnostics/Cargo.toml`.
  - No new `[dependencies]` required; existing `tokio` workspace
    features (process, time, sync) already pulled in via the
    bridge/diagnostics modules.
- **Feature coverage**: The single feature in the WO
  (`P1-W5-F07`) is exercised by:
  - Module-root `test_fallback_spawn` (`:689`) — end-to-end
    lifecycle: spawn two languages, assert endpoints installed on
    bridge, PIDs > 0, children alive, `touch` advances `last_used`,
    `shutdown_all` drains within `SHUTDOWN_TIMEOUT`.
  - `tests::test_binary_not_found` (`:796`) — error-path coverage
    for the `BinaryNotFound` / `SpawnFailed` acceptance criterion.
  - `tests::test_partial_spawn_failure_does_not_panic` (`:849`) —
    reinforces the no-panic guarantee.
  - `tests::test_touch_unknown_language_is_noop` (`:865`) — covers
    the `touch` no-op branch.
  - `tests::test_bridge_with_fallback_spawner_installs_endpoints`
    (`:895`) — exercises the `DEC-0008`-linked additive bridge
    constructor.
  - `tests::test_error_display_renders_context` (`:821`) — asserts
    the error enum carries useful `Display` context.
  Frozen-pattern selector `server_sharing::test_fallback_spawn`
  resolves at module root (not `tests::test_fallback_spawn`) per
  the WO-0006/0007/0011/0013 lesson captured in
  `lessons_applied`. Grep confirms:
  `server_sharing.rs:689:async fn test_fallback_spawn()` and it
  is preceded by `#[tokio::test]`, not by `mod tests { … }`.
- **Commit hygiene**:
  - `feat(lsp-diagnostics): …` — Conventional Commits header,
    imperative mood, ≤70 chars. Body has `Phase: 1`, `Feature:
    P1-W5-F07`, `Work-order: WO-0029`, and `Co-Authored-By`
    trailer.
  - `fix(lsp-diagnostics): parallelize shutdown_all + fix
    rustdoc links` — same discipline, body explains the *why*
    (each child burns SHUTDOWN_TIMEOUT+1s serially ⇒ outer 10s
    test timeout breached with two stand-in children).
  - `chore(work-order): WO-0029 ready for review` — ready-marker
    commit, trailers present.
  - No `--amend`-after-push observed (linear history, three
    monotonic parents).
  - No `--no-verify` / `-n` in the executor flow (pre-push and
    pre-commit hooks pass per the ready marker).
- **Doc + public API**: Every new `pub` item has rustdoc:
  - module-level docs (`server_sharing.rs:1-58`)
  - `DEFAULT_GRACE_PERIOD_MINUTES`, `MAX_CONCURRENT_SERVERS`,
    `REAP_INTERVAL`, `SHUTDOWN_TIMEOUT` constants (`:93-124`)
  - `ServerSharingError` enum + all three variants (`:128-174`)
  - `ServerHandle` struct (`:178-199`) and all four accessors
    (`:201-232`)
  - `FallbackSpawner` struct (`:237-271`), `Debug` impl
    (`:273-287`), and every method: `new`, `with_grace_period`,
    `install_into`, `touch`, `is_alive`, `last_used_for`,
    `pid_for`, `languages`, `grace_period`, `shutdown_all`
    (`:289-542`)
  - `Drop for FallbackSpawner` (`:545-567`)
  - `bridge.rs` new items: `with_fallback_spawner`,
    `fallback_spawner`, `fallback_spawner_mut`, new struct field
    `fallback_spawner` — all documented.
  - `lib.rs` re-exports `FallbackSpawner`, `ServerSharingError`
    without stripping their source docs.
- **Timeout discipline**: Every IO `.await` is wrapped per
  `rust-style.md` Async §:
  - `shutdown_handle` wraps `child.wait()` in
    `timeout(SHUTDOWN_TIMEOUT, …)` (`:604`) and the post-SIGKILL
    wait in `timeout(Duration::from_secs(1), …)` (`:610`).
  - `reap_idle` uses `tokio::time::sleep` (timer, not IO) and
    `Notify::notified()` (sync primitive, not IO) inside a
    `tokio::select!` (`:634-637`) — correctly documented in the
    module header.
  - `test_fallback_spawn` wraps `shutdown_all` itself in
    `tokio::time::timeout(Duration::from_secs(10), …)` (`:764`)
    as a defence-in-depth against a test-hang.
- **No `std::process::Command`** in the new module. Acceptance
  assertion `! grep -Rn 'std::process::Command' …/server_sharing.rs`
  holds. All spawns go through `tokio::process::Command`.
- **Forbidden-path audit**: No files touched under
  `crates/ucil-{core,daemon,treesitter,cli,agents,embeddings}/`,
  `scripts/gate/**`, `.githooks/**`, `tests/fixtures/**`,
  `feature-list.json`, `feature-list.schema.json`, or the master
  plan. Scope stays entirely in `ucil-lsp-diagnostics`.
- **`new(serena_managed: bool)` signature stability** (DEC-0008
  §Consequences): `bridge.rs:172` still reads
  `pub fn new(serena_managed: bool) -> Self` byte-for-byte; the
  new `with_fallback_spawner` is additive.

## Summary

The executor delivered a well-documented, single-module feature that
respects every `forbidden_paths` entry, uses real subprocesses (as
sanctioned by the WO), surfaces typed errors via `thiserror`, wraps
every IO `.await` in `tokio::time::timeout`, and maintains the frozen
test-selector discipline from prior WO lessons. The only observation
worth flagging is the 893-line single commit — a soft-rule miss, not
a blocker.

**Verdict: CLEAN.** Forward to verifier.
