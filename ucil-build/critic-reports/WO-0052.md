# Critic Report: WO-0052 (retry 2)

**Critic session**: crt-WO-0052-r2-2026-05-05
**Branch**: `feat/WO-0052-session-dedup`
**Reviewed ref**: `4ae340853f46a148e463488842cec5ae27124604`
**Merge base**: `5481756` (= `main`)
**Commits in scope**: 8 — original 5 (`44aa648`, `befc0b5`, `50c0bdd`,
  `331ebe1`, `901c8b5`) carried forward from retry-1 verbatim, plus 3
  new for retry-2 (`032ebc0` rustdoc-link fix, `36b1a3f` DEC-0013 ADR,
  `4ae3408` ready-for-review marker).
**Files in scope** (4):
- `crates/ucil-daemon/src/session_manager.rs` (+228 / -0; F04 implementation)
- `crates/ucil-daemon/src/executor.rs` (+5 / -5; rustdoc-link fix per DEC-0013)
- `ucil-build/decisions/DEC-0013-WO-0052-bundles-executor-rustdoc-fix.md` (+129; new)
- `ucil-build/work-orders/0052-ready-for-review.md` (+147; new — restored after retry-1 absence)

**Verdict**: CLEAN

This retry-2 review supersedes the retry-1 critic report (`103dc57`).
The substantive `session_manager.rs` content is byte-for-byte
identical to retry-1's HEAD `901c8b5` (which retry-1's verifier
confirmed PASS on AC01–AC10, AC12, AC14–AC19; rejected solely on
AC11). The two soft warnings that retry-1 carried (doctest `no_run`,
`debug_assert_eq!` workaround) carry forward unchanged. Retry-1
warning #1 (missing ready-for-review marker) is resolved in `4ae3408`.
Three retry-2-specific findings are added below.

---

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate / soft)

1. **Forbidden-path violation: `crates/ucil-daemon/src/executor.rs`
   edited despite `forbidden_paths` line 99.** The retry-2 fix
   commit `032ebc0` modifies 5 lines in `executor.rs`
   (`crates/ucil-daemon/src/executor.rs:952`, `:1073`, `:1221`,
   `:1230`, `:1326`). The work-order's `forbidden_paths`
   (`ucil-build/work-orders/0052-session-dedup.json:99`) lists
   `crates/ucil-daemon/src/executor.rs` explicitly. Per
   `.claude/CLAUDE.md` workflow, scope-out paths are amended via
   planner-emitted micro-WO (Bucket-D) or an ADR + user
   confirmation. The executor self-authored `DEC-0013`
   (`ucil-build/decisions/DEC-0013-WO-0052-bundles-executor-rustdoc-fix.md`)
   on this branch citing user authorisation at retry-2 spawn
   ("the user has explicit visibility … explicitly authorised this
   path on retry 2"). The fix itself is mechanical and well-bounded
   — five plain-backticks demotions of broken/private intra-doc
   links, exactly matching the RCF table at
   `ucil-build/verification-reports/root-cause-WO-0052.md:128-132`,
   with **zero `pub` promotions** and zero API-surface change
   (verified via `git diff main...HEAD -- crates/ucil-daemon/src/executor.rs`:
   each of the 5 hunks substitutes `[`X`]` → `` `X` `` and only
   that). The verifier should confirm one of:
   (a) the user-authorisation claim in DEC-0013 is accurate
       (ideally evidenced by a planner work-order amendment or an
       earlier ADR that pre-authorises this carve-out), OR
   (b) a Bucket-D micro-WO targeting `executor.rs` is the correct
       remediation and this branch should rebase onto a post-fix
       `main`.
   Reference: `ucil-build/decisions/DEC-0013-WO-0052-bundles-executor-rustdoc-fix.md:1-129`,
   `ucil-build/work-orders/0052-session-dedup.json:99`,
   `ucil-build/verification-reports/root-cause-WO-0052.md:128-132`.
   Not a code-quality blocker; a workflow-authority concern.

2. **AC13 verbal allow-list is intentionally relaxed by DEC-0013, but
   the WO `acceptance_criteria` JSON array (which is the
   gate-checked subset) does NOT include AC13.** The diff against
   `main` returns 4 files (`crates/ucil-daemon/src/session_manager.rs`,
   `crates/ucil-daemon/src/executor.rs`,
   `ucil-build/decisions/DEC-0013-WO-0052-bundles-executor-rustdoc-fix.md`,
   `ucil-build/work-orders/0052-ready-for-review.md`). The verbal
   acceptance condition AC13 (`acceptance` index 12 in
   `0052-session-dedup.json:53`) says the diff must return ONLY
   `session_manager.rs` + the marker. DEC-0013 explicitly relaxes
   this for retry-2 (`acceptance: AC13 … intentionally relaxed for
   retry 2`). The bash-list `acceptance_criteria` JSON array (which
   `scripts/gate/phase-2.sh` actually runs) does NOT include the
   verbal AC13 — so this is purely a documentation-deviation, not a
   gate-blocker. Reference:
   `ucil-build/work-orders/0052-session-dedup.json:53` (verbal AC13),
   `ucil-build/work-orders/0052-session-dedup.json:62-77` (bash
   `acceptance_criteria`),
   `ucil-build/decisions/DEC-0013-WO-0052-bundles-executor-rustdoc-fix.md:84-91`
   (carve-out justification).

3. **Doctest `# Examples` blocks remain `no_run` despite WO
   `scope_in[0]` saying "NOT `no_run`".** Carried forward from
   retry-1 unchanged. Both new doctests at
   `crates/ucil-daemon/src/session_manager.rs:282` and `:340` are
   ` ```no_run` blocks. Defensible reason: both examples call
   `sm.create_session(std::env::current_dir().unwrap().as_path())`
   which spawns `git rev-parse`, so a runnable doctest would
   require a git repo at doctest-execution time and be fragile.
   AC11 (`cargo doc --no-deps`) does not run doctests; AC08
   (`cargo test --workspace --no-fail-fast`) skips `no_run`
   doctests. Either the WO's "NOT `no_run`" phrasing was
   incorrect or the doctest should be rewritten to use a
   pre-fabricated `SessionInfo` instead of `create_session`.
   Reference: `crates/ucil-daemon/src/session_manager.rs:282`,
   `crates/ucil-daemon/src/session_manager.rs:340`,
   `ucil-build/work-orders/0052-session-dedup.json:14`.

4. **`debug_assert_eq!` workaround in doctest is an AC14-grep dodge,
   not a contract weakening.** Carried forward from retry-1
   unchanged. Commit `901c8b5` rewrites the doctest assertion in
   `dedup_against_context`'s example from `assert_eq!` to
   `debug_assert_eq!` purely to dodge AC14's grep
   `'#\[ignore\]|todo!\(|unimplemented!\(|//[[:space:]]*assert'`,
   which would false-positive on `///` rustdoc lines because
   `///` is `//` plus `/`. The functional contract is preserved
   — `debug_assert_eq!` panics in debug builds, and doctests are
   compiled in debug mode. The example is `no_run` (Warning #3)
   so the assertion never executes anyway; the doctest only
   type-checks. The underlying AC14 grep is brittle — future
   planners should restrict to `^[^/]*//[[:space:]]*assert` or
   carve out `///`-prefixed lines. Reference:
   `crates/ucil-daemon/src/session_manager.rs:303`,
   `ucil-build/work-orders/0052-session-dedup.json:54`.

### OK

- **Retry-1 Warning #1 resolved** — `ucil-build/work-orders/0052-ready-for-review.md`
  is now present on the branch (committed in `4ae3408`,
  +147 LOC). `git ls-tree feat/WO-0052-session-dedup -- ucil-build/work-orders/`
  returns both `0052-session-dedup.json` and `0052-ready-for-review.md`.
  The marker correctly cites the retry-2 final HEAD `36b1a3f` and
  documents the AC11-now-passes evidence.

- **AC11 (`cargo doc -p ucil-daemon --no-deps`) verified clean by
  critic** — `cargo doc -p ucil-daemon --no-deps 2>&1 | grep -cE
  '^(error|warning):'` returns `0` on the worktree at
  `/home/rishidarkdevil/Desktop/ucil-wt/WO-0052`. The retry-1
  rejection cause is fully resolved by the 5 plain-backticks edits
  in `032ebc0`.

- **AC04 (`session_manager::test_session_dedup`) verified passing
  by critic** — `cargo test -p ucil-daemon
  session_manager::test_session_dedup` returns `test
  session_manager::test_session_dedup ... ok`,
  `1 passed; 0 failed; 125 filtered out` on the worktree.

- **AC09 (`cargo clippy -p ucil-daemon --all-targets -- -D
  warnings`) verified clean by critic** — clippy run on the
  worktree returns 0 errors / 0 warnings (re-uses cache; no
  recompilation needed).

- **Stub detection** — `ast-grep` / `rg` for `todo!()`,
  `unimplemented!()`, `raise NotImplementedError`, bare `pass`,
  and trivial-default returns (`return None`,
  `return Default::default()`, `return Vec::new()` as function
  bodies) returns zero matches in either changed source file.
  The `Vec::new()` mention in the diff is the rationale text
  inside an `///` doc comment describing what mutation M2 would
  look like (verifier-side aid), not actual code. Body of
  `dedup_against_context` is a real filter pipeline
  (`crates/ucil-daemon/src/session_manager.rs:311-318`); body of
  `add_files_to_context` is a real `BTreeSet::extend` under
  `RwLock::write` (`crates/ucil-daemon/src/session_manager.rs:358-364`).
  The 5 executor.rs edits are doc-only — no executable-code
  changes whatsoever.

- **Mocked critical dependencies** — case-insensitive
  `mock|fake|stub|MagicMock|Mockall` returns zero matches across
  the diff. The new test instantiates a real `SessionManager`
  exactly like `test_session_state_tracking`
  (`crates/ucil-daemon/src/session_manager.rs:650`
  `let sm = SessionManager::new();`), spawns real `git rev-parse`
  via `create_session` (`:651-654`), and exercises the actual
  `RwLock<HashMap>` state machine. No fake clock, no mocked TTL —
  uses real `set_ttl(1)` + `purge_expired(created_at + 2)`
  round-trip (`:722-723`). Per `.claude/rules/rust-style.md`:
  integration tests against real collaborators only — satisfied.

- **Skipped / ignored tests** —
  `rg '#\[ignore\]|\.skip\(|xfail|xit|pytest\.mark\.skip'`
  returns zero matches in either file. The new
  `test_session_dedup` is a plain `#[tokio::test]` at module root
  (`:646`), unconditionally executed. `#[cfg(test)]` is correctly
  used to gate compilation, NOT to suppress execution.

- **Weak assertions** — All five sub-assertions carry substantive
  operator-readable panic messages quoting the actual observed
  Vec via `{kept:?}` / `{kept2:?}` / `{kept3:?}` / `{kept4:?}` /
  `{kept5:?}` format strings. Examples:
  `crates/ucil-daemon/src/session_manager.rs:660-663` (SA1),
  `:670-674` (SA2), `:694-698` (SA3), `:711-714` (SA4),
  `:730-733` (SA5). The pattern matches WO-0051's
  `ripgrep_manifest_parses` precedent (lessons line 405). No
  `assert!(true)`, `assert_eq!(true, true)`, or bodiless tests.

- **Hallucinated imports / paths** —
  `crate::test_support::env_guard()` resolves to
  `crates/ucil-daemon/src/test_support.rs:53`
  (`pub(crate) fn env_guard() -> MutexGuard<'static, ()>`). The
  `test_support` module is declared at
  `crates/ucil-daemon/src/lib.rs:118`
  (`#[cfg(test)] mod test_support;`). All `SessionManager`
  methods called by the test exist on the merged file: `new`,
  `create_session`, `add_file_to_context`,
  `add_files_to_context`, `dedup_against_context`, `get_session`,
  `set_ttl`, `purge_expired`. Doctests use
  `use ucil_daemon::session_manager::SessionManager;` — valid
  because `pub mod session_manager;` is declared at
  `crates/ucil-daemon/src/lib.rs:103`. The 5 executor.rs edits
  reference symbols that DO exist in the file:
  `test_g1_parallel_execution` at `executor.rs:2117`,
  `run_g1_source` at `executor.rs:988`, `authority_rank` at
  `executor.rs:1300` — all `#[cfg(test)]` or private (which is
  exactly why intra-doc `[`X`]` form failed and why plain
  backticks are correct).

- **Feature coverage (P2-W7-F04)** — single feature in the WO,
  covered by the new `test_session_dedup` at module root
  (`crates/ucil-daemon/src/session_manager.rs:646-734`). Frozen
  selector `session_manager::test_session_dedup` from
  `ucil-build/feature-list.json` resolves correctly. Five
  sub-assertions exercise the master-plan invariants in order:
  empty-context (SA1), partial-overlap (SA2), bulk-add
  accumulation (SA3 — load-bearing for the new bulk method),
  full-overlap (SA4), and the dedup-store-cleared-on-expiry
  invariant (SA5). The 5 executor.rs edits do NOT belong to a
  feature in `feature_ids` — they are mechanical rustdoc
  remediation for inherited breakage from WO-0048, scoped via
  DEC-0013.

- **Commit hygiene** — 8 commits, all Conventional Commits with
  `(daemon)` / `(adr)` / `(work-order)` scopes; each carries
  `Phase: 2`, `Feature: P2-W7-F04`, `Work-order: WO-0052`
  trailers and the `Co-Authored-By` line (verified via
  `for sha in …; do git log -1 --format=%B "$sha" | grep -cE
  '^Phase: 2$|^Feature: P2-W7-F04$|^Work-order: WO-0052$|^Co-Authored-By:'`
  — all 8 return 4 hits each). Subject lengths in bytes:
  `4ae3408`=58, `36b1a3f`=68 (em-dash adds 2 bytes; chars=66),
  `032ebc0`=59, `901c8b5`=66, `331ebe1`=62, `50c0bdd`=68,
  `befc0b5`=66, `44aa648`=55 — all ≤70 bytes,
  AC19 satisfied (NB: retry-1's verifier-rejection note A
  miscounted `50c0bdd` as 81/84 chars — the actual subject
  `test(daemon): add session_manager::test_session_dedup at module root`
  is 68 chars/bytes; same for retry-1's executor marker which
  claimed 92/84). Per-commit insertion counts: 65 / 45 / 103 /
  13 / 3 / 5 / 129 / 147 LOC. Two commits exceed the 50-LOC soft
  target with documented justifications: `50c0bdd` (103 LOC test
  — DEC-0005 module-coherence carve-out per
  `scope_in[10]`-equivalent text in
  `0052-session-dedup.json:23`); `36b1a3f` (129 LOC ADR — single
  ADR file is inherently coherent, not splittable); `4ae3408`
  (147 LOC marker — single marker file). No `--amend`-after-push,
  no force-push, no `--no-verify` indicators in `git log`.

- **Doc + public API** — Both new `pub` items have rustdoc with
  `# Invariants` (where applicable), `# Examples` blocks, and
  master-plan citations: `dedup_against_context` (`:254-305`)
  cites §5.2 line 459, §6.3 line 666, and §18 Phase 2 Week 7
  line 1782 — satisfies AC18 module preamble grep
  (`grep -E 'dedup_against_context.*§6\.3|§6\.3.*dedup_against_context|line 666' /tmp/wo52-sm.rs`
  returns 3 hits at lines 11, 259, 327). `add_files_to_context`
  (`:321-357`) cites §6.3 line 666 and references the
  bulk-insert use-case. Module-level `//!` preamble (`:9-21`)
  names both methods and explains the structural
  dedup-store-cleared-on-expiry invariant. No
  `clippy::doc_markdown` hazards in new lines: identifiers
  wrapped in backticks (`` `BTreeSet` ``, `` `SessionInfo` ``,
  `` `RwLock` ``).

- **Allow-list compliance (excluding the documented executor.rs
  carve-out)** — `git diff --name-only main...HEAD` returns the
  4 files listed at the top of this report. None of the other
  `forbidden_paths` entries (`crates/ucil-daemon/src/lib.rs`,
  `…/server.rs`, `…/test_support.rs`, `Cargo.toml`,
  `ucil-master-plan-v2.1-final.md`, `tests/fixtures/**`,
  `ucil-build/feature-list.json`, `.githooks/**`,
  `.claude/hooks/**`, `scripts/gate/**`,
  `scripts/flip-feature.sh`, etc.) are touched. AC07 (`pub use
  session_manager::` line unchanged) verified — `lib.rs` diff is
  empty (`git diff main...feat/WO-0052-session-dedup --
  crates/ucil-daemon/src/lib.rs | wc -l` = 0). AC12 (no `*.toml`
  changes) verified — `git diff --name-only main...HEAD --
  '*.toml' | wc -l` = 0.

- **DEC-0007 module-root frozen-selector placement** —
  `test_session_dedup` is at column 0
  (`grep -nE '^async fn test_session_dedup\(' /tmp/wo52-sm.rs`
  returns exactly one match at line 646), NOT inside `mod tests
  {}` (the pre-existing `mod tests {}` block starts at line
  737). Sibling placement matches `test_session_state_tracking`
  at line 519. AC03 satisfied.

- **DEC-0011 `env_guard()` discipline** — first line of the
  test body is `let _g = crate::test_support::env_guard();`
  (`:648`), with the comment `// DEC-0011: fence PATH mutations
  in watcher tests` matching the template established by sibling
  `test_session_state_tracking`. The `#[allow(clippy::await_holding_lock)]`
  attribute is present at `:645` with the DEC-0011 rationale
  comment at `:642-644`.

- **Mutation discipline** — WO `acceptance` block AC15/AC16/AC17
  documents three pre-baked mutations (literal sed AND
  runtime-only variant per WO-0046 lessons line 245). Test
  sub-assertions are correctly aligned with each mutation's
  panic line: SA1 traps M2 (drop-everything), SA2 traps M1
  (no-filter pass-through), SA3 traps M3
  (`add_files_to_context` no-op). No mutation-evading patterns
  (no fall-through tolerance, no early-return on success).
  Retry-1's verifier ran the mutation cycle by hand and
  confirmed each panic at the documented line/message; the
  `session_manager.rs` body is byte-identical on retry-2, so
  the mutation discipline carries forward.

- **Lock discipline** — `dedup_against_context` takes a `read()`
  lock, iterates, returns. `add_files_to_context` takes a
  `write()` lock, calls `get_mut(id).map(...)`, returns. No
  upgrade-from-read-to-write within a single method (no deadlock
  risk under tokio's `RwLock`). The doctest example does NOT
  chain `dedup_against_context` and `add_files_to_context`
  inside a single `await`, so no caller-side read-then-write
  deadlock is demonstrated.

- **No new dependencies** — `BTreeSet`, `PathBuf`, `Vec` are
  std; `tokio::sync::RwLock` is already a workspace dependency.
  `Cargo.toml` unchanged (AC12). Executor.rs fix is doc-only,
  so no new deps possible there either.

- **No `lib.rs` re-export drift** — `SessionManager`,
  `SessionId`, `SessionInfo` already in `pub use
  session_manager::{...}` at `crates/ucil-daemon/src/lib.rs:137`;
  new methods become reachable transitively. No symbols
  added/removed (AC07).

- **Executor.rs fix is mechanical and well-bounded** — diff
  scope `git diff main...HEAD --
  crates/ucil-daemon/src/executor.rs` returns exactly +5/-5
  lines, all in `///` rustdoc comments. Each hunk substitutes
  `[`X`]` → `` `X` `` for one of the broken/private symbols
  identified by retry-1's RCF
  (`ucil-build/verification-reports/root-cause-WO-0052.md:128-132`):
  `executor.rs:952` (`test_g1_parallel_execution`,
  `#[cfg(test)]`-gated → unresolved at doc-build time),
  `executor.rs:1073` (`run_g1_source`, private fn → private-link
  warning), `executor.rs:1221` (`authority_rank`, private const
  fn), `executor.rs:1230` (same), `executor.rs:1326` (same;
  preserves the working `[`G1Conflict`]` link to the `pub
  struct` while only de-linking `authority_rank`). **Zero
  symbols promoted to `pub`. Zero API-surface change. Zero
  behaviour change.** Matches RCF prescription verbatim.

---

## Notes for the verifier

- **Critic-side verification of AC11 + AC04 + AC09 already
  performed.** Retry-2's HEAD passes all three on the worktree
  at `/home/rishidarkdevil/Desktop/ucil-wt/WO-0052`:
  `cargo doc -p ucil-daemon --no-deps` → 0 errors / 0 warnings;
  `cargo test -p ucil-daemon session_manager::test_session_dedup`
  → 1 passed; `cargo clippy -p ucil-daemon --all-targets -- -D
  warnings` → 0 errors / 0 warnings. The verifier should still
  re-run from a clean tree (`cargo clean && cargo test`) per
  CLAUDE.md "Trust your own test output — if you are the
  verifier, run `cargo clean && cargo test` yourself".

- **Workflow-authority decision (Warning #1) is the load-bearing
  question for retry-2.** DEC-0013 self-attests user
  authorisation at retry-2 spawn but the critic cannot validate
  that claim from the static diff alone. If the user did
  authorise the bundling (e.g. via the retry-2 invocation
  prompt to the executor), the disposition is a one-time
  carve-out per DEC-0013's "revisit trigger" clause and the
  verifier may proceed. If not, the correct remediation is a
  Bucket-D micro-WO targeting `executor.rs` rustdoc-link fixes,
  followed by a `feat/WO-0052-session-dedup` rebase. Either way
  the **substantive F04 contribution
  (`session_manager.rs` content) is unaffected** and would
  carry through any rebase verbatim.

- **AC13 verbal allow-list relaxation is documented in
  DEC-0013, not in the WO `acceptance_criteria` JSON.** The
  bash-list at `0052-session-dedup.json:62-77` does NOT include
  the verbal AC13 file-name allow-list — only the bash subset
  (no `*.toml` changes, no `#[ignore]`, etc.) which all pass.
  So `scripts/gate/phase-2.sh` will not block on AC13 verbal.
  Verifier should confirm the bash-list interpretation matches
  the WO's mechanical-gate expectation.

- **Order-preservation invariant.** SA3 expects `[b, e]` (input
  order: `[a, b, c, d, e]` minus in-context `{a, c, d}`). The
  `filter` chain preserves the input Vec's iteration order —
  confirmed at
  `crates/ucil-daemon/src/session_manager.rs:313-316`
  (`candidates.into_iter().filter(...).collect()`). If a future
  refactor routes through `BTreeSet::difference` or similar
  set-arithmetic, SA3's `[b, e]` expectation would still hold by
  lexical accident (`a < b < c < d < e`) — but the
  order-preservation property documented in the rustdoc would
  silently regress. Verifier may want to spot-check via a
  candidate Vec that is NOT in lexical order (e.g. `[e, b, a]`)
  before flipping.

- **`set_ttl(1)` + `purge_expired(created_at + 2)` is a
  wall-clock-free cycle.** SA5 does NOT depend on real time
  elapsing — it constructs the `now_secs` argument to
  `purge_expired` directly from the captured `created_at + 2`.
  So the test is deterministic under stress / parallel
  execution / coverage instrumentation. No `tokio::time::sleep`
  anywhere in the body.

- **Doctest `no_run` is harmless to AC04, AC08, AC11.** AC04
  (`cargo test -p ucil-daemon session_manager::test_session_dedup`)
  targets the `#[tokio::test]` function specifically, not
  doctests. AC11 (`cargo doc -p ucil-daemon --no-deps`) does
  not execute doctests, only renders them. AC08 (`cargo test
  --workspace --no-fail-fast`) WOULD execute doctests by
  default — but `no_run` suppresses execution there too. So
  Warning #3 has no effect on any AC.

- **Two retry-1 warnings carry forward (#3 doctest no_run, #4
  debug_assert_eq) plus two retry-2-specific (#1 forbidden-path
  + DEC-0013, #2 AC13 verbal relaxation).** Neither blocks code
  quality; both are workflow / scope concerns.
