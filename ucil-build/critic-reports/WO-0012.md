---
work_order: WO-0012
branch: feat/WO-0012-kg-crud-and-hot-staging
head_commit: 3887fe5ec627b3508757291800ec623fb47155a4
merge_base: 3887fe5ec627b3508757291800ec623fb47155a4
features: [P1-W4-F02, P1-W4-F08]
critic_session: crt-wo0012-2026-04-17
verdict: BLOCKED
reviewed_at: 2026-04-17T00:00:00Z
---

# Critic Report: WO-0012

**Critic session**: `crt-wo0012-2026-04-17`
**Branch**: `feat/WO-0012-kg-crud-and-hot-staging`
**Head**: `3887fe5ec627b3508757291800ec623fb47155a4`
**Merge base with `main`**: `3887fe5ec627b3508757291800ec623fb47155a4` (**identical** — branch has no commits past main)
**Range reviewed**: `git log --oneline main..feat/WO-0012-kg-crud-and-hot-staging` → **0 commits**.
**Worktree**: `/home/rishidarkdevil/Desktop/ucil-wt/WO-0012`
**Working-tree state**: **dirty** — one modified file, zero commits.

## Verdict

**BLOCKED** — the branch has no implementation. The executor has not done the work specified in
`ucil-build/work-orders/0012-kg-crud-and-hot-staging.json`. The only thing that exists is a
6-line, **uncommitted** dependency add in `Cargo.toml`, and no
`ucil-build/work-orders/0012-ready-for-review.md` marker. The critic **must not** advance this
to the verifier.

## Objective facts pinned for the verifier / planner

Command transcripts:

```
$ git merge-base main feat/WO-0012-kg-crud-and-hot-staging
3887fe5ec627b3508757291800ec623fb47155a4

$ git rev-parse feat/WO-0012-kg-crud-and-hot-staging
3887fe5ec627b3508757291800ec623fb47155a4

$ git rev-parse main
3887fe5ec627b3508757291800ec623fb47155a4

$ git log --format='%H %s' main..feat/WO-0012-kg-crud-and-hot-staging
(empty — 0 commits)

$ git -C /home/rishidarkdevil/Desktop/ucil-wt/WO-0012 status --porcelain
 M Cargo.toml

$ git -C /home/rishidarkdevil/Desktop/ucil-wt/WO-0012 diff main -- 'crates/**/*.rs' | wc -l
0

$ ls ucil-build/work-orders/ | grep 0012
0012-kg-crud-and-hot-staging.json        # WO envelope only, no ready-for-review marker
```

Full diff (tree vs. `main`):

```diff
diff --git a/Cargo.toml b/Cargo.toml
index 70e6316..d707909 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -69,6 +69,12 @@ tree-sitter-json = "0.24"
 # Unique IDs for session manager
 uuid = { version = "1", features = ["v4", "serde"] }

+# Date/time — RFC3339 parsing for bi-temporal SQLite columns
+# (`t_valid_from`, `t_valid_to`). `serde` feature so the typed
+# `Entity`/`Relation` structs can round-trip through serde without a
+# hand-rolled `DateTime` visitor.
+chrono = { version = "0.4", default-features = false, features = ["clock", "serde", "std"] }
+
 [profile.dev]
 opt-level = 0
```

That is the **entire** diff. Zero `.rs` changes. Zero test changes.
`crates/ucil-core/src/knowledge_graph.rs` still holds its WO-0011 body verbatim (599 lines, last
touched by `e29b927 chore(core): quiet clippy pedantic + nursery on knowledge_graph` — already on
main).

## Findings

### Blockers (must fix before verifier)

1. **Empty branch** — `git log --oneline main..feat/WO-0012-kg-crud-and-hot-staging` returns zero
   commits. Cites: `git rev-parse` output above. The executor has produced no implementation
   commits for this work-order. Every single `scope_in` bullet in
   `ucil-build/work-orders/0012-kg-crud-and-hot-staging.json:14-25` is unaddressed —
   `Entity`/`Relation` structs, `upsert_entity`, `get_entity_by_qualified_name`,
   `list_entities_by_file`, `upsert_relation`, `list_relations_by_source`, `get_entity_as_of`,
   `HotObservation` + `stage_hot_observation`, `stage_hot_convention_signal`,
   `stage_hot_architecture_delta`, `checkpoint_wal`, `WalCheckpointMode`, and the seven required
   tests. Nothing exists. Cite: `crates/ucil-core/src/knowledge_graph.rs` diff against `main`
   = 0 lines.

2. **Missing ready-for-review marker** — no
   `ucil-build/work-orders/0012-ready-for-review.md` exists (cf.
   `ucil-build/work-orders/0011-ready-for-review.md` which WO-0011 produced before verifier
   handoff). Per root CLAUDE.md §Workflow step 4, the executor writes this marker before the
   critic is spawned. This critic invocation therefore fires against a pre-implementation state
   and must report that upstream.

3. **Dirty working tree with uncommitted dep bump** —
   `git -C /home/rishidarkdevil/Desktop/ucil-wt/WO-0012 status --porcelain` reports
   ` M Cargo.toml`. The 6 added lines (68-74 of the new `Cargo.toml`) add `chrono` but nothing
   in the tree consumes it. This violates:
   - Root CLAUDE.md §"Commit + push cadence" — "Commit at every logical step … Push after every
     commit. No hoarding." A dep add touched >15 minutes ago with no commit is a cadence
     violation; a dep add with no corresponding code is a dangling intermediate.
   - Root CLAUDE.md §"Commit + push cadence" — "Stop-hook refuses to end the turn if the working
     tree is dirty OR the branch is ahead of upstream." Either the executor has not run the
     Stop-hook, or the hook was not authoritative on this turn. Flag for triage.

4. **Zero feature coverage** — critic.md §6 requires that every feature_id in the work-order
   has at least one new or modified test referencing its behaviour.
   - `P1-W4-F02` acceptance selector (`cargo nextest run -p ucil-core 'knowledge_graph::'`)
     would match only the WO-0011 test `test_schema_creation`, which is unrelated to F02's
     CRUD + bi-temporal surface. **No new F02 test exists.**
   - `P1-W4-F08` acceptance selector (`cargo nextest run -p ucil-core
     knowledge_graph::test_hot_staging_writes`) would fail to match anything because
     `test_hot_staging_writes` does not exist anywhere on the branch. Verified:
     `grep -n 'test_hot_staging_writes' crates/ucil-core/src/knowledge_graph.rs` → no matches.
   - None of the seven required tests from `scope_in` line 23 exists
     (`test_upsert_and_get_entity`, `test_bi_temporal_as_of`, `test_list_entities_by_file`,
     `test_upsert_relation_and_list`, `test_entity_unique_constraint_updates`,
     `test_hot_staging_writes`, `test_wal_checkpoint_truncates`).

5. **Zero commit hygiene signal** — critic.md §7 inspects commits in the range for
   Conventional Commits + `Phase:`/`Feature:`/`Work-order:` trailers. With zero commits in
   range there is nothing to check, so the check cannot produce a positive signal. Equivalent
   to an automatic fail on §7 because `estimated_commits: 8` in the WO and `0 ≠ 8` with no
   authorial justification recorded anywhere.

### Warnings (should fix before gate)

1. **Forward-referenced dependency without a consumer** — the `chrono` dep add in `Cargo.toml`
   is well-intentioned preparation (the WO does call for `chrono::DateTime<Utc>` on
   `get_entity_as_of`), but adding a dep in isolation is not the first logical commit. The
   WO's `lessons_applied[1]` (DEC-0005) explicitly suggests splitting into *coherent* commits —
   "struct commit → upsert_entity + test commit → …". A cleaner first commit is either
   (a) dep add + `Entity` struct + re-export together, or (b) dep add alone only if followed
   immediately by the first struct commit. As-is, the dep sits orphan. When resuming, the
   executor should either fold this into the first real commit or commit it immediately with a
   `build(core): add chrono for bi-temporal SQLite columns` message whose body cites the WO.

2. **Cargo.toml edit is at workspace root, not `crates/ucil-core/Cargo.toml`** — the WO
   `scope_in` line 24 says "crates/ucil-core/Cargo.toml — add `chrono` with `serde` feature".
   The executor instead edited the root workspace `Cargo.toml`, adding it to
   `[workspace.dependencies]` (line 69-75 region). This is *not* necessarily wrong — promoting
   to workspace deps is a fine pattern and matches how other workspace deps are organised on
   line 70 (`uuid = { … }`). But it drifts from the letter of the WO and leaves
   `crates/ucil-core/Cargo.toml` without a `chrono = { workspace = true }` line — the
   downstream crate cannot yet see it. Either bring the WO in line (split into "add to workspace"
   then "consume in ucil-core") or document in the first commit body that the consuming
   `chrono = { workspace = true }` line will arrive in the struct commit.

### OK (what passed trivially because nothing was reviewed)

The following checks from `.claude/agents/critic.md` pass trivially because the diff contains no
Rust source, no test source, and no imports. **These are not endorsements of future commits**;
they apply only to the 6-line diff that exists today.

1. **Stub detection (§1)** — diff contains no `todo!()`, `unimplemented!()`,
   `raise NotImplementedError`, bare `pass`, or trivial-default return. The only change is a
   `Cargo.toml` dependency line.
2. **Mocked critical dependencies (§2)** — no test files changed; no mock/fake/stub
   references introduced. Existing tests on the branch match the WO-0011 state exactly.
3. **Skipped / ignored tests (§3)** — no test files changed; no `#[ignore]`, `.skip()`,
   `xfail`, `it.skip`, or `@pytest.mark.skip` introduced.
4. **Weak assertions (§4)** — no tests added; nothing to weaken.
5. **Hallucinated paths / imports (§5)** — no `use` / `import` / `require` added. The
   `chrono` dep exists on crates.io, `version = "0.4"` with features `clock`, `serde`, `std` is
   valid, `default-features = false` is valid. Feature set matches what `get_entity_as_of`
   will need (Utc timestamps + serde round-trip). No hallucination.
8. **Doc + public API (§8)** — no new `pub` items introduced, so the rule is vacuously met.
   (Will re-check when real commits land.)

## What the executor must do before this critic can re-run usefully

1. Implement the WO's `scope_in` items in ~8 commits per
   `estimated_commits: 8`, pushing each one. Include the `chrono` workspace dep as part of the
   first real commit, and add `chrono = { workspace = true }` to
   `crates/ucil-core/Cargo.toml`.
2. Keep tests **at module root** inside `#[cfg(test)]`, **not** wrapped in
   `mod tests { ... }` — per the WO's `lessons_applied[0]` and scope_in line 23, the frozen
   selector `knowledge_graph::test_hot_staging_writes` must match after nextest's
   module-path expansion.
3. Run `cargo nextest run -p ucil-core 'knowledge_graph::'`, `cargo build --workspace`,
   `cargo clippy -p ucil-core -- -D warnings`, and `cargo doc -p ucil-core --no-deps`
   locally — all per the WO's `acceptance` block. Do **not** flip `passes`.
4. Write `ucil-build/work-orders/0012-ready-for-review.md` listing each commit SHA + line-count
   + feature id, same shape as `0011-ready-for-review.md`.
5. Push the branch (`git push -u origin feat/WO-0012-kg-crud-and-hot-staging`). Only then
   respawn the critic against the non-empty range.

## Summary

- **5 blockers**: empty branch, missing ready-for-review marker, dirty worktree w/ orphan dep,
  zero feature coverage for both P1-W4-F02 and P1-W4-F08, broken commit-hygiene signal.
- **2 warnings**: orphan dep not yet followed by a struct commit; dep placed at workspace root
  rather than consumer crate (minor drift from WO `scope_in`).
- **6 checks pass trivially** because there is no substantive diff to review.

The branch is **not** ready for the verifier. Do not spawn one. The executor must resume work
on `feat/WO-0012-kg-crud-and-hot-staging` (worktree
`/home/rishidarkdevil/Desktop/ucil-wt/WO-0012`) and produce real commits before the critic is
re-invoked.
