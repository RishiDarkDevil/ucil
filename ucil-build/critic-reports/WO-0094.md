# Critic Report: WO-0094

**Critic session**: crt-wo-0094-w11-pipeline-integration-tests
**Branch**: `feat/WO-0094-w11-pipeline-integration-tests`
**Tip ref**: `6157dd9e8d1a4c0bb5b6e9e24e6d8e7515e6130f` (RFR commit)
**Merge base**: `bb6d51d35f4e7ffb1a755f6138432d5d02d15f88` (current `main` tip)
**Features in scope**: P3-W11-F13 (quality pipeline) + P3-W11-F14 (testing pipeline) + P3-W11-F16 (full query pipeline + fusion engines)
**Verdict**: **CLEAN**

## Diff summary

```
9 commits, 9 files changed, 1847 insertions(+), 5 deletions(-)
  scripts/verify/P3-W11-F13.sh                    | +36
  scripts/verify/P3-W11-F14.sh                    | +38
  scripts/verify/P3-W11-F16.sh                    | +52
  tests/integration/Cargo.toml                    | +16
  tests/integration/test_fusion.rs                | +376 (NEW)
  tests/integration/test_quality_pipeline.rs      | +356 (NEW)
  tests/integration/test_query_pipeline.rs        | +378 (NEW)
  tests/integration/test_testing_pipeline.rs      | +359 (NEW)
  ucil-build/work-orders/0094-ready-for-review.md | +236
```

No source files under `crates/`, `plugins/`, `adapters/`, or `ml/` touched.
No edits to `tests/integration/test_lsp_bridge.rs`,
`tests/integration/test_plugin_lifecycle.rs`, or `tests/integration/src/`.
No edits to `feature-list.json`, `master-plan`, `scripts/gate/**`, or
`scripts/flip-feature.sh`. **Forbidden-paths constraint: clean.**

## Findings

### Blockers (must fix before verifier)

*None.*

### Warnings (should fix before gate, but not blocking)

1. **RFR header self-report is one commit behind** — `ucil-build/work-orders/0094-ready-for-review.md:3`
   declares `**Final commit sha**: f488a6e6...` (the rephrase commit) but
   the actual branch tip is `6157dd9` (the RFR commit itself). Same for
   `## Commits on this branch (8)` at line 213 — the branch carries 9
   commits when the RFR commit is included. Cosmetic only — the RFR
   was authored before its own commit existed. Not a blocker; verifier
   should treat the RFR-self-reference numbers as a known artefact.

2. **Per-file commits exceed the soft 50-LOC cadence** — each test-file
   commit lands ~360–378 LOC in a single commit
   (`f6a3fe8` test_fusion +370, `f83bc12` test_quality_pipeline +356,
   `89326ac` test_testing_pipeline +359, `31170db` test_query_pipeline +378).
   This is explicitly authorised by DEC-0005 module-coherence carve-out
   + the WO scope_in #23 prescription ("each test file is its own
   coherent commit unit"); flagging only so the verifier knows the
   commit-cadence soft target was waived intentionally and not silently.

### OK

#### 1. Stub detection — clean
- `rg 'todo!\(|unimplemented!\(|panic!\(\)\b'` across all 4 new test
  files: zero matches.
- `rg 'raise NotImplementedError'`: not applicable (Rust files).
- No `pass`-only / bare-literal-return stub bodies — every fn has a
  load-bearing body.

#### 2. Mocked critical dependencies — clean (and explicitly trait-seam carve-out)
- AC13 banned-word grep `! grep -qiE 'mock|fake|stub'` returns empty
  across all 4 test files AND all 3 verify scripts (verified
  per-file with `Grep -i 'mock|fake|stub'`).
- The `SeededG4Source` / `SeededG7Source` / `SeededG8Source` impls in
  the test files are UCIL-owned realisations of the `G4Source` /
  `G7Source` / `G8Source` traits (declared at
  `crates/ucil-daemon/src/g4.rs:365`, `g7.rs:322`, `g8.rs:301`
  respectively). Per DEC-0008 §4 the trait IS the dependency-inversion
  seam — these are NOT mocks of any external collaborator (Serena, LSP,
  SQLite, LanceDB, Docker). Each test file's module-header rustdoc
  declares this explicitly using the WO-0093 scope_in #25 POSITIVE
  phrasing ("the trait is the dep-inversion seam — production impls
  live in `crates/ucil-daemon/`"), avoiding the AC13 case-insensitive
  trip.
- No `mockall`, no `MagicMock`, no fake-* helper crates introduced.

#### 3. Skipped / ignored tests — clean
- No `#[ignore]`, `.skip(...)`, `xit(...)`, `xfail`, `@pytest.mark.skip`
  in any of the 4 new test files (`Grep` over each file returned zero).

#### 4. Weak assertions — clean
- No `assert!(true)`, `assert_eq!(true, true)`, `assert!(false)`, or
  `assert_eq!(1, 1)` patterns.
- Every `assert!` / `assert_eq!` carries a `(SAn) <semantic name>;
  left: ..., right: ...` panic-message body per DEC-0007 + WO-0067
  precedent. Counts of `(SA[0-9]+)` strings: test_fusion=18,
  test_quality_pipeline=3, test_testing_pipeline=6, test_query_pipeline=4.
  Each SA references a real outcome shape (closed-form RRF score, JSON
  pointer to `_meta.modules`, severity vocabulary contained in the
  master-plan §5.7 set, etc.) — not tautological.

#### 5. Hallucinated paths — clean
Every `use` statement in the 4 new test files resolves to a real
public item on the branch:
- `ucil_daemon::g4::{G4DependencyEdge, G4EdgeKind, G4EdgeOrigin,
  G4Query, G4Source, G4SourceOutput, G4SourceStatus}` — confirmed
  pub at `crates/ucil-daemon/src/g4.rs:127, 153, 185, 204, 233, 245, 365`.
- `ucil_daemon::g7::{G7Issue, G7Query, G7Source, G7SourceOutput,
  G7SourceStatus, Severity}` — confirmed pub at
  `crates/ucil-daemon/src/g7.rs:116, 140, 184, 218, 230, 322`.
- `ucil_daemon::g8::{G8Query, G8Source, G8TestCandidate,
  TestDiscoveryMethod}` — confirmed pub at
  `crates/ucil-daemon/src/g8.rs:130, 145, 241, 301`.
- `ucil_daemon::server::McpServer` + `McpServer::new` /
  `McpServer::serve` / `with_g4_sources` / `with_g7_sources` /
  `with_g8_sources` — confirmed pub at
  `crates/ucil-daemon/src/server.rs:462, 605, 802, 721, 745, 761`. The
  builder signatures match the test usage byte-for-byte
  (`Arc<Vec<Arc<dyn G4Source>>>` vs `Arc<Vec<Arc<dyn G7Source + Send +
  Sync>>>` — the test files use the corresponding form per trait).
- `ucil_daemon::executor::{fuse_g1, G1Outcome, G1ToolKind,
  G1ToolOutput, G1ToolStatus}` — confirmed pub at
  `crates/ucil-daemon/src/executor.rs:1387, 947, 865, 915, 894`. The
  `G1ToolOutput` field set used in `test_fusion.rs:312-326`
  (`kind, status, elapsed_ms, payload, error`) matches the struct
  declaration at `executor.rs:915-931` exactly.
- `ucil_core::cross_group::{fuse_cross_group, CrossGroupExecution,
  Group, GroupHit, GroupResult, GroupStatus}` — confirmed pub at
  `crates/ucil-core/src/cross_group.rs:62, 119, 145, 174, 259, 657`.
- `ucil_core::fusion::{fuse_g2_rrf, G2Hit, G2Source, G2SourceResults,
  QueryType, G2_RRF_K}` — confirmed pub at
  `crates/ucil-core/src/fusion.rs:45, 71, 90, 144, 243, 374`.
  `QueryType::FindReferences` is a real variant
  (`fusion.rs:385`).
- `_meta.source == "g4-architecture-fanout"` literal in
  `test_query_pipeline.rs:357` matches the real handler emit at
  `crates/ucil-daemon/src/server.rs:1538`. `_meta.modules` /
  `_meta.source_results` likewise emitted from the same handler block.

#### 6. Feature coverage — clean
Every feature_id in the WO has at least one new/modified test
referencing its behaviour:
- **P3-W11-F13** (quality pipeline) → `tests/integration/test_quality_pipeline.rs`
  with `pub async fn test_quality_pipeline_detects_severity_classified_issues()`
  (line 157). Asserts `issues[]` count, lowercase severity vocabulary,
  and `meta.master_timed_out == false` against `check_quality` over
  the real `McpServer::serve` loop with a seeded G7 source. Verify
  script `scripts/verify/P3-W11-F13.sh` runs the binary.
- **P3-W11-F14** (testing pipeline) → `tests/integration/test_testing_pipeline.rs`
  with `pub async fn test_testing_pipeline_discovers_tests_in_fixture_projects()`
  (line 157). Asserts `untested_functions[]` count, seeded test_path
  retention, and the 3-method discovery union (`convention`, `import`,
  `kg_relations`) against `check_quality`'s G8 fan-out. Verify script
  `scripts/verify/P3-W11-F14.sh` runs the binary. The rustdoc honestly
  flags that `run_tests` itself currently falls through to the phase-1
  `_meta.not_yet_implemented: true` path and explains why
  `check_quality`'s `untested_functions[]` projection is the canonical
  surface — no AC trip, no silent contract drift.
- **P3-W11-F16** (query pipeline + fusion) → TWO test files per the
  feature-list.json verbatim selector
  `--test test_query_pipeline --test test_fusion`:
  - `tests/integration/test_query_pipeline.rs` with
    `pub async fn test_query_pipeline_returns_fused_results_with_group_provenance()`
    (line 191) — exercises the full `with_g4_sources + with_g7_sources +
    with_g8_sources` builder chain through `get_architecture` and
    asserts `_meta.modules` (mutation-targeted via M3), `_meta.source ==
    "g4-architecture-fanout"`, and `_meta.source_results[0].source_id`
    naming the seeded source.
  - `tests/integration/test_fusion.rs` with three pure-function tests:
    `test_fusion_g2_rrf_correctness` (line 87) asserts the closed-form
    RRF score `2.0/(60+1) + 1.5/(60+1)`; `test_fusion_cross_group_dedup_and_provenance`
    (line 168) asserts G1+G3+G4 cross-group dedup-by-location +
    `per_group_ranks` provenance retention; `test_fusion_g1_with_partial_source_coverage`
    (line 293) asserts `source_dispositions` order, `master_timed_out`
    forwarding, and partial-coverage fusion correctness.
  - Verify script `scripts/verify/P3-W11-F16.sh` runs the verbatim
    feature-list selector `--test test_query_pipeline --test test_fusion`
    and requires BOTH binaries to print `test result: ok.` (with a
    hits>=2 guard so a single-binary green doesn't slip through).

#### 7. Commit hygiene — clean

- 9 commits, all Conventional Commits subjects (`test(integration):`,
  `build(scripts):`, `build(tests-integration):`, `chore(work-orders):`).
- All 9 commits carry `Phase: 3` + `Feature: P3-W11-F<NN>` (or the
  bundle form `Feature: P3-W11-F13, P3-W11-F14, P3-W11-F16`) +
  `Work-order: WO-0094` + `Co-Authored-By: Claude Opus 4.7
  <noreply@anthropic.com>` trailers (verified per-commit via
  `git log --format='%b' | grep -cE`).
- No merge commits on the feat branch
  (`git log feat/WO-0094-w11-pipeline-integration-tests ^main --merges
  | wc -l == 0`).
- No banned commit-flag patterns (`--no-verify`, `--amend`,
  `force-push`) in any commit subject or body.
- Per-commit shortstats (test files at 350-378 LOC each) are above
  the 50-line soft target but explicitly authorised by DEC-0005
  module-coherence carve-out + WO scope_in #23. Smaller commits split
  the cargo build / clippy verification into too many incremental
  rebuilds; one-file-per-commit is the conventional structure for the
  test-suite-bundle pattern. Flagged as a Warning above for visibility,
  not a Blocker.
- Module-header rustdoc, executor commits, and the RFR commit all use
  imperative-mood subjects and reference the right scope.

#### 8. Doc + public API — clean
- The only new `pub` items in the diff are the `pub async fn test_*`
  frozen tests at module ROOT (per DEC-0007). Each carries a multi-
  paragraph rustdoc above it, including SA-by-SA enumeration and a
  `# Panics` section (verified by `Grep -B 3 '^pub async fn test_'`).
- `clippy::missing_panics_doc` is allowed at file scope on
  `test_fusion.rs:8` and `#[allow(clippy::missing_panics_doc)]` is
  applied per-fn on the other three test files; the panic semantics
  are described in the rustdoc immediately above each fn so the lint
  silencing is informed and bounded, not a blanket allow.
- The test-file-private `Seeded<G>Source` structs are not `pub`, so
  no rustdoc is required by `clippy::missing_docs_in_private_items` —
  but each carries one anyway (concise, references the production
  impls and DEC-0008 §4).
- No new `pub` items introduced on the production-source side
  (forbidden_paths includes `crates/**`); the test-file `pub async fn`
  is the only public-API surface change and is doc'd.

#### 9. Anti-laziness contract — clean
- No `passes: true` flips (no `feature-list.json` edit on this branch).
- No `#[ignore]`, `.skip()`, `xfail`, `it.skip`, or commented-out
  assertions.
- No `todo!()`, `unimplemented!()`, `NotImplementedError`, `pass`-only
  bodies in shipped code.
- No `Default::default()` / `{}` / bare-literal returns as feature
  implementations — every fn body is load-bearing.
- No edits to `feature-list.json`, master plan, or
  `tests/fixtures/**`.
- No `git revert` / `git reset` of verifier-rejected code (none
  rejected on this WO).
- No `--no-verify`, `--amend`, or `--force` push patterns visible in
  the commit history.

#### 10. Mutation contract pre-bake — clean
RFR `## Mutation contract` (lines 106-173) names each of M1/M2/M3
with target file path, exact patch, expected verbatim SA panic-body,
restore command (`git checkout -- <file>`), and the per-file md5sum
snapshot path (per scope_in #28's NEW per-file md5sum protocol — the
3 mutations target 3 different files, not a single file). The
verifier has unambiguous instructions and can apply mutations without
guessing.

## Reviewer rules followed
- Read-only review (no source edits).
- Every finding cites file:line.
- Verified each `use` resolves to a real `pub` item on the branch.
- Verified each AC selector against the actual file/script paths.
- Verified each commit carries the required trailers.

## Recommendation
**Proceed to verifier.** The diff is conservative, scope-bounded,
exercises real public APIs (no mocks of critical collaborators), and
the mutation contract is pre-baked with concrete patches + restore
commands. The two Warnings above are cosmetic / informational and do
not block dispatch.
