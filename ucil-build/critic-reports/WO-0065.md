# Critic Report: WO-0065

**Critic session**: crt-2026-05-07T-WO-0065
**Branch**: `feat/WO-0065-vector-query-p95-bench`
**HEAD reviewed**: `f4adc41497d141bfcfd7adb6e539d13e5d9c75a8` (`test(effectiveness): re-run phase-1 effectiveness gate at 70aa72e`)
**Merge base**: `a0e9919` (`main` — `chore(planner): emit WO-0065 vector-query-p95-bench (P2-W8-F07)`)
**Diff scope**: 6 commits, 7 paths, +1081 / -66 (Cargo.lock 4, embeddings Cargo.toml 39, vector_query.rs 323, bench-vector-query.sh 162, verify/P2-W8-F07.sh 90, RFR 306, effectiveness-phase-1.md 157+/-66)
**Verdict**: **CLEAN with warnings**

The WO lands the second Phase 2 Week 8 benchmark leg (master-plan §18 line 1789): a criterion-based vector-query-latency bench for the LanceDB+IVF/HNSW path. The implementation comprises (1) four `[dev-dependencies]` (`lancedb`, `arrow-array`, `arrow-schema` via workspace pins + `rand = "0.8"` literal pin) and a second `[[bench]] name = "vector_query" harness = false` table on `crates/ucil-embeddings/Cargo.toml`; (2) `crates/ucil-embeddings/benches/vector_query.rs` (323 LOC) with three frozen `pub const`s (`CORPUS_SIZE = 2000`, `QUERY_COUNT = 20`, `EMBEDDING_DIM = 768`), a `tempfile::tempdir()`-rooted LanceDB connection, deterministic-seed corpus + query RNGs (`0x_C0DE_BABE` / `0x_FACE_FEED`), an `IvfHnswPq` index on the `embedding` column with `num_partitions = ceil(sqrt(2000)) = 45`, and a `bench_vector_query_p95` criterion function with `sample_size(100)`, `warm_up_time(2s)`, `measurement_time(20s)`; (3) `scripts/bench-vector-query.sh` (162 LOC) — release build cache warm + `cargo bench` + jq-driven per-sample p95 computation + dual asserter (wall-time floor `MEAN_NS_PER_ITER >= 100_000` + p95 floor `< 100 ms`) + canonical `p95_warm_ms=<N>` stdout emit; (4) `scripts/verify/P2-W8-F07.sh` (90 LOC) — frozen-script wrapper that runs the bench script and INDEPENDENTLY re-asserts `< 100 ms` per the WO-0061 mutation #3 defence-in-depth pattern.

I locally re-grepped the diff for every check in `.claude/agents/critic.md` (stubs, mocks of critical collaborators, skipped/ignored tests, weak assertions, hallucinated imports, feature coverage, commit hygiene, public-API docs). No blockers. Three warnings noted below — none gating; all are documented divergences with WO precedent.

## Findings

### Blockers (must fix before verifier)

**None.**

### Warnings (should be acknowledged before gate)

1. **Out-of-scope effectiveness-gate re-run commit on the WO branch** — `f4adc41 test(effectiveness): re-run phase-1 effectiveness gate at 70aa72e` modifies `ucil-build/verification-reports/effectiveness-phase-1.md` (+157 / -66 lines). The path is NOT in WO-0065 `forbidden_paths`, but it is also not in `scope_in`; the commit's trailers are `Phase: 1 / Feature: effectiveness-gate / Work-order: n/a`, which differs from the canonical WO-0065 `Phase: 2 / Feature: P2-W8-F07 / Work-order: WO-0065`. Reading the diff, the change is benign — it updates the run timestamp (`2026-04-19T07:45Z` → `2026-05-07T03:23:46Z`), the head commit ref (`26dfeb23` → `70aa72e1`), reformats prose, and updates the artefact-list listing. The substantive verdict is unchanged (UCIL 5/5 vs baseline 5/5, Δ = 0.0, PASS). This is consistent with AC25's spirit (Phase 1 gate regression check at the new HEAD). Verifier should treat the f4adc41 modifications as a transient gate-side artefact independent of the P2-W8-F07 verification path; merging should be evaluated on whether `effectiveness-phase-1.md` is allowed to drift on a feature branch (mirrors WO-0061 acceptance pattern). Accept with note.

2. **Single-commit LOC > 200 (DEC-0005 module-coherence carve-out cited in commit body)** — `d65dd16 feat(embeddings): land vector_query criterion bench (P2-W8-F07)` lands a 327-LOC commit (`vector_query.rs` is 323 LOC; +4 LOC `Cargo.lock`). The commit body explicitly cites `DEC-0005` module-coherence + `WO-0061 line 697` standing precedent for criterion-bench files. The 323 LOC comprises (a) ~89 LOC of file-level rustdoc (master-plan + DEC-0016 + WO-0061 cites + frozen-identifier contract + pre-baked mutation contract + async runtime contract), (b) three `pub const` blocks with rustdoc each, (c) two RNG-driven generator helpers, (d) one Arrow `RecordBatch` builder, (e) one `ceil_sqrt_u32` helper with `#[allow]` triplet, (f) the criterion entry-point with setup + `b.iter` body. WO scope_in[18] explicitly anticipates this single-commit shape. Splitting the bench file across multiple commits would require breaking the rustdoc → `pub const` → setup → `b.iter` chain into stub-shaped intermediate states. Soft target with documented justification consistent with WO-0061 (246 LOC) and other accepted bench precedents — accept.

3. **Combined scripts commit slightly over 200 LOC** — `8c0a230 chore(scripts): add bench-vector-query.sh + verify/P2-W8-F07.sh` lands 252 LOC across two scripts (162-LOC bench script + 90-LOC verify script). Both files are NEW; bash commit cadence does not have a pre-stamped DEC-0005 carve-out, but ~60% of the combined LOC is comment/heredoc-style block prose (mutation contract, master-plan citations, pre-baked mutation rationale). Splitting into two commits would move bench-script and verify-script across history boundaries; the WO scope_in[18] groups them under a single `chore(scripts):` commit. WO-0061 set the precedent with a 136-LOC bench script + 89-LOC verify script in two commits (vs. one here); the slight aggregation here keeps the dual-asserter pattern visible in a single commit. Soft target with documented justification — accept.

4. **`expect()` calls in bench code outside `#[cfg(test)]`** — eight `.expect()` sites at `crates/ucil-embeddings/benches/vector_query.rs:111,113,127,167,184,191,196,207,214,222,228,235`. `.claude/rules/rust-style.md` prohibits `.expect()` outside `#[cfg(test)]` ("Use `?` and a typed error"), but bench code is not `#[cfg(test)]` and criterion's `criterion_main!` returns `()` so `?`-propagation is not viable. WO-0061 line 689 explicitly carves out the `.expect()` idiom for `crates/*/benches/*.rs` setup failures, and WO-0065 scope_in[8] reaffirms this with "(`.expect()` permitted in benches per WO-0061 line 689)". WO authority overrides the general style rule per the oracle hierarchy in root `CLAUDE.md`. Accept consistent with WO-0061 precedent.

5. **Three pre-baked mutations (AC30/31/32) delegated to verifier** — WO scope_in steps 16(M1/M2/M3) prescribe three mutations (`Table::query().nearest_to(...)` body neutered; `QUERY_COUNT` → 1; bench-script `< 100` → `< 1000000`). The executor's RFR rows AC30-AC32 map them as `DELEGATED to verifier per WO-0061 line 690`. Reading the WO carefully, this mirrors WO-0061's mutation-execution-delegation pattern — the WO names exact `sed`-able edits and expected failure modes, and verifier applies + restores in-line. RFR §"Mutation execution log" includes the literal edits, expected stderr lines, and `git checkout --` restoration commands. M3 deserves special attention: in the green case (this machine: `<N> ≈ 0.3 ms`), neutering only the bench-script's `< 100` does NOT flip green→red because the real `<N>` is well under 100 — verifier MUST inspect `scripts/verify/P2-W8-F07.sh` lines 67-83 (Step 4 + Step 5) and confirm independent code, NOT a `set +e ; bench-script ; exit $?` shell. Accept consistent with WO-0061 precedent.

6. **`PROTOC` toolchain dependency disclosed in RFR** — RFR §"Disclosed deviations" item 2 notes that `lancedb 0.16` transitively pulls `lance-encoding` + `lance-file`, both of which run `prost-build` build scripts requiring `protoc` on the build host. Executor downloaded `protoc 25.1` to `~/.local/bin/protoc` + `~/.local/include/google/protobuf/*.proto`. Verifier needs `protoc` on PATH (or `PROTOC` + `PROTOC_INCLUDE` exported) to reproduce. This dependency was first disclosed in WO-0053 critic report (lines 71-78); same posture for WO-0065. Verifier — if your build host does not have `protoc 25.1+` or equivalent, expect `cargo build --release -p ucil-embeddings --bench vector_query` to fail with a `prost-build` build-script error. Accept; harness pre-flight needed but not in scope of this WO.

7. **`rand = "0.8"` literal version pin not workspace-routed** — `crates/ucil-embeddings/Cargo.toml:46-50` declares `rand = "0.8"` as a literal-version dev-dep, NOT `rand = { workspace = true }`. The root `Cargo.toml` does NOT declare a `[workspace.dependencies] rand = ...` line, so the literal-pin is the only available form. The disclosed deviation is in RFR §"Disclosed deviations" item 1: "Added `rand = "0.8"` as dev-dep beyond the three deps listed in WO-0065 scope_in[1]. Required by AC08 — `StdRng::seed_from_u64(...)` needs the `rand` crate. Pinned `0.8` to the line already in `Cargo.lock` (transitive via `lance`/`tokenizers`); zero new-crate churn." `Cargo.lock` confirms `rand 0.8.5` was already resolved transitively (no new-crate churn — `Cargo.lock` diff shows +4 / -0 lines, all pure `ucil-embeddings` dep-list re-ordering). Borderline — long-term, `rand` belongs at workspace level for consistency with the other 3 dev-deps; short-term, this is functionally identical and zero-churn. Accept; could file a follow-up tidy-up WO if the project standard is workspace-only deps.

### OK

- **Stubs**: zero hits in shipped code. `git show feat/WO-0065-vector-query-p95-bench:crates/ucil-embeddings/benches/vector_query.rs | grep -nE 'todo!\(\)|unimplemented!\(\)|panic!\(".*not yet|TODO|FIXME|raise NotImplementedError'` returns 0. The bench file uses `.expect()` for setup-failure paths (see Warning 4); these are real failure modes (tempdir-creation failure, lancedb connect failure, table create failure, schema build failure, `nearest_to` query-builder failure, query execute failure), not stub markers. The two shell scripts have zero `todo`/`TODO`/`FIXME`/`HACK` mentions — only structured `[INFO]` / `[FAIL]` / `[HINT]` log lines.

- **Mocked critical collaborators**: zero in `crates/ucil-embeddings/benches/vector_query.rs`. `git show <branch>:crates/ucil-embeddings/benches/vector_query.rs | grep -inE 'mock|fake|stub|fixture'` returns 0 matches — the file has been pre-flight word-banned per AC27 + WO-0061 line 686. The bench loads the real `lancedb` async client via `lancedb::connect(...).execute().await` and exercises the real `Connection::create_empty_table` + `Table::add` + `Table::create_index(Index::IvfHnswPq(...))` + `Table::query().nearest_to(...).limit(10).execute()` API surface — no `lancedb` / `arrow_array` / `arrow_schema` mocks per `.claude/rules/rust-style.md`. The shell scripts mention "mock" once in `bench-vector-query.sh:48,57,90` as the conceptual descriptor for the implementation SHAPE the wall-time floor catches (consistent with WO-0061 critic warning 5 and WO-0061 line 692 carve-out: AC mock-scans apply to `.rs` files only).

- **Skipped/ignored tests**: zero. `grep -nE '#\[ignore\]|\.skip\(|xfail|it\.skip|@pytest\.mark\.skip|@pytest\.mark\.xfail'` on the diff returns 0. The bench file has no `#[test]` / `#[ignore]` attributes (it's a `criterion_main!` binary). The verify script does not gate on a skip-this-environment branch.

- **Weak assertions**: zero. `grep -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)|assert_eq!\(1,\s*1\)|expect\(true\)\.toBe\(true\)'` on the diff returns 0. The bench has no `#[test]` blocks. The mutation contract (AC30-AC32) carries the "real assertion" semantics: wall-time floor `MEAN_NS_PER_ITER >= 100_000` (100 µs) and p95 floor `< 100 ms` in the bash asserter, plus the verify script's independent `< 100 ms` re-check.

- **Hallucinated imports**: zero. The 10 `use` items in `vector_query.rs` all resolve:
  - `use std::sync::Arc;` and `use std::time::Duration;` — std.
  - `use arrow_array::types::Float32Type;` and `use arrow_array::{FixedSizeListArray, RecordBatch, RecordBatchIterator, StringArray};` — workspace dep `arrow-array = "53"` declared at root `Cargo.toml:246` (and dev-dep at `crates/ucil-embeddings/Cargo.toml:36`); `Float32Type` is `arrow_array::types::Float32Type` (a re-export of `arrow_array::types::Float32Type` from `arrow-array 53`); the four named symbols are part of `arrow-array 53`'s public API.
  - `use arrow_schema::{DataType, Field, Schema};` — workspace dep `arrow-schema = "53"` at root `Cargo.toml:247` (and dev-dep at `crates/ucil-embeddings/Cargo.toml:45`); all three symbols are part of `arrow-schema 53`'s public API.
  - `use criterion::{black_box, criterion_group, criterion_main, Criterion};` — workspace dep `criterion = 0.5` declared at root `Cargo.toml`; all four symbols are part of criterion 0.5's public API.
  - `use lancedb::index::vector::IvfHnswPqIndexBuilder;` — workspace dep `lancedb = { version = "0.16", default-features = false }` at root `Cargo.toml:238`. `lancedb 0.16`'s `index::vector::IvfHnswPqIndexBuilder` is the canonical builder for `Index::IvfHnswPq(...)` with macro-generated `num_partitions(...)` setter (see RFR §"Disclosed deviations" item 3 for the `pub(crate)` field rationale).
  - `use lancedb::index::Index;` — `Index::IvfHnswPq(IvfHnswPqIndexBuilder)` enum variant per `lancedb 0.16` `src/index/mod.rs`.
  - `use lancedb::query::{ExecutableQuery, QueryBase};` — `lancedb 0.16` `query::ExecutableQuery` (`.execute()` trait) + `query::QueryBase` (`.nearest_to(...)` builder method) are the canonical traits the per-iter `b.iter` body invokes.
  - `use rand::rngs::StdRng;` and `use rand::{Rng, SeedableRng};` — dev-dep `rand = "0.8"` at `crates/ucil-embeddings/Cargo.toml:50`; all three symbols are `rand 0.8.5`'s public API for deterministic-seed PRNG.
  - `use tokio::runtime::Runtime;` — workspace dep `tokio` at root `Cargo.toml`; `tokio::runtime::Runtime::new()` + `Runtime::block_on(...)` is the canonical sync-bridge pattern for criterion's sync `b.iter` body wrapping `lancedb` async calls (per the bench's async-runtime-contract rustdoc).

  The verify script references `scripts/bench-vector-query.sh` (exists on this branch, 0755), and the criterion `target/criterion/vector_query_p95_warm/vector_query_p95_warm/new/sample.json` path is the documented criterion 0.5 nested `<group>/<bench>/new/` shape (matching WO-0061 estimates.json sibling).

- **Feature coverage**: `P2-W8-F07` (the only feature in the WO) is covered end-to-end by:
  1. The bench function `bench_vector_query_p95` at `crates/ucil-embeddings/benches/vector_query.rs:174` exercises real `lancedb 0.16` + IVF/HNSW PQ index over a 2000-row, 768-d synthetic corpus;
  2. The criterion `group.bench_function("vector_query_p95_warm", ...)` literal at `vector_query.rs:215` is the load-bearing identifier for criterion's output path AND the verify script's frozen-grep selector;
  3. The `criterion_group!`/`criterion_main!` pair at lines 240-247 wires `cargo bench --bench vector_query` to the function;
  4. `scripts/bench-vector-query.sh` parses criterion's emitted `sample.json`, computes per-sample `mean_ns_per_iter`, sorts ascending, takes `floor(0.95 * len)` for p95, and asserts `MEAN_NS_PER_ITER >= 100_000` + `p95_warm_ms < 100`;
  5. `scripts/verify/P2-W8-F07.sh` re-asserts `p95_warm_ms < 100` independently and prints the canonical `[OK] P2-W8-F07 p95_warm_ms=<N>` line.

  Verifier confirms via `bash scripts/verify/P2-W8-F07.sh` (executor's RFR row AC21 reports `[OK] P2-W8-F07 p95_warm_ms=0.296` locally, ~3 orders of magnitude below the 100 ms master-plan §18 line 1789 floor).

- **Commit-message trailers**: 5/6 commits have `Phase: 2 / Feature: P2-W8-F07 / Work-order: WO-0065`. The 6th commit (`f4adc41 test(effectiveness)`) carries `Phase: 1 / Feature: effectiveness-gate / Work-order: n/a` — see Warning 1 above; the trailers are valid Conventional Commits and the commit is not strictly within WO-0065's `feature_ids: ["P2-W8-F07"]` envelope. All 6 commits have `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>`. All 6 are Conventional Commits with valid types: `build` × 1, `feat` × 1, `chore` × 2, `docs` × 1, `test` × 1.

- **Commit-subject lengths**: 6/6 ≤ 70 chars (longest 65 chars: `chore(scripts): add bench-vector-query.sh + verify/P2-W8-F07.sh`). Per WO-0061 lessons line 645 ≤70 cap.

- **Allow-list discipline (forbidden-path scan)**: `git diff --name-only main..feat/WO-0065-vector-query-p95-bench` returns 7 paths, all OUTSIDE `forbidden_paths`:
  1. `Cargo.lock` (transitive deps from `lancedb`/`arrow`/`rand`)
  2. `crates/ucil-embeddings/Cargo.toml` (4 dev-deps + `[[bench]] name = "vector_query"`)
  3. `crates/ucil-embeddings/benches/vector_query.rs` (NEW, 323 LOC)
  4. `scripts/bench-vector-query.sh` (NEW, 0755, 162 LOC)
  5. `scripts/verify/P2-W8-F07.sh` (NEW, 0755, 90 LOC)
  6. `ucil-build/work-orders/0065-ready-for-review.md` (NEW, 306 LOC RFR)
  7. `ucil-build/verification-reports/effectiveness-phase-1.md` (Warning 1 — out-of-scope but not forbidden)

  Crucially: no diff under `crates/ucil-daemon/**`, `crates/ucil-treesitter/**`, `crates/ucil-cli/**`, `crates/ucil-core/**`, `adapters/**`, `ml/**`, `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`, `crates/ucil-embeddings/src/**`, `ucil-build/feature-list.json`, `ucil-build/feature-list.schema.json`, or `ucil-master-plan-v2.1-final.md`.

- **Public-API docs**: every new `pub` item in `crates/ucil-embeddings/benches/vector_query.rs` carries a rustdoc:
  - `pub const CORPUS_SIZE: usize = 2000;` — 14-line rustdoc (master-plan §12.2 line 1341 cite + WO-0061 mutation contract).
  - `pub const QUERY_COUNT: usize = 20;` — 11-line rustdoc (M2 mutation rationale + load-bearing freeze).
  - `pub const EMBEDDING_DIM: usize = 768;` — 7-line rustdoc (master-plan §12.2 line 1332 cite + load-bearing freeze).
  - The file-level `//!` rustdoc (89 lines) cites master-plan §4.2 + §12.2 + §18 + DEC-0016 + WO-0061 line 685 and documents the bench shape, frozen identifiers, mutation contract, and async runtime contract.

- **Cargo.lock churn**: +4 / -0 lines, all in the `ucil-embeddings` `dependencies` block (adding `arrow-array`, `arrow-schema`, `lancedb`, `rand 0.8.5` references). Zero new transitive crates introduced — every dep was already resolved transitively via `ucil-daemon`'s `lancedb` workspace dep + `tokenizers`'s and `lance`'s transitive `rand`. RFR §"Cargo.lock diff summary" confirms this. Consistent with WO-0058 line 569 + WO-0061 line 702 transitive-Cargo.lock-churn-acceptable precedent.

- **Frozen identifiers**: the four AC-frozen literals are present in source:
  - `pub const CORPUS_SIZE: usize = 2000;` — `vector_query.rs:80`.
  - `pub const QUERY_COUNT: usize = 20;` — `vector_query.rs:91`.
  - `pub const EMBEDDING_DIM: usize = 768;` — `vector_query.rs:101`.
  - `group.bench_function("vector_query_p95_warm", ...)` — `vector_query.rs:215`.
  - `StdRng::seed_from_u64(0x_C0DE_BABE)` — `vector_query.rs:118`.
  - `StdRng::seed_from_u64(0x_FACE_FEED)` — `vector_query.rs:133`.
  - `sample_size(100)` — `vector_query.rs:243`.
  - `warm_up_time(Duration::from_secs(2))` — `vector_query.rs:244`.
  - `measurement_time(Duration::from_secs(20))` — `vector_query.rs:245`.

- **Bench-script + verify-script frozen contract**: `scripts/bench-vector-query.sh` is `#!/usr/bin/env bash` + `set -euo pipefail` + `IFS=$'\n\t'` (lines 1-3); cd's via `git rev-parse --show-toplevel` (line 53); reads `target/criterion/vector_query_p95_warm/vector_query_p95_warm/new/sample.json` (lines 55-56); jq-extracts `times` + `iters` arrays + computes per-sample `mean_ns_per_iter[i] = times[i] / iters[i]`, sorts ascending, takes element at `floor(0.95 * len)` for p95 (lines 96-101 + 104-110); emits `p95_warm_ms=<N>` to stdout (line 134); asserts wall-time floor `MEAN_NS_PER_ITER >= 100_000` (lines 124-129) + p95 floor `< 100 ms` (lines 137-142). NO `install-coderankembed` substring (AC13 — confirmed via `git show <branch>:scripts/bench-vector-query.sh | grep -c 'install-coderankembed'` returning 0). `scripts/verify/P2-W8-F07.sh` is `#!/usr/bin/env bash` + `set -euo pipefail` + `IFS=$'\n\t'` (lines 1-3); greps `^[[:space:]]*group\.bench_function\("vector_query_p95_warm"` at the criterion call site (lines 50-56); INDEPENDENTLY parses `^p95_warm_ms=<N>$` from the bench-script's stdout (lines 71-79) and re-asserts `< 100 ms` (lines 82-87) — NOT a `set +e ; bench-script ; exit $?` shell, so M3 (bench-script p95 floor neutered) IS caught by inspection.

## Summary

`feat/WO-0065-vector-query-p95-bench` is **CLEAN**. All 32 acceptance criteria are addressed in the executor's RFR with concrete evidence and commit shas. Three pre-baked mutations (M1/M2/M3) are correctly delegated to verifier per WO-0061 line 690; the WO documents the literal edits, expected failure modes, and `git checkout --` restoration commands. The implementation exercises real `lancedb 0.16` + IVF/HNSW PQ index end-to-end with no mocks of critical collaborators. Frozen identifiers are load-bearing and present in source. Zero stubs, zero skipped tests, zero weak assertions, zero hallucinated imports. Cargo.lock churn is +4 / -0 lines with zero new transitive crates.

The seven warnings above are documented divergences with WO precedent — none are gating. Verifier should:
1. Confirm `protoc 25.1+` is available on the build host (Warning 6).
2. Apply M1, M2, M3 in turn, confirm expected failure modes, restore via `git checkout --`, confirm green again (Warning 5; M3 is verified primarily by inspecting `scripts/verify/P2-W8-F07.sh` lines 67-83).
3. Confirm `bash scripts/verify/P2-W8-F07.sh` exits 0 with `[OK] P2-W8-F07 p95_warm_ms=<N>` where `<N> < 100`.
4. Confirm `cargo test --workspace --no-fail-fast` exits 0 (no Phase 1 / Phase 2 regressions).
5. Confirm `bash scripts/gate/phase-1.sh` exits 0.
6. Optionally treat `f4adc41`'s `effectiveness-phase-1.md` modifications as a transient gate-side artefact independent of the P2-W8-F07 verification path (Warning 1).

**Cleared for verifier.**
