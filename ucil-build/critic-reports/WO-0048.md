---
work_order: WO-0048
feature: P2-W7-F02
branch: feat/WO-0048-g1-result-fusion
ref: d450ea540254a2e37dfa9c8188d1e879fc669c4f
critic_session: crt-WO-0048-2026-05-05
verdict: CLEAN
ready_for_verifier: true
---

# Critic Report: WO-0048 — g1-result-fusion

**Critic session**: `crt-WO-0048-2026-05-05`
**Branch**: `feat/WO-0048-g1-result-fusion`
**Ref**: `d450ea540254a2e37dfa9c8188d1e879fc669c4f`
**Diff range**: `main..feat/WO-0048-g1-result-fusion` (merge-base `a2c78a7`)
**Verdict**: **CLEAN** — ready for verifier.

## Diff summary

```
$ git diff --name-only main..feat/WO-0048-g1-result-fusion
crates/ucil-daemon/src/executor.rs
crates/ucil-daemon/src/lib.rs
scripts/verify/P2-W7-F02.sh
ucil-build/work-orders/0048-ready-for-review.md
```

4 files, 5 commits, +774 / −1 across the branch.

```
$ git log --oneline main..feat/WO-0048-g1-result-fusion
d450ea5 chore(work-order): mark WO-0048 ready for review
4fc1684 ci(verify): add scripts/verify/P2-W7-F02.sh
23ad2b0 test(daemon): add test_g1_result_fusion + lib.rs re-exports for fusion
67fd2eb feat(daemon): add fuse_g1 + authority_rank for G1 result fusion
11272a2 feat(daemon): add G1 fusion types (location, entry, conflict, outcome)
```

Allow-list grep — every diffed path matches the WO AC18 acceptance whitelist
(`crates/ucil-daemon/src/executor.rs`, `crates/ucil-daemon/src/lib.rs`,
`scripts/verify/P2-W7-F02.sh`, `ucil-build/work-orders/0048-ready-for-review.md`).
`git diff --name-only main..feat/WO-0048-g1-result-fusion -- '*.toml' 'Cargo.lock' 'tests/fixtures/**' 'ucil-build/feature-list.json' 'ucil-build/feature-list.schema.json' 'ucil-master-plan-v2.1-final.md'`
returns empty — every `forbidden_paths` entry untouched (no `Cargo.toml`,
no `Cargo.lock`, no `tests/fixtures/**`, no `crates/ucil-daemon/src/{lifecycle,
main,plugin_manager,priority_queue,server,session_manager,session_ttl,startup,
storage,test_support,text_search,understand_code,watcher}.rs`, no
`crates/ucil-daemon/tests/**`, no `feature-list*.json`, no master plan, no
decisions, no phase-log).

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Commit `23ad2b0` size — 297 insertions, ~95 LOC above the 200-LOC
   soft target.** `git log --shortstat main..feat/WO-0048-g1-result-fusion`
   reports 297 insertions (1 deletion) for the
   `test(daemon): add test_g1_result_fusion + lib.rs re-exports for fusion`
   commit. WO `acceptance` AC26 explicitly notes "DEC-0005 module-coherence
   permits up to ~200 LOC for a coherent test+helper commit (see WO-0046
   252-LOC framing precedent)." 297 LOC is one coherent module-root test
   (the frozen selector `test_g1_result_fusion` with five sub-assertions +
   a local `TestG1Source` impl + a `make_entry` helper) plus the lib.rs
   re-export line update + WO-0048 preamble paragraph. Bundling the test
   with its lib.rs surface honours the cumulative-debt-avoidance discipline
   (now 7 consecutive cleared) cited in the WO `plan_summary` line 12 — the
   alternative is a separate trailing commit for `lib.rs` that introduces
   a transient state where the new fn `fuse_g1` is reachable internally
   but not from `ucil_daemon::fuse_g1`. Soft warning, not a blocker;
   informational and within the spirit of DEC-0005's module-coherence
   carve-out (DEC-0005 itself permits 605-line + 293-line + 234-line
   commits when splitting would break compilation or strand dead code).

### OK (passing every critic gate)

#### 1. Stub detection — clean
`git show feat/WO-0048-g1-result-fusion:crates/ucil-daemon/src/executor.rs | grep -cE 'todo!\(\)|unimplemented!\(\)|panic!\(".*not yet|TODO|FIXME'`
returns `0`. The baseline on `main` is also `0`, so AC17 (zero new stub
hits) holds.
No `raise NotImplementedError` (Rust file, N/A). No `pass`-only bodies
(Rust file, N/A). No bare-literal stubs as feature implementations —
every constructor call returns a fully populated value:
  - `G1FusedOutcome` literal at `executor.rs:1442–1446` carries real
    `entries`, `master_timed_out`, `source_dispositions` derived from
    the input `outcome` — not a `Default::default()`.
  - `G1FusedEntry` literal at `executor.rs:1435–1440` carries real
    `location`, `contributing_sources`, `fields`, `conflicts`.
  - `G1Conflict` literal at `executor.rs:1422–1427` carries real
    `field`, `winner`, `winner_value`, `losers`.
  - The only `Default::default()` reference in the diff is in the WO's
    AC21 mutation patch documentation (`ucil-build/work-orders/0048-ready-for-review.md`),
    NOT in shipped code. `derive(Default)` on `G1FusedLocation`,
    `G1FusedEntry`, `G1FusedOutcome` is additive (only consumed by the
    verifier's mutation patch) and the rustdoc on `G1FusedLocation`
    explicitly cites this transitively-required derive.
  - `panic!` count: branch=5, main=3 → 2 new panics, both inside
    `#[cfg(test)]` test fns (`make_entry`'s "fields argument must be a
    JSON object" arm at `executor.rs:2424`, plus the existing
    `expected UnsupportedExtension` arm at `:1713`). Neither matches
    the AC17 `panic!\(".*not yet` pattern.
  - `.expect(...)` count in the diff: 3 occurrences, all inside the
    `#[cfg(test)] pub async fn test_g1_result_fusion` body
    (`executor.rs:2408`, `:2505`, `:2613`). Per `.claude/rules/rust-style.md`
    "Never `.unwrap()` / `.expect()` outside `#[cfg(test)]`" — honoured.
    Production `fuse_g1` itself uses zero `.unwrap` / `.expect` and
    handles `serde_json::from_value` errors via a `match` that logs
    via `tracing::warn!` and continues (per the partial-results
    semantic from master-plan §5.1 line 445).

#### 2. Mocked critical dependencies — clean
`git diff main..feat/WO-0048-g1-result-fusion -- 'crates/ucil-daemon/src/executor.rs' 'crates/ucil-daemon/src/lib.rs' 'scripts/verify/P2-W7-F02.sh' | grep -iE '^\+.*\b(mock|fake|stub|MagicMock|Mockall)\b' | grep -iE 'serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker'`
returns no matches.

The new `TestG1Source` impl at `executor.rs:2401–2419` is a local impl of
UCIL's own `G1Source` trait (introduced in WO-0047 at
`executor.rs:966`). Per **DEC-0008 §4** a UCIL-owned dependency-inversion
seam is NOT a critical-dep mock; it is the same shape as the pre-existing
`SerenaHoverClient` pattern (`executor.rs:645`) the critic explicitly
cleared in WO-0037 and re-cleared in WO-0047 for `G1Source` itself. The
WO-0048 plan_summary line 12 + scope_in line 22 both cite DEC-0008 + the
WO-0047 lessons line 313 confirmation that local `G1Source` impls in tests
are NOT critical-dep mocks.

The `G1ToolKind::Serena`, `G1ToolKind::AstGrep`, `G1ToolKind::TreeSitter`,
`G1ToolKind::Diagnostics` enum variant *names* reference real
production-wiring targets, but the test uses local `TestG1Source` impls
that never instantiate Serena, an LSP, SQLite, LanceDB, or Docker —
production wiring of the four sources stays deferred to P2-W7-F05
(`find_references`) per the WO `scope_out` line 30 + WO-0047 lessons
line 298 + line 318.

The verify script (`scripts/verify/P2-W7-F02.sh`) shells out to
`cargo test`, `git rev-parse --show-toplevel`, `grep`, `tee`, `tail`,
optional `shellcheck` — none of these are critical-dep mocks; all are
standard test-runner wiring.

#### 3. Skipped / ignored tests — clean
`git diff main..feat/WO-0048-g1-result-fusion | grep -nE '^\+.*#\[ignore\]|\.skip\(|xfail|@pytest\.mark\.skip|@pytest\.mark\.xfail'`
returns no matches. The acceptance test at `executor.rs:2391` is annotated
only with `#[cfg(test)]`,
`#[allow(clippy::too_many_lines, clippy::missing_panics_doc)]`, and
`#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` — no
`#[ignore]`, no `#[cfg(ignore)]`, no `.skip(` invocation.

The verify script contains no `--skip`, `--ignored`, or `--exclude` test
flags. The single occurrence of "skip" in the script is the informational
log line `[INFO] P2-W7-F02: shellcheck not on PATH; skipping lint.` —
about the optional lint, not the test selector.

#### 4. Weak assertions — clean
`git diff main..feat/WO-0048-g1-result-fusion | grep -nE '^\+.*assert!\(true\)|assert_eq!\(true,\s*true\)|assert!\(1\b|assert_eq!\(1,\s*1\)'`
returns no matches. Every assertion in `test_g1_result_fusion` is
non-trivial and includes an operator-actionable panic message that
quotes the actual value:

  - **Sub-assertion 1 (location merge, AC05)** at `executor.rs:2486–2493`:
    `assert_eq!(fused.entries.len(), 2, "...; got {}: entries={:?}", ...)`.
    Pinned to `2`; on failure prints both the count and the full
    `fused.entries` debug dump.
  - **Sub-assertion 2 (location.file_path)** at `executor.rs:2500–2503`:
    `assert_eq!(entry_at_10_20.location.file_path, PathBuf::from("util.rs"), ...)`.
  - **Sub-assertion 2 (field union)** at `executor.rs:2510–2515`:
    `assert_eq!(keys, vec!["ast_kind", "hover_doc", "pattern", "signature"], "...; got fields={:?}", ...)`.
    Pinned to the exact 4-key superset.
  - **Sub-assertion 2 (contributing_sources order)** at
    `executor.rs:2517–2526`: `assert_eq!(entry_at_10_20.contributing_sources, vec![G1ToolKind::Serena, G1ToolKind::TreeSitter, G1ToolKind::AstGrep], ...)`.
    Pinned to the authority-ordered triple.
  - **Sub-assertion 3 (signature winner, AC07)** at `executor.rs:2530–2535`:
    `assert_eq!(entry_at_10_20.fields.get("signature"), Some(&serde_json::json!("fn foo() -> i32")), ...)`.
    Pinned to Serena's value, NOT AstGrep's.
  - **Sub-assertion 3 (conflict count)** at `executor.rs:2537–2543`:
    `assert_eq!(entry_at_10_20.conflicts.len(), 1, ...)`. Exactly one
    conflict (only `signature` had multi-source contributors with
    non-equal values).
  - **Sub-assertion 3 (conflict.field)** at `executor.rs:2545–2549`:
    `assert_eq!(conflict.field, "signature", ...)`.
  - **Sub-assertion 3 (conflict.winner)** at `executor.rs:2551–2555`:
    `assert_eq!(conflict.winner, G1ToolKind::Serena, ...)`.
  - **Sub-assertion 3 (conflict.winner_value)** at `executor.rs:2557–2561`:
    `assert_eq!(conflict.winner_value, serde_json::json!("fn foo() -> i32"), ...)`.
  - **Sub-assertion 3 (conflict.losers)** at `executor.rs:2563–2567`:
    `assert_eq!(conflict.losers, vec![(G1ToolKind::AstGrep, serde_json::json!("fn foo()"))], ...)`.
  - **Sub-assertion 4 (disposition count, AC08)** at `executor.rs:2571–2577`:
    `assert_eq!(fused.source_dispositions.len(), 4, ...)`.
  - **Sub-assertion 4 (disposition status)** at `executor.rs:2579–2585`:
    `assert_eq!(*status, G1ToolStatus::Available, ...)` inside a `for`
    loop with the panic message capturing both `kind` and `status`.
  - **Sub-assertion 4 (disposition input order)** at
    `executor.rs:2589–2596`: `assert_eq!(kinds_in_order, vec![TreeSitter, Serena, AstGrep, Diagnostics], ...)`.
  - **Sub-assertion 4 (master_timed_out)** at `executor.rs:2598–2601`:
    `assert!(!fused.master_timed_out, ...)` — pinned to `false`, not a
    tautology.
  - **Sub-assertion 5 (Diagnostics-only entry)** at `executor.rs:2603–2625`:
    three additional `assert_eq!`s on `(util.rs, 30, 35)`'s
    `contributing_sources`, `diagnostic` field, and empty `conflicts`.

No bool-on-bool tautologies. No `matches!()` where `assert_eq!` would
work (per WO-0042 lessons line 92, transferred via WO-0048 scope_in
line 22).

#### 5. Hallucinated imports — clean
The only new top-level `use` statement in the diff:

  - `std::collections::BTreeMap` (`executor.rs:70`) — std lib, stable.

`#[async_trait::async_trait]` on `TestG1Source` is provided by
`async-trait = { workspace = true }` already declared in
`crates/ucil-daemon/Cargo.toml` (existing dep, no new addition — WO AC19
honoured: `git diff --name-only main..feat/WO-0048-g1-result-fusion -- '*.toml'`
returns empty). `#[tracing::instrument]` and `tracing::warn!` are
provided by `tracing` already declared. `serde::Serialize`,
`serde::Deserialize`, `serde_json::Value`, `serde_json::Map`,
`serde_json::json!`, `serde_json::from_value`, `serde_json::to_value`
are all provided by the existing `serde` / `serde_json` deps.

The lib.rs `pub use executor::{...}` line at
`crates/ucil-daemon/src/lib.rs:121` re-exports all 6 new public
symbols (`fuse_g1`, `G1Conflict`, `G1FusedEntry`, `G1FusedLocation`,
`G1FusedOutcome`, `G1FusionEntry`); each target exists in
`crates/ucil-daemon/src/executor.rs` at lines 1369, 1236, 1257, 1196,
1282, 1217 respectively. AC20 (zero deferred re-export debt — now 7
consecutive cleared per WO-0042/0043/0044/0045/0046/0047/0048) is
honoured.

Within-block alphabetical ordering on the re-export line is preserved:
fns (`enrich_find_definition, execute_g1, fuse_g1`) precede types
(`Caller, EnrichedFindDefinition, ExecutorError, G1Conflict, G1FusedEntry,
G1FusedLocation, G1FusedOutcome, G1FusionEntry, G1Outcome, G1Query,
G1Source, G1ToolKind, G1ToolOutput, G1ToolStatus, HoverDoc, HoverFetchError,
HoverSource, IngestPipeline, SerenaHoverClient`) precede consts
(`G1_MASTER_DEADLINE, G1_PER_SOURCE_DEADLINE, SOURCE_TOOL,
TREE_SITTER_VALID_FROM`) — ordering inside each block is alphabetical.

#### 6. Feature coverage — clean
WO-0048 declares `feature_ids: ["P2-W7-F02"]`. The diff adds **one**
module-root `#[cfg(test)] #[tokio::test(flavor = "multi_thread", worker_threads = 4)]`
selector — `pub async fn test_g1_result_fusion` at
`crates/ucil-daemon/src/executor.rs:2391` — that covers the four
sub-slices of the P2-W7-F02 contract:

| Sub-assertion | Slice covered | WO acceptance bullet |
|---------------|---------------|----------------------|
| (1) Location merge | `fused.entries.len() == 2` (3 sources at one location, 1 at another) | AC05 |
| (2) Field union | 4-key superset; `contributing_sources` authority-ordered | AC06 |
| (3) Authority resolution | Serena's `signature` wins; `G1Conflict` records AstGrep's losing value; `ast_kind` (single contributor) records NO conflict | AC07 |
| (4) Disposition pass-through | `source_dispositions.len() == 4`, all `Available`, input order, `master_timed_out == false` | AC08 |
| (5) Diagnostics-only entry (extra) | `(util.rs, 30, 35)` carries `[Diagnostics]` only, `diagnostic` field verbatim, empty `conflicts` | beyond WO checklist (cheap insurance) |

Module-root placement verified: `git show feat/WO-0048-g1-result-fusion:crates/ucil-daemon/src/executor.rs | grep -nE '^pub async fn test_g1_result_fusion'`
returns `2391:pub async fn test_g1_result_fusion() {` — at column 0,
NOT inside any `mod tests {}` block. The only `mod` declaration in the
file (`mod fake_serena_hover_client { … }` at `:1571–1633`) ends well
before line 2391, and it does NOT contain the test fn. Per **DEC-0007**
the frozen `feature-list.json` selector
`-p ucil-daemon executor::test_g1_result_fusion` resolves directly to
`ucil_daemon::executor::test_g1_result_fusion` without a `tests::`
intermediate. AC04 honoured.

The `feature-list.json` entry for P2-W7-F02 (acceptance_tests
`-p ucil-daemon executor::test_g1_result_fusion`) matches the selector
verbatim — verified via `grep -A 16 '"P2-W7-F02"' ucil-build/feature-list.json`.

#### 7. Commit hygiene — clean
All 5 commits in `git log main..feat/WO-0048-g1-result-fusion`:

  - `11272a2` — `feat(daemon): add G1 fusion types (location, entry, conflict, outcome)` (131 insertions)
  - `67fd2eb` — `feat(daemon): add fuse_g1 + authority_rank for G1 result fusion` (193 insertions)
  - `23ad2b0` — `test(daemon): add test_g1_result_fusion + lib.rs re-exports for fusion` (297 insertions, 1 deletion; see Warning #1)
  - `4fc1684` — `ci(verify): add scripts/verify/P2-W7-F02.sh` (70 insertions)
  - `d450ea5` — `chore(work-order): mark WO-0048 ready for review` (83 insertions)

  - **Conventional Commits subjects**: ✓ all 5 commits use
    `<type>(<scope>): <imperative summary>` per `.claude/rules/commit-style.md`
    (`feat(daemon)`, `test(daemon)`, `ci(verify)`, `chore(work-order)`).
    All subjects ≤ 70 chars.
  - **`Phase: 2` trailer**: ✓ all 5 commits.
  - **`Feature: P2-W7-F02` trailer**: ✓ all 5 commits.
  - **`Work-order: WO-0048` trailer**: ✓ all 5 commits.
  - **`Co-Authored-By: Claude Opus 4.6 (1M context)` footer**: ✓ all 5
    commits.
  - **No `--amend` after push**: ✓ — branch
    `feat/WO-0048-g1-result-fusion` is the canonical branch and
    `d450ea5` is its tip; no rebase / amend artefacts in
    `git log --oneline main..HEAD`.
  - **No force-push artefacts**: ✓ — `git log main..HEAD` is linear and
    contiguous. `d450ea5` is the published HEAD on
    `origin/feat/WO-0048-g1-result-fusion`.
  - **No `--no-verify`**: ✓ — no signed-off-as-bypass markers.
  - **Commit cadence**: 4 source-touching commits + 1 chore (review-marker)
    commit = 5 total. WO AC26 minimum is "at least 3 commits"; honoured
    with margin. The 4 source-touching commits map cleanly to the
    estimated_commits=5 plan: types / `fuse_g1` / test+lib.rs re-exports /
    verify script / ready-for-review.
  - **Commit-size sanity**: 4 of 5 commits are ≤ 193 LOC (well within
    DEC-0005 module-coherence). One commit (297 LOC) exceeds the
    200-LOC soft target; flagged as Warning #1 above and is explicitly
    authorised by the WO's DEC-0005 carve-out (test + lib.rs re-exports
    bundled to honour the cumulative-debt-avoidance discipline).

#### 8. Doc + public API — clean
The diff introduces **6 new `pub` items** in
`crates/ucil-daemon/src/executor.rs`:

| Item | File:line | Doc comment |
|------|-----------|-------------|
| `pub struct G1FusedLocation` | `executor.rs:1196` | ✓ `:1167–1183` (struct doc cites master-plan §5.1 line 430); per-field docs at `:1197–1205` |
| `pub struct G1FusionEntry` | `executor.rs:1217` | ✓ `:1207–1216` (struct doc cites P2-W7-F05 deferral); per-field docs at `:1218–1224` |
| `pub struct G1Conflict` | `executor.rs:1236` | ✓ `:1225–1235` (struct doc); per-field docs at `:1237–1245` |
| `pub struct G1FusedEntry` | `executor.rs:1257` | ✓ `:1247–1256` (struct doc cites WO-0046 lessons + AC21 mutation rationale); per-field docs at `:1258–1268` |
| `pub struct G1FusedOutcome` | `executor.rs:1282` | ✓ `:1269–1281` (struct doc); per-field docs at `:1283–1290` |
| `pub fn fuse_g1` | `executor.rs:1369` | ✓ `:1303–1361` (incl. `# Examples` block with `no_run`-gated 2-source fusion case) |

The private `fn authority_rank` at `executor.rs:1300` is `const fn`
(per WO-0048 ready-for-review note #3 — satisfies
`clippy::missing_const_for_fn`) and carries an `:1292–1300` doc comment
citing the master-plan source-authority list and the exhaustive-`match`
compile-time guarantee.

The `pub async fn test_g1_result_fusion` at `executor.rs:2391` is
`#[cfg(test)]`-gated, so its visibility is irrelevant for production
rustdoc — but it carries a full doc comment regardless (`:2371–2389`)
enumerating the four named sub-assertions.

The lib.rs preamble paragraph at `lib.rs:74–81` documents WO-0048's
contribution and cites the F05 deferral note — the load-bearing
signpost the WO `plan_summary` line 12 explicitly required (per WO-0045
lessons line 218 + WO-0047 lessons line 298). Backticked links to
`[`executor::fuse_g1`]` and `[`executor::G1Outcome`]` resolve cleanly
via `#![deny(rustdoc::broken_intra_doc_links)]` (relied on by the WO's
AC02 clippy gate).

The `# Examples` rustdoc block on `fuse_g1` at `executor.rs:1336–1361`
is `no_run`-gated (compiles but not executed in `cargo test`), which
avoids needing a real `Vec<Box<dyn G1Source>>` fixture in doc-tests.
Every uppercase identifier in the new rustdoc lives inside backticks
(e.g. `` `G1Outcome` ``, `` `G1FusedOutcome` ``, `` `G1ToolStatus` ``,
`` `BTreeMap` ``, `` `HashMap` ``, `` `HashMap`-keyed ``,
`` `Default` ``, `` `MUST` `` (the SHOULD/MUST keywords also backticked))
per the AC02 pre-flight grep — no `clippy::doc_markdown` hits anticipated.

The executor's ready-for-review marker reports
`cargo clippy -p ucil-daemon --all-targets -- -D warnings → exit 0`,
confirming the doc-markdown pre-flight is satisfied.

## Verifier checklist

Verifier should run (per WO-0048 acceptance_criteria, with optional
verifier-extension on a third symmetric mutation per WO-0045 lessons
line 199):

```
# 1. Existence + grep gates (mechanical, runs in seconds)
test -f crates/ucil-daemon/src/executor.rs
test -f scripts/verify/P2-W7-F02.sh
grep -nE '^pub async fn test_g1_result_fusion' crates/ucil-daemon/src/executor.rs | grep -v 'mod tests'
grep -cE 'fuse_g1|G1Conflict|G1FusedEntry|G1FusedLocation|G1FusedOutcome|G1FusionEntry' crates/ucil-daemon/src/lib.rs   # = 6 (re-export line)

# 2. Build + clippy + test (clean state)
cargo clean
env -u RUSTC_WRAPPER cargo build -p ucil-daemon
env -u RUSTC_WRAPPER cargo clippy -p ucil-daemon --all-targets -- -D warnings
env -u RUSTC_WRAPPER cargo test -p ucil-daemon executor::test_g1_result_fusion -- --nocapture

# 3. WO-0047 G1-orchestrator regression
env -u RUSTC_WRAPPER cargo test -p ucil-daemon executor::test_g1_parallel_execution -- --nocapture

# 4. Phase-2 W6 regression sentinels (each as separate cargo test invocation
#    because cargo test only accepts ONE positional arg per invocation —
#    per executor's ready-for-review note on AC10)
for t in test_hot_cold_lifecycle test_manifest_parser test_lifecycle_state_machine test_hot_reload test_circuit_breaker; do
    env -u RUSTC_WRAPPER cargo test -p ucil-daemon "plugin_manager::${t}"
done
env -u RUSTC_WRAPPER cargo test -p ucil-daemon --test plugin_manager
unset UCIL_SKIP_EXTERNAL_PLUGIN_TESTS
env -u RUSTC_WRAPPER cargo test -p ucil-daemon --test plugin_manifests
env -u RUSTC_WRAPPER cargo test --test test_plugin_lifecycle

# 5. Phase-1 regression sentinels
env -u RUSTC_WRAPPER cargo test -p ucil-daemon --test e2e_mcp_stdio --test e2e_mcp_with_kg
env -u RUSTC_WRAPPER cargo test --test test_lsp_bridge

# 6. Workspace pre-flight
env -u RUSTC_WRAPPER cargo test --workspace --no-fail-fast

# 7. Coverage gate (manual workaround per WO-0046 lessons line 270 — now
#    11th consecutive WO using the env -u RUSTC_WRAPPER protocol per the
#    standing escalation 20260419-0152-monitor-phase1-gate-red-integration-gaps.md)
bash scripts/verify/coverage-gate.sh ucil-daemon 85 75 || \
  ( env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --summary-only ; \
    LLVM_PROFDATA="$(rustc --print target-libdir)/../bin/llvm-profdata" ; \
    find target/llvm-cov-target -name '*.profraw' -size 0 -delete ; \
    find target/llvm-cov-target -name '*.profraw' -print0 | while IFS= read -r -d '' f; do "$LLVM_PROFDATA" show "$f" >/dev/null 2>&1 || rm -f "$f"; done ; \
    env -u RUSTC_WRAPPER cargo llvm-cov report --package ucil-daemon --summary-only )

# 8. Pre-baked mutation A (fusion body neutered, AC21) — runtime-only variant
#    Per WO scope_in line 25 + ready-for-review marker.  Add
#    `#[allow(unreachable_code)]` to fuse_g1, then prepend
#    `let _ = outcome; return G1FusedOutcome::default();` as the first
#    executable statement.  Run
#    `cargo test -p ucil-daemon executor::test_g1_result_fusion`.
#    Expect: panic at AC05 location-merge assertion (executor.rs:2486)
#    `expected 2 fused entries; got 0: entries=[]`. Restore via
#    `git checkout -- crates/ucil-daemon/src/executor.rs`.

# 9. Pre-baked mutation B (authority resolution disabled, AC22)
#    Literal sed: `sed -i 's|G1ToolKind::Serena => 0,|G1ToolKind::Serena => 3,|' crates/ucil-daemon/src/executor.rs`
#    Run `cargo test -p ucil-daemon executor::test_g1_result_fusion`.
#    Per WO scope_in line 26 the WO predicted panic at AC07 (signature
#    value mismatch), but the executor's ready-for-review marker
#    observed the panic actually fires earlier at sub-assertion 2
#    (`contributing_sources` order — Serena now last instead of first
#    once its rank is 3 ≥ AstGrep's 2).  EITHER panic catches the
#    mutation; both prove `authority_rank` is load-bearing.
#    Restore via `git checkout -- crates/ucil-daemon/src/executor.rs`.

# 10. Optional verifier-extension (per WO-0045 lessons line 199):
#     Replace the `let mut by_field: BTreeMap<String, ...>` with
#     `let mut by_field: BTreeMap<String, ...> = BTreeMap::new();` and
#     SKIP the inner per-field collection loop (no-op the body).
#     Expect: panic at AC06 (field union — `keys` returns empty).
#     Symmetric coverage on the per-group-fusion step.
#     Restore via `git checkout -- crates/ucil-daemon/src/executor.rs`.

# 11. End-to-end driver
bash scripts/verify/P2-W7-F02.sh    # → [OK] P2-W7-F02

# 12. Allow-list invariants (three-dot diff per WO-0046 lessons line 250)
[ "$(git diff --name-only main...HEAD | sort)" = "$(printf 'crates/ucil-daemon/src/executor.rs\ncrates/ucil-daemon/src/lib.rs\nscripts/verify/P2-W7-F02.sh\nucil-build/work-orders/0048-ready-for-review.md\n' | sort)" ]
[ -z "$(git diff --name-only main...HEAD -- '*.toml')" ]
[ -z "$(git diff --name-only main...HEAD -- 'tests/fixtures/**')" ]
[ -z "$(git diff --name-only main...HEAD -- 'ucil-build/feature-list.json' 'ucil-build/feature-list.schema.json')" ]
[ -z "$(git diff --name-only main...HEAD -- 'ucil-master-plan-v2.1-final.md')" ]
```

## Summary

CLEAN — no blockers. One soft warning (commit `23ad2b0` at 297 LOC,
~95 LOC over the 200-LOC soft target; bundles the `test_g1_result_fusion`
selector with its lib.rs re-export surface to honour the
cumulative-debt-avoidance discipline now 7 consecutive cleared per
DEC-0005 module-coherence). Verifier may proceed in a fresh session;
pre-baked mutation patterns are documented in the ready-for-review
marker with both literal-sed and runtime-only variants for the
`#![deny(warnings)]` cascade case (per WO-0046 lessons line 245).

— UCIL critic
