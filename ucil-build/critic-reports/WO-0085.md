# Critic Report: WO-0085

**Critic session**: crt-2026-05-09-WO-0085
**Branch**: `feat/WO-0085-g7-quality-pipeline-foundation`
**Final commit**: `80cbed347fbf471aa699c786327e908cfb603207`
**Merge base**: `e1fc691f5daf1e897cfd17637d914e5108a5d7a5`
**Verdict**: **CLEAN** — no blockers. Soft warnings noted below.

## Diff scope

`git diff --stat e1fc691..feat/WO-0085-g7-quality-pipeline-foundation`:

| File | Change |
|------|--------|
| `crates/ucil-daemon/src/g7.rs` | +1273 (new file) |
| `crates/ucil-daemon/src/lib.rs` | +1 (`pub mod g7;`) |
| `crates/ucil-lsp-diagnostics/src/quality_pipeline.rs` | +528 / -25 |
| `scripts/verify/P3-W11-F01.sh` | +51 (new) |
| `scripts/verify/P3-W11-F05.sh` | +42 (new) |
| `scripts/verify/P3-W11-F06.sh` | +37 (new) |
| `ucil-build/work-orders/0085-ready-for-review.md` | +97 (new) |
| **Total** | 7 files, +2004 / -25 |

Commit graph (4 commits, zero merges):

```
80cbed3 chore(wo): WO-0085 ready-for-review marker
7d26f0d chore(verify): scripts for P3-W11-F01/F05/F06
04954b4 feat(lsp-diagnostics): persist quality_issues with upsert and soft-delete
7beb568 feat(daemon): add g7 quality pipeline orchestrator and severity merge
```

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix; not blocking)

1. **Commit subject 73 chars** (3 over the 70-char soft target from `.claude/rules/commit-style.md`):
   `feat(lsp-diagnostics): persist quality_issues with upsert and soft-delete` (`04954b4`).
   The other 3 subjects are within budget (42 / 45 / 69 chars).
   Cite: WO scope_in #21 calls 70 chars a soft target. Not blocking.

2. **`unwrap()` in production code** at `crates/ucil-daemon/src/g7.rs:650`:
   `let highest_severity = group.iter().map(|i| i.severity).min().unwrap();`
   `.claude/rules/rust-style.md` says "Never `.unwrap()` / `.expect()` outside `#[cfg(test)]`. Use `?` and a typed error."
   The author addresses this with a `# Panics` rustdoc block at `g7.rs:618-625` explicitly documenting unreachability (`BTreeMap::entry().or_default().push(issue)` ensures every group is non-empty) and ties the literal `.unwrap()` shape to the M2 mutation contract (a single-token swap to `.max()`).
   Not disclosed as a rule deviation in the RFR's `## Disclosed deviations` section. Suggest the RFR add a one-line cite of the `# Panics` justification + M2 contract anchoring so the verifier sees the deliberate choice.

3. **`expect()` in production code** at `crates/ucil-daemon/src/g7.rs:422`:
   `.map(|r| r.expect("join_all_g7: every slot must be filled before returning"))`
   This mirrors the g3.rs precedent verbatim — `crates/ucil-daemon/src/g3.rs:376`:
   `.map(|r| r.expect("join_all_g3: every slot must be filled before returning"))`
   Same justification (the `poll_fn` keeps spinning until every slot is `Some`). Consistent with shipped code on `main`. Not blocking; flagged only because the RFR does not cite the precedent.

4. **Doc rot in pre-existing comment** at `crates/ucil-lsp-diagnostics/src/quality_pipeline.rs:107`:
   `"future Phase-1 work-orders will extend this enum"` — this is Phase 3 work. The line was carried forward from the original `WO-0016` comment unchanged. Minor; safe to leave or fix in follow-up.

5. **Test-side word `Fake` carve-out** (informational, not blocking): `quality_pipeline.rs` reuses the pre-existing `mod fake_serena_client` from `WO-0048` and the `ScriptedFakeSerenaClient` type — all 24 occurrences confirmed inside `#[cfg(test)]`. The WO's scope_in #19 explicitly carves this out under WO-0048 line 363's `#[cfg(test)]` carve-out + WO-0069 §executor lessons. The `SerenaClient` trait is the UCIL-internal dependency-inversion seam (DEC-0008 §4) — the `Fake` impl is not a mock of an external wire format.

### OK (passing checks)

#### 1. Stub detection ✓
- `grep -nE 'todo!\(\)|unimplemented!\(\)|NotImplementedError'` on `g7.rs` and `quality_pipeline.rs` — **no matches**.
- No `pass`-only Python bodies (zero Python in this WO).
- No bare-literal-default function bodies.

#### 2. Mocked critical dependencies ✓
- **Serena**: production code does not call `serena` directly; the `SerenaClient` trait (`crates/ucil-lsp-diagnostics/src/diagnostics.rs:145`) is the UCIL-internal dependency-inversion seam per DEC-0008 §4. Test-side `ScriptedFakeSerenaClient` (line 629) implements that UCIL trait — not a mock of Serena's MCP wire format. Pre-existing pattern from WO-0048; carve-out documented in WO scope_in #19.
- **LSP servers**: not invoked directly in this WO. `quality_pipeline.rs` consumes already-fetched `lsp_types::Diagnostic` values via `DiagnosticsClient`.
- **SQLite (rusqlite)**: REAL — tests open a fresh on-disk `KnowledgeGraph` via `tempfile::TempDir` + `KnowledgeGraph::open(...)` (`quality_pipeline.rs:727`). All SELECTs/INSERTs/UPDATEs/DELETEs run against real `sqlite3`.
- **LanceDB**: not touched by this WO.
- **Docker**: not used (these are unit-tier tests; integration-tier docker fixtures are out of scope).
- Word-ban scrub on production-side `g7.rs` (lines 1–733, before the first `#[cfg(test)]` block at line 734): zero `mock|fake|stub` matches.

#### 3. Skipped / ignored tests ✓
- `grep -nE '#\[ignore\]|\.skip\(|xfail|#\[tokio::test\(ignore\)\]'` on the changed files — **no matches**.
- No `#[allow(dead_code)]` masking unused tests; the lone `#[allow(dead_code)]` on `join_all_g7` (g7.rs:389) is explicitly tied to the M1 mutation contract (mirrors WO-0070 line 192 precedent) and `join_all_g7` IS referenced by `execute_g7`.

#### 4. Weak assertions ✓
- `grep -nE 'assert!\(true\)|assert_eq!\(true,\s*true\)|expect\(true\)|assert_eq!\(1,\s*1\)'` — **no matches**.
- All assertions carry SA-numbered panic bodies (33 `(SAn)` cites in `g7.rs`, 20 in `quality_pipeline.rs`'s new test) per DEC-0007 + WO-0066/0067/0068/0070/0083/0084 mature pattern.
- Asserts compare meaningful values (`outcome.results[1].status == TimedOut`, `merged.severity == Critical`, `first_seen == first_seen_a`, `count_b == 1`, etc.).

#### 5. Hallucinated paths / imports ✓
Verified each import on the branch:
- `ucil_core::{KnowledgeGraph, KnowledgeGraphError}` → `crates/ucil-core/src/knowledge_graph.rs:622, 65` ✓
- `crate::diagnostics::{DiagnosticsClient, DiagnosticsClientError, SerenaClient}` → `crates/ucil-lsp-diagnostics/src/diagnostics.rs:206, 98, 145` ✓
- `crate::types::Language` → `crates/ucil-lsp-diagnostics/src/types.rs:45` ✓
- `lsp_types::{Diagnostic, DiagnosticSeverity, NumberOrString, Url, Position, Range, …}` — workspace dep, present in `Cargo.toml`.
- `tempfile::TempDir` — workspace test-dep, used in pre-existing tests in the same module.
- `serde::{Deserialize, Serialize}`, `async_trait::async_trait`, `tokio::time::timeout`, `thiserror::Error` — all already-present workspace deps.
- `crate::g3::G3_PER_SOURCE_DEADLINE` referenced via intra-doc-link only (`g7.rs:87`), and `g3.rs` exists.
- `lib.rs` `pub mod g7;` placed alphabetically between `pub mod g4;` (line 161) and `pub mod lancedb_indexer;` (line 162) — verified via `git diff`.

#### 6. Feature coverage ✓
- **P3-W11-F01** (G7Source trait + execute_g7): `pub async fn test_g7_parallel_pipeline` at `g7.rs:737`, 6 SA-numbered assertions covering result-count / master-deadline / Available / TimedOut / Errored / parallelism.
- **P3-W11-F05** (severity-weighted merge): `pub fn test_g7_severity_merge` at `g7.rs:941`, 6 SA-numbered scenarios covering empty / single / mixed-severity / cross-group / tied-severity / sentinel-vocabulary.
- **P3-W11-F06** (quality_issues table tracking): `pub async fn test_quality_issues_tracking` at `quality_pipeline.rs:1090`, 6 SA-numbered scenarios covering first-INSERT / re-observation-UPDATE / last_seen-advance / resolve-transition / soft-delete-past-retention / soft-delete-within-retention.
- All three frozen tests are at module root (NOT inside `mod tests {}`) per DEC-0007.
- Selector substring-match resolves uniquely for each: confirmed by inspection (no `tests::` namespace prefix).

#### 7. Commit hygiene ✓
- 4 commits, zero merge commits (`git log feat/... ^main --merges` returns 0 lines).
- Every commit has a Conventional Commits subject in the form `<type>(<scope>): <summary>` — types used: `feat` (×2), `chore` (×2).
- Every commit body contains:
  - `Phase: 3` ✓
  - `Feature: P3-W11-F0X` (per-feature) ✓
  - `Work-order: WO-0085` ✓
  - `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>` ✓
- 3 of 4 subject lines ≤70 chars (one is 73 — see Warning #1 above).
- DEC-0005 module-coherence carve-out invoked for the 1274-LOC `feat(daemon)` commit (`g7.rs` orchestrator + types + trait + helpers + module-root tests + `TestG7Source`), and the 503-LOC `feat(lsp-diagnostics)` commit (UPSERT + soft-delete + `test_quality_issues_tracking`). Splitting either further would produce stub-shaped intermediate states.
- No `--amend` / `--no-verify` / `--force` indicators in the commit log.
- All commits push-able from local (RFR claims pushed; not in critic scope to verify remote ref state).

#### 8. Doc + public API ✓
Every new `pub` item has rustdoc with a `///` block. Verified by inspection of each line:
- `g7.rs`:
  - `pub const G7_PER_SOURCE_DEADLINE` (L90) — rustdoc L76-89 ✓
  - `pub const G7_DEFAULT_MASTER_DEADLINE` (L104) — rustdoc L92-103 ✓
  - `pub struct G7Query` (L116) + 2 fields — rustdoc L108-114 + per-field ✓
  - `pub enum Severity` (L140) + variants — rustdoc L126-137 + per-variant + `as_str` ✓
  - `pub struct G7Issue` (L184) — rustdoc L177-182 + per-field ✓
  - `pub enum G7SourceStatus` (L218) — rustdoc L210-216 + per-variant ✓
  - `pub struct G7SourceOutput` (L230) + fields — rustdoc L228 + per-field ✓
  - `pub struct G7Outcome` (L255) + fields — rustdoc L248-253 + per-field ✓
  - `pub struct MergedG7Issue` (L275) + fields — rustdoc L265-273 + per-field ✓
  - `pub trait G7Source` (L322) + methods — rustdoc L303-320 + per-method ✓
  - `pub async fn execute_g7` (L491) — rustdoc L428-484 with `# Examples` ✓
  - `pub fn merge_g7_by_severity` (L627) — rustdoc L566-624 with `# Examples` + `# Panics` ✓
  - `pub async fn test_g7_parallel_pipeline` (L737) — rustdoc L714-733 ✓
  - `pub fn test_g7_severity_merge` (L941) — rustdoc L913-937 ✓
- `quality_pipeline.rs`:
  - `pub enum QualityPipelineError` (L113) — rustdoc L104-110 + per-variant ✓
  - `pub const fn severity_to_quality` (L154) — rustdoc L141-152 ✓
  - `pub const fn category_from_severity` (L179) — rustdoc L168-177 ✓
  - `pub const fn language_default_server` (L212) — rustdoc L191-211 ✓
  - `pub async fn persist_diagnostics` (L340) — rustdoc L303-339 with `# Re-ingest behaviour` / `# Tracing` / `# Errors` ✓
  - `pub async fn soft_delete_resolved_quality_issues` (L565) — rustdoc L534-558 with `# Tracing` / `# Errors` ✓
  - `pub async fn test_quality_issues_tracking` (L1090) — rustdoc L1060-1086 ✓
- Module rustdoc on both files cites master-plan §s + WO precedents.
- Crate-level `#![deny(rustdoc::broken_intra_doc_links)]` on `g7.rs` per `.claude/rules/rust-style.md`.

#### 9. RFR (`ucil-build/work-orders/0085-ready-for-review.md`) ✓
All required sections present per WO-0084 template:
- `## What I verified locally` (L7-32) — 25-row gate-by-gate table.
- `## Mutation contract` (L34-49) — M1/M2/M3 with file/lines/patch/SA/observed-panic/restore/pre-md5 columns.
- `## Test-type effectiveness` (L51-72) — load-bearing vs ceremonial classification per SA.
- `## Disclosed deviations` (L74-80) — 5 disclosures including tracing carve-out, `master_deadline_ms` cast, SA6 bound, `TestBehaviour::Sleep` trim, `#[allow(dead_code)]`.
- `## Trace-span coverage` (L82-84) — confirms `ucil.group.quality` span on `execute_g7` per §15.2 line 1519.
- `## DEC reference` (L86-91) — DEC-0005 / DEC-0007 / DEC-0008 §4 + M1/M2/M3 contract.
- `## Standing carry-forward` (L93-97) — scope_out #11 / #12 / #13.

#### 10. Verify scripts ✓
All three are `#!/usr/bin/env bash` `set -euo pipefail`, executable (`-rwxrwxr-x`), mirror the `P3-W10-F10/F12.sh` shape per WO scope_in #22, and:
- F01: cargo-test selector + grep `pub trait G7Source` + grep `pub async fn execute_g7` + grep `tracing::instrument` + grep `ucil.group.quality` + clippy + fmt — green-path exits 0, sub-check fails exit 1. ✓
- F05: cargo-test selector + grep `pub fn merge_g7_by_severity` + grep `pub enum Severity` + clippy + fmt. ✓
- F06: cargo-test selector + grep `pub async fn soft_delete_resolved_quality_issues` + clippy + fmt. ✓

#### 11. No `unsafe` ✓
`grep -nE 'unsafe' crates/ucil-daemon/src/g7.rs` — no matches.

#### 12. No `#![allow(warnings)]` blanket suppression ✓
Only attribute-level `#[allow]` uses are pinpoint:
- `g7.rs:63`: `#![allow(clippy::module_name_repetitions)]` — module-scoped, justified by the `g7::G7*` naming pattern (mirrors `g3.rs` / `g4.rs`).
- `g7.rs:389`: `#[allow(dead_code)]` on `join_all_g7` — tied to M1 mutation contract per WO scope_in #23.
- `g7.rs:735, 939`: `#[allow(clippy::too_many_lines, clippy::missing_panics_doc)]` on the frozen tests — necessary because the SA-tagged panic bodies grow the test functions past the default lint threshold.
- `quality_pipeline.rs:91`: `#![allow(clippy::module_name_repetitions)]` — pre-existing module-scope allow.

## Summary

The diff implements the WO-0085 contract faithfully: G7 (Quality) parallel pipeline orchestrator, severity-weighted merge, and `quality_issues` trend-tracking with SELECT-then-UPSERT + soft-delete. The mutation-contract anti-laziness layer (M1+M2+M3) is fully wired and disclosed. No stubs, no skipped tests, no weak assertions, no mocks of critical collaborators in production code, no hallucinated imports, all `pub` items documented. Commit hygiene is solid (zero merges, full trailers, DEC-0005 invoked for the >50-LOC commits).

The 5 warnings above are soft — none are blockers; the executor may either address them in follow-up or leave them for the verifier to evaluate alongside the rest of the gate.

**Verdict: CLEAN** — the work-order is ready for verifier dispatch.
