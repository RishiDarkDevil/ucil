# Critic Report: WO-0028

**Critic session**: crt-wo-0028-20260418
**Branch**: `feat/WO-0028-progressive-startup`
**Ref reviewed**: `2fa247ea46710a38313ba831bbb07f697cd35081`
**Merge base**: `origin/main`
**Verdict**: CLEAN

## Scope

Diff: `main..feat/WO-0028-progressive-startup`

```
 crates/ucil-daemon/src/lib.rs                   |  16 +
 crates/ucil-daemon/src/priority_queue.rs        | 305 +++
 crates/ucil-daemon/src/server.rs                | 137 +
 crates/ucil-daemon/src/startup.rs               | 418 +++
 ucil-build/work-orders/0028-ready-for-review.md |  48 +
 5 files changed, 924 insertions(+)
```

All five paths are inside the work-order's `scope_in`; no `forbidden_paths` touched.

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should note — non-blocking)

1. **Commit size exceeds the ~50-line soft target.** Commit-style rule in
   `.claude/rules/commit-style.md` sets ~50 lines as a soft target and allows
   review-friendly chunks for big refactors. The branch has:
   - `a482dbf` feat(daemon) priority_queue — 312 insertions in one commit.
   - `c28e895` feat(daemon) startup — 413 insertions in one commit.
   - `22a3f1d` test(daemon) server test — 137 insertions.

   The WO's own `commit_plan` targeted 5 commits at 90-150 lines each; the
   executor consolidated commits 2+3+4 of the plan into a single `startup`
   commit. Each commit is still one cohesive logical change (a whole new
   module with docs + tests), and all five commits have proper Conventional
   Commits subjects plus `Phase: 1` / `Feature: P1-W3-F08` /
   `Work-order: WO-0028` trailers. Flagging as a soft warning only — not a
   blocker.

### OK (checks run, all green)

- **Stub detection** (`crates/ucil-daemon/src/{priority_queue,startup,server,lib}.rs`):
  no `todo!()`, no `unimplemented!()`, no `raise NotImplementedError`, no
  `pass`-only bodies, no trivial-literal returns in shipped code.
- **Mocked critical deps** (Serena, LSP, SQLite, LanceDB, Docker): none.
  `test_progressive_startup` drives a **real** `McpServer::serve` over a
  **real** `tokio::io::duplex(64 KiB)` pair.
  `test_ready_handle_resolves_on_first_write` likewise uses real
  `tokio::io::duplex`. No `mock` / `fake` / `stub` / `MagicMock` /
  `mockall` identifiers anywhere in the diff.
- **Skipped/ignored tests**: none. No `#[ignore]`, no `.skip(`, no
  `#[cfg(test)] mod tests { }` wrapper in either new module — DEC-0005
  compliance confirmed.
- **Weak assertions**: none. Every assertion carries either a meaningful
  equality check (`assert_eq!(popped.path, path)`) or a bounded inequality
  (`assert!(elapsed < STARTUP_DEADLINE)`) with a contextual message. No
  `assert!(true)` / `assert_eq!(true, true)` / `expect(true).toBe(true)`.
- **Hallucinated imports / paths**:
  - `crates/ucil-daemon/src/startup.rs:68` —
    `use crate::{priority_queue::PriorityIndexingQueue, server::McpError, server::McpServer};`
    — all three targets exist on the branch (priority_queue.rs newly added,
    `server::McpError` + `server::McpServer` are re-exported in `lib.rs`).
  - `crates/ucil-daemon/src/server.rs:+770` — test imports
    `crate::{priority_queue::PriorityIndexingQueue,
    startup::{handle_call_for_priority, ProgressiveStartup,
    STARTUP_DEADLINE}}` — all four items exist and are `pub`.
  - `crates/ucil-daemon/src/lib.rs:50` — `pub mod priority_queue;` and
    `pub mod startup;` each point at a real file in the tree (confirmed
    via `git ls-tree`).
- **Feature coverage**: WO feature `P1-W3-F08` is exercised by
  - `server::test_progressive_startup` (frozen acceptance selector — covers
    both the `STARTUP_DEADLINE` invariant and the priority-ordering
    invariant end-to-end),
  - `priority_queue::test_queue_entry_ord_is_newest_first`,
    `test_enqueue_and_pop_single_path`, `test_touch_reorders_by_recency`,
    `test_len_tracks_entries`, `test_peek_does_not_consume` (queue
    primitives),
  - `startup::test_startup_deadline_constant_is_2s`,
    `test_handle_call_for_priority_touches_files_in_context`,
    `test_handle_call_for_priority_ignores_missing_files_key`,
    `test_ready_handle_resolves_on_first_write` (orchestrator primitives).

  Exactly the five + four + one tests the WO's acceptance list requires.
- **Commit hygiene**: five commits, every subject Conventional Commits
  (`feat(daemon)`, `fix(daemon)`, `test(daemon)`, `chore(work-order)`);
  every body carries the trio of `Phase: 1`, `Feature: P1-W3-F08`,
  `Work-order: WO-0028` trailers plus the required
  `Co-Authored-By: Claude Opus 4.6 (1M context)` trailer. No `--amend`
  after push, no force-push.
- **Doc + public API**: every new `pub` item in `priority_queue.rs`
  (`QueueEntry`, `QueueEntry::{path, last_queried}`, `PriorityIndexingQueue`,
  `PriorityIndexingQueue::{new, enqueue, touch, pop, peek, len, is_empty}`)
  and every new `pub` item in `startup.rs` (`STARTUP_DEADLINE`,
  `StartupError` + its three variants + inner fields, `ProgressiveStartup`,
  `ProgressiveStartup::{new, queue, start}`, `ReadyHandle`,
  `ReadyHandle::wait`, `handle_call_for_priority`) has a rustdoc block.
  `ReadyHandle::wait` additionally carries an `# Errors` section per
  rust-style rule. `#![deny(rustdoc::broken_intra_doc_links)]` on
  `lib.rs:34` will mechanically enforce doc correctness in the verifier
  build.
- **Rust invariants**:
  - No `unwrap()` / `expect()` / `panic!()` in non-test code — all 15
    occurrences across the two new modules are inside `#[cfg(test)]`
    items or inside safe `u64::try_from(...).unwrap_or(u64::MAX)`
    fallbacks that are not panic paths (startup.rs:207, 215, 222).
  - `ReadyHandle::wait` wraps its `oneshot::Receiver` await in
    `tokio::time::timeout(STARTUP_DEADLINE + READY_WAIT_SLACK, ...)` —
    rust-style rule "every IO `.await` wrapped in `tokio::time::timeout`"
    satisfied.
  - `PriorityIndexingQueue` uses `std::sync::Mutex` with `PoisonError`
    recovered via `into_inner` — documented in the module rustdoc.
  - `StartupError` is `#[derive(thiserror::Error)] #[non_exhaustive]` —
    matches the libraries-use-thiserror rule.
  - Tracing spans named `ucil.daemon.startup.start`,
    `ucil.daemon.priority_queue.enqueue`,
    `ucil.daemon.priority_queue.touch` — §15.2 compliant.
- **Forbidden-paths guard**: `git diff main..feat/WO-0028-progressive-startup`
  touches only `crates/ucil-daemon/src/{lib,priority_queue,startup,server}.rs`
  plus the WO marker. `watcher.rs`, `session_manager.rs`, `session_ttl.rs`,
  `lifecycle.rs`, `main.rs`, `plugin_manager.rs`, `storage.rs`,
  `feature-list.json`, the master plan, and every other crate stay
  byte-for-byte untouched.
- **DEC-0005 flat-tests compliance**: no `^mod\s+tests\s*\{` in either
  new module or in the addition to `server.rs`.

## Verdict

**CLEAN.** The work-order may proceed to the verifier. The one commit-size
warning is non-blocking — the executor chose cohesive module-per-commit
granularity over strict line-count chunking, which is consistent with WO-0011
/ WO-0016 / WO-0023 / WO-0026 precedent on this crate.
