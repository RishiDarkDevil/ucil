# Critic Report: WO-0091

**Critic session**: crt-WO-0091-g5-context-parallel-query
**Branch**: `feat/WO-0091-g5-context-parallel-query`
**Reviewed ref**: `c55d409e924be3466982eb241b6b00b13f6838e9`
**Merge base**: `7dd8590` (main)
**Commit range**: `git log --oneline main..feat/WO-0091-g5-context-parallel-query` → 4 commits
**Work-order**: `ucil-build/work-orders/0091-g5-context-parallel-query.json`
**Feature(s)**: `P3-W10-F04` (G5 Context parallel-query backbone)
**Verdict**: **CLEAN**

## Summary of changes

| File | Change | LOC |
|---|---|---|
| `crates/ucil-daemon/src/g5.rs` | NEW (G5Source trait + execute_g5 + assemble_g5_context + types/constants) | +673 |
| `crates/ucil-daemon/src/executor.rs` | EDIT (add `pub async fn test_g5_context_assembly` at module root + `TestG5Source`) | +670 |
| `crates/ucil-daemon/src/lib.rs` | EDIT (`pub mod g5;` + alphabetical re-export block) | +3 |
| `scripts/verify/P3-W10-F04.sh` | NEW (mirrors P3-W9-F07.sh G3 verify template) | +130 |
| `ucil-build/work-orders/0091-ready-for-review.md` | NEW (RFR with M1/M2/M3 mutation table, md5sum path, deviations table) | +126 |
| **Total** | 5 files changed | +1602 |

Commits (4):
- `bee3462` `feat(daemon): add g5.rs context backbone — G5Source + execute_g5 + assemble_g5_context`
- `17a387d` `test(daemon): add executor::test_g5_context_assembly frozen acceptance test`
- `7e3ea23` `build(verify): add P3-W10-F04 acceptance script for G5 context backbone`
- `c55d409` `docs(rfr): WO-0091 ready for review — G5 context backbone`

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate)

1. **Commit subject lines exceed the 70-char soft target on three of four commits**
   (`.claude/rules/commit-style.md`).  Lengths: `feat(daemon)` 86 chars, `test(daemon)` 75 chars, `build(verify)` 71 chars, `docs(rfr)` 57 chars.  The em-dash `—` reads as 1 char visually but is 3 bytes UTF-8 — even subtracting that, the `feat` subject is still 84 visual chars.  Soft warning only — the commit-style rule prefixes the 70-char limit with "imperative, ≤70 chars" not a hard cap, and the bodies + trailers are perfectly compliant.  RFR was not asked to address this.

2. **`feat` (676 LOC) and `test` (670 LOC) commits exceed the ~50-line soft target.**  WO-0091 `scope_in` #16 explicitly invokes the DEC-0005 module-coherence carve-out and cites precedent (`g3.rs` 721 LOC / WO-0070, `g4.rs` 898 LOC / WO-0083, `g7.rs` 1273 LOC / WO-0085).  The g5.rs file would not compile without the trait + execute_g5 + assemble_g5_context shipping together, and the executor.rs test would not compile without TestG5Source + all 8 SA assertions shipping together.  Pattern is the same as the four prior G-backbone commits already accepted.  Soft warning only — same shape as WO-0090 critic report Warning #1.

3. **M2 spirit-vs-literal disclosed deviation: M2 panic fires at SA1, not SA3.**  Disclosed in RFR §"Disclosed deviations" #1.  Both SA1 and SA3 are dedup-load-bearing (SA1 asserts `chunks.len() == 4` / `deduped_count == 1`; SA3 asserts `iter().all(|c| c.path != "b.rs")`); execution-order means SA1 panics first.  The mutation contract's invariant — "an SA-tagged panic fires when M2 is applied" — is met.  Cites WO-0090 §verifier lesson "mutation-patch simplification within scope_in's permissive clause".  Verifier should confirm the SA1 panic body shape (`left: 5, right: 4`) is what they expect for the AC18 evidence; if they expected SA3 specifically, this is a reframe-the-AC discussion, not a re-implement-the-test discussion.

4. **M1 mutation site is `run_g5_source` helper (g5.rs:336), not `execute_g5` directly.**  Disclosed in RFR §"Disclosed deviations" #2.  `execute_g5` calls `run_g5_source` per source via the `join_all_g5` fan-out — the per-source `tokio::time::timeout` lives in the helper, mirroring `run_g3_source` / `run_g4_source` / etc.  M1 still bypasses the per-source timeout (the spec's intent), and SA4 still fires.  Soft warning only — matches the G3/G4/G7/G8 backbone template.

### OK

#### 1. Stub detection — CLEAN
- `grep -nE 'todo!\(\)|unimplemented!\(\)|raise NotImplementedError'` against `crates/ucil-daemon/src/g5.rs` and the `executor.rs` diff hunks → **zero hits**.
- `grep -nE '^\+.*pass\s*$'` against the full diff → zero hits.
- `grep -nE '^\+\s*return\s+(None|Default::default\(\)|true|false|0|"")\s*$'` against the full diff → zero hits.
- All three production-side functions in `g5.rs` have real bodies:
  - `run_g5_source` (line 328) wraps `tokio::time::timeout(per_source_deadline, source.execute(query))` with `unwrap_or_else` synthesising a `TimedOut` placeholder — real fallback, not stub.
  - `execute_g5` (line 476) builds boxed futures, polls them concurrently via `join_all_g5`, wraps in `tokio::time::timeout(master_deadline, ...)`, and synthesises in-order `TimedOut` placeholders on master-trip — real orchestrator.
  - `assemble_g5_context` (line 611) flattens chunks, builds a `BTreeSet<&str>` for dedup membership, filters, sorts by `pagerank_score` descending with stable tiebreak, sums token counts, and counts distinct source IDs — real assembler.
- Production-side `.unwrap()` enumeration in RFR §"Production-side `.unwrap()` / `.expect()` enumeration" is honest:
  - `g5.rs:339` / `:464` / (synthesis path) `u64::try_from(start.elapsed().as_millis()).unwrap_or(u64::MAX)` — saturating cast, non-panicking.
  - `g5.rs:380` `r.expect("join_all_g5: every slot must be filled before returning")` — inside the dead-code-allowed `join_all_g5` helper; the `poll_fn` loop only yields `Ready(())` when every `slots[i].is_some()`, so the expect is unreachable.

#### 2. Mocked critical dependencies — CLEAN
- `grep -niE 'mock|fake|stub' crates/ucil-daemon/src/g5.rs` → **zero hits** (production-side word-ban scrub clean per AC15).
- `grep` against the executor.rs diff hits **only**:
  - Two doc-comment lines disclosing `// internal — NOT a mock of any external wire format. Production` and `/// a mock of any external wire format — same shape as the` — both negate-form disclaimers, not actual mocks.  Pattern follows the four prior G-backbone WOs (G3/G4/G7/G8) and the `TestG3Source` / `TestG4Source` / `TestG7Source` / `TestG8Source` precedent.
- The local `TestG5Source` impl (executor.rs:4553) is a UCIL-internal `G5Source` trait impl per DEC-0008 §4 dependency-inversion seam — explicitly carved out under WO-0091 `scope_in` #15 and CLAUDE.md "Test helpers under `#[cfg(test)]` exempt" (the `Test*` prefix differentiates UCIL-internal trait impls from external-wire-format mocks).
- No Serena MCP / LSP / SQLite / LanceDB / Docker mocking introduced.  G5Source is a UCIL-owned trait that does NOT wrap any external wire format.

#### 3. Skipped / ignored tests — CLEAN
- `grep -nE '^\+.*#\[ignore\]'` → zero hits.
- `grep -nE '^\+.*(\.skip\(|xfail|@pytest\.mark\.skip|@pytest\.mark\.xfail|it\.skip|xit\()'` → zero hits.

#### 4. Weak assertions — CLEAN
- `grep -nE '^\+.*(assert!\(true\)|assert_eq!\(true,\s*true\)|assert True\b|expect\(true\)\.toBe\(true\))'` → zero hits.
- 39 SA-numbered assertions in the test diff (AC11 requires ≥ 8) covering 8 scenarios.  Spot-check:
  - `executor.rs:4691` `assert_eq!(outcome_sa1.results.len(), 3, "(SA1) outcome.results must contain exactly 3 entries (one per source); left: {}, right: 3", outcome_sa1.results.len());`
  - `executor.rs:4798` `assert!(assembled_sa3.chunks.iter().all(|c| c.path != "b.rs"), "(SA3) no surviving chunk may have path == \"b.rs\"; ...");`
  - `executor.rs:5119` `assert!(outcome_sa8.wall_elapsed_ms < 500, "(SA8) parallel wall < 500 ms; left: {}, right: 500 (proves serial 3x200=600 ms did NOT happen → parallelism confirmed)", outcome_sa8.wall_elapsed_ms);`
- Each `assert!` / `assert_eq!` carries `(SAn) <semantic name>; left: ..., right: ...` panic-body shape per DEC-0007 + WO-0085 §verifier lesson.

#### 5. Hallucinated paths — CLEAN
- New `use` statements in `g5.rs` (lines 66-72) all resolve:
  - `std::collections::BTreeSet`, `std::future::Future`, `std::pin::Pin`, `std::task::Poll`, `std::time::Duration` — std lib, ✓
  - `serde::{Deserialize, Serialize}` → workspace dep `serde.workspace = true` in `crates/ucil-daemon/Cargo.toml`, ✓
- Cross-references inside g5.rs:
  - `tokio::time::timeout` (line 336, 510) → workspace dep `tokio.workspace = true`, ✓
  - `tracing::instrument` (line 475) → workspace dep `tracing.workspace = true`, ✓
  - `async_trait::async_trait` (line 294) → workspace dep `async-trait = { workspace = true }` in `crates/ucil-daemon/Cargo.toml:`, ✓
- `crates/ucil-daemon/src/lib.rs:161` `pub mod g5;` lands in alphabetical order between `pub mod g4;` (line 160) and `pub mod g7;` (line 162) per AC08.
- `crates/ucil-daemon/src/lib.rs:198` `pub use g5::{...}` re-export block names `assemble_g5_context, execute_g5, G5AssembledContext, G5ContextChunk, G5Outcome, G5Query, G5Source, G5SourceKind, G5SourceOutput, G5SourceStatus, G5_MASTER_DEADLINE, G5_PER_SOURCE_DEADLINE` — superset of the AC09 minimum requirement.
- Test-side `use crate::g5::{...}` (executor.rs:4527) imports resolve to the new module and are exercised by all 8 SA scenarios.
- RFR `Final commit sha: 7e3ea230abf0e55a4a80f74647c2629fa99df0c0` matches `7e3ea23` (the build commit), but the actual final commit on the branch is `c55d409` (the RFR commit).  This is a self-referential anomaly — the RFR commit cannot reference itself.  Same shape as prior G-backbone WOs.  Informational only.

#### 6. Feature coverage — CLEAN
- **P3-W10-F04**: `crates/ucil-daemon/src/executor.rs:4524` `pub async fn test_g5_context_assembly()` lives at MODULE ROOT (NOT inside `mod tests {}` — line is the first non-comment, non-blank line after the `// ── G5 ...` section header at `:4461`, with `#[cfg(test)] #[allow(...)] #[tokio::test(flavor = "multi_thread", worker_threads = 4)]` attribute trio above it).  Per DEC-0007 + WO-0068 lesson, this enables the substring selector `executor::test_g5_context_assembly` to resolve uniquely without a `tests::` intermediate.
- 8 SA scenarios cover the full WO-0091 contract:
  - SA1 — Basic available case (3 sources / 5 chunks / 1 dedup → 4 surviving / `source_count == 3`).
  - SA2 — `PageRank` ranking descending (M3 target) with explicit `[0.9, 0.5, 0.3, 0.1]` order check.
  - SA3 — Session dedup invariant (M2 target) with explicit `path != "b.rs"` invariant.
  - SA4 — Per-source timeout (M1 target) with `[per_source_ms, 5_000)` elapsed range.
  - SA5 — Error pass-through (broken source's chunks dropped, others survive).
  - SA6 — Master-deadline trip (`master = 100 ms`, all `7 s` sleepers → `master_timed_out == true`, `wall_elapsed_ms < 2000 ms`).
  - SA7 — Empty input (zero sources sub-case + all-empty sub-case).
  - SA8 — Parallelism wall-clock canary (3 × 200 ms sleeps → `wall_elapsed_ms in [180, 500)`).
- M1 / M2 / M3 mutation contract pre-baked in RFR §"M1 / M2 / M3 mutation table" with patch + restore commands and md5sum snapshot path `/tmp/wo-0091-g5-orig.md5sum`.  Md5 hashes in the RFR (`59d80673c7c66f4cd572d27cd8f9ffb4 g5.rs`, `dbbb64aa64351a5aaa6039bbafcdab49 executor.rs`) verified locally — match the contents on the feature branch.

#### 7. Commit hygiene — CLEAN
- Conventional Commits subject types on every commit (`feat`, `test`, `build`, `docs`).
- Every commit body carries `Phase: 3`, `Feature: P3-W10-F04`, `Work-order: WO-0091` trailers + `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>` line.
- No merge commits introduced: `git log feat/WO-0091-g5-context-parallel-query ^main --merges | wc -l` → 0 (AC23).
- No `--amend` after push detected; no force-push.
- Branch is a clean linear sequence (4 commits) on top of merge-base `7dd8590`.
- See Warning #1 (subject length) and Warning #2 (commit size) above for soft deviations.

#### 8. Doc + public API — CLEAN
- All 12 public items in `g5.rs` carry rustdoc:
  - Module-level rustdoc (lines 1-62) cites master-plan §5.5 + §3.2 rows 1/7/8/11/20 + DEC-0008 §4 no-substitute-impls policy.
  - `G5_MASTER_DEADLINE` (line 85): 9 lines of rustdoc above.
  - `G5_PER_SOURCE_DEADLINE` (line 100): 11 lines of rustdoc with WO-0068/0070/0083 lesson citation.
  - `G5Query` (line 111): 5 lines of struct-level rustdoc + per-field rustdoc.
  - `G5SourceKind` (line 137): 7 lines of enum-level rustdoc + per-variant rustdoc.
  - `G5SourceStatus` (line 162): 7 lines of enum-level rustdoc + per-variant rustdoc.
  - `G5ContextChunk` (line 182): 7 lines of struct-level rustdoc + per-field rustdoc.
  - `G5SourceOutput` (line 211): 1 line of struct-level rustdoc + per-field rustdoc.
  - `G5Outcome` (line 236): 5 lines of struct-level rustdoc + per-field rustdoc.
  - `G5AssembledContext` (line 253): 5 lines of struct-level rustdoc + per-field rustdoc.
  - `G5Source` trait (line 295): 17 lines of trait-level rustdoc + per-method rustdoc.
  - `execute_g5` (line 476): 51 lines of rustdoc including pipeline-shape, per-source-deadline rationale, tracing carve-out, and `# Examples` block.
  - `assemble_g5_context` (line 611): 31 lines of rustdoc including the four-step assembly contract, tracing-omission rationale, and `# Examples` block.
- Both private helpers (`run_g5_source` line 328, `join_all_g5` line 374) carry rustdoc; `join_all_g5` includes the `#[allow(dead_code)]` justification citing the M1 mutation contract.

## Auxiliary checks

- AC01 (`g5.rs` exists): ✓ at `crates/ucil-daemon/src/g5.rs`, 673 LOC.
- AC02 (`G5_MASTER_DEADLINE = 5_000`): line 85 ✓.
- AC03 (`G5_PER_SOURCE_DEADLINE = 4_500`): line 100 ✓.
- AC04 (`#[async_trait::async_trait]` + `pub trait G5Source`): lines 294 + 295 ✓.
- AC05 (7 public structs/enums): G5Query / G5SourceKind / G5SourceStatus / G5ContextChunk / G5SourceOutput / G5Outcome / G5AssembledContext = 7 ✓.
- AC06 (`#[tracing::instrument(name = "ucil.group.context"`): line 475 ✓.
- AC07 (`pub async fn execute_g5` + `pub fn assemble_g5_context`): lines 476 + 611 ✓.
- AC08 (`pub mod g5;` between g4 and g7): lib.rs:161 ✓.
- AC09 (re-export block): lib.rs:198 ✓.
- AC10 (test at module root with `#[tokio::test]`): executor.rs:4521-4524 ✓.
- AC11 (≥ 8 SA-tagged assertions): 39 in the diff ✓.
- AC15 (g5.rs word-ban scrub): zero hits ✓.
- AC16 (`TestG5Source`): executor.rs:4553 ✓.
- AC23 (no merge commits): 0 ✓.
- AC24 (Conventional Commits trailers): all 4 commits ✓.
- AC25 (4-5 commits): 4 ✓.
- AC26 (RFR carries M1/M2/M3 + md5 + deviations + .unwrap() table): ✓.

## Test-tier observations

- `cargo test -p ucil-daemon executor::test_g5_context_assembly --no-fail-fast` PASS in ~5 s per RFR (AC14 budget is 30 s).
- The test runs under `tokio::test(flavor = "multi_thread", worker_threads = 4)` — appropriate for the parallel `join_all_g5` fan-out and the SA8 wall-clock canary.
- The SA4 lower bound (`slow_elapsed >= per_source_ms` = 4500) and upper bound (`slow_elapsed < 5000`) gives the per-source deadline a 500 ms margin before the master deadline — consistent with the unconditional-const design (per scope_in #2 / WO-0068+0070+0083 lessons-learned).
- The SA8 upper bound (`wall_elapsed_ms < 500`) is ~2.5× the 200 ms per-source sleep — gives parallelism + tokio scheduler overhead room without admitting a serial 3 × 200 = 600 ms run.  Same shape as WO-0070 SA1 tightened-bound pattern.
- The SA6 master-deadline trip uses `master_deadline = 100 ms` < `G5_PER_SOURCE_DEADLINE = 4500 ms`, ensuring the master timer fires deterministically (per scope_in #2 — without the unconditional-const, the inner per-source would race the outer master).

## Verdict rationale

All 8 critic checks pass with **zero blockers**.  The 4 warnings are all soft:
- (1) commit subject lengths exceed the 70-char soft target by 1–16 chars — bodies + trailers fully compliant;
- (2) `feat` / `test` commit sizes exceed the 50-line soft target — DEC-0005 module-coherence carve-out invoked, with explicit precedent (g3.rs / g4.rs / g7.rs);
- (3) M2 panic fires at SA1 not SA3 — disclosed in RFR, both SAs are dedup-load-bearing, mutation contract intent (SA-tagged panic on M2) is met;
- (4) M1 mutation site is `run_g5_source` helper not `execute_g5` directly — disclosed in RFR, mirrors G3/G4/G7/G8 backbone template precedent.

None of these obstruct verifier work or change the substantive correctness of the G5 backbone (`G5Source` trait + `execute_g5` parallel orchestrator + `assemble_g5_context` PageRank-ranked + session-deduped assembler).  The diff is internally consistent, all public surface is documented, the frozen test exercises 8 SA-numbered scenarios with `(SAn) <semantic>; left: ..., right: ...` panic bodies, and the M1/M2/M3 mutation contract is pre-baked in the RFR with concrete patches, restore commands, and md5sum verification.  **Approving for verifier dispatch.**
