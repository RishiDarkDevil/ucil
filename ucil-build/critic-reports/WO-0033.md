# Critic Report: WO-0033

**Critic session**: crt-WO-0033-20260418
**Branch**: `feat/WO-0033-find-definition-tool`
**Ref reviewed**: `d0df074f248977ed093f8f6c5390f42ecc688e87`
**Range**: `main..feat/WO-0033-find-definition-tool` (9 commits, 947 / 10 LOC +/–)
**Feature(s)**: `P1-W4-F05` — `find_definition` MCP tool (master-plan §3.2 row 2)
**Verdict**: **CLEAN**

## Summary

WO-0033 promotes `find_definition` from the Phase-1 22-tool stub catalog
to a real knowledge-graph-backed handler. The diff touches four source
files + one ready-for-review marker:

- `crates/ucil-core/src/knowledge_graph.rs` — +2 `pub` read helpers
  (`get_entity_by_id`, `list_relations_by_target`) + additive
  `SymbolResolution { id, qualified_name }` fields + 2 unit tests.
- `crates/ucil-daemon/src/server.rs` — new `McpServer::with_knowledge_graph`
  constructor, optional `kg` field, `handle_find_definition` with 4
  helpers, 3 new module-level tests (acceptance + 2 negatives).
- `crates/ucil-daemon/src/executor.rs` — one-line visibility bump on
  `rust_project_fixture` (`fn` → `pub(crate) fn`) so the server test
  reuses the cwd-probing fixture locator.
- `crates/ucil-daemon/src/lib.rs` — +11-line module-doc paragraph
  describing the promotion.
- `ucil-build/work-orders/0033-ready-for-review.md` — new.

All 8 anti-laziness checks pass. No stubs, no mocks of critical
collaborators, no skipped tests, no weak assertions, no hallucinated
imports, and the frozen `server::test_find_definition_tool` selector
lives at the required module-root scope (DEC-0005).

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate — or justify now)

1. **Commit size > 200 LOC soft cap.**
   - `272ffd8 test(daemon): find_definition acceptance + negative tests` — 315 lines.
   - `6a3ab37 feat(daemon): handle_find_definition with KG-backed dispatch` — 249 lines.

   `critic.md` rule is "no commit > 200 lines of diff without a good
   reason." Both commits **do** carry a stated reason in their bodies:
   the test commit bundles the `build_find_definition_fixture` helper
   with the 3 tests that share it (splitting would leave an unused
   helper mid-stack), and the handler commit lands the dispatch +
   helpers together to keep `cargo clippy -D warnings` green per
   intermediate commit. Acceptable, but flagging so the verifier is
   aware that the reported `estimated_commits: 8` was met with two
   commits near the upper bound. No action required.

### OK

- **Stub detection** (check 1): `todo!()`, `unimplemented!()`,
  `NotImplementedError`, `raise NotImplementedError`, and `pass`-only
  bodies → **0 hits** across the diff (`git diff main..HEAD -- '*.rs'`
  piped through the critic.md patterns). The handler returns real JSON
  envelopes built from real `KnowledgeGraph::resolve_symbol` /
  `list_relations_by_target` / `get_entity_by_id` reads, not
  default-literal placeholders.
- **Mocked critical deps** (check 2): No matches for `mock|fake|stub|
  MagicMock|Mockall` against `serena|lsp|pyright|rust-analyzer|
  rusqlite|sqlx|lancedb|docker` in any new test. Textual hits of
  "stub" / "mock" in the diff are documentation comments (phase-1
  `_meta.not_yet_implemented` stubs for the other 21 tools per
  invariant #9, and affirmative statements that tests do *not* mock
  rusqlite / tokio::io / serde_json). The acceptance test opens a
  **real** tempfile-backed SQLite via `KnowledgeGraph::open`, runs
  **real** tree-sitter ingest via `IngestPipeline::ingest_file` on
  `tests/fixtures/rust-project/src/util.rs`, then upserts a single
  synthetic `calls` relation targeting the ingested `evaluate` row.
  The synthetic edge is explicitly in-scope per WO `scope_out` bullet 3
  ("the acceptance test injects a synthetic relation to exercise the
  read path").
- **Skipped / ignored tests** (check 3): 0 new `#[ignore]` / `.skip(` /
  `xfail` / `xit(` / `@pytest.mark.skip` in any of the 5 new tests.
  The one pre-existing `#[ignore]` in
  `crates/ucil-core/tests/fixture.rs:20` is outside the diff
  (unchanged on this branch).
- **Weak assertions** (check 4): 0 hits for `assert!(true)`,
  `assert_eq!(true, true)`, `assert_eq!(1, 1)`, `expect(true).toBe(true)`.
  Every assertion in the 5 new tests checks a concrete projection of
  the JSON-RPC response envelope or KG row (`_meta.found`, `_meta.tool`,
  `_meta.source`, `_meta.file_path`, `_meta.start_line > 0`,
  `_meta.callers` contains synthetic caller by qualified_name, entity
  fields round-trip, relations ordered by id ASC, empty-vec miss path,
  `error.code == -32602`, no `result` on error response, etc.).
- **Hallucinated paths** (check 5): All new `use` statements resolve to
  symbols that exist on the branch —
  - `ucil_core::KnowledgeGraph` — `crates/ucil-core/src/lib.rs:30`.
  - `ucil_core::{Entity, Relation, SymbolResolution}` —
    `crates/ucil-core/src/lib.rs:27-28`.
  - `crate::executor::{IngestPipeline, SOURCE_TOOL,
    TREE_SITTER_VALID_FROM, rust_project_fixture}` —
    `crates/ucil-daemon/src/executor.rs:87,101,359,511` (last one is
    the new `pub(crate)` bump).
  - `tempfile::TempDir`, `serde_json::{json, Value}`, `std::sync::{Arc,
    Mutex}`, `tokio::io::duplex` — all standard and already listed in
    `ucil-daemon/Cargo.toml` / `ucil-core/Cargo.toml` transitively.
  - Fixture file `tests/fixtures/rust-project/src/util.rs` — exists on
    disk.
- **Feature coverage** (check 6): `WO-0033` declares
  `feature_ids: ["P1-W4-F05"]`. The frozen selector
  `server::test_find_definition_tool` exists at module root
  (`server.rs:1287` on the branch). Plus two negative-path tests
  (`server::test_find_definition_tool_unknown_symbol` — not-found
  envelope; `server::test_find_definition_tool_missing_name_param` —
  JSON-RPC `-32602`) and two KG read-helper unit tests
  (`knowledge_graph::test_get_entity_by_id`,
  `knowledge_graph::test_list_relations_by_target`) that exercise the
  new helpers the handler depends on. Every code path introduced by the
  WO is covered.
- **Commit hygiene** (check 7): 9 commits, all Conventional-Commits
  subjects (`feat(core):`, `feat(daemon):`, `test(core):`,
  `test(daemon):`, `docs(daemon):`, `chore(wo):`). Every `feat/fix/
  refactor/test/docs` commit body carries the required
  `Phase: 1` / `Feature: P1-W4-F05` / `Work-order: WO-0033` trailers.
  No `--amend` / `--no-verify` / `git push --force` patterns in
  `git reflog` for this branch. See Warning #1 for the two commits
  near the soft LOC cap.
- **Doc + public API** (check 8): Every new `pub` item has a rustdoc —
  - `KnowledgeGraph::get_entity_by_id` — `knowledge_graph.rs:793-811`.
  - `KnowledgeGraph::list_relations_by_target` — `knowledge_graph.rs:943-961`.
  - `SymbolResolution::id`, `SymbolResolution::qualified_name` — both
    have per-field docs (`knowledge_graph.rs:262-277`).
  - `McpServer::with_knowledge_graph` — `server.rs:350-365`.
  - `McpServer::kg` field — `server.rs:317-323`.
  - `McpServer::new` doc was extended with the new phase-1-stub-preserving
    narrative (`server.rs:335-341`).

## Cross-WO invariants

- **Phase-1 invariant #9 (21 stub tools)**: preserved. The dispatch
  guard (`if name == "find_definition" { if let Some(kg) = … }`) only
  routes when a KG handle is attached; every other tool and the
  no-KG default-constructed server still produce
  `_meta.not_yet_implemented: true`. The acceptance test explicitly
  asserts that the stub marker is **absent** in the happy-path
  response (see `server.rs:1363-1370` on the branch), while the
  pre-existing `test_all_22_tools_registered` test (unchanged) still
  exercises a default-constructed server that returns the stub for
  `understand_code`.
- **DEC-0005 (module-root test placement)**: preserved. All 3 new
  server tests live at module root (`#[cfg(test)] #[test]` without a
  `mod tests { … }` wrapper), matching the placement of the frozen
  WO-0010 selector.
- **DEC-0007 (no per-WO cargo-mutants)**: respected. This is an
  extension of an existing module, not a new module, so `reality-
  check.sh` handles mutation verification without the manual
  extension.
- **DEC-0008 (Serena LSP bridge)**: respected. WO-0033 does **not**
  wire Serena into the hover doc; that enrichment is explicitly
  deferred to P1-W5-F02 per the WO's `scope_out` bullet 2.

## Verdict

**CLEAN** — no blockers. Recommend proceeding to verifier.

The only warning is informational (two large-but-justified commits).
Phase-1 invariant #9 and DEC-0005 are both preserved, the frozen
selector resolves, and every new `pub` item carries rustdoc. No
forbidden paths touched (`ucil-build/feature-list.json`,
`feature-list.schema.json`, `ucil-master-plan-v2.1-final.md`,
`tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`,
`.githooks/**`, `decisions/DEC-000{1..8}-*.md` — all untouched).
