# Critic Report: WO-0042

**Critic session**: crt-WO-0042-20260504T1749Z
**Branch**: `feat/WO-0042-plugin-manifest-and-lifecycle-statemachine`
**Final commit (ready-for-review)**: `c72536ac4f3ed8bf40a43f4c8947c2b7b85089f3`
**Merge base**: `main` (no shared-history surprises — branch is a strict descendant)
**Verdict**: **CLEAN**

## Scope of review

7 commits reviewed (`main..feat/WO-0042-plugin-manifest-and-lifecycle-statemachine`):

| SHA       | Subject                                                                                  | Size (LOC) |
|-----------|------------------------------------------------------------------------------------------|------------|
| 2ae808d   | feat(daemon): add CapabilitiesSection + ResourcesSection to plugin manifest              | +81 / -5   |
| c0de35f   | feat(daemon): add PluginManifest::validate + activates_for helpers                       | +116       |
| 7c272a4   | feat(daemon): add full plugin lifecycle transition methods + tracing                     | +171       |
| 3415865   | test(daemon): add plugin_manager::test_manifest_parser acceptance test                   | +171       |
| bdd9709   | test(daemon): add plugin_manager::test_lifecycle_state_machine acceptance test           | +124       |
| 5cc34d0   | refactor(daemon): split test_manifest_parser into helpers                                | +51 / -33  |
| c72536a   | chore(WO-0042): mark ready for review                                                    | +132 (md)  |

Files changed (only two — exactly matches WO `scope_in`):
- `crates/ucil-daemon/src/plugin_manager.rs` (+681 / -5)
- `ucil-build/work-orders/0042-ready-for-review.md` (+132 / 0, new file)

No `forbidden_paths` entries touched. No edits to `feature-list.json`, `tests/fixtures/**`, `scripts/gate/**`, `.githooks/**`, `tests/plugin_manager.rs`, `tests/e2e_mcp_stdio.rs`, `tests/e2e_mcp_with_kg.rs`, or any other crate.

## Findings

### Blockers (must fix before verifier)

_None._

### Warnings (should fix before gate, or note for next WO)

1. **Feature-commit size over the soft target.** Two non-test commits exceed `~50 LOC/commit`:
   - `7c272a4` (`feat(daemon): add full plugin lifecycle transition methods + tracing`) — **+171 LOC** (~90 code + ~73 rustdoc + state-machine ASCII diagram). The WO `scope_in` commit ladder budgeted **~60 LOC**. The commit bundles five transition methods (`register` / `mark_loading` / `mark_active` / `stop` / `mark_error`), the `log_transition` helper, the new `PluginError::IllegalTransition` variant, the new `error_message` field, the new `discovered` constructor, and a substantial state-machine rustdoc block on `PluginRuntime`. A cleaner split would have been three commits (`feat: error_message + discovered`, `feat: 4 happy-path transitions`, `feat: mark_error catch-all`). Heavy rustdoc inflation (≈45% of the diff is `///` comments) softens this — most of the bulk is non-code. **Not blocking** because (a) the branch is already pushed and `--amend` after push is forbidden by the root contract, (b) every method is independently documented and tested, (c) DEC-0005 spirit covers tightly-coupled introductions. Flagged for executor style-awareness on the next WO.
   - `c0de35f` (`feat(daemon): add PluginManifest::validate + activates_for helpers`) — **+116 LOC** vs WO-budgeted ~40 LOC. The bulk is the `validate()` body (5 invariants × ~10 LOC each with structured error messages) plus a sizable rustdoc block. `activates_for_*` helpers are tight (~10 LOC each). Borderline. **Not blocking** — same DEC-0005 spirit + rustdoc inflation.

   Test-introduction commits `3415865` (+171) and `bdd9709` (+124) are explicitly permitted to exceed the soft target under DEC-0005 — flagged here only for completeness.

2. **None of the new public symbols are re-exported via `crates/ucil-daemon/src/lib.rs`.** The ready-for-review note (lines 80–105 of `ucil-build/work-orders/0042-ready-for-review.md`) cites this gap deliberately and argues no consumer needs them in this WO. Verified: the existing `tests/plugin_manager.rs` integration suite imports only `HealthStatus` / `PluginError` / `PluginManager` / `PluginManifest`, none of which changed identity. Reachable via fully-qualified `ucil_daemon::plugin_manager::CapabilitiesSection` etc. Not blocking — but a future sibling WO that builds the `ucil plugin` CLI subcommand (P2-W6-F07) will need to add explicit `pub use` lines, so the next planner should plan for that surface change.

### OK

- **Stub detection** (`crates/ucil-daemon/src/plugin_manager.rs`): no `todo!()`, `unimplemented!()`, `NotImplementedError`, `raise NotImplementedError`, or `pass`-only bodies introduced. Searched `^\+.*(todo!\(\)|unimplemented!\(\)|NotImplementedError)` against the diff — **0 hits**. The `Ok(())` returns in `register` / `mark_loading` / `mark_active` / `stop` are post-transition success returns, not stub bodies — each method first mutates `self.state`, optionally captures `error_message`, and emits a `tracing::info!` event before returning.

- **Mocked critical collaborators**: searched `\b(mock|fake|stub|MagicMock|Mockall)\b` (case-insensitive) against the added lines — **0 hits**. Both new acceptance tests exercise real code paths:
  - `test_manifest_parser` writes a real `plugin.toml` to a `tempfile::tempdir()`, drives the real `PluginManifest::from_path` (which calls real `toml::from_str` and the real `validate()`), and asserts on real parsed structs. No mocked filesystem, no mocked TOML, no mocked validation.
  - `test_lifecycle_state_machine` constructs a `PluginManifest` literal in-memory and walks the real `PluginRuntime` state machine through every public transition method. No subprocess mock, no Serena, no LSP, no SQLite, no LanceDB, no Docker. The lifecycle test deliberately does **not** install a global tracing subscriber (per WO `scope_out`) — it asserts against the public `state` / `error_message` fields directly.

- **Skipped / ignored tests**: searched `#\[ignore\]|\.skip\(|xfail|@pytest\.mark\.(skip|xfail)|it\.skip|xit\(` against the diff — **0 hits**. No commented-out `assert!` / `assert_eq!` introduced.

- **Weak assertions**: searched `assert!\(true\)|assert_eq!\(true,\s*true\)|assert True\b|expect\(true\)\.toBe\(true\)` — **0 hits**. Every new assertion compares against a meaningful value:
  - `test_manifest_parser`: 11 `assert_eq!` / `assert!` / `assert!(!...)` checks against parsed manifest fields, plus an `expect_err` + `match` arm that pins the error variant to `PluginError::InvalidManifest { field: "plugin.name", .. }`.
  - `test_lifecycle_state_machine`: 16+ `assert_eq!` / `assert!(matches!(...))` checks against `runtime.state` after every transition, plus a `match` arm pinning `IllegalTransition { from, to }` to the expected `(Active, Registered)` and `(Error, Stopped)` pairs.

- **Hallucinated paths / imports**: every type touched by the new code is reachable on the branch. The only new `use` line is `use std::time::Duration as Dur;` inside `test_lifecycle_state_machine` (line 1480) — `std::time::Duration` is already imported at module scope (`plugin_manager.rs:46`). All in-module symbols (`PluginError`, `PluginState`, `PluginManifest`, `PluginRuntime`, `PluginSection`, `TransportSection`, `CapabilitiesSection`, `ActivationSection`, `ResourcesSection`, `LifecycleSection`) resolve in-file. `tempfile::tempdir` is reachable — `tempfile.workspace = true` is in **both** `[dependencies]` (line 65) and `[dev-dependencies]` (line 79) of `crates/ucil-daemon/Cargo.toml`. No new workspace deps required.

- **Feature coverage**: both feature IDs in the WO have a dedicated, properly-selectored test:
  - **P2-W6-F01** → `plugin_manager::test_manifest_parser` at `src/plugin_manager.rs:1436` (module root, NOT inside `mod tests {}` — DEC-0007 frozen-selector placement). Matches the feature-list selector exactly.
  - **P2-W6-F02** → `plugin_manager::test_lifecycle_state_machine` at `src/plugin_manager.rs:1479` (module root). Matches the feature-list selector exactly.
  - **P1-W3-F06 regression guard** — `plugin_manager::test_hot_cold_lifecycle` at `src/plugin_manager.rs:1175` is structurally untouched apart from the additive `capabilities: CapabilitiesSection::default(),` and `resources: None,` field initialisers required by the new struct fields. Same shape applied at the two struct literals in `mod tests` (lines 1697 and 1754) — no semantic change to those tests' meanings.

- **Commit hygiene**: every commit subject matches Conventional Commits (`feat(daemon):`, `test(daemon):`, `refactor(daemon):`, `chore(WO-0042):`); every commit body carries the required `Phase: 2`, `Feature: P2-W6-F0[12]`, `Work-order: WO-0042`, and `Co-Authored-By:` trailers. No `--no-verify`, no force-push markers, no `--amend`-after-push markers (linear `main`-descendant history; reflog clean). Subject-line lengths well under the 70-char ceiling.

- **Doc + public API**: every new public item carries a rustdoc block.
  - Structs: `CapabilitiesSection` (lines 137–145), `ActivationSection` (lines 156–166), `ResourcesSection` (lines 178–187) — each has a multi-line `///` block describing semantics + the master-plan §14.1 reference + the conservative default behaviour.
  - Methods on `PluginManifest`: `validate` (lines 254–268, includes `# Errors`), `activates_for_language` (lines 320–326, includes empty-list semantics), `activates_for_tool` (lines 337–343).
  - `PluginRuntime` field: `error_message` (lines 446–449).
  - `PluginRuntime` ctor: `discovered` (lines 474–479).
  - Transition methods: `register` (529–533, `# Errors`), `mark_loading` (550–553, `# Errors`), `mark_active` (570–573, `# Errors`), `stop` (596–600, `# Errors`), `mark_error` (619–621, "never returns an error" note).
  - Error variants: `PluginError::InvalidManifest` (705–707), `PluginError::IllegalTransition` (737–740). Both carry per-field `///` docs on the inner fields.
  - Module-root rustdoc on `PluginRuntime` (lines 415–428) gains an ASCII state-machine diagram.
  Rustdoc-link discipline: only fully-qualified intra-doc links (`[`PluginError::InvalidManifest`]`, `[`PluginState::Discovered`]`, `[`PluginManager::run_idle_monitor`]`) are used — no shorthand `[Foo]` style that prior phase-1 WOs got rejected for. Acceptance criterion `cargo doc -p ucil-daemon --no-deps` is reported clean by the executor (line 41 of the ready-for-review marker).

- **Logging discipline**: every successful transition emits **exactly one** `tracing::info!` event under target `ucil.plugin.lifecycle` (master-plan §15.2 `ucil.<layer>.<op>`); `mark_error` uses `tracing::warn!` with the same target plus an `error = %msg` field. Centralised via the private `log_transition(plugin, from, to)` helper at lines 542–548. No `println!` / `eprintln!` introduced anywhere — searched `^\+.*(println!|eprintln!)` → **0 hits**.

- **No-unwrap-in-prod-code invariant**: searched `^\+.*\.unwrap\(\)` → **0 hits**. Searched `^\+.*\.expect\(` → 15 hits, **all inside `#[cfg(test)]` test bodies or `#[cfg(test)]` test helpers** (lines 551–756 of the diff, all inside `assert_fixture_fields` / `assert_empty_plugin_name_rejected` / `helper_manifest` / `test_manifest_parser` / `test_lifecycle_state_machine`). Permitted by `.claude/rules/rust-style.md` ("Never `.unwrap()` / `.expect()` outside `#[cfg(test)]`").

- **WO `scope_out` compliance**: spot-checked. No edits to any other crate (`ucil-core`, `ucil-treesitter`, `ucil-cli`, `ucil-lsp-diagnostics`, `ucil-embeddings`, `ucil-agents`); no edits to any other `ucil-daemon` source module (`lifecycle.rs`, `server.rs`, `executor.rs`, `session_manager.rs`, `session_ttl.rs`, `storage.rs`, `startup.rs`, `priority_queue.rs`, `text_search.rs`, `understand_code.rs`, `watcher.rs`, `test_support.rs`, `lib.rs`, `main.rs`); no edits to the frozen integration suites (`tests/plugin_manager.rs`, `tests/e2e_mcp_stdio.rs`, `tests/e2e_mcp_with_kg.rs`); no fixture changes; no schema / hook / settings drift; no global subscriber install. The HOT/COLD `tick` / `mark_call` / `with_idle_timeout` surface is preserved verbatim — only the additive `error_message` field is added to `PluginRuntime`.

## Recommendation

Proceed to verifier. No blockers. Two soft warnings on commit cadence, both ineligible for in-place fix (post-push, `--amend` forbidden) — flagged for executor awareness on the next WO.

The two pre-baked mutation checks in the WO `acceptance` block (stash `PluginRuntime::register` body → `test_lifecycle_state_machine` should fail at the first post-`register()` `assert_eq!`; stash `PluginManifest::validate` body → `test_manifest_parser` should fail at `assert_empty_plugin_name_rejected`) are well-targeted — both touch the precise lines the verifier should mutate.
