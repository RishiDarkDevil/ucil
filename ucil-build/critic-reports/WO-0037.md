# Critic Report: WO-0037

**Critic session**: crt-WO-0037-serena-g1-hover-fusion
**Branch**: feat/WO-0037-serena-g1-hover-fusion
**Ref**: 4a5d7211a73524d7615fee0c2d042fb5bc31ea37
**Work order**: `ucil-build/work-orders/0037-serena-g1-hover-fusion.json`
**Feature**: P1-W5-F02 (Serena → G1 structural fusion)
**Verdict**: CLEAN

## Scope of review

Diff `main..feat/WO-0037-serena-g1-hover-fusion`:

```
Cargo.lock                                      |   1 +
crates/ucil-daemon/Cargo.toml                   |   5 +
crates/ucil-daemon/src/executor.rs              | 487 ++++++++++++++++++
crates/ucil-daemon/src/lib.rs                   |  16 +-
ucil-build/work-orders/0037-ready-for-review.md |  48 +
```

5 commits:

```
c521987  build(daemon): add async-trait dep for SerenaHoverClient seam
0108679  feat(daemon): add SerenaHoverClient trait + enrich_find_definition fusion
7d4d358  test(daemon): add test_serena_g1_fusion + scripted fake (P1-W5-F02)
c065799  feat(daemon): re-export hover fusion surface and fix rustdoc links
4a5d721  chore(daemon): WO-0037 ready for review (P1-W5-F02)
```

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Commit subject over 70-char soft target** — `feat(daemon): add SerenaHoverClient trait + enrich_find_definition fusion` at commit `0108679` measures 73 chars. `.claude/rules/commit-style.md` says "imperative, <=70 chars". Minor; the extra 3 chars name both the trait and the fusion function in a single subject, which is arguably a better summary than truncating either name. Non-blocking — noted for future tightening.

2. **Commit size over the ~50-LOC soft target** — commit `0108679` adds 286 lines in a single commit; commit `7d4d358` adds 201. Both are logically-atomic (one surface area each: public types + fusion fn in 0108679, scripted fake + frozen acceptance test in 7d4d358) and the bulk of 0108679 is rustdoc prose (~60% of the diff) rather than executable code. The work-order estimated 6 commits and delivered 5, which is close. Non-blocking — flagged so the next WO of similar shape can split "types alone" from "fusion fn alone" to hit the 50-LOC target more cleanly.

### OK

1. **No stubs** — `ast-grep`/`rg` over the diff finds zero hits for `todo!()`, `unimplemented!()`, `raise NotImplementedError`, `pass`-only bodies, or trivial literal returns. `enrich_find_definition` (executor.rs:748-772) contains actual merge logic over a live `match` on the client option plus error-to-`None` mapping.

2. **No mocking of critical collaborators** — `rg -Ei '(serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker)'` over `mock|fake|stub|MagicMock|Mockall` yields zero hits for forbidden mocks. The `fake_serena_hover_client::ScriptedFakeSerenaHoverClient` (executor.rs:1185-1247) is a hand-written impl of UCIL's own `SerenaHoverClient` trait — the DEC-0008 dependency-inversion seam — not a mock of Serena's MCP wire format. Precedent: the identical pattern already ships in `crates/ucil-lsp-diagnostics/src/call_hierarchy.rs` (`fake_serena_client`) behind the verifier-passed `P1-W5-F06`.

3. **No skipped tests** — `rg '#\[ignore\]|\.skip\(|xfail|pytest\.mark\.skip|xit\('` returns zero hits. The frozen acceptance selector `executor::test_serena_g1_fusion` (executor.rs:1277) is a real `#[tokio::test(flavor = "current_thread")]`.

4. **No weak assertions** — every assertion in `test_serena_g1_fusion` (executor.rs:1277-1364) carries a descriptive failure message and a real comparison:
   - `assert_eq!(result_a.hover, Some(scripted_doc), "Scenario A…")` — executor.rs:1310-1314
   - `assert_eq!(result_a.resolution, resolution, …)` — executor.rs:1315-1318
   - `assert!(result_a.callers.is_empty(), …)` — executor.rs:1319-1322
   - `assert!(result_b.hover.is_none(), …)` — executor.rs:1336-1339
   - `assert_eq!(result_b.resolution, resolution, …)` — executor.rs:1340-1343
   - `assert_eq!(result_b.callers, callers_b, …)` — executor.rs:1344-1347
   - `assert!(result_c.hover.is_none(), …)` — executor.rs:1355-1359
   - `assert_eq!(result_c.resolution, resolution, …)` — executor.rs:1360-1363

5. **No hallucinated imports** — every path the diff references resolves on-branch:
   - `async_trait = { workspace = true }` — workspace root `Cargo.toml:103` declares `async-trait = "0.1"`.
   - `ucil_core::knowledge_graph::SymbolResolution` — defined at `crates/ucil-core/src/knowledge_graph.rs:261-298` with fields `id`, `qualified_name`, `file_path`, `start_line`, `signature`, `doc_comment`, `parent_module` — exact match for the test's struct-literal construction at executor.rs:1290-1298.
   - `tempfile::TempDir` used only inside `#[cfg(test)]` — already a dev-dep before this WO (unchanged).

6. **Feature coverage** — feature `P1-W5-F02` declares the acceptance selector `-p ucil-daemon executor::test_serena_g1_fusion`. The branch lands `test_serena_g1_fusion` at module root of `crates/ucil-daemon/src/executor.rs:1277` (no `mod tests { }` wrapper, per DEC-0005). The test exercises all three scripted scenarios (Serena-active, Serena-absent, Serena-error) the WO's plan summary specified.

7. **Commit hygiene** — every commit carries a Conventional Commits subject with a valid type/scope and ends with a body containing the `Phase: 1`, `Feature: P1-W5-F02`, `Work-order: WO-0037` trailers plus the Claude co-author trailer. No `--amend`-after-push, no `--no-verify`, no force-push observable. One subject slightly over 70 chars (warning above); four within bounds.

8. **Doc + public API** — every new `pub` item carries rustdoc:
   - `HoverSource` (executor.rs:526-555) — module rustdoc + per-variant docs.
   - `HoverDoc` (executor.rs:557-576) — type rustdoc + per-field docs.
   - `HoverFetchError` (executor.rs:578-611) — type rustdoc + per-variant `#[error(...)]` messages + prose variant docs.
   - `SerenaHoverClient` (executor.rs:613-653) — trait rustdoc + method rustdoc with `Returns:` block.
   - `Caller` (executor.rs:655-683) — type rustdoc + per-field docs.
   - `EnrichedFindDefinition` (executor.rs:685-707) — type rustdoc + per-field docs.
   - `enrich_find_definition` (executor.rs:709-742) — function rustdoc with `# Examples` section using `no_run` so doctests don't require a live Serena binary.
   - `pub use executor::{…}` (lib.rs:93) — new re-exports are covered by the module-level rustdoc paragraph at lib.rs:51-65.

9. **Scope adherence** — diff touches only paths the WO's `scope_in` permits (`Cargo.lock` is a required side-effect of adding `async-trait`; acceptable):
   - `crates/ucil-daemon/Cargo.toml` (one dep added)
   - `crates/ucil-daemon/src/executor.rs` (new public surface + test + scripted fake)
   - `crates/ucil-daemon/src/lib.rs` (module rustdoc + re-export line)
   - `ucil-build/work-orders/0037-ready-for-review.md` (executor's review marker)

   No edits to any `forbidden_paths` entry (`server.rs`, `plugin_manager.rs`, `lifecycle.rs`, `session_manager.rs`, `session_ttl.rs`, `storage.rs`, `startup.rs`, `priority_queue.rs`, `text_search.rs`, `understand_code.rs`, `watcher.rs`, `main.rs`, `crates/ucil-core/**`, `crates/ucil-lsp-diagnostics/**`, `tests/fixtures/**`, `ucil-build/feature-list.json`, `ucil-master-plan-v2.1-final.md`, `scripts/gate/**`, `scripts/flip-feature.sh`, `adapters/**`, `ml/**`, `plugins/**`).

10. **Phase-1 invariant compliance** —
    - Invariant 1 (no mocks of Serena critical deps): satisfied — scripted fake is a UCIL-owned-trait impl, not a Serena MCP mock.
    - Invariant 7 (tokio::time::timeout on IO awaits): not applicable — `enrich_find_definition` has no IO await of its own; the timeout is the responsibility of the live trait impl (landing in a follow-up WO). The `HoverFetchError::Timeout(Duration)` variant is prepared for that contract.
    - DEC-0005 (module-root flat tests): satisfied — `test_serena_g1_fusion` lives at module root, no `mod tests { }` wrapper. The `fake_serena_hover_client` sub-module holds non-test fixture code only, matching the lsp-diagnostics precedent.
    - `#[non_exhaustive]` discipline: `HoverSource`, `HoverFetchError` are both `#[non_exhaustive]`, preserving SemVer headroom for `HoverSource::TreeSitter` / `HoverFetchError::RateLimited` follow-ups.

## Verdict

**CLEAN** — no blockers. Verifier may proceed. The two warnings (one subject-length, one commit-size) are soft-style issues that do not affect correctness, coverage, scope, or the frozen F02 selector and should not delay the gate.

---

*Generated by the UCIL critic per `.claude/agents/critic.md` — read-only adversarial review.*
