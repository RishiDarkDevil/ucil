# Critic Report: WO-0015

**Critic session**: crt-WO-0015-20260418
**Branch**: `feat/WO-0015-lsp-diagnostics-client`
**Ref reviewed**: `b1129d8ff10918299de3f5d72cf1437304846e06`
**Merge base**: `main` (`0983b315056b9a77ab6c9e2ddb46017480a2bbf6`)
**Commits reviewed**:

```
851c608 build(lsp-diagnostics): add lsp-types and async-trait workspace deps
c09b468 feat(lsp-diagnostics): add SerenaClient trait + DiagnosticsClient module
b1129d8 chore(wo): WO-0015 ready-for-review marker — P1-W5-F04 acceptance green
```

**Diff**: 7 files changed, 1205 insertions(+), 6 deletions(-) (incl. Cargo.lock regen).
**Verdict**: **CLEAN**

---

## Findings

### Blockers (must fix before verifier)

_None._ Every mechanical blocker from `.claude/agents/critic.md` §1–8 passes:

1. **Stub detection (§1)** — `grep -nE 'todo!\(|unimplemented!\(|NotImplementedError|raise NotImplementedError'` on `crates/ucil-lsp-diagnostics/src/`: **0 hits**. No `pass`-only bodies, no trivial default returns (`Ok(Default::default())`, `-> Option<_> { None }`, bare literals) masquerading as feature impls. Each dispatch method (`diagnostics.rs:236`, `:258`, `:277`, `:295`) does real work: it calls `self.serena.<method>(…)` through the trait and lifts `Elapsed` into `DiagnosticsClientError::Timeout`.
2. **Mocked critical collaborators (§2)** — scan of `crates/ucil-lsp-diagnostics/src/` for `mock|fake|stub|MagicMock|Mockall` alongside `serena|lsp|pyright|rust-analyzer|sqlite|rusqlite|sqlx|lancedb|docker`: every hit references `FakeSerenaClient` (`diagnostics.rs:312-453`). That struct is a real impl of UCIL's own `SerenaClient` trait — the trait is the dependency-inversion seam mandated by DEC-0008 §4 so `ucil-lsp-diagnostics` stays cycle-free. It does **not** mock Serena's MCP wire protocol, an LSP subprocess, `rusqlite`, `lancedb`, or a Docker container, so phase-log invariant 1 (no mocking of critical collaborators) is respected. The literal `"fake-serena"` string in the canned `Diagnostic.source` field is a log label, not a transport mock.
3. **Skipped / ignored tests (§3)** — scan for `#\[ignore\]|\.skip\(|xfail|xit\(|@pytest\.mark\.skip|@pytest\.mark\.xfail`: **0 hits**. All five module-root `diagnostics::test_*` run unconditionally under `#[tokio::test]`.
4. **Weak assertions (§4)** — scan for `assert!\(true\)|assert_eq!\(true,\s*true\)|assert True\b|expect\(true\)\.toBe\(true\)`: **0 hits**. Each test asserts: (a) the dispatch method returned the expected canned payload (`diagnostics.rs:491-499`, `:533-540`, `:565-572`, `:599-606`), AND (b) the `FakeSerenaClient` recorded the argument so the arg actually flowed through the `tokio::time::timeout` wrapper (`diagnostics.rs:503-513`, `:542-546`, `:575-579`, `:608-616`). `test_no_lsp_configured_returns_error` (`diagnostics.rs:619-650`) destructures `BridgeError::NoLspServerConfigured { language }` and asserts `language == Language::Rust` rather than just `is_err()`.
5. **Hallucinated paths (§5)** — every `use` in the new code resolves:
   - `std::sync::Arc`, `std::time::Duration` (`diagnostics.rs:58-59`) — stdlib.
   - `async_trait::async_trait` (`diagnostics.rs:61`) — new workspace dep added in `851c608` at `Cargo.toml:85` (`async-trait = "0.1"`) and wired into `crates/ucil-lsp-diagnostics/Cargo.toml:22`.
   - `lsp_types::{CallHierarchyIncomingCall, CallHierarchyItem, CallHierarchyOutgoingCall, Diagnostic as LspDiagnostic, TypeHierarchyItem, Url}` (`diagnostics.rs:62-65`) — all six are real LSP 3.17 types from the `lsp-types` community crate (`Cargo.toml:80: lsp-types = "0.95"`, wired at `crates/ucil-lsp-diagnostics/Cargo.toml:18`).
   - `thiserror::Error` (`diagnostics.rs:66`) — existing workspace dep.
   - `crate::diagnostics::{DiagnosticsClient, SerenaClient}` in `bridge.rs:31` — resolves to new symbols at `diagnostics.rs:145` and `:206`.
   - `crate::types::{Diagnostic, Language, LspEndpoint}` in `bridge.rs:32` — unchanged from WO-0014.
   - `fake_serena_client::{FakeSerenaClient, fake_call_hierarchy_item, fake_type_hierarchy_item}` in each test (`diagnostics.rs:474`, `:519`, `:552`, `:585`) — all declared in the nested `#[cfg(test)] mod fake_serena_client` submodule.
   - `lib.rs:25-29` re-exports `BridgeError`, `LspDiagnosticsBridge`, `DiagnosticsClient`, `DiagnosticsClientError`, `SerenaClient`, `LSP_REQUEST_TIMEOUT_MS`, plus the pre-existing `types::*` — every referenced item exists.
6. **Feature coverage (§6)** — WO declares `feature_ids: ["P1-W5-F04"]`. The feature-list selector is `-p ucil-lsp-diagnostics diagnostics::` (prefix match). Five new module-root tests in `diagnostics.rs` match and cover each WO goal:
   - `diagnostics::test_diagnostics_via_serena` (`diagnostics.rs:472`) — `textDocument/diagnostic` round-trip, asserts arg reached `FakeSerenaClient.diagnostics_calls`.
   - `diagnostics::test_call_hierarchy_incoming_via_serena` (`diagnostics.rs:517`) — `callHierarchy/incomingCalls` round-trip.
   - `diagnostics::test_call_hierarchy_outgoing_via_serena` (`diagnostics.rs:550`) — `callHierarchy/outgoingCalls` round-trip.
   - `diagnostics::test_type_hierarchy_supertypes_via_serena` (`diagnostics.rs:583`) — `typeHierarchy/supertypes` round-trip.
   - `diagnostics::test_no_lsp_configured_returns_error` (`diagnostics.rs:620`) — degraded-mode error path through `bridge.require_endpoint(Language::Rust)` → `BridgeError::NoLspServerConfigured`.
   All five are at module root (not inside `mod tests { … }`) per DEC-0005 module-root discipline and the WO's `lessons_applied` §1.
7. **Commit hygiene (§7)** — all three commits use Conventional Commits (`build`, `feat`, `chore`) with the required `Phase: 1 / Feature: P1-W5-F04 / Work-order: WO-0015` trailers plus the `Co-Authored-By: Claude Opus 4.6 (1M context)` footer per `.claude/rules/commit-style.md`. No `--amend` / force-push signatures in `git log`. No touches of forbidden paths (`feature-list.json`, `feature-list.schema.json`, `ucil-master-plan-v2.1-final.md`, `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`, `ucil-build/decisions/DEC-0008-*`, `crates/ucil-lsp-diagnostics/src/types.rs`).
   - Sizes: `851c608` 314 lines (300 auto-generated `Cargo.lock` + 14 real), `c09b468` 771 lines (new module + bridge wiring + tests), `b1129d8` 120 lines (WO marker doc). The 771-line `feat` commit exceeds the 200-line soft ceiling; see the warning below.
8. **Doc + public API (§8)** — every new `pub` item carries rustdoc, and every `Result`-returning `pub` method has a `# Errors` section per `.claude/rules/rust-style.md` Errors §:
   - `diagnostics.rs`: `LSP_REQUEST_TIMEOUT_MS` (`:85`), `DiagnosticsClientError` + `Timeout` / `Transport` variants with field docs (`:98-125`), `SerenaClient` trait (`:145`) and all four trait methods with `# Errors` (`:156`, `:168`, `:178`, `:188`), `DiagnosticsClient` struct (`:206`), `DiagnosticsClient::new` (`:223`), and all four dispatch methods with `# Errors` (`:232`, `:255`, `:274`, `:292`).
   - `bridge.rs`: `BridgeError::NoLspServerConfigured` variant (`:78-87`), `LspDiagnosticsBridge::with_serena_client` (`:189`), `LspDiagnosticsBridge::diagnostics_client` (`:271`), `LspDiagnosticsBridge::require_endpoint` with `# Errors` (`:296`). The private `serena_client` field also carries a doc paragraph (`:131-147`).
   - Each `Arc<dyn SerenaClient + Send + Sync>` site calls out why `Arc` (not `Box`) and why `Send + Sync` (`tokio::spawn` downstream) — matches the master-plan §13.4 phrasing.
   - `#![deny(rustdoc::broken_intra_doc_links)]` is in effect at `lib.rs:18` and every intra-doc link resolves.

### Warnings (should fix before gate)

1. **Commit size ceiling (§7 soft)** — `c09b468 feat(lsp-diagnostics): add SerenaClient trait + DiagnosticsClient module` is **771 insertions** against the `.claude/rules/commit-style.md` ~50-line soft target and the DEC-0005 / master-plan 200-line ceiling on commits without a good reason. The stated reason is defensible: the module-root test discipline required by DEC-0005 (and WO-0015 `lessons_applied` §1) forces the five frozen-selector tests to co-locate with the trait, the error enum, the dispatch client, **and** the `FakeSerenaClient` impl that drives them — splitting any of these four pieces into a separate commit would either land a non-compiling intermediate state or break the module-root discipline for the frozen `diagnostics::` prefix. The `build` commit (`851c608`, 314 lines) is similarly justified by 300 lines of mechanical `Cargo.lock` churn. Neither is a verifier blocker, but the executor should consider splitting future WOs that introduce both a trait **and** its impl **and** its tests **and** a consumer (four logical units) into at least trait+impl+error, tests, consumer-wiring if that can be done without producing non-compiling intermediate commits. Calling this out per WO-0014 precedent so the phase post-mortem can track the ratio.
2. **`serena_client` field doc references `Self::with_serena_client` at the `P1-W5-F04` milestone** — `bridge.rs:131-147` is accurate today but the sentence "Populated via [`Self::with_serena_client`] at `P1-W5-F04`" will read as stale once later WOs add additional populators. Non-blocking; flag in case docs-writer wants to re-thread the tense during Phase 1 docs pass.

### OK

- No skipped / ignored / flaky-quarantined tests.
- No mocks of Serena MCP, LSP subprocess (`pyright`, `rust-analyzer`, `typescript-language-server`, `gopls`), `rusqlite`, `sqlx`, `lancedb`, or Docker. `FakeSerenaClient` implements UCIL's own trait.
- `tokio::time::timeout` appears 7× in `diagnostics.rs` (≥ 4 required per WO `acceptance[9]`), wrapping every `.await` in all four `DiagnosticsClient` dispatch methods with a named `Duration::from_millis(LSP_REQUEST_TIMEOUT_MS)` per rust-style.md Async §.
- `LspDiagnosticsBridge::new(serena_managed: bool) -> Self` signature is byte-for-byte preserved at `bridge.rs:161` (matches the main-branch copy at `bridge.rs:123` from `109bea4`) — DEC-0008 §Consequences freeze honoured.
- `with_serena_client` is strictly additive (`bridge.rs:189`) and `diagnostics_client` returns `Option<DiagnosticsClient>` so the `new(false)` degraded-mode path is unaffected.
- No `ucil-daemon` dep added to `crates/ucil-lsp-diagnostics/Cargo.toml` — no cycle per DEC-0008.
- `#![warn(clippy::all, clippy::pedantic, clippy::nursery)]` + `#![deny(warnings)]` in place (`lib.rs:16-17`); only two module-scoped `#![allow(clippy::module_name_repetitions)]` and a single `#[allow(clippy::struct_field_names)]` on the test-side `FakeSerenaClient`, each with an inline justification comment.
- `BridgeError` is `#[non_exhaustive]` and the new `NoLspServerConfigured` variant uses `#[error(…)]` derive without a manual `Display`, per `.claude/rules/rust-style.md` Errors §.
- Conventional Commits + `Phase: / Feature: / Work-order:` trailers on all three commits. No commit touches forbidden paths. No `--amend` / `--no-verify` / force-push footprints in `git log`.
- Ready-for-review marker at `ucil-build/work-orders/0015-ready-for-review.md` mirrors the WO-0014 precedent (manual reality-check narrative for new-module features, because `scripts/reality-check.sh` reports 0 tests when the module is stashed).
