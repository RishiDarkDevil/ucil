# Critic Report: WO-0081 — test-runner-mcp G8 plugin manifest (DEC-0024 revival)

**Critic session**: crt-wo-0081-dec-0024-tool-surface-halt
**Branch**: `feat/WO-0081-test-runner-mcp-plugin-manifest`
**Ref reviewed**: `7e7ba2c10c710f15bb4c250d27277f9a5306953d`
**Merge base**: `main`
**Commits reviewed**: 1 (`7e7ba2c chore(escalation): WO-0081 — DEC-0024 source-data errors, halt loop`)
**Diff scope**: 3 files, +525 / -0 — all under `ucil-build/` (decisions + escalations); zero source/test/plugin/script changes
**Verdict**: **BLOCKED — no implementation; halt-and-escalate authored**

---

## Executive summary

This branch does **not** implement WO-0081. The executor performed the planner-prescribed Tier-1 live capture (scope_in #5) against the upstream `test-runner-mcp` and discovered two material errors in DEC-0024's premises:

1. **`test-runner-mcp` is NOT published to npm** under the bare name (npm registry returns 404). Only a third-party scoped mirror `@iflow-mcp/mcp-test-runner@0.2.1` exists.
2. **Upstream advertises ONE tool** (`run_tests`) with a 7-element `framework` enum, NOT six distinct tools as DEC-0024 §Decision point 3 prescribed.

Hard AC #4 (`health.tools.len() >= 6`) is **unsatisfiable** by Tier-1 at every commit in upstream history (back to `463bd5d6`, 2025-01-18). The executor halted rather than silently deviate, authored `proposed-DEC-0025` with Options A/B/C, and emitted a `blocks_loop: true` escalation pending user decision.

Per the Anti-laziness contract this is the **correct behavior** — silent deviation would violate the oracle hierarchy. But for the standard verifier-acceptance pipeline the branch is **not landable**: no manifest, no install script, no replacement verify script, no new test, no implementation of any acceptance criterion.

---

## Findings

### Blockers (must fix before verifier can flip `passes=true`)

1. **No implementation of feature P3-W11-F07.** Acceptance criteria #1–#9 (manifest, install script, verify script, integration test, mutation contracts) all unsatisfied. Diff against source/test/plugin/script directories is empty (`git diff main..HEAD -- 'crates/**' 'plugins/**' 'scripts/**' 'adapters/**' 'ml/**' 'plugin/**'` = 0 lines).
   - `plugins/testing/test-runner/plugin.toml` — **MISSING** (AC #1).
   - `scripts/devtools/install-test-runner-mcp.sh` — **MISSING** (AC #2).
   - `scripts/verify/P3-W11-F07.sh` — **NOT REPLACED**; current TODO-stub on `main` still in place (AC #3).
   - `crates/ucil-daemon/tests/g8_plugin_manifests.rs::test_runner_manifest_health_check` — **MISSING** (AC #4).
   - M1 / M2 mutation contracts — **N/A** (no production manifest or test exists to mutate).

2. **No test for feature P3-W11-F07** (per critic.md §6 *Feature coverage*). Verbatim rule violation: "no test for a feature → BLOCK with 'no test for feature X'." This is a deliberate halt, not an oversight, but the rule fires regardless.

3. **`ucil-build/work-orders/0081-ready-for-review.md` not authored** (AC #10). The executor produced an escalation file in lieu of an RFR, which is consistent with halt-and-escalate semantics but makes verifier dispatch unsafe — there is nothing for the verifier to verify.

### Warnings (advisory; the executor's decision was correct, but the harness needs to react)

1. **`feat-branch-only ADR placement.`** `ucil-build/decisions/proposed-DEC-0025-test-runner-mcp-tool-surface-correction.md` (file:1–220) is a *proposed* ADR carried only on the feature branch. It is well-structured — Status, Context, two-error breakdown with verbatim `npm view` evidence and verbatim `tools/list` JSON, three decision options with cost/coverage analysis, recommendation, references — but no `decisions/` ADR has ever been merged to `main` while still in `proposed-` status. If the user accepts an option, a follow-up planner pass should rename to `DEC-0025-...` (drop the `proposed-` prefix), bump status to `accepted`, and re-emit a fresh WO with corrected ACs. Filing as a warning, not a blocker, because the prefix-flagging convention is the explicit signal to the user.

2. **Escalation references a tmp path that the verifier will not see.** `proposed-DEC-0025` cites `/tmp/wo-0081-capture.out` (file:217) as the live transcript source; the executor mitigated by also persisting the capture under `ucil-build/escalations/wo-0081-tools-list-capture.txt` (committed to the branch). Cite only the in-repo path in any successor WO body; `/tmp/*` will not survive a session boundary.

3. **Anti-laziness rule cluster around floating-ref.** WO-0081 AC criteria #5 explicitly tests `! grep -qE '"(main|latest|master|head|develop|dev|nightly)"' plugins/testing/test-runner/plugin.toml` — N/A on this branch (no manifest), but the proposed-DEC-0025 §References cites the iflow-mcp version `0.2.1` (a frozen semver) as the recommended pin. Successor WO must continue pinning to `@iflow-mcp/mcp-test-runner@0.2.1` (or its sha256 `d6ccbd99...`), not the bare scoped name.

4. **scope_in #5 pivot path is fully exhausted.** The escalation correctly documents that the upstream src/index.ts has carried the single-tool dispatcher since the repo's init commit `463bd5d6` (2025-01-18), so there is **no preceding ref** that exposes a 6-tool surface. This forecloses the "pivot down" remediation; only Option A (amend AC #4) / Option B (Tier-2 vendor) / Option C (re-defer) remain.

### OK (no findings)

- **Stub detection** — N/A; no `*.rs` / `*.py` / `*.ts` source touched. `ast-grep` for `todo!()` / `unimplemented!()` over the diff returns 0 hits.
- **Mocked critical dependencies** (Serena, LSP, SQLite, LanceDB, Docker) — N/A; no `tests/` files modified.
- **Skipped / ignored tests** (`#[ignore]`, `.skip()`, `xfail`, `pytest.mark.skip`) — N/A; no test files modified.
- **Weak assertions** (`assert!(true)`, `expect(true).toBe(true)`, etc.) — N/A; no test files modified.
- **Hallucinated paths / imports** — N/A; no `use` / `import` / `require` lines added. Cross-checked: every external-doc citation in the proposed-DEC-0025 (`DEC-0024-revive-test-runner-mcp-via-npm.md`, `DEC-0021-defer-test-runner-mcp-plugin-no-canonical-upstream.md`, `master-plan §4.8`, the WO-0081 work-order JSON) resolves to an existing file on `main` (`ls ucil-build/decisions/DEC-0024*` / `DEC-0021*` confirms presence).
- **Commit hygiene** — green:
  - Single commit `7e7ba2c`. Conventional Commits format: `chore(escalation): WO-0081 — DEC-0024 source-data errors, halt loop` (type=`chore`, scope=`escalation`, summary ≤ 70 chars).
  - Body carries `Phase: 3` / `Feature: P3-W11-F07` / `Work-order: WO-0081` trailers verbatim.
  - `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>` trailer present.
  - Diff size: 525 lines added across 3 documentation/escalation files — meta artifacts only, well within the soft 200-line guideline for justified escalation-class commits (the body itself documents the why-larger-than-guideline rationale).
  - No `--amend`, no `--no-verify`, no force-push detected. No source-side diff to reset/revert.
- **Doc + public API** — N/A; no `pub` items added. The escalation file and proposed ADR are themselves prose documentation of the halt rationale; no rustdoc / TSdoc / docstrings in scope.
- **Forbidden-path discipline** — green; the diff touches only `ucil-build/decisions/` (proposed-ADR placement allowed by harness `CLAUDE.md`) and `ucil-build/escalations/` (escalation directory; the escalate skill explicitly authors here). `ucil-build/feature-list.json` not touched. `scripts/gate/**` not touched. `scripts/flip-feature.sh` not touched. `crates/ucil-daemon/tests/plugin_manifests.rs` not touched. No fixture mutations.
- **Anti-laziness contract** — green on the affirmative items: no `passes` flip attempted, no `#[ignore]` / `.skip()`, no `todo!()` / `unimplemented!()`, no `git revert` / `git reset` of verifier-rejected code, no `--no-verify`, no `--amend` after push, no `feature-list.json` mutation outside whitelist. The executor's halt-and-escalate is the *prescribed* response (CLAUDE.md root §Oracle hierarchy: "If you encounter a conflict, STOP and write `ucil-build/decisions/proposed-*.md` — never silently deviate"); they followed the rule verbatim.

---

## Verdict rationale

**BLOCKED, but not for any anti-laziness violation.** The executor encountered a planner-side oracle conflict (DEC-0024 prescribes 6 tools; live capture returns 1) and triggered the documented escape hatch — `proposed-*.md` ADR + escalation + halt — exactly as `CLAUDE.md` root requires. They wrote no source code because writing source code under either:
- the WO's literal AC #4 (`health.tools.len() >= 6`) — would be unsatisfiable, OR
- the iflow-mcp mirror's actual single-tool surface — would be a silent deviation from a frozen WO field,

…both of which are forbidden.

The verifier **must not** flip `P3-W11-F07.passes = true` from this branch — there is nothing implemented to verify. The correct downstream action is for the user to review `proposed-DEC-0025` and choose:

- **Option A** (recommended by the proposal): land Tier-1 with iflow-mcp mirror; planner re-emits WO-0082 with AC #4 amended to framework-enum coverage. Phase 3 reaches 21/45 with minimal infra cost.
- **Option B**: pivot to Tier-2 vendored TS wrapper. ~400-600 LOC scoped re-emit.
- **Option C**: re-defer F07; supersede DEC-0024 with DEC-0025-restore-deferral. Phase 3 stays at 20/45 pending a fresh F07 strategy.

The proposed ADR is well-organized; the live-capture transcript is verbatim and reproducible (`/tmp/wo-0081-capture.py` driver shape mirrors the WO-0067/0069/0075/0076/0077/0079/0080 capture pattern); the escalation file's `blocks_loop: true` + `severity: high` + `requires_planner_action: true` + `requires_user_action: true` frontmatter correctly stops the outer loop. Triage cannot Bucket-A this (the underlying condition is not already resolved in HEAD); cannot Bucket-B it (the fix is not a < 120-line harness-script tweak); cannot Bucket-D it (the fix is not a narrow source bug). It is correctly a **Bucket-E halt** — user decision required.

---

## Recommended next actions (out of critic scope; for the user / planner)

1. **User decides Option A/B/C** on `ucil-build/decisions/proposed-DEC-0025-test-runner-mcp-tool-surface-correction.md`.
2. **Planner promotes** the chosen option to `DEC-0025-<slug>.md` (drop the `proposed-` prefix; bump `status: accepted`).
3. **Planner re-emits WO-0082** (or a successor ID) with corrected ACs that match the chosen option's structural scope.
4. **Verifier writes a rejection** for WO-0081 citing this critic report and the escalation; closes WO-0081 cleanly without flipping `passes`.
5. **Mark `ucil-build/escalations/20260508T1435Z-wo-0081-dec-0024-tool-surface-mismatch.md` resolved** only after the successor WO is emitted and the user has signed off on the option.

---

**End of critic report. Read-only review; no source code mutated.**
