## Critic Report: WO-0062

**Critic session**: crt-2026-05-07T-WO-0062
**Branch**: `feat/WO-0062-ucil-embeddings-qwen3-config-gate`
**HEAD reviewed**: `e6e041c8a534d28ee484d1728fd3a1fc671b308b` (`chore(wo): mark WO-0062 ready for review (P2-W8-F03)`)
**Merge base**: `9e8ac69` (`main`)
**Diff scope**: 7 commits, 7 paths, +1199 / -1
  - `Cargo.lock` (+2 / -1)
  - `crates/ucil-embeddings/Cargo.toml` (+2 / 0)
  - `crates/ucil-embeddings/src/config.rs` (+396 / 0, NEW)
  - `crates/ucil-embeddings/src/lib.rs` (+25 / -1)
  - `crates/ucil-embeddings/src/models.rs` (+592 / 0)
  - `scripts/verify/P2-W8-F03.sh` (+111 / 0, NEW, 0755)
  - `ucil-build/work-orders/0062-ready-for-review.md` (+72 / 0, NEW)
**Verdict**: **CLEAN with warnings**

The WO lands the config-gated Qwen3-Embedding-8B GPU upgrade path per master-plan §4.2 line 303 + §17.6 lines 2026-2030 + §18 Phase 2 Week 8 line 1787. The implementation comprises:

1. **NEW `crates/ucil-embeddings/src/config.rs`** (396 LOC): `EmbeddingBackend` enum (`CodeRankEmbed`/`Qwen3` mapping the master-plan-frozen literals), `VectorStoreConfig` struct with per-field `#[serde(default = "...")]` helpers matching the master-plan-frozen defaults, `from_toml_str` parser, `ConfigError` thiserror enum (4 variants: `Toml`, `UnknownEmbeddingModel`, `UnknownBackend`, `Validation`), and a 7-test `config_tests` block covering each variant.
2. **Additions to `models.rs`** (+592 LOC): `MIN_MATRYOSHKA_DIM`/`MAX_MATRYOSHKA_DIM` constants (32/7168 inclusive), `GpuKind` enum, `Qwen3EmbeddingError` thiserror enum (6 variants: `NoGpuDetected`, `DimensionOutOfRange`, `MissingModelFile`, `Onnx`, `Tokenizer`, `Io`), `validate_matryoshka_dimension` helper, `detect_gpu_execution_provider` runtime probe, `Qwen3Embedding::load`/`dimensions`/`model_dir` methods, the **frozen acceptance test `test_qwen3_config_gate` at column-0 module root (DEC-0007) with 12 sub-assertions**, and a 7-test `qwen3_tests` block covering each error variant + the load-bearing dim-then-gpu check ordering.
3. **`lib.rs`** (+25 / -1): `pub mod config;` declaration + `pub use config::{ConfigError, EmbeddingBackend, VectorStoreConfig};` + extension of the `pub use models::{...}` line to add `detect_gpu_execution_provider, validate_matryoshka_dimension, GpuKind, Qwen3Embedding, Qwen3EmbeddingError, MAX_MATRYOSHKA_DIM, MIN_MATRYOSHKA_DIM` — cumulative re-export streak now 15 consecutive WOs.
4. **`Cargo.toml`** (+2): `serde.workspace = true` + `toml.workspace = true` (workspace pins already in place; Cargo.lock churn is +2/-1, near-zero as the WO predicted).
5. **NEW `scripts/verify/P2-W8-F03.sh`** (111 LOC, mode 100755): `set -euo pipefail` head, frozen-selector module-root grep, frozen-test runner, qwen3_tests + config_tests runners, optional shellcheck self-lint, `[OK]`/`[FAIL]` markers.

I locally re-grepped the diff for every check in `.claude/agents/critic.md` (stubs, mocks of critical collaborators, skipped/ignored tests, weak assertions, hallucinated imports, feature coverage, commit hygiene, public-API docs) and re-ran the load-bearing AC commands inside the executor's existing worktree at `/home/rishidarkdevil/Desktop/ucil-wt/WO-0062`:

- `cargo check -p ucil-embeddings` — green.
- `cargo clippy -p ucil-embeddings --all-targets -- -D warnings` — green.
- `cargo test -p ucil-embeddings models::test_qwen3_config_gate -- --nocapture` — green; "test result: ok. 1 passed".
- `bash scripts/verify/P2-W8-F03.sh` — green; prints `[OK] P2-W8-F03 qwen3 config gate verified`.

The only divergence from a strict AC reading is AC39's `cargo doc -p ucil-embeddings --no-deps` clause — see Warning 1 below.

## Findings

### Blockers (must fix before verifier)

**None.**

### Warnings (should be acknowledged before gate)

1. **New broken intra-doc link adds 1 instance to the pre-existing `cargo doc` red state** — `crates/ucil-embeddings/src/models.rs:673` adds `/// [`test_qwen3_config_gate`] (`SA1`) and the negative-path` to the rustdoc for `Qwen3Embedding::load`. `lib.rs:53` declares `#![deny(rustdoc::broken_intra_doc_links)]`, and `cargo doc -p ucil-embeddings --no-deps` on the WO-0062 branch fails with `error: unresolved link to test_qwen3_config_gate` at `models.rs:673:11` because the test function is not part of the public API surface. AC39 ("Verifier confirms via `cargo doc -p ucil-embeddings --no-deps` exiting 0") is therefore broken on the strict reading. **However**: `cargo doc -p ucil-embeddings --no-deps` on `main` (HEAD `9e8ac69`) is **already** broken with 6 pre-existing errors (3× `OnnxSession::infer` from `models.rs:27/31` etc., 2× `OnnxSession` from `models.rs:12/48`, 1× `tokenizers::Tokenizer::encode` from `chunker.rs:176`). The WO adds 1 more on top of those 6 — the same `[`test_*`]` convention as the pre-existing `[`test_coderankembed_inference`]` reference at `models.rs:506` (also broken on main). The pre-existing red state across WO-0058 → WO-0061 indicates AC39's cargo-doc clause has not been enforced for the `lib.rs` re-export streak; the executor's RFR row for AC39 only mentions the symbol-list aspect. Verifier should decide whether to (a) accept the new instance under the same precedent, (b) require the executor to remove the new `[`test_qwen3_config_gate`]` references and add follow-up scope to repair the pre-existing 6, or (c) escalate AC39 wording to a future WO. Cargo build / clippy / test all pass; this is rustdoc-only.

2. **Single-commit LOC > 200 (DEC-0005 carve-out)** — three commits exceed the `.claude/rules/commit-style.md` ~50-LOC soft target: `12b2ef7 feat(embeddings): add Qwen3Embedding + GPU detection (P2-W8-F03)` lands +490 / -1, `c7298ed feat(embeddings): add VectorStoreConfig + EmbeddingBackend (F03)` lands +268, and `709367f test(embeddings): cover ConfigError negative paths` lands +130. The `12b2ef7` commit body explicitly cites the WO-0048 297-LOC, WO-0056 542-LOC, WO-0058 305-LOC, WO-0059 425-LOC, WO-0060 503-LOC, WO-0061 246-LOC NEW-module precedent stack and invokes the DEC-0005 module-coherence carve-out. Splitting the new types from the frozen `test_qwen3_config_gate` at module root would have triggered the `#![deny(warnings)]` cascade through `unused_imports`. Soft target with documented justification consistent with prior accepted precedents — accept per AC40.

3. **Three pre-baked mutations (AC33/34/35) verified locally then restored, NOT committed** — the WO scope_in steps 15/16/17 prescribe three pre-baked mutations on `validate_matryoshka_dimension`, `detect_gpu_execution_provider`, and `EmbeddingBackend::from_config_str`. The executor's RFR documents applying each mutation locally, observing the expected SA failure with operator-readable message, then `git checkout --` restoring; per WO-0061 lessons line 690, the verifier re-applies these mutations independently. The WO does not require the executor to commit-then-revert. Same delegation pattern as WO-0048 / WO-0056 / WO-0060 / WO-0061. Accept.

4. **Upstream-fit deviation: implementation calls `is_available()` directly rather than gating each EP probe with `cfg!(feature = "<gpu>")`** — the WO scope_in[7] prescribes an `if cfg!(feature = "<gpu>") { /* attempt EP probe */ }` ladder, but `cuda` / `tensorrt` / `directml` are features on the `ort` workspace dep, not on `ucil-embeddings` itself. The executor disclosed this in the RFR's "Upstream-fit deviation note" and calls `is_available()` directly — verified via `~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/ort-2.0.0-rc.12/src/ep/mod.rs:86` (`pub trait ExecutionProvider`) and `ep/mod.rs:118` (default `is_available()` body calling `is_ep_available(self.name())` which queries `GetAvailableProviders`). The runtime semantics are equivalent: under `default-features = false` the loaded shared library has no GPU EPs registered and every `is_available()` returns `Ok(false)` → `Err(NoGpuDetected)`. SA12 / SA1 of the frozen test pass; cargo clippy passes. WO-0058 lessons line 543 ("upstream-fitting deviations don't need ADRs") applies. Accept.

5. **`Cargo.lock` +2 / -1 churn** — `569a9ba build(embeddings): add toml + serde deps for vector_store config` adds the per-crate ucil-embeddings package entry for `toml` and `serde`; both already in the workspace dep graph (every other ucil-* crate uses them) so the churn is the predicted "near-zero" shape per WO scope_in[3]. Accept.

### OK

- **Stubs**: zero hits in shipped code. `git diff --unified=0 main...feat/WO-0062-... -- crates/ucil-embeddings/src/config.rs crates/ucil-embeddings/src/models.rs scripts/verify/P2-W8-F03.sh | grep -E '^\+' | grep -nE 'todo!\(\)|unimplemented!\(\)|panic!\(".*not yet|TODO|FIXME|raise NotImplementedError'` returns 0 matches. The `panic!()` calls inside the frozen `test_qwen3_config_gate` test (e.g. `models.rs:767, 815, 824, 838, 877`) are real assertion-failure paths with operator-readable formatting (`"SA1: expected Err(Qwen3EmbeddingError::NoGpuDetected); got {other:?}"`), not stubs. The verify script's `[FAIL]` exits are real failure paths.
- **Mocked critical collaborators**: zero hits. `git diff --unified=0 main...HEAD -- crates/ucil-embeddings/src/config.rs crates/ucil-embeddings/src/models.rs scripts/verify/P2-W8-F03.sh | grep -E '^\+' | grep -niE '\b(mock|fake|stub|fixture|magicmock|mockall)\b'` returns 0 matches. No `serena` / `lsp` / `pyright` / `rust-analyzer` / `sqlite` / `rusqlite` / `sqlx` / `lancedb` / `docker` references in additive lines. The `Qwen3Embedding::load` real-tempdir path uses `tempfile::TempDir` (already a workspace dev-dep, used in F02's negative-path tests) — production-quality fixture isolation, not a mock of the model/runtime.
- **Skipped/ignored tests**: zero. `grep -nE '#\[ignore\]|#\[cfg_attr\(.*ignore\)\]|\.skip\(|xit\(|@pytest\.mark\.skip|@pytest\.mark\.xfail|it\.skip\('` on additive lines returns 0. None of the 14 new `#[test]` blocks (1 frozen + 7 qwen3_tests + 6 config_tests, with 1 round-trip test grouping multiple checks) are gated by `#[ignore]` or have early-return bypasses.
- **Weak assertions**: zero. `grep -nE 'assert!\(true\)|assert!\(1\)|assert_eq!\(true,\s*true\)|assert_eq!\(1,\s*1\)'` on additive lines returns 0. Every `#[test]` body has at least one of `assert!`, `assert_eq!`, `assert_ne!`, or a `match`-arm `panic!` with operator-readable formatting. Spot-check: SA5 at `models.rs:792-799` uses `matches!(r, Err(Qwen3EmbeddingError::DimensionOutOfRange { value: 31, .. }))` plus an explicit panic message capturing `r:?` for diagnostic.
- **Hallucinated imports**: zero. The two new imports verified:
  - `use serde::Deserialize;` (config.rs:49) — workspace `serde = { version = "1", features = ["derive"] }` (root Cargo.toml line 34); per-crate `serde.workspace = true` (this WO).
  - `use ort::ep::ExecutionProvider;` (models.rs:609, in `detect_gpu_execution_provider`) — resolves to the trait at `~/.cargo/registry/src/.../ort-2.0.0-rc.12/src/ep/mod.rs:86` (`pub trait ExecutionProvider: Any + Send + Sync`).
  - `ort::ep::CUDA::default()` / `ort::ep::TensorRT::default()` / `ort::ep::DirectML::default()` (models.rs:611/614/617) — resolve to `~/.cargo/registry/src/.../ort-2.0.0-rc.12/src/ep/mod.rs:39/41/55` (`pub use self::cuda::CUDA;` / `pub use self::tensorrt::TensorRT;` / `pub use self::directml::DirectML;`); each struct is at `ep/cuda.rs:82` / `ep/tensorrt.rs:7` / `ep/directml.rs:52` and is **not** `#[cfg(feature = "...")]`-gated at the type level — only the runtime `is_available()` semantics vary based on the loaded shared library. `cargo check -p ucil-embeddings` confirms compilation under workspace `default-features = false`.
  - `tempfile::TempDir` in the frozen test (models.rs:756) and `qwen3_tests::test_qwen3_load_returns_dim_out_of_range_before_gpu_check` (models.rs:1208) — already in `crates/ucil-embeddings/Cargo.toml [dev-dependencies] tempfile.workspace = true` (used by F02's negative-path tests).
  - `std::path::PathBuf` (qwen3_tests:1115) — std.
- **Feature coverage**: `P2-W8-F03` (the only feature in the WO) is covered end-to-end by:
  1. **Frozen acceptance test** `models::test_qwen3_config_gate` at `models.rs:755` (column-0 `fn`, DEC-0007 module-root placement) with 12 sub-assertions:
     - SA1: `Qwen3Embedding::load(tmp.path(), 1024)` returns `Err(NoGpuDetected)` with reason text containing "GPU execution provider" or "default-features".
     - SA2: `validate_matryoshka_dimension(1024)` → `Ok(1024)` (master-plan §17.6 line 2029 default).
     - SA3: lower-bound inclusive (`32` → `Ok(32)`).
     - SA4: upper-bound inclusive (`7168` → `Ok(7168)`).
     - SA5: just-below-min (`31` → `Err(DimensionOutOfRange { value: 31, .. })`).
     - SA6: just-above-max (`7169` → `Err(DimensionOutOfRange { value: 7169, .. })`).
     - SA7: `from_config_str("qwen3-embedding")` → `Ok(Qwen3)`.
     - SA8: `from_config_str("coderankembed")` → `Ok(CodeRankEmbed)`.
     - SA9: `from_config_str("invalid-model-name")` → `Err(UnknownEmbeddingModel { name: "invalid-model-name" })`.
     - SA10: empty TOML → `VectorStoreConfig::default()` field-by-field match against master-plan-frozen defaults.
     - SA11: explicit `[vector_store]\nembedding_model = "qwen3-embedding"\nembedding_dimensions = 1024` → struct override preserved + non-overridden fields keep defaults.
     - SA12: `detect_gpu_execution_provider()` → `Err(NoGpuDetected)`.
  2. **`#[cfg(test)] mod qwen3_tests`** at `models.rs:1113-1224` — 7 tests: `test_dimension_out_of_range_value_zero`, `test_dimension_out_of_range_max_value`, `test_qwen3_error_display_renders_no_gpu`, `test_qwen3_error_display_renders_dimension_out_of_range`, `test_qwen3_error_display_renders_missing_model_file`, `test_gpu_detection_returns_no_gpu_on_default_workspace`, `test_qwen3_load_returns_dim_out_of_range_before_gpu_check` (the load-bearing dim-then-gpu check ordering).
  3. **`#[cfg(test)] mod config_tests`** at `config.rs:267-396` — 7 tests: `test_from_toml_str_returns_default_for_empty_input`, `test_from_toml_str_parses_explicit_qwen3`, `test_from_toml_str_returns_toml_error_for_malformed_input`, `test_from_config_str_returns_unknown_for_invalid_model`, `test_config_error_display_renders_canonical_text` (covers `UnknownEmbeddingModel`, `UnknownBackend`, `Validation` variants), `test_default_vector_store_config_matches_master_plan_spec`, `test_embedding_backend_from_config_str_round_trip`.
  4. **`scripts/verify/P2-W8-F03.sh`** runs the frozen + qwen3_tests + config_tests blocks and re-asserts `0 failed` on each. Locally exits 0 with `[OK] P2-W8-F03 qwen3 config gate verified`.
- **Commit-message trailers**: 7/7 commits have `Phase: 2`, 7/7 have `Feature: P2-W8-F03`, 7/7 have `Work-order: WO-0062`, 7/7 have `Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>`. All Conventional Commits with valid types: `build` × 1, `feat` × 3, `test` × 2, `chore` × 1.
- **Commit-subject lengths**: 7/7 ≤ 70 chars (max 64 chars: `feat(embeddings): add Qwen3Embedding + GPU detection (P2-W8-F03)`, `feat(embeddings): add VectorStoreConfig + EmbeddingBackend (F03)`, `build(embeddings): add toml + serde deps for vector_store config`). Per WO-0059 lessons line 607 + WO-0060 lessons line 645 ≤70 cap.
- **Allow-list discipline**: `git diff --name-only main...feat/WO-0062-...` returns exactly 7 paths, all in WO scope_in[21] / AC27 allow-list:
  1. `Cargo.lock` (+2 / -1, predicted near-zero)
  2. `crates/ucil-embeddings/Cargo.toml` (+2: `serde.workspace = true` + `toml.workspace = true`)
  3. `crates/ucil-embeddings/src/config.rs` (NEW, 396 LOC)
  4. `crates/ucil-embeddings/src/lib.rs` (+25 / -1: `pub mod config;` + per-symbol `pub use` + module-level rustdoc preamble)
  5. `crates/ucil-embeddings/src/models.rs` (+592)
  6. `scripts/verify/P2-W8-F03.sh` (NEW, 0755)
  7. `ucil-build/work-orders/0062-ready-for-review.md` (RFR marker)

  Zero forbidden-path touches: workspace `Cargo.toml` untouched (per scope_in[3]; toml/serde already pinned), all sibling crates / `adapters/**` / `ml/**` / `plugins/**` / `tests/fixtures/**` / `.githooks/**` / `.claude/**` / `scripts/gate/**` / `scripts/flip-feature.sh` untouched. `ucil-build/feature-list.json` and `ucil-master-plan-v2.1-final.md` untouched. `feat/WO-0053-lancedb-per-branch` not touched / not merged / not cherry-picked (AC32).
- **Public-API rustdoc**: every new `pub` item carries a rustdoc block:
  - `pub enum ConfigError` (config.rs:59) — 5-line `///` block citing `.claude/rules/rust-style.md` + `#[non_exhaustive]` rationale; each of the 4 variants has a `///` block of its own (lines 60-94).
  - `pub enum EmbeddingBackend` (config.rs:109) — 9-line `///` block citing master-plan §4.2 line 303; each variant carries a `///` block.
  - `pub fn EmbeddingBackend::from_config_str` (config.rs:140) — 8-line `///` with `# Errors` section.
  - `pub struct VectorStoreConfig` (config.rs:182) — 8-line `///` block; each of the 5 fields carries a `///` block citing the master-plan-§17.6 line numbers.
  - `pub fn from_toml_str` (config.rs:262) — 13-line `///` with `# Errors` + `# Examples`.
  - `pub const MIN_MATRYOSHKA_DIM` (models.rs:439) and `MAX_MATRYOSHKA_DIM` (models.rs:445) — `///` blocks citing master-plan §4.2 line 303.
  - `pub enum GpuKind` (models.rs:458) — 11-line `///` block; each of the 3 variants has a `///` line.
  - `pub enum Qwen3EmbeddingError` (models.rs:475) — 5-line `///` + each of the 6 variants has a `///` block.
  - `pub fn validate_matryoshka_dimension` (models.rs:570) — 16-line `///` with `# Errors` + `# Examples`.
  - `pub fn detect_gpu_execution_provider` (models.rs:608) — 24-line `///` with `# Errors`.
  - `pub struct Qwen3Embedding` (models.rs:645) — 12-line `///` block.
  - `pub fn Qwen3Embedding::load` (models.rs:690) — 26-line `///` with implementation order, load-bearing-check-ordering note, `# Errors` (the only intra-doc-link warning lives here — Warning 1).
  - `pub fn Qwen3Embedding::dimensions` / `model_dir` — 1-line `///` each.
  - The `#[test] fn test_qwen3_config_gate` itself has a 41-line rustdoc enumerating the 12 sub-assertions — load-bearing for verifier reference.
- **Frozen identifier `test_qwen3_config_gate` at column 0 of `models.rs`**: `grep -nE '^(pub )?fn test_qwen3_config_gate' crates/ucil-embeddings/src/models.rs | head -1` returns `755:fn test_qwen3_config_gate() {` — column-0 `fn`, DEC-0007 module-root placement. NOT inside `mod tests {}`, `mod qwen3_tests {}`, or any other sub-module. The substring selector `models::test_qwen3_config_gate` matches as `ucil_embeddings::models::test_qwen3_config_gate`.
- **`Qwen3EmbeddingError` ≥6 variants (AC12)**: `NoGpuDetected`, `DimensionOutOfRange`, `MissingModelFile`, `Onnx`, `Tokenizer`, `Io` — 6 variants, each with operator-readable `#[error("...")]` text.
- **`ConfigError` ≥4 variants (AC13)**: `Toml`, `UnknownEmbeddingModel`, `UnknownBackend`, `Validation` — 4 variants.
- **Master-plan-frozen Matryoshka bounds (AC09)**: `pub const MIN_MATRYOSHKA_DIM: usize = 32;` (models.rs:439) and `pub const MAX_MATRYOSHKA_DIM: usize = 7168;` (models.rs:445) — both `pub`, both at module root, both citing master-plan §4.2 line 303 in their rustdoc.
- **`#![deny(warnings)]` cascade preserved**: `cargo clippy -p ucil-embeddings --all-targets -- -D warnings` exits 0 — no `unused_imports`, `unused_variables`, `dead_code`, `clippy::pedantic`, or `clippy::nursery` triggers from the additions. The `#[allow(dead_code)] model_dir: PathBuf` field on `Qwen3Embedding` (models.rs:646) silences the otherwise-unread field warning since the production load path returns `Err(NoGpuDetected)` before the field is consumed; documented in the struct's rustdoc.
- **Cumulative `lib.rs` re-export streak (AC39 symbol-list)**: 15 consecutive WOs (WO-0042 → WO-0058 → WO-0059 → WO-0060 → WO-0061 → WO-0062). The 10 new public symbols are all surfaced under `ucil_embeddings::*`: `ConfigError`, `EmbeddingBackend`, `VectorStoreConfig`, `detect_gpu_execution_provider`, `validate_matryoshka_dimension`, `GpuKind`, `Qwen3Embedding`, `Qwen3EmbeddingError`, `MAX_MATRYOSHKA_DIM`, `MIN_MATRYOSHKA_DIM`. The `cargo doc -p ucil-embeddings --no-deps` clause is the only AC39 sub-condition with an issue — see Warning 1.
- **Branch sync with origin**: `git rev-parse feat/WO-0062-...` matches `git rev-parse origin/feat/WO-0062-...` — both `e6e041c8a534d28ee484d1728fd3a1fc671b308b`. RFR marker committed and pushed.
- **`unsafe` discipline**: zero `unsafe` blocks introduced.
- **Tracing instrumentation**: every public Qwen3 method is annotated with `#[tracing::instrument(name = "ucil.embeddings.qwen3.<op>", level = "debug", ...)]` per master-plan §15.2 line 1571 span-naming convention — `qwen3.detect_gpu`, `qwen3.load`, `qwen3.dimensions`, `qwen3.model_dir`.
- **Mutation prep (AC33/34/35)**: per Warning 3, the WO does not require commit-then-revert; the executor's RFR documents the local sanity-check execution ("mutation #1 — SA5 fails", "mutation #2 — SA1 fails", "mutation #3 — SA7 fails") with restoration. The verifier should re-apply each mutation literally per the WO scope_in[15/16/17] sed-able edits and confirm fail-then-restore-green.
- **Workspace regression**: the executor's RFR claims `cargo test --workspace --no-fail-fast` exits 0 (AC21). Critic does not re-run the full workspace suite (verifier scope); structural / lint / frozen-test / verify-script checks all green locally.

Phase: 2
Feature: P2-W8-F03
Work-order: WO-0062
