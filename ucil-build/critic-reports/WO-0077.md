# Critic Report: WO-0077

**Critic session**: crt-WO-0077-2026-05-08
**Branch**: feat/WO-0077-mcp-pytest-runner-plugin-manifest
**Ref**: dbe4558bd88eb2ebf7084b3ed4abd30053ede690
**Verdict**: CLEAN

## Diff scope

5 files changed, 1151 insertions(+), 4 deletions(-) (range `main..origin/feat/WO-0077-mcp-pytest-runner-plugin-manifest`):

- `plugins/testing/mcp-pytest-runner/plugin.toml` (new, 186 LOC)
- `scripts/devtools/install-mcp-pytest-runner.sh` (new, 104 LOC)
- `crates/ucil-daemon/tests/g8_plugin_manifests.rs` (new, 200 LOC)
- `scripts/verify/P3-W11-F08.sh` (TODO stub → 472 LOC real implementation; net +468)
- `ucil-build/work-orders/0077-ready-for-review.md` (new, 189 LOC informational RFR)

5 atomic commits on the feature branch, 0 merges.

## Findings

### Blockers (must fix before verifier)

None.

### Warnings (should fix before gate)

1. **Large single commit — verify script** (`8342633 test(verify): replace P3-W11-F08 TODO stub with real smoke`, 472 LOC). Exceeds the ~200-LOC soft target in `.claude/rules/commit-style.md`. Body explains the rationale (single cohesive `P3-W11-F08.sh` shell script with embedded python tool-level smoke driving the full MCP `initialize → tools/list → tools/call discover_tests → tools/call execute_tests` round-trip), and the script is one coherent shell file where splitting would be artificial. WO-0072/0074/0075/0076 verify scripts have similar sizes for the same reason — accept on the precedent. Consider citing DEC-0005 module-coherence carve-out in future commit bodies of the same shape.

### OK

- **Stub detection** — `crates/ucil-daemon/tests/g8_plugin_manifests.rs` contains no `todo!()`, `unimplemented!()`, `raise NotImplementedError`, bare-`pass`-bodies, or trivial-default returns. Real assertions throughout (`crates/ucil-daemon/tests/g8_plugin_manifests.rs:143-198`).
- **TODO-stub replacement** — `scripts/verify/P3-W11-F08.sh` no longer contains the `TODO: implement acceptance test for P3-W11-F08` literal (removed at the diff hunk; replaced with the full implementation, ad0fd78).
- **Mocked critical deps** — no `mock|fake|stub|MagicMock|Mockall` references in any of the changed files at the `tests/` or production-side paths (the only `Mocking ... is forbidden` strings are negative-assertion rustdoc lines at `crates/ucil-daemon/tests/g8_plugin_manifests.rs:11-15` per WO-0048/WO-0072/0075/0076 §executor "negative assertion forbidding mocking, NOT a mock implementation"). Real `uvx mcp-pytest-runner@0.2.1` subprocess driven by `PluginManager::health_check_with_timeout`; real pytest collection; real JSON-RPC over stdio.
- **Skipped/ignored tests** — no `#[ignore]`, `.skip(`, `xit(`, `@pytest.mark.skip`, `@pytest.mark.xfail` in any changed file.
- **Weak assertions** — no `assert!(true)`, `assert_eq!(true, true)`, `assert True`, `expect(true).toBe(true)` in any changed file. The test asserts on real return values: `manifest.plugin.name == "mcp-pytest-runner"`, `manifest.transport.kind == "stdio"`, all-`testing.*`-namespace-prefix capabilities, `health.status == HealthStatus::Ok`, `health.tools` non-empty, and presence of the `discover_tests` literal in the live `tools/list` reply (`crates/ucil-daemon/tests/g8_plugin_manifests.rs:143-198`).
- **`std::process::Command` discipline** — `! grep -qE 'std::process::Command' crates/ucil-daemon/tests/g8_plugin_manifests.rs` returns empty per the WO-0075/0076 W1 lesson pre-emptive carve-out (rustdoc rephrased to avoid the literal string).
- **Hallucinated paths** — every `use ucil_daemon::{HealthStatus, PluginManager, PluginManifest}` import resolves to a real re-export in `crates/ucil-daemon/src/lib.rs:198-202`. Methods invoked: `PluginManifest::from_path` exists at `crates/ucil-daemon/src/plugin_manager.rs:327`; `PluginManager::health_check_with_timeout` exists at `crates/ucil-daemon/src/plugin_manager.rs:1057`. Field accesses (`manifest.plugin.name`, `manifest.transport.kind`, `manifest.capabilities.provides`, `health.name`, `health.status`, `health.tools`) all resolve to real `pub` fields in `crates/ucil-daemon/src/plugin_manager.rs:113-184` and `:774-790`. `HealthStatus::Ok` enum variant at `crates/ucil-daemon/src/plugin_manager.rs:785+`. `cargo check -p ucil-daemon --tests` compiles cleanly.
- **Feature coverage** — feature P3-W11-F08 (mcp-pytest-runner G8 testing manifest) has the dedicated `mod g8_plugin_manifests::mcp_pytest_runner_manifest_health_check` test (`crates/ucil-daemon/tests/g8_plugin_manifests.rs:133-199`) exercising the manifest load + real `uvx`-launched `tools/list` round-trip + `discover_tests` advertisement assertion. The verify script `scripts/verify/P3-W11-F08.sh` adds a tool-level smoke driving `discover_tests` + `execute_tests` selective re-run against a tmpdir copy of `tests/fixtures/python-project` — directly satisfying the F08 spec line "pytest hierarchical test discovery and selective re-run by node ID verified on the fixture python-project".
- **Module-root placement** — `mod g8_plugin_manifests { ... }` declared at file root (`crates/ucil-daemon/tests/g8_plugin_manifests.rs:86`); no nested `mod tests { ... }` per DEC-0007 + WO-0069/0072/0074/0075/0076 lessons.
- **Manifest TOML sanity** — parses cleanly; required keys present: `plugin.name=mcp-pytest-runner`, `plugin.category=testing`, `plugin.priority=P0`, `plugin.version=0.2.1`, `transport.type=stdio`, `transport.command=uvx`, `capabilities.provides=[testing.pytest.discover, testing.pytest.run, testing.pytest.run_node_id]`, `capabilities.languages=[python]`, `lifecycle.hot_cold=true`, `lifecycle.idle_timeout_minutes=10`. No moving tags (`main|latest|master|head|develop|dev|nightly`).
- **Capability namespace** — all advertised capabilities under `testing.*` prefix (peer of `quality.*`, `search.*`, `navigate.*`, `knowledge.*`, `architecture.*`, `context.*`, `platform.*`).
- **Forbidden-paths discipline** — `git diff main..feat -- crates/ucil-daemon/src/ crates/ucil-core/src/ tests/fixtures/ ucil-build/feature-list.json ucil-build/feature-list.schema.json ucil-master-plan-v2.1-final.md ucil-build/decisions/ scripts/verify/P3-W11-F07.sh` returns 0 lines. `git diff main..feat -- crates/ucil-daemon/tests/g{3,4,5,6,7}_plugin_manifests.rs crates/ucil-daemon/tests/plugin_manifests.rs plugins/{architecture,context,knowledge,platform,quality,search,structural}/` returns 0 lines. All 24+ forbidden paths empty.
- **DEC-0021** — present at `ucil-build/decisions/DEC-0021-defer-test-runner-mcp-plugin-no-canonical-upstream.md` on `main` (planner-authored alongside this WO; not modified by the executor). F07 scope-out documented; lineage chain `DEC-0019 → DEC-0020 → DEC-0021` cited in the manifest, install script, and RFR.
- **Commit hygiene**:
  - All 5 commits on the feature branch are Conventional-Commit-formatted (`feat(plugins-testing)`, `feat(devtools)`, `test(daemon)`, `test(verify)`, `chore(rfr)`).
  - All 5 commits carry `Phase: 3` + `Feature: P3-W11-F08` + `Work-order: WO-0077` trailers (5 hits each via `git log feat ^main --pretty=%B | grep`).
  - 0 merges (`git log feat ^main --merges | wc -l == 0`).
  - No `--amend` / `--no-verify` / `--force` traces in the git history.
- **Doc + public API** — no new `pub` items introduced; the integration test file is private to `tests/` (cargo integration test crate). The plugin.toml carries an extensive top-of-file rustdoc-style comment block documenting source URL, PyPI package name, pinned version, binary launch command, advertised tools (live `tools/list` capture verbatim), transport type, API key requirements (NONE), working-directory invariant, and the `testing.*` namespace context. Install script has thorough header comments. The integration test file has a 70-line `//!` file-level rustdoc explaining the `UCIL_SKIP_*` env vars, fixture-pristineness invariant, peer-suite topology, W1 lesson application, and DEC-0021 lineage.
- **Live tool-name capture protocol** — RFR documents the live `serverInfo` (`pytest-mcp` v0.2.1) + advertised tool list (`execute_tests`, `discover_tests`) verbatim with full `inputSchema` subset. M2 mutation pinned on the canonical pytest-discovery entry-point (`discover_tests`) per WO-0074/0075/0076 §executor #1 lesson (snake_case-as-emitted-by-upstream, no kebab-case translation).
- **W1 lesson applied** — `! grep -qE 'std::process::Command' crates/ucil-daemon/tests/g8_plugin_manifests.rs` enforced; rustdoc rephrased ("the tokio variant of the process-spawn API (NOT the blocking standard-library variant)") to avoid the literal string per the pre-emptive carve-out.
- **AC23 substantive coverage** — RFR documents `89.65%` line coverage (≥ 85% floor per master-plan §15.4) measured via the standing `env -u RUSTC_WRAPPER cargo llvm-cov` workaround.
- **M1/M2 mutation contracts** — both pre-baked in WO scope_in #5/#6 with file/line/restore/md5-confirm protocol; RFR documents both as exercised cleanly with the WO-0072/0076 §10/§11 verbatim shapes (`PluginError::Spawn { source: Os { code: 2, kind: NotFound } }` for M1; SA-numbered panic body with full live tool-list for M2).
- **Forbidden words** — `! grep -qiE 'mock|fake|stub' plugins/testing/mcp-pytest-runner/plugin.toml scripts/devtools/install-mcp-pytest-runner.sh scripts/verify/P3-W11-F08.sh` returns empty. The only `Mocking` string in the test file is a `//! Mocking ... is forbidden` negative assertion (per WO-0048 line 363 + WO-0072/0075/0076 §executor exemption).

## Verdict

**CLEAN** — no blockers. One soft warning on commit size for the verify script (precedent-acceptable; consider DEC-0005 citation in future). Verifier may proceed.
