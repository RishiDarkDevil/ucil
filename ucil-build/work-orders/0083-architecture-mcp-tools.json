{
  "id": "WO-0083",
  "slug": "architecture-mcp-tools",
  "phase": 3,
  "week": 10,
  "features": ["P3-W10-F16", "P3-W10-F17", "P3-W10-F18"],
  "feature_ids": ["P3-W10-F16", "P3-W10-F17", "P3-W10-F18"],
  "branch": "feat/WO-0083-architecture-mcp-tools",
  "worktree_branch": "feat/WO-0083-architecture-mcp-tools",
  "executor_agent": "executor",
  "goal": "Wire three coordinated MCP tools (`get_architecture`, `trace_dependencies`, `blast_radius`) — all G4-primary per master-plan §3.2 rows 8/9/10 — through the F09 `execute_g4` orchestrator (WO-0073, `crates/ucil-daemon/src/g4.rs`) so each tool returns a structured architecture-side envelope backed by the same dependency-inversion seam (`G4Source` trait, DEC-0008 §4) that F09 already exposes. Adds three sibling handlers (`handle_get_architecture`, `handle_trace_dependencies`, `handle_blast_radius`) + a `g4_sources: Option<Arc<Vec<Arc<dyn G4Source>>>>` field on `McpServer` + `with_g4_sources()` builder + three frozen `#[tokio::test]` selectors at module root in `crates/ucil-daemon/src/server.rs` (`server::test_get_architecture_tool`, `server::test_trace_dependencies_tool`, `server::test_blast_radius_tool`). Each test injects deterministic in-process `TestG4Source` impls (DEC-0008 §4 dependency-inversion; pattern from `executor::test_g4_architecture_query` at `executor.rs:3790`) so the WO ships fully self-contained with NO real CodeGraphContext / LSP-call-hierarchy production wiring (deferred to a follow-up WO per the WO-0072/0077 production-wiring scope_out conventions).",
  "plan_summary": "Three-feature bundle around the shared G4 orchestration backbone, mirroring the WO-0067 (classifier+reason parser around §6.2/§8.6) / WO-0068 (executor+fusion around §6.1/§6.2) / WO-0070 (G3 parallel merge with TestG3Source) coordinated-features template. (1) Extend `McpServer` with `pub g4_sources: Option<Arc<Vec<Arc<dyn G4Source>>>>` + builder method `with_g4_sources(sources: Arc<Vec<Arc<dyn G4Source>>>) -> Self` (chainable off `new`/`with_knowledge_graph`/`with_g2_sources`/`with_find_similar_executor`). (2) Add three async handlers — `handle_get_architecture(id, params, sources)`, `handle_trace_dependencies(id, params, sources)`, `handle_blast_radius(id, params, sources)` — each builds a `G4Query` from MCP `arguments`, invokes `execute_g4`, projects `G4Outcome` into a tool-specific JSON envelope. (3) Wire dispatch in `handle_tools_call` for tool names `get_architecture` / `trace_dependencies` / `blast_radius` (only when `g4_sources` is `Some`; otherwise fall through to the phase-1 `_meta.not_yet_implemented: true` stub per invariant #9). (4) Three frozen `#[tokio::test]` tests at module root with deterministic `TestG4Source` impls (mirroring `executor.rs:3822` `TestG4Source` / `executor.rs:3790` `test_g4_architecture_query` shape). (5) Pre-baked M1/M2/M3 mutation contract — one mutation per feature (see scope_in #18-#20). Single feat commit lands the three handlers + the field + the builder + the three tests as one cohesive unit per DEC-0005 module-coherence (the M1/M2/M3 mutation-restoration contract requires all three sites to coexist in the same diff). Estimated total LOC: ~700-900 (three handlers ~80 LOC each, three tests ~120-180 LOC each including TestG4Source helpers, +builder/field/dispatch ~30 LOC).",
  "scope_in": [
    "1. Extend `crates/ucil-daemon/src/server.rs` `McpServer` struct: add a new optional field `pub g4_sources: Option<Arc<Vec<Arc<dyn G4Source>>>>`. Mirror the `g2_sources` / `find_similar` field shape verbatim — `Option<Arc<...>>` so callers can attach the source list after construction without forcing an existing call-site refactor. Update the four existing constructor paths (`new`, `with_knowledge_graph`, `with_g2_sources`, `with_find_similar_executor`) to default `g4_sources` to `None`. Phase-1 invariant #9 (the 19 unwired tools fall through to `_meta.not_yet_implemented: true`) is preserved for `get_architecture` / `trace_dependencies` / `blast_radius` whenever `g4_sources.is_none()`.",
    "2. Add a chainable builder method `pub fn with_g4_sources(mut self, sources: Arc<Vec<Arc<dyn G4Source>>>) -> Self` that sets `self.g4_sources = Some(sources)` and returns `self` (mirror `with_g2_sources` / `with_find_similar_executor` precedent). Rustdoc cites master-plan §3.2 rows 8/9/10 + DEC-0008 §4 (UCIL-internal trait boundary so the daemon's startup orchestrator can attach a `Vec<Arc<dyn G4Source>>` consisting of `CodeGraphContextG4Source` + `LSPCallHierarchyG4Source` (production wiring deferred per WO-0072 scope_out + WO-0073 production-wiring deferral).",
    "3. Add three sibling async handlers in the existing `impl McpServer { ... }` block (under `handle_search_code`/`handle_find_similar`): `async fn handle_get_architecture(id: &Value, params: &Value, sources: &Arc<Vec<Arc<dyn G4Source>>>) -> Value` (P3-W10-F16 / master-plan §3.2 row 8 / §18 Phase 3 Week 10 line 1812). Read MCP `arguments`: optional `target` (string seed module/file/symbol; if absent, treat as project-root scan), optional `max_depth` (u32, default 3, clamped to [0, 8]), optional `max_edges` (usize, default 256, clamped to [0, 4096]). Construct `G4Query { changed_nodes: vec![target.unwrap_or_default()], max_blast_depth: max_depth, max_edges }`. Call `crate::g4::execute_g4(sources, query).await`. Project the resulting `G4Outcome` into the response envelope: `_meta.tool == \"get_architecture\"`, `_meta.source == \"g4-architecture-fanout\"`, `_meta.modules` (sorted unique `source` and `target` symbols from `unioned.unified_edges` — the architecture surface), `_meta.edges` (the `unified_edges[]` projected as `{source, target, edge_kind, contributing_source_ids, coupling_weight}`), `_meta.master_timed_out`, `_meta.source_results` (per-source `{source_id, status, edge_count, elapsed_ms}` tuples), `_meta.token_count` (advisory only). `content[0].text` is a one-line summary `\"architecture overview: N modules, M edges from K sources\"`.",
    "4. `handle_trace_dependencies` (P3-W10-F17 / master-plan §3.2 row 9). Read MCP `arguments`: required `target` (string symbol/file), optional `direction` (string, one of `\"upstream\"` / `\"downstream\"` / `\"both\"`, default `\"both\"`), optional `max_depth` (u32, default 3, clamped to [0, 8]). Construct `G4Query { changed_nodes: vec![target], max_blast_depth: max_depth, max_edges: 1024 }`. Invoke `execute_g4`. The outcome's `unioned.blast_radius` carries depth-annotated downstream nodes (BFS following edges where target is `source`); compute the upstream chain by symmetric BFS over edges where target is `target` (i.e., reverse the edge direction in a pure-functional pass over `unioned.unified_edges`). Project the response envelope: `_meta.tool == \"trace_dependencies\"`, `_meta.source == \"g4-architecture-fanout\"`, `_meta.target` (echo), `_meta.direction` (echo), `_meta.upstream` (only when direction in {\"upstream\", \"both\"} — array of `{node, depth}` BFS-derived from reverse edges, sorted by depth ascending then node ascending), `_meta.downstream` (only when direction in {\"downstream\", \"both\"} — array of `{node, depth}` from `blast_radius` BFS, sorted same), `_meta.master_timed_out`, `_meta.source_results`, `_meta.token_count`. `content[0].text` is a one-line summary `\"trace_dependencies: U upstream + D downstream nodes (target='X', max_depth=N)\"`. Missing/non-string `target` → JSON-RPC error -32602 (invalid params) per the `find_definition` precedent.",
    "5. `handle_blast_radius` (P3-W10-F18 / master-plan §3.2 row 10). Read MCP `arguments`: required `target` (string OR array-of-strings — accept both shapes; convert single string to `vec![s]`), optional `max_depth` (u32, default 3, clamped to [0, 8]), optional `max_edges` (usize, default 1024, clamped to [0, 4096]). Construct `G4Query { changed_nodes, max_blast_depth: max_depth, max_edges }`. Invoke `execute_g4`. The outcome's `unioned.blast_radius` is the BFS-derived impacted-nodes list with `coupling_weight`-multiplicative path weighting per master-plan §5.4 line 495 — F09 already implements this in `merge_g4_dependency_union`. Project response envelope: `_meta.tool == \"blast_radius\"`, `_meta.source == \"g4-architecture-fanout\"`, `_meta.target` (echo of changed_nodes), `_meta.max_depth` (echo), `_meta.impacted` (array of `{node, depth, path_weight}` from `blast_radius`, sorted descending by `path_weight` then ascending by depth then ascending by node — coupling-weighted ranking per the master-plan), `_meta.dependency_chain` (top-K=10 paths from each seed to a leaf in the BFS tree, formatted as `[\"seed -> hop1 -> hop2 -> leaf\", ...]`; empty when blast_radius is empty), `_meta.master_timed_out`, `_meta.source_results`, `_meta.token_count`. `content[0].text` is `\"blast_radius: K impacted nodes from N seed(s), top weight=W (depth=D)\"`.",
    "6. Wire dispatch in `handle_tools_call` (currently around `crates/ucil-daemon/src/server.rs:710-734`): immediately AFTER the `find_similar` block and BEFORE the phase-1 stub fall-through, insert three guarded blocks of the same shape: `if name == \"get_architecture\" { if let Some(srcs) = self.g4_sources.as_ref() { return Self::handle_get_architecture(id, params, srcs).await; } }` (and likewise for `trace_dependencies` / `blast_radius`). When `g4_sources.is_none()`, control falls through to the `_meta.not_yet_implemented: true` stub path so phase-1 invariant #9 is preserved AND the existing `server::test_all_22_tools_registered` selector remains wire-compatible (the catalog count is unchanged at 22).",
    "7. Add THREE frozen `#[tokio::test]` tests at module root inside `crates/ucil-daemon/src/server.rs`'s existing `#[cfg(test)] mod tests { ... }` block — BUT placed at MODULE ROOT (NOT nested under any inner `mod`) per DEC-0007 + WO-0068 lessons §planner: 'frozen-test selector substring-match REQUIRES module-root placement'. Selector substring-match `cargo test -p ucil-daemon server::test_get_architecture_tool` (and the two siblings) MUST resolve uniquely without `--exact`. Confirm via `cargo test -p ucil-daemon server::test_get_architecture_tool 2>&1 | grep -E 'running|test result'` showing `1 passed; 0 failed`.",
    "8. Each test constructs an in-process `TestG4Source` impl (mirror `executor::test_g4_architecture_query`'s `TestG4Source` at `crates/ucil-daemon/src/executor.rs:3822`) declaring a deterministic dependency graph. Build a `Vec<Arc<dyn G4Source>>` of one or more TestG4Source impls, wrap in `Arc::new(...)`, attach via `McpServer::new().with_g4_sources(...)`. Drive `handle_tools_call` (or the underlying handler directly) with a constructed `params` payload, decode the response, assert on `_meta.*` fields. Use `serde_json::json!(...)` for both inputs and assertion-side expected envelopes.",
    "9. `test_get_architecture_tool` (P3-W10-F16 frozen test). Inject a `TestG4Source` whose `execute()` returns four edges: `A -> B (Import, 0.9)`, `B -> C (Call, 0.7)`, `C -> D (Implements, 0.6)`, `A -> D (Inherits, 0.4)`. Construct `params = {\"name\": \"get_architecture\", \"arguments\": {\"target\": \"A\", \"max_depth\": 4, \"max_edges\": 256}}`. Sub-assertions (each panic message uses the SA-numbered `(SAn) <semantic name>; left: ..., right: ...` format per DEC-0007 + WO-0067 lessons): SA1 `_meta.tool == \"get_architecture\"`, SA2 `_meta.source == \"g4-architecture-fanout\"`, SA3 `_meta.modules` is a sorted unique list containing `[\"A\", \"B\", \"C\", \"D\"]`, SA4 `_meta.edges.len() == 4`, SA5 the highest-weight edge's `coupling_weight == 0.9` (the `A -> B` import edge), SA6 `_meta.master_timed_out == false`, SA7 `_meta.source_results.len() == 1` (one TestG4Source), SA8 `_meta.source_results[0].status == \"available\"`.",
    "10. `test_trace_dependencies_tool` (P3-W10-F17 frozen test). Inject a `TestG4Source` whose graph yields a small chain plus a fork: `A -> B (Call, 0.9)`, `B -> C (Call, 0.8)`, `C -> D (Call, 0.7)`, `E -> B (Call, 0.6)`. Drive `params = {\"name\": \"trace_dependencies\", \"arguments\": {\"target\": \"B\", \"direction\": \"both\", \"max_depth\": 3}}`. Sub-assertions: SA1 `_meta.tool == \"trace_dependencies\"`, SA2 `_meta.target == \"B\"`, SA3 `_meta.direction == \"both\"`, SA4 `_meta.upstream.len() == 1` AND `_meta.upstream[0] == {\"node\": \"A\", \"depth\": 1}` (the only upstream — `A -> B`; `E -> B` is also upstream and SA4 is amended to `len() == 2` with both A@depth=1 AND E@depth=1, sorted alphabetically), SA5 `_meta.downstream` contains both `{\"node\": \"C\", \"depth\": 1}` AND `{\"node\": \"D\", \"depth\": 2}` sorted by depth ascending, SA6 `_meta.master_timed_out == false`, SA7 a separate sub-test calls `params` with `direction = \"upstream\"` and asserts `_meta.downstream` is absent (or null) — direction-filtering is load-bearing.",
    "11. `test_blast_radius_tool` (P3-W10-F18 frozen test). Inject a `TestG4Source` whose graph yields a 5-node BFS tree: `A -> B (Import, 0.9)`, `A -> C (Call, 0.5)`, `B -> D (Call, 0.8)`, `C -> E (Call, 0.4)`. Drive `params = {\"name\": \"blast_radius\", \"arguments\": {\"target\": \"A\", \"max_depth\": 3, \"max_edges\": 256}}`. Sub-assertions: SA1 `_meta.tool == \"blast_radius\"`, SA2 `_meta.target == [\"A\"]` (single-string target lifted to array), SA3 `_meta.impacted.len() == 4` (B, C, D, E — A itself is the seed and excluded from impacted per F09 BFS contract), SA4 `_meta.impacted[0].node` (highest path_weight) is one of `B` / `D` (the heavier-weighted import-chain — `A -> B = 0.9` and `A -> B -> D = 0.9 * 0.8 = 0.72` per multiplicative weighting), SA5 `_meta.impacted[0].path_weight >= _meta.impacted[1].path_weight` (descending sort invariant), SA6 a separate sub-test drives `params` with `target = [\"A\", \"C\"]` (array shape) and asserts `_meta.target == [\"A\", \"C\"]` (array preserved), SA7 `_meta.dependency_chain.len() >= 1` AND each entry is a non-empty string containing `\" -> \"` substring (path-shape canary), SA8 `_meta.master_timed_out == false`.",
    "12. The `TestG4Source` impl MUST live INSIDE the `#[cfg(test)] mod tests { ... }` block in `crates/ucil-daemon/src/server.rs` — under `#[cfg(test)]` so it compiles only in test profile. Test source code is exempt from the production-side word-ban grep (per WO-0048 line 363 + WO-0069 carve-out). Each test may define its own `TestG4Source` OR they may share a single `TestG4Source` helper whose `Vec<G4DependencyEdge>` is parameterised by a constructor — the latter is more compact AND more aligned with WO-0070's pattern. Either shape is acceptable; document the choice in the rustdoc adjacent to the helper.",
    "13. The new field + builder + handlers + dispatch wiring + tests + TestG4Source helper SHOULD ship in ONE cohesive feat commit per DEC-0005 module-coherence (mirroring WO-0067 ceqp.rs at 548 LOC and WO-0068 cross_group.rs at 788 LOC). The M1/M2/M3 mutation-restoration contract requires all sites to coexist in the same diff — splitting helper-by-helper would produce stub-shaped intermediate states (a handler without a dispatch wire would not be reachable) AND a non-compilable test (with the field but without the builder method, or vice versa). Cite DEC-0005 + WO-0067/0068 precedent in the commit body explicitly. Estimated LOC: ~700-900.",
    "14. Master-plan §15.2 tracing applies to the three new handlers (they ARE async/IO orchestration). Annotate each with `#[tracing::instrument(name = \"ucil.tool.<tool_name>\", skip_all, fields(target = ?args.target, max_depth = args.max_depth))]` (or equivalent) where `<tool_name>` is `get_architecture` / `trace_dependencies` / `blast_radius`. Cite §15.2 + WO-0070 lesson `For executor: §15.2 tracing on the orchestrator applies whenever async/IO is involved` (for the negative inverse: the WO-0067 /WO-0068 pure-deterministic carve-out does NOT apply here).",
    "15. The three handlers' rustdoc MUST cite (a) the master-plan tool-spec row (§3.2 row 8/9/10 verbatim), (b) the feature ID (P3-W10-F16/F17/F18), (c) the WO ID (WO-0083), (d) the underlying G4 orchestrator function (`crate::g4::execute_g4`), (e) the dependency-inversion rationale (DEC-0008 §4 — UCIL-internal `G4Source` trait so production-wiring is decoupled from MCP-tool dispatch).",
    "16. Word-ban: pre-flight grep `! grep -qiE 'mock|fake|stub' crates/ucil-daemon/src/server.rs` MUST hold for production-side surface (the three new handlers + field + builder + dispatch wiring). The TestG4Source helper + the three test bodies live under `#[cfg(test)]` and are exempt per WO-0048 line 363 + WO-0069 carve-out. Confirm via grep BEFORE first push: `head -n $(grep -n '#\\[cfg(test)\\]' crates/ucil-daemon/src/server.rs | head -1 | cut -d: -f1) crates/ucil-daemon/src/server.rs | grep -niE 'mock|fake|stub'` returns empty (only matches in production-side prefix; the existing rustdoc may mention `_meta.not_yet_implemented: true` stub — that is a documentation reference to the phase-1 stub path, NOT a stub implementation; the executor must not introduce new occurrences of the literal word `stub` in their new rustdoc beyond the existing phase-1 stub references).",
    "17. The `unsafe` keyword MUST NOT appear in the new code. No `std::env::set_var` calls. No `unsafe` blocks. All async work uses `tokio::time::timeout` only via `crate::g4::execute_g4` (which already wraps each per-source `.await` in a per-source deadline per F09 contract).",
    "18. **M1 mutation contract (P3-W10-F16 — get_architecture)**: in `handle_get_architecture` after invoking `execute_g4`, replace the `unified_edges` projection with an empty array (e.g., change `let edges_json: Vec<_> = outcome.unioned.unified_edges.iter().map(|e| ...).collect();` to `let edges_json: Vec<serde_json::Value> = vec![];` OR clear the `_meta.modules` list). Cargo-test selector `cargo test -p ucil-daemon server::test_get_architecture_tool` MUST FAIL with the SA3 panic `(SA3) modules contains all four nodes; left: [], right: [\"A\", \"B\", \"C\", \"D\"]` (or analogous SA4 `_meta.edges.len() == 4` panic). Restore via `git checkout -- crates/ucil-daemon/src/server.rs`. Pre-mutation md5 snapshot: `md5sum crates/ucil-daemon/src/server.rs > /tmp/wo-0083-server-orig.md5`.",
    "19. **M2 mutation contract (P3-W10-F17 — trace_dependencies)**: in `handle_trace_dependencies`, drop the `downstream` projection (e.g., set `_meta.downstream` to an empty array unconditionally OR skip the `blast_radius` BFS pass). Cargo-test selector `cargo test -p ucil-daemon server::test_trace_dependencies_tool` MUST FAIL with the SA5 panic `(SA5) downstream contains C@1 + D@2; left: [], right: [{C, 1}, {D, 2}]` (or analogous downstream-count assertion). Same md5 snapshot + restore as M1.",
    "20. **M3 mutation contract (P3-W10-F18 — blast_radius)**: in `handle_blast_radius`, INVERT the path-weight sort (e.g., change `.sort_by(|a, b| b.path_weight.partial_cmp(&a.path_weight).unwrap_or(Ordering::Equal))` to `.sort_by(|a, b| a.path_weight.partial_cmp(&b.path_weight).unwrap_or(Ordering::Equal))` — ascending instead of descending). Cargo-test selector `cargo test -p ucil-daemon server::test_blast_radius_tool` MUST FAIL with the SA5 panic `(SA5) impacted[0].path_weight >= impacted[1].path_weight; left: 0.36 (E), right: 0.72 (D)` (or analogous descending-sort invariant violation). Same md5 snapshot + restore as M1.",
    "21. Each mutation MUST flip its targeted test from PASS to FAIL with a substantively distinct failure mode. M1 = SA3 module-list emptiness OR SA4 edge-count mismatch (data-projection-zeroing failure mode); M2 = SA5 downstream-list emptiness (BFS-direction failure mode); M3 = SA5 sort-invariant violation (sort-direction failure mode). Three substantively distinct surfaces — verifier accepts. Pre-restoration md5sum match confirmed for each; reality-check.sh's pre-existing-stash bug is the authoritative anti-laziness layer per WO-0072/0073 lessons §verifier — `git stash` is NOT used; only `Edit` for in-place mutation + `git checkout --` for restore.",
    "22. The frozen tests' SA-numbered panic messages MUST follow the format `(SAn) <semantic name>; left: <observed>; right: <expected>` per DEC-0007 + the established WO-0066/0067/0068/0069/0070/0072 pattern. Each `assert!` / `assert_eq!` carries a panic body that names the SA, identifies the semantic check, and includes both observed and expected values. Verifier reads the panic line, identifies the SA, restores the file. No grep for which test asserted what.",
    "23. The TestG4Source impl MUST be `Send + Sync` (matches the trait bound from `crate::g4::G4Source`). Async `execute()` returns a `G4SourceOutput` with `source_id` set to a deterministic string (e.g., `\"test-g4-source\"` or `\"test-graph-A\"` per test) and `edges` populated from the test's pre-canned `Vec<G4DependencyEdge>`.",
    "24. The three handlers' input-arg validation MUST follow the `find_definition` precedent at `server.rs:789` for missing/non-string required args: return `jsonrpc_error(id, -32602, &format!(\"<tool>: <arg> required (string)\"))`. Optional args missing-or-malformed → use the spec'd default. Out-of-range values (e.g., `max_depth > 8`) MUST clamp silently to the spec'd cap (NO error envelope) — clamping is the canonical UX per master-plan §3.2 + WO-0035 (search_code) precedent for `max_results`.",
    "25. The 22-tool catalog count is preserved (the three new handlers wire EXISTING tool names from `ucil_tools()`; no new tools are added). Confirm via `cargo test -p ucil-daemon server::test_all_22_tools_registered` returning green from a `cargo clean` baseline (this test was added in WO-0010 and is the canonical regression detector for the catalog count).",
    "26. Phase-1 invariant #9 is preserved: when `g4_sources.is_none()`, the three tools fall through to the `_meta.not_yet_implemented: true` stub path. Confirm via a fourth optional sub-test (sibling to the three frozen tests) constructing `McpServer::new()` (no `with_g4_sources` call) and asserting `handle_tools_call` for `get_architecture` returns `_meta.not_yet_implemented == true`. This is OPTIONAL but recommended — it confirms the dispatch wiring respects the absent-source-list path. (NOT a load-bearing acceptance criterion for the WO; mutation contract M1/M2/M3 already cover the load-bearing surface.)",
    "27. Pre-flight cargo-clippy: `cargo clippy -p ucil-daemon -- -D warnings` MUST be green. Pre-flight cargo-fmt: `cargo fmt --check -p ucil-daemon` MUST be green. The new handlers MUST NOT introduce `.unwrap()` / `.expect()` outside `#[cfg(test)]` per `.claude/rules/rust-style.md`; use `?` and the existing `jsonrpc_error` boundary for malformed-input cases. Test code MAY use `.unwrap()` per the standard test-code carve-out.",
    "28. The new field `g4_sources` MUST be wrapped in `Arc` (matching `find_similar: Option<Arc<FindSimilarExecutor>>`) so cloning McpServer is cheap. The inner `Vec<Arc<dyn G4Source>>` is similarly `Arc`-wrapped to avoid forcing the daemon orchestrator to hold a copy of a potentially-large source list. Inner `Arc<dyn G4Source>` mirrors the boxed-trait pattern that `crate::g4::execute_g4` already accepts (the F09 signature takes `&[Arc<dyn G4Source>]`).",
    "29. M1/M2/M3 mutations are AT MOST 3 lines of source change each (in-place via `Edit`); the verifier MUST be able to apply, observe, restore, AND confirm md5sum match within 60 seconds per mutation. Pre-mutation md5: `md5sum crates/ucil-daemon/src/server.rs > /tmp/wo-0083-server-orig.md5`. Post-restore confirmation: `md5sum -c /tmp/wo-0083-server-orig.md5` MUST return `OK`. Verifier MUST take the snapshot BEFORE applying the FIRST mutation and MUST confirm restoration AFTER each mutation in sequence (M1 → restore → confirm → M2 → restore → confirm → M3 → restore → confirm).",
    "30. The RFR (ready-for-review) MUST include: (a) a `## Mutation contract` section with one row per mutation (file, line range, exact patch, targeted SA, expected panic, restore command, pre-md5 path), (b) a `## Test-type effectiveness` section naming which SA caught which mutation (zero ceremonial assertions invariant), (c) a `## Disclosed deviations` section if any scope_in directive was reinterpreted (e.g., per WO-0070 spirit-over-literal precedent — the executor is empowered to tighten an SA bound that does not catch the prescribed mutation, with disclosure), (d) a `## Trace-span coverage` section confirming each handler is annotated with `#[tracing::instrument]`, (e) a `## DEC reference` section citing DEC-0008 §4 dependency-inversion + DEC-0007 frozen-selector + DEC-0005 module-coherence + master-plan §3.2 rows 8/9/10 + §5.4 G4 BFS contract.",
    "31. Carry-forward standing protocol (38 WOs deep, WO-0058..WO-0082): coverage-gate.sh sccache RUSTC_WRAPPER workaround — `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --tests --summary-only --json | jq '.data[0].totals.lines.percent'` is the authoritative measurement. The gate-script `coverage-gate.sh ucil-daemon 85 75` reporting `line=0%` is the known sccache interaction. Skip the `[FAIL] coverage gate: ucil-{core,daemon,embeddings}` lines in phase-3.sh output per scope_out + AC standing protocol.",
    "32. Carry-forward standing protocol: AC30/AC31 phase-1/phase-2 effectiveness-gate flake (3 open escalations: `20260507T0357Z-effectiveness-nav-rust-symbol-rs-line-flake.md`, `20260507T1629Z-effectiveness-refactor-rename-python-fixture-missing-symbol.md`, `20260507T1930Z-effectiveness-nav-rust-symbol-doctest-caller-flake.md`). Executor runs constituent cargo-test + clippy + MCP-smoke sub-checks in isolation; verifier re-runs the gate-script entry point from a clean session. Pre-existing standing scope_out item; awaiting dedicated harness-improvement WO. NOT a blocker for this WO.",
    "33. AC convention per WO-0070 lessons §planner #4: 'feat branch carries zero merge commits' — `git log feat/WO-0083-architecture-mcp-tools ^main --merges` returns empty.",
    "34. Each handler's rustdoc MUST include a `# Examples` section showing a minimal `params` payload and the resulting `_meta` envelope shape (per `.claude/rules/rust-style.md` 'Every public item has a rustdoc with `# Examples` when non-trivial'). The examples may be `no_run` doctests (do not require the daemon to be live) or pure prose-comments — either is acceptable.",
    "35. The three handler functions (`handle_get_architecture` etc.) are EXPOSED at module scope as `Self::handle_*` static-dispatch (mirror `Self::handle_find_definition` precedent at `server.rs:712`) — NOT free functions. They take `(id: &Value, params: &Value, sources: &Arc<Vec<Arc<dyn G4Source>>>) -> Value` (or `-> impl Future<Output = Value> + Send` for the async variants). Dispatch in `handle_tools_call` invokes them via `Self::handle_<tool>(id, params, srcs).await`.",
    "36. The dispatch wiring MUST run AFTER the existing `find_similar` block and BEFORE the phase-1 stub fall-through `json!({...not_yet_implemented...})`. Order matters: each new tool name takes priority over the stub fall-through. The `find_similar` block sits before `// Phase-1 invariant #9: every tool handler is a stub...` comment block at server.rs:736; new blocks insert directly above that comment.",
    "37. The TestG4Source helper (parameterised constructor returning a `Vec<Arc<dyn G4Source>>`) keeps test code compact AND lets each test inject a tailored graph. Parameterise by `Vec<G4DependencyEdge>` + `String source_id`; default `G4SourceStatus::Available` + zero `elapsed_ms` for the deterministic positive path. Future negative-path testing (per-source timeouts) is OUT OF SCOPE for this WO (deferred to integration-tier coverage).",
    "38. Estimated commit cadence: ~3-4 commits total. (1) `feat(daemon): wire G4 source list onto McpServer + with_g4_sources builder` (the field + builder + dispatch hooks; ~50 LOC). (2) `feat(daemon): add handle_get_architecture (P3-W10-F16) + frozen test` (~250 LOC including TestG4Source helper). (3) `feat(daemon): add handle_trace_dependencies (P3-W10-F17) + frozen test` (~250 LOC reusing the helper). (4) `feat(daemon): add handle_blast_radius (P3-W10-F18) + frozen test` (~250 LOC). Each commit is verifiably-buildable in isolation (the dispatch hooks are gated by `g4_sources.is_some()`, so an unused field compiles cleanly) BUT per DEC-0005 module-coherence the entire suite SHOULD ship as fewer larger cohesive commits — so an alternative shape is `feat(daemon): wire three architecture MCP tools (P3-W10-F16/F17/F18)` as a SINGLE commit at ~700-900 LOC. The single-commit shape is preferred for the M1/M2/M3 mutation-restoration contract since all three sites coexist in one diff. Executor MAY choose whichever cadence supports clean compilation at every step — both are acceptable per DEC-0005 + WO-0067/0068 carve-out cited in the relevant commit body.",
    "39. The SA-numbered assertion canon: every `assert!` / `assert_eq!` in the three frozen tests carries a `(SAn) <semantic name>; left: ..., right: ...` panic body. SA1..SA8 enumerate the load-bearing semantic checks per test. The verifier should be able to read a single panic line and identify (a) which test, (b) which SA, (c) what failed semantically. NO ceremonial assertions — every SA maps to a load-bearing check (data-projection completeness, BFS-direction correctness, sort-invariant ordering, response-shape canonicality, master-deadline non-trip on the deterministic positive path).",
    "40. The DEC-0008 §4 dependency-inversion seam is the AUTHORITATIVE rationale for shipping these MCP tools without a live CodeGraphContext / LSP-call-hierarchy production-source impl. The MCP-tool surface lands NOW; the production-side `CodeGraphContextG4Source` / `LSPCallHierarchyG4Source` impls land in a follow-up WO that bundles the daemon-startup-orchestration wiring (paired with the WO-0072 plugin-runtime activation + WO-0073 G4 source registration). This is the SAME shape as G1Source (WO-0047), G2 sources (WO-0044/WO-0050/WO-0051), G3Source (WO-0070). This WO ships the MCP-tool dispatch surface fully self-contained with a deterministic test TestG4Source — production wiring is OUT OF SCOPE.",
    "41. Phase 3 progress impact: with WO-0083 landing the three architecture MCP tools, Phase 3 reaches 24/45 = 53.3% complete (up from 21/45 = 46.7%). Subsequent planner passes (WO-0084+) target the remaining 21 unfinished features (P3-W9-F11 incremental test suite, P3-W10-F01 Aider repo-map, P3-W10-F04/F09/F10/F11/F12/F13/F14/F15 G5 Context query + conflict-resolution + bonus-context selector + multi-tier merging + warm processors + dependency-cruiser + Zoekt manifests, P3-W11-F01/F05/F06 G7 quality pipeline entry + severity-merge + quality_issues table, P3-W11-F09/F10/F11/F12/F13/F14/F15/F16 G8 test discovery + check_quality + review_changes + feedback loop + integration test suites + type_check + full-pipeline integration suite)."
  ],
  "scope_out": [
    "1. Do NOT implement a real `CodeGraphContextG4Source` adapter that calls the WO-0072 codegraphcontext plugin runtime — production wiring is deferred to a follow-up WO that bundles the F09 G4 source registration with the codegraphcontext plugin lifecycle activation (per WO-0072/WO-0073 production-wiring scope_out conventions).",
    "2. Do NOT implement a real `LSPCallHierarchyG4Source` adapter that calls the P1-W5-F06 LSP call/type hierarchy bridge — same deferral as scope_out #1.",
    "3. Do NOT modify `crates/ucil-daemon/src/g4.rs` (the F09 orchestrator + trait definitions are FROZEN by WO-0073 + DEC-0008 §4). Any fix to F09 surfaces requires a separate WO + ADR.",
    "4. Do NOT modify `crates/ucil-daemon/src/executor.rs` test_g4_architecture_query (the F09 frozen test is FROZEN by WO-0073 + DEC-0007). The new tests live in `crates/ucil-daemon/src/server.rs`.",
    "5. Do NOT add new tools to the `ucil_tools()` catalog — the three tool names (`get_architecture`, `trace_dependencies`, `blast_radius`) ALREADY exist in the 22-tool catalog (per `server::test_all_22_tools_registered` — WO-0010). This WO wires existing catalog entries to real handlers.",
    "6. Do NOT introduce new external dependencies in `crates/ucil-daemon/Cargo.toml` — the three handlers reuse `serde_json` + `tracing` + `tokio` + `crate::g4` (all already present per WO-0073).",
    "7. Do NOT introduce a new MCP-server transport, sub-process spawn, or external service. The handlers are pure in-process projections of `execute_g4` outputs.",
    "8. Do NOT implement bonus-context selection (P3-W10-F11) — the response envelope's `_meta` is purely architecture-side per master-plan §3.2 row 8/9/10 + §5.4. Bonus context (conventions, pitfalls, etc.) lands in a separate WO that consumes the `_meta` shapes through the `bonus_context` selector layer.",
    "9. Do NOT implement quality-maximalist response assembly (P3-W10-F09) — that's a separate WO that consumes the `_meta` shapes through the response-assembly layer.",
    "10. Do NOT implement the `_meta.token_count` enforcement (P3-W10-F09 partial) — emit the field as advisory-only per the existing convention (`token_count` is reported but not enforced; the host adapter trims if it must).",
    "11. Do NOT modify any file under `tests/fixtures/**` — verify scripts and integration tests are read-only against the fixtures.",
    "12. Do NOT alter `scripts/gate/**` or `scripts/flip-feature.sh` (forbidden_paths).",
    "13. Do NOT add or modify entries in `ucil-build/feature-list.json` — verifier flips `passes` via `scripts/flip-feature.sh`.",
    "14. Do NOT change `ucil-master-plan-v2.1-final.md` (immutable spec).",
    "15. Do NOT modify `ucil-build/feature-list.schema.json` (post-freeze).",
    "16. Do NOT introduce `unsafe` blocks. No `std::env::set_var`. No `unsafe impl Send/Sync`. The new handlers are pure-functional projections; thread-safety comes from `Arc + Send + Sync` bounds on `G4Source`.",
    "17. Do NOT modify `scripts/verify/coverage-gate.sh` OR `scripts/verify/effectiveness-gate.sh` — the standing carry-forwards (38 WOs deep) remain out-of-scope.",
    "18. Do NOT introduce a new `UCIL_SKIP_*` env-var gate — the three handlers are in-process and have no external dependency that would warrant a skip path.",
    "19. Do NOT implement live `tools/call` integration with the daemon's stdio MCP server (WO-0040 / WO-0041) — the three frozen tests construct `McpServer` directly and invoke `handle_tools_call` (or the underlying handlers) in-process. Live stdio integration testing is OUT OF SCOPE for this WO; it lands as part of the production-wiring follow-up.",
    "20. Do NOT modify the existing seven peer integration-test files under `crates/ucil-daemon/tests/g{1..8}_plugin_manifests.rs` AND `crates/ucil-daemon/tests/plugin_manifests.rs` — they are unrelated to this WO's surface.",
    "21. Do NOT add new `#[tracing::instrument]` annotations to existing functions — only the THREE new handlers are instrumented per scope_in #14.",
    "22. Do NOT introduce a new `g4_sources` factory analogous to `G2SourceFactory` — the field is a plain `Arc<Vec<Arc<dyn G4Source>>>`; factory wiring is deferred to the production-wiring follow-up."
  ],
  "acceptance": [
    "1. `crates/ucil-daemon/src/server.rs` `McpServer` struct gains a new field `pub g4_sources: Option<Arc<Vec<Arc<dyn G4Source>>>>` AND a builder method `pub fn with_g4_sources(self, sources: Arc<Vec<Arc<dyn G4Source>>>) -> Self`.",
    "2. Three new async handlers exist as `impl McpServer` methods: `Self::handle_get_architecture`, `Self::handle_trace_dependencies`, `Self::handle_blast_radius`. Each is annotated with `#[tracing::instrument]` per master-plan §15.2.",
    "3. `handle_tools_call` dispatches the three tool names to their handlers when `g4_sources.is_some()`; falls through to the `_meta.not_yet_implemented: true` stub path when `g4_sources.is_none()`.",
    "4. Three frozen `#[tokio::test]` tests at module root: `server::test_get_architecture_tool`, `server::test_trace_dependencies_tool`, `server::test_blast_radius_tool`. Each constructs an in-process `TestG4Source`, attaches via `with_g4_sources`, exercises the handler, and asserts on SA-numbered semantic checks per scope_in #9-#11.",
    "5. `cargo test -p ucil-daemon server::test_get_architecture_tool` resolves UNIQUELY (substring-match without `--exact`) AND green from a `cargo clean` baseline.",
    "6. `cargo test -p ucil-daemon server::test_trace_dependencies_tool` resolves uniquely AND green from a `cargo clean` baseline.",
    "7. `cargo test -p ucil-daemon server::test_blast_radius_tool` resolves uniquely AND green from a `cargo clean` baseline.",
    "8. `cargo test -p ucil-daemon server::test_all_22_tools_registered` (WO-0010) remains green — the 22-tool catalog count is unchanged.",
    "9. `cargo clippy -p ucil-daemon -- -D warnings` is green.",
    "10. `cargo fmt --check -p ucil-daemon` is green.",
    "11. `cargo test -p ucil-daemon` is fully green (full-package tests pass — no regressions).",
    "12. M1 mutation (`handle_get_architecture` projection-zeroing) FAILS `server::test_get_architecture_tool` with the SA-tagged panic per scope_in #18.",
    "13. M2 mutation (`handle_trace_dependencies` downstream-drop) FAILS `server::test_trace_dependencies_tool` with the SA-tagged panic per scope_in #19.",
    "14. M3 mutation (`handle_blast_radius` sort-inversion) FAILS `server::test_blast_radius_tool` with the SA-tagged panic per scope_in #20.",
    "15. Each mutation, after `git checkout -- crates/ucil-daemon/src/server.rs`, restores the file to a state whose `md5sum` matches the pre-mutation snapshot at `/tmp/wo-0083-server-orig.md5`.",
    "16. Pre-flight word-ban grep `head -n $(grep -n '#\\[cfg(test)\\]' crates/ucil-daemon/src/server.rs | head -1 | cut -d: -f1) crates/ucil-daemon/src/server.rs | grep -niE 'mock|fake|stub'` does NOT introduce NEW `mock|fake|stub` matches in the production-side prefix beyond the existing `_meta.not_yet_implemented: true` stub-path documentation references (a baseline diff comparison is acceptable evidence).",
    "17. `git log feat/WO-0083-architecture-mcp-tools ^main --merges` returns empty (zero merge commits on the feat branch per WO-0070 lesson).",
    "18. RFR (`ucil-build/work-orders/0083-ready-for-review.md`) includes (a) Mutation contract section, (b) Test-type effectiveness section, (c) Disclosed deviations section (or `None` if the executor followed scope_in verbatim), (d) Trace-span coverage section, (e) DEC reference section.",
    "19. AC23 standing-protocol coverage measurement: `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --tests --summary-only --json | jq '.data[0].totals.lines.percent'` ≥ 80% (the `ucil-daemon` floor; pre-WO baseline is preserved by this WO's purely additive shape — the new handlers add covered paths).",
    "20. `scripts/gate/phase-3.sh` exits 0 from the verifier's clean session OR the standing-protocol carve-outs (coverage-gate sccache RUSTC_WRAPPER + effectiveness-gate flake) are explicitly cited as disclosed deviations in the RFR per scope_in #31-#32."
  ],
  "acceptance_criteria": [
    "test -f crates/ucil-daemon/src/server.rs",
    "grep -q 'pub g4_sources: Option<Arc<Vec<Arc<dyn G4Source>>>>' crates/ucil-daemon/src/server.rs",
    "grep -q 'pub fn with_g4_sources' crates/ucil-daemon/src/server.rs",
    "grep -q 'fn handle_get_architecture' crates/ucil-daemon/src/server.rs",
    "grep -q 'fn handle_trace_dependencies' crates/ucil-daemon/src/server.rs",
    "grep -q 'fn handle_blast_radius' crates/ucil-daemon/src/server.rs",
    "grep -qE '#\\[tracing::instrument' crates/ucil-daemon/src/server.rs",
    "grep -q 'fn test_get_architecture_tool' crates/ucil-daemon/src/server.rs",
    "grep -q 'fn test_trace_dependencies_tool' crates/ucil-daemon/src/server.rs",
    "grep -q 'fn test_blast_radius_tool' crates/ucil-daemon/src/server.rs",
    "cargo test -p ucil-daemon server::test_get_architecture_tool 2>&1 | grep -qE '1 passed|tests passed'",
    "cargo test -p ucil-daemon server::test_trace_dependencies_tool 2>&1 | grep -qE '1 passed|tests passed'",
    "cargo test -p ucil-daemon server::test_blast_radius_tool 2>&1 | grep -qE '1 passed|tests passed'",
    "cargo test -p ucil-daemon server::test_all_22_tools_registered 2>&1 | grep -qE '1 passed|tests passed'",
    "cargo clippy -p ucil-daemon -- -D warnings",
    "cargo fmt --check -p ucil-daemon",
    "! grep -E 'unsafe[[:space:]]*\\{' crates/ucil-daemon/src/server.rs | grep -vE '^\\s*//'",
    "test -z \"$(git log feat/WO-0083-architecture-mcp-tools ^main --merges 2>/dev/null)\"",
    "test -f ucil-build/work-orders/0083-ready-for-review.md"
  ],
  "forbidden_paths": [
    "ucil-build/feature-list.json",
    "ucil-build/feature-list.schema.json",
    "ucil-master-plan-v2.1-final.md",
    "tests/fixtures/**",
    "scripts/gate/**",
    "scripts/flip-feature.sh",
    "crates/ucil-daemon/src/g4.rs",
    "crates/ucil-daemon/src/executor.rs",
    "crates/ucil-daemon/tests/plugin_manifests.rs",
    "crates/ucil-daemon/tests/g1_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g2_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g3_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g4_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g5_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g6_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g7_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g8_plugin_manifests.rs"
  ],
  "context_refs": [
    "master-plan:§3.2 row 8 (get_architecture)",
    "master-plan:§3.2 row 9 (trace_dependencies)",
    "master-plan:§3.2 row 10 (blast_radius)",
    "master-plan:§5.4 G4 (Architecture) BFS contract + coupling-weighted ranking",
    "master-plan:§6.1 query pipeline (parallel executor)",
    "master-plan:§8.4 response shape (primary_result + _meta envelope)",
    "master-plan:§15.2 tracing spans on async/IO orchestration",
    "master-plan:§18 Phase 3 Week 10 line 1812 (G5/G6 + response assembly + bonus context)",
    "decision:DEC-0005-module-coherence-carve-out",
    "decision:DEC-0007-frozen-test-at-module-root",
    "decision:DEC-0008-lsp-bridge-via-serena-mcp-channel (§4 UCIL-internal trait dependency-inversion)",
    "phase-log:WO-0067 lessons §planner (frozen-test selector module-root placement; M1/M2/M3 mutation contract; SA-numbered panic format)",
    "phase-log:WO-0068 lessons §planner (frozen-test selector substring-match REQUIRES module-root placement)",
    "phase-log:WO-0070 lessons §planner (compute parallelism dual-bound upper limits; #[allow(dead_code)] on parallel-join helper for M1 mutation; spirit-over-literal SA tightening; G3/G4 source dependency-inversion seam)",
    "phase-log:WO-0073 lessons §planner (G4 architecture parallel query as the orchestration backbone these MCP tools dispatch through)",
    "phase-log:WO-0070 + WO-0072 production-wiring scope_out conventions (real source impls deferred to follow-up WO)",
    "rules:.claude/rules/rust-style.md (no unwrap/expect outside #[cfg(test)]; thiserror in libs; tracing on async/IO)",
    "rules:.claude/rules/commit-style.md (Conventional Commits + Phase: 3 + Feature: P3-W10-F1{6,7,8} + Work-order: WO-0083 trailers)",
    "code:crates/ucil-daemon/src/g4.rs (G4Source trait, G4Query, G4Outcome, G4UnionOutcome, execute_g4, merge_g4_dependency_union — F09 surface)",
    "code:crates/ucil-daemon/src/executor.rs:3790 (test_g4_architecture_query — TestG4Source pattern)",
    "code:crates/ucil-daemon/src/server.rs:485 (find_similar field — Option<Arc<...>> precedent)",
    "code:crates/ucil-daemon/src/server.rs:557 (with_g2_sources builder — chainable mutator precedent)",
    "code:crates/ucil-daemon/src/server.rs:710 (handle_tools_call dispatch — guarded routing pattern)",
    "code:crates/ucil-daemon/src/server.rs:789 (handle_find_definition — argument validation + jsonrpc_error envelope)"
  ],
  "dependencies_met": true,
  "estimated_commits": 4,
  "estimated_complexity": "medium",
  "created_at": "2026-05-08T22:00:00Z",
  "created_by": "planner",
  "lessons_applied": [
    "WO-0067 §planner #1 — bundle coordinated features around shared master-plan-§N.M contract (here §3.2 rows 8/9/10 + §5.4)",
    "WO-0067 §planner #2 — §15.2 tracing applies to async/IO orchestration handlers (the inverse of the pure-deterministic carve-out)",
    "WO-0068 §planner #1 — frozen-test selector substring-match REQUIRES module-root placement (NOT under inner mod)",
    "WO-0068 §planner #6 — alphabetical lib.rs ordering; NOT applicable here (no new pub mod in lib.rs — handlers stay inside server.rs)",
    "WO-0070 §planner #1 — pre-baked M1/M2/M3 mutation contract with named patches + SA expectations + restore commands",
    "WO-0070 §planner #4 — `git log feat ^main --merges = 0` form for the 'no merge commits' AC",
    "WO-0072 §planner #5 — DEC-0008 §4 dependency-inversion seam (test impls suffice for MCP-tool surface; production wiring deferred)",
    "WO-0073 §planner — G4 architecture parallel query is the orchestration backbone; MCP tools dispatch through `execute_g4`",
    "WO-0077 §planner #2 — capture both pinned-version manifest names + serverInfo.name (NOT applicable here — no plugin manifest)",
    "WO-0082 §planner #5 — bundle-around-shared-contract template (here three coordinated MCP tools)"
  ]
}
