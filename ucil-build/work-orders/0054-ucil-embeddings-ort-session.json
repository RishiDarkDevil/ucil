{
  "id": "WO-0054",
  "slug": "ucil-embeddings-ort-session",
  "phase": 2,
  "week": 8,
  "features": ["P2-W8-F01"],
  "feature_ids": ["P2-W8-F01"],
  "branch": "feat/WO-0054-ucil-embeddings-ort-session",
  "worktree_branch": "feat/WO-0054-ucil-embeddings-ort-session",
  "executor_agent": "executor",
  "goal": "Land the foundational `ort` (ONNX Runtime) integration in `crates/ucil-embeddings/` per master-plan §18 Phase 2 Week 8 line 1786 ('ucil-embeddings crate: ONNX Runtime (`ort` crate) inference'). Create `crates/ucil-embeddings/src/ort_session.rs` (NEW module) declaring `pub struct OrtSession` whose `OrtSession::from_path(model_path: &Path) -> Result<Self, OrtSessionError>` opens an ONNX model file via `ort::Session::builder().commit_from_file(...)` and exposes `input_names()` / `output_names()` accessors plus `infer(token_ids: &[i64]) -> Result<Vec<f32>, OrtSessionError>` per the feature description ('infer() accepts token IDs and returns embedding vectors'). The frozen acceptance test `ort_session::test_ort_session_loads_minimal_model` lands at MODULE ROOT of `ort_session.rs` per DEC-0007, exercising session creation + metadata + a single infer round-trip against a tiny synthetic ONNX model checked in at `crates/ucil-embeddings/tests/data/minimal.onnx`. This WO is foundational for P2-W8-F02 (CodeRankEmbed model load), P2-W8-F03 (Qwen3 GPU upgrade), P2-W8-F05 (chunker), P2-W8-F06 (throughput bench) — all those features consume `OrtSession`.",
  "plan_summary": "Create NEW file `crates/ucil-embeddings/src/ort_session.rs` (~140-200 LOC including doc + impl + 1 test). Add `ort = { version = \"2\", default-features = false, features = [\"std\", \"download-binaries\"] }` and `ndarray = { version = \"0.16\", default-features = false, features = [\"std\"] }` to workspace `[workspace.dependencies]` (alphabetical insertion). Add `ort.workspace = true` and `ndarray.workspace = true` to `crates/ucil-embeddings/Cargo.toml [dependencies]`. The `OrtSession` struct stores an `ort::Session` plus cached `Vec<String>` of input/output names so callers can introspect without re-querying the session each call. `OrtSession::from_path` does (1) `Session::builder()?.commit_from_file(model_path)` (synchronous I/O — `ort` is sync-by-default; future async wrap via `tokio::task::spawn_blocking` is the natural F02 follow-up), (2) `let input_names = session.inputs.iter().map(|i| i.name.clone()).collect();` (3) `let output_names = session.outputs.iter().map(|o| o.name.clone()).collect();` (4) `Ok(Self { session, input_names, output_names })`. `OrtSession::infer(token_ids: &[i64])` does (1) build a 2-D `ndarray::Array2<i64>` of shape `[1, token_ids.len()]` (single-batch row vector), (2) wrap as `ort::Value` via `ort::inputs!{ self.input_names[0].as_str() => array.view() }?`, (3) `let outputs = self.session.run(inputs)?;`, (4) extract first output as `outputs[0].try_extract_tensor::<f32>()?`, (5) flatten to `Vec<f32>` and return. `OrtSessionError` is a `thiserror::Error` enum with `#[non_exhaustive]` and variants `Ort { source: ort::Error }`, `Ndarray { source: ndarray::ShapeError }`, `Io { source: std::io::Error }`, `MissingInput { name: String }`, `MissingOutput { name: String }`. The test `test_ort_session_loads_minimal_model` is `#[test]` (sync — `ort` API is sync) at module root of `ort_session.rs` (NOT inside `mod tests {}`) per DEC-0007. Test exercises 4 sub-assertions: (SA1) `OrtSession::from_path` succeeds against the bundled minimal model; (SA2) `input_names().len() >= 1` and the first input name matches the minimal model's known input (e.g. `\"input_ids\"`); (SA3) `output_names().len() >= 1`; (SA4) `infer(&[1i64, 2, 3])` returns `Vec<f32>` with the expected length per the minimal model's output shape. Add `crates/ucil-embeddings/tests/data/minimal.onnx` — a tiny synthetic ONNX model (~200-2000 bytes) whose graph is a single Identity / Cast op consuming `input_ids: int64[1, *]` and producing `embedding: float32[1, *]` (or similar shape that lets `infer(&[i64; 3])` succeed). The executor MAY generate this via a one-shot `python3 -c \"import onnx; ...\"` helper at WO setup (committed alongside the .onnx fixture for reproducibility), OR copy a public-domain tiny ONNX model from upstream `ort` test fixtures with attribution. Add `pub mod ort_session;` to `crates/ucil-embeddings/src/lib.rs` and a re-export line `pub use ort_session::{OrtSession, OrtSessionError};` per the WO-0042 → WO-0053 cumulative-debt-avoidance discipline (now 11 consecutive cleared). Add `scripts/verify/P2-W8-F01.sh`. Pre-bake 2 mutation entries (literal sed AND runtime-only variant per WO-0046 lessons): (M1) `from_path` body neutered to early `Err(OrtSessionError::MissingInput { name: String::new() })` → SA1 panics on `OrtSession::from_path` returning Err; (M2) `infer` body neutered to early `Ok(Vec::new())` → SA4 panics on empty Vec when expected length > 0.",
  "scope_in": [
    "Add to `Cargo.toml` workspace `[workspace.dependencies]` section, alphabetical positions (between `lsp-types` line 105 and the `notify` block line 61 — actual position: after `ndarray` candidate row would land alphabetically between `lsp-types` and `notify`, and `ort` would land between `opentelemetry-stdout` and `rusqlite`): `ndarray = { version = \"0.16\", default-features = false, features = [\"std\"] }` and `ort = { version = \"2\", default-features = false, features = [\"std\", \"download-binaries\"] }`. The `download-binaries` feature pulls a pre-built ONNX Runtime shared library (CPU-only on x86_64-unknown-linux-gnu, ~50 MB) into the build cache so the test does not require system-installed `libonnxruntime.so` — DEC-0006 spirit (avoid manual operator setup for tests). `default-features = false` on both crates avoids transitive bloat (e.g. `ort`'s `cuda` / `tensorrt` / `directml` execution providers are NOT pulled). The pin to `ort 2.x` is the current stable line as of 2026-Q1; if the executor finds a build break on `2.0`, pick the latest `2.x` patch and document. Add to `crates/ucil-embeddings/Cargo.toml [dependencies]`: `ndarray.workspace = true`, `ort.workspace = true`. Existing entries (`thiserror.workspace = true`, `tokio.workspace = true`, `tracing.workspace = true`, `ucil-core = { path = \"../ucil-core\" }`) MUST keep their alphabetical order; insert the new lines accordingly. AC02 verifies via `grep -E '^ort\\.workspace = true' crates/ucil-embeddings/Cargo.toml` and the matching ndarray line.",
    "Create NEW file `crates/ucil-embeddings/src/ort_session.rs` (~140-200 LOC including doc + impl + 1 test). Module-level `//!` rustdoc preamble cites master-plan §18 Phase 2 Week 8 line 1786 verbatim ('ucil-embeddings crate: ONNX Runtime (`ort` crate) inference'). The preamble names: (a) the role of `OrtSession` as the foundational session abstraction shared across all W8 features (CodeRankEmbed F02, Qwen3 F03, chunker F05, throughput bench F06), (b) the deliberate sync API surface (ONNX Runtime is sync by design; future async wrap is `tokio::task::spawn_blocking` per F02), (c) the input/output-names cache so consumers can introspect once without re-querying the session, (d) the deferral of the actual CodeRankEmbed model download to F02 (P2-W8-F02). Include a `# Examples` block on `OrtSession::from_path` and `OrtSession::infer` per `.claude/rules/rust-style.md`.",
    "Declare `#[derive(Debug, thiserror::Error)] #[non_exhaustive] pub enum OrtSessionError` with variants: (a) `#[error(\"ort session error: {source}\")] Ort { #[from] source: ort::Error }` (auto-conversion from ort errors via `?`); (b) `#[error(\"ndarray shape error: {source}\")] Ndarray { #[from] source: ndarray::ShapeError }`; (c) `#[error(\"io error reading model file: {source}\")] Io { #[from] source: std::io::Error }`; (d) `#[error(\"model has no input named {name:?}\")] MissingInput { name: String }`; (e) `#[error(\"model has no output named {name:?}\")] MissingOutput { name: String }`. The `#[non_exhaustive]` attribute follows `.claude/rules/rust-style.md` libraries-use-thiserror invariant.",
    "Declare `pub struct OrtSession { session: ort::Session, input_names: Vec<String>, output_names: Vec<String> }` with `#[derive(Debug)]` only (NOT `Clone` — `ort::Session` is not `Clone`; consumers that need shared ownership must wrap in `Arc`). The struct is NOT `Send + Sync` automatically — `ort::Session` may or may not be (depends on backend); document the actual auto-trait status in the rustdoc and add `// SAFETY:` notes only if any unsafe trait impl is needed. Most likely `ort::Session: Send + Sync` directly so no manual impl required.",
    "Implement `pub fn from_path(model_path: &Path) -> Result<Self, OrtSessionError>` with this exact contract: (1) `let session = ort::Session::builder()?.commit_from_file(model_path)?;` (returns `OrtSessionError::Ort` on parse / file I/O via the `?` chain — `ort::Error` covers both); (2) `let input_names: Vec<String> = session.inputs.iter().map(|input| input.name.clone()).collect();` (3) `let output_names: Vec<String> = session.outputs.iter().map(|output| output.name.clone()).collect();` (4) `Ok(Self { session, input_names, output_names })`. The function is sync — `ort::Session::builder()` and `commit_from_file` are sync. Tracing: `#[tracing::instrument(name = \"ucil.embeddings.ort_session.load\", level = \"debug\", skip(model_path), fields(model_path = ?model_path))]` per master-plan §15.2 span naming `ucil.<layer>.<op>`. The `model_path: &Path` (NOT `&PathBuf`) follows Rust idiom; callers can pass `&path_buf`.",
    "Implement `pub fn input_names(&self) -> &[String]` and `pub fn output_names(&self) -> &[String]` (zero-cost slice accessors). Rustdoc cites the cache rationale (one tensor introspection at load time → consumers iterate without re-querying).",
    "Implement `pub fn infer(&self, token_ids: &[i64]) -> Result<Vec<f32>, OrtSessionError>` with this contract: (1) Build a 2-D `ndarray::Array2<i64>` of shape `[1, token_ids.len()]` from the input slice via `ndarray::Array2::from_shape_vec((1, token_ids.len()), token_ids.to_vec())?` (returns `OrtSessionError::Ndarray` on shape mismatch — defensive; the literal shape construction always matches but the `from_shape_vec` API is fallible). (2) Locate the first input name: `let input_name = self.input_names.first().ok_or_else(|| OrtSessionError::MissingInput { name: String::from(\"<first>\") })?;` (3) Build the `ort::Value` input via `ort::inputs!{ input_name.as_str() => array.view() }?` (the `inputs!` macro is the ergonomic ort 2.x API; if a future `ort` revision changes the macro name, the executor adapts and documents). (4) `let outputs = self.session.run(inputs)?;` — synchronous run. (5) Locate the first output name: `let output_name = self.output_names.first().ok_or_else(|| OrtSessionError::MissingOutput { name: String::from(\"<first>\") })?;` (6) Extract `let tensor = outputs[output_name.as_str()].try_extract_tensor::<f32>()?;` (returns `OrtSessionError::Ort` on dtype mismatch). (7) `let view = tensor.view();` `let flat: Vec<f32> = view.iter().copied().collect();` (8) `Ok(flat)`. The function is sync. Tracing: `#[tracing::instrument(name = \"ucil.embeddings.ort_session.infer\", level = \"debug\", skip(self, token_ids), fields(token_count = token_ids.len()))]`. NOTE: `ort` 2.x specifics may shift the exact API surface; the executor adapts the call shape (e.g. `ort::Value::from_array` vs `ort::inputs!`) while preserving the contract — IF the API has changed materially since the WO was written, document the divergence in the ready-for-review note and NOT in an ADR (no novel design choice — purely fitting upstream).",
    "Add `crates/ucil-embeddings/tests/data/minimal.onnx` — a tiny synthetic ONNX model (~200-2000 bytes) whose graph satisfies the test contract: input `input_ids` of dtype `int64` and shape `[batch=1, seq=*]` (dynamic seq dimension), output `embedding` of dtype `float32` with a deterministic shape that lets `infer(&[1i64, 2, 3])` produce a non-empty `Vec<f32>`. The model body MAY be a single `Cast` op (int64 → float32) or `Identity` over a literal initializer, OR a `Gather` from a tiny embedding table (closer to real CodeRankEmbed structure). The simplest valid graph is `Cast int64 → float32` followed by no-op shape reshape. The executor MAY generate this via `python3 -c 'import onnx, onnx.helper as h; ...; onnx.save(model, \"crates/ucil-embeddings/tests/data/minimal.onnx\")'` and check the resulting bytes (along with the generation script at `crates/ucil-embeddings/tests/data/build_minimal_onnx.py` for reproducibility) — OR copy a public-domain tiny ONNX from upstream sources with explicit attribution in a sibling LICENSE.txt. The fixture path `crates/ucil-embeddings/tests/data/` is intentionally inside the crate (NOT under repo-root `tests/fixtures/**` which is forbidden) so the per-crate test discipline holds.",
    "Add the frozen acceptance test `#[test] fn test_ort_session_loads_minimal_model()` at MODULE ROOT of `crates/ucil-embeddings/src/ort_session.rs` (NOT inside `mod tests {}`) per DEC-0007. The test selector `ort_session::test_ort_session_loads_minimal_model` must resolve to this exact module-root function. Test body (4 sub-assertions in order):  \n  **Setup**: locate the bundled fixture via `let manifest_dir = std::path::PathBuf::from(env!(\"CARGO_MANIFEST_DIR\")); let model_path = manifest_dir.join(\"tests\").join(\"data\").join(\"minimal.onnx\");`. `env!(\"CARGO_MANIFEST_DIR\")` resolves to `crates/ucil-embeddings/` so the fixture lookup is workspace-position-invariant.  \n  **(SA1) Session loads**: `let session = OrtSession::from_path(&model_path).expect(\"OrtSession::from_path on minimal model\");` — any `Err` panics with the underlying `OrtSessionError` Debug-print so the verifier sees the actual failure mode.  \n  **(SA2) Input metadata accessible**: `let inputs = session.input_names(); assert!(!inputs.is_empty(), \"minimal model must declare at least one input; got {:?}\", inputs); assert_eq!(inputs[0], \"input_ids\", \"first input name must match the minimal model contract; got {:?}\", inputs[0]);` (the executor pins the literal input name in the .onnx fixture so the assertion is deterministic).  \n  **(SA3) Output metadata accessible**: `let outputs = session.output_names(); assert!(!outputs.is_empty(), \"minimal model must declare at least one output; got {:?}\", outputs);` (output name is more variable; just check non-empty).  \n  **(SA4) Infer round-trip**: `let result = session.infer(&[1i64, 2, 3]).expect(\"infer on minimal model\"); assert!(!result.is_empty(), \"infer must produce a non-empty Vec<f32>; got len={}\", result.len()); assert_eq!(result.len(), 3, \"minimal model with Cast/Identity over [1,3] returns 3 floats; got len={}\", result.len());` — the literal `3` matches the input slice length for an Identity / Cast minimal model. If the executor picks a different minimal model topology (e.g. Gather producing a fixed-dim embedding), the assertion's expected length matches the actual output shape; document any non-`len()==3` choice in the ready-for-review note.  \n  All assertions follow the `assert_eq!(actual, expected, \"<plain-English description>; got {:?}\", actual)` shape per WO-0042 lessons line 92 + WO-0051 lessons line 405.",
    "Update `crates/ucil-embeddings/src/lib.rs`: (a) add `pub mod ort_session;` immediately after the existing crate-level lints block (lines 5-7) so the module is exported; (b) add a NEW re-export line `pub use ort_session::{OrtSession, OrtSessionError};` immediately after the `pub mod` line. (c) extend the existing module-level `//!` preamble (line 1) with one trailing paragraph describing the `OrtSession` surface + the F02/F05 deferral pattern (consumers of `OrtSession` land in subsequent WOs) + master-plan §18 Phase 2 Week 8 line 1786 citation. Verify post-edit via `grep -nE 'pub mod ort_session;|pub use ort_session::' crates/ucil-embeddings/src/lib.rs | wc -l` returning `2`.",
    "Add `scripts/verify/P2-W8-F01.sh` per the standing convention (model on `scripts/verify/P2-W7-F02.sh`). Script must: (a) run `cargo test -p ucil-embeddings ort_session::test_ort_session_loads_minimal_model -- --nocapture` and tee stdout to `/tmp/test-WO-0054.log`; (b) match either `cargo test`'s `test result: ok. ... 0 failed` shape OR nextest's `... tests passed` shape via the tolerant alternation `grep -Eq 'test result: ok\\. .* 0 failed|[0-9]+ tests? passed'` (per WO-0042 / WO-0048 / WO-0051 lessons — same reusable shape); (c) on non-zero exit, emit `[FAIL] P2-W8-F01: <reason>` with a hint pointing at `tests/data/minimal.onnx` + the WO + master-plan §18 Phase 2 Week 8 line 1786; (d) `chmod +x scripts/verify/P2-W8-F01.sh` so the verifier can run it directly. Header comment block cites WO-0054 + P2-W8-F01 + the test selector verbatim. Include the `shellcheck` PATH-fallback pattern from WO-0044 onwards.",
    "**Anti-cascade for `#![deny(warnings)]`**: per-mutation patches MUST use the runtime-only variant pattern from WO-0046 lessons line 245 — for `from_path` mutation (M1: early-return Err), the body becomes `#[allow(unused_variables)] { let _ = model_path; return Err(OrtSessionError::MissingInput { name: String::new() }); }` and `#[allow(unreachable_code)]` on the remainder; for `infer` mutation (M2: empty Vec return), body becomes `#[allow(unused_variables)] { let _ = self; let _ = token_ids; return Ok(Vec::new()); }` and `#[allow(unreachable_code)]` on the remainder. Document BOTH the literal sed AND runtime-only variants in `acceptance` mutation entries (AC15 / AC16).",
    "**Pre-flight rustdoc grep before pushing** (per WO-0043 lessons line 128 `clippy::doc_markdown`): `rg -nE '^\\s*///.*\\b[A-Z][A-Z_0-9]+\\b' crates/ucil-embeddings/src/ort_session.rs` — every uppercase identifier in rustdoc paragraphs MUST be wrapped in backticks (`` `OrtSession` ``, `` `ONNX` ``, `` `CodeRankEmbed` ``, etc.). The crate sets `#![deny(rustdoc::broken_intra_doc_links)]`; use plain backticks NOT shorthand `[Foo]` intra-doc links unless fully-qualified.",
    "**Operator-readable assertion messages** (per WO-0051 lessons line 405): every `assert_eq!` / `assert!` in `test_ort_session_loads_minimal_model` follows the shape `assert!(<predicate>, \"<plain-English description>; got {:?}\", <actual_value>)` so a verifier failure prints the actual observed value, not just the line number.",
    "**Commit ladder — 6 commits** per `.claude/rules/commit-style.md` and DEC-0005 module-coherence carve-out:  \n  (1) `build(embeddings): add ort + ndarray workspace deps for ONNX Runtime inference` (~15 LOC: 2 workspace deps + 2 `[dependencies]` lines), \n  (2) `feat(embeddings): add OrtSession skeleton with error type and metadata cache` (~50 LOC: module preamble, `OrtSessionError`, `OrtSession` struct, `input_names` + `output_names` accessors), \n  (3) `feat(embeddings): implement OrtSession::from_path loading via ort::Session::commit_from_file` (~40 LOC: `from_path` + tracing instrument), \n  (4) `feat(embeddings): implement OrtSession::infer with single-batch ndarray tensor wrapping` (~50 LOC: `infer` + tracing instrument), \n  (5) `test(embeddings): add ort_session::test_ort_session_loads_minimal_model with bundled minimal.onnx fixture` (~80 LOC: minimal.onnx fixture + build_minimal_onnx.py generator + test body — DEC-0005 carve-out: 4 sub-assertions form one coherent regression-guard for the feature contract), \n  (6) `feat(embeddings): wire ort_session into lib.rs preamble and re-exports + add scripts/verify/P2-W8-F01.sh` (~30 LOC). \n  Each commit signed via the conventional `Phase: 2 / Feature: P2-W8-F01 / Work-order: WO-0054` trailer."
  ],
  "scope_out": [
    "**No CodeRankEmbed model download or integration.** Master-plan §18 Phase 2 Week 8 line 1787 names CodeRankEmbed as the default 137M / CPU embedding model — that is the F02 (P2-W8-F02) deliverable. F01 ships the foundational `OrtSession` API + a tiny synthetic minimal.onnx fixture for testing only. NO download to `ml/models/coderank/`, NO download to anywhere else; F02 owns the production model placement.",
    "**No Qwen3-Embedding GPU upgrade path.** P2-W8-F03 is the Qwen3 + GPU + Matryoshka-dimension feature; F01 stays CPU-only (the `ort` `cuda` / `tensorrt` / `directml` features are explicitly NOT enabled in the workspace dep). F03's WO will add `cuda` feature gating.",
    "**No async API surface.** `ort::Session::run` is sync; F01 exposes the sync API directly (`OrtSession::from_path` and `OrtSession::infer` are `fn`, not `async fn`). The natural async wrap is `tokio::task::spawn_blocking(move || session.infer(&token_ids))` at the consumer site — that pattern lands in F02 / F05 where the tokio runtime is in play. F01's `OrtSession` MUST stay sync so the test runs in `#[test]` (NOT `#[tokio::test]`); confirms the API surface is testable without a runtime.",
    "**No tokenizer integration.** The infer() signature accepts pre-tokenized `&[i64]` token IDs; producing those token IDs from raw source text (via `tokenizers` crate or similar) is the F02 / F05 chunker layer. F01 trusts callers to provide the IDs.",
    "**No LanceDB integration.** P2-W8-F04 lands the LanceDB chunk indexer that consumes `OrtSession::infer` outputs; F01 does NOT wire any database path. `crates/ucil-daemon/src/branch_manager.rs` (WO-0053) is the LanceDB owner — F01 does NOT touch it.",
    "**No new ADR.** DEC-0007 (frozen-selector module-root placement), DEC-0005 (module-coherence commits up to ~200 LOC for the test commit specifically), DEC-0006 (download-binaries feature precedent — avoiding manual operator setup for tests) all cover the WO. If a NEW pattern surfaces (e.g. `ort` 2.x's `inputs!` macro is renamed in the pinned version, or ndarray needs a different shape constructor), STOP and write an ADR before working around. The most likely escalation is upstream ort API drift since ort 2.x is younger than 1.x; document any divergence inline in the rustdoc + ready-for-review marker.",
    "**No `cargo-mutants` per-WO gate.** DEC-0007 removed it; pre-baked function-body mutations in `acceptance` (M1, M2 per scope_in[12]) are the authoritative anti-laziness layer (carry from WO-0042 / 0043 / 0044 / 0045 / 0046 / 0047 / 0048 / 0050 / 0051 / 0052 / 0053, now 11 consecutive cleared).",
    "**No changes to `crates/ucil-core/**`, `crates/ucil-cli/**`, `crates/ucil-treesitter/**`, `crates/ucil-lsp-diagnostics/**`, `crates/ucil-daemon/**`, `crates/ucil-agents/**`, `adapters/**`, `ml/**`, `plugins/**`, `tests/**`, `tests/integration/**`, `tests/fixtures/**`** — F01 is single-crate (`ucil-embeddings`), single-source-file (NEW `ort_session.rs`) plus the `lib.rs` re-exports + the minimal.onnx fixture under the crate's own `tests/data/` (NOT repo-root `tests/fixtures/**`).",
    "**No `#[ignore]`, `.skip()`, `xfail`, commented-out assertions, `todo!()`, `unimplemented!()`, `pass`-only test bodies** — forbidden by root CLAUDE.md.",
    "**No mock of `ort::Session`, `ort::SessionBuilder`, `ort::Value`, or `ndarray::Array`.** ONNX Runtime is a critical dep per `.claude/rules/rust-style.md` (no mocks of Serena, LSP, SQLite, LanceDB, Docker, ONNX Runtime); the test uses real `ort::Session::commit_from_file` against a real on-disk minimal.onnx fixture. WO-0046 lessons line 248 confirms tempfile-/fixture-backed real-binary patterns are the correct approach.",
    "**No `git commit --no-verify`, `--amend` after push, or force-push** — forbidden by root CLAUDE.md.",
    "**No edits to `ucil-build/feature-list.json`, `feature-list.schema.json`, `tests/fixtures/**`, `scripts/gate/**`, `scripts/flip-feature.sh`, `ucil-master-plan-v2.1-final.md`, `.githooks/**`, `.claude/hooks/**`, `.claude/settings.json`**."
  ],
  "acceptance": [
    "AC01: `Cargo.toml` workspace `[workspace.dependencies]` declares `ort` and `ndarray` — verified via `grep -cE '^ort = |^ndarray = ' Cargo.toml` returning `2`.",
    "AC02: `crates/ucil-embeddings/Cargo.toml` declares both as workspace deps — verified via `grep -cE '^ort\\.workspace = true|^ndarray\\.workspace = true' crates/ucil-embeddings/Cargo.toml` returning `2`.",
    "AC03: `crates/ucil-embeddings/src/ort_session.rs` exists and declares `pub struct OrtSession` — verified via `test -f crates/ucil-embeddings/src/ort_session.rs && grep -nE '^pub struct OrtSession' crates/ucil-embeddings/src/ort_session.rs`.",
    "AC04: `crates/ucil-embeddings/src/ort_session.rs` declares `pub fn from_path` and `pub fn infer` and the two accessors — verified via `grep -nE '^[[:space:]]*pub fn from_path|^[[:space:]]*pub fn infer|^[[:space:]]*pub fn input_names|^[[:space:]]*pub fn output_names' crates/ucil-embeddings/src/ort_session.rs | wc -l` returning `4`.",
    "AC05: `test_ort_session_loads_minimal_model` is at MODULE ROOT (NOT inside `mod tests {}`) per DEC-0007 — verified via `grep -nE '^fn test_ort_session_loads_minimal_model\\(' crates/ucil-embeddings/src/ort_session.rs | wc -l` returning `1` (un-indented, column 0).",
    "AC06: `crates/ucil-embeddings/tests/data/minimal.onnx` exists and is non-empty — verified via `test -s crates/ucil-embeddings/tests/data/minimal.onnx`.",
    "AC07: `crates/ucil-embeddings/src/lib.rs` declares `pub mod ort_session;` AND re-exports the public symbols — verified via `grep -cE '^pub mod ort_session;|^pub use ort_session::' crates/ucil-embeddings/src/lib.rs` returning `2`.",
    "AC08: `cargo build -p ucil-embeddings --tests --quiet 2>&1 | tee /tmp/build-WO-0054.log` exits 0.",
    "AC09: Frozen acceptance — `cargo test -p ucil-embeddings ort_session::test_ort_session_loads_minimal_model -- --nocapture 2>&1 | tee /tmp/test-WO-0054.log && grep -Eq 'test result: ok\\. .* 0 failed|[0-9]+ tests? passed' /tmp/test-WO-0054.log`.",
    "AC10: Phase-1 regression sentinel — `cargo test -p ucil-core --no-fail-fast 2>&1 | tee /tmp/test-core-WO-0054.log && ! grep -q 'test result: FAILED' /tmp/test-core-WO-0054.log` (P0/P1 ucil-core stays green).",
    "AC11: Workspace-wide regression — `cargo test --workspace --no-fail-fast 2>&1 | tee /tmp/test-ws-WO-0054.log && ! grep -q 'test result: FAILED' /tmp/test-ws-WO-0054.log`.",
    "AC12: `cargo clippy -p ucil-embeddings --all-targets -- -D warnings 2>&1 | tee /tmp/clippy-WO-0054.log && ! grep -q '^error' /tmp/clippy-WO-0054.log` — clippy clean under `#![deny(warnings)]`, `#![warn(clippy::all, clippy::pedantic, clippy::nursery)]`.",
    "AC13: `cargo fmt --check` — workspace fmt clean.",
    "AC14: `cargo doc -p ucil-embeddings --no-deps 2>&1 | tee /tmp/doc-WO-0054.log && ! grep -Eq '(error|warning):' /tmp/doc-WO-0054.log` — rustdoc clean under `#![deny(rustdoc::broken_intra_doc_links)]`.",
    "AC15: **Mutation M1 (literal sed)** — neuter `from_path` body to early `return Err(OrtSessionError::MissingInput { name: String::new() });` (prepend at function-body start, allow unreachable_code on remainder) → re-run AC09 → expect FAILURE at SA1 panic line (panic message: 'OrtSession::from_path on minimal model'). **Runtime-only variant**: prefix body with `#[allow(unused_variables)] { let _ = model_path; return Err(OrtSessionError::MissingInput { name: String::new() }); }` and `#[allow(unreachable_code)]` on the remainder. Restore: `git checkout -- crates/ucil-embeddings/src/ort_session.rs` → AC09 green again.",
    "AC16: **Mutation M2 (literal sed)** — neuter `infer` body to early `return Ok(Vec::new());` (prepend at function-body start, allow unreachable_code on remainder) → re-run AC09 → expect FAILURE at SA4 (panic message: 'infer must produce a non-empty Vec<f32>; got len=0'). **Runtime-only variant**: prefix body with `#[allow(unused_variables)] { let _ = self; let _ = token_ids; return Ok(Vec::new()); }` and `#[allow(unreachable_code)]` on the remainder. Restore: `git checkout`.",
    "AC17: NO `#[ignore]`, `.skip()`, `todo!()`, `unimplemented!()`, or commented-out assertions in `ort_session.rs` — verified via `! grep -nE '#\\[ignore\\]|todo!\\(|unimplemented!\\(|//[[:space:]]*assert' crates/ucil-embeddings/src/ort_session.rs`.",
    "AC18: Module-level `//!` rustdoc preamble in `ort_session.rs` mentions `OrtSession` + cites master-plan §18 Phase 2 Week 8 line 1786 — verified via `grep -E 'OrtSession.*§18|§18.*OrtSession|line 1786' crates/ucil-embeddings/src/ort_session.rs | wc -l` returning at least `1`.",
    "AC19: `bash scripts/verify/P2-W8-F01.sh` exits 0.",
    "AC20: All commit subject lines ≤ 70 chars per `.claude/rules/commit-style.md`. Verified via `git log main...HEAD --pretty='%s' | awk '{ if (length($0) > 70) { print \"too-long: \" $0; exit 1 } }'` returning success.",
    "AC21: `git diff --name-only main...HEAD` returns ONLY: `Cargo.toml`, `Cargo.lock`, `crates/ucil-embeddings/Cargo.toml`, `crates/ucil-embeddings/src/lib.rs`, `crates/ucil-embeddings/src/ort_session.rs`, `crates/ucil-embeddings/tests/data/minimal.onnx`, `crates/ucil-embeddings/tests/data/build_minimal_onnx.py` (the optional generator script — present if executor used Python to build the fixture; OPTIONAL: omit if executor sourced minimal.onnx from upstream with a sibling LICENSE.txt instead), `scripts/verify/P2-W8-F01.sh`, `ucil-build/work-orders/0054-ready-for-review.md`. (`Cargo.lock` is expected because the new `ort` + `ndarray` deps update it.)",
    "AC22: NO `unsafe` blocks added unless documented per `.claude/rules/rust-style.md` ('any unsafe block requires an ADR justification and `// SAFETY:` comment'). Verified via `! grep -nE '^[[:space:]]*unsafe[[:space:]]*\\{' crates/ucil-embeddings/src/ort_session.rs` (zero matches expected — `ort` 2.x's safe Rust API surface should be sufficient)."
  ],
  "acceptance_criteria": [
    "[ \"$(grep -cE '^ort = |^ndarray = ' Cargo.toml)\" = '2' ]",
    "[ \"$(grep -cE '^ort\\.workspace = true|^ndarray\\.workspace = true' crates/ucil-embeddings/Cargo.toml)\" = '2' ]",
    "test -f crates/ucil-embeddings/src/ort_session.rs",
    "grep -nE '^pub struct OrtSession' crates/ucil-embeddings/src/ort_session.rs",
    "[ \"$(grep -cE '^[[:space:]]*pub fn from_path|^[[:space:]]*pub fn infer|^[[:space:]]*pub fn input_names|^[[:space:]]*pub fn output_names' crates/ucil-embeddings/src/ort_session.rs)\" = '4' ]",
    "grep -nE '^fn test_ort_session_loads_minimal_model\\(' crates/ucil-embeddings/src/ort_session.rs",
    "test -s crates/ucil-embeddings/tests/data/minimal.onnx",
    "[ \"$(grep -cE '^pub mod ort_session;|^pub use ort_session::' crates/ucil-embeddings/src/lib.rs)\" = '2' ]",
    "test -x scripts/verify/P2-W8-F01.sh",
    "cd $REPO_ROOT && cargo build -p ucil-embeddings --tests --quiet 2>&1 | tee /tmp/build-WO-0054.log",
    "cd $REPO_ROOT && cargo test -p ucil-embeddings ort_session::test_ort_session_loads_minimal_model -- --nocapture 2>&1 | tee /tmp/test-WO-0054.log && grep -Eq 'test result: ok\\. .* 0 failed|[0-9]+ tests? passed' /tmp/test-WO-0054.log",
    "cd $REPO_ROOT && cargo test --workspace --no-fail-fast 2>&1 | tee /tmp/test-ws-WO-0054.log && ! grep -q 'test result: FAILED' /tmp/test-ws-WO-0054.log",
    "cd $REPO_ROOT && cargo clippy -p ucil-embeddings --all-targets -- -D warnings 2>&1 | tee /tmp/clippy-WO-0054.log && ! grep -q '^error' /tmp/clippy-WO-0054.log",
    "cd $REPO_ROOT && cargo fmt --check",
    "cd $REPO_ROOT && cargo doc -p ucil-embeddings --no-deps 2>&1 | tee /tmp/doc-WO-0054.log && ! grep -Eq '(error|warning):' /tmp/doc-WO-0054.log",
    "cd $REPO_ROOT && bash scripts/verify/P2-W8-F01.sh",
    "! grep -nE '#\\[ignore\\]|todo!\\(|unimplemented!\\(' crates/ucil-embeddings/src/ort_session.rs",
    "! grep -nE '^[[:space:]]*unsafe[[:space:]]*\\{' crates/ucil-embeddings/src/ort_session.rs"
  ],
  "forbidden_paths": [
    "ucil-build/feature-list.json",
    "ucil-build/feature-list.schema.json",
    "ucil-master-plan-v2.1-final.md",
    "tests/fixtures/**",
    "scripts/gate/**",
    "scripts/flip-feature.sh",
    ".githooks/**",
    ".claude/hooks/**",
    ".claude/settings.json",
    ".claude/settings.local.json",
    ".claude/agents/**",
    "crates/ucil-core/**",
    "crates/ucil-cli/**",
    "crates/ucil-treesitter/**",
    "crates/ucil-lsp-diagnostics/**",
    "crates/ucil-daemon/**",
    "crates/ucil-agents/**",
    "adapters/**",
    "ml/**",
    "plugins/**",
    "tests/**",
    "tests/integration/**",
    "rust-toolchain.toml",
    "ucil-build/decisions/**",
    "ucil-build/post-mortems/**",
    "ucil-build/phase-log/**"
  ],
  "context_refs": [
    "master-plan:§18 Phase 2 Week 8 line 1786 (ucil-embeddings crate: ONNX Runtime (`ort` crate) inference)",
    "master-plan:§18 Phase 2 Week 8 line 1787 (CodeRankEmbed default — F02 follow-up)",
    "master-plan:§18 Phase 2 Week 8 line 1788 (LanceDB integration: background indexing — F04 follow-up)",
    "master-plan:§15.2 (ucil.<layer>.<op> tracing span naming)",
    "master-plan:§3.2 (ucil-embeddings crate position in workspace)",
    "decision:DEC-0007 (frozen-selector module-root placement; pre-baked function-body mutations as anti-laziness layer)",
    "decision:DEC-0005 (module-coherence commits up to ~200 LOC for the test commit specifically)",
    "decision:DEC-0006 (download-binaries / pre-built test-time binaries pattern — avoid manual operator setup)",
    "rules:.claude/rules/rust-style.md (errors: thiserror in libs with #[non_exhaustive]; tests: cargo nextest; rustdoc # Examples; no mocks of ONNX Runtime; #![deny(warnings)] + clippy::pedantic + clippy::nursery)",
    "rules:.claude/rules/commit-style.md (Conventional Commits with Phase/Feature/Work-order trailers; commit subject ≤ 70 chars)",
    "phase-log:ucil-build/phase-log/02-phase-2/CLAUDE.md (Phase 2 standing rules — no ONNX mocks, frozen selectors, no Phase-1 regressions)",
    "lessons:WO-0042 (DEC-0007 module-root frozen-selector placement; pre-baked function-body mutations as anti-laziness layer; operator-readable assertion messages)",
    "lessons:WO-0046 (runtime-only mutation variant for #![deny(warnings)] cascade safety)",
    "lessons:WO-0048 (cumulative-debt-avoidance discipline — re-exports land in same WO)",
    "lessons:WO-0051 (operator-readable assert! panic messages; parse-only / fixture-backed test patterns)",
    "lessons:WO-0053 (single-file-NEW blast radius pattern: NEW source file + lib.rs additive edits + verify script + fixture under crate's tests/ — exact shape this WO follows)",
    "code:crates/ucil-embeddings/Cargo.toml (existing crate package definition — additive edit target)",
    "code:crates/ucil-embeddings/src/lib.rs (existing minimal lib.rs with crate-level lints — additive edit target)",
    "code:crates/ucil-daemon/src/branch_manager.rs (WO-0053 sibling pattern — module-root frozen-selector test, fixture under crate's tests/, single-file blast radius)"
  ],
  "dependencies_met": true,
  "estimated_commits": 6,
  "estimated_complexity": "medium",
  "created_at": "2026-05-05T20:37:23Z",
  "created_by": "planner"
}
