{
  "id": "WO-0077",
  "slug": "mcp-pytest-runner-plugin-manifest",
  "phase": 3,
  "week": 11,
  "features": ["P3-W11-F08"],
  "feature_ids": ["P3-W11-F08"],
  "branch": "feat/WO-0077-mcp-pytest-runner-plugin-manifest",
  "worktree_branch": "feat/WO-0077-mcp-pytest-runner-plugin-manifest",
  "executor_agent": "executor",
  "goal": "Land the G8 (Testing+CI) external-binary plugin manifest for Phase 3 Week 11 — `plugins/testing/mcp-pytest-runner/plugin.toml` (P3-W11-F08) — plus its devtools install hint, an end-to-end `crates/ucil-daemon/tests/g8_plugin_manifests.rs` integration test exercising a real `PluginManager::health_check_with_timeout` round-trip via `uvx mcp-pytest-runner@0.2.1`, and a replacement for the TODO-stub acceptance script at `scripts/verify/P3-W11-F08.sh` that pairs an MCP `tools/list` smoke with a tool-level pytest-discover + selective-run smoke against `tests/fixtures/python-project`. Direct port of the WO-0072 solo-manifest template (codegraphcontext G4, single-feature shape) layered with WO-0076 G7 paired-manifest discipline (W1 tokio::process::Command pre-emptive carve-out, live-tools/list capture protocol, UCIL_SKIP_<GROUP>_PLUGIN_E2E env var). Establishes the new `testing.*` capability namespace under `plugins/testing/` (parallel to `quality.*`, `context.*`, `knowledge.*`, `architecture.*`, `search.*`, `navigate.*`, `platform.*`). Per WO-0073/0074/0075/0076 lesson §planner: `G6 → G7 → G8 paired-manifest template port — apply same shape to G8`. P3-W11-F07 (test-runner-mcp) is EXPLICITLY EXCLUDED from this WO per DEC-0021 — no canonical upstream test-runner-mcp server is published to PyPI/npm at this date; F07 is deferred to Phase 4/7 hardening pending upstream emergence.",
  "plan_summary": "Mirror the WO-0072 G4 codegraphcontext solo-manifest precedent verbatim, retargeted from Architecture to Testing group, while applying the WO-0076 W1 + live-tools/list + UCIL_SKIP_<GROUP>_PLUGIN_E2E discipline carry-overs. One stdio MCP server — mcp-pytest-runner — gets a hand-authored TOML manifest under a new `plugins/testing/mcp-pytest-runner/plugin.toml` directory tree, pinned to the verified-published-immutable upstream tag `mcp-pytest-runner@0.2.1` (PyPI; latest publish 2025-12+, summary 'MCP server providing opinionated pytest execution interface for AI agents') with `transport.type = \"stdio\"` and a documented `capabilities.provides` list under the `testing.*` namespace. One devtools install script (`scripts/devtools/install-mcp-pytest-runner.sh`) mirrors the `install-mem0-mcp.sh` / `install-semgrep-mcp.sh` shape verbatim — informational-only, idempotent, optional warm-up via `--help`. A new `crates/ucil-daemon/tests/g8_plugin_manifests.rs` integration test file declares one `mod g8_plugin_manifests { ... }` at file root with one `#[tokio::test] async fn` health-check test — substring selector `g8_plugin_manifests::mcp_pytest_runner_manifest_health_check` MUST resolve cleanly per DEC-0007 + WO-0069/0072/0074/0075/0076 lessons §\"For planner: module-root placement\". One `scripts/verify/P3-W11-F08.sh` script replaces the TODO stub and runs the targeted `cargo test` selector plus a tool-level smoke (gated by `UCIL_SKIP_TESTING_PLUGIN_E2E` per the WO-0074/0075/0076 `UCIL_SKIP_<GROUP>_PLUGIN_E2E` precedent, defaulting to RUN). The M1/M2 mutation contract is pre-baked verbatim from WO-0076 §10/§11: M1 poisons `transport.command` (uvx → ENOENT), M2 replaces the expected canonical tool name in the test file (executor MUST confirm exact tool names from live `tools/list` BEFORE choosing the literal, per WO-0074/0075/0076 lesson §executor #1: `DON'T translate live tools/list names to snake_case when they ship kebab-case` — mcp-pytest-runner tool names are TBD-by-live-capture since the upstream README does not enumerate them in this snapshot). No daemon source changes; no `crates/ucil-daemon/src/**` edits permitted. F08 verify-script smoke runs against a tmpdir copy of `tests/fixtures/python-project` (which has `pyproject.toml` + `src/python_project/` + `tests/` carrying `test_evaluator.py`, `test_lexer.py`, `test_parser.py`, `test_scoring.py`) per F08 spec line `pytest hierarchical test discovery and selective re-run by node ID verified on the fixture python-project`. Per WO-0075 §executor lesson W1: ALL `tokio::test` async helpers MUST use `tokio::process::Command` (NOT `std::process::Command`) for fixture-init / git-init / tmpdir-mutation operations — pre-emptive carve-out to avoid the WO-0075 W1 critic warning carried verbatim into this WO via AC `! grep -qE 'std::process::Command' crates/ucil-daemon/tests/g8_plugin_manifests.rs`.",
  "scope_in": [
    "1. NEW plugin manifest at `plugins/testing/mcp-pytest-runner/plugin.toml` for the official mcp-pytest-runner published to PyPI at `mcp-pytest-runner@0.2.1` (latest publish 2025-12+ confirmed via PyPI pre-flight: `https://pypi.org/pypi/mcp-pytest-runner/json` returns HTTP 200; versions seen `['0.1.0', '0.2.0', '0.2.1']`; summary `MCP server providing opinionated pytest execution interface for AI agents`; install via `uvx mcp-pytest-runner` per upstream README). Required keys: `[plugin] name=\"mcp-pytest-runner\" category=\"testing\" priority=\"P0\" version=\"0.2.1\"`; `[transport] type=\"stdio\" command=\"uvx\" args=[\"mcp-pytest-runner@0.2.1\"]`; `[capabilities] provides=[\"testing.pytest.discover\", \"testing.pytest.run\", \"testing.pytest.run_node_id\"]` (executor: confirm exact tool names by spawning the live binary and capturing the canonical `tools/list` response BEFORE writing the literal — prefer kebab-case-as-emitted-by-upstream over snake-case translation per WO-0074/0075/0076 lesson §executor #1; the master-plan §4.8 / §5.8 vocabulary `discover` / `run` etc. is the capability NAME independent of the upstream's literal tool name; the PyPI bin entry is implied as a console-script per the package's `pyproject.toml` — `uvx mcp-pytest-runner@0.2.1` resolves the canonical entry-point); `[lifecycle] hot_cold=true idle_timeout_minutes=10` mirrors the ast-grep / Context7 / GitHub-MCP / ESLint-MCP pattern; `[capabilities] languages=[\"python\"]` per the upstream's pytest-only scope; `[capabilities] activation.on_language=[\"python\"]` and `activation.on_tool=[\"check_quality\", \"review_changes\", \"run_tests\"]` per master-plan §14.1 line 1455+ shape. Top-of-file rustdoc comment must document: source URL (https://pypi.org/project/mcp-pytest-runner/), PyPI package name (`mcp-pytest-runner`), pinned tag (`0.2.1`), binary launch command (`uvx mcp-pytest-runner@0.2.1`), advertised tools (full live list from the live `tools/list` capture per scope_in #14), transport type (stdio), API key requirements (NONE — runs entirely locally; no auth), and the working-directory invariant (the upstream binary runs pytest in the cwd or against an explicitly-passed path — the integration test + verify script MUST copy `tests/fixtures/python-project` into a `mktemp -d` tmpdir and invoke from the tmpdir as cwd; NO modifications to the read-only fixture).",
    "2. NEW devtools script at `scripts/devtools/install-mcp-pytest-runner.sh`. Idempotent bash; verifies python3 + uv (or uvx) available; runs `uvx mcp-pytest-runner@0.2.1 --help 2>&1 | head -1` (or analogous warm-up that triggers PyPI fetch + uv cache without blocking on stdin) to ensure the package is in the uv cache for the integration test's first spawn. Must `set -euo pipefail`; must exit 0 on success; must NOT require sudo; must NOT actually install anything (informational scaffolding only — mirrors `install-mem0-mcp.sh` / `install-semgrep-mcp.sh` shape verbatim per WO-0069/0076). Document the working-directory invariant (the test/verify script handles cwd, NOT the install script) as a `[NOTE]` block at the bottom (informational only — operator-state, NOT actionable by the script). Must NOT install uv if missing (operator-state); EMIT a `[MISSING]` notice if uv/uvx is not on PATH and exit 0 gracefully (per WO-0076 install-semgrep-mcp.sh shape). Top-of-file documents the optional `--help` warm-up flag works for mcp-pytest-runner per upstream PyPI README CLI surface.",
    "3. NEW integration-test file at `crates/ucil-daemon/tests/g8_plugin_manifests.rs`. Top of file: mirror existing `g7_plugin_manifests.rs` shape verbatim — same imports, same helper-function placement, same tokio runtime flavor declaration, same `//!` rustdoc declaring mocking is FORBIDDEN. CRITICAL CARRY-FORWARD FROM WO-0075/0076 W1 LESSON: ALL fixture-init / cwd-mutation / tmpdir-population helpers in async test bodies MUST use `tokio::process::Command` (NOT `std::process::Command`), per WO-0075/0076 lesson §executor: `In async integration tests, use tokio::process::Command even for fixture-setup helpers... .claude/rules/rust-style.md §Async line 23 mandates tokio::process::Command in async paths`. Single `mod g8_plugin_manifests { ... }` block at file root containing ONE `#[tokio::test] async fn` test — `mcp_pytest_runner_manifest_health_check`. The test: (a) loads the corresponding `plugins/testing/mcp-pytest-runner/plugin.toml` via the daemon's existing manifest loader (use the same APIs as g3/g4/g5/g6/g7_plugin_manifests.rs); (b) optionally copies `tests/fixtures/python-project` into a `mktemp -d` tmpdir using `tokio::process::Command` (or `tokio::fs::copy` + `walkdir` / `async_walkdir`) IF the upstream binary's `tools/list` requires a cwd that contains a pytest-collectable target (executor confirms during live capture; if upstream `tools/list` is independent of cwd, the tmpdir copy may be deferred to the verify script's tool-level smoke section only); (c) constructs a `PluginManager` (or analogous abstraction) and calls `health_check_with_timeout` (or the equivalent method that round-trips a real MCP `tools/list` over stdio); (d) asserts the live response carries a specific canonical tool name (executor confirms via live capture; e.g. `discover_tests` / `run_tests` / analogous — whatever `mcp-pytest-runner@0.2.1` advertises); (e) panics with `(SAn) <semantic_name>; got: <full-live-tool-list>` on assertion failure per DEC-0007 SA-numbered panic-body format. Substring selector `g8_plugin_manifests::mcp_pytest_runner_manifest_health_check` MUST resolve cleanly via `cargo test -p ucil-daemon --test g8_plugin_manifests <selector>`. NO `mod tests { ... }` nesting; the test function lives at `mod g8_plugin_manifests` ROOT (NOT inside a nested `mod tests` per DEC-0007 + WO-0069/0072/0074/0075/0076 lessons §\"For planner\"). Mirror the existing `g7_plugin_manifests.rs` file shape verbatim. Provide a `skip_via_env()` helper checking `UCIL_SKIP_TESTING_PLUGIN_E2E` per WO-0074/0075/0076 lesson §executor #3 (document inline; default to RUN; verifier MUST NOT set the opt-out per scope_in #15).",
    "4. NEW `scripts/verify/P3-W11-F08.sh` (replace TODO stub) for mcp-pytest-runner. `set -euo pipefail`. (a) Sanity-check `plugins/testing/mcp-pytest-runner/plugin.toml` parses as TOML and asserts the keys named in scope_in #1 via `python3 -c 'import tomllib; ...'`. (b) Run `cargo test -p ucil-daemon --test g8_plugin_manifests g8_plugin_manifests::mcp_pytest_runner_manifest_health_check 2>&1 | tee /tmp/wo-0077-f08-cargo.log` and `grep -Eq 'test result: ok\\. 1 passed; 0 failed|1 tests run: 1 passed' /tmp/wo-0077-f08-cargo.log`. (c) Tool-level smoke (per WO-0074/0075/0076 lesson §executor #4: prefer python polling with deadline over bash sleep): copy `tests/fixtures/python-project` into `mktemp -d` tmpdir BEFORE invoking the upstream binary (per WO-0074/0075/0076 lesson §executor #5); spawn `uvx mcp-pytest-runner@0.2.1` from the tmpdir as cwd (the fixture has `pyproject.toml` declaring `tool.pytest.ini_options.testpaths=[\"tests\"]` and `tests/test_evaluator.py`, `test_lexer.py`, `test_parser.py`, `test_scoring.py` with at least dozens of test functions per pre-flight ls); send an MCP `initialize` + `notifications/initialized` + `tools/list` JSON-RPC sequence over stdio (use python with 90-second deadline polling per WO-0074/0076 §executor #4; pytest discovery may take several seconds on cold start); assert `tools/list` response carries the upstream's canonical pytest-discover and pytest-run tool names (executor confirms exact names from live `tools/list`); send a `tools/call` for the discover tool with a path argument pointing at the tmpdir; assert response is a successful JSON-RPC reply and its result contains a non-empty list of pytest node IDs in pytest's canonical `path::ClassName::method_name` or `path::function_name` format (e.g., `tests/test_lexer.py::test_tokenize_simple` or analogous from the fixture's actual tests); send a follow-up `tools/call` for the run tool with a SUBSET of the discovered node IDs (e.g., 1-3 specific node IDs from one test file) — assert the response carries structured pytest run results with at least pass/fail counts per F08 spec line `pytest hierarchical test discovery and selective re-run by node ID verified on the fixture python-project`. Gate the entire tool-level smoke section behind `UCIL_SKIP_TESTING_PLUGIN_E2E` env-var per the WO-0074/0075/0076 precedent — DEFAULT to RUN. Final exit 0 on success; non-zero on any check failure with a clear `>&2` diagnostic. The cargo-test `tools/list` path is the load-bearing assertion; the tool-level pytest-discover + selective-run smoke is the additional smoke per F08 spec.",
    "5. M1 mutation contract (transport.command poison; runs once). Pre-mutation: `md5sum plugins/testing/mcp-pytest-runner/plugin.toml > /tmp/wo-0077-mcp-pytest-runner-orig.md5` (capturing snapshot BEFORE any edit). Mutation: edit the targeted `plugin.toml` and replace `command = \"uvx\"` with `command = \"/__ucil_test_nonexistent_M1__\"`. Selector: `cargo test -p ucil-daemon --test g8_plugin_manifests g8_plugin_manifests::mcp_pytest_runner_manifest_health_check`. Expected panic: a `PluginError::Spawn { source: Os { code: 2, kind: NotFound } }` chain (or analogous `tokio::process::Command::spawn` ENOENT-class error). Restore: `git checkout -- plugins/testing/mcp-pytest-runner/plugin.toml`. Confirm: `md5sum -c /tmp/wo-0077-mcp-pytest-runner-orig.md5` returns OK. Mirror the WO-0072/0076 §10 verbatim shape.",
    "6. M2 mutation contract (expected-tool-name regression; runs once). Pre-mutation: `md5sum crates/ucil-daemon/tests/g8_plugin_manifests.rs > /tmp/wo-0077-g8tests-orig.md5`. Mutation: edit the test file and replace whichever string the test asserts as the canonical tool name (executor will have selected it from the live `tools/list` response per scope_in #14) with `\"NON_EXISTENT_TOOL_M2\"`. Selector: the targeted test. Expected: structured panic carrying the full live tool-list (`(SAn) <semantic>; got: [\"<live tool 1>\", \"<live tool 2>\", ...]; want: \"NON_EXISTENT_TOOL_M2\"`). Restore: `git checkout -- crates/ucil-daemon/tests/g8_plugin_manifests.rs`. Confirm via md5. Mirror the WO-0072/0076 §11 verbatim shape.",
    "7. Commit cadence: aim for 5-7 atomic commits per `.claude/rules/commit-style.md` ~50 LOC soft target (1× plugin.toml manifest at 60-90 LOC = candidate for DEC-0005 module-coherence carve-out citation; 1× install script at ~70 LOC; 1× integration test file at 150-250 LOC under DEC-0005 carve-out cited in commit body; 1× verify script at 200-400 LOC; 1× RFR). Suggested order: (i) plugins/testing/mcp-pytest-runner/plugin.toml + install-mcp-pytest-runner.sh (paired commit OR two adjacent commits); (ii) crates/ucil-daemon/tests/g8_plugin_manifests.rs (one cohesive feat commit covering the #[tokio::test] async fn under DEC-0005 carve-out — cite in commit body; pre-emptively use `tokio::process::Command` for any fixture-init helpers per WO-0075/0076 W1 lesson); (iii) scripts/verify/P3-W11-F08.sh; (iv) RFR. Each commit Conventional-Commit-formatted with `Phase: 3` + `Feature: P3-W11-F08` + `Work-order: WO-0077` trailers per `.claude/rules/commit-style.md`.",
    "8. Standing AC23 substantive coverage protocol per WO-0067..WO-0076 carryover (now 33 WOs deep). Use `env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --tests --summary-only --json | jq '.data[0].totals.lines.percent'` as the authoritative measurement (per WO-0074/0075/0076 lesson §executor: the literal `--no-default-features --features default` arg-pair errors because ucil-daemon has no `default` feature defined). WO is purely additive on tests + manifests + scripts — pre-WO baseline must be preserved (no `crates/ucil-daemon/src/**` source changes per scope_out #2). Document the measurement in the RFR.",
    "9. Apply WO-0076 lessons §\"For planner\" + §\"For executor\" + §\"For verifier\" verbatim: tests at module root (NOT inside `mod tests { ... }`); DEC-0007 SA-numbered panic-body format on every assert; DEC-0008 §4 dependency-inversion seam pattern for any future production-wiring follow-up (NOT in this WO scope — this WO is manifests-only); cite WO-0044 + WO-0069 + WO-0072 + WO-0074 + WO-0075 + WO-0076 lessons by ID in the RFR's §\"Lessons applied\" section. Specific WO-0076 lessons to apply verbatim: (a) §executor lessons #1-#5 from WO-0075 carry-over (live capture, no --mcp warm-up, UCIL_SKIP env var, python deadline polling, tmpdir copy); (b) WO-0075 §executor W1 pre-emptively applied — `! grep -qE 'std::process::Command' crates/ucil-daemon/tests/g8_plugin_manifests.rs` AC enforced; (c) WO-0076 §planner: `For external-CLI-dependent MCP servers, document the CLI dep as load-bearing operator-state in scope_in` — N/A here since `mcp-pytest-runner` does NOT have an external Python-side CLI dep beyond `pytest` itself which is automatically resolvable via the uvx-managed venv (the upstream package declares pytest as a dependency in its `pyproject.toml`); (d) WO-0076 §planner: `For verify scripts that use parent-process env-var mutation, prescribe --test-threads=1 enforcement OR direct Command::env propagation` — N/A here since this WO does NOT require parent-process env-var mutation (no SEMGREP_PATH-style operator-state surfacing); (e) WO-0076 §verifier: ensure UCIL_SKIP_EXTERNAL_PLUGIN_TESTS and UCIL_SKIP_TESTING_PLUGIN_E2E are NOT exported when running cargo test g8_plugin_manifests::* and bash scripts/verify/P3-W11-F08.sh.",
    "10. Pre-flight commands the executor MUST run before first push: (a) `cargo build -p ucil-daemon` — clean compile; (b) `cargo test -p ucil-daemon --test g8_plugin_manifests` — single frozen test green; (c) `cargo clippy -p ucil-daemon --all-targets --tests -- -D warnings` — zero warnings; (d) `cargo fmt -p ucil-daemon --check` — formatting clean; (e) word-ban grep on production-side files: `! grep -qiE 'mock|fake|stub' plugins/testing/mcp-pytest-runner/plugin.toml scripts/devtools/install-mcp-pytest-runner.sh scripts/verify/P3-W11-F08.sh` returns empty (the `//! Mocking ... is FORBIDDEN` rustdoc declaration in the test file is exempt under WO-0048 line 363 + WO-0072/0075/0076 §executor `Negative assertion forbidding mocking, NOT a mock implementation`); (f) `bash scripts/verify/P3-W11-F08.sh` green; (g) `command -v uvx python3` confirms PATH has both required runtimes — the verifier requires the same.",
    "11. Live-tool-name capture protocol (WO-0074/0075/0076 §executor #1 lesson — CRITICAL for M2 mutation correctness): BEFORE writing any tool-name assertion literal, the executor MUST: (a) run `uvx mcp-pytest-runner@0.2.1` directly to capture canonical tool names (the canonical npx/uvx command with stdin frames driving past `initialize` → `notifications/initialized` → `tools/list`); capture the `tools/list` response's `result.tools[].name` array verbatim; (b) embed this list in the manifest's top-of-file rustdoc comment + the integration test's panic-body string. Choose ONE canonical tool name as the M2 assertion target — prefer the most-stable / most-documented one (e.g., the upstream's primary discovery tool — pytest discovery is the canonical pytest entry point per F08 spec). Document the chosen literal + the full live tool-list in the RFR's `## Live tool name capture` section per the WO-0074/0075/0076 RFR template. CRITICAL: the upstream PyPI README does NOT enumerate the advertised tools in the snapshot the planner has access to — the executor's live capture is the ONLY source of truth.",
    "12. Capability namespace establishment (`testing.*`): parallel to `search.*` (probe), `navigate.*` (scip), `knowledge.*` (codebase-memory + mem0), `architecture.*` (codegraphcontext), `context.*` (Context7 + Repomix), `platform.*` (GitHub + Git + Filesystem), `quality.*` (ESLint + Semgrep). G8 Testing sources advertise capabilities under the `testing.*` prefix; subdivision per source: `testing.pytest.*` for the mcp-pytest-runner. Subsequent G8 plugins (test-runner-mcp deferred per DEC-0021; pytest-mcp Pytest-plugin variant per master-plan §4.8 line 404; any future Vitest/cargo-nextest/Go MCP servers when they emerge) MUST extend this namespace consistently when they land in future WOs.",
    "13. F08 fixture-aware smoke. The fixture `tests/fixtures/python-project` carries `pyproject.toml` (with `[project] name=\"python-project\" requires-python=\">=3.11\"; [project.optional-dependencies] dev=[\"pytest>=8.0\"]; [tool.pytest.ini_options] testpaths=[\"tests\"]`) + `src/python_project/` + `tests/test_evaluator.py`, `test_lexer.py`, `test_parser.py`, `test_scoring.py` per pre-flight ls. The mcp-pytest-runner needs a working pytest-discoverable tree to produce a non-empty `tools/call discover` response. Executor MUST: (a) copy the fixture into `mktemp -d` tmpdir BEFORE invoking the upstream binary (NOT in the fixture — fixture is read-only per `forbidden_paths`); (b) document the fixture-pristineness invariant in the verify script + integration test comments; (c) the F08 spec line `pytest hierarchical test discovery and selective re-run by node ID verified on the fixture python-project` REQUIRES the smoke to actually surface pytest node IDs in canonical form (`path::ClassName::method` or `path::function`) AND a successful selective-run smoke against a SUBSET of those IDs (not the whole suite). Document the chosen approach in the RFR.",
    "14. Forbidden-path audits the executor MUST run BEFORE pushing. Every entry in `forbidden_paths` (scope_out #16) MUST produce `git diff main feat -- <path>` returning empty. Specifically: (a) `git diff main feat -- crates/ucil-daemon/src/` — empty (no daemon source changes; this WO is manifests-only); (b) `git diff main feat -- crates/ucil-core/src/` — empty; (c) `git diff main feat -- tests/fixtures/` — empty (fixtures are read-only; tmpdir copies in tests/scripts are NOT fixtures); (d) `git diff main feat -- ucil-build/feature-list.json ucil-build/feature-list.schema.json ucil-master-plan-v2.1-final.md` — empty (oracle docs are immutable); (e) `git diff main feat -- ucil-build/decisions/` — empty (DEC-0021 was pre-authored by the planner alongside this WO; ADRs are planner-territory); (f) `git diff main feat -- crates/ucil-daemon/tests/g3_plugin_manifests.rs crates/ucil-daemon/tests/g4_plugin_manifests.rs crates/ucil-daemon/tests/g5_plugin_manifests.rs crates/ucil-daemon/tests/g6_plugin_manifests.rs crates/ucil-daemon/tests/g7_plugin_manifests.rs crates/ucil-daemon/tests/plugin_manifests.rs` — empty (prior group tests are isolated per WO-0069 §executor #2 separation invariant); (g) `git diff main feat -- plugins/{architecture,context,knowledge,platform,quality,search,structural}/` — empty (other groups' plugins untouched); (h) `git diff main feat -- scripts/verify/P3-W11-F07.sh` — empty (F07 is deferred per DEC-0021 — its TODO-stub verify script remains unchanged in this WO). Cite WO-0072 scope_in #2 (six-audit checklist) and WO-0075 §executor (post-cherry-pick / post-edit audit shape) as the audit template.",
    "15. Lessons-learned summary the executor MUST embed in the RFR's `## Lessons applied` section (cite each WO + lesson section + rationale): WO-0074/0075/0076 §planner: G5 → G6 → G7 → G8 paired-manifest template port (this WO is the G8 application — solo-feature shape per WO-0072 codegraphcontext G4 precedent); WO-0074/0075/0076 §executor #1: live tool-name capture before writing assertion literals; WO-0075/0076 §executor W1: tokio::process::Command in async test helpers (pre-emptively applied here to avoid the W1 critic warning); WO-0075/0076 §executor #5: tmpdir copy of fixture before invoking; WO-0074/0076 §executor #4: python deadline polling for tool-call smokes; WO-0067/0068/0069/0070/0072/0073/0074/0075/0076: pre-baked M1/M2 mutation contract; WO-0067/0068/0069/0070: DEC-0007 SA-numbered panic-body format; WO-0067..WO-0076: substantive AC23 coverage standing protocol (env -u RUSTC_WRAPPER cargo llvm-cov); WO-0067..WO-0076: AC30/AC31 phase-1/phase-2 effectiveness-gate flake carry-over with 3 standing escalations; WO-0070..WO-0076: AC25 git log feat ^main --merges = 0 wording. PLUS: DEC-0021 test-runner-mcp deferral (this WO ships F08 only; F07 deferred per the upstream-availability sweep documented in DEC-0021 — RFR pre-flight section MUST cite the DEC-0021 lineage AND the chain DEC-0019 → DEC-0020 → DEC-0021 as the proven preemptive-deferral pattern)."
  ],
  "scope_out": [
    "1. NO P3-W11-F07 (test-runner-mcp plugin) — DEFERRED per DEC-0021 to Phase 4/7 hardening pending upstream availability (no canonical PyPI/npm/GitHub test-runner-mcp server published at this date; pre-flight sweep documented in DEC-0021 §Context). The next planner pass with reason to believe upstream has shipped MUST re-check via the DEC-0021 §Revisit-trigger curl/npm/gh script and emit a new WO if applicable. F07's TODO-stub verify script at `scripts/verify/P3-W11-F07.sh` remains untouched in this WO (it's the marker of the deferral state).",
    "2. NO `crates/ucil-daemon/src/**` source changes. WO is purely additive on `plugins/testing/`, `crates/ucil-daemon/tests/`, `scripts/devtools/`, `scripts/verify/`. Critic precedent at WO-0071 (DEC-0019) flags forbidden_paths violations on daemon-src changes for plugin-manifest-scoped WOs.",
    "3. NO G8 group runtime in `crates/ucil-daemon/src/g8.rs`. That's the WO-0073/0070-equivalent G8 follow-up (G3→G4→G5→G6→G7→G8 template port per WO-0073/0074/0075/0076 lessons §\"For planner: G-group recipe\") and lands as a separate WO consuming the mcp-pytest-runner plugin manifest from this WO + (eventually) the test-runner-mcp manifest from a future post-DEC-0021-revisit WO. F09 (G8 test discovery) is the dep-blocked feature ID for that follow-up — currently dep-blocked on F07 + F08; per DEC-0021 §Decision point 5, F09's F07 dep should be treated as 'admin-satisfied via DEC-0021' for re-scoping purposes, so F09 effectively dep-blocks on F08 + P3-W9-F03 alone after this WO ships.",
    "4. NO classify_query / parse_reason daemon-side wiring (deferred since WO-0067/0068). Continues deferred orthogonal to this WO's plugin-manifest scope.",
    "5. NO new ADR creation under `ucil-build/decisions/**` BY THE EXECUTOR. The DEC-0021 ADR is pre-authored by the planner alongside this WO; no further ADRs are required for this WO's scope. ADRs are planner-territory (DEC-0019 critic precedent at WO-0071 attempt-2). If the executor encounters an ambiguity that requires an ADR, escalate via `/escalate` rather than authoring inline.",
    "6. NO modifications to `tests/fixtures/**`. The fixture `python-project` is a read-only asset — F08 verify script + integration test copy `python-project` into a tmpdir before invoking the upstream binary. NO edits to the fixture sources themselves.",
    "7. NO new entries in `feature-list.json`. F08 entry already exists with frozen `id` + `description` + `acceptance_tests` + `dependencies` per the no-mid-phase-additions rule. F07's deferral is documented via DEC-0021 — the entry stays intact with `passes: false`.",
    "8. NO P3-W9-F11 (incremental computation integration test) work. Per WO-0073 lessons §\"For planner: F11 is orthogonal to G4\" — F11 is Salsa-engine + tree-sitter pipeline surface, NOT a G8 testing manifest. Separate WO.",
    "9. NO follow-up daemon spurious-frame-drain code change (DEC-0019 §3 Phase 7 hardening). Unrelated to G8 plugins.",
    "10. NO P3-W9-F10 graphiti plugin manifest. Deferred to Phase 7 per DEC-0019 — not part of this WO.",
    "11. NO G8 fusion runtime (F09 — G8 test discovery using all three methods concurrently). Lands in a separate G8 production-wiring WO that consumes the manifest from this WO. Per DEC-0021 §Decision point 5, F09's dep on F07 (deferred) is admin-satisfied; F09's effective dep graph reduces to F08 + P3-W9-F03.",
    "12. NO pytest-mcp Pytest-plugin variant (master-plan §4.8 line 404; P1 priority). That's a separate feature not currently on the Phase 3 active list.",
    "13. NO production-wiring of `McpPytestRunnerG8Source` traits or impls. Those are part of the G8 production-wiring follow-up WO (analogous to G1/G3/G4/G5/G6/G7 production-wiring deferrals per WO-0070/0073/0074/0075/0076 lessons).",
    "14. Effectiveness-gate.sh claude-p sub-session timeout flake (3 open escalations: `20260507T0357Z-effectiveness-nav-rust-symbol-rs-line-flake.md`, `20260507T1629Z-effectiveness-refactor-rename-python-fixture-missing-symbol.md`, `20260507T1930Z-effectiveness-nav-rust-symbol-doctest-caller-flake.md`) — pre-existing standing scope_out, awaiting dedicated harness-improvement WO. WO-0067..WO-0076 carryover, now 10 WOs deep.",
    "15. `scripts/reality-check.sh` pre-existing-stash bug (carries from WO-0067..WO-0076). Workaround unchanged: M1/M2 in-place mutation contract is the authoritative anti-laziness layer; reality-check.sh is informational only.",
    "16. `scripts/verify/coverage-gate.sh` harness improvement (now 33 WOs deep; intermittent — sccache RUSTC_WRAPPER interaction). Continue substantive AC23 coverage check via `env -u RUSTC_WRAPPER cargo llvm-cov` workaround. Bucket B / Bucket D triage candidate."
  ],
  "acceptance": [
    "One stdio MCP plugin manifest exists under `plugins/testing/mcp-pytest-runner/plugin.toml` declaring `testing.*` capability namespace and pinning the PyPI package version for upstream `mcp-pytest-runner@0.2.1`.",
    "One devtools install script exists at `scripts/devtools/install-mcp-pytest-runner.sh` and warms uvx cache for the pinned package version.",
    "One integration-test file `crates/ucil-daemon/tests/g8_plugin_manifests.rs` declares `mod g8_plugin_manifests { ... }` at file root with one `#[tokio::test] async fn` test; substring selector `g8_plugin_manifests::mcp_pytest_runner_manifest_health_check` resolves cleanly via `cargo test -p ucil-daemon --test g8_plugin_manifests <selector>`.",
    "Verify script (P3-W11-F08) replaces the TODO stub with a real implementation exiting 0 on a green run.",
    "Both M1 (transport.command poison) and M2 (expected-tool-name regression) mutation contracts produce SA-tagged panics with the WO-0072/0076 §10/§11 shape; restore-and-md5-confirm protocol verifies independence — one M1 + one M2 mutation applied.",
    "F08 mcp-pytest-runner smoke runs against a tmpdir copy of `tests/fixtures/python-project` and produces ≥1 pytest node ID in canonical `path::function` or `path::ClassName::method` form via the upstream's discover tool, plus a successful selective-run smoke against a SUBSET of discovered IDs per F08 spec line `pytest hierarchical test discovery and selective re-run by node ID`.",
    "All `tokio::test` async fn helpers use `tokio::process::Command` (NOT `std::process::Command`) for fixture-init / tmpdir-mutation operations per WO-0075/0076 W1 lesson — pre-emptive carve-out.",
    "No `crates/ucil-daemon/src/**` source changes. No `tests/fixtures/**` modifications. No new feature-list.json entries. No new ADRs (DEC-0021 is pre-authored by the planner alongside this WO).",
    "All commits Conventional-Commit-formatted with `Phase: 3` + `Feature: P3-W11-F08` + `Work-order: WO-0077` trailers.",
    "ucil-daemon line coverage preserved at >= pre-WO baseline (>= 85% floor per master plan §15.4) measured via the `env -u RUSTC_WRAPPER cargo llvm-cov` workaround per WO-0074/0075/0076 standing protocol.",
    "Live tool-name capture documented in the RFR (Section §\"Live tool name capture\") with the canonical `tools/list` response from the upstream binary at the chosen pinned version.",
    "RFR §\"Lessons applied\" section cites DEC-0021 test-runner-mcp deferral lineage and the upstream-availability sweep (per scope_in #15)."
  ],
  "acceptance_criteria": [
    "test -f plugins/testing/mcp-pytest-runner/plugin.toml",
    "python3 -c 'import tomllib; m=tomllib.load(open(\"plugins/testing/mcp-pytest-runner/plugin.toml\",\"rb\")); assert m[\"plugin\"][\"name\"]==\"mcp-pytest-runner\"; assert m[\"plugin\"][\"category\"]==\"testing\"; assert m[\"transport\"][\"type\"]==\"stdio\"; assert any(c.startswith(\"testing.\") for c in m[\"capabilities\"][\"provides\"])'",
    "! grep -qE '\"(main|latest|master|head|develop|dev|nightly)\"' plugins/testing/mcp-pytest-runner/plugin.toml",
    "test -x scripts/devtools/install-mcp-pytest-runner.sh",
    "bash scripts/devtools/install-mcp-pytest-runner.sh ; test $? -eq 0",
    "test -f crates/ucil-daemon/tests/g8_plugin_manifests.rs",
    "grep -Eq '^[[:space:]]*(pub )?mod g8_plugin_manifests[[:space:]]*\\{' crates/ucil-daemon/tests/g8_plugin_manifests.rs",
    "test $(grep -c '#\\[tokio::test\\]' crates/ucil-daemon/tests/g8_plugin_manifests.rs) -ge 1",
    "! grep -qE 'std::process::Command' crates/ucil-daemon/tests/g8_plugin_manifests.rs",
    "cargo test -p ucil-daemon --test g8_plugin_manifests g8_plugin_manifests::mcp_pytest_runner_manifest_health_check 2>&1 | grep -Eq 'test result: ok\\. 1 passed; 0 failed|1 tests run: 1 passed'",
    "bash scripts/verify/P3-W11-F08.sh ; test $? -eq 0",
    "! grep -qiE 'mock|fake|stub' plugins/testing/mcp-pytest-runner/plugin.toml scripts/devtools/install-mcp-pytest-runner.sh scripts/verify/P3-W11-F08.sh",
    "! grep -qE 'TODO: implement acceptance test for P3-W11-F08' scripts/verify/P3-W11-F08.sh",
    "cargo clippy -p ucil-daemon --tests --all-targets -- -D warnings",
    "cargo fmt --all -- --check",
    "cargo test -p ucil-daemon --no-fail-fast",
    "git diff main..feat/WO-0077-mcp-pytest-runner-plugin-manifest -- crates/ucil-daemon/src/ | wc -l | xargs test 0 -eq",
    "git diff main..feat/WO-0077-mcp-pytest-runner-plugin-manifest -- crates/ucil-core/src/ | wc -l | xargs test 0 -eq",
    "git diff main..feat/WO-0077-mcp-pytest-runner-plugin-manifest -- tests/fixtures/ | wc -l | xargs test 0 -eq",
    "git diff main..feat/WO-0077-mcp-pytest-runner-plugin-manifest -- ucil-build/feature-list.json ucil-build/feature-list.schema.json ucil-master-plan-v2.1-final.md | wc -l | xargs test 0 -eq",
    "git diff main..feat/WO-0077-mcp-pytest-runner-plugin-manifest -- crates/ucil-daemon/tests/g3_plugin_manifests.rs crates/ucil-daemon/tests/g4_plugin_manifests.rs crates/ucil-daemon/tests/g5_plugin_manifests.rs crates/ucil-daemon/tests/g6_plugin_manifests.rs crates/ucil-daemon/tests/g7_plugin_manifests.rs crates/ucil-daemon/tests/plugin_manifests.rs | wc -l | xargs test 0 -eq",
    "git diff main..feat/WO-0077-mcp-pytest-runner-plugin-manifest -- scripts/verify/P3-W11-F07.sh | wc -l | xargs test 0 -eq",
    "env -u RUSTC_WRAPPER cargo llvm-cov --package ucil-daemon --tests --summary-only --json | jq '.data[0].totals.lines.percent' | awk '{ exit !($1+0 >= 85.0) }'",
    "git log feat/WO-0077-mcp-pytest-runner-plugin-manifest ^main --merges | wc -l | xargs test 0 -eq",
    "git log feat/WO-0077-mcp-pytest-runner-plugin-manifest ^main --pretty=format:%B | grep -Eq 'Work-order: WO-0077'",
    "git log feat/WO-0077-mcp-pytest-runner-plugin-manifest ^main --pretty=format:%B | grep -Eq 'Feature: P3-W11-F08'"
  ],
  "forbidden_paths": [
    "ucil-build/feature-list.json",
    "ucil-build/feature-list.schema.json",
    "ucil-master-plan-v2.1-final.md",
    "tests/fixtures/**",
    "scripts/gate/**",
    "scripts/flip-feature.sh",
    "ucil-build/decisions/**",
    "crates/ucil-daemon/src/**",
    "crates/ucil-core/src/**",
    "plugins/architecture/**",
    "plugins/context/**",
    "plugins/knowledge/**",
    "plugins/platform/**",
    "plugins/quality/**",
    "plugins/search/**",
    "plugins/structural/**",
    "crates/ucil-daemon/tests/g3_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g4_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g5_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g6_plugin_manifests.rs",
    "crates/ucil-daemon/tests/g7_plugin_manifests.rs",
    "crates/ucil-daemon/tests/plugin_manifests.rs",
    "scripts/verify/P3-W11-F07.sh",
    "ucil-build/work-orders/0044-ast-grep-probe-plugin-manifests.json",
    "ucil-build/work-orders/0067-classifier-and-reason-parser.json",
    "ucil-build/work-orders/0068-cross-group-executor-and-fusion.json",
    "ucil-build/work-orders/0069-codebase-memory-and-mem0-plugin-manifests.json",
    "ucil-build/work-orders/0070-g3-parallel-merge.json",
    "ucil-build/work-orders/0072-codegraphcontext-plugin-manifest.json",
    "ucil-build/work-orders/0073-g4-architecture-parallel-query.json",
    "ucil-build/work-orders/0074-context7-and-repomix-plugin-manifests.json",
    "ucil-build/work-orders/0075-g6-platform-plugin-manifests.json",
    "ucil-build/work-orders/0076-eslint-and-semgrep-quality-plugin-manifests.json",
    ".githooks/**",
    "ucil-build/decisions/DEC-*.md"
  ],
  "context_refs": [
    "master-plan:§4.8 G8 (Testing+CI) lines 397-410 (test-runner-mcp + mcp-pytest-runner + GitHub Actions + pytest-mcp + Coverage tools — F07 test-runner-mcp deferred per DEC-0021; F08 mcp-pytest-runner is the only G8 plugin with a stable canonical published upstream as of 2026-05-08)",
    "master-plan:§5.8 G8 sources (test-runner-mcp + pytest-runner + GitHub Actions MCP + Coverage tools parallel query)",
    "master-plan:§13 (stdio transport requirement for Phase 3 MCP plugins)",
    "master-plan:§14.1 (manifest categories — `testing` is one of the eight; mentioned at line 1683)",
    "master-plan:§15.4 (coverage floor 85%)",
    "master-plan:§17.2 (plugin manifest template — WO-0044 + WO-0069 + WO-0072 + WO-0074 + WO-0075 + WO-0076 reference shapes)",
    "master-plan:§18 Phase 3 Week 11 line 1820 (G8: Install test-runner-mcp + mcp-pytest-runner — test-runner-mcp deferred per DEC-0021)",
    "decision:DEC-0005 (module-coherence carve-out for >50-LOC plugin.toml + integration-test commits)",
    "decision:DEC-0007 (frozen-selector module-root placement for `mod g<N>_plugin_manifests` integration tests)",
    "decision:DEC-0008 §4 (dependency-inversion seam pattern for future G8 production wiring; out of scope for this WO)",
    "decision:DEC-0019 (defer P3-W9-F10 graphiti to Phase 7 hardening; cited as upstream-availability-driven preemptive-deferral lineage)",
    "decision:DEC-0020 (defer P3-W11-F03 Ruff MCP to Phase 4/7 hardening; direct preemptive-deferral precedent for the DEC-0021 test-runner-mcp deferral pattern)",
    "decision:DEC-0021 (defer P3-W11-F07 test-runner-mcp to Phase 4/7 hardening; this WO's direct authorisation for excluding F07)",
    "precedent:WO-0044 (ast-grep + probe paired-manifest template — original recipe)",
    "precedent:WO-0069 (codebase-memory + mem0 paired-manifest precedent for G3 — paired-manifest shape parent)",
    "precedent:WO-0072 (codegraphcontext solo-manifest precedent for G4 — direct shape parent for the SOLO-FEATURE-WITH-DEFERRED-PEER pattern; M1/M2 mutation contract source)",
    "precedent:WO-0073 (G4 architecture parallel query — `\"For planner: apply same shape to G5..G8\"` lesson)",
    "precedent:WO-0074 (Context7 + Repomix paired-manifest precedent for G5)",
    "precedent:WO-0075 (GitHub + Git + Filesystem tri-manifest precedent for G6 — direct shape parent for the W1 tokio::process::Command lesson + python-deadline-polling lesson + UCIL_SKIP_<GROUP>_PLUGIN_E2E lesson)",
    "precedent:WO-0076 (ESLint + Semgrep paired-manifest precedent for G7 — direct shape parent for the live-tool-name capture protocol + DEC-0020 preemptive-deferral pattern application)",
    "rules:.claude/rules/rust-style.md (clippy::pedantic, tracing crate, no unwrap outside #[cfg(test)], §Async line 23 mandate of tokio::process::Command in async paths)",
    "rules:.claude/rules/commit-style.md (Conventional Commits + Phase/Feature/Work-order trailers)",
    "phase-log:ucil-build/phase-log/03-phase-3/CLAUDE.md §\"Lessons Learned (WO-0076)\" §\"For planner\" + §\"For executor\" + §\"For verifier\" (G7 → G8 paired-manifest template port — ALL lessons applied verbatim including W1 tokio::process::Command pre-emptive carve-out + DEC-0020 preemptive-deferral pattern AND live tools/list capture-at-pinned-version protocol)",
    "phase-log:ucil-build/phase-log/03-phase-3/CLAUDE.md §\"Lessons Learned (WO-0075)\" §\"For planner\" (G6 → G7/G8 paired-manifest template port; W1 lesson originator)",
    "phase-log:ucil-build/phase-log/03-phase-3/CLAUDE.md §\"Lessons Learned (WO-0074)\" §\"For planner\" (G5 → G6/G7/G8 paired-manifest template port; live tool-name capture protocol; UCIL_SKIP_<GROUP>_PLUGIN_E2E opt-out)",
    "phase-log:ucil-build/phase-log/03-phase-3/CLAUDE.md §\"Lessons Learned (WO-0072)\" §\"For planner\" (M1/M2 mutation contract shape; transport.command poison + expected-tool-name regression; SOLO-FEATURE WO shape)",
    "phase-log:ucil-build/phase-log/03-phase-3/CLAUDE.md §\"Lessons Learned (WO-0069)\" §\"For planner\" (paired-manifest module-root placement; substring selector resolves cleanly)",
    "upstream:https://pypi.org/project/mcp-pytest-runner/0.2.1/ (mcp-pytest-runner v0.2.1 on PyPI — pre-flight HTTP 200; versions ['0.1.0', '0.2.0', '0.2.1']; summary 'MCP server providing opinionated pytest execution interface for AI agents'; install via `uvx mcp-pytest-runner` per upstream README; stdio transport per upstream README's Claude Code MCP configuration section)",
    "upstream-pre-flight:DEC-0021 §Context (pypi-404 + npm-search + GitHub-search triad confirming no canonical test-runner-mcp server published — F07 deferred)"
  ],
  "dependencies_met": true,
  "dependencies_audit": {
    "P2-W6-F01": {"passes": true, "rationale": "F01 (PluginManager + plugin lifecycle) ships per WO-0010 + WO-0042. F08 consumes `PluginManager::health_check_with_timeout` from g8_plugin_manifests.rs integration tests in the same shape as g3/g4/g5/g6/g7_plugin_manifests.rs from WO-0069/0072/0074/0075/0076."}
  },
  "estimated_commits": 5,
  "estimated_complexity": "low",
  "lessons_applied_summary": [
    "WO-0076 §planner: G7 → G8 paired-manifest template port — direct shape parent, scaled-down from 2 to 1 feature (test-runner-mcp deferred per DEC-0021); WO is in the SOLO-FEATURE shape per WO-0072 codegraphcontext G4 precedent",
    "WO-0076 §planner: DEC-0020 preemptive-deferral pattern (PyPI-404 + npm-search + GitHub-search triad) APPLIED via DEC-0021 — chain DEC-0019 → DEC-0020 → DEC-0021 establishes the upstream-availability-driven deferral convention",
    "WO-0076 §planner: live tools/list MUST be captured AT THE SPECIFIC PINNED VERSION before sealing manifest `[capabilities] provides` list — scope_in #1 + #14 prescribe the live-capture protocol verbatim",
    "WO-0075/0076 §executor W1: tokio::process::Command in async test helpers — PRE-EMPTIVELY APPLIED in scope_in #3/#7 + AC enforcement (`! grep -qE 'std::process::Command' crates/ucil-daemon/tests/g8_plugin_manifests.rs`) to avoid the WO-0075/0076 W1 critic warning entirely",
    "WO-0075 §planner: G6+ keep separate g<N>_plugin_manifests.rs peer file (NOT consolidated) — applied to g8_plugin_manifests.rs (peer of g3/g4/g5/g6/g7_plugin_manifests.rs)",
    "WO-0074 §executor #1: DON'T translate live tools/list names to snake_case when they ship kebab-case — capture canonical names from live binary BEFORE writing assertion literals (scope_in #14 Live-tool-name capture protocol)",
    "WO-0074 §executor #2: DON'T use --mcp as a devtools warm-up flag — use --help (scope_in #2 install script)",
    "WO-0074 §executor #3: document UCIL_SKIP_TESTING_PLUGIN_E2E in test rustdoc + verify script (scope_in #3/#4)",
    "WO-0074/0076 §executor #4: prefer python polling with deadline over bash sleep for tools/call wall-time-sensitive smokes (scope_in #4: 90-second deadline for pytest discovery)",
    "WO-0074/0075/0076 §executor #5: DO copy tests/fixtures/<project> into a mktemp -d tmpdir BEFORE invoking — required for F08 (python-project) (scope_in #4/#13)",
    "WO-0074/0075/0076 §verifier: ensure UCIL_SKIP_EXTERNAL_PLUGIN_TESTS and UCIL_SKIP_TESTING_PLUGIN_E2E are NOT exported (scope_in #10)",
    "WO-0072 §planner: For external-plugin manifests with persistent operator-state config files, prescribe an M3 mutation that the operator-state file cannot rescue — N/A here (mcp-pytest-runner does NOT create persistent operator-state config files in the operator's HOME; uvx-managed venv is ephemeral per uvx's contract); M1+M2 are sufficient",
    "WO-0072 §planner: SOLO-FEATURE WO shape (single feature, paired-deferred-peer pattern) — direct shape parent for this WO; F07 deferred via DEC-0021 mirrors F10 deferred via DEC-0019 in WO-0072",
    "WO-0069 §planner: paired-manifest WO with API-key-presence short-circuit gate — N/A as load-bearing here (mcp-pytest-runner requires no auth for the basic-discover smoke)",
    "WO-0067/0068/0069/0070/0072/0073/0074/0075/0076: pre-bake M1/M2 mutation contract in scope_in #5/#6 + acceptance_criteria with file/line/restore command for verifier",
    "WO-0067/0068/0069/0070: DEC-0007 SA-numbered panic-body format for mutation-diagnosis trivia (scope_in #3)",
    "WO-0067..WO-0076: substantive AC23 coverage standing protocol (env -u RUSTC_WRAPPER cargo llvm-cov) — scope_in #8, AC23",
    "WO-0067..WO-0076: AC30/AC31 phase-1/phase-2 effectiveness-gate flake carry-over with 3 standing escalations (scope_out #14)",
    "WO-0070..WO-0076: AC25 reworded to git log feat ^main --merges = 0 — workflow-timing tolerant (acceptance_criteria)",
    "DEC-0021 NEW: P3-W11-F07 test-runner-mcp deferred to Phase 4/7 pending upstream — pre-flight upstream-availability sweep (PyPI-404 + npm-search + GitHub-search triad) authored alongside this WO; RFR §\"Lessons applied\" MUST cite DEC-0021 lineage AND the chain DEC-0019 → DEC-0020 → DEC-0021"
  ],
  "created_at": "2026-05-08T09:30:00Z",
  "created_by": "planner"
}
