//! UCIL integration-test harness crate — binaries live at `tests/integration/test_*.rs`.
//! No library API is exposed; this crate exists only to carry `[[test]]` entries per DEC-0010.

#![deny(warnings)]
#![warn(clippy::all, clippy::pedantic, clippy::nursery)]
